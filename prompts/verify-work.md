# Verify the current work

Verify the current work for the scope below.

- Inspect the actual diff and determine the smallest checks that cover the acceptance conditions.
- Run those checks and direct behavioral probes; include exact commands and real outcomes.
- Investigate failures to root cause instead of rerunning unchanged commands.
- Flag untested paths, environment-dependent results, and remaining risks.
- Do not modify files or external state unless the request explicitly asks for a fix.

Scope:
$ARGUMENTS
