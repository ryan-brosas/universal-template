# Qualify external inspiration

Find or assess ONE external GitHub implementation that can close a named question in the active project. Codebase Memory is the approved external source map; direct source and tests prove claims. The process is manual, evidence-led, and stops when the question is answered.

Explicitly load `inspo-qualify` from the installed skill catalog and follow its workflow:

1. Derive the named question from the active project's source and tests. If only the command is given, derive the most useful unresolved question yourself instead of starting a questionnaire.
2. Check existing Codebase Memory projects and Git identity first. Reuse an equivalent approved source when it closes the question.
3. Stop discovery when a missing current-project question, duplicate inspiration identity, actively owned project, or missing recorded OSI-recognized license (or NO-LICENSE pattern-only obligation) blocks it.
4. Read-only reuse checks, identity checks, and candidate discovery may precede approval. Before any clone or index action, present one in-chat record (project need, candidate with exact commit, evidence, counter-evidence, decision) and obtain explicit approval for that exact candidate and pin.
5. Only after approval, clone the resolved commit, index it, and prove every claim against direct source and tests.

Explicit approval is a separate gate before cloning or indexing the selected commit. Read-only discovery does not grant permission for either operation. Report the delegated question, candidate, evidence, and decision, then stop.

Request:
$ARGUMENTS
