<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->

# AI SDK Foundation

## Use this for
Building multi-provider LLM libraries, agent step loops, approval workflows, structured-output layers, or schema plumbing behind one synchronous seam. Vercel AI SDK source and direct tests are ground truth; references carry decisive excerpts and graph retrieval.

## Load the matching source dump
- `./streaming.md` — how do you bound, smooth, stitch, and terminate a multi-step streaming loop without wedging consumers?
- `./tool-calls.md` — how do you parse, repair, execute, and prune tool traffic so nothing is dropped and no history reference is orphaned?
- `./provider-interface.md` — how do you publish one spec-versioned provider surface that dozens of packages implement without breaking deployed ones?
- `./generate-text-orchestrator.md` — when does a non-streaming agent loop take another step, and what executes before the first model call?
- `./output-specification.md` — how does one `output` spec become provider responseFormat plus complete/partial/element parsing without leaking the wire envelope?
- `./response-messages.md` — how do step content parts become exactly one assistant message plus one tool message every provider accepts back?
- `./approval-round-trip.md` — how are approvals from prior runs collected from client history and re-validated server-side before execution?
- `./tool-fingerprinting.md` — how do you fingerprint tools to catch definition drift ("rug pull") without hashing function identity?
- `./simulate-streaming.md` — how do you fake a stream from a generate call so stream consumers cannot tell the difference?
- `./extract-reasoning.md` — how do you turn XML-tagged reasoning inside a token stream into reasoning parts across chunk boundaries?
- `./tool-input-examples.md` — how do you give example tool inputs to providers whose wire format has no native examples field?
- `./flexible-schemas.md` — how do Zod v3/v4, Standard Schema, JSON Schema, and lazy factories collapse into one deferred-conversion Schema interface?
- `./stream-object-pipeline.md` — how did the legacy `streamObject` turn accumulating text into a monotone partial-object stream with per-strategy validation?
- `./fix-json-partial-parse.md` — how do you repair truncated JSON in one linear pass and classify every parse as exact vs repaired?
- `./final-validation-repair.md` — how do you validate a complete structured response once, with one repair attempt, failing with full response context?
- `./json-instruction-injection.md` — how do you force JSON output from providers without native responseFormat without corrupting an existing system message?
- `./lazy-result-primitives.md` — which micro-contracts keep lazily-awaited result properties rejection-safe and partial streams dedup-quiet?
- `./event-ledger.md` — how do you reduce an open-ended chunk stream into per-step content snapshots that survive out-of-order and adversarial ids?
- `./enriched-stream-publisher.md` — how do you attach streaming parse results to a pass-through chunk stream without dropping or duplicating text?
- `./stream-continuation-engine.md` — when does a finished step chain into the next model call, and how do deferred provider results keep the loop alive?
- `./tool-loop-agent.md` — how do you wrap generateText/streamText into an agent class without breaking callback layering, defaults, or attribution?
- `./output-interface-vs-strategies.md` — which structured-output abstraction should a porter take, and what does each mode's parse contract demand?
- `./retry-kernel.md` — how do you retry failed LLM calls with backoff while honoring server rate-limit headers without retrying the unrepeatable?
- `./event-ingestion-and-tool-names.md` — how do you consume SSE-framed model events and reconcile renamed tools across the wire boundary?
- `./result-promises-and-approval-replay.md` — how do approved/denied tool calls from a prior run execute before step 0 so the model sees a consistent history?
- `./tool-call-parse-and-repair-ladder.md` — how does a raw model tool call become a validated typed call, and what exactly happens on each failure?
- `./tool-execution-lifecycle.md` — how does a single tool call run with callbacks, streaming outputs, timeouts, and error capture without any throw escaping?
- `./tool-timeout-resolution.md` — which timeout value reaches a tool's abort signal, and what does the number form mean?
- `./abort-signal-merging.md` — how do user aborts and timeout timers become one signal without inventing timers that were never configured?
- `./request-tool-assembly.md` — how does the user's tool map become the ordered provider tool array, including description resolution and partial ordering?
- `./prototype-safe-lookup.md` — why must every model-controlled name resolve through an own-property read?
- `./swallowing-callback-bus.md` — how do you fan one event out to zero-or-many callbacks without letting a user callback kill the run?
- `./async-iterable-stream-duality.md` — how does one object serve both ReadableStream consumers and for-await loops with correct teardown on every exit path?
- `./background-stream-drain.md` — how do you guarantee a result stream is consumed even when the user never awaits it, without surfacing duplicate errors?
- `./http-transport-verbs.md` — how do POST/GET transport verbs normalize every failure into APICallError while aborts and retryability survive every layer?
- `./ssrf-download-guard.md` — how do you fetch model-supplied URLs so no redirect hop or DNS rebinding ever reaches the internal network?
- `./response-handler-factory.md` — how does one handler-factory signature serve JSON, SSE streams, binary payloads, and bare-status errors with degrade-never-throw error parsing?
- `./response-body-size-limit.md` — how do you read a response body under a byte ceiling so a lying Content-Length or huge download becomes a catchable error instead of an OOM kill?
- `./header-sanitization-origin-trust.md` — which headers must be stripped before fetching untrusted URLs, and when may credentials ride along?
- `./provider-toolcall-tracker.md` — how do OpenAI-compatible argument deltas become complete tool calls without acting on a parsable prefix?
- `./experimental-tool-callers.md` — how can one tool stay executable but invisible to the model, routed through local or provider callers?
- `./model-call-stream-normalizer.md` — how does one TransformStream turn provider V4 parts into user-facing parts while resolving approvals and computing performance stats?
- `./prompt-standardization-conversion.md` — how do prompt inputs become validated, tool-result-complete, provider-shaped messages?
- `./micro-utility-kernels.md` — what do SerialJobExecutor, setAbortTimeout, mergeObjects, simulateReadableStream, and handleFetchError pin that porters silently get wrong?
- `./download-asset-ladder.md` — what does a single URL download owe its caller in error classification, size limiting, and socket hygiene?
- `./model-url-passthrough-policy.md` — when should an asset be fetched by the SDK versus passed through to the model as a URL?
- `./file-part-data-normalization.md` — how do legacy and tagged file-part shapes collapse into one v4 shape without misclassifying a data URL?
- `./data-content-codec.md` — when does string content mean base64 versus raw bytes, and how do binary variants normalize?
- `./tool-result-output-shaping.md` — how does an arbitrary tool output become a typed model-facing result, and why does error mode bypass toModelOutput?
- `./call-options-validation-gate.md` — which model call options are validated eagerly, and what shape does the normalized object keep?
- `./provider-tool-name-mapping.md` — how do provider-executed tools keep their client-side names when the provider renames them on the wire?
- `./magic-byte-media-sniffing.md` — how do you detect a file's IANA type from bytes or base64 without decoding the whole input, and why is audio/mp4 deliberately excluded from generic detection?
- `./websocket-transport-seam.md` — how does a provider-agnostic WebSocket layer keep message order across async decoding while never throwing from its own callbacks?
- `./multipart-form-encoding.md` — how do typed request bodies flatten to FormData with array-key conventions both API styles accept?
- `./header-default-fallback.md` — why does header merging prefer caller values over defaults, and what are the failure modes of the legacy data-URL text decoder?
- `./react-external-store-bridge.md` — how does a mutable class Chat store feed useSyncExternalStore without stale closures, missed subscriptions, or tearing on chat switch?
- `./chat-request-lifecycle.md` — how does AbstractChat serialize concurrent mutations, resume streams, and keep onFinish total across abort/error/success?
- `./ui-chunk-reducer.md` — how does a UIMessageChunk stream fold into an assistant message without duplicating parts or losing cross-step tool calls?
- `./ui-to-model-conversion.md` — how does a client UIMessage history become provider-valid ModelMessages with exactly one outcome per tool call?
- `./http-chat-transport.md` — how does the chat wire layer resolve async options, merge header layers, and answer "is there a stream to resume?"
- `./swr-use-object.md` — how do partial objects reach React cheaply during streaming, and where does schema validation actually run?
- `./realtime-hook-store.md` — how does a realtime audio session object become React state with bounded event history and identity-keyed recreation?
- `./streamable-value-protocol.md` — how does a server value stream updates to the client as a promise chain of snapshots with string patches?
- `./streamable-ui-suspense-ladder.md` — how do React nodes stream over RSC as a recursive Suspense chain, and what do append/done/error mean on the wire?
- `./ai-state-delta-sync.md` — how does server AI state flow to the client as jsondiffpatch deltas under AsyncLocalStorage?
- `./resolvable-promise-kernel.md` — what externally-resolvable-promise kernel do both RSC streamables share, and how do they warn about forgotten `.done()`?
- `./use-completion-legacy.md` — what remains when the chat plane generalizes down to plain text streaming?
- `./filelist-ingestion.md` — how do browser file picks become FileUIParts, and why must the conversion be cancellable before the request fires?
- `./ui-tool-invocation-state-union.md` — which fields exist in which tool-invocation state, and how does the discriminated union make wrong-state fields unrepresentable?
- `./ui-message-validation-ladder.md` — when does UI message validation deliberately NOT re-validate tool input, and why can terminal calls survive a missing tool schema?
- `./last-step-completion-predicates.md` — what exact state of the last assistant step authorizes auto-continuation for client tools vs approval responses?
- `./direct-in-process-transport.md` — how does a ChatTransport run the full chat stack in-process with zero HTTP, and why does an un-resumable transport return null?
- `./text-stream-ui-adapters.md` — how does a bare token stream masquerade as a UIMessageChunk stream without breaking the reducer?
- `./wire-dialect-registry.md` — which transport subclasses exist, and what does the ChatTransport interface owe its implementors?
- `./ui-stream-writer-kernel.md` — how does a server keep a chunk stream open for merges that arrive AFTER execute() returns, and how do errors cross it?
- `./ui-stream-wire-contract.md` — which chunk shapes cross the SSE boundary, and why does the schema tolerate unknown FIELDS but not unknown TYPES?
- `./ui-part-to-chunk-conversion.md` — how does the model-event stream become wire chunks, and where do undefined outputs and provider errors get special treatment?
- `./ui-stream-metadata-adapter.md` — where does per-part metadata ride, and why do start/finish get it inline while every other part gets a second chunk?
- `./ui-stream-persistence-ladder.md` — how does the SERVER run the client reducer over its own outgoing stream to fire onEnd/onStepEnd exactly once, even when the browser walks away?
- `./ui-stream-snapshot-reader.md` — how does a consumer turn UIMessageChunks into per-write message states without cloning megabytes of accumulated text?
- `./ui-stream-response-family.md` — what is the exact byte shape on the wire, which headers are contract, and how do you tee the encoded stream for logging without blocking the response?
- `./ui-agent-http-entry.md` — what does the server do between receiving UI messages and returning the SSE stream, and in what order?
- `./provider-registry.md` — how do `"provider:model"` composite ids resolve across seven model types without letting a missing separator masquerade as a provider id?
- `./custom-provider-fallback.md` — how do you compose a static model map with a fallback provider without changing failure semantics?
- `./extract-json-stream-guard.md` — how do you strip a markdown fence from a STREAM when the closing ``` may arrive split across deltas?
- `./default-injection-middlewares.md` — how does the SDK inject default settings and a default system prompt WITHOUT overriding user choices?
- `./embed-batching-parallelism.md` — how does embedMany split a large values array across model calls without ever misaligning embeddings to inputs?
- `./rerank-orchestration-contract.md` — how do you rerank documents against a query and hydrate results back to the caller's originals without off-by-one or type loss?
- `./model-resolution-versioning.md` — where does `embed('my-model')` actually resolve, and how do v2/v3 models become v4 without a copy?
- `./embedding-middleware-wrapper.md` — how do transformParams / wrapEmbed compose, and which override hooks can silently change batching behavior?
- `./warning-pipeline.md` — how do provider warnings reach stderr once, in what format, and how does the AI_SDK_LOG_WARNINGS global suppress or replace them?
- `./retrieval-operation-lifecycle.md` — what is the shared callback/telemetry/retry skeleton that embed, embedMany, and rerank all execute?
- `./gateway-provider-factory.md` — how does one `createGateway()` closure serve eight model modalities plus billing endpoints without duplicating config, and which members are aliases vs distinct factories?
- `./gateway-auth-ladder.md` — when is the credential an API key vs a Vercel OIDC token, and why must the chosen method ride every request as a header?
- `./gateway-metadata-cache.md` — why does the stale-while-revalidate model cache bump its staleness clock at REQUEST START and leave failed fetches rejected until TTL?
- `./gateway-error-plane.md` — how does any failure (HTTP, undici timeout, unknown throw) become exactly ONE typed GatewayError without double wrapping, and why Symbol.for markers?
- `./gateway-wire-identity-headers.md` — why do ALL language models POST to one `/language-model` endpoint with the model id in a header instead of the URL or body?
- `./gateway-file-part-base64.md` — where do binary file parts get base64-encoded for JSON transport, and which three part kinds must the walk touch?
- `./gateway-client-secret-mint.md` — how does a browser get a short-lived WebSocket credential without ever seeing the long-lived gateway key, and why is the guard on minting not model construction?
- `./gateway-subprotocol-auth.md` — how does a bearer token cross a WebSocket handshake that cannot carry headers, and why is only the team scope encoded?
- `./gateway-realtime-identity-codec.md` — what does a realtime "model" class own when the gateway normalizes every event server-side?
- `./gateway-transcription-http-path.md` — why does doGenerate base64 audio into JSON while doStream derives subprotocols from resolved headers, and why is authMethod parsed BEFORE the stream starts?
- `./gateway-transcription-ws-pump.md` — how does a duplex audio pump over one socket survive error parts, aborts, and close-before-finish with exactly-once cleanup?
- `./gateway-observability-gets.md` — why do credits/spend/generation-info hit the ORIGIN while inference rides the baseURL prefix, and what does the snake_case→camelCase transform boundary own?
- `./gateway-catalog-type-filter.md` — why does the model-config endpoint silently DROP unknown modelType entries inside the zod schema?
- `./gateway-provider-executed-tools.md` — how does a provider ship a server-side search tool with zero local execute function, and why are errors DATA not throws?
- `./gateway-o11y-header-split.md` — why are deployment coordinates snapshotted once at creation but the request id re-read per call?
- `./gateway-open-provider-options.md` — why is the gateway routing-options type an open record validated service-side rather than a closed union?
- `./gateway-zod-shim-workflow-serialize.md` — why import eleven named zod factories instead of the namespace, and what do static WORKFLOW symbols buy?
- `./gateway-modality-family.md` — what do the embedding/image/video/reranking/speech twins share byte-for-byte with the language model, and where do they deliberately differ?
- `./tool-execution-safety-gate.md` — which model finish reasons may legally trigger tool execution, and where does that check have to sit?
- `./stream-tool-buffering.md` — how are approved tool calls buffered to the model-call-end boundary and drained in parallel?
- `./callback-containment-streaming.md` — what happens when a user's onChunk/onError throws mid-stream?
- `./part-id-crossstep-remap.md` — how do provider content-block ids stay unique across a multi-step stream?
- `./stream-object-terminal-latch.md` — when an error chunk and a later finish chunk disagree, which wins?
- `./preliminary-output-suppression.md` — when does a delivered tool output stay out of model history?
- `./video-async-lifecycle.md` — how does fire-and-forget video generation stay idempotent and resumable across processes?
- `./video-webhook-arbitration.md` — who decides whether an async generation completes by webhook or polling?
- `./gateway-batch-trio.md` — how do durable multi-request batches stay replay-safe and abort-honest?
- `./mcp-parameter-headers.md` — how do tool arguments become HTTP headers without injection?
- `./mcp-era-discovery.md` — how does one client serve both 2025-11-25 and 2026-07-28 servers?
- `./mcp-http-era-split.md` — which session/SSE features survive in the modern streamable-HTTP transport?
- `./workflow-resume-index-spaces.md` — why does a UI cursor crash when applied to a raw model stream?
- `./ui-stream-frame-repair.md` — how does a durable shared stream survive duplicated or orphaned part chunks?
- `./rsc-concurrent-state-writes.md` — why did keyed setters and delta patches drop sibling writes?
- `./harness-builtin-replay-split.md` — why did an approved builtin tool's real result vanish from the continued stream?
- `./empty-toolcall-id-coercion.md` — why must tool-call id fallback use falsy not nullish coalescing?
- `./bedrock-eventstream-framing.md` — how do length-prefixed binary frames become typed events without silent loss?
- `./schema-additionalproperties-preservation.md` — when is `additionalProperties: false` the WRONG strictness?
- `./mcp-app-bridge-init-gate.md` — when may the host push notifications into an untrusted iframe?
- `./mcp-app-tool-allowlist.md` — how does an untrusted iframe get scoped access to host tools?
- `./mcp-app-request-validation-ladder.md` — what can an iframe safely ask the host to do?
- `./mcp-app-csp-sanitizer.md` — how do untrusted server CSP domains become a safe Content-Security-Policy?
- `./mcp-app-permission-intersection.md` — how do server-requested iframe capabilities get granted?
- `./mcp-app-frame-lifecycle.md` — how does the React host wire, bootstrap, and tear down the sandbox proxy?
- `./mcp-app-tool-part-extraction.md` — how is app metadata read from a UI tool part, and when is the app kept alive?
- `./mcp-app-server-resource-contract.md` — how does @ai-sdk/mcp normalize app resources, metadata, and visibility?
- `./mcp-app-resource-fingerprint.md` — how do you detect a changed app resource without a shared hash utility?
- `./realtime-session-skeleton.md` — how does an experimental browser voice session bootstrap and route events?
- `./realtime-tool-gating.md` — when does a voice session request the follow-up response after tool calls?
- `./realtime-barge-in.md` — how is model audio stopped and truncated when the user interrupts?
- `./realtime-audio-codec-kernel.md` — how do Float32 samples become wire PCM16 and scheduled playback?
- `./realtime-websocket-transport.md` — how are sends ordered, stale sockets discarded, and health checks answered?
- `./realtime-event-reducer.md` — how do server events become UIMessage parts without message churn?
- `./realtime-tool-definitions.md` — how are SDK tools serialized for a realtime session config?
- `./code-mode-execution-policy-byte-expansion.md` — how do friendly executionPolicy byte caps become runner limits without rejecting valid programs?
- `./code-mode-proxy-bindings-source-wrapping.md` — how does sandboxed code call host tools it can't see, including non-identifier names?
- `./code-mode-interrupt-resume-ladder.md` — how does a sandbox pause mid-program, collect a human decision, and replay without side effects?
- `./code-mode-signed-continuation-capability.md` — what makes a continuation token safe to hand to an untrusted client?
- `./code-mode-approval-funnel.md` — how does a needsApproval tool get gated, interrupted, or denied inside sandboxed code?
- `./code-mode-host-tool-invocation-contract.md` — what must hold for every host tool call made from the sandbox?
- `./code-mode-tool-prompt-compiler.md` — how is the `tools.*` API surface compiled into the description the model reads?
- `./code-mode-error-taxonomy.md` — how do typed errors survive the worker bridge with actionable stacks?
- `./code-mode-json-boundary-kernel.md` — how do values cross the sandbox↔host bridge and what JSON semantics apply at each edge?
- `./code-mode-generic-interrupts.md` — how do non-approval interruptions work and how are they found in arbitrary result shapes?
- `./code-mode-tool-caller-late-binding.md` — how does one code_mode tool gain access to the other tools in the same generation call?
- `./code-mode-sandbox-semantics.md` — what execution semantics can a generated program assume?
- `./code-mode-outer-tool-call-identity.md` — how are outer calls, inner host calls, and interrupts correlated by id?
- `./code-mode-approval-message-roundtrip.md` — how do approval interrupts become message parts and recover human decisions from history?
- `./code-mode-interrupt-payload-protocol.md` — how are interruption payloads typed, validated, and discriminated?
- `./code-mode-package-boundary.md` — what does @ai-sdk/code-mode export and where is the VM kernel boundary?
- `./code-mode-behavior-test-battery.md` — which tests pin the sandbox, approval, and resume contracts?
- `./otel-call-state-machine.md` — how does a telemetry integration reconstruct a span tree from flat per-part events?
- `./otel-lazy-attribute-specs.md` — how do you gate recordInputs/recordOutputs without paying serialization cost?
- `./otel-sanitizer-kernel.md` — why do OTel arrays reject mixed types and non-finite numbers, and how are they pre-dropped?
- `./otel-semconv-formatters.md` — how do AI SDK messages become OpenTelemetry GenAI semantic-convention payloads?
- `./otel-provider-tool-synthesis.md` — how are provider-executed tool spans reconstructed when no execution events fire?
- `./otel-supplemental-enrich-span.md` — how do opt-in extras extend standardized spans without breaking SemConv backends?
- `./otel-abort-error-drain.md` — how do half-built span trees terminate exactly once when a call dies mid-flight?
- `./otel-legacy-plane-boundary.md` — what differs between LegacyOpenTelemetry (ai.*) and the modern gen_ai.* plane?
- `./otel-test-doubles.md` — how do MockTracer/noopTracer and InMemorySpanExporter make span behavior deterministic in tests?
- `./otel-span-taxonomy.md` — which span types, names, SpanKinds, and performance attributes does a GenAI tracer emit?
- `./harness-agent-stateless-session-split.md` — where does agent-run state live when one definition drives many third-party runtimes through explicit sessions?
- `./harness-session-dual-state-machine.md` — how do session-lifecycle and turn-progress axes stay orthogonal so late callbacks cannot corrupt reuse gating?
- `./harness-turn-suspend-resume-payloads.md` — what envelope, validation, and nesting rules let an unfinished turn cross a process boundary without losing pendings?
- `./harness-approval-continuation-recovery.md` — how does an id-only approval response get reattached to its original tool call, and which responses are skipped as consumed?
- `./harness-host-tool-execution-join.md` — where do out-of-band host tool executions join step boundaries, and how do generator executes surface preliminary results?
- `./harness-stream-abort-settle-contract.md` — how does a user abort become one terminal part while adapter rejections become stream events, never both?
- `./harness-bootstrap-marker-idempotency.md` — what content-addressed marker makes sandbox bootstrap idempotent and self-invalidating without locks?
- `./harness-bridge-claim-stream-ownership.md` — when many hosts hold sockets to one runtime process, who receives the events?
- `./harness-bridge-monotonic-seq-replay-log.md` — how do per-turn memory bounds coexist with a client cursor that outlives turns?
- `./harness-bridge-disk-replay-recovery.md` — how does a respawned runtime process serve a resume cursor for a turn it never saw?
- `./harness-channel-reconnect-resume-ladder.md` — how does a mid-turn socket drop become invisible to the consumer?
- `./harness-channel-suspend-cursor-freeze.md` — how does a host process exit mid-turn without losing or duplicating the tail?
- `./harness-channel-event-checkpoint-pin.md` — how can a consumer suspend AT an already-dispatched boundary event?
- `./harness-bridge-user-message-steering.md` — how do steering retries across reconnects stay exactly-once?
- `./harness-result-streamtext-twin.md` — how does a push-driven third-party runtime expose streamText's exact consumer surface?
- `./harness-result-driver-writer-surface.md` — which pushed parts become step content, which stay continuation-only, when is replay discarded?
- `./harness-result-finish-gate-and-steps.md` — when is a terminal finish a protocol violation, and how do steps aggregate into turn answers?
- `./harness-result-start-part-contract.md` — why must a synthesized result stream open with `{type:'start'}` before anything else?
- `./harness-result-output-transform-lock.md` — how do partial structured-output parses ride a text stream on exactly one text id?
- `./harness-turn-telemetry-lifecycle-shim.md` — how does a non-streamText turn fire the Telemetry lifecycle @ai-sdk/otel requires?
- `./harness-usage-normalization-nested-flat.md` — how do nested V4 token counters flatten without NaN-poisoning totals?
- `./harness-acp-respawn-strategy-ladder.md` — how does a host reattach to a third-party agent process it can no longer see: live attach, disk replay, lossy rerun, or cold restore?
- `./harness-acp-lifecycle-compatibility-gate.md` — which four identities must match before persisted recovery state may be reused?
- `./harness-acp-turn-start-config-fingerprint.md` — how do you prove a persisted non-secret config still matches current settings without ever storing secrets?
- `./harness-acp-cold-restoration-negotiation.md` — how do you negotiate resume-vs-load with a replacement agent and keep historical updates out of the new stream?
- `./harness-acp-recovered-session-update-queue.md` — how do semantic updates and the prompt stop share one buffered queue so stale errors never kill the next prompt?
- `./harness-acp-wire-turn-event-pump.md` — how do protocol events become stream parts whose blocks always close, even when the transport dies mid-block?
- `./harness-acp-stream-translator-inference.md` — how do you manufacture step/block structure for a protocol that only emits flat session updates?
- `./harness-acp-host-tool-loopback-relay.md` — what does a bridge owe an untrusted sandbox agent that must list and call host tools over stdio MCP?
- `./harness-acp-host-tool-correlation-window.md` — when one invocation produces both a relay-side emission and the agent's own announcement, which evidence justifies dropping the echo?
- `./harness-acp-permission-plane.md` — who answers a sandbox agent's permission requests from host state, and which requests must fail closed?
- `./harness-acp-credential-brokering-twins.md` — how do provider credentials stay real on the host while the sandbox receives structurally-equivalent placeholders?
- `./harness-acp-bridge-session-services.md` — which per-session host services does a bridge owe an agent, and how do they degrade when the agent SDK lags the protocol?
- `./harness-dialect-wireturn-invariant-core.md` — what is the per-turn pump contract every runtime dialect replicates, and which knobs may vary between dialects?
- `./harness-opencode-compaction-rail.md` — how do compaction events that happen outside any active turn reach the next turn's consumer in order?
- `./harness-codex-cli-shim-host-tools.md` — how do you expose host tools to a runtime whose only execution surface is a bash tool?
- `./harness-claude-code-websocket-hello-ladder.md` — how do you complete a bridge handshake against a slow sandbox without missing a hello that lands immediately after open?
- `./harness-deepagents-instructions-once.md` — when reattaching to a live agent process, how do you know whether session instructions must be resent?
- `./harness-cline-inprocess-parked-sessions.md` — when the runtime is an in-process library with no bridge, which lifecycle methods can still continue a live turn and what must degrade to lossy rerun?
- `./harness-cline-sandbox-remote-ops.md` — how do you keep path containment sound against traversal, symlinks, and not-yet-existing writes when every file op crosses the sandbox boundary as a shell command?
- `./harness-pi-workspace-mirror-vfs.md` — how do you let an in-process runtime's own fs-based resource loading see a sandboxed workspace without copying the whole project to the host?
- `./harness-pi-dangling-host-tool-rerun-barrier.md` — how do you resume a journal that ends with host tool calls still awaiting results without racing the framework's re-delivery?
- `./workflow-harness-durable-step-engine.md` — how do you drive one long agent turn across workflow step boundaries so each execution is resumable and the UI stream stays coherent?
- `./harness-codex-bridge-cli-relay-shim.md` — when the runtime's MCP registration is broken upstream, how do you make the model call host tools through its own bash tool?
- `./harness-codex-bridge-step-tracker.md` — how do you manufacture harness steps and text parts from a runtime that streams items, not steps?
- `./harness-dialect-tool-relay-plane.md` — how do you gate a model-readable loopback endpoint to host tools without any credential?
- `./harness-claude-code-compaction-latch.md` — how do you merge a two-part, unordered runtime notification into one well-formed event?
- `./harness-opencode-bridge-turn-settlement.md` — how do you settle a turn against an event stream that can end early, while steering messages keep arriving, without fabricating a response?
- `./harness-opencode-bridge-context-fallback.md` — when the event stream settles without streamed assistant content, how do you recover the response from the runtime's own store without emitting a stale message?
- `./harness-opencode-bridge-permission-plane.md` — how do you answer a sandboxed agent's native permission prompts from host policy without letting it touch files outside the workspace?
- `./harness-opencode-bridge-event-envelope.md` — how do you normalize a runtime's evolving event wire format into one translation state that keeps text, reasoning, and tool parts well-formed?
- `./harness-opencode-bridge-usage-accounting.md` — how do you compute honest per-turn usage from a runtime that reports cumulative session totals and per-step tokens in different shapes?
- `./harness-claude-code-bridge-turn-pump.md` — when the runtime is an in-process SDK with a pull-based prompt channel, how do you drive it while reconciling host steering against what the runtime actually did?
- `./harness-claude-code-bridge-emit-translator.md` — how do you manufacture well-formed steps and blocks from an SDK that streams content-block indices, not stable part ids?
- `./harness-dialect-json-schema-zod-conversion.md` — how do you convert host-provided JSON Schemas into runtime validators for SDKs that demand Zod without ever throwing on an inexpressible schema?
- `./harness-deepagents-bridge-hitl-loop.md` — how do you drive a graph agent whose human-in-the-loop pauses arrive as state interrupts, not stream events?
- `./harness-opencode-bridge-compaction-operation.md` — how do you settle a native summarize operation whose completion is an event, not an API return?
- `./harness-acp-stream-capture-sidecar.md` — how do you preserve raw wire fidelity when a validating SDK strips undeclared fields?
- `./harness-acp-stderr-sentinel-monitor.md` — how do you detect an agent that failed WITHOUT its RPC ever failing?
- `./harness-acp-diagnostics-plane.md` — how do you log third-party protocol state without leaking `_meta` secrets, and wrap failures with stage context?
- `./harness-acp-instruction-mapping.md` — how do you deliver host instructions into an opaque agent's config without prototype pollution or secret echo?
- `./harness-acp-protocol-configuration-envelope.md` — how do you negotiate a protocol with a spawned agent and carry credentials to it through one env var?
- `./harness-acp-emit-stream-facade.md` — how do you wire a stream translator and a host-tool correlator into one emit surface without either owning the other?
- `./harness-dialect-auth-resolver-ladder.md` — how do you resolve provider credentials into an env blob ONCE with a mode ladder that survives five runtimes and a legacy option shape?
- `./harness-pi-provider-registration.md` — how do you turn ambient env vars into registered model providers when the runtime has no env-based auth of its own?
- `./harness-claude-code-apikey-helper.md` — how do you mirror a CLI's password-manager credential hook without becoming an injection vector?
- `./harness-bootstrap-asset-embedding.md` — how do you ship bridge assets into a sandbox so snapshots survive resume cycles?
- `./harness-grok-build-minimal-acp-dialect.md` — what does a dialect look like when every mechanism already lives in the shared kernel?
- `./harness-dialect-entry-default-instances.md` — how do you give consumers a zero-config default without freezing the factory surface?
- `./harness-build-time-version-injection.md` — how do you inject a package version into a client-identity string and keep the un-injected fallback testable?
- `./harness-v1-adapter-contract.md` — what is the minimal portable contract for a third-party coding-agent adapter?
- `./harness-cline-private-history-persistence.md` — how do you persist private session state in a sandbox so resume survives cross-process without leaking into the agent workspace?
- `./harness-opencode-loopback-server-auth.md` — how do you authenticate a loopback dev server you just spawned without a config file or a fixed secret?
- `./harness-opencode-event-type-guard-kernel.md` — how do you parse a third-party event stream whose fields you cannot trust to exist?
- `./policy-shadow-rollout-wrapper.md` — how do you roll out a deny-policy without letting it deny anything until you trust it?
- `./policy-total-approval-config.md` — how do you make a policy config total over a tool surface you don't control (MCP discovery)?
- `./policy-fail-closed-substrate.md` — how do you evaluate an external policy engine inside an approval callback without a throw aborting the run?
- `./policy-rego-decision-normalizer.md` — how do you normalize two generations of Rego output conventions into one decision type?
- `./policy-capability-scoping-middleware.md` — how do you hide tools from the model (not just deny calls) with a policy engine, failing closed?
- `./policy-lazy-peer-dependency-clients.md` — how do you bind an optional policy backend without making it a hard dependency or swallowing a broken bundle?

## Capsule map
- **Streaming & termination** — `./streaming.md`: stop conditions as pure predicates over accumulated StepResults (`Promise.all(...).some()`), prepareStep per-step override-with-fallback (context mutations persist), smoothStream buffer + prefix-contract detector + fixed-delay drain with metadata carry-through, four-layer timeout ladder merged into one AbortSignal (non-semantic chunks never reset timers), pull-blocking stitchable stream where inner errors terminate everything.
- **Tool-call recovery** — `./tool-calls.md`: parse/repair/degrade keyed on NoSuchToolError|InvalidToolInputError with ToolCallRepairError cause chains and visible `{invalid: true}` parts; approval-gated stream-time execution (queue-at-chunk, drain-at-model-call-end, HMAC-signed auto pairs); pruneMessages global id→toolName maps + keep-window-before-filter so approval responses never orphan.
- **Provider abstraction** — `./provider-interface.md`: two-operation (`doGenerate`/`doStream`) spec-versioned model interface; middleware = identity overrides + transformParams + wrap hooks receiving uninvoked thunks, composed by reversed reduce; core-side v2→v3→v4 Proxy adapter pays the versioning cost once.
- **Non-stream orchestrator** — `./generate-text-orchestrator.md`: do-while continuation = (every client tool call has an output-or-denial outcome OR pendingDeferredToolCalls remain) AND !stop-condition; pre-loop phase collects/revalidates/executes approvals from the last tool message before step 0; output parses only on finishReason `'stop'`; `not-applicable` approvals skip id generation to keep sequences deterministic.
- **Structured output** — `./output-specification.md`: Output factories (text/object/array/choice/json) each promise responseFormat + parseCompleteOutput + parsePartialOutput + optional element-stream transform; array wraps items in an `{elements}` envelope hoisting `$defs`/definitions, drops the last element on repaired partial parses, publishes elements exactly once via monotonic cursor.
- **History round-trip** — `./response-messages.md`: two-pass conversion → ≤1 assistant + ≤1 tool message; sources skipped, empty text skipped, invalid inputs coerced to `{}`; denied approvals synthesize `execution-denied` tool results so calls never dangle; results reordered into first tool-call appearance order with stable fallback for unmatched ids.
- **Approval round-trip** — `./approval-round-trip.md`: last-tool-message-only collection with prototype-less (`Object.create(null)`) id maps; re-validation gate order HMAC signature → input schema (throw InvalidToolInputError on forgery) → policy re-run; already-resulted approvals skip; user-config precedence over tool-defined needsApproval.
- **Tool drift detection** — `./tool-fingerprinting.md`: pin server-controlled fields only (string description presence+value via tagged shape, resolved inputSchema, title); function descriptions hash as `{type:'function'}` presence; own-property diff classifies added/removed/changed; baseline storage is the app's concern.
- **Stream simulation** — `./simulate-streaming.md`: middleware invokes `doGenerate` inside wrapStream and replays content as framed stream parts (start/delta/end triples, counter ids, empty text skipped, non-text parts verbatim, final finish part).
- **Tagged reasoning extraction** — `./extract-reasoning.md`: per-text-id buffer state machine holds partial-tag suffixes, toggles reasoning/text at full matches, synthesizes start/end pairs for empty blocks, delays `text-start` until first delta; wrapGenerate path uses matchAll + reverse splice.
- **Example enrichment** — `./tool-input-examples.md`: transformParams appends serialized `inputExamples` to function-tool descriptions (blank-line join, custom prefix/formatter) and strips the field by default so strict providers accept the request; tools without examples stay byte-identical.
- **Schema facade** — `./flexible-schemas.md`: `asSchema` dispatches symbol-marked → Standard Schema (`~standard`, zod-vendor shortcut) → lazy factory call, undefined → strict empty object; JSON-Schema conversion deferred + cached; validation always routes to the original validator; references inlined by default for `$ref`-intolerant providers.
- **Legacy object pipeline** — `./stream-object-pipeline.md`: accumulate→`parsePartialJson`→double deep-equal dedup (raw value AND validated partial)→emit `{object, textDelta}` pairs; `isFinalDelta = parseState === 'successful-parse'`; array strategy grants the torn tail element grace until the final delta and re-synthesizes consumer JSON (`[`, `,`, `]`) while validating every other element strictly; enum strategy prefix-filters and snaps only when unambiguous; array envelope hoists root `$defs`/definitions (twin of modern Output.array); element stream publishes via a monotonic cursor.
- **Partial-JSON substrate** — `./fix-json-partial-parse.md`: four-state classification (`undefined-input | successful-parse | repaired-parse | failed-parse`) over a two-attempt ladder; `fixJson` is ONE linear scan tracking `lastValidIndex` + state stack, then truncates to last-valid char and closes open strings/objects/arrays/literals from the innermost state outward — never deletes interior content; exact parses never pay for the scanner.
- **Final validation & repair** — `./final-validation-repair.md`: shared by generateObject AND streamObject's finish handler; repair engages ONLY when the error cause is exactly JSONParseError or TypeValidationError; exactly ONE attempt, `null` repair rethrows the ORIGINAL error; failures are NoObjectGeneratedError bodies carrying text + response + usage + finishReason; absent model text fails before parsing.
- **JSON prompt injection** — `./json-instruction-injection.md`: conditional defaults ('JSON schema:' prefix only with schema; MUST-answer suffix vs generic), empty-string prompt treated as absent; message twin copies (never mutates) the first system message or synthesizes one; used by mistral/bedrock adapters lacking native responseFormat.
- **Lazy result primitives** — `./lazy-result-primitives.md`: DelayedPromise stores terminal status immediately but creates the native Promise on first `.promise` read (replays stored outcome) so unawaited rejections can't crash; seven such properties on stream-object results; `isDeepEqualData` uses constructor-guard + key-set equality making array↔object transitions count as change.
- **Event ledger** — `./event-ledger.md`: eventProcessor forwards BEFORE recording; `text/reasoning-start` open id-keyed (prototype-less `createIdMap`) accumulators pushed BY REFERENCE into per-step content, deltas mutate them in place; preliminary tool-results stream but never enter history; unknown ids enqueue inline error parts; finish-step snapshots content into StepResults, flush aggregates cross-steps and REJECTS all result promises when zero steps recorded.
- **Enriched stream publisher** — `./enriched-stream-publisher.md`: `{part, partialOutput}` envelope over the whole pipeline; first-text-chunk id LOCK picks the single parse carrier (other ids pass unenriched); stringify-compare dedupe publishes only changed parses as a re-emitted buffered delta; tee-and-replace fans out to textStream/stream/partialOutputStream/elementStream; elementStream throws without an output-spec transform.
- **Continuation engine** — `./stream-continuation-engine.md`: flush-time predicate = (all client tool calls accounted (outputs + denials) OR constructor-closure `pendingDeferredToolCalls` non-empty) AND no stop condition met; provider tools with `supportsDeferredResults` register pending until a result/error arrives ANY step later; `stepFinish` handshake orders recording before deciding; incomplete-no-output streams emit NoOutputGeneratedError while partial-output streams keep results.
- **Tool-loop agent wrapper** — `./tool-loop-agent.md`: strip callbacks → inject agent default `isStepCount(20)` (NOT streamText's 1) → spread call options → user prepareCall hook → two-layer callback merge via allSettled (`mergeCallbacks(settings ?? deprecatedAlias, call)`); UA suffix APPENDS `ai-sdk-agent/tool-loop` so attribution chains across layers.
- **Output interface vs strategies** — `./output-interface-vs-strategies.md`: canonical extension point is the 4-method `generate-text/output.ts` interface; `generate-object/output-strategy.ts` trio is legacy shape. Per-mode contracts: object partials are NEVER validated; choice does live prefix-matching publishing only unambiguous repairs; complete failures always throw NoObjectGeneratedError with full context.
- **Retry kernel** — `./retry-kernel.md`: four-outcome state machine (abort rethrow / maxRetries=0 rethrow / exhausted→maxRetriesExceeded wrap / shouldRetry gate → backoff recurse or raw-vs-errorNotRetryable by try-number); header-respecting wrapper honors retry-after-ms/s/HTTP-date only within 0–60s sanity window; wired via prepareRetries integer validation.
- **Event ingestion & tool names** — `./event-ingestion-and-tool-names.md`: parseJsonEventStream yields per-item ParseResults (validation failures are DATA, never stream errors; `[DONE]` swallowed pre-parse); createToolNameMapping is an ID-keyed bidirectional rename table for provider-defined tools only — function tools excluded, identity fallback load-bearing.
- **Pre-loop approval replay** — `./result-promises-and-approval-replay.md`: synthetic "step -1" stream stitched FIRST executes approved and enqueues denied prior-run approvals; EVERY-run revalidation (provider-executed exempt); denials synthesize `{type:'execution-denied'}` tool-results so history never orphans; `_initialResponseMessages` resolves only after replay.
- **Tool-call parse & repair** — `./tool-call-parse-and-repair-ladder.md`: three-ring try/catch; repair engages ONLY for `NoSuchToolError | InvalidToolInputError`; repaired wire calls re-parse through the full schema gate; `null` repair rethrows the ORIGINAL error; terminal failures degrade to visible `{invalid: true, dynamic: true}` parts with best-effort parsed input; empty-string inputs validate `{}` instead of JSON-parsing.
- **Tool execution lifecycle** — `./tool-execution-lifecycle.md`: no-execute ⇒ `undefined` (not an error); execution errors return as `{type:'tool-error', error}` data; `notify` runs start/end callbacks in parallel with per-callback try/catch; preliminary results fan out mid-execution; `toolExecutionMs` measured inside the inner execute only; timeout signal = per-tool override ?? toolMs merged with outer abort via `AbortSignal.any`.
- **Tool timeout resolution** — `./tool-timeout-resolution.md`: `getToolTimeoutMs` resolves `tools['${name}Ms']` then falls back to `toolMs`; the bare-number form NEVER reaches tools; `undefined` ⇒ no timer created at all.
- **Abort-signal merging** — `./abort-signal-merging.md`: ladder `0 sources ⇒ undefined | 1 ⇒ returned as-is (reason fidelity) | ≥2 ⇒ AbortSignal.any`; numbers coerce via `AbortSignal.timeout(ms)`; nullish entries ignored.
- **Request-side tool assembly** — `./request-tool-assembly.md`: empty/absent tool maps OMIT the field (never send `[]`); untyped/dynamic/function all become wire function-tools with `asSchema(...).jsonSchema`; provider tools pass `{id,args}`; partial `toolOrder` = listed-in-order then alphabetical tail, never filters or duplicates; functional descriptions resolve per call with context+sandbox; choice absent ⇒ `{type:'auto'}`.
- **Prototype-safe lookup** — `./prototype-safe-lookup.md`: every model-controlled key (`constructor`, `__proto__`, `toString`) resolves via `Object.hasOwn` so inherited values can't pass truthiness gates; pairs with prototype-less id maps as one defense pattern.
- **Swallowing callback bus** — `./swallowing-callback-bus.md`: `notify` normalizes single/array callbacks via `asArray`, invokes synchronously in array order but awaits concurrently under `Promise.all`, swallows each callback's throw individually — observers can never veto or break generation.
- **Async-iterable stream duality** — `./async-iterable-stream-duality.md`: one object, both protocols; `finished` latch makes cleanup exactly-once across break/throw/completion/error; break+throw cancel the source, read-errors release without cancelling; two constructors because piping an already-piped stream adds a spurious unhandled rejection on Node 26 early-cancel.
- **Background stream drain** — `./background-stream-drain.md`: lazy result accessors trigger `consumeStream()` automatically so un-awaited streams still finish; read errors deliver at-most-once through `onError` and reject pending result promises exactly once; already-aborted signals cancel immediately.
- **HTTP transport** — `./http-transport-verbs.md`: two-handler skeleton (`successfulResponseHandler`/`failedResponseHandler` produce values/errors the verb wrapper throws), abort/APICallError passthrough at every layer, body pair `{content, values}` keeping wire bytes and error-context data separate; getFromApi adds credential withholding unless same-origin with `credentialedOrigin` plus optional validated redirects.
- **SSRF download guard** — `./ssrf-download-guard.md`: validate EVERY hop before requesting it (manual redirects — native follow defeats the guard), opaque redirects fail closed outside browsers, cross-origin hops drop ALL caller headers except user-agent (spec strips only Authorization; servers carry keys in custom headers), embedded-IPv6 addresses reuse IPv4 range checks, Node DNS pinning closes rebinding.
- **Response handlers** — `./response-handler-factory.md`: handler envelope `{value, rawValue?, responseHeaders?}`; error factories degrade in three tiers (empty → statusText, unparsable → statusText, parsable → provider message + per-error isRetryable); success factories THROW on schema violation with full raw body attached.
- **Body size limit** — `./response-body-size-limit.md`: Content-Length fast-path (advisory only) + streamed counter (the real enforcement) + cancel-in-finally; 2 GiB default because arrayBuffer has ~2x peak overhead and OOM kills are uncatchable.
- **Header trust split** — `./header-sanitization-origin-trust.md`: five blocked families (hop-by-hop, host routing, proxy spoofing, cloud metadata, cookies) on first untrusted hop; Authorization/custom keys deliberately survive sanitize but drop on every cross-origin redirect; credentialedOrigin vs trustedOrigin answer "may credentials ride" vs "must this hop be checked", both from config never responses; invalid URL ⇒ fail-closed false.
- **Provider tool-call tracker** — `./provider-toolcall-tracker.md`: id→index→latest resolution ladder over nullable deltas; NO early finalization even on parsable JSON (#13137 prefix trap); index reuse after finish starts a NEW call; flush() finalizes unfinished calls exactly once.
- **Tool callers (experimental)** — `./experimental-tool-callers.md`: validate config up front with own-property checks; per-step split into modelTools (wire visibility) vs executionTools (run loop); local-only caller ⇒ tool executable but INVISIBLE to the model; local callers bind their collected tool set after collection.
- **Model-call normalizer** — `./model-call-stream-normalizer.md`: one TransformStream holding id→parsed-tool-call map (approval/provider-result joins), content accumulator, part-index maps, and output-chunk timing (non-empty deltas only); dangling references become error PARTS, never throws; responseId generated up-front so missing provider ids stay stable.
- **Prompt plane** — `./prompt-standardization-conversion.md`: five validation gates (mutual exclusion → instructions type → normalization → emptiness → system placement) then schema check; consecutive tool messages merge with providerOptions migrating onto the earlier last part; approved-aware dangling-tool-call guard; empty tool messages filtered post-merge.
- **Micro-kernels** — `./micro-utility-kernels.md`: SerialJobExecutor shift-after-await serial queue; setAbortTimeout reproduces AbortSignal.timeout's TimeoutError DOMException exactly; mergeObjects skips `__proto__`/`constructor`/`prototype` and lets explicit undefined never override; simulateReadableStream null-vs-zero delay asymmetry under pull control; handleFetchError marks ONLY connectivity failures retryable.
- **Download consumer plane** — `./download-asset-ladder.md`, `./model-url-passthrough-policy.md`: one `download()` entry wraps the SSRF-guard fetch + size-limited read and owes every caller cancel-body-before-reject socket hygiene, single-flavor DownloadError classification (isInstance rethrow vs cause-wrap), and final-response content-type extraction; the default download function answers each requested URL independently — supported-by-model ⇒ `null` (pass through, never skip) else fetch — and returns results INDEX-ALIGNED because position is the only correlation channel back to plannedDownloads.
- **File-part normalization & codecs** — `./file-part-data-normalization.md`, `./data-content-codec.md`: tagged v4 shapes (data/url/reference/text) plus legacy inputs collapse through a decision ladder where a `data:` URL is NEVER valid inline data (explicit throw), parseable-string-vs-base64 is classified by try/catch URL construction, binaries normalize to Uint8Array at the boundary; DataContent strings are ALWAYS base64 (decode failures wrap in InvalidDataContentError) while splitDataUrl stays non-throwing so the caller owns typed errors.
- **Tool result & option shaping** — `./tool-result-output-shaping.md`, `./call-options-validation-gate.md`: tool outputs resolve errorMode FIRST (error-text/error-json bypass any custom projection — tools can't leak stacks into model-visible errors), then toModelOutput with preserved `this`, then string→text / everything-else→json with undefined→null at the JSON boundary; call options validate eagerly per field behind `!= null` gates (maxOutputTokens splits non-integer from <1; sampling params typeof-number) while stopSequences/reasoning pass through untouched.
- **Provider wire naming** — `./provider-tool-name-mapping.md`: provider-executed tools rename via a build-once dual map keyed by stable `provider.tool-id`; both directions fall through to IDENTITY for unmapped names because stream events arrive unconditionally — throwing on unmapped names would break every function-tool event.
- **Media sniffing** — `./magic-byte-media-sniffing.md`: null-wildcard prefix tables match ≤18 decoded bytes identically for raw and base64 (whole 4-char groups, trimmed), bounded ID3v2 strip rescues tag-buried MP3s up to an exported 128KiB limit, RIFF appears under three media types disambiguated by table order, and generic detection deliberately EXCLUDES audio/mp4 (ftyp can't split audio/video — convention says video/mp4 unless the caller asked for audio).
- **WebSocket transport** — `./websocket-transport-seam.md`: constructor resolution + undefined-stripped headers + pre-construction abort branch belong to the seam; callers own URL/auth-channel/wire protocol; a shared promise tail chains decode→handle→close so async Blob decoding delays but never reorders messages or lets close overtake a pending terminal frame; every handler failure routes to onProcessingError and close() itself never throws.
- **Multipart & header helpers** — `./multipart-form-encoding.md`, `./header-default-fallback.md`: FormData flattening skips nulls entirely, sends single-element arrays under the bare key but multi-element under `key[]` (with an opt-out flag for bracket-hostile APIs); header merging is DEFAULTS-FILL-GAPS (`has()` guard — the opposite of combine-headers' later-wins), and the legacy data-URL text decoder throws two distinct plain Errors (malformed structure vs bad base64) while remaining browser-flavored (`window.atob`).
- **React chat binding** — `./react-external-store-bridge.md`, `./chat-request-lifecycle.md`, `./filelist-ingestion.md`: a once-created Chat instance reads fresh callbacks/transport through per-render-refreshed refs behind stable wrapper identities; the paired `{chat, messages}` snapshot ref + post-subscribe resync guards useSyncExternalStore tearing; status subscribers flush messages BEFORE terminal ready/error renders; AbstractChat funnels every chunk and tool-output through ONE SerialJobExecutor, gates resume staleness with `isCurrentRequest()`, checks reconnect BEFORE flipping to submitted (204 ⇒ silent no-resume), aborts pending-preparations→resume→active-response in order, fires onFinish from nested finally even when it throws; FileList conversion runs under a tracked AbortController with the aborted-check AFTER the await.
- **UI message protocol (client)** — `./ui-chunk-reducer.md`, `./ui-to-model-conversion.md`, `./http-chat-transport.md`: the chunk reducer mutates parts IN PLACE between writes with step-scoped-then-global tool lookup, sticky `providerExecuted`, call-vs-result providerMetadata split, transient data parts that bypass history, and error-as-data for unknown ids; convertToModelMessages splits assistant parts into blocks at every step-start and emits exactly one outcome per call (result / error-text / execution-denied) while provider-executed results ride inline in the assistant message and are skipped in the tool role; HttpChatTransport resolves Resolvable headers/body/credentials at request time, layers call-site over init headers, treats prepare hooks as REPLACE-not-merge, never caches fetch, and answers resume probes with 204⇒null.
- **React streaming hooks** — `./swr-use-object.md`, `./realtime-hook-store.md`, `./use-completion-legacy.md`: useObject shares partial-object state across components via SWR `[completionId,'object']` keys with a write-path deep-equal render gate and close-boundary-only schema validation (aborts swallowed silently); realtime sessions expose five per-field external-store subscriptions over immutably-replaced state with a tail-bounded event ring and identity-keyed store recreation on {model,token,sessionConfig,sampleRate,maxEvents}; useCompletion is the legacy plain-text surface riding call-completion-api.
- **RSC generative-UI bridge** — `./streamable-value-protocol.md`, `./streamable-ui-suspense-ladder.md`, `./ai-state-delta-sync.md`, `./resolvable-promise-kernel.md`: streamable values encode updates as a promise chain of snapshots where prefix-extends shrink to `[0, suffix]` string patches (reader re-validates string-typed appends; `.done()` is mandatory or clients hang; stream sources lock the wrapper during pumping); streamable UI streams React nodes as a recursive `<Suspense>` row chain with promise-rebinding updates, frozen-on-append semantics, error-to-error-boundary delivery, and a finished-promise-chain serializing overlapping renders; server AI state lives in AsyncLocalStorage scoped clones whose jsondiffpatch deltas (against the ORIGINAL, applied by the client against ITS OWN snapshot) cross the wire inside sealed action returns.
- **UI message protocol (types & validation)** — `./ui-tool-invocation-state-union.md`, `./ui-message-validation-ladder.md`: one 7-state discriminated union (`input-streaming → input-available → approval-requested ⇄ approval-responded? → output-available | output-error | output-denied`) shared by static `tool-*` parts (name in the type prefix) and `dynamic-tool` parts (name in a field), every cross-state field never-gated so wrong-state assignments fail at compile time; denial lives in `approval.approved:false`, terminal states may carry `approval.approved:true`; static tool names extract via split-then-JOIN because names contain dashes. Validation runs structural union → optional metadata/data schemas → tool checks GATED BY STATE: input validated only in `input-available` (terminal input deliberately NOT re-validated — replay must not crash on stale payloads), output validated only when `outputSchema` exists, missing TOOL schema fatal only pre-terminal while missing DATA schema is always fatal.
- **UI message stream writer (server)** — `./ui-stream-writer-kernel.md`, `./ui-stream-wire-contract.md`, `./ui-part-to-chunk-conversion.md`, `./ui-stream-metadata-adapter.md`, `./ui-stream-persistence-ladder.md`, `./ui-stream-snapshot-reader.md`, `./ui-stream-response-family.md`, `./ui-agent-http-entry.md`: the server-side inverse of the client reducer — `createUIMessageStream` keeps its ReadableStream open via a promise-array drain loop that RE-READS the array after every await so sub-streams may merge after execute() returns, funnels every producer error into `{type:'error'}` chunks behind a detail-hiding default onError, and swallows writes-after-close; the wire contract validates a 26-member looseObject union where unknown FIELDS pass but unknown TYPES reject and the `data-${string}` family is a startsWith predicate; toUIMessageChunk is a pure exhaustive-switch converter whose load-bearing rules are undefined→null at the output field (JSON drops undefined keys) and two-tier error text (provider-executed errors serialize verbatim, local tool errors route through onError); toUIMessageStream computes messageMetadata PER PART and emits it inline on start/finish but as a follow-up `message-metadata` chunk otherwise; handleUIMessageStreamFinish re-runs the CLIENT reducer server-side for persistence — id injection into start chunks, assistant-tail continuation by id equality, structuredClone snapshots for onStepEnd vs live state for onEnd, an isAborted flag observed in pass-through mode, and a finishCalled latch shared by flush AND cancel because a cancelled TransformStream never flushes; readUIMessageStream exposes per-write snapshots with text/reasoning bodies lifted out before structuredClone (O(n²) clone trap) reattached by part index; the response family encodes chunks as `data: <json>\n\n` + `[DONE]`-on-flush under five contract headers including the `x-vercel-ai-ui-message-stream: v1` version pin, teeing AFTER encoding for fire-and-forget observers; createAgentUIStream pins the canonical ordering validate→convert→agent.stream→toUIMessageStream with validation deliberately BEFORE conversion.
- **Completion predicates & transport family** — `./last-step-completion-predicates.md`, `./direct-in-process-transport.md`, `./text-stream-ui-adapters.md`, `./wire-dialect-registry.md`: both auto-resume predicates reduce to the LAST `step-start` window — tool-calls variant counts ≥1 non-provider-executed invocation all terminal (output-error counts complete), approval variant requires ≥1 `approval-responded` AND zero still-requested over ALL tools (providerExecuted filter present ONLY in the former; porting it onto approvals deadlocks HITL) — consumed via `sendAutomaticallyWhen` with un-awaited auto-send inside the SerialJobExecutor. DirectChatTransport = validate→convert→agent.stream→toUIMessageStream in-process with reconnectToStream returning null (the protocol's "nothing to resume"); text streams lift into six framing chunks under constant part id `text-1` (start/start-step/text-start … text-end/finish-step/finish, emitted even for empty streams); HttpChatTransport exposes ONE template-method hook (`processResponseStream`) so Default (SSE JSON, parse failures thrown mid-transform) / Text / Direct dialects are constructor-plus-one-method subclasses, and chunk validation happens only at the wire boundary.
- **Provider registry & default injection** — `./provider-registry.md`, `./custom-provider-fallback.md`, `./extract-json-stream-guard.md`, `./default-injection-middlewares.md`: the registry splits composite ids on the FIRST separator (model ids may contain more), answers unknown-half failures with two DIFFERENT errors (`NoSuchProviderError` listing available providers vs `NoSuchModelError` naming the full id), wraps ONLY language/image models with registry-level middleware, and converts v3 video models to v4 lazily via a prototype-cloned wrapper; `customProvider` runs one membership→resolve→fallback ladder per family with lazy v2/v3→v4 resolution and per-family fallback existence guards; `extractJsonMiddleware` strips markdown fences from streams through a three-phase state machine whose load-bearing parts are the 12-char suffix holdback (split fences never emit), suffix-only stripping once any byte streamed, and a prototype-less id map against hostile `__proto__` chunk ids; the defaults middlewares show merge-objects semantics at the call-options surface (explicit wins, `undefined` falls to default, `null` beats numeric defaults, providerOptions deep-merge) and prepend-only system-prompt injection suppressed by ANY system message anywhere (earlier middlewares included).
- **Retrieval plane (embed/rerank)** — `./embed-batching-parallelism.md`, `./rerank-orchestration-contract.md`, `./model-resolution-versioning.md`, `./embedding-middleware-wrapper.md`, `./warning-pipeline.md`, `./retrieval-operation-lifecycle.md`: embedMany runs a two-path scheduler (null/Infinity limit ⇒ ONE retry-wrapped doEmbed with all values; else splitArray into value chunks, grouped into parallel batches of `supportsParallelCalls ? maxParallelCalls : 1`) whose reassembly is PUSH-ORDER — embeddings[i] matches values[i] even when calls complete out of order; per-chunk retry preserves completed siblings and missing provider usage defaults to `{tokens: NaN}` so under-reporting cost is impossible while one omission poisons the sum by design; rerank short-circuits EMPTY documents BEFORE retries/tracing but still fires start+end callbacks with a synthetic response, discriminates wire documents by FIRST-ELEMENT runtime type only (`{type:'text'|'object', values}`), delegates topN to the provider without local slicing, and hydrates ranking through the caller's original array preserving `originalIndex` (no range validation — trusted-index contract); bare string model ids resolve through `globalThis.AI_SDK_DEFAULT_PROVIDER ?? gateway` (NO local registry on that path — a remote gateway call hides behind the type union) while reranking's provider method is OPTIONAL with an explicit gateway hint error; v2/v3→v4 lifting is a one-property Proxy (`specificationVersion` trap only) over the ORIGINAL object so prototype methods keep their binding; embedding middleware composes by reverse-reduce (first-listed transforms params first) and its capability overrides (`overrideMaxEmbeddingsPerCall`/`overrideSupportsParallelCalls`, evaluated once at wrap time) silently change scheduler batching; warnings flow through a three-rung ladder read PER CALL from `AI_SDK_LOG_WARNINGS` (`false` suppresses → function receives raw options → default emits via process.emitWarning) behind an empty-array no-op that never trips the module-level one-time info-note latch; all three entrypoints share one skeleton — injectable callId, deprecated alias resolution, UA suffix APPENDED once before the try block, notify(start) → retry(notify(inner-start) → model call → notify(inner-end)) → logWarnings → notify(end), errors to telemetryDispatcher.onError ONLY then rethrown verbatim.
- **Gateway plane** — `./gateway-provider-factory.md`, `./gateway-auth-ladder.md`, `./gateway-metadata-cache.md`, `./gateway-error-plane.md`, `./gateway-wire-identity-headers.md`, `./gateway-file-part-base64.md`, `./gateway-client-secret-mint.md`, `./gateway-subprotocol-auth.md`, `./gateway-realtime-identity-codec.md`, `./gateway-transcription-http-path.md`, `./gateway-transcription-ws-pump.md`, `./gateway-observability-gets.md`, `./gateway-catalog-type-filter.md`, `./gateway-provider-executed-tools.md`, `./gateway-o11y-header-split.md`, `./gateway-open-provider-options.md`, `./gateway-zod-shim-workflow-serialize.md`, `./gateway-modality-family.md`: one closure-per-instance factory serves eight modalities with alias-by-assignment members and headers passed as an UNAWAITED thunk so OIDC tokens stay fresh for the instance lifetime; auth is a two-rung ladder (explicit apiKey option → AI_GATEWAY_API_KEY env → ambient Vercel OIDC) whose chosen method MUST ride every request as `ai-gateway-auth-method` because the error plane reads it back via parseAuthMethod to pick remediation text; getAvailableModels is hand-rolled SWR (promise-slot coalescing, staleness clock bumped at REQUEST START, cache returned when present even while a refresh is pending, failed first-ever fetch stays rejected until TTL); every failure funnels once through asGatewayError — already-GatewayError passthrough → undici UND_ERR_* timeout detection incl. APICallError.cause chain → typed class dispatch on server error.type with generationId appended to the message → synthetic-500 fallback — using global Symbol.for markers so cross-realm instances still match and retryability DERIVED from status (408/409/429/5xx); ALL language models POST to one `/language-model` endpoint carrying identity in headers (`ai-language-model-id`, spec-version 4, streaming flag as String(boolean)) with abortSignal destructured OUT of the body and inline Uint8Array file data base64-encoded IN PLACE across file/reasoning-file/tool-result-content part kinds; client secrets (`vcst_`) mint ONLY server-side (window guard on minting itself, never on codec construction) via an ORIGIN-relative `/v1/realtime/client-secrets` POST because baseURL carries the /v4/ai prefix; WebSocket auth rides Sec-WebSocket-Protocol subprotocols (`ai-gateway-auth.<raw-token>` — JWT dots are valid RFC tokens — plus base64url-encoded team scope and a negotiation marker), client encode + server decode living in ONE module; the realtime model is a deliberate IDENTITY codec (events pass through unchanged because the gateway owns provider translation) with createClientSecret injected; streaming transcription pumps duplex frames through one socket under a single finished latch — start frame, ≤64KiB binary audio splits with buffer-drain backpressure, audio-done, error parts stop audio but keep the stream open until the server's billing-flush close, finish closes with code 1000, close-before-finish synthesizes typed errors from a canonical status map while authMethod is parsed EAGERLY before async gaps; credits/spend/generation-info GETs hit the ORIGIN (/v1/*) not the baseURL prefix with snake_case→camelCase zod transforms keeping absent metrics absent and tags comma-joined into one param; the model catalog silently DROPS unknown modelType entries inside the response schema (forward compat by subtraction, legacy null kept); gateway tools (exa/parallel/perplexity search) ship via createProviderExecutedToolFactory with NO local execute and failure-as-data output unions; o11y headers split static deployment env (snapshot at creation) from per-request x-vercel-id context; provider options are an OPEN record validated service-side so new routing knobs need no SDK release; the zod namespace shim imports eleven named factories for tree-shaking and WORKFLOW_SERIALIZE/DESERIALIZE statics let models cross workflow step boundaries; the embedding/image/video/reranking/speech twins share the skeleton byte-for-byte with batching knobs expressed as CLASS FIELDS (maxEmbeddingsPerCall=2048, supportsParallelCalls=true).

- **Drift re-entry @9d9a73f (pass 13)** — `./tool-execution-safety-gate.md`, `./stream-tool-buffering.md`, `./callback-containment-streaming.md`, `./part-id-crossstep-remap.md`, `./stream-object-terminal-latch.md`, `./preliminary-output-suppression.md`, `./video-async-lifecycle.md`, `./video-webhook-arbitration.md`: the 96-commit drift wave resolved into ai-core hardening seams — tool execution is gated by a two-value finish-reason allowlist (`stop`/`tool-calls` ONLY; length/error/content-filter/other must never execute) checked at model-call-end over a buffer of approval-resolved calls drained in parallel, with `not-applicable` approvals skipping id minting to keep sequences deterministic; ALL streaming user callbacks route through the swallowing notify bus because direct awaits let thrown onChunk/onError kill consumers and mask provider errors; multi-step merged streams remap colliding provider part ids first-wins with generated-suffix ladders and per-step correlation maps (empty deltas without metadata dropped); streamObject's first error chunk latches terminalError, rejects all seven result promises eagerly, and forces finishReason to `'error'` even when a later success frame arrives; history conversion excludes preliminary tool outputs from the model-visible tool message so interrupted async-generator tools can't replay "half finished" as fact; async video splits into start/status with an idempotency token minted OUTSIDE the retry closure (`aisdk_vid_*`, caller key wins) and throw-don't-split per-call caps, completing via webhook when the model implements `handleWebhookOption` else warn-and-poll with a one-shot post-webhook status check that errors on incomplete.
- **Drift re-entry: protocol planes (pass 13)** — `./gateway-batch-trio.md`, `./mcp-parameter-headers.md`, `./mcp-era-discovery.md`, `./mcp-http-era-split.md`, `./workflow-resume-index-spaces.md`, `./ui-stream-frame-repair.md`, `./rsc-concurrent-state-writes.md`, `./harness-builtin-replay-split.md`, `./empty-toolcall-id-coercion.md`, `./bedrock-eventstream-framing.md`, `./schema-additionalproperties-preservation.md`: gateway batch start/status/results trio keeps idempotency keys in the HEADER not the body (server hashes raw body for replay identity — body-borne keys make equivalent retries hash differently → false 422), preserves Abort/TimeoutError raw instead of wrapping into retryable 500s, and forwards requestCounts only when all four counters are numeric; MCP 2026 support adds x-mcp-header argument→header bindings validated through reachability/token/uniqueness/type ladder with base64-sentinel encoding onto `Mcp-Param-*`, optimistic server/discover probe (1s timeout, modern rejection codes fatal vs other failures falling back to legacy initialize), and era-gated transport behavior (no session ids, no inbound SSE, per-request header mirroring in modern mode); Workflow resume replays the RAW durable stream from 0 and applies the non-negative UI cursor inside createModelCallToUIChunkTransform because raw parts and UI chunks are not one-to-one; the UI-stream frame repair normalizer synthesizes missing starts / drops duplicates for text+reasoning families with consumer-mirrored resets at reset-step/finish-step while deliberately excluding tool parts; RSC AI-state mutations now all derive their base inside functional setters so concurrent deltas/keyed writes compose instead of last-writer-wins; harness runPrompt separates builtin-approval replay tracking from host-tool outcome tracking so approved builtin tools still emit their genuine result exactly once; empty-string provider tool-call ids coerce through falsy `||` (not nullish `??`) across the adapter family; Bedrock ConverseStream decodes big-endian length-prefixed frames with fail-loud flush and NO catch-and-stop around decode; JSON-Schema strictification recurses into schema-valued additionalProperties instead of erasing Zod4 record/catchall value types.
- **MCP Apps host plane (pass 14, react/src/mcp-apps + @ai-sdk/mcp app resources)** — `./mcp-app-bridge-init-gate.md`, `./mcp-app-tool-allowlist.md`, `./mcp-app-request-validation-ladder.md`, `./mcp-app-csp-sanitizer.md`, `./mcp-app-permission-intersection.md`, `./mcp-app-frame-lifecycle.md`, `./mcp-app-tool-part-extraction.md`, `./mcp-app-server-resource-contract.md`, `./mcp-app-resource-fingerprint.md`: the host-side JSON-RPC bridge QUEUES all host→app notifications until the app's own `ui/notifications/initialized` (only sandbox-bootstrap notifications bypass) so late-mounting apps still receive streamed tool input; `tools/call` is deny-by-default TWICE — no `callTool` callback ⇒ hard error, and an omitted `allowedTools` exposes ZERO tools rather than forwarding everything; per-method validators run before any host callback with parsed-URL scheme checks (https/http/mailto only for open-link) and `ui://`-only resource reads; untrusted server CSP domains are canonicalized through `new URL()` decode-then-recheck because `%3B`-style encodings slip raw-string denylists, inside a fixed 9-directive lockdown (`default-src 'none'`…); iframe permissions grant only the INTERSECTION of server-requested × host-allowed (both sides null ⇒ nothing), and the outer frame must delegate features hierarchically for the inner app frame; the React frame bootstraps pull-style on the proxy's `sandbox-proxy-ready` (html+csp+sandbox+allow reply), derives a CONCRETE outbound origin (never `'*'`, `'null'` fallback for opaque origins), tears down request-before-close, and replays current input/output via refs at init; renderer metadata is gated on exact MIME `text/html;profile=mcp-app` + ui:// prefix with URI-keyed identity pinning so streaming part churn never re-mounts the iframe; server-side normalization splits failure philosophies — identity fields (resourceUri) THROW while embellishment fields degrade via `.catch(undefined)` schemas, with legacy flat `ui/resourceUri` fallback and text/base64-blob html decoding; drift detection fingerprints {html, csp, permissions} through canonicalJSON SHA-256 duplicated BY DESIGN across packages (wrong dependency direction to import upward).
- **Realtime browser session plane (pass 14, ai/src/realtime)** — `./realtime-session-skeleton.md`, `./realtime-tool-gating.md`, `./realtime-barge-in.md`, `./realtime-audio-codec-kernel.md`, `./realtime-websocket-transport.md`, `./realtime-event-reducer.md`, `./realtime-tool-definitions.md`: the abstract session owns lifecycle only — token-endpoint POST mints credentials, playback AudioContext is created BEFORE the socket opens, merged config rides `session-update` on open, and every state transition flows through the reducer's `{state, effects}` output applied per-changed-key via an abstract setState seam; tool follow-ups fire exactly ONE `response-create` under a two-condition conjunction (response-delivering-calls closed AND every call's output submitted) because per-output triggering lets the model continue without full multi-tool context, while `onToolCall → undefined` is the documented human-in-the-loop escape hatch; barge-in measures played milliseconds BEFORE stopping playback then sends `conversation-item-truncate` with the rounded offset (contentIndex pinned 0); the audio kernel clamps asymmetrically (negative ×0x8000 vs positive ×0x7FFF), builds base64 in 32KiB chunks (spread arg limit), and schedules gapless playback by chaining a clock advanced per buffer duration; the WebSocket transport chains every send onto a self-catching promise queue (async serialization can't reorder, one bad event can't poison), tracks sockets synchronously with identity guards on open/close so ghost sockets never fire callbacks, and answers provider health-check pings BEFORE event parsing; the reducer keeps streaming UI stable via accumulator maps + itemId→part-location maps updating slots IN PLACE (never re-created parts), seeds dynamic-tool parts with empty names until `-done` supplies them, converts arg-parse failures into error EFFECTS instead of executing garbage, and inserts late input transcriptions at their audio-committed position instead of after the model reply; realtime tool definitions admit function/dynamic tools only (provider tools silently excluded as server-executed) with fail-loud `never` exhaustiveness on unknown types and context-resolved function descriptions.
- **Code-mode sandboxed tool plane (pass 15, packages/code-mode whole ~3.9k LOC)** — `./code-mode-execution-policy-byte-expansion.md`, `./code-mode-proxy-bindings-source-wrapping.md`, `./code-mode-interrupt-resume-ladder.md`, `./code-mode-signed-continuation-capability.md`, `./code-mode-approval-funnel.md`, `./code-mode-host-tool-invocation-contract.md`, `./code-mode-tool-prompt-compiler.md`, `./code-mode-error-taxonomy.md`, `./code-mode-json-boundary-kernel.md`, `./code-mode-generic-interrupts.md`, `./code-mode-tool-caller-late-binding.md`, `./code-mode-sandbox-semantics.md`, `./code-mode-outer-tool-call-identity.md`, `./code-mode-approval-message-roundtrip.md`, `./code-mode-interrupt-payload-protocol.md`, `./code-mode-package-boundary.md`, `./code-mode-behavior-test-battery.md`: the QuickJS-backed code-mode package lets a model write TypeScript that calls host tools through a sorted-name→indexed-host-function Proxy (unknown names still round-trip to the host so errors stay real); every byte cap is enforced EXACTLY at each host boundary while the runner receives 2×+1KiB-expanded serialization budgets and overhead-adjusted source limits, with non-positive limits meaning disabled not defaulted; interruptions freeze the whole program into an HMAC-SHA256-signed continuation over canonical JSON (key-sorted, undefined-dropping) covering js/toolNames/ledger — resume is positional (resolutions.length indexes the next pending), requires byte-equal source AND sorted tool names, exposes the batch ONE interrupt at a time, re-checks no approval was denied before final replay, and never repeats completed sibling work because resolutions ride the signed ledger; approvals gate validate→approve→execute with undefined-callback-means-ApprovalRequired (never allow) and denial persisting across replays; nested ids are `${outer}:tool-N[:interrupt]` stable across processes via the signed outerToolCallId; errors preserve class identity by matching stable codes across the RunError bridge, translate runner limit paths to public executionPolicy names, and rewrite run.js stack frames into user-source coordinates (offset by the one preamble line); async-generator host tools drain to their FINAL yielded part; the description compiler renders depth-capped cycle-safe JSON-Schema→TS types plus format-aware example synthesis promising exactly the proxy's call arity.

- **OpenTelemetry integration plane (pass 16, packages/otel whole ~11.8k LOC)** — `./otel-call-state-machine.md`, `./otel-lazy-attribute-specs.md`, `./otel-sanitizer-kernel.md`, `./otel-semconv-formatters.md`, `./otel-provider-tool-synthesis.md`, `./otel-supplemental-enrich-span.md`, `./otel-abort-error-drain.md`, `./otel-legacy-plane-boundary.md`, `./otel-test-doubles.md`, `./otel-span-taxonomy.md`: a telemetry integration is an event→span-tree compiler keyed on callId state (single-slot step/inference vs Map-based tool/embed, settings replayed from Start events because later events don't re-carry them, silent no-op on unknown callId); attribute specs defer serialization behind `{input|output}` thunks gated BEFORE invocation by isEnabled≠false / recordInputs / recordOutputs; every value passes the OTel-legality sanitizer (homogeneous primitive arrays only, non-finite numbers kill the whole attribute, invalid members filtered); SemConv translation maps provider strings longest-prefix-first (`google.vertex` before `google`), collapses all four text ops to `invoke_agent`, normalizes finish reasons (`tool-calls`→`tool_call`, other/unknown→`stop`) and degrades unknown part types to `{type:'<raw>'}` instead of throwing; provider-executed tools get synthesized `execute_tool` spans at model-call-end with preliminary results skipped and deferred calls kept visible; user enrichment fails open (throw⇒undefined) and standard attributes win spread collisions; abort ends silently while error stamps ERROR+exception down the innermost→root drain ladder with slot clearing so late events no-op; legacy plane differs wholesale in vocabulary (`ai.*` keys, doGenerate/doStream span names, stream perf as EVENTS not attributes) and must never mix with gen_ai.*; test doubles split — MockTracer for start-vs-runtime attribute assertions, InMemorySpanExporter for real parenting/export.

- **Harness agent kernel (pass 17, packages/harness/src/agent whole)** — `./harness-agent-stateless-session-split.md`, `./harness-session-dual-state-machine.md`, `./harness-turn-suspend-resume-payloads.md`, `./harness-approval-continuation-recovery.md`, `./harness-host-tool-execution-join.md`, `./harness-stream-abort-settle-contract.md`, `./harness-bootstrap-marker-idempotency.md`: a HarnessAgent is a STATELESS definition (adapter + merged tool surface + sandbox config; caller-owned sessions via `createSession` XOR resumeFrom/continueFrom with schema-validated payloads and cleanupAfterStartFailure stopping only harness-owned sandboxes); HarnessAgentSession runs TWO orthogonal state machines (sessionState active/detached/stopped/destroyed × turnState idle/running/awaiting-approval/awaiting-tool-result/suspended) where finishTrackedTurn clears pendings ONLY when session is active and the turnId is current, so late callbacks cannot corrupt reuse gating; unfinished turns cross processes as continue-turn envelopes carrying pendingToolApprovals/pendingToolResults (resume-session state must NEST them under continueFrom — validation throws otherwise) with idempotent `??=` suspension; id-only approval responses reattach to their original tool call through prototype-safe lookups and consumed responses are skipped; host tool executions track in `outstandingHostToolExecutions` joined via `Promise.allSettled` at every step boundary while generator executes normalize yields into consumer-only `preliminary` parts; user aborts settle as one `abort` part matching streamText contract while adapter rejections become stream error events (never both); bootstrap recipes apply once behind a content-hash marker file `.bootstrap-<identity>.ok`.

- **Harness bridge transport plane (pass 18, bridge/index.ts 951L + utils/sandbox-channel.ts 526L + utils/bridge-user-message-submitter.ts)** — `./harness-bridge-claim-stream-ownership.md`, `./harness-bridge-monotonic-seq-replay-log.md`, `./harness-bridge-disk-replay-recovery.md`, `./harness-channel-reconnect-resume-ladder.md`, `./harness-channel-suspend-cursor-freeze.md`, `./harness-channel-event-checkpoint-pin.md`, `./harness-bridge-user-message-steering.md`: the in-sandbox WebSocket bridge claims its event stream by WORK (`start`/`resume`), never by connection, so an abort-only second client cannot steal events while control frames reply to their sending socket; seq numbering is process-monotonic across turns while the replay LOG clears each turn (append-before-send makes live delivery best-effort); every event mirrors to ndjson behind a setImmediate single-flight flush and a respawned bridge with `BRIDGE_REPLAY_FROM_DISK=1` reloads pre-connection, restoring seqCounter from the last line (corrupt log ⇒ empty ⇒ host degrades to rerun); the host-side SandboxChannel serializes all inbound work through one dispatch-chain promise and reconnects inside an ELAPSED-time budget (50ms ×1.5 → 2s cap, 30s default) that re-wires, sends `resume{lastSeenEventId}`, then flushes queued sends — a transient drop never fires onClose, exhaustion finalizes once as `'reconnect failed'`; suspend() drops inbound frames BEFORE dispatch so the cursor freezes exactly at the last delivered event and closes with reason `'suspended'`; events stamped with a non-enumerable Symbol.for checkpoint property (survives separate agent/utilities bundles) let a slice pin suspension at an already-dispatched boundary so the next process replays the tail; user steering stays exactly-once because the server queue dedups by messageId (replaying stored responses) while the client submitter blindly re-sends all pendings on reconnect.

- **Harness result-object + turn-telemetry plane (pass 19, agent/internal/harness-stream-text-result.ts 964L + turn-telemetry.ts 489L)** — `./harness-result-streamtext-twin.md`, `./harness-result-driver-writer-surface.md`, `./harness-result-finish-gate-and-steps.md`, `./harness-result-start-part-contract.md`, `./harness-result-output-transform-lock.md`, `./harness-turn-telemetry-lifecycle-shim.md`, `./harness-usage-normalization-nested-flat.md`: HarnessStreamTextResult makes a third-party runtime LOOK like streamText — 22 per-accessor DelayedPromises typed against StreamTextResult's own keys behind one `settled` latch (abort/fail enqueue their terminal part, close the stream normally, then reject ALL promises so awaiters never hang); the writer surface is semantics-split (enqueue = step-attributed with lazy start-step synthesis; enqueueContinuation = inter-call approval/host-tool outcomes that must never create or alter a StepResult; discardCurrentStepContent = replay/slice-tail content dropped when a suspended pause already recorded the step); finishStep builds one DefaultStepResult per boundary normalizing V4 `{unified,raw}` reasons and nested usage, accumulating usage additively with LAST-step-wins reason/warnings aggregation, while terminal `finish` treats buffered unclosed content as an adapter protocol VIOLATION (fail, don't flush) and empty terminal turns are legal (steps=[], text=''); the constructor enqueues `{type:'start'}` in the stream's start callback because toUIMessageStream persistence mode injects the message id THERE — omitting it forks useChat ids from persisted ids; with an output spec a first-text-chunk-id lock picks the single parse carrier, parses republish only on stringified change as `{part,partialOutput}` envelopes, and buffered tails flush at finish-step/text-end (harness twin of enriched-stream-publisher with different boundaries); createTurnTelemetry maps turn→`ai.harness` operation / finish-step→step+inference spans / tool-calls→executions onto the shared dispatcher behind a telemetry==null⇒NOOP gate, defers onStart until the runtime-resolved modelId arrives, and end() synthesizes missing closes (open tools end as `'tool span unclosed'` errors) while toolEnd dedups by toolCallId; normalizeUsage shape-sniffs V4 nested counters into flat LanguageModelUsage where both-null totals stay undefined (never NaN).

- **Harness ACP v1 lifecycle/session plane (pass 20–21, packages/harness-acp v1 adapter + bridge session kernels)** — `./harness-acp-respawn-strategy-ladder.md`, `./harness-acp-lifecycle-compatibility-gate.md`, `./harness-acp-turn-start-config-fingerprint.md`, `./harness-acp-cold-restoration-negotiation.md`, `./harness-acp-recovered-session-update-queue.md`, `./harness-acp-wire-turn-event-pump.md`: doStart is a RESPAWN-STRATEGY DECIDER — with bridge coordinates it first tries LIVE ATTACH (SandboxChannel seeded with lastSeenEventId; continue ⇒ open({resume:true})), classifies a dead sandbox's disk log on continue (coherent tail ⇒ disk-replay respawn under BRIDGE_REPLAY_FROM_DISK=1 with a replayOnly session that rejects later prompts; incoherent tail ⇒ lossy-rerun requiring persisted turnStartConfig + acpSessionId), and plain resume with dead coords goes cold-restore (persisted coldSession config revalidated by fingerprint compare before the replacement bridge negotiates resume-vs-load); validateACPLifecycleCompatibility gates ANY reuse on FOUR identities (harnessId string, implementationIdentity digest, authenticationProfile digest, sandboxId only when both sides know it) with skills-fingerprint mismatch throwing even on incomplete prior materialization; createACPTurnStartConfig SHA-256s a key-sorted stable stringify over ONLY non-secret config while validation VALIDATES-BY-RECOMPUTES from current settings (cold additionally compares permissionMode/modelId literally); restoreACPBridgeSession prefers advertised sessionCapabilities.resume, falls back to loadSession===true, else throws fail-closed rather than creating a fresh unrelated session — suppressing historical updates around the request and discarding captured raw history AT THE RESPONSE BOUNDARY so replayed history never enters the new turn's stream; the recovered-session queue joins semantic updates AND the prompt stop in ONE buffer where clearErrors() drops buffered errors per prompt attempt and fail() latches terminal; wireTurn pairs every forwarded block with a synthesized close at finish/error/close so consumers never see unclosed blocks, classifies dynamic-tool candidates ONCE via caller isMcpToolCall into an id map, settles done exactly once with close reason 'suspended' resolving SUCCESS as a slice boundary.

- **Harness ACP bridge internals plane (pass 22, v1/bridge lower half + acp-auth)** — `./harness-acp-stream-translator-inference.md`, `./harness-acp-host-tool-loopback-relay.md`, `./harness-acp-host-tool-correlation-window.md`, `./harness-acp-permission-plane.md`, `./harness-acp-credential-brokering-twins.md`, `./harness-acp-bridge-session-services.md`: stream translation is INFERENCE not relay — text/thought coalesce into (type,messageId)-keyed blocks with `-N` collision suffixes, every update rides a lossless `{type:'raw'}` first, pending tool calls hold back until non-pending or permission-forced, builtins re-identify through name → recursive `_meta` walk → unique const/enum-discriminated schema shape (waiting for required input instead of emitting skeletons), unknown calls get sanitized dynamic names plus an emitToolCallCandidate hook for MCP classification, steps are INFERRED when the last pending call settles, and finish closes open steps with the real mapped stop reason exactly once; host tools reach the agent as an stdio MCP server over a 127.0.0.1 ephemeral-port relay guarded by randomBytes(32) length-checked timingSafeEqual bearer, fingerprinted through canonical JSON so unchanged catalogs keep their revision while changes bump it past 20s long-pollers, stale-revision invocations 409 naming both revisions and unknown tools 404 WITHOUT consuming invocation order, and the client keeps a timeout:0 keep-alive because approvals may pend on humans; the correlation kernel buffers announcements ≤1000ms/128 entries and suppresses echoes proven by EXACT 64-hex token containment or ALL THREE portable signals (server identity via delimited pair, tool name, canonical input fingerprint found recursively), lifting suppression only at terminal updates and flushing unmatched candidates in original order (fail-open to duplication, never loss); permissions answer from host state through a four-rung gate (session match → advertised allow_once+reject_once or named-gap cancel → host-tool claim auto-allows without native approval → unmapped-mode truth table) with mapped modes validated against EVERY advertised capability before any apply and boolean configs carrying an explicit type discriminator; credentials resolve ONCE on the host where the broker callback receives REAL env while the bridge/child receive masked placeholder twins (control/secret vars stripped from inherited env), profiles reference secrets BY NAME via `$source:` interpolation, and OIDC resolution re-runs scoped so ambient keys can't shadow it; external user MCP servers gate fail-closed against advertised capabilities, structured output plants schemas into prompt `_meta` at the mapped path, and promptActiveSession degrades through native meta → raw request with synthesized stop events so legacy SDKs still terminate consumer streams.

- **Harness dialect adapter plane (pass 23, opencode/codex/claude-code/deepagents host-side turn pumps)** — `./harness-dialect-wireturn-invariant-core.md`, `./harness-opencode-compaction-rail.md`, `./harness-codex-cli-shim-host-tools.md`, `./harness-claude-code-websocket-hello-ladder.md`, `./harness-deepagents-instructions-once.md`: every runtime dialect replicates ONE invariant turn-pump core (settle-once done promise, try/catch-swallowed forward, verbatim eventTypes subscription, finish→success / error→failure, close reason 'suspended'→SUCCESS as slice boundary vs any other close→'bridge closed before the turn finished', abort→best-effort {type:'abort'} + settleError(signal.reason ?? AbortError), submitToolResult/submitToolApproval channel frames) while five knobs vary per dialect (extra eventTypes, hello-gated steering, start-frame timing, respawn seed field, compaction strategy); opencode buffers out-of-turn compaction parts and flushes them at the next wireTurn entry after a between-turns-only operation:'compact' gate; codex exposes host tools as a CLI shim invoked through the agent's own bash tool with first-message-only framed guidance and defers its start frame one event-loop turn for short tool-less turns; claude-code completes the bridge handshake through a deadline-bounded retry ladder with dual open/hello timeouts shrunk from the remaining budget and rides /compact on the user-message rail; deepagents seeds instructionsApplied from the attached flag so instructions ride the start frame only on fresh spawns.

- **In-process dialect + workflow plane (pass 24, packages/harness-cline + packages/harness-pi + packages/workflow-harness)** — `./harness-cline-inprocess-parked-sessions.md`, `./harness-cline-sandbox-remote-ops.md`, `./harness-pi-workspace-mirror-vfs.md`, `./harness-pi-dangling-host-tool-rerun-barrier.md`, `./workflow-harness-durable-step-engine.md`: a third adapter archetype — the runtime is an IN-PROCESS Node library on the host while the model's workspace stays in the sandbox, so there is no attach and no replay: same-process continuation parks the live session in a module-level Map (detach/suspend with pending host input) while cross-process resume is ALWAYS lossy rerun from sandbox-persisted state (cline history JSON / pi journal file), with a `suspending` flag that swallows only abort-class errors so an unanticipated failure mid-suspend still surfaces; cline's built-in tools run as sandbox remote ops with two-tier containment (logical posix check + in-sandbox canonical realpath via sentinel-marker shell one-liners, writable resolution walking to the closest existing ancestor); pi keeps its own fs-based resource loading honest through a scoped host mirror (.pi/.agents/AGENTS.md only — a full mirror of node_modules would take hours) plus a global node:fs VFS mounted at the sandbox workDir path (collision-free because it cannot exist on the host; longest-prefix routing, patches restored after the last unmount) with filesystem extension discovery disabled so host-developer Pi extensions never execute in the server process; pi's journal-based rerun holds the turn behind a dangling-host-tool barrier until the framework re-delivers every pending result, injects them into the restored journal before rebuild, and emits a synthetic inferredStep finish-step; workflow-harness threads one JSON-serializable state with two independent cursors (resumeFrom = warm session for the next user turn vs continueFrom = this run's suspended turn), races the turn against an unref'd 750s time-slice timer (Fluid Compute recycles at ~800s), drops `start` on continued slices and intermediate `finish` chunks in favor of one engine-written terminal finish, re-emits missing part-start preludes from the persisted streamContext, and closes the workflow output writable ONLY on finished/awaiting_tool_approval.

- **Dialect in-sandbox bridge plane (pass 25, packages/harness-codex/src/bridge + opencode/claude-code bridge kernels)** — `./harness-codex-bridge-cli-relay-shim.md`, `./harness-codex-bridge-step-tracker.md`, `./harness-dialect-tool-relay-plane.md`, `./harness-claude-code-compaction-latch.md`: a dialect bridge is THIN over the shared runBridge kernel (WebSocket/token/reconnect/seq/disk-replay all live in @ai-sdk/harness/bridge; the dialect file supplies only the turn driver); codex works around its upstream MCP-registration bug with a zero-credential CLI shim written into the model-readable session dir — the shim embeds no auth material, recognition of `node <shim> <tool> '<json>'` invocations is fail-closed (exact argv match, every &&-segment must be a relay command, character-class rejection, bounded shell-eval unwrap), and item.started pre-authorizes each parsed call before the shim's HTTP request can arrive while suppressing the raw bash events from the consumer stream; codex step inference is a two-item-class tracker (model items open, tool items gate closure through a pending-id set, finishTurn force-closes) and text parts open lazily on map-presence with prefix-diff deltas because Codex does not guarantee a started-with-text event; the cross-dialect tool-relay kernel replaces bearer tokens with exact-match pre-authorization (canonical-JSON keys over toolName+input, 10s TTL, FIFO request parking, consume-exactly-once, close-rejects-all) because the calling helper is model-readable — codex mints authorizations by parsing command_execution events and coalesces duplicate in-flight calls, opencode by observing server tool-call announcements deduped per callID behind a local MCP server entry; claude-code's compaction arrives as two unordered halves (compact_boundary system message + PostCompact hook) merged by a reset-on-emit latch into exactly one compaction event per compaction.
- **Opencode bridge plane (pass 26, packages/harness-opencode/src/bridge)** — `./harness-opencode-bridge-turn-settlement.md`, `./harness-opencode-bridge-context-fallback.md`, `./harness-opencode-bridge-permission-plane.md`, `./harness-opencode-bridge-event-envelope.md`, `./harness-opencode-bridge-usage-accounting.md`: the opencode bridge boots a REAL opencode server on 127.0.0.1:0 inside the sandbox (random-password Basic auth minted in-process) and drives it through the official SDK over HTTP+SSE; a turn settles through ONE resolve-once deferred fed by two sources — event-side settlers (structured-output short-circuit, terminal error, busy→idle status transition) and the stream's .finally ('stream-ended') — gated so a submitting or pending steering message can never be orphaned, and the post-settlement ladder throws on stream-ended/terminal error or falls back to re-fetching the runtime's own assistant store; that fallback correlates against a pre-prompt baseline (existed? id?) and fails closed on missing ids so a stale pre-prompt message can never be emitted as this turn's answer; native permission asks are answered from host policy in rung order hard-reject-external-path → filtered-builtin-needs-human → mode-auto-allow → human, with filtered builtins demoted to 'ask' in the generated config so they must ask at all; the event feed is normalized at the boundary (sync-wrapper unwrap, version-suffix strip, properties??data, four-shape session-id walk, per-turn session filter) with set-gated part opening, prefix-contract tail reconciliation, and reasoning-under-field:"text" quirk routing; per-turn usage prefers the clamped session-token delta (before/after, max(0,·) per field) over accumulated finish-step usage, with malformed envelopes degrading to undefined rather than half-parsed records.

- **Dialect bridge remainder plane (pass 27, packages/harness-claude-code/src/bridge + packages/harness-deepagents/src/bridge + opencode compaction operation)** — `./harness-claude-code-bridge-turn-pump.md`, `./harness-claude-code-bridge-emit-translator.md`, `./harness-dialect-json-schema-zod-conversion.md`, `./harness-deepagents-bridge-hitl-loop.md`, `./harness-opencode-bridge-compaction-operation.md`: the claude-code bridge is the third bridge ARCHITECTURE — it drives the Claude Agent SDK in-process, so the "transport" is an async-iterable user-message pump the bridge implements itself: initial prompt first (randomUUID id), steering from the shared queue tagged priority:'next' with its messageId as wire uuid, and each submission reconciled against the runtime's command_lifecycle events (queued/started ⇒ accept, cancelled/discarded ⇒ reject) so a dropped message never silently vanishes; permissions are SDK OPTIONS not event answers — a three-rung split (bypassPermissions only when nothing is gated / mode-mapped canUseTool ladder that announces-then-awaits and echoes toolUseID back on deny / settings ask-rules `${name}(*)` as defense-in-depth with autoAllowBashIfSandboxed forced false), unknown native tools defaulting to 'edit' kind fail-closed; the emit translator keys partial blocks by CONTENT BLOCK INDEX (minted ids, kind-routed deltas, delete-at-stop) and closes steps only through a triple gate (stepOpen && no pending tool ids && no open partials) so steps always contain complete tool round-trips, with a suppression ladder for tools announced through two channels (StructuredOutput internal tool, mcp__harness-tools__ host tools, approval-requested ids) and bash results synthesized as {exitCode: isError?1:0, stdout} because the SDK exposes no real exit code; both claude-code and deepagents convert host JSON Schemas to Zod for their SDKs but DIVERGE by design — the claude-code converter adds const/enum/union rungs before type dispatch with z.any() safe fallbacks at the narrowest node while the deepagents converter stays flat scalars+objects+arrays+nullable, both total (never throw); the deepagents bridge reuses ONE agent per process across turns (MemorySaver checkpointer — LangGraph rejects `true` for root graphs) and runs a while(true) stream→state→decide→Command({resume:{decisions}}) loop where gated calls are announced at APPROVAL time (per-name approvedToolQueue + runId→approvalId map so execution reuses the approval id) and rejections emit their tool-result immediately since they never execute, with builtin filtering as an afterModel middleware that denies inactive builtins IN STATE (error-status ToolMessages) and jumps to the model only when every call was denied, plus PATH-only shell env; the opencode compaction OPERATION side settles through one deferred fed by explicit-ended events, busy→idle (gated on having-seen-busy), and session.error, races it against a 250ms grace after the summarize API returns, degrades model resolution downward (assistant snapshot → session info → start frame, each rung catch-swallowed), and emits a synthetic missingSummary-flagged compaction event only when no real one was seen — exactly one compaction event per operation, always.

- **ACP bridge support plane (pass 28, packages/harness-acp/src/v1/bridge)** — `./harness-acp-stream-capture-sidecar.md`, `./harness-acp-stderr-sentinel-monitor.md`, `./harness-acp-diagnostics-plane.md`, `./harness-acp-instruction-mapping.md`, `./harness-acp-protocol-configuration-envelope.md`, `./harness-acp-emit-stream-facade.md`: the support plane around the pass-20/22 turn machinery — the stream-capture sidecar records every session/update raw value in arrival order BEFORE SDK validation and re-associates validated updates to their raw twins by identity keys (toolCallId/messageId) with prefix-consuming splices, filtering unknown discriminants only from well-formed notifications and flushing everything raw on stream error; the stderr sentinel monitor is a Promise<never> that rejects on one normalized log line ('failed to deserialize responsestreamevent from stream') because an agent can log a decoding failure without ever rejecting its pending JSON-RPC prompt; the diagnostics plane recursively strips `_meta` at every depth for the initialize snapshot (protocolVersion/sessionId/agentInfo/capabilities/authMethods) and wraps the three protocol stages (session initialization / session cancellation / prompt update stream) in fixed-stage errors that always preserve `error.cause` and degrade to stage-only text for message-less causes; instruction mapping delivers host instructions into sessionMeta or a JSON launch-env variable at a declared path with prototype-pollution rejection BEFORE mutation, schema-validated parse of pre-existing config, and error messages that name the variable but never its contents; protocol configuration builds the initialize request (boolean session-config capability merged only when a permission mode actually maps to one), resolves gateway `$source` placeholders into launch env with JSON-stringified non-strings, and enforces EXACT protocol-version match plus advertisement-checked authentication; the bridge environment envelope carries the whole child configuration in ONE env var (`AI_SDK_ACP_BRIDGE_CONFIGURATION`) validated on read by a recursive z.lazy profile-value schema (absent ⇒ {}, invalid ⇒ fixed throw without content echo); and the emit-stream facade composes translator + host-tool correlation with correlation-first routing, a terminal stop short-circuit that closes correlation before finishing the translator, and permission-side tool calls reshaped into ordinary tool_call_update at the door.

- **Dialect auth/bootstrap plane (pass 29, packages/harness-{cline,codex,claude-code,opencode,deepagents,pi,grok-build} auth + bootstrap files)** — `./harness-dialect-auth-resolver-ladder.md`, `./harness-pi-provider-registration.md`, `./harness-claude-code-apikey-helper.md`, `./harness-bootstrap-asset-embedding.md`, `./harness-grok-build-minimal-acp-dialect.md`: the credential and seeding plane around every dialect's doStart — each dialect resolves provider credentials ONCE on the host through a two-function family (resolve*Env env-blob + resolve*AuthenticationMode mode) sharing one skeleton (normalize legacy-object options with a deprecation warn, explicit mode rungs before ambient detection, ambient AI Gateway via getAiGatewayAuthFromEnv incl. VERCEL_OIDC_TOKEN beats ambient direct, explicit selection never silently falls back) while diverging on provider sets (CLINE_*, OPENAI/CODEX_*, ANTHROPIC_*, opencode's model-prefix provider selection + openaiCompatible escape hatch, pi's five modes), gateway base-URL shape (codex+opencode force /v1; deepagents keeps the gateway at its ROOT because the Anthropic SDK appends /v1/messages; claude-code forwards as-is), and codex's direct-mode OPENAI_BASE_URL materialization so the brokered bridge selects its WebSocket-disabled custom provider; pi alone REGISTERS providers (resolve-then-register split, dual-write registerProvider + setRuntimeApiKey so keys live in the runtime auth store, prefix-derived provider names gated on a matching base URL, explicit-mode isolation); claude-code alone adds the apiKeyHelper executable hook from ~/.claude/settings.json as a fail-soft last-resort after explicit auth and static env, filling both ANTHROPIC_API_KEY and ANTHROPIC_AUTH_TOKEN; bootstrap is a per-dialect cached singleton reading bridge assets through two-candidate import.meta.url resolution (ENOENT-only tolerance) into .harness-bootstrap/<name>/ under the sandbox's PERSISTENT default working directory so installs survive detach/attach and snapshot/resume, with deepagents adding a checksum-verified pinned ripgrep install; and grok-build closes the dialect inventory as the MINIMAL ACP dialect — one pure-config createACP call whose only dialect-specific surface is the 25-tool inventory captured from the pinned @xai-official/grok 0.2.111 model requests, _meta['x.ai/tool'].namespace MCP classification, npm-locked source pinning, and an inert-without-key credential-brokering callback.

- **Dialect entry surface (pass 30, packages/harness-*/src/index.ts + version.ts + packages/harness/src/v1/harness-v1.ts + harness-cline/src/cline-resume-state.ts + opencode bridge thin helpers)** — `./harness-dialect-entry-default-instances.md`, `./harness-build-time-version-injection.md`, `./harness-v1-adapter-contract.md`, `./harness-cline-private-history-persistence.md`, `./harness-opencode-loopback-server-auth.md`, `./harness-opencode-event-type-guard-kernel.md`: the outermost layer of every dialect package — each package index.ts is a two-layer export (a module-scope DEFAULT INSTANCE `create<Name>()` with no overrides plus the factory, VERSION, and settings/auth types; the default instance is the documented consumer path used by every harness-e2e-next example, and it is side-effect-free at import because auth/bootstrap/registration all defer to doStart); version.ts is a bundler free-variable constant (`declare const __PACKAGE_VERSION__` + `'0.0.0-test'` fallback, byte-identical across six dialects) that feeds each dialect's `ai-sdk/<package>/<version>` client-app identity, which grok-build splits into {name, version} for gateway `$source: 'client-app-version'` interpolation and whose un-injected fallback is itself pinned by the grok-build snapshot test; the HarnessV1 interface is the portability contract all seven factories satisfy — one literal spec tag modelled after LanguageModelV4, one harnessId used to refuse mismatched lifecycle payloads, builtinTools keyed by `commonName ?? nativeName`, two optional capability booleans with a documented auto-deny fallback, optional lifecycleStateSchema and getBootstrap, and ONE required doStart, with capability discovery by presence/absence plus HarnessCapabilityUnsupportedError instead of any static capabilities object; the one thin file with real mechanisms is cline-resume-state.ts — the private-history persistence kernel that hashes the session id into `<sandboxHome>/.ai-sdk/harness-cline/<sha256>` (neutralizing unsafe ids), rejects a private dir inside the session workspace, re-checks containment on the final file path, enforces a strict .json basename pattern, persists `JSON.stringify(messages)` through writeTextFile, and degrades resume fail-soft (missing or corrupt history returns undefined instead of failing doStart), fully test-pinned in 12 cases; the opencode bridge adds two thin helpers with real contracts — configureOpenCodeServerAuth mints a per-process 256-bit password into the shared env block and returns the matching Basic header in one call (client and server can never diverge; loopback-only surface), prependOpenCodeBinToPath puts the bootstrap node_modules/.bin at the front of PATH with an FHS fallback; and opencode-types.ts owns the asOpenCodeObject non-array-object guard plus all-unknown-optional structural types that every event consumer chains (the pass-26 event-envelope capsule owns the semantics; this capsule owns the guard).

- **Policy plane (pass 31, packages/policy-opa whole — engine-neutral policy layer over toolApproval + LanguageModelV4Middleware)** — `./policy-shadow-rollout-wrapper.md`, `./policy-total-approval-config.md`, `./policy-fail-closed-substrate.md`, `./policy-rego-decision-normalizer.md`, `./policy-capability-scoping-middleware.md`, `./policy-lazy-peer-dependency-clients.md`: the whole package rests on ONE fail-closed substrate — `evaluatePolicy` captures a backend throw as `{ok:false, error}` so a thrown `client.evaluate` never escapes into an SDK callback, and each consumer turns failure into its own safe fallback (deny-with-reason for approvals, `tools: undefined` for capability scoping); `shadow` wraps any approval config to observe-then-enforce (evaluate, report `decision` vs `effective` in a fire-and-forget event whose throwing/slow logger can never affect the returned decision, return `{type:'approved'}` unless `enforce:true` — the rollout ladder), with per-tool-map dispatch through own-property reads so literal `constructor`/`toString`/`valueOf` tool names are unconfigured, never inherited prototype values; `wrapMcpTools` makes an approval config TOTAL over a discovered (MCP) tool surface — `Object.keys(tools)` iteration fills unlisted tools with a fallback default (`user-approval`, or `denied` for hard allowlist mode) into an `Object.create(null)` map, and wraps per-tool FUNCTIONS so a no-opinion result (`undefined`/`'not-applicable'`/`{type:'not-applicable'}`) also falls back instead of running unapproved; `opaPolicy` shapes the OPA input (default `{tool:{name}, args, messages, runtimeContext}` or custom `toInput`) and on backend error returns `{type:'denied', reason:'policy evaluation failed: …'}` so the model reasons over a structured result instead of the run aborting, with `optionalOpaPolicy` returning `undefined` when no client is configured (per-environment policy files degrade to the SDK default); `normalizeOpaDecision` accepts explicit `{decision: allow|deny|requires-approval|not-applicable, reason?}` then legacy `{allow: boolean, reason?}`, everything else (null, primitives, unrecognized shapes) is `not-applicable` — a non-matching Rego rule defaults to no opinion, never to a block; `opaCapabilityMiddleware` narrows `params.tools` to an OPA allowlist BEFORE the model call (function tools by `name`, provider tools by dotted `id` OR bare `name`; strict per-item string validation where one non-string poisons the list to null; identity preserved by reference when nothing was dropped) for defense in depth plus capability disclosure; and the two backends bind optional peer deps lazily — `httpPolicyClient` defers the dynamic import to first `evaluate` (constructor never throws; missing module throws an install-instruction error with the import failure as `cause`), `wasmPolicyClient` loads the compiled bundle in-process, `setData`s bundled documents, and THROWS on an empty/non-array result array (wrong `opa build -e` entrypoint) so callers fail closed while preserving a legitimately falsy `{result: false}`.

## Extending the foundation
Add one `./<seam>.md` capsule for one graph-selected, source-confirmed porting question. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf.

## Provenance
Vercel AI SDK (Apache-2.0), `main@9d9a73f1551f2243035491e9de5a2e00ebf9eb17`; Codebase Memory project `ai` (`$REFERENCE_ROOT/ai`, canonical `$REFERENCE_ROOT/frameworks/ai` — same HEAD through both paths; LIVE-SYMLINK root verified via readlink), full mode, 69,060 nodes / 208,477 edges (pass-13 refresh-in-place @new pin — NO twin adoption needed per [DONE:366] linkedin precedent). ~70 provider packages under `packages/`. Confirm every claim against source — the graph is an index, not truth. Passes 1–4 mined the ai-package kernel (34 capsules); pass 5 (2026-08-23) mined the provider-utils HTTP plane + prepare-step plumbing + prompt standardization/conversion + never-cited util kernels; pass 6 (2026-08-23) mined the download consumer plane, prompt file-data plumbing, and transport-adjacent provider-utils remainder; pass 7 (2026-08-24) mined the React UI plane (`packages/react` hooks + `packages/ai/src/ui`: chat lifecycle, chunk reducer, model-message conversion, transports, useObject/useRealtime/useCompletion) and the RSC generative-UI bridge (`packages/rsc`: streamable value/UI protocols, AI-state ALS delta sync) — +13 capsule-v2, 68 total; pass 8 (2026-08-24) mined the queued ui-plane remainder whole-file at the UNCHANGED pin — validate-ui-messages.ts (584L), ui-messages.ts (640L), the completion-predicate pair, direct/text-stream transports + ChatTransport interface, and both text-stream adapters — +6 capsule-v2, 74 total; pass 9 (2026-08-24) mined the SERVER-side UI-message-stream writer plane (`packages/ai/src/ui-message-stream/` + agent UI-stream entry) whole-file at the UNCHANGED pin — createUIMessageStream writer kernel, chunk wire contract, TextStreamPart converter, metadata adapter, server persistence ladder, snapshot reader, SSE response family, and the agent HTTP entry — +8 capsule-v2, 82 total; pass 10 (2026-08-24, cycle-completion verification at the SAME pin) ran the mechanical uncited-file census over all 486 ai-package files and mined the two genuinely unrecorded seams the census exposed — registry plane (provider-registry.ts + custom-provider.ts) and middleware default/extraction injection (extract-json-middleware.ts + both defaults middlewares) — +4 capsule-v2, 86 total. Pass 11 (2026-08-24, dedicated lane) mined the retrieval-API plane at the UNCHANGED pin — embed/embedMany/rerank orchestrators whole-file plus the uncited support seams (model resolution/version-lifting, embedding middleware wrapper, warning pipeline) — +6 capsule-v2, 92 total. Pass 12 (2026-08-24, dedicated lane drain-lane-ai-sdk) mined the queued named-question #1 GATEWAY PLANE (`packages/gateway/src`, ~5.6k LOC source + 11.5k test lines) whole-package at the UNCHANGED pin — provider factory closure + alias fan-out, two-rung auth ladder with auth-method header tagging, hand-rolled SWR metadata cache, four-rung error funnel (Symbol.for markers, undici timeout detection incl. cause chains, derived retryability), single-endpoint header-carried wire identity, in-place file-part base64 sweep, origin-relative server-only client-secret minting, shared client/server WebSocket subprotocol auth module, realtime identity codec, transcription HTTP+WS duplex pump (finished latch, 64KiB frame splits, error-part-then-wait protocol), origin-split billing GETs with zod snake_case→camelCase boundaries, catalog modelType filter, provider-executed tool factory trio, o11y static/per-request header split, open service-side-validated provider options, zod tree-shake shim + WORKFLOW serialize pair, and the modality-twin family contract — +18 capsule-v2, 110 total; probe battery 54/54 live-executed pre-write; coverage ×18 paths no_recorded_issue; upstream drifted 96 commits behind pin mid-fleet — re-index DEFERRED to avoid stale-twin registration through the /inspo/ai symlink (twin-protocol note recorded); gateway plane squeezed-to-last-drop at current pin alongside kernel+HTTP+React+RSC+ui-plane+writer+retrieval planes. Pass-6 dedup errata: prepare-tools/tool-choice seams already owned by request-tool-assembly.md (pass 4) and parseJsonEventStream/DelayedPromise by event-ingestion-and-tool-names.md + lazy-result-primitives.md — draft duplicates were deleted after parity check; lazy-result-primitives.md received a source-truth correction (delayed-promise.test.ts DOES exist). Pass 13 (2026-08-24, dedicated lane drain-lane-ai-sdk) RE-PIN + DRIFT RE-ENTRY — the pass-12 twin-adoption plan was OVERTURNED BY EVIDENCE: readlink proved `/inspo/ai` is a LIVE SYMLINK into `/inspo/frameworks/ai` (dangling-root precondition absent), so per the [DONE:366] linkedin precedent the repo was ff-pulled d25cae27→9d9a73f (96 upstream commits, 597 files) and `index_repository --repo-path $REFERENCE_ROOT/ai --name ai` refreshed IN PLACE (68,288n/204,996e → 69,060n/208,477e, status indexed; content freshness proven by stdin-JSON check_index_coverage on newly-cited paths = no_recorded_issue + generation_matches true, generation 2026-08-24T05:06Z). Mined the drift wave as ~14 porting seams → +19 capsule-v2, 110→129: ai-core tool-execution safety gate (`isToolExecutionAllowedFinishReason`, #19066) + buffered stream-time execution (#19066/#19052), uniform callback containment via notify (#19187), cross-step part-id remap (#19269), streamObject terminal-error latch + eager promise rejection (#18934/#19187), preliminary tool-output suppression in history conversion (#19084), async video start/status lifecycle with outside-retry idempotency minting (#19263), webhook-vs-polling arbitration via optional handleWebhookOption capability (#19232), gateway batch trio with header-not-body idempotency + abort preservation + NDJSON incremental parser + all-or-nothing requestCounts (#19032), MCP x-mcp-header parameter bindings (#19292 family), MCP protocol-era discovery (#19026), era-gated streamable-HTTP transport (#19029), Workflow resume index-space separation (#19109), UI-stream frame-repair normalizer, RSC concurrent functional state writes (#19249), harness builtin-approval replay split (#19160) + Claude-Code usage precedence flip (#19070), empty-toolcall-id falsy coercion family (#19091), Bedrock event-stream fail-loud framing (#18996/#19040), and JSON-Schema additionalProperties schema preservation (#18046). Probe battery v2 39/39 GREEN byte-exact pre-write from repo root (v1 ran 33/39 — all 6 failures were MY probe defects: alternation escaping, backtick needles, closure-local symbols; repaired by re-derivation, never by touching expectations); retrieves live-resolved ×14 line-exact on project ai @9d9a73f. SOURCE-WINS ERRATA on two existing capsules: #19052 changed the step-loop continuation predicate in BOTH generateText (:1441–1449) and streamText (~:2503–2515) from `(all-accounted OR pendingDeferred)` to AND-form hard-precondition — generate-text-orchestrator.md + stream-continuation-engine.md decisive excerpts replaced with new source + PASS-13 ERRATUM notes instead of trusting stale pins. Pass 14 (2026-08-24, dedicated lane drain-lane-ai-sdk) mined BOTH remaining recorded named-question targets at the UNCHANGED pin `9d9a73f` (fetch-first: local HEAD == origin/main == pin, zero drift; graph ready 69,060n/208,477e @generation 2026-08-24T05:06Z, coverage stdin-JSON over all 12 newly-cited paths = no_recorded_issue + metadata_match + generation_matches true) → +16 capsule-v2, 129→145: the MCP Apps HOST plane (`packages/react/src/mcp-apps` whole ~1.7k lines incl. tests + the `@ai-sdk/mcp` server-side app-resource helpers `packages/mcp/src/tool/mcp-apps.ts`/`mcp-app-fingerprint.ts`) — queue-until-initialized bridge notification gate with sandbox-notification bypass, double deny-by-default tools/call (no handler ⇒ error; omitted allowedTools ⇒ zero visible tools), per-method request validation ladder (ui:// scope, parsed-URL https/http/mailto scheme checks, display-mode union), CSP source canonicalization through URL-decode-recheck inside a fixed 9-directive lockdown policy, permission intersection (server-requested × host-allowed, hierarchical allow delegation), proxy-pull frame lifecycle (sandbox-proxy-ready bootstrap, concrete-origin derivation never '*', teardown-request-before-close, ref-mirrored input/output replay at init), exact-MIME+ui:// tool-part metadata gate with URI-keyed identity pinning against streaming churn, server-side identity-throws/embellishment-degrades normalization split with legacy ui/resourceUri fallback and text/blob html decode, and canonicalJSON SHA-256 resource drift fingerprint deliberately duplicated across packages per dependency-direction hygiene — plus the REALTIME browser-session plane (`packages/ai/src/realtime` whole ~1.9k lines incl. tests): AbstractRealtimeSession token-endpoint bootstrap with context-before-socket ordering and reducer-owned per-key state application via the abstract setState seam, single-followup response-create conjunction gate (calls-closed AND all-submitted), measure-before-stop barge-in truncation with rounded audioEndMs, asymmetric-clamp PCM16 codec kernel with 32KiB-chunked base64 and clock-chained gapless playback scheduling, self-catching sendQueue transport with synchronous socket tracking + identity guards + pre-parse health-check interception, in-place part-location reducer with empty-name dynamic-tool seeding / parse-failure-as-effect / position-recorded late transcriptions, and function/dynamic-only realtime tool definitions with fail-loud exhaustiveness. Probe battery 58/58 GREEN byte-exact pre-write (/tmp/aisdk_p14_battery.py, lane-suffixed; v1 ran 51/57 — all failures were MY probe defects: miscounted occurrences, off-by-one line anchors, unquoted multi-word patterns; repaired by re-derivation against source). Retrieves live-resolved pre-write for every capsule (16 queries; two tightened to hit exact symbols). Parity disk=loader=map 145=145=145 BIDIRECTIONAL set-equality all-v2 line-1 (/tmp/aisdk_p14_parity.py). Boundaries updated: the two standing omissions ("AbstractRealtimeSession WebAudio internals", "sandbox implementations") RETRACTED as now-capsuled. OMITTED-WITH-REASON pass 14: `packages/react/src/use-realtime.ts` hook wrapper (React binding over the already-capsuled realtime-hook-store — thin surface), `packages/gateway/src/gateway-realtime-model.ts` serializer bodies (gateway-plane twin already covered by pass-12 identity-codec contract), mcp-apps.test.ts/app-frame.test.tsx bodies beyond cited probe anchors (test twins of mined contracts). Pass 15 (2026-08-24, dedicated lane drain-lane-ai-sdk) ran the citation-vs-inventory grep (every leaf ref's `packages/<name>/` citations vs the on-disk package list) at the UNCHANGED pin — fetch-first zero drift, graph ready 69,060n/208,477e @generation 2026-08-24T05:06Z generation_matches true — and found `packages/code-mode` (~3.9k LOC source+tests, QuickJS-backed sandboxed code execution) wholly uncited with no standing omission; mined it WHOLE top-to-bottom → +17 capsule-v2, 145→162; every other zero-citation package is a concrete provider dialect or carries an explicit standing omission (otel telemetry dispatcher bodies, devtools viewer UI, sandbox-* hosts, tui/angular/svelte/vue bindings). Coverage stdin-JSON ×17 newly-cited paths = ALL no_recorded_issue + metadata_match + generation_matches true. Retrieves live-resolved pre-write for every capsule family (6 queries, all rank#1 line-exact @pin; closure-local symbols documented graph-invisible per precedent). Probe battery executed byte-exact PRE-write from repo root across all 17 new capsules — v1 caught 2 MY-side defects (copyStack occurrence count 7 not 6; serialization anchor pattern needed `valueJson === '` prefix form), repaired by re-derivation against source before any expectation was pinned; post-write spot re-runs green. Pass 16 (2026-08-24) mined the OpenTelemetry integration plane (`packages/otel` whole, ~11.8k LOC src+tests — the largest package with zero prior citations; census-driven pick per pass-10 precedent after fetch-first zero drift) — +10 capsule-v2, 172 total.

Passes 17–18 (2026-08-25/26, same pin `9d9a73f1551f`): pass 17 mined the harness agent kernel (`packages/harness/src/agent` whole-file) into seven capsule-v2 references; pass 18 mined the harness bridge transport plane (`bridge/index.ts` 951L, `utils/sandbox-channel.ts` 526L, `utils/bridge-user-message-submitter.ts`) into seven more — both passes re-verified zero drift against the pin via `index_status(verbose)` git block (head_sha == base_sha == checkout HEAD); origin/main drift (+55 commits, e21bde7) stays DEFERRED per pass-12 precedent. Pass-18 coverage: check_index_coverage no_recorded_issue ×7 cited paths @generation 2026-08-24T14:04:21Z; direct tests read whole-file (index.test.ts 601L/15 cases, sandbox-channel.test.ts 485L/16 cases, reconnect.integration.test.ts, disk-replay.integration.test.ts). Pass 18 also repaired pass-17 parity debt (missing Capsule-map group).

Pass 19 (2026-08-26, same pin `9d9a73f1551f`): mined pass-18 next-pass target #3 — the harness result-object + turn-telemetry plane (`agent/internal/harness-stream-text-result.ts` 964L whole-file + `turn-telemetry.ts` 489L whole-file, drive sites in run-prompt.ts :108–187/:320–479/:600–714/:1005–1059) into seven capsule-v2 references. Pin re-verified live (checkout HEAD == graph head_sha == base_sha == pin; origin/main drift e21bde7 stays deferred). Coverage: check_index_coverage no_recorded_issue ×2 source paths @generation 2026-08-24T14:04:21Z; coverage caveats — NO dedicated harness-stream-text-result.test.ts exists (freshness=missing; behavioral coverage via run-prompt.test.ts :655–769/:2005–2086), turn-telemetry.test.ts is ONE step-numbering test (54L). Direct tests read: turn-telemetry.test.ts whole, run-prompt.test.ts ranges. Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial query "fake streamText result for a remote coding agent session" MISSES HarnessStreamTextResult entirely (test fixtures only — observed RED); GREEN vocabulary queries rank discardCurrentStepContent :240–244 #1, createOutputTransformStream/finishStep/constructor line-exact. Leaf now carries 193 references / 193 capsule-v2 (loader lines == disk refs verified this pass).

Pass 20–21 (2026-08-26, same pin): mined the ACP v1 lifecycle/session plane (`acp-v1-harness.ts` 1753L + lifecycle/turn-start-config/session-lifecycle/recovered-session kernels + bridge upper half) into six capsule-v2 references. Pass 21 was an interrupted-predecessor reconciliation whose evidence chains were independently re-executed and recorded; its SKILL.md wiring (loader lines, map group, provenance, Full-view block) was subsequently LOST to a stale overwrite — discovered and repaired during pass 22 by re-verifying pin identity (graph head==base==checkout HEAD==pin, porcelain clean) and re-wiring all six entries verbatim.

Pass 22 (2026-08-26, same pin `9d9a73f1551f`): mined pass-21 next-pass target #2 — the ACP bridge internals plane (`stream-translator.ts` 948L, `host-tool-relay.ts` 510L, `host-tool-mcp.ts` 185L + `-server.ts` 180L, `host-tool-relay-client.ts` 53L, `refresh-host-tool-catalog.ts` 33L, `host-tool-correlation.ts` 640L, `permission-controller.ts` 152L + `permission-mode.ts` 163L, `acp-auth.ts` 254L + `profile-values.ts` 97L, bridge/index.ts lower half :368–877, acp-v1-harness.ts doStart :225–374) into six capsule-v2 references. Every chain independently executed at pin: search_graph family queries rank-resolved each seam; trace_path callers_total=2 for createACPStreamTranslator/createHostToolCorrelation (createEmitStreamEvent+runTurn) and configureACPPermissionMode (ensureSession+runTurn); get_code_snippet createBlockId byte-matched. Coverage: ONE check_index_coverage over all 22 cited paths → no_recorded_issue+metadata_match @generation 2026-08-24T14:04:21Z, generation_matches true, index_mode full. Direct tests read whole-file: stream-translator.test.ts 1303L, host-tool-relay.test.ts 439L, host-tool-mcp-server.test.ts 234L (real InMemoryTransport pair), host-tool-relay-client.test.ts 70L, refresh-host-tool-catalog.test.ts 84L, host-tool-correlation.test.ts 647L, permission-controller.test.ts 307L, permission-mode.test.ts 352L, plus acp-harness.test.ts :482–645 brokering ranges. Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed ("how do I show tool calls from an external agent in my chat UI" returns ONLY UI/examples symbols — zero bridge-plane hits); GREEN = all six capsule Retrieve queries resolved cited symbols with line-exact ranges. Leaf now carries 205 references / 205 capsule-v2. Pass 23 (2026-08-27, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md, recorded in the work record): created the CANONICAL host-neutral leaf at `~/.agents/skills/ai-foundation` by verbatim migration of the DSH host mirror's SKILL.md + all 205 reference files (fleet convention: canonical catalog carries full content; mirror left untouched), then mined the sibling dialect adapter plane — the four recorded wireTurn twins read whole-file (`opencode-harness.ts` 1277L, `codex-harness.ts` 1268L, `claude-code-harness.ts` 1980L, `deepagents-harness.ts` 941L) plus their test suites (734L/639L/1034L/409L) into five capsule-v2 references: the invariant turn-pump core replicated across all four dialects, opencode's out-of-turn compaction rail, codex's CLI-shim host-tool plane + deferred start frame, claude-code's deadline-bounded WebSocket hello retry ladder + /compact-over-user-message-rail, and deepagents' instructions-once attach semantics. Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I run Claude Code through my agent" query hits only docs/examples symbols, zero dialect-adapter hits without capsule vocabulary); GREEN = every capsule's cited symbol line-exact against checkout HEAD. Leaf now carries 210 references / 210 capsule-v2. Pass 24 (2026-08-27, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined pass-23 next-pass target #2 — the remaining sibling dialects. Whole-file reads: cline-harness.ts 197L, cline-session.ts 963L, cline-translate.ts 261L, cline-remote-ops.ts 421L, cline-resume-state.ts 139L, pi-harness.ts 178L, pi-session.ts 1813L, pi-workspace-vfs.ts 437L, pi-workspace-mirror.ts 367L, pi-translate.ts 413L, pi-resume-state.ts 160L, run-harness-agent.ts 609L, harness-workflow-state.ts 153L + slice/step/time-slice wrappers; grok-build-harness.ts 357L read whole but deliberately NOT capsuled (pure-config ACP dialect whose load-bearing mechanisms are already documented in the pass-20/22 ACP capsules — carried as a named next-pass target). Direct tests read: cline-session.test.ts 916L, cline-remote-ops.test.ts 419L, cline-translate.test.ts 696L, pi-session.test.ts 1380L, pi-workspace-vfs.test.ts 117L, pi-workspace-mirror.test.ts 453L, pi-translate.test.ts 640L, run-harness-agent-slice.test.ts 728L, grok-build-harness.test.ts 223L + test-d.ts 36L. Five new capsule-v2 references: the in-process parked-session continuation split (cline), two-tier sandbox remote-ops containment (cline), the scoped host mirror + global fs VFS (pi), the dangling-host-tool rerun barrier (pi), and the workflow-harness durable-step engine (two-cursor state, 750s time slice, single-terminal-finish stream discipline, close-only-on-terminal writable rule). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I keep an agent turn alive when my process dies" query hits only bridge/replay symbols — zero in-process-dialect or workflow-engine hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD. Coverage caveats: NO direct test pins cline doDetach/doSuspendTurn/parkedClineSessions (deterministic-read-only); pi and workflow-harness planes fully test-pinned. Leaf now carries 215 references / 215 capsule-v2. Pass 25 (2026-08-28, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined state.md next-pass target #2 — the dialect in-sandbox bridge sides. Whole-file reads: harness-codex/src/bridge/index.ts 327L, cli-relay.ts 268L, codex-step-tracker.ts 86L, create-emit-stream-event.ts 289L, tool-relay.ts 119L, tool-relay-auth.ts 156L; harness-opencode/src/bridge/tool-relay.ts 107L + tool-relay-auth.ts 124L (diff vs codex twins: codex adds only ToolRelayPendingCalls + ToolRelayResult); harness-claude-code/src/bridge/compaction-latch.ts 62L. Ranges: opencode index.ts :150–290/:1140–1200/:1340–1370 (relay wiring + announcement authorization), claude-code index.ts :290–430 + create-emit-stream-event.ts :174–186 (latch wiring). Direct tests read whole-file: cli-relay.test.ts 87L, codex-step-tracker.test.ts 136L, create-emit-stream-event.test.ts 248L, tool-relay.test.ts ×2 (100L each), tool-relay-auth.test.ts codex 208L + opencode 129L, index.test.ts codex 291L, compaction-latch.test.ts 86L. Four new capsule-v2 references: the zero-credential CLI-relay shim with fail-closed command recognition (codex), the two-item-class step tracker + lazy-open prefix-diff text translation (codex), the cross-dialect exact-match pre-authorization tool-relay kernel (opencode+codex twins), and the two-half reset-on-emit compaction latch (claude-code). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I let a sandboxed agent call my host tools safely" query hits only ACP relay symbols — zero dialect-bridge hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD. Coverage caveats: NO test drives a full relay-command event through codex runTurn (suppression path deterministic-read-only); opencode announcement→authorization wiring deterministic-read-only; claude-code latch wiring sites deterministic-read-only (latch kernel fully test-pinned). Leaf now carries 219 references / 219 capsule-v2. Pass 26 (2026-08-28, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined state.md next-pass target #2 — the opencode bridge plane whole. Whole-file reads: harness-opencode/src/bridge/index.ts 1512L, create-emit-stream-event.ts 588L, opencode-events.ts 315L, opencode-usage.ts 168L, opencode-context-fallback.ts 25L, opencode-finish-step.ts 47L, opencode-path.ts 17L, opencode-server-auth.ts 15L, opencode-types.ts 33L. Direct tests read whole-file: index.test.ts 138L (1 case), create-emit-stream-event.test.ts 399L (7 cases), opencode-events.test.ts 265L (10 cases), opencode-usage.test.ts 66L (3 cases), opencode-context-fallback.test.ts 44L (3 cases), opencode-finish-step.test.ts 53L (3 cases), opencode-path.test.ts 29L (2 cases), opencode-server-auth.test.ts 27L (2 cases). Five new capsule-v2 references: the dual-settler turn protocol with steering gate and post-settlement throw ladder, the baseline-correlated context fallback with fail-closed id check, the two-sided permission plane (config-side 'ask' demotion + event-side rung ladder with external-path hard reject), the boundary event-envelope normalization (sync unwrap, version-suffix strip, four-shape session filter, prefix-contract tail reconciliation, reasoning-under-field:"text" quirk routing), and the clamped session-delta usage accounting with step-accumulation fallback. Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I know when my agent turn is done" query hits only streamText/stop-condition symbols — zero opencode-bridge hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD. Coverage caveats: permission ladder + runPrompt settlement gates + context-fallback/usage wiring sites deterministic-read-only (kernels fully test-pinned; index.test.ts pins one full path). Leaf now carries 224 references / 224 capsule-v2. Pass 27 (2026-08-28, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined state.md next-pass targets #2 (claude-code bridge remainder — top recorded target), #3 (deepagents bridge), and #4 (opencode compaction operation side). Whole-file reads: harness-claude-code/src/bridge/index.ts 679L, create-emit-stream-event.ts 483L, json-schema-to-zod.ts 141L, tool-filtering.ts 58L; harness-deepagents/src/bridge/index.ts 422L, create-emit-stream-event.ts 345L, approvals.ts 92L, local-shell-backend.ts 19L, tool-filtering.ts 129L, json-schema-to-zod.ts 64L. Ranges: opencode index.ts :755–853 (runCompaction), :1380–1450 (resolveCompactionModel + modelRefFrom* helpers), :445–456 (legacySessionSummarize), :140–163 (runTurn operation branch). Direct tests read whole-file: claude-code index.test.ts 301L (6 cases), create-emit-stream-event.test.ts 468L (6 cases incl. subagent fixture stream), json-schema-to-zod.test.ts 291L (10 cases), tool-filtering.test.ts 42L (6 cases); deepagents index.test.ts 190L (3 cases), create-emit-stream-event.test.ts 278L (5 cases), approvals.test.ts 151L (14 cases), local-shell-backend.test.ts 54L (2 cases), tool-filtering.test.ts 252L (5 cases), json-schema-to-zod.test.ts 81L (6 cases). Five new capsule-v2 references: the async-iterable user-message pump with command_lifecycle reconciliation + three-rung permission-options plane (claude-code turn pump), the index-keyed partial-block state machine + triple-gate step closure + two-channel suppression ladder (claude-code emit translator), the cross-dialect total JSON-Schema→Zod conversion contract with divergent converter depth (claude-code superset vs deepagents flat), the one-agent-per-process HITL interrupt loop with announce-at-approval-time id reuse + afterModel deny-in-state filtering middleware (deepagents), and the one-deferred settle race with 250ms grace + degrade-downward model ladder + synthetic missingSummary emission (opencode compaction operation). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I let my agent ask the user before running a tool" query hits only approval-round-trip/UI symbols — zero dialect-bridge hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD. Coverage caveats: claude-code permission-ladder wiring + deepagents HITL-loop wiring + opencode runCompaction operation side deterministic-read-only (all kernels they feed fully test-pinned; opencode event side pinned at create-emit-stream-event.test.ts :343–377). Leaf now carries 229 references / 229 capsule-v2.

Pass 28 (2026-08-29, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md, recorded in the work record): first RECONCILED the interrupted pass-27 delivery (leaf/records were complete at 229 refs but the ledger row still said pass 26/224 and the OpenViking sync had never run — both repaired this pass), then mined the ACP bridge support plane (state.md targets #3 diagnostics + #4 glue stragglers). Whole-file reads: harness-acp/src/v1/bridge/acp-stream-capture.ts 152L, agent-stderr-monitor.ts 52L, acp-diagnostics.ts 83L, instruction-mapping.ts 132L, protocol-configuration.ts 146L, acp-v1-bridge-environment.ts 99L, create-emit-stream-event.ts 104L. Ranges: bridge/index.ts :120–145/:285–300/:370–470/:530–545/:570–680 (runTurn stage wiring, monitor requirement, capture consumption, initialize/auth sequence, MCP capability gate); acp-v1-harness.ts :540–565/:730–745 (parent-side envelope serialization + gateway env resolution). Direct tests read whole-file: acp-stream-capture.test.ts 178L (5 cases), agent-stderr-monitor.test.ts 25L (1 case), acp-diagnostics.test.ts 111L (5 cases), instruction-mapping.test.ts 112L (5 cases), protocol-configuration.test.ts 171L (6 cases), acp-v1-bridge-environment.test.ts 92L (5 cases). Six new capsule-v2 references: the raw-fidelity stream-capture sidecar with identity-keyed prefix-consuming re-association, the stderr sentinel monitor as Promise<never>, the _meta-stripping diagnostics plane + fixed-stage error wrapper, the fail-closed two-channel instruction mapping, the exact-match protocol negotiation + one-env-var zod bridge-configuration envelope, and the translator+correlation composition facade (closes the pass-22 assumed-seam gap). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I capture raw protocol messages my SDK strips" query hits only stream-translator/UI symbols — zero support-plane hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD. Coverage caveats: the emit-stream facade is deterministic-read-only (its two composed kernels are fully test-pinned from pass 22); stderr monitor partial-line flush + pump-error branches deterministic-read-only; diagnostics snapshot once-per-fingerprint emission deterministic-read-only; instruction-mapping pre-spawn wiring order deterministic-read-only. Leaf now carries 235 references / 235 capsule-v2. Pass 29 (2026-08-29, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session (stdio env ref CBM_CACHE_DIR unavailable) → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined state.md next-pass targets #2 (grok-build minimal-ACP-dialect) and #4 (in-process dialect auth/bootstrap files). Whole-file reads: grok-build-harness.ts 357L + grok-build-harness.test.ts 223L + index.ts 10L, cline-auth.ts 40L, pi-auth.ts 492L, opencode-auth.ts 314L, codex-auth.ts 201L, claude-code-auth.ts 232L, deepagents-auth.ts 170L, claude-code-bootstrap.ts 69L, codex-bootstrap.ts 60L, opencode-bootstrap.ts 58L, deepagents-bootstrap.ts 87L, sandbox-credential-brokering.ts 33L + harness-v1-bootstrap.ts 47L + harness-v1.ts getBootstrap :80–95. Ranges: claude-code-harness.ts :820–880, codex-harness.ts :230–300, opencode-harness.ts :265–300, deepagents-harness.ts :225–260, cline-harness.ts :145–175, pi-session.ts :400–430, acp-v1-harness.ts :130–145/:246–270. Direct tests read whole-file: cline-auth.test.ts 88L (6), codex-auth.test.ts 212L (17), claude-code-auth.test.ts 249L (19), opencode-auth.test.ts 238L (13), deepagents-auth.test.ts 188L (13), pi-auth.test.ts 494L (18), sandbox-credential-brokering.test.ts 128L (8). Five new capsule-v2 references: the dialect auth resolver ladder (two-function resolve*Env/resolve*AuthenticationMode family, normalize-then-ladder, ambient-gateway-over-direct, explicit-no-fallback, per-runtime /v1-vs-root base-URL divergence, codex OPENAI_BASE_URL materialization), pi provider registration (resolve-then-register, dual-write setRuntimeApiKey, prefix-derived names), claude-code apiKeyHelper mirror (fail-soft executable hook, fill-both-slots, injectable seam), bootstrap asset embedding (cached singletons, two-candidate import.meta.url resolution, persistent-mount placement, deepagents checksum-verified ripgrep), and the grok-build minimal ACP dialect (pure-config createACP, four irreducible dialect decisions, snapshot-as-contract test). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "auth"/"register provider"/"bootstrap"/"grok" queries localize nothing among the dialect auth files without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD. Coverage caveats: NO dedicated bootstrap test file exists in any dialect package (deterministic-read-only, behavior pinned indirectly via pass-14 marker-idempotency tests); opencode splitOpenCodeModel wire-format helper deterministic-read-only. Leaf now carries 240 references / 240 capsule-v2. Pass 30 (2026-08-29, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session (stdio env ref CBM_CACHE_DIR unavailable) → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined the dialect ENTRY SURFACE — the dedicated question named by state.md target #5's escape hatch, closing the dialect inventory at this pin. Whole-file reads: all seven package index.ts files (harness-cline 13L, harness-codex 13L, harness-pi 12L, harness-opencode 15L, harness-claude-code 17L, harness-deepagents 15L, harness-grok-build 10L), version.ts family (6L ×7, md5-verified byte-identical across six dialects), packages/harness/src/v1/harness-v1.ts :1–91 (HarnessV1 contract whole), harness-v1-lifecycle-state.ts :1–60 (resume/continue payload types), harness-cline/src/cline-resume-state.ts 139L whole + cline-resume-state.test.ts (12 cases), harness-opencode/src/bridge/opencode-path.ts 17L + opencode-path.test.ts 29L, opencode-server-auth.ts 15L + opencode-server-auth.test.ts 27L, opencode-types.ts 33L. Ranges: cline-harness.ts :140–200 (createCline + lifecycleStateSchema + resume forward), cline-session.ts :240–400 (private-dir resolution, resume pull, persistHistory, lifecycle data stamping :791/:891/:919/:957), bridge/index.ts :100–220 (PATH prepend + server-auth + client pinning call sites), grok-build-harness.ts :285–345 (client-app split + gateway interpolation), examples/harness-e2e-next/agent/harness/{claude-code,pi}/basic-agent.ts (default-instance consumer path). Six new capsule-v2 references: the dialect entry default-instance two-layer export (side-effect-free at import, factory + types alongside), the build-time version injection free-variable with testable '0.0.0-test' fallback feeding ai-sdk/<package>/<version> client identities (grok-build splits into {name, version}), the HarnessV1 adapter contract (tagged spec version, one doStart, capability-by-absence + HarnessCapabilityUnsupportedError, harnessId-refused lifecycle payloads), the cline private-history persistence kernel (sha256 session key neutralizing unsafe ids, outside-workdir dir rejection, re-checked file containment, strict basename pattern, fail-soft resume pull — previously mislabeled 'schema-only', disproved by direct read), the opencode loopback server auth (mint-into-shared-env + derive-header-in-one-call, per-process 256-bit password, loopback-only surface), and the opencode event type-guard kernel (asOpenCodeObject non-array-object guard + all-unknown-optional structural types). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive 'default harness instance'/'package version'/'private session state' queries hit only factory/auth/bootstrap symbols — zero entry-surface hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD (safeClineHistoryFileName :12, resolveClinePrivateSessionDirectory :41, configureOpenCodeServerAuth :3, prependOpenCodeBinToPath :6, asOpenCodeObject :29, __PACKAGE_VERSION__ :2). Coverage caveats: no dedicated test imports the default instances (exercised via e2e examples, not unit suite); no dedicated opencode-types.test.ts (6-line kernel pinned through consumer suites); bridge server-auth integration path binds real sockets (excluded upstream) — helpers unit-pinned. Leaf now carries 246 references / 246 capsule-v2. Pass 31 (2026-08-29, pin `9d9a73f1551f` UNCHANGED — `git rev-parse HEAD` byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session (stdio env ref CBM_CACHE_DIR unavailable) → direct source+test reading fallback per AGENTS.md, recorded in the work record): mined `packages/policy-opa` WHOLE (874L src + 1464L tests — the largest wholly-uncited non-provider package; census: codemod is a jscodeshift migration library [standing omission], open-responses is a provider dialect [pass-15 precedent], valibot is a 17L schema adapter, test-server/sandbox-*/devtools carry standing omissions). Whole-file reads: shadow.ts 189L, wrap-mcp-tools.ts 122L, opa-policy.ts 160L, evaluate-policy.ts 24L, normalize-opa-decision.ts 56L, http-policy-client.ts 57L, wasm-policy-client.ts 78L, opa-capability-middleware.ts 130L, policy-client.ts 20L, policy-decision.ts 13L, index.ts 19L, test-helpers.ts 14L. Direct tests read whole-file: shadow.test.ts 230L (10 cases), wrap-mcp-tools.test.ts 306L (16 cases), opa-policy.test.ts 234L (9 cases), normalize-opa-decision.test.ts 82L (13 cases), http-policy-client.test.ts 40L (3), wasm-policy-client.test.ts 27L (2), wasm-policy-client.evaluate.test.ts 49L (4), opa-capability-middleware.test.ts 230L (10), opa-policy.integration.test.ts 266L (4 full-generateText cases). Six new capsule-v2 references: the shadow observe-then-enforce rollout wrapper (fire-and-forget audit event that can never affect the returned decision), the total-approval-config filler over discovered tool surfaces (Object.keys(tools) totality + prototype-safe reads + per-tool-function no-opinion shims), the fail-closed evaluatePolicy substrate + opaPolicy deny-with-reason adapter (+optionalOpaPolicy undefined-when-unconfigured), the two-convention Rego decision normalizer (explicit → legacy boolean → not-applicable), the capability-scoping transformParams middleware (allowlist narrowing before the model call, fail-closed to tools:undefined, identity preservation), and the lazy peer-dependency clients (deferred dynamic import with actionable install errors + cause preservation; WASM empty-result throw preserving falsy results). Deterministic Gate-5 battery (no node_modules → vitest unrunnable): adversarial RED observed (naive "how do I gate my agent's tools with a policy" query hits only approval-round-trip/tool-approval SDK symbols — zero policy-opa hits without capsule vocabulary); GREEN = every cited symbol line-exact against checkout HEAD (shadow :74, wrapMcpTools :51, evaluatePolicy :14, opaPolicy :45, normalizeOpaDecision :19, opaCapabilityMiddleware :55, httpPolicyClient :17, wasmPolicyClient :28). Coverage caveats: none for the six kernels (all fully test-pinned, 71 cases total incl. the integration suite); the real HTTP/WASM round-trip is covered upstream only by the integration-suite pattern, not unit runs. Leaf now carries 252 references / 252 capsule-v2.

## Full view (memory graph)
Revalidate `ai` before porting: run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, and `get_code_snippet`. PASS-31 LIVE STATE (2026-08-29, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session (stdio env ref CBM_CACHE_DIR unavailable) → direct source+test reading fallback per AGENTS.md (graph revalidation stays a carried next-pass target, now nine passes deep); last recorded graph state remains pass-22's (ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Pass-31 direct reads line-exact @pin: `packages/policy-opa/src/shadow.ts` shadow :74–127 / PolicyDecisionEvent :29–52 / evaluateApproval :129–170 / normalizePolicyDecision :172–189, `wrap-mcp-tools.ts` wrapMcpTools :51–113 / isNotApplicable :115–123 / WrappedMcpTools :9–17, `opa/opa-policy.ts` opaPolicy :45–90 / optionalOpaPolicy :132–160 / errorMessage :92–105, `opa/evaluate-policy.ts` EvaluateOutcome :3–6 / evaluatePolicy :14–24, `opa/normalize-opa-decision.ts` normalizeOpaDecision :19–49 / withReason :51–56, `opa/http-policy-client.ts` httpPolicyClient :17–57 / getUnderlying :34–50, `opa/wasm-policy-client.ts` wasmPolicyClient :28–78 / evaluate :57–77, `opa/opa-capability-middleware.ts` opaCapabilityMiddleware :55–115 / extractAllowedNameSet :117–131, `policy-client.ts` PolicyClient :8–20, `policy-decision.ts` PolicyDecision :4–13; direct tests whole-file: shadow.test.ts 230L (10), wrap-mcp-tools.test.ts 306L (16), opa-policy.test.ts 234L (9), normalize-opa-decision.test.ts 82L (13), http-policy-client.test.ts 40L (3), wasm-policy-client.test.ts 27L (2), wasm-policy-client.evaluate.test.ts 49L (4), opa-capability-middleware.test.ts 230L (10), opa-policy.integration.test.ts 266L (4 full-generateText cases). Adversarial RED observed: naive "how do I gate my agent's tools with a policy" query hits only approval-round-trip/tool-approval SDK symbols — zero policy-opa hits without capsule vocabulary. Leaf at 252 references / 252 wired / 252 v2. PASS-30 LIVE STATE (2026-08-28, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md (graph revalidation of the entry-surface files stays a next-pass target, now eight passes deep); last recorded graph state remains pass-22's (ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Leaf at 246 references / 246 wired / 246 v2. PASS-27 LIVE STATE PASS-27 LIVE STATE (2026-08-28, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md (graph revalidation of the newly cited claude-code/deepagents bridge files stays a next-pass target, now five passes deep); last recorded graph state remains pass-22's (ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Pass-27 direct reads line-exact @pin: `harness-claude-code/src/bridge/index.ts` runTurn :250–505 / createPermissionOptions :139–212 / createPermissionSettings :214–238 / nativeToolRequiresApproval :240–248 / NATIVE_TOOL_KINDS :79–107 / host-tool MCP construction :266–307 / query options :339–390 / terminal-error discipline :393–407 / result handling :430–483 / createQueryInput :507–630 (pump next() :589–625, handleLifecycle :558–576) / addUsage :632–659, `create-emit-stream-event.ts` createEmitStreamEvent :95–302 (stream-start :114–134, api_retry :136–149, subagent gate :190–195, assistant tool_use :202–248, user tool_result :250–300) / finishApprovalStep :304–316 / emitFinishStep :318–336 / closeStepIfReady :338–353 / handleStreamEvent :374–429 / mapUsage :453–476, `json-schema-to-zod.ts` jsonSchemaToZodShape :20–23 / toZodType :40–52 / zodForUnion :93–103, `tool-filtering.ts` resolveNativeTools :40–45 / resolveInactiveNativeTools :47–57; `harness-deepagents/src/bridge/index.ts` buildModel :53–82 / createReasoningMiddleware :84–118 / responseFormatMiddleware :147–157 / buildHostTools :160–184 / runTurn :186–383 (agent construction :199–253, config :268–275, readPendingApprovals :278–289, HITL loop :296–370) / loadMcpTools :396–415, `approvals.ts` buildInterruptOn :52–75 / collectActionRequests :81–92, `tool-filtering.ts` createBuiltinToolFilteringMiddleware :56–129 (hook :67–126), `local-shell-backend.ts` whole 19L, `json-schema-to-zod.ts` jsonSchemaToZodObject :13–17 / toZodType :30–63, opencode `index.ts` runTurn operation branch :148–149 / runCompaction :763–853 / legacySessionSummarize :445–456 / resolveCompactionModel :1393–1415 / modelRefFrom* :1417–1456 / sleep :1375–1377; direct tests whole-file: claude-code index.test.ts 301L, create-emit-stream-event.test.ts 468L, json-schema-to-zod.test.ts 291L, tool-filtering.test.ts 42L; deepagents index.test.ts 190L, create-emit-stream-event.test.ts 278L, approvals.test.ts 151L, local-shell-backend.test.ts 54L, tool-filtering.test.ts 252L, json-schema-to-zod.test.ts 81L. Adversarial RED observed: naive "how do I let my agent ask the user before running a tool" query hits only approval-round-trip/UI symbols — zero dialect-bridge hits without capsule vocabulary. Leaf at 229 references / 229 wired / 229 v2. PASS-26 LIVE STATE (2026-08-28, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md (graph revalidation of the newly cited opencode bridge files stays a next-pass target, now four passes deep); last recorded graph state remains pass-22's (ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Pass-26 direct reads line-exact @pin: `harness-opencode/src/bridge/index.ts` runTurn :140–163 / ensureRuntime :165–212 / buildOpenCodeConfig :214–273 (permission defaults :224–235, inactive demotion :239–250, harness-tools MCP entry :254–270) / buildProviderConfig :275–377 / legacySession* shims :395–474 / ensureSession :522–554 / runPrompt :556–761 (structured short-circuit :611–645, status ladder :663–680, settlement ladder :726–748, usage delta :749–760) / runCompaction :763–853 / consumeEvents :855–925 / handlePermissionV2 :931–969 / handlePermission :971–1009 / selectPermissionReply :1011–1072 / toPermissionToolName :1074–1088 / resolveInactiveBuiltinToolNames :1090–1100 / isBuiltinToolInactive :1102–1110 / isExternalPath :1112–1119 / emitContextFallback :1177–1230 / latestAssistantSnapshot :1257–1275 / emitAssistantContentPart :1330–1348 / createDeferred :1364–1373 / resolveCompactionModel :1393–1415, `create-emit-stream-event.ts` createEmitStreamEvent :22–311 (text :91–119, tool.called :194–223, step.ended :263–278) / closeLegacyOpenParts :313–330 / emitLegacyStepFinishPart :332–352 / emitLegacyToolPart :354–434, `opencode-events.ts` TranslationState :10–26 / emitOpenCodeStreamStart :48–68 / unwrapOpenCodeEvent :70–92 / getOpenCodeEventSessionId :94–108 / emitMissingFinalDelta :118–139 / emitLegacyPartDelta :145–191 / emitLegacyTextPartUpdate :197–247 / stripSyncVersion :249–251, `opencode-usage.ts` mapUsage :18–34 / extractSessionTokens :40–51 / subtractSessionTokens :53–69 / addUsage :71–105 / diff :143–145, `opencode-context-fallback.ts` whole 25L, `opencode-finish-step.ts` mapOpenCodeFinishReason :7–18 / translateLegacyStepFinishPart :26–47, `opencode-server-auth.ts` configureOpenCodeServerAuth :3–15; direct tests whole-file: index.test.ts 138L, create-emit-stream-event.test.ts 399L, opencode-events.test.ts 265L, opencode-usage.test.ts 66L, opencode-context-fallback.test.ts 44L, opencode-finish-step.test.ts 53L, opencode-path.test.ts 29L, opencode-server-auth.test.ts 27L. Adversarial RED observed: naive "how do I know when my agent turn is done" query hits only streamText/stop-condition symbols — zero opencode-bridge hits without capsule vocabulary. Leaf at 224 references / 224 wired / 224 v2. PASS-25 LIVE STATE (2026-08-28, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md (graph revalidation of the newly cited codex/opencode/claude-code bridge files stays a next-pass target, now three passes deep); last recorded graph state remains pass-22's (ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Pass-25 direct reads line-exact @pin: `harness-codex/src/bridge/index.ts` runTurn :80–272 / shim write :105–121 / event-loop interception :235–253 / threadState :69/:75, `cli-relay.ts` CLI_SHIM_FILENAME :29 / buildCliShimScript :31–68 / parseToolRelayCommandsInternal :101–139 / parseDirectToolRelayArgv :141–157 / extractShellEvalCommand :159–167 / splitShellAndCommands :169–210 / parseShellWords :212–268, `codex-step-tracker.ts` createCodexStepTracker :22–61 / finishStep :28–38 / isToolStepItem :71–79, `create-emit-stream-event.ts` createEmitStreamEvent :59–251 / lazy-open text :113–131 / NATIVE_TO_COMMON :50–53 / mapUsage :274–289, `tool-relay.ts` startAuthorizedToolRelay :14–119, `tool-relay-auth.ts` ToolRelayAuthorizer :11–103 / ToolRelayPendingCalls :108–133 / toolRelayCallKey :135–137, opencode `tool-relay-auth.ts` ToolRelayAuthorizer :6–99 + `tool-relay.ts` :10–107, opencode `index.ts` relay start :176–181 / harness-tools MCP entry :254–271 / authorizeHostToolCall :1161–1174, claude-code `compaction-latch.ts` createCompactionLatch :29–62 / index.ts latch wiring :309–312/:363–378/:414 / compact_boundary extraction :174–187; direct tests whole-file: cli-relay.test.ts 87L, codex-step-tracker.test.ts 136L, create-emit-stream-event.test.ts 248L, tool-relay.test.ts ×2, tool-relay-auth.test.ts 208L+129L, index.test.ts 291L, compaction-latch.test.ts 86L. Adversarial RED observed: naive "how do I let a sandboxed agent call my host tools safely" query hits only ACP relay symbols — zero dialect-bridge hits without capsule vocabulary. Leaf at 219 references / 219 wired / 219 v2. PASS-24 LIVE STATE (2026-08-27, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md (graph revalidation of the newly cited cline/pi/workflow-harness files stays a next-pass target); last recorded graph state remains pass-22's (ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Leaf at 215 references / 215 wired / 215 v2. PASS-23 LIVE STATE (2026-08-27, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean; Codebase Memory MCP NOT connected this session → direct source+test reading fallback per AGENTS.md (graph revalidation deferred to a connected session; last recorded graph state remains pass-22's: ready 69,060n/208,477e @generation 2026-08-24T14:04:21Z). Pass-23 direct reads line-exact @pin: `opencode-harness.ts` wireTurn :804–946 / pendingCompactionParts :799 / doCompact :1039–1064 / runCompactOperation :1211–1260 / startBase :948–961, `codex-harness.ts` wireTurn :736–881 / sendStart :862–880 / frameInitialPromptGuidance :1200–1211 / composeToolUsageInstructions :1213–1251 / initialPromptGuidanceApplied :717, `claude-code-harness.ts` openWebSocketAndWaitForBridgeHello :1271–1380 / openBridgeWebSocket :1382–1417 / wireTurn :1517–1655 / doCompact :1780–1793 / pendingResumeFlag :1502–1508, `deepagents-harness.ts` wireTurn :629–742 / attached flag :616–619 / instructionsApplied :627 / applyInstructions :771–778; direct tests: opencode-harness.test.ts :250–335 + :615–690, codex-harness.test.ts :207–231 + :515–556, claude-code-harness.test.ts describe 'bridge WebSocket startup' :856–978 (seven cases), deepagents-harness.test.ts :345–409. Adversarial RED observed: naive query "how do I run Claude Code through my agent" returns only docs/examples symbols — zero dialect-adapter hits without capsule vocabulary. Canonical leaf created this pass (verbatim migration of 205 mirror refs + five new); leaf now 210 refs == 210 v2 markers. PASS-22 LIVE STATE (2026-08-26, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): index_status(verbose) ready 69,060n/208,477e, git block branch main head_sha==base_sha==pin; checkout `git rev-parse HEAD` == pin byte-for-byte, porcelain clean. Pass-22 check_index_coverage over all 22 newly cited paths (13 source + 9 test): no_recorded_issue+metadata_match @generation 2026-08-24T14:04:21Z generation_matches true index_mode full. Pass-22 search_graph line-exact @pin: `stream-translator.update :315-365`, `.emitToolUpdate :167-268`, `.mergeToolUpdate :656-691`, `createACPStreamTranslator :33-408`, `createBlockId :96-107`, `mapACPFinishReason :410-431`, `host-tool-relay.updateCatalog :121-131`, `.handleCatalogNext :199-226`, `.handleInvocation :254-338`, `.catalogFingerprint :412-418`, `.credentialsMatch :464-477`, `startHostToolRelay :64-143`, `refreshHostToolCatalog :5-33`, `suppressToolCallUpdate :117-121`, `createHostToolCorrelation :35-262`, `hasPortableEvidence :264-344`, `claimHostToolPermission :218-244`, `shouldAutoApprove :128-148`, `requestPermission :38-118`, `configureACPPermissionMode :20-72`, `validateTarget :74-147`, `resolveGatewayCredentialSource :206-216`, `resolveACPProviderAuthentication :87-162`, `createExternalMcpServers :593-626`, `createOutputSchemaPromptMeta :738-760`, `promptActiveSession :762-811`, `resolveGatewayValues :672-690`, `createChildEnvironment :692-732`; trace_path inbound: createACPStreamTranslator→{createEmitStreamEvent, runTurn}, createHostToolCorrelation→{createEmitStreamEvent, runTurn}, configureACPPermissionMode→{ensureSession, runTurn} (all callers_total=2). Adversarial RED observed: naive query "how do I show tool calls from an external agent in my chat UI" returns ONLY examples/UI symbols — zero harness-acp bridge-plane hits without capsule vocabulary. PARITY REPAIR this pass: pass-20/21 ACP wiring lost to a stale overwrite was re-wired (six loader entries + lifecycle/session map group restored); leaf now 205 refs == 205 v2 markers. PASS-19 LIVE STATE (2026-08-26, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): index_status(verbose) ready 69,060n/208,477e, git block branch main head_sha==base_sha==pin; checkout `git rev-parse HEAD` == pin byte-for-byte. Pass-19 check_index_coverage: harness-stream-text-result.ts + turn-telemetry.ts no_recorded_issue+metadata_match @generation 2026-08-24T14:04:21Z; harness-stream-text-result.test.ts freshness=missing (file does not exist — behavioral coverage via run-prompt.test.ts). Pass-19 search_graph line-exact @pin: `HarnessStreamTextResult.finish :332-485`, `.finishStep :252-316`, `.enqueue :221-225`, `.enqueueContinuation :232-234`, `.discardCurrentStepContent :240-244`, `.abort :495-504`, `.fail :510-519`, `.constructor :170-213`, `createOutputTransformStream :844-939`, `publishTextChunk :859-875`, `createTurnTelemetry :158-489`, `normalizeUsage :116-156`; trace_path inbound createTurnTelemetry → 3 callers (runPrompt hop-1; promptTurn/continueTurn hop-2); inbound HarnessStreamTextResult → same 3 sites. Adversarial RED observed: query "fake streamText result for a remote coding agent session" returns ONLY test fixtures (fakeNetworkSandboxSessionForStartupSuccess, createFakeSandbox, fakeSession ×2) — HarnessStreamTextResult absent without capsule vocabulary. PASS-18 LIVE STATE (2026-08-26, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): index_status(verbose) ready 69,060n/208,477e, git block branch main head_sha==base_sha==pin, generation 2026-08-24T14:04:21Z generation_matches true; checkout `git rev-parse HEAD` == pin byte-for-byte; origin/main drifted to e21bde7 (deferred). Parse-partial remains the same 8 cosmetic files (css/mdx/lockfile) — ZERO harness files flagged. Pass-18 check_index_coverage over all 7 newly cited paths (bridge/index.ts, bridge/harness-bridge-capability-unsupported-error.ts, utils/sandbox-channel.ts, bridge/index.test.ts, disk-replay.integration.test.ts, reconnect.integration.test.ts, utils/sandbox-channel.test.ts) = ALL no_recorded_issue + metadata_match. Pass-18 search_graph line-exact @pin: `runBridge :354-926`, `bridge.replay :538-544`, `bridge.emit :522-536`, `handleInbound :655-802`, `createBridgeUserMessageQueue :98-215`, `SandboxChannel :124-526` with `reconnectLoop :363-419` / `suspend :302-327` / `attachEventCheckpoint :500-518` / `dispatch :472-498`; query_graph caller sweep: dialect harnesses (claude-code/codex/opencode/acp/deepagents/pi) read `.lastSeenEventId` in doStart and `experimental_createBridgeUserMessageSubmitter` registers onReconnect — 4 callers. PASS-16 LIVE STATE (2026-08-24, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED): fetch-first zero drift (local HEAD == origin/main == pin); index_status ready 69,060n/208,477e @generation 2026-08-24T05:06Z generation_matches true; ZERO packages/otel files in parse_partial (8 flagged files are css/mdx/lockfile only). Pass-16 stdin-JSON check_index_coverage over all 8 newly-cited otel source paths (open-telemetry.ts, legacy-open-telemetry.ts, gen-ai-format-messages.ts, select-attributes.ts, sanitize-attribute-value.ts, stringify-for-telemetry.ts, supplemental-attributes.ts, record-span.ts) = ALL no_recorded_issue + metadata_match. Pass-16 search_graph line-exact @pin: `OpenTelemetry.executeLanguageModelCall` :185-200 (rank#1), `LegacyOpenTelemetry.executeLanguageModelCall` :190-204 (rank#2), `mapProviderName` :76-109 (total:1 exact), `selectAttributes` :19-65 + legacy in-file twin :66-118, `stringifyForTelemetry` :13-30 + `serializeFileData` :32-51, `recordSpan` :8-50 / `recordErrorOnSpan` :60-74, `assembleOperationName` :3-21 (rank#1), `normalizeSupplementalAttributes` :103-117, `OpenTelemetry.cleanupCallState` :127-129 (rank#1); name_pattern note: `^Class\.` anchored regexes return total:0 on this graph — use bare symbol names or BM25 queries; closure-local handlers (onStart/onStepStart/etc.) documented graph-invisible per [DONE:396] precedent. Probe battery 46 greps byte-exact pre-write from repo root across all 10 new capsules (counts verified: callStates.set×4, gen_ai.operation.name×11, type:'extension'×1, preliminary!==true@847, wellKnownPrefixes×2, execution-denied×3, primitiveTypes.size!==1@28, recordErrorOnSpan×9/0 split, ai.stream.firstChunk×2). PASS-14 LIVE STATE (2026-08-24, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17` UNCHANGED from pass 13): fetch-first zero drift (local HEAD == origin/main == pin); index_status ready 69,060n/208,477e @generation 2026-08-24T05:06Z generation_matches true; stdin-JSON check_index_coverage over all 12 newly-cited paths (react/src/mcp-apps/{bridge,sandbox,utils}.ts + app-frame.tsx + app-renderer.tsx, mcp/src/tool/{mcp-apps,mcp-app-fingerprint}.ts, ai/src/realtime/{realtime-session,browser-realtime-audio,browser-realtime-transport,audio-utils,get-realtime-tool-definitions}.ts) = ALL no_recorded_issue + metadata_match. Pass-14 search_graph line-exact @pin: `MCPAppBridge.handleMessage` :218-238 (rank#1), `sandbox.sanitizeCSPSources` :11-44 (-43.48), `getMCPAppAllowAttribute` :129-142, `MCPAppBridge.sendToolInput` :254-259, `app-frame.deriveTargetOrigin` :19-26, `fingerprintMCPAppResource` :45-59, `splitMCPAppTools` :182-212, `readMCPAppResource` :268-285, `AbstractRealtimeSession.maybeRequestToolResponse` :221-233, `handleReducerEffect` :342-381, `encodeRealtimeAudio` :8-25, `schedulePlayback` :136-167, `addInputTranscriptionMessage` :293-345 (+ ensureToolPart :409-441), `getRealtimeToolDefinitions` :9-48. Probe battery 58/58 byte-exact pre-write (/tmp/aisdk_p14_battery.py). PASS-13 LIVE STATE (2026-08-24, pin `9d9a73f1551f2243035491e9de5a2e00ebf9eb17`): the pass-12 twin-adoption instruction below is SUPERSEDED — readlink proved `/inspo/ai` is a LIVE SYMLINK into `/inspo/frameworks/ai`, so refresh-in-place applied: ff-pull d25cae27→9d9a73f then `index_repository --repo-path $REFERENCE_ROOT/ai --name ai` updated the SAME project in place (69,060n/208,477e, status indexed; head_sha==base_sha==pin). Freshness proven per metadata-trap protocol: stdin-JSON check_index_coverage over newly-cited paths (execute-tools-from-stream.ts / gateway-language-model-batch.ts / start-video.ts / mcp-http-headers.ts + more) = no_recorded_issue, freshness metadata_match, generation 2026-08-24T05:06Z generation_matches true. Pass-13 search_graph line-exact @new pin: `isToolExecutionAllowedFinishReason` :3-7 (rank#1 -23.65), `executeToolsFromStream` :35-251 (-24.54), `DefaultStreamObjectResult.rejectResultPromises` :931-939, `experimental_startVideo` :90-214, `waitForWebhook` :605-640 (+ `GatewayVideoModel.handleWebhookOption` :177-190), `GatewayBatchLanguageModel.experimental_doStartBatch` :61-124 (-32.37), `GatewayNotFoundError` Class :12-35, `getMCPToolHeaderBindings` :35-114 (-40.31) + `encodeMCPHeaderValue` :18-33, `DefaultMCPClient.tryProtocolDiscovery` :609-639, `HttpMCPTransport.isModernProtocol` :125-130 (-47.76), workflow `repairPart` :27-58 + `normalizeUIMessageStreamParts` :110-174 (total:2 exact), `createModelCallToUIChunkTransform` :224-264, harness `runPrompt` :68-1076. KNOWN graph-invisible symbol: stream-text closure-local `createPartIdReserver` returns total:0 by construction — anchor on source pins (:1167–1187) or `reserveTextPartId` usage. Probe battery v2 39/39 byte-exact pre-write (/tmp scratch, lane-suffixed); adversarial wrong-project check implicit via rank#1 exclusivity. Prior-pass evidence retained below (pass 12 at OLD pin d25cae27: fetch-first zero local commits but 96 upstream pending, re-index DEFERRED under then-believed stale-twin risk — overturned this pass; coverage ×18 gateway paths no_recorded_issue; line-exact createGateway :286–638 etc.; probe battery 54/54). Pass-12 stdin-JSON `check_index_coverage` over EIGHTEEN gateway paths (gateway-provider.ts / gateway-language-model.ts / gateway-fetch-metadata.ts / gateway-model-entry.ts / gateway-realtime-auth.ts / gateway-realtime-model.ts / gateway-transcription-model.ts / errors/as-gateway-error.ts / errors/create-gateway-error.ts / errors/gateway-error.ts / gateway-spend-report.ts / gateway-generation-info.ts / tool/exa-search.ts + gateway-provider.test.ts / gateway-transcription-model.test.ts / gateway-realtime-model.test.ts / gateway-fetch-metadata.test.ts / errors/create-gateway-error.test.ts) = ALL `no_recorded_issue`/`metadata_match` generation_matches true. Pass-12 search_graph line-exact: `createGateway` :286–638, `getAvailableModels` :429–453, `GatewayFetchMetadata.getAvailableModels` :34–56, `isTimeoutError` :11–28, `asGatewayError` :30–75, `createGatewayErrorFromResponse` :23–138, `maybeEncodeFileParts` :199–220, `doStream` :116–192, `mintClientSecret` :351–387, `assertGatewayClientSecretServerEnvironment` :664–670, `buildGatewayProtocols` :68–82, `encodeSubprotocolValue` :136–146, `createGatewayTranscriptionStream` :219–396 (+ cartesia/xai sendAudio twins surfaced by BM25), `getSpendReport` :76–128, `GatewayGenerationInfoFetcher.getGenerationInfo` :61–87, `getGatewayAuthToken` :642–662, `createO11yHeaders` :389–417, `exaSearch` :350–352). Pass-12 probe battery: 54 deterministic greps executed byte-exact PRE-write from repo root, all counts as authored (T04 authMethod union ×2 = interface+impl signatures, T16 internal-server-error ×2 = explicit case + default fallthrough, T43 cache-refresh assertion ×2 across two tests, T50 image-encoding describes ×2 doGenerate+doStream, T52 no-double-wrap test title ×2). Prior-pass evidence retained below (pass 11 re-verified 2026-08-24 at the SAME pin: fetch-first confirmed zero upstream commits behind `d25cae27…`, project `ai` serving 68,288n/204,996e from generation 2026-08-16T00:20:25Z with generation_matches true; pass-11 stdin-JSON `check_index_coverage` over ELEVEN retrieval-plane paths (embed-many.ts / embed.ts / rerank.ts / resolve-model.ts / wrap-embedding-model.ts / log-warnings.ts / embedding-model-v4.ts / reranking-model-v4-result.ts + embed-many.test.ts / embed.test.ts / rerank.test.ts) = ALL `no_recorded_issue`/`metadata_match`; pass-11 search_graph line-exact: `embedMany` :49–416, `splitArray` :9–20, `rerank` :38–343 (+ `DefaultRerankResult.rerankedDocuments` :363–365), `resolveEmbeddingModel` :48–63 / `resolveRerankingModel` :186–214, `wrapEmbeddingModel` :23–40, `logWarnings` :121–161). Pass-9 re-verified 2026-08-24; pass-10 re-checked 2026-08-24 at the SAME pin: HEAD==base==`d25cae27…`, fetch confirmed zero upstream commits, parity 86=86=86 bidirectional set-equality; pass-10 stdin-JSON coverage over the five newly cited source paths plus both registry/middleware test files = ALL no_recorded_issue). project `ai`, root `$REFERENCE_ROOT/ai`, branch `main@d25cae2722bfaed94c56d992c6df399a736db7a9` (base==HEAD, unchanged since pass 1), status ready, full mode, 68,288 nodes / 204,996 edges, generation 2026-08-16T00:20:25Z (generation_matches true). parse_partial = 8 files (incl. `provider-utils/src/index.ts` L131 — grep preferred there); skipped = 0; not_indexed = `.claude`/`.git`/`.vscode` + 77 files BY DESIGN. Pass-9 stdin-JSON `check_index_coverage` over 12 source paths (all seven ui-message-stream producer files + ui-message-chunks + get-response-ui-message-id + json-to-sse + both response adapters + create-agent-ui-stream + pipe-agent-ui-stream-to-response) = ALL `no_recorded_issue`/`metadata_match`. Pass-9 search_graph line-exact: `createUIMessageStream` :28-161, `handleUIMessageStreamFinish` :14-189, `toUIMessageChunk` :30-387, `readUIMessageStream` :55-117, `getResponseUIMessageId` :15-35, `JsonToSseTransformStream` Class :6-17, `createAgentUIStream` :46-118, `DataUIMessageChunk` Type :217-224; NOTE a workflow-package twin (`packages/workflow/src/to-ui-message-chunk.ts:toUIMessageChunk` :11-212) shares the toUIMessageChunk query. Pass-9 probes executed byte-exact ×14 GREEN: create-ui-message-stream.test.ts:317 (delayed merged streams) with safeEnqueue×6; ui-message-chunks.test.ts:78 (rejects unknown types) with startsWith at :173; to-ui-message-chunk.ts:281 comment with isDynamic×4; to-ui-message-stream.ts:72 metadata gate + test :158; handle-ui-message-stream-finish.ts:179 ts-expect-error + test :425 cancelled-reader onFinish + structuredClone×3; read-ui-message-stream.ts textByPartIndex×3 + test :241 + close-guard comment :110; headers file :5 version pin + no-await tee comments :36/:40; create-agent-ui-stream.ts:82 cast comment + response test :366 sandbox passthrough. Adversarial wrong-project retrieval (teable × UIMessageChunk/SSE/writer terms) returned only teable's own SSE utils — zero ai-project seams. Pass-8 stdin-JSON `check_index_coverage` over 9 source paths (validate-ui-messages.ts / ui-messages.ts / both last-assistant-message predicates / transform-text-to-ui-message-stream.ts / process-text-stream.ts / direct-chat-transport.ts / text-stream-chat-transport.ts / chat.ts) = ALL `no_recorded_issue`/`metadata_match`. Pass-8 search_graph line-exact: `validateUIMessages` :554-584, `safeValidateUIMessages` :382-545, `DirectChatTransport.sendMessages` :89-130 + `reconnectToStream` :138-142, `lastAssistantMessageIsCompleteWithToolCalls` :7-38, `lastAssistantMessageIsCompleteWithApprovalResponses` :7-42, `getStaticToolName` :591-595, `getToolName` :603-607, `isToolUIPart` :580-584, `UIToolInvocation` type :284-387, `callCompletionApi` :18-157, `processTextStream` :1-16, `transformTextToUiMessageStream` :3-27, `TextStreamChatTransport.processResponseStream` :16-22. Pass-8 probes pinned to real suites: validate-ui-messages.test.ts:1101 (input-available validated) /:1145 (no input re-validation at output-available) /:1430/:1458/:1486/:1514 (unknown-tool throw vs terminal-state skips) /:1334 (output-error absent input); last-assistant-message-is-complete-with-tool-calls.test.ts:341 (provider-executed ⇒ false), -with-approval-responses.test.ts:199 (mixed pending approval ⇒ false); direct-chat-transport.test.ts:318/:404/:427 (prompt conversion/invalid messages/null reconnect); transform-text-to-ui-message-stream.test.ts:9/:56/:88 (sequence incl. empty stream); ui-messages.test.ts:21-31 (dash tool name). Prior-pass evidence retained below. Pass-7 search_graph confirmed `processUIMessageStream` at :82-992; pass-7 probes pinned to real suites: process-ui-message-stream.test.ts:4872 (transient part bypasses write), chat.test.ts:998/:1061/:1111 (resumed-stream stop/pending-stop/latest-wins), :800/:804 (abort ⇒ onFinish), http-chat-transport.test.ts:185/:193 (abort passthrough, mocked 204 ⇒ null), ai-state.test.ts:49/:55/:64/:80 (ALS throw/sealed/onSetAIState/keyed updates), create-streamable-value.test.tsx:76/:114/:150/:165 (patches/mixed payloads/stream source), stream-ui.ui.test.tsx:122/:132/:189/:213 (text/tool renders incl. generators), use-object.ui.test.tsx:90/:101/:109. Pass-6 stdin-JSON `check_index_coverage` over 11 source paths (download.ts/download-function.ts/file-part-data.ts/data-content.ts/split-data-url.ts/prepare-tools.ts/create-tool-model-output.ts/detect-media-type.ts/connect-to-websocket.ts/delayed-promise.ts/create-tool-name-mapping.ts) + 5 direct-test paths + prepare-tools.test.ts = ALL `no_recorded_issue`/`metadata_match`. Pass-6 search_graph confirmed exact ranges (`prepareTools` :15-80, `connectToWebSocket` :25-132, `stripID3` :221-228, `detectMediaType` :283-311, `DelayedPromise.promise` :15-32); adversarial wrong-project retrieval on biome returned only irrelevant Rust CSS/markdown nodes. Pass-4 coverage evidence for its own cited paths retained below. Pass-4 `check_index_coverage` over all 17 newly cited source/test paths = `no_recorded_issue`/`metadata_match`. Pass-3 retrieval queries confirmed exact line ranges for every pass-4 symbol. Source and direct tests decide shipped claims.

## Boundaries
Adopt the predicate-termination, prepare-step, streaming-smoothing, timeout-ladder, repair-hook, integrity-pruning, spec-versioned-provider, approval-replay, output-envelope, history-round-trip, drift-fingerprint, schema-facade, partial-json-repair, legacy-object-pipeline, final-validation-repair, json-instruction-injection, lazy-result-primitive, event-ledger, enriched-stream-publisher, continuation-engine, agent-wrapper, output-interface-selection, retry-kernel, sse-ingestion-plus-tool-name-mapping, pre-loop-replay, tool-parse-repair-ladder, tool-execution-lifecycle, tool-timeout-resolution, abort-signal-merging, request-tool-assembly, prototype-safe-lookup, swallowing-callback-bus, async-iterable-stream-duality, background-stream-drain, http-transport-verbs, ssrf-download-guard, response-handler-factory, response-body-size-limit, header-sanitization-origin-trust, provider-toolcall-tracker, experimental-tool-callers, model-call-stream-normalizer, prompt-standardization-conversion, micro-utility-kernels, download-ladder, url-passthrough-policy, file-part-normalization, data-content-codec, tool-result-shaping, call-options-validation, provider-name-mapping, media-sniffing, websocket-seam, multipart-form, header-fallback, react-external-store-bridge, chat-request-lifecycle, ui-chunk-reducer, ui-to-model-conversion, http-chat-transport-resume, swr-use-object, realtime-hook-store, streamable-value-protocol, streamable-ui-suspense-ladder, ai-state-delta-sync, resolvable-promise-kernel, use-completion-shape, and filelist-ingestion contracts; adapt provider dialects, chunk regexps/delays, timeout defaults, approval UX, error taxonomies, and schema vendor detection to host; adopt additionally the ui-tool-invocation-state-union, ui-message-validation-ladder, last-step-completion-predicates, direct-in-process-transport, text-stream-ui-adapters, wire-dialect-registry, ui-stream-writer-kernel, ui-stream-wire-contract, ui-part-to-chunk-conversion, ui-stream-metadata-adapter, ui-stream-persistence-ladder, ui-stream-snapshot-reader, ui-stream-response-family, ui-agent-http-entry, provider-registry-first-separator-two-error-taxonomy, custom-provider-lazy-resolve-fallback-ladder, extract-json-suffix-holdback-prototype-less-map, and default-injection-prepend-only-null-beats-default contracts; adopt additionally the embed-batching-push-order-reassembly, rerank-empty-short-circuit-and-trusted-index-hydration, string-id-gateway-fallback-resolution, proxy-one-property-version-lift, embedding-middleware-capability-overrides, warning-ladder-with-once-latch, and retrieval-two-tier-callid-skeleton contracts; adopt additionally the gateway closure-factory-with-alias-by-assignment, gateway-two-rung-auth-method-tagging, gateway-swr-request-start-clock, gateway-single-funnel-symbol-marker-derived-retryability, gateway-headers-carry-wire-identity, gateway-in-place-file-base64, gateway-origin-relative-server-only-mint, gateway-subprotocol-auth-shared-module, gateway-realtime-identity-codec, gateway-transcription-finished-latch-duplex-pump, gateway-origin-split-billing-transform-boundary, gateway-catalog-modeltype-subtraction-filter, gateway-provider-executed-failure-as-data, gateway-o11y-static-per-request-split, gateway-open-service-validated-options, gateway-zod-tree-shake-shim-workflow-serialize, and gateway-modality-twin-class-field-contracts contracts; adopt additionally the drift-reentry finish-reason-tool-execution-allowlist, buffered-model-call-end-drain, uniform-notify-callback-containment, crossstep-part-id-first-wins-remap, streamobject-terminal-error-latch, preliminary-output-history-suppression, video-outside-retry-idempotency-mint, video-capability-gated-webhook-one-shot-completion, gateway-batch-header-idempotency-abort-preservation-all-or-nothing-counts, mcp-header-binding-reachability-ladder-base64-sentinel, mcp-era-probe-fatal-vs-fallback-discovery, mcp-era-single-predicate-transport-split, workflow-replay-raw-filter-ui-cursor, uistream-frame-repair-consumer-mirrored-resets, rsc-functional-updater-only-state-writes, harness-builtin-vs-host-replay-set-split, empty-id-falsy-coercion-family, bedrock-length-prefix-fail-loud-flush, jsonschema-additionalproperties-semantics-preserving, mcp-app-queue-until-initialized-bridge-gate, mcp-app-deny-by-default-tool-allowlist, mcp-app-parsed-url-request-validation-ladder, mcp-app-csp-decode-recheck-sanitizer, mcp-app-permission-intersection-hierarchy, mcp-app-proxy-pull-frame-lifecycle-with-ref-replay, mcp-app-exact-mime-uri-keyed-identity-pinning, mcp-app-identity-throws-embellishment-degrades, mcp-app-canonical-json-drift-fingerprint, realtime-reducer-owned-per-key-state, realtime-single-followup-response-conjunction-gate, realtime-measure-before-stop-barge-in-truncate, realtime-asymmetric-pcm16-chunked-base64-clock-chained-playback, realtime-self-catching-send-queue-with-socket-identity-guards, realtime-in-place-part-location-transcription-positioning, and realtime-function-only-exhaustive-tool-definitions contracts; omit the concrete provider packages (their per-provider drift fixes are mined as family patterns at one decisive instance each), telemetry dispatcher bodies beyond the onError contract, and the new harness-* package bodies beyond the run-prompt replay split unless a target requires them. (PASS-14 RETRACTION: the formerly omitted `AbstractRealtimeSession` WebAudio internals and the react/src/mcp-apps sandbox implementations are NOW capsuled — see the MCP Apps host plane and Realtime browser session plane map groups; remaining uncapsuled: `packages/react/src/use-realtime.ts` hook wrapper over the store, `packages/gateway/src/gateway-realtime-model.ts` serializer bodies.) (PASS-15 ADOPTION: additionally adopt the code-mode sandboxed-tool-plane contracts — execution-policy-byte-expansion, proxy-bindings-source-wrapping, interrupt-resume-ladder, signed-continuation-capability, approval-funnel, host-tool-invocation-contract, tool-prompt-compiler, error-taxonomy, json-boundary-kernel, generic-interrupts, tool-caller-late-binding, sandbox-semantics, outer-tool-call-identity, approval-message-roundtrip, interrupt-payload-protocol, package-boundary, behavior-test-battery — omit only the external `run` VM kernel internals (versioned dependency), the network e2e suite, and test-file bodies beyond cited anchors.) (PASS-16 ADOPTION: additionally adopt the OpenTelemetry integration-plane contracts — callid-state-machine-span-tree, lazy-input-output-attribute-specs, otel-legality-sanitizer-homogeneous-array-nonfinite-drop, semconv-provider-operation-finish-reason-mapping, provider-executed-tool-span-synthesis-with-preliminary-skip, fail-open-enrichspan-standard-wins-collision, abort-silent-vs-error-stamped-drain-ladder, legacy-ai-vocabulary-never-mix-boundary, mocktracer-vs-inmemory-exporter-double-layer, and genai-six-type-span-taxonomy-ms-to-seconds — RETRACTION of the pass-15 standing omission "otel telemetry dispatcher bodies": the packages/otel integration plane is NOW capsuled; the dispatcher itself lives in packages/ai/src/telemetry/create-telemetry-dispatcher.ts and its bodies beyond the execute* context-forwarding contract remain omitted as ai-package plumbing.)

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`abort-signal-merging.md`](./abort-signal-merging.md)
- [`ai-state-delta-sync.md`](./ai-state-delta-sync.md)
- [`approval-round-trip.md`](./approval-round-trip.md)
- [`async-iterable-stream-duality.md`](./async-iterable-stream-duality.md)
- [`background-stream-drain.md`](./background-stream-drain.md)
- [`bedrock-eventstream-framing.md`](./bedrock-eventstream-framing.md)
- [`call-options-validation-gate.md`](./call-options-validation-gate.md)
- [`callback-containment-streaming.md`](./callback-containment-streaming.md)
- [`chat-request-lifecycle.md`](./chat-request-lifecycle.md)
- [`code-mode-approval-funnel.md`](./code-mode-approval-funnel.md)
- [`code-mode-approval-message-roundtrip.md`](./code-mode-approval-message-roundtrip.md)
- [`code-mode-behavior-test-battery.md`](./code-mode-behavior-test-battery.md)
- [`code-mode-error-taxonomy.md`](./code-mode-error-taxonomy.md)
- [`code-mode-execution-policy-byte-expansion.md`](./code-mode-execution-policy-byte-expansion.md)
- [`code-mode-generic-interrupts.md`](./code-mode-generic-interrupts.md)
- [`code-mode-host-tool-invocation-contract.md`](./code-mode-host-tool-invocation-contract.md)
- [`code-mode-interrupt-payload-protocol.md`](./code-mode-interrupt-payload-protocol.md)
- [`code-mode-interrupt-resume-ladder.md`](./code-mode-interrupt-resume-ladder.md)
- [`code-mode-json-boundary-kernel.md`](./code-mode-json-boundary-kernel.md)
- [`code-mode-outer-tool-call-identity.md`](./code-mode-outer-tool-call-identity.md)
- [`code-mode-package-boundary.md`](./code-mode-package-boundary.md)
- [`code-mode-proxy-bindings-source-wrapping.md`](./code-mode-proxy-bindings-source-wrapping.md)
- [`code-mode-sandbox-semantics.md`](./code-mode-sandbox-semantics.md)
- [`code-mode-signed-continuation-capability.md`](./code-mode-signed-continuation-capability.md)
- [`code-mode-tool-caller-late-binding.md`](./code-mode-tool-caller-late-binding.md)
- [`code-mode-tool-prompt-compiler.md`](./code-mode-tool-prompt-compiler.md)
- [`custom-provider-fallback.md`](./custom-provider-fallback.md)
- [`data-content-codec.md`](./data-content-codec.md)
- [`default-injection-middlewares.md`](./default-injection-middlewares.md)
- [`direct-in-process-transport.md`](./direct-in-process-transport.md)
- [`download-asset-ladder.md`](./download-asset-ladder.md)
- [`embed-batching-parallelism.md`](./embed-batching-parallelism.md)
- [`embedding-middleware-wrapper.md`](./embedding-middleware-wrapper.md)
- [`empty-toolcall-id-coercion.md`](./empty-toolcall-id-coercion.md)
- [`enriched-stream-publisher.md`](./enriched-stream-publisher.md)
- [`event-ingestion-and-tool-names.md`](./event-ingestion-and-tool-names.md)
- [`event-ledger.md`](./event-ledger.md)
- [`experimental-tool-callers.md`](./experimental-tool-callers.md)
- [`extract-json-stream-guard.md`](./extract-json-stream-guard.md)
- [`extract-reasoning.md`](./extract-reasoning.md)
- [`file-part-data-normalization.md`](./file-part-data-normalization.md)
- [`filelist-ingestion.md`](./filelist-ingestion.md)
- [`final-validation-repair.md`](./final-validation-repair.md)
- [`fix-json-partial-parse.md`](./fix-json-partial-parse.md)
- [`flexible-schemas.md`](./flexible-schemas.md)
- [`gateway-auth-ladder.md`](./gateway-auth-ladder.md)
- [`gateway-batch-trio.md`](./gateway-batch-trio.md)
- [`gateway-catalog-type-filter.md`](./gateway-catalog-type-filter.md)
- [`gateway-client-secret-mint.md`](./gateway-client-secret-mint.md)
- [`gateway-error-plane.md`](./gateway-error-plane.md)
- [`gateway-file-part-base64.md`](./gateway-file-part-base64.md)
- [`gateway-metadata-cache.md`](./gateway-metadata-cache.md)
- [`gateway-modality-family.md`](./gateway-modality-family.md)
- [`gateway-o11y-header-split.md`](./gateway-o11y-header-split.md)
- [`gateway-observability-gets.md`](./gateway-observability-gets.md)
- [`gateway-open-provider-options.md`](./gateway-open-provider-options.md)
- [`gateway-provider-executed-tools.md`](./gateway-provider-executed-tools.md)
- [`gateway-provider-factory.md`](./gateway-provider-factory.md)
- [`gateway-realtime-identity-codec.md`](./gateway-realtime-identity-codec.md)
- [`gateway-subprotocol-auth.md`](./gateway-subprotocol-auth.md)
- [`gateway-transcription-http-path.md`](./gateway-transcription-http-path.md)
- [`gateway-transcription-ws-pump.md`](./gateway-transcription-ws-pump.md)
- [`gateway-wire-identity-headers.md`](./gateway-wire-identity-headers.md)
- [`gateway-zod-shim-workflow-serialize.md`](./gateway-zod-shim-workflow-serialize.md)
- [`generate-text-orchestrator.md`](./generate-text-orchestrator.md)
- [`harness-acp-bridge-session-services.md`](./harness-acp-bridge-session-services.md)
- [`harness-acp-cold-restoration-negotiation.md`](./harness-acp-cold-restoration-negotiation.md)
- [`harness-acp-credential-brokering-twins.md`](./harness-acp-credential-brokering-twins.md)
- [`harness-acp-diagnostics-plane.md`](./harness-acp-diagnostics-plane.md)
- [`harness-acp-emit-stream-facade.md`](./harness-acp-emit-stream-facade.md)
- [`harness-acp-host-tool-correlation-window.md`](./harness-acp-host-tool-correlation-window.md)
- [`harness-acp-host-tool-loopback-relay.md`](./harness-acp-host-tool-loopback-relay.md)
- [`harness-acp-instruction-mapping.md`](./harness-acp-instruction-mapping.md)
- [`harness-acp-lifecycle-compatibility-gate.md`](./harness-acp-lifecycle-compatibility-gate.md)
- [`harness-acp-permission-plane.md`](./harness-acp-permission-plane.md)
- [`harness-acp-protocol-configuration-envelope.md`](./harness-acp-protocol-configuration-envelope.md)
- [`harness-acp-recovered-session-update-queue.md`](./harness-acp-recovered-session-update-queue.md)
- [`harness-acp-respawn-strategy-ladder.md`](./harness-acp-respawn-strategy-ladder.md)
- [`harness-acp-stderr-sentinel-monitor.md`](./harness-acp-stderr-sentinel-monitor.md)
- [`harness-acp-stream-capture-sidecar.md`](./harness-acp-stream-capture-sidecar.md)
- [`harness-acp-stream-translator-inference.md`](./harness-acp-stream-translator-inference.md)
- [`harness-acp-turn-start-config-fingerprint.md`](./harness-acp-turn-start-config-fingerprint.md)
- [`harness-acp-wire-turn-event-pump.md`](./harness-acp-wire-turn-event-pump.md)
- [`harness-agent-stateless-session-split.md`](./harness-agent-stateless-session-split.md)
- [`harness-approval-continuation-recovery.md`](./harness-approval-continuation-recovery.md)
- [`harness-bootstrap-asset-embedding.md`](./harness-bootstrap-asset-embedding.md)
- [`harness-bootstrap-marker-idempotency.md`](./harness-bootstrap-marker-idempotency.md)
- [`harness-bridge-claim-stream-ownership.md`](./harness-bridge-claim-stream-ownership.md)
- [`harness-bridge-disk-replay-recovery.md`](./harness-bridge-disk-replay-recovery.md)
- [`harness-bridge-monotonic-seq-replay-log.md`](./harness-bridge-monotonic-seq-replay-log.md)
- [`harness-bridge-user-message-steering.md`](./harness-bridge-user-message-steering.md)
- [`harness-build-time-version-injection.md`](./harness-build-time-version-injection.md)
- [`harness-builtin-replay-split.md`](./harness-builtin-replay-split.md)
- [`harness-channel-event-checkpoint-pin.md`](./harness-channel-event-checkpoint-pin.md)
- [`harness-channel-reconnect-resume-ladder.md`](./harness-channel-reconnect-resume-ladder.md)
- [`harness-channel-suspend-cursor-freeze.md`](./harness-channel-suspend-cursor-freeze.md)
- [`harness-claude-code-apikey-helper.md`](./harness-claude-code-apikey-helper.md)
- [`harness-claude-code-bridge-emit-translator.md`](./harness-claude-code-bridge-emit-translator.md)
- [`harness-claude-code-bridge-turn-pump.md`](./harness-claude-code-bridge-turn-pump.md)
- [`harness-claude-code-compaction-latch.md`](./harness-claude-code-compaction-latch.md)
- [`harness-claude-code-websocket-hello-ladder.md`](./harness-claude-code-websocket-hello-ladder.md)
- [`harness-cline-inprocess-parked-sessions.md`](./harness-cline-inprocess-parked-sessions.md)
- [`harness-cline-private-history-persistence.md`](./harness-cline-private-history-persistence.md)
- [`harness-cline-sandbox-remote-ops.md`](./harness-cline-sandbox-remote-ops.md)
- [`harness-codex-bridge-cli-relay-shim.md`](./harness-codex-bridge-cli-relay-shim.md)
- [`harness-codex-bridge-step-tracker.md`](./harness-codex-bridge-step-tracker.md)
- [`harness-codex-cli-shim-host-tools.md`](./harness-codex-cli-shim-host-tools.md)
- [`harness-deepagents-bridge-hitl-loop.md`](./harness-deepagents-bridge-hitl-loop.md)
- [`harness-deepagents-instructions-once.md`](./harness-deepagents-instructions-once.md)
- [`harness-dialect-auth-resolver-ladder.md`](./harness-dialect-auth-resolver-ladder.md)
- [`harness-dialect-entry-default-instances.md`](./harness-dialect-entry-default-instances.md)
- [`harness-dialect-json-schema-zod-conversion.md`](./harness-dialect-json-schema-zod-conversion.md)
- [`harness-dialect-tool-relay-plane.md`](./harness-dialect-tool-relay-plane.md)
- [`harness-dialect-wireturn-invariant-core.md`](./harness-dialect-wireturn-invariant-core.md)
- [`harness-grok-build-minimal-acp-dialect.md`](./harness-grok-build-minimal-acp-dialect.md)
- [`harness-host-tool-execution-join.md`](./harness-host-tool-execution-join.md)
- [`harness-opencode-bridge-compaction-operation.md`](./harness-opencode-bridge-compaction-operation.md)
- [`harness-opencode-bridge-context-fallback.md`](./harness-opencode-bridge-context-fallback.md)
- [`harness-opencode-bridge-event-envelope.md`](./harness-opencode-bridge-event-envelope.md)
- [`harness-opencode-bridge-permission-plane.md`](./harness-opencode-bridge-permission-plane.md)
- [`harness-opencode-bridge-turn-settlement.md`](./harness-opencode-bridge-turn-settlement.md)
- [`harness-opencode-bridge-usage-accounting.md`](./harness-opencode-bridge-usage-accounting.md)
- [`harness-opencode-compaction-rail.md`](./harness-opencode-compaction-rail.md)
- [`harness-opencode-event-type-guard-kernel.md`](./harness-opencode-event-type-guard-kernel.md)
- [`harness-opencode-loopback-server-auth.md`](./harness-opencode-loopback-server-auth.md)
- [`harness-pi-dangling-host-tool-rerun-barrier.md`](./harness-pi-dangling-host-tool-rerun-barrier.md)
- [`harness-pi-provider-registration.md`](./harness-pi-provider-registration.md)
- [`harness-pi-workspace-mirror-vfs.md`](./harness-pi-workspace-mirror-vfs.md)
- [`harness-result-driver-writer-surface.md`](./harness-result-driver-writer-surface.md)
- [`harness-result-finish-gate-and-steps.md`](./harness-result-finish-gate-and-steps.md)
- [`harness-result-output-transform-lock.md`](./harness-result-output-transform-lock.md)
- [`harness-result-start-part-contract.md`](./harness-result-start-part-contract.md)
- [`harness-result-streamtext-twin.md`](./harness-result-streamtext-twin.md)
- [`harness-session-dual-state-machine.md`](./harness-session-dual-state-machine.md)
- [`harness-stream-abort-settle-contract.md`](./harness-stream-abort-settle-contract.md)
- [`harness-turn-suspend-resume-payloads.md`](./harness-turn-suspend-resume-payloads.md)
- [`harness-turn-telemetry-lifecycle-shim.md`](./harness-turn-telemetry-lifecycle-shim.md)
- [`harness-usage-normalization-nested-flat.md`](./harness-usage-normalization-nested-flat.md)
- [`harness-v1-adapter-contract.md`](./harness-v1-adapter-contract.md)
- [`header-default-fallback.md`](./header-default-fallback.md)
- [`header-sanitization-origin-trust.md`](./header-sanitization-origin-trust.md)
- [`http-chat-transport.md`](./http-chat-transport.md)
- [`http-transport-verbs.md`](./http-transport-verbs.md)
- [`json-instruction-injection.md`](./json-instruction-injection.md)
- [`last-step-completion-predicates.md`](./last-step-completion-predicates.md)
- [`lazy-result-primitives.md`](./lazy-result-primitives.md)
- [`magic-byte-media-sniffing.md`](./magic-byte-media-sniffing.md)
- [`mcp-app-bridge-init-gate.md`](./mcp-app-bridge-init-gate.md)
- [`mcp-app-csp-sanitizer.md`](./mcp-app-csp-sanitizer.md)
- [`mcp-app-frame-lifecycle.md`](./mcp-app-frame-lifecycle.md)
- [`mcp-app-permission-intersection.md`](./mcp-app-permission-intersection.md)
- [`mcp-app-request-validation-ladder.md`](./mcp-app-request-validation-ladder.md)
- [`mcp-app-resource-fingerprint.md`](./mcp-app-resource-fingerprint.md)
- [`mcp-app-server-resource-contract.md`](./mcp-app-server-resource-contract.md)
- [`mcp-app-tool-allowlist.md`](./mcp-app-tool-allowlist.md)
- [`mcp-app-tool-part-extraction.md`](./mcp-app-tool-part-extraction.md)
- [`mcp-era-discovery.md`](./mcp-era-discovery.md)
- [`mcp-http-era-split.md`](./mcp-http-era-split.md)
- [`mcp-parameter-headers.md`](./mcp-parameter-headers.md)
- [`micro-utility-kernels.md`](./micro-utility-kernels.md)
- [`model-call-stream-normalizer.md`](./model-call-stream-normalizer.md)
- [`model-resolution-versioning.md`](./model-resolution-versioning.md)
- [`model-url-passthrough-policy.md`](./model-url-passthrough-policy.md)
- [`multipart-form-encoding.md`](./multipart-form-encoding.md)
- [`otel-abort-error-drain.md`](./otel-abort-error-drain.md)
- [`otel-call-state-machine.md`](./otel-call-state-machine.md)
- [`otel-lazy-attribute-specs.md`](./otel-lazy-attribute-specs.md)
- [`otel-legacy-plane-boundary.md`](./otel-legacy-plane-boundary.md)
- [`otel-provider-tool-synthesis.md`](./otel-provider-tool-synthesis.md)
- [`otel-sanitizer-kernel.md`](./otel-sanitizer-kernel.md)
- [`otel-semconv-formatters.md`](./otel-semconv-formatters.md)
- [`otel-span-taxonomy.md`](./otel-span-taxonomy.md)
- [`otel-supplemental-enrich-span.md`](./otel-supplemental-enrich-span.md)
- [`otel-test-doubles.md`](./otel-test-doubles.md)
- [`output-interface-vs-strategies.md`](./output-interface-vs-strategies.md)
- [`output-specification.md`](./output-specification.md)
- [`part-id-crossstep-remap.md`](./part-id-crossstep-remap.md)
- [`policy-capability-scoping-middleware.md`](./policy-capability-scoping-middleware.md)
- [`policy-fail-closed-substrate.md`](./policy-fail-closed-substrate.md)
- [`policy-lazy-peer-dependency-clients.md`](./policy-lazy-peer-dependency-clients.md)
- [`policy-rego-decision-normalizer.md`](./policy-rego-decision-normalizer.md)
- [`policy-shadow-rollout-wrapper.md`](./policy-shadow-rollout-wrapper.md)
- [`policy-total-approval-config.md`](./policy-total-approval-config.md)
- [`preliminary-output-suppression.md`](./preliminary-output-suppression.md)
- [`prompt-standardization-conversion.md`](./prompt-standardization-conversion.md)
- [`prototype-safe-lookup.md`](./prototype-safe-lookup.md)
- [`provider-interface.md`](./provider-interface.md)
- [`provider-registry.md`](./provider-registry.md)
- [`provider-tool-name-mapping.md`](./provider-tool-name-mapping.md)
- [`provider-toolcall-tracker.md`](./provider-toolcall-tracker.md)
- [`react-external-store-bridge.md`](./react-external-store-bridge.md)
- [`realtime-audio-codec-kernel.md`](./realtime-audio-codec-kernel.md)
- [`realtime-barge-in.md`](./realtime-barge-in.md)
- [`realtime-event-reducer.md`](./realtime-event-reducer.md)
- [`realtime-hook-store.md`](./realtime-hook-store.md)
- [`realtime-session-skeleton.md`](./realtime-session-skeleton.md)
- [`realtime-tool-definitions.md`](./realtime-tool-definitions.md)
- [`realtime-tool-gating.md`](./realtime-tool-gating.md)
- [`realtime-websocket-transport.md`](./realtime-websocket-transport.md)
- [`request-tool-assembly.md`](./request-tool-assembly.md)
- [`rerank-orchestration-contract.md`](./rerank-orchestration-contract.md)
- [`resolvable-promise-kernel.md`](./resolvable-promise-kernel.md)
- [`response-body-size-limit.md`](./response-body-size-limit.md)
- [`response-handler-factory.md`](./response-handler-factory.md)
- [`response-messages.md`](./response-messages.md)
- [`result-promises-and-approval-replay.md`](./result-promises-and-approval-replay.md)
- [`retrieval-operation-lifecycle.md`](./retrieval-operation-lifecycle.md)
- [`retry-kernel.md`](./retry-kernel.md)
- [`rsc-concurrent-state-writes.md`](./rsc-concurrent-state-writes.md)
- [`schema-additionalproperties-preservation.md`](./schema-additionalproperties-preservation.md)
- [`simulate-streaming.md`](./simulate-streaming.md)
- [`ssrf-download-guard.md`](./ssrf-download-guard.md)
- [`stream-continuation-engine.md`](./stream-continuation-engine.md)
- [`stream-object-pipeline.md`](./stream-object-pipeline.md)
- [`stream-object-terminal-latch.md`](./stream-object-terminal-latch.md)
- [`stream-tool-buffering.md`](./stream-tool-buffering.md)
- [`streamable-ui-suspense-ladder.md`](./streamable-ui-suspense-ladder.md)
- [`streamable-value-protocol.md`](./streamable-value-protocol.md)
- [`streaming.md`](./streaming.md)
- [`swallowing-callback-bus.md`](./swallowing-callback-bus.md)
- [`swr-use-object.md`](./swr-use-object.md)
- [`text-stream-ui-adapters.md`](./text-stream-ui-adapters.md)
- [`tool-call-parse-and-repair-ladder.md`](./tool-call-parse-and-repair-ladder.md)
- [`tool-calls.md`](./tool-calls.md)
- [`tool-execution-lifecycle.md`](./tool-execution-lifecycle.md)
- [`tool-execution-safety-gate.md`](./tool-execution-safety-gate.md)
- [`tool-fingerprinting.md`](./tool-fingerprinting.md)
- [`tool-input-examples.md`](./tool-input-examples.md)
- [`tool-loop-agent.md`](./tool-loop-agent.md)
- [`tool-result-output-shaping.md`](./tool-result-output-shaping.md)
- [`tool-timeout-resolution.md`](./tool-timeout-resolution.md)
- [`ui-agent-http-entry.md`](./ui-agent-http-entry.md)
- [`ui-chunk-reducer.md`](./ui-chunk-reducer.md)
- [`ui-message-validation-ladder.md`](./ui-message-validation-ladder.md)
- [`ui-part-to-chunk-conversion.md`](./ui-part-to-chunk-conversion.md)
- [`ui-stream-frame-repair.md`](./ui-stream-frame-repair.md)
- [`ui-stream-metadata-adapter.md`](./ui-stream-metadata-adapter.md)
- [`ui-stream-persistence-ladder.md`](./ui-stream-persistence-ladder.md)
- [`ui-stream-response-family.md`](./ui-stream-response-family.md)
- [`ui-stream-snapshot-reader.md`](./ui-stream-snapshot-reader.md)
- [`ui-stream-wire-contract.md`](./ui-stream-wire-contract.md)
- [`ui-stream-writer-kernel.md`](./ui-stream-writer-kernel.md)
- [`ui-to-model-conversion.md`](./ui-to-model-conversion.md)
- [`ui-tool-invocation-state-union.md`](./ui-tool-invocation-state-union.md)
- [`use-completion-legacy.md`](./use-completion-legacy.md)
- [`video-async-lifecycle.md`](./video-async-lifecycle.md)
- [`video-webhook-arbitration.md`](./video-webhook-arbitration.md)
- [`warning-pipeline.md`](./warning-pipeline.md)
- [`websocket-transport-seam.md`](./websocket-transport-seam.md)
- [`wire-dialect-registry.md`](./wire-dialect-registry.md)
- [`workflow-harness-durable-step-engine.md`](./workflow-harness-durable-step-engine.md)
- [`workflow-resume-index-spaces.md`](./workflow-resume-index-spaces.md)
