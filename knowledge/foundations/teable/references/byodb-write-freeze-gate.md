<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** What must be drained, frozen, or pre-checked before a consistent copy can start?
**Path/Symbol:** `runMigrationJob` (:1975-2140), `assertSourceInventoryUnchangedForJob` (:3637-3690), `waitForSchemaOperationsForJob` (:3402-3510), `waitForBackgroundWritersForJob` (:3512-3635), `waitForSourceComputedDrainForJob` (:3768-3873), `pauseSourceComputedForJob`/`insertMigrationComputedPause` (:3975-4024)
**Signature:** drains poll with per-drain timeout/poll env knobs; stale non-terminal rows older than 7 days are EXCLUDED from blocking counts but reported in stats; computed pause rows `{id:'sdmp_<jobId>_<spaceId>', scope_type:'space', reason:'space-data-db-migration:<jobId>'}` upserted ON CONFLICT (scope_type,scope_id) WHERE existing reason is ours OR lease already expired.
**Data Shape:** three writer classes: (1) schema operations — meta-DB rows status pending/running/error scoped by base/table/resourceId; (2) background writers — provisioning resources in states {pending,deleting} PLUS open BullMQ jobs across 4 import queues scanned in bounded batches (100/1000) with per-probe 30s timeouts and job→inventory scope matching via data.baseId / data.table.id / data.tableIdMap values; (3) computed updates — outbox rows status 'processing' split by locked_at vs processingLease into ACTIVE vs RECLAIMABLE.
**Decisive source:** :996-1028 gate order at start: cross-space-flag off → related-spaces closure → no active migration → large-migration confirmation (50GB/10M rows default, throws CONFIRMATION_REQUIRED with thresholds payload) → target extension deps → target disk ≥ max(bytes×2, 1GB free) → retry-artifact cleanup → target conflict scan. Copy phase: freeze marker + `BYODB_SPACE_DATA_DB_INITIAL_WRITE_DRAIN_MS` (default 2000) grace BEFORE opening the snapshot so in-flight writes settle, then inventory re-check.
**Flow/Invariant:** final gate repeats the full drain ladder AFTER first delta replay when switchOnCompletion (freeze → computed pause+drain → schema ops → background writers → inventory re-check → cutover replay to lag 0). Every loop iteration re-checks cancellation. Failure paths resume pauses, clean source triggers, drop target artifacts (only if created), and mark the job failed — never leave the source paused.
**Probe (direct test):** specs 'waits for provisioning resources and import queue jobs before copy' (:5132), 'scans open queue jobs in bounded batches while draining background writers' (:5402), 'ignores stale non-terminal schema operations while draining before copy' (:5014), 'fails before copy when the temp work directory has insufficient free space' (:5775), 'does not require full dump space for streaming base-schema copy' (:5835); live: `grep -c 'ProvisionState.deleting' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"waitForBackgroundWritersForJob","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt the drain taxonomy and the pause-reason ownership rule (upsert only own reason or expired leases); adapt queue names/states to your stack.
