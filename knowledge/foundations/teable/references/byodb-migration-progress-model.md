<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` + `space-data-db-migration-progress.ts` @ pin `06a4461e`
**Question:** How does a long DB-to-DB migration report progress and ETA that never goes backwards?
**Path/Symbol:** `buildMigrationProgress` (:5914-5955), `buildSpaceDataDbMigrationPercent`, `SpaceDataDbMigrationProgressTracker` (progress.ts :97-238)
**Signature:** stages weighted `{prepare 8, copy 56, shared 10, delta 6, validate 14, switch 6}` (sum 100, spec-pinned); ~40 worker phases map to `(stage, fraction)`; tracker smooths with EWMA α=0.3, min sample interval 250ms.
**Data Shape:** live fractions: copy stage from target-side bytes (`pg_total_relation_size` sum over migrated schemas ÷ inventory estimate, capped 0.99); shared stage = copiedTables/totalTables; delta stage splits 0-0.6 (first pass) and 0.6-0.89 (cutover pass) by replayed-seq fraction over the job's OWN baseline-relative span. Output: `{phase, totalSteps:9, completedSteps, percent, stage, stages[], copiedBytes, bytesPerSecond, etaMs}` persisted inside copyStats on EVERY phase write.
**Decisive source:** progress.ts :32-34 — "Drain phases repeat at the final write gate after delta replay; the monotonic tracker keeps the reported percent from moving backwards when they do" — `percent = max(previous, input)`; terminal phases (`succeeded`, failures, canceled_before_copy) clear the in-memory entry (:240-250).
**Flow/Invariant:** ETA ladder: velocity-based `(100−percent)/percentPerMs` when EWMA exists → else linear elapsed×(100−p)/p → else null; clamped at 30 days; 0 exactly at 100. Percent is ROUNDED to 0.1 so UI jitter dies. completedSteps comes from a static phase→steps table (:749-788) — drain-timeout phases deliberately sit BELOW their drained success phases.
**Probe (direct test):** REAL RUNNER vitest `space-data-db-migration-progress.spec.ts` 11/11 GREEN (standalone /var/tmp/tvp venv): 'keeps percent monotonic when drain phases repeat at the final write gate', 'sums stage weights to 100', 'falls back to the linear elapsed-time eta without velocity samples'; live: `grep -c 'speedEwmaAlpha = 0.3' apps/nestjs-backend/src/features/space/space-data-db-migration-progress.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"SpaceDataDbMigrationProgressTracker","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt tracker+weights as a portable monotonic-progress kernel; adapt the live-fraction probes (bytes/tables/seq) to whatever your pipeline exposes.
