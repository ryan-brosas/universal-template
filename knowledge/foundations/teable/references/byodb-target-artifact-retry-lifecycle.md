<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do you make a destructive migration retryable without leaking artifacts or destroying a switched target?
**Path/Symbol:** `findRestartableTargetArtifactJob` (:1376-1402), `cleanupRetryableTargetArtifacts` (:1344-1374), `cleanupTargetArtifactsForJob` (:6700-6771), `buildSharedTableCleanupPlans` (:6869-6886), `canTruncateTargetSharedTables` (:1410-1420)
**Signature:** restartable job = same spaceId + same targetUrlFingerprint + same targetInternalSchema AND (failed | canceled | succeeded-without-switch); cleanup reasons: 'retry_before_start', 'stale_active_job', 'pre_switch_failure', 'manual_cleanup'.
**Data Shape:** shared-table cleanup plans carry per-table scoped WHERE (same predicates as copy) and a PRIORITY ORDER — seed 0, history/trash 1-3, dead-letter 4, outbox 5, pause-scope 6, undo-log 7 — deleting dependents before sources; stats record deletedRows per table (CTE DELETE…RETURNING count) or truncated flag.
**Decisive source:** :1356-1367 — before cleaning a non-dry-run predecessor, its stored inventory is compared to the current one (`compareInventoryForCopy(..., {compareSharedScope:false})`); ANY mismatch aborts cleanup so a new start can never inherit stale partial state from a DIFFERENT data shape. :1410-1419 — TRUNCATE (fast path) is allowed ONLY when zero bindings reference the connection (`bindings.length === 0`); otherwise scoped deletes. :6718-6723 — automatic cleanup REFUSES after `succeeded && switchOnCompletion` ("has already switched; automatic target cleanup is unsafe").
**Flow/Invariant:** startMigrationForSpace runs conflict checks AFTER cleanupRetryableTargetArtifacts but the conflict check itself is SKIPPED when the previous job was a successful dry-run (:1160-1171) — its own leftovers legitimately occupy the target. Every cleanup failure is recorded into copyStats.targetCleanup with failedAt/error and rethrown as TARGET_CLEANUP_FAILED.
**Probe (direct test):** specs 'cleans retryable same-job target artifacts before starting a new migration' (:800), 'cleans previous successful dry-run target artifacts before starting a new migration' (:869), 'does not clean retry target artifacts when the previous job inventory differs' (:971), 'truncates target shared tables when the target connection is unbound' (:3396), 'refuses automatic target cleanup after a successful switch' (:3505), 'allows preflight to pass when conflicts belong to a previous successful dry-run' (:928); live: `grep -c 'computedUpdateOutboxSeed, 0' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"cleanupTargetArtifactsForJob","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt inventory-gated idempotent retry + truncate-only-unbound + never-clean-switched as invariants; adapt plan priority order when adding tables.
