<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do you capture and replay source writes during a bulk copy so the target converges to the source?
**Path/Symbol:** `openSourceSnapshot` (:2239-2264), `installSourceDeltaCaptureForJob` (:2277-2412), `replayDeltaForJob` (:2781-2912), `applyDeltaRowToTarget` (:2615-2681), `shouldReplayDeltaRow` (:2545-2595)
**Signature:** snapshot = client.beginTransaction({isolationLevel:'repeatable read', readOnly:true}) + `SET LOCAL idle_in_transaction_session_timeout = 0` + `pg_export_snapshot()`; delta log `__teable_space_migration_delta_log(seq bigserial PK, job_id, txid, schema_name, table_name, op, pk, old_row, new_row, captured_at)`.
**Data Shape:** trigger name `__teable_mig_delta_<sanitized jobId last 32>` (+`_del` twin); scoped triggers carry WHEN guards so out-of-scope rows never log. Replay cursor `{baselineSeq, lastCapturedSeq, lastReplayedSeq, lag, rowsApplied}` persisted in job copyStats.
**Decisive source:** :2368-2371 — "A single multi-event trigger cannot reference NEW and OLD in one WHEN clause, so scoped relations get an INSERT/UPDATE trigger filtered on NEW and a DELETE trigger filtered on OLD. The WHEN clause runs before the function call, so out-of-scope rows cost almost nothing." Shared-table scoping: history/trash by table_id, outbox/dead-letter by base_id, pause_scope UNFILTERED (composite scope not expressible cheaply; churn negligible — filter at replay, :2232-2236). :2831-2842 — seq is ONE bigserial shared by every migration job on the instance: baselineSeq = min-seen seq − 1 floors the cursor so "lag and progress never report the ranges burned by previous migrations as pending catch-up work".
**Flow/Invariant:** open snapshot → install triggers (snapshot tx stays OPEN through both copies) → pg_dump/psql COPY with `SET TRANSACTION SNAPSHOT <id>` → close snapshot → replay pass 1 (`delta_replaying`) → final write gate → replay pass 2 to lag 0 (`delta_cutover_replaying`). Apply = INSERT … ON CONFLICT(pk) DO UPDATE SET col=EXCLUDED.col (DO NOTHING when only the pk); DELETEs by pk ladder `__id`→`id`; generated columns stripped from the column list; json/jsonb values JSON.stringify'd before binding; metadata cached per relation.
**Probe (direct test):** specs 'serializes JSON scalar values before replaying delta rows' (:4006), 'omits generated columns when replaying delta rows' (:4055), 'ignores active migration delta triggers during base table validation' (:3962); live: `grep -c 'WHEN (NEW\.' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"shouldReplayDeltaRow","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt the exported-snapshot + per-job trigger + baseline-relative-cursor trio as one unit — dropping any one silently corrupts convergence or progress reporting.
