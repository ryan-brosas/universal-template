<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-process-runner.service.ts` @ pin `06a4461e`
**Question:** How do you supervise a pg_dump|pg_restore cross-process pipe so a half-alive pair can never hang the migration?
**Path/Symbol:** `SpaceDataDbProcessRunnerService.runPipeline` (:560-1166), `run` (:320-558)
**Signature:** `runPipeline({source,target,label}, {timeoutMs, pollMs, pollTimeoutMs, pollFailureTimeoutMs, shouldCancel, onPoll, cancelPollMs=1s, exitFallbackMs=5s, stderrLimit=16KB}) → {source: RunResult, target: RunResult}`
**Data Shape:** success requires BOTH children exit 0; every failure rejects with `SpaceDataDbProcessPipelineError{result:{source,target}}` or `...CanceledError`; output buffers are tail-truncated to limit and secret-redacted.
**Decisive source:** :926-943 — target-stdin EPIPE handling: kill the SOURCE immediately but WAIT (timer capped at min(exitFallbackMs,1000)ms) before rejecting, so target stderr arriving late is captured ("keeps target stderr when target stdin reports EPIPE before pg_restore exits", spec :771). :974-988 — `rejectMissingExitedProcesses` probes `child.kill(0)` for processes that vanished without emitting exit/close events. :369/:404/:480/:517/:554 — five fallback paths resolve pipelines when Node delivers `exit` without `close`, stdio-close before child-close, or exit status only via property polling (`syncProcessExitStatus` reads `child.exitCode/signalCode` on a 5s interval).
**Flow/Invariant:** spawn with `shell:false`, stdio ignore|pipe|pipe (source) and pipe×3 (target); wire stdout→stdin LAST after all listeners. Cancellation (`shouldCancel` polled at 1s) SIGTERMs BOTH sides and rejects CanceledError — migration checks DB state each tick so UI cancel reaches long copies within ~1s. Progress polls are single-flight (`inFlight` guard), a poll hanging >pollTimeoutMs OR failing repeatedly >pollFailureTimeoutMs kills both processes.
**Probe (direct test):** specs 'keeps target stderr when target stdin reports EPIPE before pg_restore exits' (:771), 'rejects a pipeline when a child process disappears without exit or close events' (:594), 'kills both COPY processes when cancellation is requested' (:795), 'redacts postgres_fdw user mapping passwords from process args and output' (:80); live: `grep -c 'targetCopyFailurePending' apps/nestjs-backend/src/features/space/space-data-db-process-runner.service.ts` → `3`.
**Retrieve:** `echo '{"project":"teable","pattern":"runPipeline","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt whole runner as a portable subprocess-pipeline kernel; the event-timing fallback lattice is 15 years of Node subprocess edge cases in one file.
