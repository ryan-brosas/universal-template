<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do you atomically switch traffic to the migrated database and keep rollback provably safe?
**Path/Symbol:** `validateAndSwitchJob` (:5069-5207), `inspectPostSwitchRollbackProof` (:4351-4411), `collectRollback*Findings` (:4413-4592), `rollbackMigrationForSpace` (:4156-4299)
**Signature:** dry-run path (switchOnCompletion=false): resume computed on BOTH sides → connection status ready → job succeeded → invalidate cached clients; switch path: mark 'switching' → ONE meta transaction flips DataDbConnection.ready AND upserts every copy-space binding to {mode:'byodb', state:'ready'} → invalidate target+source client caches → resume target computed (failure becomes a WARNING, job still succeeds) → succeeded.
**Data Shape:** rollback proof `{eligible, switchedAt, checkedAt, findings[]}` — eligible ⟺ findings.length === 0. Findings: relation inventory drift, row-count drift vs stored validationStats (base + scoped shared plans), post-switch timestamp rows (`__created_time/__last_modified_time > switchedAt`, shared tables via created_time/created_at/updated_at per-table plans incl. pause-scope and split_part undo-log scoping).
**Decisive source:** :4205-4212 — state is flipped to 'switching' BEFORE the proof runs so a concurrent worker sees it busy; :4287-4297 error path restores `state:'succeeded'` with the error UNLESS already restored or completed (`restoredBeforeThrow`/`rollbackCompleted` guards). Rollback refuses: non-succeeded jobs, jobs that never switched (dry-runs have nothing to roll back — the SOURCE was never abandoned), BYODB sources ('Rolling back migrations from a BYODB source is not supported yet'), missing timestamps.
**Flow/Invariant:** proof queries are read-only against the TARGET; any proof-query failure itself becomes a finding (`proof_query_failed:`) making rollback ineligible — fail-closed. Route smoke before switching: resolve routing with previewBinding and require !isMetaFallback && connectionId===target && internalSchema===target && cacheKey===target (:8503-8509).
**Probe (direct test):** specs 'validates copied data and switches the space binding to the target BYODB connection' (:4517), 'proves a post-switch rollback is clean when target inventory, counts, and timestamps match' (:6681), 'rejects post-switch rollback when target writes are detected' (:6513), 'keeps a switched migration succeeded when target computed resume fails after cutover' (:4636), 'fails validation when dry-run routing does not resolve to the target BYODB connection' (:4266); live: `grep -c 'eligible: proof.findings.length === 0' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"inspectPostSwitchRollbackProof","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt the binding-flip-in-one-transaction + proof-gated-rollback pair as an atomic-cutover pattern; adapt finding collectors to your schema's audit columns.
