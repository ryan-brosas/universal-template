<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->

# MCP Spec & Servers Foundation

## Use this for
Use when building or porting an MCP server/client: the stateless modern-era request lifecycle (`_meta` carries version/capabilities on every request), Streamable HTTP and stdio transport contracts, Multi Round-Trip Requests, `subscriptions/listen` streams, cancellation/progress patterns and the ping legacy surface, the protocol wire schemas (tools/resources/prompts registration shapes incl. tool-name grammar), task-based tool authoring and bidirectional task polling, structured tool outputs and resource_link emission, the OAuth 2.1 authorization framework protecting HTTP servers, the resource/prompt/completion data surfaces, server-to-client feature requests (elicitation incl. its -32042 error path / sampling / roots), dual-era dispatch and the Tasks/deprecation lifecycle of the 2026-07-28 revision, bounded URL fetches and storage-path/startup/exclusion operational ladders, and the official reference-server patterns that exercise them. Source code, spec prose, and direct tests are ground truth; references carry decisive excerpts and graph retrieval.

## Load the matching source dump
- `./modern-era-lifecycle.md` — stateless per-request `_meta` replaces the initialize handshake; `server/discover`; `-320xx` error partition; `resultType` polymorphism; era detection.
- `./streamable-http.md` — single POST MCP endpoint, dual Accept, header mirroring + `-32020` validation, request-scoped SSE, legacy-era fallback probing.
- `./stdio-transport.md` — newline-delimited JSON-RPC purity rules, cancellation by notification, shutdown ladder, discover-probe fallback.
- `./mrtr.md` — `InputRequiredResult`/`inputRequests`/`requestState`: server needs input ⇒ client retries with a NEW id; attacker-controlled state integrity.
- `./subscriptions.md` — `subscriptions/listen` opt-in filter, acknowledged-first ordering, `subscriptionId` demux, graceful-closure response.
- `./schema-registration.md` — wire-schema contract: Tool/Prompt/Resource shapes, `inputSchema` root `"object"` + JSON Schema 2020-12 default, CallToolResult `isError` vs protocol errors, cursors, `ttlMs`/`cacheScope`.
- `./resources-surface.md` — resources list/read/templates contract: connection-stable listings, multi-content reads, `-32602` not-found (never empty contents), `listChanged`/`subscribe` features, URI scheme guidance.
- `./prompts-surface.md` — prompts list/get contract: user-controlled selection, rendered PromptMessage lists, five content types, same stability invariant, `-32602` error mapping.
- `./content-blocks.md` — the ContentBlock union (text/image/audio/resource_link/embedded-resource) as result currency, display vs behavior annotations, unstructured+structured coexistence, isError semantics.
- `./tool-annotations-hints.md` — readOnly/destructive/idempotent/openWorld hint vocabulary, conservative defaults, readOnly-conditionality, hints-are-never-authorization rule.
- `./completion-api.md` — `completion/complete` behind `completions:{}`: ref/prompt + ref/resource refs, context.arguments threading, ≤100 values cap, hasMore/total discipline.
- `./authorization-overview.md` — OAuth 2.1 roles (server=resource server), 401→WWW-Authenticate→metadata→PKCE flow, least-privilege scope selection, union-based step-up, no token passthrough.
- `./authorization-security.md` — hard security gates: PKCE S256 verified-before-proceed, `iss` validation, audience binding via `resource`, HTTPS-only, refresh rotation, confused-deputy consent.
- `./authorization-server-discovery.md` — two-stage discovery ladder (RFC9728 protected-resource metadata → RFC8414/OIDC AS metadata), path-insertion probe order, issuer-must-match-URL anti-spoofing gate.
- `./client-registration.md` — registration priority (pre-registered → CIMD → dynamic → user prompt), Client ID Metadata Document contract, per-AS credential binding.
- `./cimd-security-risks.md` — CIMD fetch-side risk taxonomy: localhost impersonation (unmitigable, cost-raising only), SSRF guard set (URL+IP validation, size cap, timeout, rate limit), DDoS non-amplification, user-auth-before-fetch ordering, ≤24h cache, hostname display MUSTs.
- `./issuer-mixup-matrix.md` — the four-row `iss` decision matrix: record issuer from validated metadata BEFORE redirect, simple-string comparison with NO URL normalization, reject-on-absence only when advertised, compare-even-if-unadvertised deviation from RFC 9207 §2.4, never act on mismatched error fields.
- `./offline-access-refresh-contract.md` — `offline_access` tri-party duties: client MAY request it only when AS-advertised and MUST NOT assume issuance; MCP servers SHOULD NEVER advertise it in WWW-Authenticate or PRM `scopes_supported`; AS retains policy discretion over self-reported `grant_types`.
- `./client-credentials-m2m.md` — M2M authorization ladder: exactly two methods (RFC 7523 JWT assertion RECOMMENDED / HTTP Basic secret allowed), mTLS deliberately excluded, JWT contents + JWKS discovery intentionally unspecified pending WIMSE/CIMD maturity, actor-split step-up (user clients step up, M2M clients may abort).
- `./cross-app-access-enterprise.md` — SEP-990 enterprise profile: ID token→ID-JAG exchange at the IdP with policy evaluation ONLY at the IdP, ID-JAG→access-token exchange at the MAS with validation ONLY at the MAS; opt-in augmentation of the standard flow, never the default path.
- `./elicitation.md` — `elicitation/create` form vs URL modes, flat-primitive requestedSchema subset, sensitive-data-only-in-URL rule, accept/decline/cancel model.
- `./sampling.md` — `sampling/createMessage` contract incl. capability-gated tool-use loop and tool-result-only message constraints; deprecated SEP-2577.
- `./roots.md` — `roots/list` advisory filesystem-guidance contract; deprecated SEP-2577 (pass dirs/files via tool params instead).
- `./meta-key-grammar.md` — `_meta` two-segment key grammar, second-label `modelcontextprotocol`/`mcp` reservation rule, required per-request `protocolVersion`/`clientCapabilities` keys, untrusted `clientInfo`.
- `./capability-negotiation-matrix.md` — full `ClientCapabilities` vs `ServerCapabilities` matrix: presence-gating (`{}` not truthy), open sets, sub-capability granularity, per-request client vs construct-time server declaration.
- `./icon-implementation-metadata.md` — Icon/Icons/BaseMetadata/Implementation display-metadata family: name-vs-title split, title > annotations.title > name precedence, PNG/JPEG client floor, SVG-as-active-content caution.
- `./logging-stream-contract.md` — request-scoped `notifications/message`: `_meta` logLevel opt-in gate, response-stream-only delivery, syslog levels, `-32602` on unknown level; deprecated SEP-2577.
- `./pagination-caching-rules.md` — opaque-cursor discipline (empty string ≠ end), cache-key method+params rule, ttlMs freshness ladder, cacheScope public/private sharing, MRTR no-cache exclusion, notification invalidation, per-page TTL clocks.
- `./server-factory.md` — everything-server factory: capabilities at construction, register-all seams, conditional registration post-init, cleanup closure, dynamic-import launcher.
- `./http-session-transport.md` — express `/mcp` POST/GET/DELETE host: per-session transport map, connect-before-handle, event-store replay, SIGINT sweep.
- `./capability-gating.md` — register tools only when the client declares the capability; roots sync cache keyed by session with live re-fetch.
- `./conditional-tool-registration.md` — two-phase reference-server split: 12 standard tools at construction, 7 client-capability-gated tools registered inside `oninitialized` via presence checks (`|| {}`, `!== undefined`), roots sync deferred 350 ms past the initialized handler; test-pinned counts.
- `./progress-notifications.md` — progressToken opt-in gate, `notifications/progress` with relatedRequestId; test-pinned notification counts.
- `./filesystem-sandbox.md` — validatePath containment→realpath→parent chain, prefix-vulnerability-safe boundary check, wx-flag atomic writes, dual original/resolved allowlist.
- `./tool-guard-python.md` — low-level Python Server: pydantic models as schemas, McpError code mapping, robots.txt consent gate, repo-path validation + flag-injection defense.
- `./git-tool-hardening.md` — CLI-wrapper git server: four-layer defense ladder (leading-`-` prefix-reject, ref-existence validation before dispatch, `--` separator, resolve+relative_to scoping), CVE-2026-27735 path-traversal fix, symlink-aware repo containment, honest four-hint annotations.
- `./knowledge-graph-memory.md` — JSONL entity/relation/observation store: idempotent create-with-delta, cascade-on-delete, strict-add/lenient-delete asymmetry.
- `./thinking-tool.md` — LLM-facing tool hardening: preprocess coercion schemas, isError short-circuit before structured parsing, text+structuredContent dual emission.
- `./timezone-conversion-kernel.md` — DST-correct cross-zone conversion: utcoffset()-delta arithmetic after astimezone, today-anchored HH:MM wall clock, McpError INVALID_PARAMS for bad IANA keys, fractional-offset formatting, local-tz embedded in tool descriptions; freeze_time-pinned edge matrix.
- `./dual-era-server-dispatch.md` — one server process serving modern `_meta` requests AND legacy initialize clients: opening-move era selection, era caching, compatibility matrix.
- `./tasks-extension-lifecycle.md` — `io.modelcontextprotocol/tasks` extension: five-status machine, durable-create-before-respond, poll via `tasks/get`, mid-flight input via `tasks/update`, cooperative cancel.
- `./deprecated-features-registry.md` — SEP-2596 Deprecated registry: six rows (Roots/Sampling/Logging/DCR/includeContext/HTTP+SSE), SHOULD-NOT-adopt rule, migration paths, earliest-removal dates.
- `./content-annotations-audience.md` — per-content-block `Annotations {audience: Role[], priority: 0..1}` display hints with the reference error/success/debug calibration.
- `./resource-template-completers.md` — URI-template resources with `list: undefined` (invisible to list), read-time integer validation, per-variable completers, string-only prompt args.
- `./session-resource-reregistration.md` — tools creating runtime resources idempotently: map-keyed remove-then-register ladder, payload-capturing closures, resource_link returns.
- `./resource-subscription-fanout.md` — URI→Set(sessionId) subscription tracking, per-session notification intervals, undefined-as-stdio key, lazy dead-subscriber reaping.
- `./resource-updated-subscribe-ack.md` — memory-server subscribe trio: declare resources.subscribe, raw-schema handlers resolving `{}` acks, emit-side subscriber gate at every mutation site; test-pinned.
- `./dependent-argument-completion.md` — `completable()` two-stage department→name filtering via `context.arguments`; static file resources with skip-family-vs-error-content failure asymmetry.
- `./legacy-sse-transport-twin.md` — deprecated HTTP+SSE host shape: GET /sse session creation vs POST /message routing, per-session server instances, onclose reaper.
- `./stdio-transport-twin.md` — minimal reference main(): connect → SIGINT → await close → cleanup → exit 0; stderr-only diagnostics.
- `./cross-platform-path-normalization.md` — Windows/Unix/WSL/UNC classification ladder: WSL-never-converts invariant, platform-gated /c/ conversion, bare-drive-letter and UNC traps.
- `./roots-validation-ladder.md` — Root.uri → file:// strip → tilde expand → resolve → realpath → isDirectory, per-item failure isolation for batch validation.
- `./task-based-tool-authoring.md` — server-side task tools via `registerToolTask`: createTask/getTask/getTaskResult over an injected store, durable-create-before-spawn, fail-the-task `.catch`, capability-baked degradation; test-pinned `relatedTask` elicitation.
- `./bidirectional-task-client.md` — server AS polling client: dual-schema `z.union` responses (CreateTaskResult OR sync result), bounded `tasks/get` poll loops, `tasks/result` fetch, status-transcript accumulation; per-feature poll budgets.
- `./url-elicitation-error-path.md` — `-32042` `UrlElicitationRequiredError` vs request path: prerequisite points at a DIFFERENT URL + stable-input-keyed one-shot marker so client retries never loop; both guards test-pinned.
- `./bounded-url-fetch.md` — fetching attacker-supplied URLs safely: protocol+domain allowlists, AbortController deadline, Content-Length untrusted — count actual stream bytes and cancel past cap.
- `./tool-output-schema-dual-emission.md` — `outputSchema` + `structuredContent` beside backward-compatible text blocks; string-not-array payloads (#3110/#3106/#3093), byte-exact media mirroring + percent-encoded file URIs (#4029).
- `./resource-link-emission.md` — `resource_link` result blocks across static/generated/session resources: builder-constructed URIs only, metadata-complete links, manual numeric validation BEFORE pointer construction.
- `./session-toggle-tools.md` — per-session feature toggles over raw-keyed Sets (`undefined` is a legal stdio key); symmetric start/stop; independent concerns get independent sets.
- `./prompt-registration-shapes.md` — `registerPrompt` forms: no-args, required+optional args composed defensively, completer-backed schemas WITH handler re-validation, embedded resources as their own message block.
- `./cancellation-pattern.md` — modern-era cancellation: closing the SSE stream IS the signal on HTTP, `notifications/cancelled` on stdio; server→client cancelled reserved for subscription teardown; races ignored silently.
- `./progress-pattern.md` — `progressToken` opt-in grammar, strictly-increasing `progress` even without `total`, sender-discretionary cadence, stop-on-completion.
- `./tool-name-grammar.md` — SEP-986 Final naming rule: 1–64 chars, case-sensitive, `[A-Za-z0-9._/-]`; alias migration duty for renames.
- `./ping-keepalive.md` — legacy-era ping contract (empty `{}` result, sender-owned timeout policy); page ABSENT from the modern era — prefer transport-native liveness.
- `./memory-path-migration.md` — storage-path decision table: env override absolutization, guarded ONE-WAY json→jsonl migration (new-file-wins), verbatim content moves; platform-split Windows-path handling pinned by test.
- `./startup-directory-validation.md` — boot allowlist ladder: warn-and-skip inaccessible/non-directory args, exit(1) ONLY at zero-valid, stderr-only diagnostics keeping stdout protocol-pure.
- `./directory-tree-exclude-grammar.md` — exclusion patterns matched against ROOT-RELATIVE paths: glob-direct OR `**/<pat>` OR `**/<pat>/**` with `{dot:true}`; bare names reach nested dirs; subtree pruned by omission.
- `./release-changed-package-matrix.md` — calver release train: .py/.ts/.md changed-suffix detection against last tag, shared day-granular version, tag-before-publish ordering, OIDC trusted publishing both ecosystems, PyPI skip-existing vs npm fail-closed dedup asymmetry.
- `./html-to-markdown-ladder.md` — fetched-page conversion ladder: readability-style main-content extraction BEFORE markdownify ATX; three-way inclusive HTML sniff (body prefix OR text/html OR missing content-type); conversion failure degrades to an inline `<error>` string; raw payloads honestly self-label via non-empty prefix; JSON falls through unspecial-cased.
- `./fetch-outbound-request-plane.md` — consent-check and page-fetch hops build fresh clients with IDENTICAL proxy+redirect settings (robots evaluated from the same vantage as the action); robots URL keeps scheme+netloc only; comment-strip before Protego; timeout asymmetry (30s page hop only) recorded honestly.
- `./dual-user-agent-identity.md` — mode-scoped outbound identity: Autonomous UA rides the gated tool surface (consent GET included), User-Specified rides the human-driven prompt surface which runs NO gate; custom UA overrides collapse the pair.
- `./manual-fetch-prompt-twin.md` — same-named Tool+Prompt pair: every autonomous refusal NAMES the fetch prompt; the prompt twin re-runs the shared pipeline ungated and softens McpError into readable user-role content; raise_exceptions=False keeps one bad request from killing the process.
- `./tool-catalog-static-conditional-split.md` — the everything-server catalog as data: 12 static tools at construction vs 7 capability-gated post-init; explicit `{}` input schemas; always-present four-hint annotations; minimal sync sampling round trip.
- `./validation-error-taxonomy.md` — SEP-1303 error routing: input-validation failures ride `isError: true` (Tool Execution Error, LLM-visible) NOT `-32602` (Protocol Error, client-swallowed); only unknown-tool/malformed/server errors are `-32xxx`; the model can only self-correct from feedback that reaches its context window.
- `./stateful-tool-handles.md` — SEP-2567 explicit-handle pattern: no protocol session, so cross-call state rides a server-minted handle returned by a creation tool and threaded through later calls; handle is a name not a capability (authorize every call, opaque, bounded lifetime, expiry as tool-execution error).
- `./extension-negotiation.md` — SEP-2133 extensions: `{vendor-prefix}/{name}` identifiers in an `extensions` capabilities map (per-extension settings, `{}` = supported); mandatory graceful degradation when a peer lacks an extension; new-id for breaking changes.
- `./x-mcp-header-param-mirroring.md` — `x-mcp-header` in a parameter's JSON Schema mirrors its value into `Mcp-Param-{name}` so intermediaries route without parsing bodies; primitive-only, statically-reachable-only, case-insensitively-unique; clients reject per-tool (exclude from tools/list); never annotate secrets.
- `./sse-polling-at-will-disconnect.md` — SEP-1699 era ladder: ≤2025-11-25 servers MAY close the connection before responding once an `{id, data:""}` priming event reached the client; client polls via GET + `Last-Event-ID` honoring the SSE `retry` field; disconnect ≠ cancel (explicit `CancelledNotification`); 2026-07-28 removes resumability and inverts close semantics to cancel-on-stream-close.
- `./schema-artifact-pipeline.md` — published `schema.json` is GENERATED from `schema/<v>/schema.ts`; era partition (legacy draft-07 vs modern 2020-12) is one array plus one shared transform applied on BOTH write and `--check` paths; hand edits fail CI with the exact regeneration command.
- `./example-validation-gate.md` — Ajv validates `examples/<TypeName>/*.json` fixtures with dialect sniffed from each artifact's own `$schema`; unknown fixture type dirs fail closed, absent examples dirs fail open.
- `./mdx-esm-comment-gate.md` — docs gate parses MDX with remark-parse+remark-mdx (the production consumer's parser) to reject JS comments inside ESM blocks that only break production; parse errors delegate to other checks.
- `./sep-render-drift-gate.md` — SEP pages/nav are regenerated then compared Prettier-NORMALIZED (like-for-like bytes); convention-parsed metadata with skip-lists-before-grammar; idempotent docs.json tab upsert.
- `./typedoc-schema-docs-pipeline.md` — typedoc custom output renders ONE schema-reference page to STDOUT (flush-awaited): publish `> path`, verify raw-byte `| cmp published -`; compare strictness matches byte ownership; `.prettierignore` fences generated docs.
- `./template-category-slotting.md` — `{/* @category X */}` slots fail closed BOTH directions (missing-category throw, Invalid-category throw); RPC categories = backtick+lowercase; suffix-ladder ordering Request→RequestParams→ResultResponse→Result→Notification→NotificationParams.
- `./mintlify-markup-laundering.md` — anchors mirror the consumer's lowercase ID scheme; h1–h6 → `data-typedoc-h` divs; examples → `<details>` with reflection-namespaced ids inside hidden content; Markdown metachars entity-encoded inside HTML.
- `./server-request-association.md` — SEP-2260 (Final): `roots/list`/`sampling/createMessage`/`elicitation/create` MUST be nested inside an originating client request; standalone server-initiated forms MUST NOT be implemented; `ping` is the sole exception; clients SHOULD answer unassociated instances `-32602`; human-in-the-loop timeout/keepalive duty; Final-but-not-yet-in-2026-07-28-text drift.
- `./local-server-install-consent.md` — SEP-1024 (Final): one-click LOCAL server install MUST show the exact untruncated command + all args + danger warning, require explicit affirmative opt-in, offer cancel, and abort on denial/no-response; client-side duty, wire format unchanged.
- `./payload-method-decoupling.md` — SEP-1319 (Final): every RPC `params`/`result` is a standalone named schema referenced by the method wrapper (Data Layer vs Transport Layer); non-breaking (wire JSON identical); the escape hatch for non-JSON-RPC bindings; already the shipped shape of `schema.ts`.

## Capsule map
- **Protocol core** — `modern-era-lifecycle`, `meta-key-grammar`, `schema-registration`, `tool-name-grammar`, `resources-surface`, `prompts-surface`, `dual-era-server-dispatch`, `tasks-extension-lifecycle`, `task-based-tool-authoring`, `bidirectional-task-client`, `deprecated-features-registry`: every request self-describes (`io.modelcontextprotocol/*` `_meta` keys under a two-segment grammar with reserved second labels); capability state is declared PER REQUEST (`clientCapabilities` required, never inferred); tool names are case-sensitive 1–128-char `[A-Za-z0-9_.-]` strings (no `/`) with an alias migration duty; results are polymorphic via `resultType`; error codes are partitioned (`-32020..-32099` spec-reserved); list/read results are paginated, cacheable, and connection-stable (vary by auth, never per-connection); dual-era servers pick semantics from the client's opening move; long-running work rides the tasks extension on BOTH sides (servers author task tools over an injected store; servers polling client-executed tasks accept sync-or-task responses); SEP-2596 gates which legacy surfaces new code may adopt.
- **Error routing & state** — `validation-error-taxonomy`, `stateful-tool-handles`: input-validation failures ride `isError: true` (Tool Execution Error, LLM-visible) while only request-structure failures are `-32xxx` protocol errors (SEP-1303) — the model can only self-correct from feedback that reaches its context window; with no protocol session, cross-call state rides explicit server-minted handles returned by a creation tool and threaded through later calls, where a handle is a name not a capability (authorize every call, opaque, bounded lifetime, expiry as a tool-execution error) (SEP-2567).
- **Transports** — `streamable-http`, `stdio-transport`, `http-session-transport`, `legacy-sse-transport-twin`, `stdio-transport-twin`, `sse-polling-at-will-disconnect`: one endpoint POST-everything with mirrored headers (HTTP) vs one newline-framed channel with stdout purity (stdio); cancellation is closing-the-stream (HTTP) or `notifications/cancelled` (stdio); reference twins pin per-session server instances (legacy SSE) and the SIGINT close→cleanup→exit ladder; SEP-1699 adds the ≤2025-11-25 at-will disconnect ladder (prime → retry-paced poll → GET+Last-Event-ID resume) that the modern era deliberately removed.
- **Extension & header surfaces** — `extension-negotiation`, `x-mcp-header-param-mirroring`: optional extensions are identified as `{vendor-prefix}/{name}` and advertised in an `extensions` capabilities map with per-extension settings, gated on the peer's map with mandatory graceful degradation (fallback to core behavior or reject if mandatory) and a new-id for breaking changes (SEP-2133); routing-relevant tool parameters can be mirrored into `Mcp-Param-{name}` headers via `x-mcp-header` (primitive-only, statically-reachable-only, case-insensitively-unique; clients reject per-tool, never annotate secrets) so intermediaries act without parsing bodies.
- **Interaction patterns** — `mrtr`, `subscriptions`, `progress-notifications`, `progress-pattern`, `cancellation-pattern`, `logging-stream-contract`: server-needs-input becomes a retry-with-state loop, change notifications ride explicit opt-in listen streams, progress rides the originating request's token (strictly-increasing values, stop-on-completion), cancellation is transport-native (stream-close on HTTP, notification on stdio) with server-side use reserved to subscription teardown, and log messages ride the requesting response stream behind a per-request `_meta` logLevel opt-in (deprecated SEP-2577).
- **Data surfaces** — `completion-api`, `content-blocks`, `content-annotations-audience`, `tool-output-schema-dual-emission`, `resource-link-emission`, `tool-catalog-static-conditional-split`, `tool-annotations-hints`, `icon-implementation-metadata`, `pagination-caching-rules`, `resource-template-completers`: one five-type ContentBlock union fills tool results, prompt messages, and sampling; typed tools declare `outputSchema` and emit structuredContent BESIDE backward-compatible text blocks (string-not-array payloads, byte-exact media mirroring); tools hand clients resource POINTERS via builder-constructed `resource_link` blocks; the reference catalog splits 12 static vs 7 capability-gated registrations with explicit `{}` schemas and always-present hint quadruples; completion autocompletes prompt args/template URIs under a 100-item cap; annotation families split into display hints (tool-level), untrusted behavior hints, and per-block audience/priority display hints; Icon/BaseMetadata mixins give every primitive a name/title/icons identity; opaque cursors page long lists while ttlMs/cacheScope hint freshness with hard MRTR no-cache exclusions; URI templates expose unbounded resource families invisibly to list.
- **Authorization (HTTP transports)** — `authorization-overview`, `authorization-security`, `authorization-server-discovery`, `client-registration`, `cimd-security-risks`, `issuer-mixup-matrix`, `offline-access-refresh-contract`, `client-credentials-m2m`, `cross-app-access-enterprise`: the MCP server is an OAuth 2.1 resource server; discovery ladders end in issuer-validated AS metadata recorded per-request beside PKCE verifier/state; PKCE/iss/audience/no-passthrough are hard MUSTs; CIMD is the modern registration default with an honest risk taxonomy (localhost impersonation cost-raised, not solved); `iss` validation is a four-row matrix with zero URL normalization; refresh tokens ride client-requested `offline_access` that resources never advertise; M2M deployments get a two-method client-credentials ladder; enterprise SSO rides SEP-990 ID-JAG delegation as an opt-in profile.
- **Server↔client features** — `elicitation`, `url-elicitation-error-path`, `sampling`, `roots`, `roots-validation-ladder`: server-initiated requests ride MRTR `inputRequests` gated on client capabilities; elicitation is current (secrets URL-only) with an error-path variant (`-32042` + different-URL prerequisite + stable-keyed one-shot marker) for browser-first prerequisites; sampling and roots are deprecated SEP-2577 legacy interop whose Root URIs still validate through realpath-first per-item ladders.
- **Reference-server patterns** — `server-factory`, `conditional-tool-registration`, `capability-negotiation-matrix`, `capability-gating`, `prompt-registration-shapes`, `session-toggle-tools`: construct-with-capabilities then register static tools immediately; register client-capability-gated tools only inside `oninitialized` (presence checks against `{}` defaults); defer outbound requests like roots sync past the initialized handler; prompts ship in four canonical forms with completer-plus-revalidation discipline; per-session feature toggles keep raw session keys in symmetric start/stop sets; always return a cleanup closure.
- **Runtime resource & subscription mechanics** — `session-resource-reregistration`, `resource-subscription-fanout`, `resource-updated-subscribe-ack`, `dependent-argument-completion`: tools create resources idempotently via remove-then-register maps and hand back resource_link pointers; subscriber Sets keyed by URI tolerate undefined-as-stdio sessions and reap dead subscribers lazily inside notification loops; the memory-server subscribe trio declares `resources.subscribe`, answers subscribe/unsubscribe with `{}` acks, and gates every mutation's updated-notification behind an emit-side membership check; dependent prompt arguments autocomplete through `context.arguments`.
- **Security boundaries** — `filesystem-sandbox`, `bounded-url-fetch`, `cross-platform-path-normalization`, `tool-guard-python`: containment checked against REAL paths (symlink-aware) plus parent-of-new-file; outbound fetches of client-supplied URLs run protocol→domain→deadline→counted-stream gates; cross-platform path classification never converts WSL paths; consent gates (robots.txt) and argument hardening ('-' prefix rejection) before side effects.
- **Artifact & docs gates** — `schema-artifact-pipeline`, `example-validation-gate`, `mdx-esm-comment-gate`, `sep-render-drift-gate`, `typedoc-schema-docs-pipeline`, `template-category-slotting`, `mintlify-markup-laundering`: every published protocol artifact is GENERATED from a source of truth (`schema/<v>/schema.ts` types; hand-written `seps/*.md`) and guarded by a regenerate-and-compare CI gate that exits 1 naming the exact regeneration command; the legacy/modern JSON-Schema dialect split lives in one version array plus ONE shared transform applied identically on write and verify paths; example fixtures validate under dialect sniffed from each artifact's own `$schema` with unknown type dirs failing closed and absent optional planes failing open; docs gates parse with the production consumer's remark-mdx parser because local-tolerant JS comments break production MDX; SEP page checks compare Prettier-NORMALIZED regenerated output so formatter drift never false-positives; the published schema.mdx reference page instead rides a typedoc custom output that renders ONE page to STDOUT (flush-awaited) so publish (`> path`) and verify (`| cmp published -`) share a single deterministic code path behind a RAW-BYTE cmp gate that `.prettierignore` fences from the formatter, with fail-closed `{/* @category */}` template slots ordered by name-suffix ladders and anchors mirroring Mintlify's lowercase ID scheme (`typedoc-schema-docs-pipeline`, `template-category-slotting`, `mintlify-markup-laundering`).
- **Storage & lifecycle ops** — `memory-path-migration`, `startup-directory-validation`, `ping-keepalive`, `directory-tree-exclude-grammar`: storage paths resolve via env-override precedence with guarded one-way format migration; boot allowlists warn-and-skip bad dirs and fail closed only at zero-valid; legacy ping answers `{}` promptly while modern code prefers transport-native liveness; recursive listings exclude by root-relative triple-match glob grammar with dot-awareness.
- **CLI-wrapper hardening & ops kernels** — `git-tool-hardening`, `timezone-conversion-kernel`, `release-changed-package-matrix`: tools that forward LLM strings toward CLIs layer prefix-reject → domain-API existence validation → `--` separators → resolve+relative_to containment (both staged paths AND the requested repo against a launch-time root); timezone math derives differences from utcoffset() deltas AFTER astimezone on a today-anchored wall clock so DST asymmetry windows, 45/30-minute offsets, and date-line zones need no special cases; release trains detect changed packages by payload suffix against the last tag, share one calver version per day, push the tag BEFORE publishing, and encode each registry's re-run semantics explicitly.
- **Web-fetch reference pipeline** — `html-to-markdown-ladder`, `fetch-outbound-request-plane`, `dual-user-agent-identity`, `manual-fetch-prompt-twin`: fetched pages are simplified readability-FIRST and converted to ATX markdown only after a three-way inclusive HTML sniff (body prefix OR text/html OR missing content-type), where conversion failure degrades to an inline `<error>` string and raw payloads honestly self-label via their non-empty prefix (JSON needs no special case); the consent-check and page-fetch hops build fresh clients with IDENTICAL proxy+redirect settings so robots.txt is evaluated from the same network vantage as the action it authorizes, with origin-root URL derivation keeping scheme+netloc only; outbound identity is mode-scoped — an Autonomous UA on the gated tool surface (consent GET included) versus User-Specified on the human-driven prompt surface that skips the gate entirely; and every autonomous refusal NAMES the same-named fetch prompt, which re-runs the shared pipeline ungated and softens McpError into readable user-role content while `raise_exceptions=False` keeps single failures from killing the process.
- **Data & reasoning tools** — `knowledge-graph-memory`, `thinking-tool`: set-semantics JSONL graph store; coercion-first schemas with dual text/structured output.
- **Final SEPs — forward-looking invariants** — `server-request-association`, `local-server-install-consent`, `payload-method-decoupling`: Standards-Track SEPs that are Final but whose MUST language is not always yet folded into the current normative revision — server→client feature requests (`roots/list`/`sampling/createMessage`/`elicitation/create`) are reactive and MUST nest inside an originating client request (only `ping` may be unsolicited; clients reject unassociated instances with `-32602`); one-click local-server installs are gated by exact-command transparency + affirmative opt-in + cancel (a client duty, not a wire change); and every RPC payload is a standalone named schema referenced by its method wrapper so non-JSON-RPC bindings can reuse the same message models without forking them (non-breaking — wire JSON identical).

## Extending the foundation
Add one `./<seam>.md` capsule for one graph-selected, source-confirmed porting question. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf.

## Provenance
modelcontextprotocol/specification (MIT), `main@57ac4a2ec742e0cb7622d899b0f5d3bcf769fd69`; modelcontextprotocol/servers (MIT), `main@599dafc1054550a6eeb87a6545c1e1b03b3ca827`; Codebase Memory projects `modelcontextprotocol` (12,014 nodes / 15,491 edges) and `servers` (1,151 nodes / 2,590 edges) — the LIVE short-name graphs at exactly these HEADs (roots `$REFERENCE_ROOT/modelcontextprotocol` and `$REFERENCE_ROOT/servers`, FULL mode, skipped 0; parse_partial = 6 blog HTML layouts / 3 Dockerfiles only). Graph-naming history matters here: pass 9 [DONE:169] rewrote every capsule citation short-name → path-slugged when `mnt-hdd-utopia-inspo-mcp-*` was canonical; the later codebase-memory/inspo re-org DELETED the slugged projects (they now return "project not found") and re-created the short names fresh at the SAME pins, so **pass 11 (2026-08-25T07:11:41Z) inverted pass 9** and repaired all 154 dead citations back to the live short names (Source lines, Retrieve `project:` fields, CLI `--project` args, and `trace_path` qn prefixes such as `servers.src.filesystem.lib.validatePath`). Earlier passes: 1–4 core protocol surfaces; 5 symbol-granular sweep (+16 → 60); 6 SEP/modern-spec drift sweep (+4 → 64; corrected `tool-name-grammar` against 2026-07-28: 1–128 chars, no `/`, superseding SEP-986); 7 upstream-drift re-entry (rewrote `subscriptions` graceful-closure wording; added `resource-updated-subscribe-ack`, → 65); 8 [DONE:161] added `sse-polling-at-will-disconnect` SEP-1699 (→ 66); 10 [DONE:192] recovered five unwired OAuth-family capsules authored by an interrupted sibling (→ 71). **Pass 11 also mined the previously-uncited artifact-integrity tooling plane (+4 → 75): `schema-artifact-pipeline` (era-partitioned schema.json generation with regenerate-and-compare `--check`), `example-validation-gate` (dialect-sniffed Ajv over `examples/<TypeName>/*.json`; fail-closed unknown types, fail-open absent dirs), `mdx-esm-comment-gate` (production-consumer parser gate for JS comments in MDX ESM blocks), `sep-render-drift-gate` (format-normalized regenerate-and-compare, convention-parsed metadata, idempotent docs.json nav upsert). Every RED failure mode executed in an isolated twin (exit 1 each with exact diagnostics); GREEN = repo gates `check:schema:json` (6/6 up to date), `check:schema:examples` (258 passed), `check:docs:js-comments` (344 files OK), `check:seps` (41 SEPs up to date). **Pass 12 (2026-08-25, same pin) mined the last uncited in-boundary code subsystem — the typedoc schema.mdx generation plane (+3 → 78): `typedoc-schema-docs-pipeline` (stdout single-page custom output behind a raw-byte `cmp` gate with `.prettierignore` formatter fencing), `template-category-slotting` (fail-closed `{/* @category */}` slots + name-suffix ordering ladder), `mintlify-markup-laundering` (consumer-identity anchors, heading demotion, entity-encoded metachars); a 62-file indexed-code inventory enumeration proved every remaining in-boundary seam cited or boundary-excluded — the repo's CODE plane is exhausted at this pin; only upstream drift can reopen it.** **Pass 13 (2026-08-26, same pin 57ac4a2e) mined the last uncited in-boundary subsystem — the SEP corpus (`seps/*.md`, 41 files) (+3 → 81): `server-request-association` (SEP-2260 Final: server→client `roots/list`/`sampling/createMessage`/`elicitation/create` MUST nest inside an originating client request, `ping` excepted, clients reject unassociated with `-32602`; drift finding: Final but absent from the 2026-07-28 normative pages), `local-server-install-consent` (SEP-1024 Final: one-click local-server install gated by exact-untruncated-command + affirmative opt-in + cancel), `payload-method-decoupling` (SEP-1319 Final: standalone named Params/Result schemas referenced by the RPC wrapper = Data Layer vs Transport Layer, non-breaking, enables non-JSON-RPC bindings; confirmed in `schema.ts` :1863/:1884). A full 41-SEP disposition sweep proved every remaining SEP is cited, covered-by-an-existing-capsule, or boundary-excluded (process/governance + ext-apps pointer) — the SPEC plane is now exhausted at this pin too; only upstream drift can reopen it.

**Pass 12 — servers lane, src/fetch subsystem completed (2026-08-25): +4 servers-lane capsules `html-to-markdown-ladder`, `fetch-outbound-request-plane`, `dual-user-agent-identity`, `manual-fetch-prompt-twin` (whole-file reads of server.py 288L + tests 326L; first live fetch-suite run: pytest 20/20 via throwaway venv). The conditional everything-server corners resolved as omissions-with-reason: no `greeting*` symbol exists at this pin (graph text search 0 matches), and `get-structured-content` duplicates `tool-output-schema-dual-emission`.**

## Full view (memory graph)
Revalidate before porting: projects `modelcontextprotocol` (root `$REFERENCE_ROOT/modelcontextprotocol`, branch `main@57ac4a2ec742e0cb7622d899b0f5d3bcf769fd69`, ready, full mode, 12,014 nodes / 15,491 edges, skipped 0; parse_partial = 6 blog HTML layouts only; 133 blog images not-indexed by design) and `servers` (root `$REFERENCE_ROOT/servers`, branch `main@599dafc1054550a6eeb87a6545c1e1b03b3ca827`, ready, full mode, 1,151 nodes / 2,590 edges; parse_partial = 3 Dockerfiles only). The path-slugged projects `mnt-hdd-utopia-inspo-mcp-modelcontextprotocol` / `mnt-hdd-utopia-inspo-mcp-servers` were deleted by the graph re-org and answer "project not found" — cite ONLY the live short names above. Run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, `get_code_snippet`; source, spec prose, and direct tests decide shipped claims.

## Boundaries
Adopt the stateless per-request `_meta` lifecycle, header/body cross-validation, MRTR retry semantics, opt-in subscription streams, fail-closed sandbox validation chains, the register-then-conditionally-extend server factory, the OAuth 2.1 authorization framework (mandatory for any HTTP-exposed server), its M2M and enterprise extensions (client-credentials two-method ladder and SEP-990 ID-JAG delegation only when no end user / an enterprise IdP is in play — both opt-in profiles over the standard flow), and the `offline_access`/`iss` client-side duties (never advertise refresh scopes as a resource; record-and-compare issuer with zero URL normalization), the connection-stable resource/prompt listing rules, the SEP-1303 validation-vs-execution error routing, SEP-2567 explicit-handle state (never a protocol session), SEP-2133 extension negotiation with mandatory graceful degradation, and `x-mcp-header` param mirroring for routing-relevant non-secret params; adopt the SEP-1699 disconnect ladder ONLY when you must serve ≤2025-11-25-era clients — pure modern-era code omits it (see `sse-polling-at-will-disconnect`); adopt the spec-repo artifact-integrity gate plane whenever you publish derived protocol artifacts — generated schema JSON with regenerate-and-compare CI, dialect-partitioned transforms, stdout-piped generated doc pages verified by raw-byte cmp behind formatter fencing (`.prettierignore`), fail-closed placeholder template slots, consumer-identity anchor schemes for embedded generator HTML, consumer-parser docs gates, and format-normalized page-drift checks (see the Artifact & docs gates group); adapt transport hosts (express/Node vs your framework), storage backends, user agents, and tool catalogs to your product; adopt sampling/roots only for legacy-client interop (both deprecated SEP-2577) and consult `deprecated-features-registry` before adopting any legacy surface (ping included — modern era has no ping page); adopt the git-tool-hardening ladder whenever a tool forwards model strings toward a CLI or option-parsing surface (prefix-reject → validate-via-domain-API → `--` → resolve+relative_to scoping), the timezone kernel's offset-delta-after-astimezone discipline for any cross-zone time math, and the release train's tag-before-publish + OIDC-trusted-publishing + explicit registry idempotency semantics when operating multi-package monorepos; adopt the three Final-SEP forward-looking invariants for new code — server→client feature requests nested only inside an originating client request with `ping` as the sole unsolicited exception and `-32602` rejection of unassociated instances (SEP-2260), exact-command-transparency + affirmative-opt-in + cancel gating on one-click local-server installs (SEP-1024), and standalone named payload schemas referenced by the RPC wrapper so non-JSON-RPC bindings reuse the same message models (SEP-1319) — noting these are Final Standards-Track SEPs whose MUST text may not yet appear in the current normative revision; omit the spec-repo website/blog tooling, SEP automation bot, community/working-group pages, SDK internals (covered by `mcp-ts-sdk-foundation`), Dockerfiles, and Inspector unless building those products directly.

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`authorization-overview.md`](./authorization-overview.md)
- [`authorization-security.md`](./authorization-security.md)
- [`authorization-server-discovery.md`](./authorization-server-discovery.md)
- [`bidirectional-task-client.md`](./bidirectional-task-client.md)
- [`bounded-url-fetch.md`](./bounded-url-fetch.md)
- [`cancellation-pattern.md`](./cancellation-pattern.md)
- [`capability-gating.md`](./capability-gating.md)
- [`capability-negotiation-matrix.md`](./capability-negotiation-matrix.md)
- [`cimd-security-risks.md`](./cimd-security-risks.md)
- [`client-credentials-m2m.md`](./client-credentials-m2m.md)
- [`client-registration.md`](./client-registration.md)
- [`completion-api.md`](./completion-api.md)
- [`conditional-tool-registration.md`](./conditional-tool-registration.md)
- [`content-annotations-audience.md`](./content-annotations-audience.md)
- [`content-blocks.md`](./content-blocks.md)
- [`cross-app-access-enterprise.md`](./cross-app-access-enterprise.md)
- [`cross-platform-path-normalization.md`](./cross-platform-path-normalization.md)
- [`dependent-argument-completion.md`](./dependent-argument-completion.md)
- [`deprecated-features-registry.md`](./deprecated-features-registry.md)
- [`directory-tree-exclude-grammar.md`](./directory-tree-exclude-grammar.md)
- [`dual-era-server-dispatch.md`](./dual-era-server-dispatch.md)
- [`dual-user-agent-identity.md`](./dual-user-agent-identity.md)
- [`elicitation.md`](./elicitation.md)
- [`example-validation-gate.md`](./example-validation-gate.md)
- [`extension-negotiation.md`](./extension-negotiation.md)
- [`fetch-outbound-request-plane.md`](./fetch-outbound-request-plane.md)
- [`filesystem-sandbox.md`](./filesystem-sandbox.md)
- [`git-tool-hardening.md`](./git-tool-hardening.md)
- [`html-to-markdown-ladder.md`](./html-to-markdown-ladder.md)
- [`http-session-transport.md`](./http-session-transport.md)
- [`icon-implementation-metadata.md`](./icon-implementation-metadata.md)
- [`issuer-mixup-matrix.md`](./issuer-mixup-matrix.md)
- [`knowledge-graph-memory.md`](./knowledge-graph-memory.md)
- [`legacy-sse-transport-twin.md`](./legacy-sse-transport-twin.md)
- [`local-server-install-consent.md`](./local-server-install-consent.md)
- [`logging-stream-contract.md`](./logging-stream-contract.md)
- [`manual-fetch-prompt-twin.md`](./manual-fetch-prompt-twin.md)
- [`mdx-esm-comment-gate.md`](./mdx-esm-comment-gate.md)
- [`memory-path-migration.md`](./memory-path-migration.md)
- [`meta-key-grammar.md`](./meta-key-grammar.md)
- [`mintlify-markup-laundering.md`](./mintlify-markup-laundering.md)
- [`modern-era-lifecycle.md`](./modern-era-lifecycle.md)
- [`mrtr.md`](./mrtr.md)
- [`offline-access-refresh-contract.md`](./offline-access-refresh-contract.md)
- [`pagination-caching-rules.md`](./pagination-caching-rules.md)
- [`payload-method-decoupling.md`](./payload-method-decoupling.md)
- [`ping-keepalive.md`](./ping-keepalive.md)
- [`progress-notifications.md`](./progress-notifications.md)
- [`progress-pattern.md`](./progress-pattern.md)
- [`prompt-registration-shapes.md`](./prompt-registration-shapes.md)
- [`prompts-surface.md`](./prompts-surface.md)
- [`release-changed-package-matrix.md`](./release-changed-package-matrix.md)
- [`resource-link-emission.md`](./resource-link-emission.md)
- [`resource-subscription-fanout.md`](./resource-subscription-fanout.md)
- [`resource-template-completers.md`](./resource-template-completers.md)
- [`resource-updated-subscribe-ack.md`](./resource-updated-subscribe-ack.md)
- [`resources-surface.md`](./resources-surface.md)
- [`roots-validation-ladder.md`](./roots-validation-ladder.md)
- [`roots.md`](./roots.md)
- [`sampling.md`](./sampling.md)
- [`schema-artifact-pipeline.md`](./schema-artifact-pipeline.md)
- [`schema-registration.md`](./schema-registration.md)
- [`sep-render-drift-gate.md`](./sep-render-drift-gate.md)
- [`server-factory.md`](./server-factory.md)
- [`server-request-association.md`](./server-request-association.md)
- [`session-resource-reregistration.md`](./session-resource-reregistration.md)
- [`session-toggle-tools.md`](./session-toggle-tools.md)
- [`sse-polling-at-will-disconnect.md`](./sse-polling-at-will-disconnect.md)
- [`startup-directory-validation.md`](./startup-directory-validation.md)
- [`stateful-tool-handles.md`](./stateful-tool-handles.md)
- [`stdio-transport-twin.md`](./stdio-transport-twin.md)
- [`stdio-transport.md`](./stdio-transport.md)
- [`streamable-http.md`](./streamable-http.md)
- [`subscriptions.md`](./subscriptions.md)
- [`task-based-tool-authoring.md`](./task-based-tool-authoring.md)
- [`tasks-extension-lifecycle.md`](./tasks-extension-lifecycle.md)
- [`template-category-slotting.md`](./template-category-slotting.md)
- [`thinking-tool.md`](./thinking-tool.md)
- [`timezone-conversion-kernel.md`](./timezone-conversion-kernel.md)
- [`tool-annotations-hints.md`](./tool-annotations-hints.md)
- [`tool-catalog-static-conditional-split.md`](./tool-catalog-static-conditional-split.md)
- [`tool-guard-python.md`](./tool-guard-python.md)
- [`tool-name-grammar.md`](./tool-name-grammar.md)
- [`tool-output-schema-dual-emission.md`](./tool-output-schema-dual-emission.md)
- [`typedoc-schema-docs-pipeline.md`](./typedoc-schema-docs-pipeline.md)
- [`url-elicitation-error-path.md`](./url-elicitation-error-path.md)
- [`validation-error-taxonomy.md`](./validation-error-taxonomy.md)
- [`x-mcp-header-param-mirroring.md`](./x-mcp-header-param-mirroring.md)
