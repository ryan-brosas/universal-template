# Global crew acceptance

- Confirm global autoload and mesh defaults (initially absent/disabled).
- Native Fabric component; no SDK session or custom daemon at runtime.
- Root/trust guards; no recursive child installation.
- Six portable roles; reuse existing actors without replacement or model changes.
- Serialized creation across concurrent roots; partial creation recoverable.
- Exact session/checkpoint reflection; no arbitrary latest-session selection.
- Project-isolated native mesh and existing Hindsight scope; capability discovery.
- Tests: isolation, idempotency, conflicts, hook dedupe, missing dependencies.
- Live SDK probe: normal global discovery in two disposable projects, resume/reuse.

Status: CLOSED 2026-09-05 (observer coverage). Live two-session probe CONFIRMED the reported gap: session 1 root session:…961f owned both event observers (self); session 2 root session:…b9dd saw both as foreign and a real tool_error delivered 0 messages to project-supervisor (inbox 0). No supported rebinding API exists (no agents adopt/rebind; ownership moves only via resident-host orphan adoption, never to a new Main root), so the component now: (a) computes observerCoverage(mainRootId, actors, desired) — an observer is a gap when its role expects host events but none are subscribed, or it subscribes events owned by a foreign root (mesh-topic subscribers are root-agnostic and excluded); (b) reports state "degraded" with gaps in /crew, the session guide, and a ui warning — never silently active; (c) preserves actor identities (no destroy/recreate); (d) queues the explicit session/checkpoint reflection when a legacy reflector is foreign-rooted (its native subscription belongs to another root) instead of silently deferring. Mailbox targets (scout/verifier ask/tell, foundation topics) still route cross-session through the owner. Regression tests: lineage.test.mjs (degraded+gaps+preserved roots, foreign-reflector explicit checkpoint, same-root/m subscription fail-closed) — 12/12 total. Earlier verification (component attach/lock/reflection; not observer coverage): Unit tests 9/9 (definitions contract, reuse, running+stopped preflight, duplicate rejection, lock serialization/contention, exact reflection routing, session_compact handler exactly-once + legacy-reflector deference, child/untrusted skip). Live two-project SDK probe PASSED: project A created 6 durable actors (component active), second session in the same project reused identical actor IDs (no replacement), project B created its own 6 with distinct IDs (project isolation); probe roots and resident hosts cleaned up. Fixed during verification: preflight now rejects running actors (not only stopped); component guidance model glob must be provider/model shape ("*/*"). Global wiring: ~/.pi/agent/fabric.json mesh.enabled=true plus components entry project-crew=local-pi-crew; package registered in global settings.json packages.

Automatic result processing: IMPLEMENTED; fresh-session live acceptance BLOCKED.

The earlier helper-only completion claim was incorrect. Production code now registers input dispatch, reserves per-actor work before awaits, consumes correlated native ask replies, delivers findings to Main without triggering turns, validates source excerpts and submits explicitly unverified proposals through the pinned broker. Full-registry lookup repairs component-level auto-actor reuse. `workflow.test.mjs` covers the registered event-to-retention path, concurrency/deduplication, bad replies/evidence, project isolation, delivery/retention errors and late replies. The new input, reuse and ask-path tests failed before implementation.

Live evidence: an existing scout returned the actual FabricActorMessage envelope; the production consumer accepted its source-backed proposal. Hindsight returned document `crew-b6bb88883ca624f785905fd7` with `enqueued: true`, `sent: 0`. This is local queue acceptance, NOT completed indexing. The default suite includes all workflow tests. The independent compiler pressure tests reject both prior premature-completion rationalizations and preserve the legitimate destructive-action permission boundary.

Remaining environment blocker: the installed SDK fails before session startup because `@earendil-works/pi-server` is missing. No global dependency installation was attempted. Fresh-session loading and a single uninterrupted live registered-event-to-indexed-memory probe remain unverified. Actor extension access is still not a sandbox; direct calls can bypass this broker. No claim of cross-session semantic deduplication or crash-proof exactly-once effects is made.

Session model bindings (CLOSED 2026-09-05): fresh-session SDK probe confirmed component active (revision 1, no extension errors, six actors reused) while all actors resolve to the fresh session's model — session bindings are per-session layers, not durable pins. Non-owner sessions CAN write session-scope bindings (disposable session 01a07048 pinned project-scout to zro/deepseek-v4-flash-0731: pinError null, binding + resolved model verified), but owner-gated scope:'project' pins stay resident-owned (setModel from non-owner throws owned-by-another-host). Added /crew-model <provider/id>|clear command pinning all six actors for the current session with per-actor error reporting; unit-tested 13/13 (registration, usage guard, six session-scope setModel calls, per-actor error tolerance, clear omits model, inactive-crew no-op). A session predating the package install cannot resolve the component definition (components.reload fails: definition unavailable) — documented, not fixable mid-session; new sessions activate normally.
