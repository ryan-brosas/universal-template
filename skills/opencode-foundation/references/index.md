<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->

# OpenCode Foundation

## Use this for
Client/server coding-agent harnesses: shadow-git snapshot undo, a deferred-suspension permission model, protocol-first API codegen, and multi-surface clients. Source and tests are the contract; references resolve to decisive excerpts and flows.

## Load the matching source dump
- `./snapshot.md` — shadow-git undo: alternates seeding, ignore-drift correction, semaphores, NUL pathspecs.
- `./permissions.md` — ruleset evaluation, Deferred suspension, rejection-as-feedback, session-scoped approval.
- `./sessions.md` — event-sourced persistence, fork-as-graph-rewrite, patch semantics, stream guards.
- `./editing.md` — nine-replacer fuzzy edit chain, collision triad, locked edit transactions, four-pass applier.
- `./write-tool.md` — permission-gated full-file write, BOM preservation, format re-sync, LSP feedback.
- `./read-tool.md` — offset/limit paginated reads, `more` flag, directory listing.
- `./grep-glob-tools.md` — ripgrep-backed content/file search, include filtering.
- `./apply-patch-tool.md` — parse-verify-apply hunks, zero-hunk failure, trailing-newline guarantee.
- `./task-tool.md` — subagent delegation, foreground/background modes, non-overlap guidance.
- `./shell-tool.md` — bounded cross-platform shell, default timeout, safe env expansion.
- `./truncate-tool.md` — line+byte-bounded output, spill-to-file with outputPath.
- `./skill-tool.md` — name-based skill loading.
- `./question-tool.md` — mid-turn user questions.
- `./tool-schema.md` — Effect Schema → JSON Schema for tool params.
- `./lsp-tool.md` — LSP diagnostics feedback loop.
- `./web-tools.md` — webfetch + websearch.
- `./plugin-loader-pipeline.md` — staged resolve/load pipeline, Bun failed-import permanence, file-plugin retry ladder.
- `./plugin-entrypoint-resolution.md` — exports→main→index entry ladder with containment jail and npm/file asymmetries.
- `./config-plugin-origins.md` — provenance-preserving plugin origin merge across config layers; later-wins dedupe.
- `./plugin-hook-runtime.md` — parallel load / sequential hook registration, per-phase error isolation, dispose finalizers.
- `./plugin-meta-fingerprint.md` — cross-process plugin-meta store classifying loads first/updated/same via fingerprints.
- `./plugin-config-patching.md` — lock-per-file JSONC surgical patching with add/replace/noop modes and descending-index deletion.
- `./session-fork.md` — chronological-prefix fork with old→new ID remapping and fail-closed unknown cutoff.
- `./session-patch-usage.md` — single patch funnel with tri-state clear semantics + cross-provider usage→cost math.
- `./prompt-loop-exit-machine.md` — four-clause loop-exit gate; stop-finish with live tool calls keeps running; interrupted orphans never prefill.
- `./prompt-subtask-dispatch.md` — subtask parts execute as real Task tool calls; error-as-data part states preserve child metadata.
- `./prompt-structured-output-capture.md` — StructuredOutput tool injection, toolChoice:"required", closure capture, missing-call error.
- `./prompt-content-filter-surfacing.md` — content-filter finishes become persisted+published message errors while keeping partial output.
- `./command-template-engine.md` — slash-command $N/$ARGUMENTS/!`cmd`/@file expansion order and last-placeholder-variadic rule.
- `./prompt-title-guardrails.md` — at-most-once auto-titling: parent/default-title/one-real-message gates + think-strip + 100-char clamp.
- `./file-part-resolution-ladder.md` — @file/data:/MCP-resource/agent parts fan out to synthetic transcript pieces; failures are narratives, not errors.
- `./session-run-state-kernel.md` — runner-per-session mutual exclusion, queued callers, latch-gated shells, transitive background-job cancel waves.
- `./prompt-shell-as-user-message.md` — user-run shell recorded as model-shaped assistant tool call with streamed output metadata + abort narrative.
- `./plan-mode-reminder-injection.md` — plan/build mode-switch reminders as synthetic last-user parts; legacy vs experimental plan-file regimes.
- `./instruction-discovery-claims.md` — AGENTS.md/CLAUDE.md first-match ladders, once-per-message claims ledger, history-derived dedup.
- `./system-prompt-family-selection.md` — pure substring model→base-prompt routing and canonical system block order (env→instructions→mcp→skills).
- `./prompt-tools-permission-projection.md` — per-prompt tools map REPLACES session permission rules wholesale (not a merge).
- `./session-model-persistence-ladder.md` — sticky session model via DB row → user-message scan → provider default; diff-gated writes; conditional variant inheritance.
- `./session-context-budget-ladder.md` — usable-context arithmetic (input-cap vs context−maxOutput, 20k buffer) + recency-first budgeted tail-turn selection with mid-turn split.
- `./compaction-replay-choreography.md` — compaction as a special assistant turn over the normal processor; prior-summary pair-hiding; overflow replay with media→text substitution and unstable continue marker.
- `./tool-output-prune-ladder.md` — marking-not-deleting prune of old tool outputs; dual thresholds (40k protect / 20k minimum), protected tools, already-pruned frontier stop.
- `./provider-error-retry-policy.md` — retryable classification precedence (overflow never retries; 5xx forces retryable; Go-limit upsell actions) + dual-cap backoff honoring retry-after headers.
- `./llm-runtime-seam.md` — native-vs-AI-SDK runtime selection with logged fallback reason; broken-tool-call case-fix→invalid-sink repair; workflow toolExecutor value-not-throw bridge.
- `./summary-git-path-unquote.md` — step-start/step-finish snapshot diff pairing; byte-buffer decoding of git quoted/octal paths.
- `./session-status-ledger.md` — ephemeral per-instance busy/idle map; publish-before-mutate; idle evicts instead of storing.
- `./session-processor-event-machine.md` — LLM stream events → session parts state machine; pre-stream snapshot capture; compact/stop/continue turn result; ensuring-cleanup.
- `./doom-loop-guard.md` — 3-repeat trailing-window canonical-JSON detector raises a `doom_loop` permission ASK with always-whitelist, never a hard fail.
- `./tool-call-cleanup-ladder.md` — abort cleanup: 250ms grace-await on tool Deferreds then force-close as error with `interrupted:true`; runs under ensuring on every exit path.
- `./network-error-finish-conversion.md` — finish-step `rawFinishReason:"network_error"` fails the event into the retry policy; unknown error bodies fall through as retryable api_error; loop-exit treats finish `"unknown"` as non-terminal.
- `./ws-session-pool-fallback.md` — per-session WebSocket pool for POST /responses streaming: busy→HTTP, sticky fallback latch, shared 5-failure budget, 1009 instant fallback, age/idle rotation.
- `./ws-responses-stream-adapter.md` — Responses-over-WebSocket protocol: one response.create frame in, SSE frames out; terminal-frame grammar; completed one-shot latch; idle-timeout watchdog.
- `./subagent-failure-propagation.md` — Task tool post-await gate: child message-error or last errored tool part fails the parent call with a `task_id` envelope instead of returning partial text.
- `./cloudflare-gateway-routing.md` — gateway models route by prefix to native passthrough SDKs (Responses/Messages APIs); only Workers AI receives the Cloudflare token upstream; npm override fires before variant computation.
- `./httpapi-route-assembly.md` — protocol-owned groups + legacy groups composed into one Effect HttpApi tree; middleware placement vs injected service keys; lazy /doc.
- `./sse-event-stream-contract.md` — eager-subscribe SSE kernel: no lost events during body startup, directory filter, disposed-frame termination, 10s heartbeat, proxy-defeating headers.
- `./sync-fence-cursor.md` — x-opencode-sync response-header cursor: pre/post event-sequence diff on mutations, strict parse, blocking wait for replica catch-up.
- `./http-auth-ladder.md` — loopback auth: auth_token query beats Basic header (EventSource), config-absence disables auth, three tiers, why HttpApiSecurity alternatives are avoided.
- `./workspace-routing-proxy-fence.md` — tagged request plan (Invalid/Missing/Local/Remote), directory ladder, session-precedence, remote proxy with fence-blocked read-your-writes.
- `./openapi-public-transform-codegen.md` — PublicApi spec transform repairing Effect OpenAPI bugs + freezing legacy SDK shape; hey-api codegen with fail-loud post-patches.
- `./deferred-disposal-ws-shutdown.md` — WeakMap request-keyed post-response disposal handshake; WS tracker closing latch with per-socket 1s timeout shutdown.
- `./httpapi-error-body-shaping.md` — defect-only error boundary with err_ refs, 1KB schema-reason cap (DoS/secret-echo guard), dual-shape error bodies by API generation, CORS Vary merge fix.
- `./session-http-handler-plane.md` — 28-endpoint session adapter: storage-NotFound→404 / busy→409 mapping ladder, busy-gated mutation set, promptAsync failure published as session error event, share 500-vs-400 split, part path/body ID agreement.
- `./session-message-cursor-pagination.md` — backward message paging: base64url {id,time} cursor, limit+1 more-probe, DESC-fetch/ASC-response, tie-safe older predicate, proxy-aware Link rel=next + X-Next-Cursor CORS exposure.
- `./pty-ticket-ws-connect.md` — browser terminal WS auth: CSRF-header-gated single-use TTL-scoped tickets (atomic invalidateWhen), 404-before-decode upgrade ordering, one-outbox replay-before-activate ordering, legacy vs canonical exited-session visibility.
- `./workspace-proxy-pump.md` — remote-workspace proxy internals: hop+context header deny list with post-sanitize auth override, reason-code-preserving WS close propagation (1011 bucket), binary-frame copy, buffered upstream 5xx logged locally and forwarded byte-identical.
- `./session-v2-durable-prompt-recording.md` — v2 prompt admission ledger: find-first-by-client-ID idempotent admit, equivalence-gated 409 on same-ID different body, projector lifecycle conflicts as defects, opt-out wake, typed 503 stubs for unfinished mutations.
- `./session-revert-stage-machine.md` — revert as three-phase machine across two generations: v2 event-publisher stage/clear/commit with first-wins per-file snapshot map + projector-side deletion; v1 imperative lastUser-boundary snap, patch collection, materializing cleanup.
- `./storage-key-value-lock-migration.md` — JSON key/value store: per-target reentrant locks (readers parallel, writers exclusive per key), dual-shape ENOENT → typed NotFoundError, marker-file migration ladder that never advances past a failed step.
- `./public-ui-bypass-grammar.md` — PWA asset auth bypass: GET-only exact-path Set at raw router level only (never typed-API auth), regression #25698 pinned.
- `./session-v2-anchor-cursor-pagination.md` — bidirectional opaque cursors: base64url JSON embedding full query context + anchor, cursor-beats-query precedence, cursor+order conflict 400, flip-fetch-then-reverse with (time,id) tie-safe predicate, excess-property-tolerant decode for evolution.
- `./mcp-client-lifecycle.md` — MCP fleet connect plane: StreamableHTTP→SSE transport ladder with auth-error classification (needs_auth vs needs_client_registration break-out), acquireUseRelease ownership transfer, identity-guarded liveness watch, pgrep-BFS descendant kill before child close, tolerant outputSchema fallback.
- `./mcp-oauth-callback-server.md` — headless OAuth authorization-code flow: memory-pending token commit-on-success (single write point), URL-scoped credential storage with expired-secret re-registration, state-keyed loopback callback server with reverse index, CSRF state double-check, idle-shutdown listener, silent-defer port probe.
- `./session-run-coordinator.md` — per-key serialized execution kernel: entry/successor algebra (done Deferred + pendingWake + stopping latch), join-semantics run, coalesced wake, yieldNow trampoline for synchronous self-wake chains, wake-during-interruption-cleanup survival, infallible wake via logged drain isolation.
- `./session-runner-drain-loop.md` — durable input-ledger drain: steer-all-below-cutoff vs queue-one-FIFO two-loop promotion, pre-run durable repair of interrupted tool parts, protocol-level max-steps tool freeze (toolChoice none), defect-as-control-flow compaction transitions with single-shot overflow recovery, eager parallel tool settlement under ordered publication.
- `./session-projector-convergence.md` — event-stream→tables convergence: duplicate-creator die (onConflictDoNothing + returning-row check), ±sign usage ledger arithmetic across sessions, revert commit truncating messages AND input ledger at one boundary seq, newest-incomplete-assistant supersession.
- `./lsp-client-fleet.md` — per-language server fleet: config ladder (undefined/true/object), NearestRoot vs StrictNearestRoot marker walks, spawn→Handle|undefined availability contract, broken-set no-retry, in-flight spawn dedupe, reuse-stops-duplicate, scope-finalizer teardown.
- `./lsp-diagnostic-subscription.md` — heterogeneous diagnostic wait: push/pull dual cache union-with-dedupe, parallel identifier pulls with early-unblock-on-current-file (PR #23771), version-aware debounced push freshness, registration-change-only loop continue, typescript first-push seed, no-wipe didChange.
- `./session-context-epoch.md` — durable system-context epoch: insert-once initialize that writes no row when blocked, reconcile-vs-replace on compaction pass, ContextUpdated-as-system-message with snapshot-only advance, decode-failure-as-typed-turn-error.
- `./session-history-baseline-gating.md` — provider-facing history partitioning in one SQL predicate: below-baseline system consumed, above-baseline system always kept across the compaction window, explicit-baseline-at-call-site for the runner vs stored-row-derived for readers.
- `./llm-event-publisher.md` — per-turn LLMEvent→SessionEvent closure: fragment start/append/end/flush with die-on-wire-violation, self-healing tool-call, media-once raw-result rule, runner-owned step settlement (no Step.Ended published), idempotent failAssistant + hostedOnly failUnsettledTools.
- `./system-context-compare-ladder.md` — per-source reconcile ladder over durable {value, removed?} snapshots: Incompatible→Replace escalation, unavailable-keeps-admitted, removal-text-or-replace for vanished sources, empty-rendering defect.
- `./system-context-registry-builtins.md` — scope-lifetime context registry (acquireRelease cleanup, per-load producer re-evaluation, two duplicate-key failure points) + builtin environment/date sources with calendar-day granularity via toDateString.
- `./tool-output-store.md` — settlement-side bound(): text-only 2k-line/50KB gate, wx-flag managed spill with complete-retention-before-preview, marker-budgeted head/tail preview, media passthrough unlimited, StorageError fails settlement, 7-day hourly global retention.
- `./lsp-language-spawn-ladders.md` — three availability patterns under Handle|undefined: RustAnalyzer workspace-root walk, Clangd discovery-before-download ladder with symlink swap, Roslyn single-flight install + Razor extension artifacts; every failure yields undefined.
- `./instruction-context-ambient-source.md` — ambient AGENTS.md instruction discovery as one reconcilable source: boundary-gated upward walk, global-file dedupe, fail-open-to-Unchanged availability on filesystem races.
- `./session-model-resolution.md` — four-stage v2 model resolution: selection ladder, strict/lenient variant overlay, credential precedence with apiKey strip + metadata projection, three-route mapping with fail-loud UnsupportedApiError.
- `./catalog-availability-projection.md` — derived-at-read model availability from credentials/integrations/policy, provider-under-model api projection, newest-enabled default fallback, policy-denial-as-removal.
- `./credential-replace-on-create.md` — one-credential-per-integration via transactional delete-then-insert, decode-tolerant reads that drop corrupt rows, global (not location-scoped) node.
- `./integration-oauth-attempt-machine.md` — OAuth attempt state machine: pending/terminal entries in one SynchronizedRef, completing single-flight latch set inside modify, 10-min lifetime + 30s scrub + 1-min terminal retention, per-attempt forked scopes closed on every exit path.
- `./models-dev-catalog-cache.md` — remote catalog cache: disk → bundled snapshot → flock-locked atomic fetch populate ladder, infinite-TTL memo invalidated by a never-fail double-checked refresh, 60-min spaced repeat.
- `./model-provider-schema-shapes.md` — ModelV2.Info/ProviderV2.Info row shapes with aisdk|native tagged api union, empty-row factories for partial registration, id/providerID re-pinning, slash-tolerant parse().
- `./catalog-plugin-composition.md` — boot-ordered State.batch plugin composition: models.dev dump → sibling-model modes (never variants) with tier-keyed cost merge → per-vendor patches → GLM reasoning_effort variant generator with explicit-wins.
- `./config-registration-pipeline.md` — config documents + markdown globs → agent/command/skill/provider rows: draft-update ordering beats built-ins, fail-soft decode with silent skip, V1 migration on unknown frontmatter keys, disabled-removes, global-before-agent permission push, path-action-only home expansion.
- `./external-plugin-fork-loader.md` — third-party plugin loading: background forkScoped so boot never waits, per-plugin ignoreCause isolation, npm.add entrypoint resolution, structural two-shape module validation, scope-attached promise bridge.
- `./vendor-sdk-patch-hooks.md` — three-hook vendor shape (catalog rewrite → SDK construction → language selection) over a mutable host adapter with output copy-back, exact-package guards, dynamic SDK import.
- `./vendor-auth-quirk-patches.md` — uniform integration-method registry over vendor-owned flows: PKCE loopback + device polling with pending-as-data unions, ADC fetch-wrap boundary, bearer/env-first AWS chain, metadata as the credential extension point surviving refresh.
- `./state-replayable-transform-kernel.md` — one 128-line kernel under five domain stores: fresh-rebuild replay of ordered transforms, finalize-before-commit atomicity, scope-attached dispose, semaphore serialization.
- `./domain-draft-editors.md` — the create-on-first-touch / re-pin-key-after-fn / remove draft contract instantiated five ways (AgentV2, CommandV2, SkillV2 sources-not-rows, Catalog nested provider/model, Integration method upsert-by-id).
- `./state-batch-deferred-rebuilds.md` — ambient CurrentBatch Set<Reload>: N transforms across M stores cost one rebuild per store, drained after the batch effect.
- `./integration-connection-projection.md` — stateless connection projection: registry stores no connections; newest-credential-first + live env detection per read, lazy 5-minute-threshold OAuth refresh.
- `./v1-provider-options-lowerers.md` — prototype-safe package-keyed lowerer table turning legacy V1 provider options into url/headers/body/settings and wire-case request bodies (reasoning fold, output_config, generationConfig, additionalModelRequestFields).
- `./skill-discovery-url-pull.md` — remote skill catalog pull: whole-skill fail-closed validation (safe name, entrypoint required, per-file path grammar, same-origin URLs, contains() containment), versioned staging swap with uninterruptible rename ladder.
- `./permission-v2-ask-machine.md` — v2 ask/assert/reply lifecycle: configured-deny short-circuit before saved layer, decline-as-defect, session-wide reject cascade, always-reply re-evaluation sweep.
- `./permission-saved-approval-store.md` — "always allow" as project-scoped row-per-resource allow rules re-entering the same rule engine, never a bypass channel.
- `./layer-node-tag-hoist-compile.md` — the DI substrate under every v2 service: typed tag factories (location may depend on global, never reverse), hoist-the-tag/compile-once/provide-per-tenant, name+tag-checked replacements with dependency rewriting.
- `./keyed-mutex-refcount.md` — per-key semaphore registry with holders+waiters refcount; entries deleted only at zero; interrupted waiters never drop the holder's lock.
- `./effect-runtime-escape-hatches.md` — calling Layer-built services from non-Effect code: lazy ManagedRuntime over a process-wide memoMap + typed Proxy accessors with per-key caching.
- `./fsutil-safe-fs-service.md` — the shared filesystem service: absence/permission as values, re-check-before-swallow EEXIST, mkdir-on-NotFound retry, collect-everything walk ladder with inclusive stop.
- `./sqlite-database-node.md` — one global SQLite node: env→channel-suffixed path ladder, PRAGMA+migration init under orDie, dual-driver condition map, scope-finalizer transaction lock.
- `./sqlite-migration-journal.md` — sentinel-table migration ladder: foreign-DB die, one-shot legacy drizzle-journal import, per-migration transactional application.
- `./db-path-column-codec.md` — Drizzle custom path columns: platform-gated normalize-on-write/restore-on-read with driver-boundary absolute validation; POSIX backslashes survive byte-for-byte.
- `./skill-v2-url-source-wiring.md` — SkillV2 sources-not-rows meets discovery: url→pull→glob→frontmatter decode, global cache node beneath per-location stores.

## Capsule map
- **Shadow-git undo** — `./snapshot.md`: per-worktree hidden repo, SHARED object DB via alternates, ignore-drift correction.
- **Permission model** — `./permissions.md`: last-match-wins rulesets, defer-on-ask, Deferred suspension.
- **Sessions & editing** — `./sessions.md`, `./editing.md`: event-sourced persistence, fork-as-rewrite, replacer chain, locked edits.
- **The write path (tools)** — `./write-tool.md`, `read-tool.md`, `apply-patch-tool.md`, `grep-glob-tools.md`: permission-gated write, paginated read, patch apply, ripgrep search.
- **Execution & delegation** — `./shell-tool.md`, `task-tool.md`, `truncate-tool.md`: bounded shell, subagent delegation, spill-to-file truncation.
- **Model-facing helpers** — `./skill-tool.md`, `question-tool.md`, `tool-schema.md`, `lsp-tool.md`, `web-tools.md`: skill loading, user questions, schema conversion, LSP feedback, web access.
- **Plugin system** — `./plugin-loader-pipeline.md`, `plugin-entrypoint-resolution.md`, `config-plugin-origins.md`, `plugin-hook-runtime.md`, `plugin-meta-fingerprint.md`, `plugin-config-patching.md`: staged loading, entry jail, origin provenance, hook isolation, change fingerprinting, config patching.
- **Session lifecycle** — `./session-fork.md`, `session-patch-usage.md`: fork-as-ID-rewrite over the chronological prefix; patch funnel + usage/cost accounting.
- **Prompt turn engine** — `./prompt-loop-exit-machine.md`, `prompt-subtask-dispatch.md`, `prompt-structured-output-capture.md`, `prompt-content-filter-surfacing.md`, `command-template-engine.md`, `prompt-title-guardrails.md`: loop-exit predicate with orphan carve-outs, subtask-as-Task-call dispatch, forced structured capture, finish-as-error surfacing, command template grammar, one-shot titling.
- **Prompt input plane** — `./file-part-resolution-ladder.md`, `plan-mode-reminder-injection.md`, `instruction-discovery-claims.md`, `system-prompt-family-selection.md`, `prompt-tools-permission-projection.md`, `session-model-persistence-ladder.md`: fail-open file fan-out, synthetic mode reminders, claims-ledger instruction dedup, substring prompt routing, replace-semantics permission projection, sticky model ladder.
- **Run-state & shell** — `./session-run-state-kernel.md`, `prompt-shell-as-user-message.md`: runner-per-session exclusion + queued callers; user-shell as model-shaped transcript records.
- **Turn processor (pass 5)** — `./session-processor-event-machine.md`, `doom-loop-guard.md`, `tool-call-cleanup-ladder.md`: LLMEvent→part state machine with compact/stop/continue result; trailing-window repeat-call permission ask; settle-then-force-close abort ladder.
- **Network-error conversion (pass 5)** — `./network-error-finish-conversion.md`: untrustworthy finish reasons become retryable failures before persistence; unknown error bodies fall through as retryable api_error.
- **Responses-over-WebSocket plane (pass 5)** — `./ws-session-pool-fallback.md`, `ws-responses-stream-adapter.md`: per-conversation socket pool with sticky HTTP fallback + shared retry budget; WS JSON→SSE bridge with single-terminal grammar.
- **Subagent & gateway planes (pass 5)** — `./subagent-failure-propagation.md`, `cloudflare-gateway-routing.md`: post-await child-failure gate with task_id envelopes; prefix-routed passthrough SDKs and the Workers-AI-only upstream-key rule.

- **Session context & compaction (pass 4)** — `./session-context-budget-ladder.md`, `./compaction-replay-choreography.md`, `./tool-output-prune-ladder.md`: usable-context arithmetic + budgeted tail selection; summary-pair hiding + overflow replay; marking prune with dual thresholds.
- **Provider failure & runtime planes (pass 4)** — `./provider-error-retry-policy.md`, `./llm-runtime-seam.md`: retryable ladder + header-aware backoff; dual-runtime stream seam + tool-call repair + workflow bridge.
- **Turn bookkeeping (pass 4)** — `./summary-git-path-unquote.md`, `./session-status-ledger.md`: snapshot-pair diffs + git path unquoting; ephemeral status ledger.
- **HTTP API server plane (pass 6)** — `./httpapi-route-assembly.md`, `sse-event-stream-contract.md`, `sync-fence-cursor.md`, `http-auth-ladder.md`, `workspace-routing-proxy-fence.md`, `openapi-public-transform-codegen.md`, `deferred-disposal-ws-shutdown.md`, `httpapi-error-body-shaping.md`: typed route composition (protocol-owned groups, per-tier middleware), SSE wire contract with eager subscription + disposed termination, mutation cursor fences over x-opencode-sync, query-first loopback auth ladder, workspace request-plan routing with fence-blocked remote proxying, legacy-freezing OpenAPI transform + fail-loud SDK codegen patches, post-response disposal handshake + WS shutdown registry, bounded dual-shape error bodies.
- **Session & PTY handler planes (pass 7)** — `./session-http-handler-plane.md`, `./session-message-cursor-pagination.md`, `./pty-ticket-ws-connect.md`, `./workspace-proxy-pump.md`: typed session adapter with engine-error mapping ladder + async-failure event publication, opaque-cursor backward message paging with proxy-aware Link headers, single-use scoped tickets for credential-free browser WS upgrades + ordered replay outbox, and the transparent HTTP/WS workspace proxy pump.
- **v2 session engine + storage substrate (pass 8)** — `./session-v2-durable-prompt-recording.md`, `./session-revert-stage-machine.md`, `./storage-key-value-lock-migration.md`, `./public-ui-bypass-grammar.md`, `./session-v2-anchor-cursor-pagination.md`: idempotent-by-client-ID prompt admission with equivalence-gated 409 + honest 503 stubs, three-phase revert (stage/cancel/commit) as event publishers vs imperative lastUser-boundary snap, per-target-locked JSON key/value store with fail-without-advance migration marker, GET-only exact-path PWA auth bypass at router level, and context-embedding bidirectional anchor cursors with flip-fetch-then-reverse.
- **MCP client plane + v2 wake→drain consumer side (pass 9)** — `./mcp-client-lifecycle.md`, `./mcp-oauth-callback-server.md`, `./session-run-coordinator.md`, `./session-runner-drain-loop.md`, `./session-projector-convergence.md`: auth-classified transport ladder with ownership-transfer connect + process-tree teardown, commit-on-success OAuth with URL-scoped credentials and state-keyed loopback callback, per-key serialized execution with coalesced wakes and trampoline successors, two-loop steer/queue drain with protocol-level step freeze and single-shot compaction recovery, and event convergence with duplicate-creator die + boundary-seq dual-table revert truncation.
- **LSP manager plane + v2 context epoch/history + turn publisher (pass 10)** — `./lsp-client-fleet.md`, `./lsp-diagnostic-subscription.md`, `./session-context-epoch.md`, `./session-history-baseline-gating.md`, `./llm-event-publisher.md`: per-language server fleet with broken-set no-retry + in-flight spawn dedupe + scope-finalizer teardown, heterogeneous diagnostic wait with parallel identifier pulls and early-unblock-on-current-file, durable system-context epoch with blocked-initialize-no-row + ContextUpdated-as-system-message, one-predicate history partitioning (consumed system / current system updates / compaction window), and the per-turn LLMEvent→SessionEvent closure with die-on-wire-violation + media-once settlement.

- **System-context engine + output retention + language spawn ladders (pass 11)** — `./system-context-compare-ladder.md`, `system-context-registry-builtins.md`, `tool-output-store.md`, `lsp-language-spawn-ladders.md`: per-source reconcile ladder with Incompatible→Replace escalation and unavailable-keeps-admitted semantics, scope-lifetime registry with two duplicate-key failure points and per-load re-evaluation, settlement-side output bounding with wx-flag spill + fail-the-settlement retention, and honest per-language spawn ladders (bounded root walk, discovery-before-download, single-flight install).
- **Ambient instructions + v2 model/catalog/credential planes (pass 13)** — `./instruction-context-ambient-source.md`, `./session-model-resolution.md`, `./catalog-availability-projection.md`, `./credential-replace-on-create.md`: whole-set ambient instruction source with fail-open-to-Unchanged availability and boundary-gated discovery, four-stage model resolution with credential precedence + apiKey strip + fail-loud route mapping, derived-at-read catalog availability with provider-under-model projection + recency default fallback + policy-denial-as-removal, and transactional replace-on-create credentials with decode-tolerant reads.
- **Integration plane + models.dev substrate + catalog schema + plugin composition (pass 14)** — `./integration-oauth-attempt-machine.md`, `./models-dev-catalog-cache.md`, `./model-provider-schema-shapes.md`, `./catalog-plugin-composition.md`: single-map OAuth attempt machine with completing single-flight latch + expiry scrub + per-attempt scopes, disk→snapshot→flock-locked-atomic-fetch cache ladder with never-fail double-checked refresh, ModelV2/ProviderV2 row shapes with empty factories + slash-tolerant parse, and boot-ordered plugin composition where modes become sibling models and explicit variants beat generated ones.
- **Config registration + external plugin loading + vendor patches (pass 15)** — `./config-registration-pipeline.md`, `./external-plugin-fork-loader.md`, `./vendor-sdk-patch-hooks.md`, `./vendor-auth-quirk-patches.md`: config documents + markdown globs become live rows through draft-update ordering with fail-soft decode and disabled-removes, external plugins load in a background fork with per-plugin ignoreCause isolation and a scope-attached promise bridge, and vendor plugins follow a three-hook shape (catalog rewrite → SDK construction → language selection) with per-vendor auth quirks (PKCE loopback, pending-as-data device polling, ADC fetch wrapping, cross-region prefix matrix) behind one uniform method registry.
- **State kernel + draft editors + connection projection + V1 lowerers (pass 16)** — `./state-replayable-transform-kernel.md`, `./domain-draft-editors.md`, `./state-batch-deferred-rebuilds.md`, `./integration-connection-projection.md`, `./v1-provider-options-lowerers.md`.
- **Skill discovery + v2 permissions + layer wiring (pass 17)** — `./skill-discovery-url-pull.md`, `./skill-v2-url-source-wiring.md`, `./permission-v2-ask-machine.md`, `./permission-saved-approval-store.md`, `./layer-node-tag-hoist-compile.md`, `./keyed-mutex-refcount.md`: whole-skill fail-closed catalog ingestion with versioned staging swaps, the v2 ask/assert/reply machine with saved-approval layering and session-wide reject cascade, project-scoped allow-rule persistence, the tag/hoist/compile substrate behind per-directory service isolation, and the refcounted keyed-mutex primitive.
- **Effect escape hatches + FSUtil + SQLite substrate (pass 18)** — `./effect-runtime-escape-hatches.md`, `./fsutil-safe-fs-service.md`, `./sqlite-database-node.md`, `./sqlite-migration-journal.md`, `./db-path-column-codec.md`: lazy ManagedRuntime over a process-wide memoMap + typed Proxy accessors, absence-as-value filesystem service with re-checked EEXIST and collect-everything walks, one global SQLite node with channel path ladder + orDie PRAGMA/migration init, sentinel-table migration journal with one-shot drizzle import and foreign-DB die, and platform-gated path column codecs with driver-boundary validation.

## Extending the foundation
Add one references-fileshaped capsule per portable seam: one loader line, one grouped map entry, decisive source with an invariant, direct-test probe, and `search_graph` retrieval.

## Provenance
Indexed in Codebase Memory as `opencode` (`/mnt/hdd/utopia/inspo/opencode`, live symlink into `coding-agents/opencode`); 64,966 nodes / 235,651 edges @ `dev@0352100`. Pass 2 re-entry @ `dev@4643e65a`. Pass 3 (deep lane) mined the session prompt plane whole-file at the same pin: 14 new capsule-v2 across prompt.ts (1,631L), run-state.ts, reminders.ts, instruction.ts, system.ts — probes pinned to prompt.test.ts (30+ `it.instance` cases), instruction.test.ts, structured-output.test.ts; check_index_coverage no_recorded_issue on all six cited paths. Pass 4 (deep-rover re-entry, same pin, zero drift): executed pass-3's queued session-context conditionals — compaction.ts (608L whole), overflow.ts, retry.ts (208L), llm.ts (404L), status.ts, summary.ts, message-error.ts read top-to-bottom → 7 capsule-v2 above; probes pinned to compaction.test.ts (:384/:408/:420/:626/:814), retry.test.ts (:260/:269/:282/:294 + delay matrix :35-150), llm.test.ts (:1347/:1514). Pass 5 (drift re-entry @ `dev@0352100`, +116 upstream commits from 4643e65, re-indexed IN PLACE via live-symlink root — no twin): executed queued target #1 (session/processor.ts ALL 718L whole-file) + diff-first triage of the wave → 8 new capsule-v2 (processor machine ×3, network-error conversion trio, WS pool+adapter pair) + drift notes on provider-error-retry-policy / prompt-loop-exit-machine / session-patch-usage; probes pinned to openai-ws.test.ts (32 tests) and test/provider/error.test.ts; graph refreshed 64,850n→64,966n head==base==0352100. Pass 6 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified live head==base; first pass with a durable work record at inspo/opencode-work/, stale ledger row reconciled): mined the previously-uncited HTTP API server plane — server/routes/instance/httpapi/{api,server,public,lifecycle,websocket-tracker}.ts + middleware/{fence,authorization,workspace-routing,instance-context,error,schema-error,cors-vary}.ts + handlers/groups/event.ts + shared/fence.ts + bus/global.ts + protocol/src/api.ts + protocol/src/groups/event.ts + sdk/js/script/build.ts read whole → 8 new capsule-v2 above; direct tests read whole: httpapi-{event,public-openapi,authorization,workspace-routing,cors-vary,schema-error-body}.test.ts; check_index_coverage no_recorded_issue on all 15 cited source/test paths; 15 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (zero node_modules in inspo checkout; install outside lane file boundary) → deterministic evidence per Gate-5 fallback; license corrected to MIT at this pin (pass-5 ws-pool capsule's "Slate-licensed" note is stale). Pass 7 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected this session → direct source+test reading fallback per AGENTS.md): mined the two biggest handler planes + proxy pump — routes/instance/httpapi/{groups/handlers}/session.ts + session-errors.ts + session/message-v2.ts page/cursor + shared/pty-ticket.ts + core/pty/ticket.ts + handlers/pty.ts + groups/pty.ts + server/proxy-util.ts + middleware/proxy.ts read whole → 4 new capsule-v2 above; direct tests read whole: httpapi-session.test.ts (1090L), httpapi-pty.test.ts (299L), httpapi-v2-pty.test.ts (250L), proxy-util.test.ts (113L); drift check vs pass-3/4/5 session capsules found no drift (handler plane is a thin adapter above the already-cited engine services). Pass 8 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected this session → direct source+test reading fallback per AGENTS.md): descended one layer below the handler planes into the v2 engine + storage substrate — core/session.ts (486L whole) + core/session/input.ts (290L whole) + core/session/revert.ts (121L whole) + opencode/src/session/revert.ts (136L whole) + opencode/src/storage/storage.ts (327L whole) + server/src/handlers/{session,message}.ts + protocol/groups/session.ts (379L whole) + protocol/errors.ts (111L whole) + shared/public-ui.ts (12L whole) read whole → 5 new capsule-v2 above; direct tests read: httpapi-session.test.ts v2 sections (:431-520 cursor, :564-664 prompt recording/unavailable), storage.test.ts (whole), session-projector.test.ts (:99-130 revert events), revert-compact.test.ts (:111-271 v1 three-phase), httpapi-ui.test.ts (:396-456 auth bypass); 14 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (zero node_modules in inspo checkout; install outside lane file boundary) → deterministic evidence per Gate-5 fallback. Pass 9 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected this session → direct source+test reading fallback per AGENTS.md): mined the previously-uncited MCP client plane + the v2 wake→drain consumer side — mcp/index.ts (1004L whole) + mcp/catalog.ts (170L) + mcp/auth.ts (163L) + mcp/oauth-provider.ts (259L) + mcp/oauth-callback.ts (194L) + handlers/groups/mcp.ts read whole, and core/session/execution.ts (34L) + execution/local.ts (46L) + run-coordinator.ts (104L) + runner/llm.ts (439L) + projector.ts (455L) read whole → 5 new capsule-v2 above; direct tests read: httpapi-mcp.test.ts (223L whole), httpapi-mcp-oauth.test.ts (73L whole), session-run-coordinator.test.ts (418L whole), session-runner.test.ts (promotion/overflow/interrupt sections :614-658, :1212-1330, :1687-1748, :1918-2230), session-projector.test.ts (:81-131, :202-245, :373-500); 29 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 10 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected this session → direct source+test reading fallback per AGENTS.md): descended into the three seams the pass-9 drain loop cited as interfaces — lsp/lsp.ts (507L whole) + lsp/server.ts registry sections (:16-86 NearestRoot/StrictNearestRoot/Info, :115-143 Typescript, :1147-1272 JDTLS) + lsp/launch.ts (21L whole) + lsp/client.ts (650L whole), core/session/context-epoch.ts (174L whole) + history.ts (101L whole) + store.ts (63L whole) + runner/llm.ts :173-224 re-read, and runner/publish-llm-event.ts (423L whole) read whole → 5 new capsule-v2 above; direct tests read: lsp/index.test.ts (231L whole), lsp/lifecycle.test.ts (160L whole), lsp/jdtls-root.test.ts (459L, 24 root-ladder tests), lsp/client.test.ts (488L whole), core/test/session-runner-tool-events.test.ts (136L whole), core/test/session-history.test.ts (165L whole), session-runner.test.ts epoch/history/publisher sections (:666-758, :1039-1075, :1337-1370, :1372+); 22 byte-exact grep probes GREEN pre-write (two probe counts self-corrected against live grep before wiring); bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 11 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): mined the system-context engine + output retention + language spawn ladders — core/system-context/{index.ts (320L), registry.ts (49L), builtins.ts (50L)} + core/tool-output-store.ts (211L) + core/config/tool-output.ts (9L) + tool/registry.ts settleWith bound() call site + lsp/server.ts ladder sections (RustAnalyzer :890-934, Clangd :935-1068, Razor/Roslyn :702-816) read whole or by exact section → 4 new capsule-v2 above; direct tests read whole: system-context/{index (307L, 18 cases), registry (114L, 7 cases), builtins (129L, TestClock-pinned)}.test.ts + tool-output-store.test.ts (247L, 10 it.live cases); coverage caveat: NO direct test pins the three language spawn ladders (0 references in packages/opencode/test/lsp/) — source-confirmed only. Pass 12 (revert recovery, same pin re-verified): an external git operation on the skills catalog checkout reverted SKILL.md to the pass-10 shape AND deleted the 4 pass-11 capsule files (never committed; unrecoverable from git) — all 4 capsules re-authored from a fresh full source+test re-read at the same pin (re-adjudication, not a copy), loader/map/provenance re-wired, Pass 13 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): closed the two interface-only citations from passes 9-12 plus their data-source seams — core/instruction-context.ts (101L whole) + session/runner/model.ts (218L whole) + catalog.ts (301L whole) + credential.ts (138L whole) read whole, runner/llm.ts :94-230 re-read for the resolve/promptCacheKey call sites → 4 new capsule-v2 above; direct tests read whole: instruction-context.test.ts (323L, 2 it.live + 5 it.effect), session-runner-model.test.ts (347L, 13 it.effect), catalog.test.ts (353L, 13 it.effect), credential.test.ts (36L, 1 it.effect); 20 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 14 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): closed the two remaining interface-only citations from pass 13 plus their data-source and composition seams — core/integration.ts (520L whole) + core/models-dev.ts (266L whole) + schema/model.ts (106L) + schema/provider.ts (72L) + core/model.ts (41L) + core/provider.ts (25L) + plugin/internal.ts (153L) + plugin/models-dev.ts (183L) + plugin/variant.ts (39L) + plugin/provider.ts (71L) + plugin/provider/anthropic.ts (27L) read whole → 4 new capsule-v2 above; direct tests read whole: integration.test.ts (349L, 9 it.effect), models.test.ts (290L, 9 it.live), plugin/models-dev.test.ts (170L, 2 it.effect), plugin/variant.test.ts (67L, 2 it.effect), model.test.ts (23L, 2 test); 24 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 15 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): closed the config→catalog registration plane and the four largest vendor patches — plugin/{agent,command,skill}.ts (202L/25L/31L whole) + config/plugin/{agent,command,skill,external,provider,reference}.ts (179L/83L/50L/91L/113L/70L whole) + plugin/host.ts (219L whole) + plugin/promise.ts (93L whole) + plugin/provider/{openai,opencode,google-vertex,amazon-bedrock}.ts (292L/320L/173L/126L whole) read whole → 4 new capsule-v2 above; direct tests read whole: plugin/command.test.ts (47L), plugin/skill.test.ts (25L), config/plugin.test.ts (248L, 5 it.live), config/agent.test.ts (349L, 5 it.effect + 1 it.live), config/command.test.ts (83L), config/skill.test.ts (80L), config/provider.test.ts (269L, 3 it.effect), plugin/provider-openai.test.ts (176L, 7 it.effect), plugin/provider-google-vertex.test.ts (387L, 9 it.effect), plugin/provider-opencode.test.ts (483L, 11 it.effect + 1 it.live), plugin/provider-amazon-bedrock.test.ts (622L, 17 it.effect); 40 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 16 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): closed pass-15 target #1 (config store draft/transform machinery) and target #2 (ConfigProviderOptionsV1 request lowerers) — core/state.ts (128L whole) + agent.ts (111L whole) + command.ts (64L whole) + skill.ts (132L whole) + catalog.ts State.create draft block (:105-170) + integration.ts State draft + connection projection (:228-301) + v1/config/provider-options.ts (227L whole) read whole → 5 new capsule-v2 above; direct tests read whole: core/test/state.test.ts (115L, 4 it.effect), agent.test.ts (131L, 7 it.effect), command.test.ts (56L, 1 it.effect), skill.test.ts (125L, 2 it.live), catalog.test.ts (353L, 13 it.effect), integration.test.ts (349L, 9 it.effect), config/provider-options.test.ts (224L, 8 test + 2 test.each); 30 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 17 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): closed pass-16 targets #1-#3 plus the shared locking primitive — skill/discovery.ts (213L whole) + skill.ts load branch (:58-96) + permission.ts (310L whole) + permission/saved.ts (79L whole) + effect/layer-node.ts (333L whole) + effect/app-node.ts + app-node-builder.ts + app-node-platform.ts (60L total) + location-services.ts (115L whole) + location-service-map.ts (18L whole) + effect/keyed-mutex.ts (45L whole) read whole → 6 new capsule-v2 above; direct tests read whole: skill-discovery.test.ts (7 tests), permission.test.ts (315L, 11 it.effect), location-layer.test.ts (226L, 4 it.live), effect/keyed-mutex.test.ts (3 it.effect); 24 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Pass 18 (dedicated lane miner-opencode, SAME pin 03521003fafd re-verified via git rev-parse; Codebase Memory MCP not connected → direct source+test reading fallback): closed pass-17 targets #1-#2 plus the DB substrate beneath PermissionSaved — effect/runtime.ts (21L) + effect/service-use.ts (43L) + effect/memo-map.ts (3L) + fs-util.ts (260L) + database/{database,path,migration,migration.gen,sqlite,sqlite.bun,sqlite.node,schema.sql}.ts (57L/91L/81L/44L/8L/183L/178L/10L) + permission/sql.ts (20L) read whole, npm.ts :240-262 + instruction-context.ts :96 + permission/saved.ts (79L) + observability.ts (24L) as call-site cross-checks → 5 new capsule-v2 above; direct test read whole: test/filesystem/filesystem.test.ts (387L) + test/database-migration.test.ts (644L, 15 tests); `dfdf` resolved as a 103-byte stale worktree-save debris file, not live code; 30 byte-exact grep probes GREEN pre-write; bun test runner BLOCKED (same standing root cause) → deterministic evidence per Gate-5 fallback. Confirm every claim against source — the graph is an index, not truth.

## Full view (memory graph)
Revalidate `opencode` before porting: run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, and `get_code_snippet`. Graph root `/mnt/hdd/utopia/inspo/opencode` (live symlink into `coding-agents/opencode`), branch dev, pin 03521003fafd, 64,966 nodes / 235,651 edges, head==base==pin re-verified at pass 6. Source and direct tests decide shipped claims — the graph is an index, not truth.

## Boundaries
Adopt shadow-git undo, deferred-suspension permissions, event-sourced sessions, the tool write-path, and the plugin pipeline (staged loader + origin provenance + fingerprinted meta); adopt the prompt turn engine + input plane + run-state kernel contracts from pass 3; adopt the session context/compaction/retry/runtime-seam contracts from pass 4; adopt the turn-processor state machine, doom-loop ask-don't-fail guard, cleanup ladder, and network-error conversion contracts from pass 5; adopt the HTTP API server-plane contracts from pass 6 (typed route assembly, eager-subscribe SSE, sync-cursor fences, query-first auth, workspace request-plan routing with fence-blocked proxying, spec-transform codegen, deferred disposal/WS shutdown, bounded error bodies); adopt the session/PTY handler-plane contracts from pass 7 (engine-error mapping ladder with async-failure event publication, opaque-cursor backward paging with proxy-aware Link headers, single-use scoped tickets for credential-free WS upgrades, reason-code-preserving proxy pump); adopt the v2 session-engine + storage-substrate contracts from pass 8 (idempotent-by-client-ID prompt admission with equivalence-gated 409 and honest 503 stubs, three-phase revert as stage/cancel/commit with projector-side convergence, per-target-locked JSON key/value store with fail-without-advance migration marker, GET-only exact-path PWA auth bypass at router level, context-embedding bidirectional anchor cursors); adopt the MCP client-plane + v2 wake→drain contracts from pass 9 (auth-classified transport ladder with ownership-transfer connect and process-tree teardown, commit-on-success OAuth with URL-scoped credentials and state-keyed loopback callback, per-key serialized execution with coalesced wakes and trampoline successors, two-loop steer/queue drain with protocol-level step freeze and single-shot compaction recovery, event convergence with duplicate-creator die and boundary-seq dual-table revert truncation); adopt the LSP manager-plane + v2 context-epoch/history + turn-publisher contracts from pass 10 (per-language fleet with broken-set no-retry and in-flight spawn dedupe, heterogeneous diagnostic wait with parallel pulls and early-unblock-on-current-file, durable context epoch with blocked-initialize-no-row and ContextUpdated-as-system-message, one-predicate baseline/compaction history partitioning, die-on-wire-violation turn publisher with media-once settlement); adopt the system-context engine + output-retention + spawn-ladder contracts from pass 11 (per-source reconcile ladder with Incompatible→Replace escalation and unavailable-keeps-admitted, scope-lifetime registry with two duplicate-key failure points, settlement-side output bounding with wx-flag spill and fail-the-settlement retention, honest per-language spawn ladders where every failure yields undefined); adopt the ambient-instruction + v2 model/catalog/credential contracts from pass 13 (whole-set ambient instruction source with fail-open-to-Unchanged availability, four-stage model resolution with strict/lenient variant split and apiKey strip, derived-at-read catalog availability with recency default fallback and policy-denial-as-removal, transactional replace-on-create credentials with decode-tolerant reads); adopt the integration-plane + models.dev-substrate + catalog-schema + plugin-composition contracts from pass 14 (single-map OAuth attempt machine with completing single-flight latch, expiry scrub, and per-attempt forked scopes; disk→snapshot→flock-locked atomic-fetch cache ladder with never-fail double-checked refresh and infinite-TTL invalidation; ModelV2/ProviderV2 row shapes with empty-row factories and slash-tolerant parse; boot-ordered plugin composition where experimental modes become sibling models and explicit variants beat generated ones); adopt the config-registration + external-plugin-loading + vendor-patch contracts from pass 15 (draft-update ordering as the config-beats-built-ins mechanism with fail-soft markdown decode, disabled-removes, and path-action-only home expansion; background-forked external plugin loading with per-plugin ignoreCause isolation and a scope-attached promise bridge; the three-hook vendor shape over a mutable host adapter with output copy-back; uniform integration-method registry over vendor-owned flows with pending-as-data polling unions and metadata surviving refresh); adopt the state-kernel + draft-editor + connection-projection + V1-lowerer contracts from pass 16 (replayable ordered transforms over fresh-rebuilt state with finalize-before-commit atomicity and scope-attached dispose; create-on-first-touch/re-pin-key draft editors with SkillV2's sources-not-rows lazy loading; ambient-batch deferred rebuilds deduplicated per store; stateless connection projection newest-credential-first plus live env detection with lazy 5-minute-threshold OAuth refresh; prototype-safe package-keyed lowerer table with raw passthrough and fold-then-delete wire-option grouping); adapt the client surfaces, Effect/Layer wiring, and transport; omit site-specific TUI, cli/cmd/run product surfaces, built-in auth plugins, per-cli behavior, OpenAI-specific WS protocol constants, gateway billing sentinel details, and the remaining v2-protocol handler bodies (agent/command/credential/fs/health/integration/location/model/project-copy/provider/pty/question/reference/skill) not yet cited unless a target requires them; adopt the pass-17 contracts (whole-skill fail-closed catalog ingestion with versioned staging swaps, v2 ask/assert/reply with saved-approval layering and session-wide reject cascade, project-scoped allow-rule persistence, tag/hoist/compile per-tenant service isolation, refcounted keyed mutex); adopt the pass-18 contracts (lazy ManagedRuntime escape hatch over a process-wide memoMap with typed Proxy accessors, absence-as-value filesystem service with re-checked EEXIST and collect-everything walk ladder, one global SQLite node with channel path ladder and orDie PRAGMA/migration init, sentinel-table migration journal with one-shot drizzle import and foreign-DB die, platform-gated path column codecs with driver-boundary validation); adapt the client surfaces, Effect/Layer wiring, and transport; omit site-specific TUI, cli/cmd/run product surfaces, built-in auth plugins, per-cli behavior, OpenAI-specific WS protocol constants, and gateway billing sentinel details.

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`apply-patch-tool.md`](./apply-patch-tool.md)
- [`catalog-availability-projection.md`](./catalog-availability-projection.md)
- [`catalog-plugin-composition.md`](./catalog-plugin-composition.md)
- [`cloudflare-gateway-routing.md`](./cloudflare-gateway-routing.md)
- [`command-template-engine.md`](./command-template-engine.md)
- [`compaction-replay-choreography.md`](./compaction-replay-choreography.md)
- [`config-plugin-origins.md`](./config-plugin-origins.md)
- [`config-registration-pipeline.md`](./config-registration-pipeline.md)
- [`credential-replace-on-create.md`](./credential-replace-on-create.md)
- [`db-path-column-codec.md`](./db-path-column-codec.md)
- [`deferred-disposal-ws-shutdown.md`](./deferred-disposal-ws-shutdown.md)
- [`domain-draft-editors.md`](./domain-draft-editors.md)
- [`doom-loop-guard.md`](./doom-loop-guard.md)
- [`editing.md`](./editing.md)
- [`effect-flock-directory-lock.md`](./effect-flock-directory-lock.md)
- [`effect-runtime-escape-hatches.md`](./effect-runtime-escape-hatches.md)
- [`external-plugin-fork-loader.md`](./external-plugin-fork-loader.md)
- [`file-mutation-locked-cas.md`](./file-mutation-locked-cas.md)
- [`file-part-resolution-ladder.md`](./file-part-resolution-ladder.md)
- [`filesystem-search-dual-backend.md`](./filesystem-search-dual-backend.md)
- [`fsutil-safe-fs-service.md`](./fsutil-safe-fs-service.md)
- [`grep-glob-tools.md`](./grep-glob-tools.md)
- [`http-auth-ladder.md`](./http-auth-ladder.md)
- [`httpapi-error-body-shaping.md`](./httpapi-error-body-shaping.md)
- [`httpapi-route-assembly.md`](./httpapi-route-assembly.md)
- [`instruction-context-ambient-source.md`](./instruction-context-ambient-source.md)
- [`instruction-discovery-claims.md`](./instruction-discovery-claims.md)
- [`integration-connection-projection.md`](./integration-connection-projection.md)
- [`integration-oauth-attempt-machine.md`](./integration-oauth-attempt-machine.md)
- [`keyed-mutex-refcount.md`](./keyed-mutex-refcount.md)
- [`layer-node-tag-hoist-compile.md`](./layer-node-tag-hoist-compile.md)
- [`llm-event-publisher.md`](./llm-event-publisher.md)
- [`llm-runtime-seam.md`](./llm-runtime-seam.md)
- [`lsp-client-fleet.md`](./lsp-client-fleet.md)
- [`lsp-diagnostic-subscription.md`](./lsp-diagnostic-subscription.md)
- [`lsp-language-spawn-ladders.md`](./lsp-language-spawn-ladders.md)
- [`lsp-tool.md`](./lsp-tool.md)
- [`mcp-client-lifecycle.md`](./mcp-client-lifecycle.md)
- [`mcp-oauth-callback-server.md`](./mcp-oauth-callback-server.md)
- [`model-provider-schema-shapes.md`](./model-provider-schema-shapes.md)
- [`models-dev-catalog-cache.md`](./models-dev-catalog-cache.md)
- [`network-error-finish-conversion.md`](./network-error-finish-conversion.md)
- [`openapi-public-transform-codegen.md`](./openapi-public-transform-codegen.md)
- [`permission-saved-approval-store.md`](./permission-saved-approval-store.md)
- [`permission-v2-ask-machine.md`](./permission-v2-ask-machine.md)
- [`permissions.md`](./permissions.md)
- [`plan-mode-reminder-injection.md`](./plan-mode-reminder-injection.md)
- [`plugin-config-patching.md`](./plugin-config-patching.md)
- [`plugin-entrypoint-resolution.md`](./plugin-entrypoint-resolution.md)
- [`plugin-hook-runtime.md`](./plugin-hook-runtime.md)
- [`plugin-loader-pipeline.md`](./plugin-loader-pipeline.md)
- [`plugin-meta-fingerprint.md`](./plugin-meta-fingerprint.md)
- [`prompt-content-filter-surfacing.md`](./prompt-content-filter-surfacing.md)
- [`prompt-loop-exit-machine.md`](./prompt-loop-exit-machine.md)
- [`prompt-shell-as-user-message.md`](./prompt-shell-as-user-message.md)
- [`prompt-structured-output-capture.md`](./prompt-structured-output-capture.md)
- [`prompt-subtask-dispatch.md`](./prompt-subtask-dispatch.md)
- [`prompt-title-guardrails.md`](./prompt-title-guardrails.md)
- [`prompt-tools-permission-projection.md`](./prompt-tools-permission-projection.md)
- [`provider-error-retry-policy.md`](./provider-error-retry-policy.md)
- [`pty-ticket-ws-connect.md`](./pty-ticket-ws-connect.md)
- [`public-ui-bypass-grammar.md`](./public-ui-bypass-grammar.md)
- [`question-tool.md`](./question-tool.md)
- [`read-tool.md`](./read-tool.md)
- [`repository-cache-masquerade-guard.md`](./repository-cache-masquerade-guard.md)
- [`ripgrep-binary-acquisition.md`](./ripgrep-binary-acquisition.md)
- [`session-context-budget-ladder.md`](./session-context-budget-ladder.md)
- [`session-context-epoch.md`](./session-context-epoch.md)
- [`session-fork.md`](./session-fork.md)
- [`session-history-baseline-gating.md`](./session-history-baseline-gating.md)
- [`session-http-handler-plane.md`](./session-http-handler-plane.md)
- [`session-message-cursor-pagination.md`](./session-message-cursor-pagination.md)
- [`session-model-persistence-ladder.md`](./session-model-persistence-ladder.md)
- [`session-model-resolution.md`](./session-model-resolution.md)
- [`session-patch-usage.md`](./session-patch-usage.md)
- [`session-processor-event-machine.md`](./session-processor-event-machine.md)
- [`session-projector-convergence.md`](./session-projector-convergence.md)
- [`session-revert-stage-machine.md`](./session-revert-stage-machine.md)
- [`session-run-coordinator.md`](./session-run-coordinator.md)
- [`session-run-state-kernel.md`](./session-run-state-kernel.md)
- [`session-runner-drain-loop.md`](./session-runner-drain-loop.md)
- [`session-status-ledger.md`](./session-status-ledger.md)
- [`session-v2-anchor-cursor-pagination.md`](./session-v2-anchor-cursor-pagination.md)
- [`session-v2-durable-prompt-recording.md`](./session-v2-durable-prompt-recording.md)
- [`sessions.md`](./sessions.md)
- [`shell-tool.md`](./shell-tool.md)
- [`skill-discovery-url-pull.md`](./skill-discovery-url-pull.md)
- [`skill-tool.md`](./skill-tool.md)
- [`skill-v2-url-source-wiring.md`](./skill-v2-url-source-wiring.md)
- [`snapshot.md`](./snapshot.md)
- [`sqlite-database-node.md`](./sqlite-database-node.md)
- [`sqlite-migration-journal.md`](./sqlite-migration-journal.md)
- [`sse-event-stream-contract.md`](./sse-event-stream-contract.md)
- [`state-batch-deferred-rebuilds.md`](./state-batch-deferred-rebuilds.md)
- [`state-replayable-transform-kernel.md`](./state-replayable-transform-kernel.md)
- [`storage-key-value-lock-migration.md`](./storage-key-value-lock-migration.md)
- [`subagent-failure-propagation.md`](./subagent-failure-propagation.md)
- [`summary-git-path-unquote.md`](./summary-git-path-unquote.md)
- [`sync-fence-cursor.md`](./sync-fence-cursor.md)
- [`system-context-compare-ladder.md`](./system-context-compare-ladder.md)
- [`system-context-registry-builtins.md`](./system-context-registry-builtins.md)
- [`system-prompt-family-selection.md`](./system-prompt-family-selection.md)
- [`task-tool.md`](./task-tool.md)
- [`tool-call-cleanup-ladder.md`](./tool-call-cleanup-ladder.md)
- [`tool-output-prune-ladder.md`](./tool-output-prune-ladder.md)
- [`tool-output-store.md`](./tool-output-store.md)
- [`tool-schema.md`](./tool-schema.md)
- [`truncate-tool.md`](./truncate-tool.md)
- [`v1-provider-options-lowerers.md`](./v1-provider-options-lowerers.md)
- [`vendor-auth-quirk-patches.md`](./vendor-auth-quirk-patches.md)
- [`vendor-sdk-patch-hooks.md`](./vendor-sdk-patch-hooks.md)
- [`watcher-degrade-empty-git-head.md`](./watcher-degrade-empty-git-head.md)
- [`web-tools.md`](./web-tools.md)
- [`workspace-proxy-pump.md`](./workspace-proxy-pump.md)
- [`workspace-routing-proxy-fence.md`](./workspace-routing-proxy-fence.md)
- [`write-tool.md`](./write-tool.md)
- [`ws-responses-stream-adapter.md`](./ws-responses-stream-adapter.md)
- [`ws-session-pool-fallback.md`](./ws-session-pool-fallback.md)
