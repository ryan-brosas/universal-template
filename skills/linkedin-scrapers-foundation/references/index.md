<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->

# linkedin-scrapers-combined: LinkedIn scraping & Easy Apply automation patterns

## Use this for
Use when building or repairing a LinkedIn scraper, Sales Navigator harvester, or Easy Apply bot: session/auth handling, rate-limit evasion and detection, pagination and lazy-load walking, profile/job data normalization, application-form automation, and run-level orchestration with crash-safe logging. Each capsule distills ONE porting question from the best implementation found across the suite; source code and direct tests are ground truth.

## Load the matching source dump
- `./voyager-api-client.md` — call `/voyager/api` with cookie auth: dual header sets, CSRF-from-JSESSIONID strip, transport-level evade.
- `./cookie-session-persistence.md` — cache-first session jars keyed per account with LOUD expiry instead of silent re-login.
- `./browser-login.md` — automate browser login and classify feed/checkpoint/authwall/still-login outcomes into distinct errors.
- `./rate-limit-detection.md` — three throttle signals (URL/CAPTCHA/body-text) raise; only timeouts retry on exponential backoff.
- `./browser-fingerprint-stealth.md` — AutomationControlled suppression trio, real-profile reuse, anti-detect CDP attach.
- `./browser-lifecycle.md` — Playwright BrowserManager: ordered-guarded teardown, half-start rollback, storage_state round-trip.
- `./scraper-base-callbacks.md` — BaseScraper guard-fused navigation + async progress callback protocol (console/JSON/multi sinks).
- `./sales-nav-pagination.md` — walk Sales Nav pages: enabled-button gate, container-scoped scroll, index re-location, response capture.
- `./voyager-pagination.md` — page count-capped feeds to exactly `limit`: remainder-shrunk counts + three-exit loop + ~1000-result wall.
- `./humanization-scroll.md` — four terminated+bounded scroll disciplines and jittered pacing that keep virtualized lists loading.
- `./response-interception.md` — substring-gated waitForResponse with JSON / `<code>`-island dual decoder beats DOM parsing.
- `./profile-schema.md` — normalize URN/composite/DOM IDs to canonical `/in/<slug>` URLs; optional-everything-but-validated-URL models.
- `./search-url-builder.md` — encode filters as query strings (f_E/f_WT/f_TPR/f_JT/f_SB2, first-bare/rest-%2C) instead of clicking UI.
- `./easy-apply-modal.md` — modal state machine with strict submit>error>next>review priority and two-way termination contract.
- `./form-question-answering.md` — widget dispatch order + decline/yes/no phrase-family snapping + random-index≥1 last resort + audit tuples.
- `./dedupe-applied-tracking.md` — never re-process: append-only CSV state, first-column key sets, set+live-DOM layered guards.
- `./job-run-orchestration.md` — full run loop: per-job exception islands, dual ledgers, three-level caps, external-vs-Easy detection ladder.
- `./queue-cache-decorators.md` — Linked Helper's per-account action queues and generation-counter cache invalidation (learn-only, no license).
- `./chunked-export-iterator.md` — bounded-chunk async export with memoized totals and Python-style negative windows (learn-only).
- `./profile-section-expansion.md` — main-page-first-then-details-page fallback ladder per profile section; `(url_path, category)` accomplishment table + seen-titles dedupe.
- `./nested-position-parsing.md` — structural nested-position detection (inner `.pvs-list__container`) + shared `·`/` - ` date-range grammar.
- `./tabbed-overlay-section-walking.md` — click-then-read ARIA tablist for Interests + `<h3>`-heading walk of the contact-info overlay.
- `./feed-post-urn-scan.md` — URN-scan identity for feed posts: `urn:li:activity` keying, selector-ladder text, dual-bounded scroll loop.
- `./voyager-mutation-endpoints.md` — POST-vs-GET split, inverted error-state bool, `action`+`decorationId`+normalized-JSON mutation shape.
- `./selenium-click-finder-ladder.md` — normalized-text XPath click helper + falsy-sentinel try ladder for tolerant Selenium automation.
- `./config-validation-ladder.md` — typed checker ladder with self-diagnosing errors; per-file validators fail fast before network.
- `./voyager-graphql-queryid.md` — `/graphql?variables=(...)&queryId=` profile components; `titleV2.text.text` + grouped-item branch.
- `./voyager-search-facet-encoding.md` — API-side search facets: people `(key,value:List(...))` filter strings vs jobs `selectedFilters` dict + brace→paren query-string assembly; `" | "` vs `,` multi-value separators; `r<seconds>` time-posted.
- `./voyager-password-auth-metadata.md` — API password auth: seed-JSESSIONID-then-POST, `login_result == "PASS"` gate with `ChallengeException`, android-header auth split, meta-tag metadata bootstrap.
- `./playwright-resilient-helpers.md` — Playwright helper toolkit: severity-split wait (self-diagnosing `ElementNotFoundError` + selector tips) vs safe-default extract/close; poll-based manual-login wait.
- `./voyager-dual-collection-sort.md` — feed order vs content arrive in TWO collections (`data['*elements']` sorted URNs + unsorted `included[]` payloads): stitch-by-collision assembler, promoted purge before sort, ordered URN replay join.
- `./linvo-service-taxonomy.md` — ONE `process(page, cdp, data)` interface behind a flat registry, with a typed `LinkedinErrors` enum whose `more` payload carries the scheduler's pause duration.
- `./throttle-classification-ladder.md` — classify WHICH LinkedIn limit hit via body-text probe table → typed DELAY error with per-limit pause minutes and enforcement scope.
- `./ghost-cursor-click-ladder.md` — ghost-cursor glide → real coordinate click → DOM click: ordered degradation that keeps human-like events first.
- `./overlay-pushstate-trigger.md` — summon the contact-info overlay by history pushState/back instead of UI clicks; capture post-redirect href BEFORE mutating history.
- `./visit-resolve-identity.md` — visiting IS identity resolution: re-read `window.location.href` AFTER the settle timer and canonicalize THAT into linkedin_id — LinkedIn redirects renamed profiles and state changes during navigation.
- `./like-with-recovery-envelope.md` — engagement actions compute the identity payload FIRST, wrap only the risky click region in try/catch, and return the SAME `{linkedin_id, url}` envelope on success/empty/error; about:blank interstitial resets SPA state before deep links; `slice(0,1)` volume-bounds by construction.
- `./connect-outreach-ladder.md` — pending-guard → 4-method connect-button ladder → email-verification wall → add-note modal ladder → post-redirect canonical id capture.
- `./message-send-guard-chain.md` — reply-state policy gate (`IgnoreProspectMessages` 0/1/2) + fuzzy self-duplicate check → contenteditable insert → count-delta send verification.
- `./inbox-harvest-shuffle.md` — visible-row budget + InMail/Offer/Sponsored filter + shuffle BEFORE walking the inbox: order randomization defeats sequential-sweep bot signatures.
- `./embedded-json-search-parse.md` — parse the raw response buffer's LAST `<code>` blob for people-search JSON (off-DOM); 1000-result-cap pagination math without DOM scraping or an API client.
- `./feed-engagement-caps.md` — collection-time `slice(0,4)` volume cap, >30-likes comment gate, shuffled generic-comment pool, per-post failure isolation.
- `./message-template-substitution.md` — fast-path-guarded `{{placeholder}}` global-replace loop over {label,value} pairs; emoji-stripped values; unfilled placeholders stay visible.
- `./dry-run-run-cap-gates.md` — a dry-run flag consulted at EVERY irreversible action (log-instead-of-submit at the last moment) plus a post-increment per-run cap with truthy-zero-means-unlimited semantics and stacked loop breaks.
- `./qa-memory-ladder.md` — self-learning answer stack: persistent qa.csv memory consulted BEFORE a keyword→answer ladder; unknown questions get a loud sentinel + timed human-takeover window and are remembered forever after.
- `./string-outcome-channel.md` — classify each outcome ONCE into a closed `* prefix` string vocabulary; derive log lines, ledger columns, and counters from that one decision point inside a per-item exception island.
- `./md5-pickle-cookie-jar.md` — Selenium session jar keyed `md5(email)` → `cookies/<hash>.pkl`: delete-BEFORE-replay restore, non-fatal guarded save, /feed liveness probe (ember14 = build-unstable trap); a failed login still persists the guest jar.
- `./displayed-count-page-budget.md` — turn the "1,234 jobs" display label into ceil(count/25) pages under a hard 40-page wall; the bare-int branch bypasses the cap (trap) and malformed text raises past an unprotected call site.
- `./stale-proof-two-pass-harvest.md` — scalarize listing IDs to plain ints IMMEDIATELY, then RE-FIND rows for the Applied-badge pass and set-difference before visiting anything; parent-scoped len(find_elements)>0 probe.
- `./progress-percent-step-budget.md` — multi-step Easy Apply walked by the COMPUTED budget floor(100/perc)-2 fill→Continue clicks (≤0 ⇒ range() no-op safety), dry-run gate after filling but before Review/Submit; issue-#72 interstitial dismissed once.
- `./blacklist-inline-annotation-gate.md` — filter hits are annotated INTO the ledger row text and the orchestrator gates on substring "blacklisted" — one channel serves logging + filtering; upstream :335 tests companies against the TITLE (bug trap).
- `./phone-field-selector-ladder.md` — known-value field filling: config→YAML value ladder, 7 CSS families then 4 translate() XPaths, every write gated visible-AND-empty so prefilled answers survive; total failure stays silent.
- `./preferred-resume-picker.md` — act only when the upload-required marker is present; click the single-or-preferredCv-th pdf card only after aria-label=='Select this resume'; `type(len(resumes)) != int` dead branch documented as a trap.
- `./sales-nav-lead-identity.md` — Sales Nav row → canonical `/in/<id>` without opening the profile: `data-scroll-into-view` URN split first, `/sales/lead/` href split second, raw href last; name from a row-scoped selector, never the anchor.
- `./na-preserving-row-extraction.md` — five pre-initialized NA fields per card keep every row schema-total (failures degrade values, never row count); `tell()==0` header probe; re-find by index after scroll; Next-button enabled/disabled/absent trichotomy.
- `./declarative-delay-validation.md` — typed `{min,max}` humanized-delay settings per action key, whitelist-stripped then validated at load with a cross-field `max>=min` rule whose error message names the human-readable action.
- `./durable-named-cursor-continuation.md` — composite `(timestamp, id)` event cursors encoded as zero-padded opaque strings, one row per (account, channel), loud validated decode, fetch→consume→commit separation for at-least-once resumption.
- `./channel-fetcher-contract.md` — open/closed event-source registry: implement five methods, register `{fetcher, codec}` by channel name; unknown channels fail loudly; fan-out rows carry parent cursor fields.
- `./ledger-contrast-csv-vs-summary.md` — append-only CSV ledger vs rewrite-on-write numbered summary: crash-safety/O(1)-per-event/state-reuse vs human readability/daily rotation; never merge the two jobs into one file.
- `./included-envelope-hydration.md` — turn any Voyager `data/*` + `included[]` envelope into typed objects: filter by exact `$type` const, transform, keyBy URN-derived id, undefined-safe join onto hits via targetUrn/*from/*participants.
- `./voyager-star-pointer-joins.md` — `*field` values are pointer REFERENCES into `included[]`, not data: each URN grammar needs its own unwrap (`urn:li:fs_messagingMember:(n,id)` → split(',')[1]; scheme-stripped miniProfile urns) before joining against the profileId-keyed map; member join fields are direction-dependent (sent→`*toMember`, received→`*fromMember`).
- `./collection-response-envelope.md` — ONE generic `LinkedInCollectionResponse<T,I,M>` types every GET endpoint: `data.elements` hold URN pointers while sibling `included[]` defines entities; per-endpoint metadata M carries pagingToken/unreadCount/invitationType; writes mark themselves with literal empty `included: []`.
- `./scroller-factory-repositories.md` — repository methods return live scroller objects built around bound private fetch closures; convenience methods are pure filter rewrites over ONE wire path.
- `./created-before-scroller.md` — newest-first timestamp pagination: cursor = last-seen key, LIFO page-stack for scroll-back, +1000 seed makes page one refetchable, empty page = idempotent no-op.
- `./skip-limit-scroller.md` — offset pagination state machine: empty-page latch short-circuits dead sources, two-limit rewind unlatches, counter answers "no previous page" honestly.
- `./dual-persona-auth-headers.md` — mobile-app fingerprint headers authenticate `/uas/authenticate`, desktop voyager-web headers call the API; csrf-token = raw JSESSIONID; cookies serialized as `k="v"; ` pairs.
- `./cookie-jar-phase-taxonomy.md` — cookie NAMES document auth phases (AnonymousCookies required-six vs AuthCookies optional superset) but are docs, not validators; `set-cookie` arrays parse through a first-key-wins kernel (`pickBy(k === Object.keys(parsed)[0])`) or attribute tails poison the jar; `{JSESSIONID}` alone is a valid partial jar.
- `./sessions-json-cache.md` — multi-account session jar keyed by username in one JSON file: read-through-create for missing/corrupt file, spread-merge writes preserve sibling accounts, useCache:false forces fresh auth.
- `./voyager-filter-grammar-serializer.md` — structured filters as `List(key->value|value2)` query grammar: three reserved separators, identity final-encode, caller-side keyword escaping, count/start wire names.
- `./decoration-id-response-shaping.md` — versioned `decorationId` params select server-side projections; parse by `$type` consts so deco bumps degrade instead of crash; pin versions to fixtures.
- `./messaging-write-path-duality.md` — one messaging namespace, two payload shapes: existing conversationId → `/events` subpath + minimal body; none → create endpoint + recipient envelope with typed MessageCreate key.
- `./send-then-verify-invitation.md` — write APIs returning no body are materialized by reading back the newest list row; direction-dependent join field (`*toMember` sent / `*fromMember` received); recency assumption recorded.
- `./own-profile-bootstrap.md` — resolve "me" through the mini-profile row then delegate to the full-profile parse path; null-on-miss as data-shaped failure; single-envelope company join via headline substring crosswalk.
- `./full-response-transport-kernel.md` — one axios instance, two return granularities: `fullResponse:true` is the only escape hatch to headers (auth calls), endpoints stay slash-less because `new URL(path, base)` drops `/voyager/api` on a leading slash, persona swaps REPLACE `defaults.headers` wholesale, and error handling is deliberately absent.
- `./invitation-mailbox-read-grammar.md` — invitation mailbox reads are asymmetric BY DESIGN: received = selector view (`relationships/invitationViews`, `q=receivedInvitation`), sent = type-filter V2 view (`relationships/sentInvitationViewsV2`, `q=invitationType` + verbatim `CONNECTION`), restli start/count renamed from skip/limit.
- `./testdouble-mock-harness.md` — replace→dynamic-import→stub-by-wire-shape→assert-parsed-output ladder that gives scraper clients real test coverage without live sessions; factories emit valid `$type`-tagged envelopes.
- `./client-field-precedence-wiring.md` — composite clients wire repositories as class fields capturing `this` BEFORE the constructor body assigns the transport hub: children must store the parent link and defer reads to call time (TS field-initializer ordering trap).
- `./schema-driven-config-panel.md` — one declarative field schema renders the whole settings UI AND derives `valid_keys()` for validate/coerce/reject; the panel never edits `.py` files, only a JSON overlay.
- `./config-override-noop-layer.md` — 55-line JSON-over-Python-defaults kernel: missing/corrupt file = clean no-op, only pre-existing module globals are overridable, user JSON can never mint new names.
- `./schema-typed-coercion-ladder.md` — untrusted form JSON → typed config without a framework: bool-guard-BEFORE-number (Python `True` is an `int`), whole-float int-snap, truthy-string vocabulary, comma-list parsing.
- `./applied-jobs-csv-json-remap.md` — expose a bot-written CSV ledger as JSON via an explicit column remap; stamp one row by whole-file rewrite with IDENTICAL fieldnames so both consumers survive.
- `./chrome-profile-degradation-ladder.md` — real Chrome profile → temp/guest → retry-as-guest: login state comes free when possible, and a broken profile degrades to a working session instead of killing the run.
- `./messaging-read-path-scrollers.md` — thin CreatedBeforeScroller subclasses parameterized ONLY by entity timestamp fieldName (`lastActivityAt` vs `createdAt`) + conditional-spread wire params + `LEGACY_INBOX` read projection.
- `./source-carrying-model-serialization.md` — re-attach live data sources during hydration via a WeakMap ambient context planted in transform options; serializing a source-less model throws loudly.
- `./live-collection-views.md` — result sets as lazy query facades: `size` = live count, asyncIterator chains prev-id cursors, random access compiles to one-row slice queries — nothing materializes wholesale.
- `./linkedin-url-canonicalization.md` — TOTAL function normalizing bare paths/full URLs/Sales Nav links to canonical `/in/<slug>`: shape-gate BEFORE `new URL()` parse, `''` is the failure value, never a throw.
- `./visit-personalize-delegate.md` — composite actions acquire their data by visiting first, render templates from LIVE page data, guard relationship preconditions with a cheap DOM probe, then delegate to leaf services.
- `./search-blended-hit-joins.md` — rehydrate full entities onto search hits by URN: single-hop targetUrn join for people/companies vs the TWO-HOP hit→jobPosting→company chain in the jobs path (keyBy per $type, iterative resolution, raw-URN keys).
- `./typed-search-filter-contracts.md` — expose search filters as typed interfaces (`PeopleSearchFilters`/`JobSearchFilters`) with letter/digit wire enums, list-or-scalar fields, and Omit-based scope locking for derived APIs.
- `./search-scroller-twins.md` — subclass a paging base ONLY to bind a typed fetch closure + entity config; all cursor semantics stay in SkipLimitScroller; filter-only variants are repository-layer rewrites, not classes.
- `./langgraph-form-answer-pipeline.md` — provider-agnostic LLM form answering: one compiled graph with conditional select/text routing, exact→ci→substring→passthrough option snapping, structured-output-with-JSON-fallback, None-safe fail-soft entry points.
- `./session-summary-ledger-append.md` — same-file dual-writer split: append-mode banner-delimited session summary coexists with rewrite-on-write row ledger because each writer strips the other's `----` markers.
- `./structural-duck-type-guards.md` — validate deserialized domain objects with presence-list + method-list duck typing and positive-id gates; boolean-returning guards accept entity OR bare id at collection boundaries (lh-basis, patterns only).
- `./url-roundtrip-extraction.md` — pull clean canonical URLs from DOM listings: selector existence probe as honest-empty signal, query-strip → absolutize → dedupe AFTER normalization ordering, per-link exception islands.
- `./overview-vocabulary-classifier.md` — extract labeled fields from redesigned DOM that lost its labels: seed-dict-all-None, classify unlabeled chips by CONTENT vocabulary (employee/k+→size, comma+state→HQ, industry words→industry, follower→skip), structural dt/dd fallback ONLY at zero progress, never raise.
- `./cookie-session-bootstrap.md` — authenticate a browser scraper WITHOUT scripting login: plant the hand-extracted `li_at` cookie, then a redirect-probe of `/login` (redirected to /feed = alive; still on /login = throw typed SessionExpired). *(landed mid-pass by sibling lane — verified against source before wiring)*
- `./puppeteer-flag-stack.md` — container floor (`--no-sandbox`, `--disable-setuid-sandbox`, forced-direct proxy pair, `--disable-dev-shm-usage`) vs cargo cult (`'---single-process'` triple-dash no-op typo, `--enable-automation` anti-stealth).
- `./request-interception-budget.md` — two-tier abort handler: TYPE list always blocked (image/media/font/…), HOST map from vendored MVPS hosts file gating script/xhr/fetch/document; stylesheet blocking breaks LinkedIn and is explicitly avoided.
- `./raw-clean-two-phase-parse.md` — dumb browser, smart Node: `page.evaluate` emits nullable raw text; ALL cleaning/parsing happens in pure Node functions (RawX → X interfaces with derived fields like durationInDays).
- `./duration-with-present.md` — 'Present' → now() at format time, +1 inclusive day diff so same-day ranges are 1 not 0, null-on-missing rather than fabricated durations.
- `./expand-then-parse.md` — two click families with a settle wait between them: unique per-section expanders first, then repeated-item see-more clamps, each in its own try/catch island; 100ms wait prevents "Node is detached" errors.
- `./geo-string-classifier.md` — comma-split location where part-count doesn't pin roles: 3 parts = positional city/province/country, 2 parts resolved by ISO country/city predicates, never guessed.
- `./zombie-browser-teardown.md` — guarantee no orphaned Chrome survives the process: catch-path close() + post-close treeKill(SIGKILL) behind browser.close(); keepAlive flag trades teardown for faster recurring scrapes. *(landed mid-pass by sibling lane — verified against source before wiring)*
- `./idle-breathe-readiness.md` — sliding-window quiet detection: recv-timeout fires an idle latch, with an interactive-vs-production dual threshold (0.75s/0.10s) so `await tab` settles without fixed sleeps (zendriver, patterns only).
- `./lazy-domain-enablement.md` — derive CDP session state from declared handler interest: auto-enable domains, keep a separate manual-intent lane that always wins, replay on reconnect, never re-enable a custom-configured domain (zendriver, patterns only).
- `./headless-expert-prep-latches.md` — one-shot per-connection stealth patches inside the send path: strip `Headless` from the real UA (never a canned spoof) and force-open shadow roots via `attachShadow` monkey-patch; reserved id −2 bootstrap channel (zendriver, patterns only).
- `./cloudflare-shadow-checkbox-solver.md` — find a CF Turnstile challenge inside closed shadow roots by marker-in-HTML scan, then click at 15%-width/vertical-center (not center) and treat input-disappearance as success (zendriver, patterns only).
- `./browser-launch-flag-assembly.md` — typed-attribute browser config with guarded add_argument, sorted dedup flag assembly, shortest-path binary resolution, root auto-sandbox-drop; the flag-set counterpart to puppeteer-flag-stack (zendriver, patterns only).
- `./pattern-filtered-cookie-jar.md` — regex-subset cookie persistence: pickle round-trip of only the cookies whose serialized fields match a pattern (e.g. `li_at`), faithful restore, requests-format interop (zendriver, patterns only).
- `./react-controlled-input-fix.md` — bypass React's `_valueTracker` with the native prototype setter + a real InputEvent so onChange fires; Backspace-from-end with per-char delay for the deleting variant (zendriver, patterns only).
- `./stale-node-retry-ladder.md` — bounded one-retry stale-node recovery keyed on backend_node_id with an explicit last-attempt marker; re-resolve elements before re-querying (zendriver, patterns only).
- `./humanized-scroll-gesture.md` — percentage-of-viewport scroll via synthesized gesture with explicit speed + prevent_fling + derived sleep (distance÷speed), the CDP-native scroll counterpart to humanization-scroll (zendriver, patterns only).
- `./cdp-keyboard-event-machinery.md` — grapheme-split typing, base-key + Shift normalization, modifier-down/main/modifier-up(reverse) ordering with accumulated masks; the input-event layer under every form-fill (zendriver, patterns only).
- `./network-expectation-context.md` — future-backed single-shot request/response expectations with body-after-loading-finished gating and self-removing handlers; fetch interception for block/modify/fulfill (zendriver, patterns only).
- `./browser-lifecycle-teardown.md` — async-factory + HTTP-readiness poll + target autodiscover + graceful-then-kill teardown with bounded grace and temp-profile preservation; the process-level counterpart to zombie-browser-teardown (zendriver, patterns only).
- `./text-find-best-match-ladder.md` — text-finding without a driver: server-side DOM.perform_search + discard, hits resolved against the local tree, useless text nodes hop to their parent element, best_match picks closest text LENGTH, and every finder doubles as a 0.5s-poll wait condition that raises TimeoutError — while xpath() contractually never raises (zendriver, patterns only).
- `./js-dumps-two-strategy-ladder.md` — export arbitrary JS objects to Python: greedy prototype-chain key harvest with depth-cap-2 dump, exception-gated fallback to a WeakSet cycle-guarded dumper, both via raw runtime.evaluate with allow_unsafe_eval_blocked_by_csp (deliberately NOT Tab.evaluate) (zendriver, patterns only).
- `./contra-dict-attr-mapping.md` — JSON-as-object dict subclass by aliasing instance `__dict__` to the mapping itself: attribute access prefers DATA over dict methods, values wrap recursively on the way in, reserved names warn instead of crash (silent=True at both call sites); docstring's snake_case promise is NOT implemented — keys stay verbatim (zendriver, patterns only).
- `./quads-center-mouse-kit.md` — real-coordinate mouse actions: get_content_quads → Position center/abs math (+scroll for page coords), press/release with click_count=1 pairing, stepped drag interpolation with cooperative yields, empty quads = skip-not-crash; the trusted-event counterpart to Element.click()'s DOM-synthetic click (zendriver, patterns only).
- `./page-capture-download-gate.md` — persist pages as MHTML/screenshot/PDF behind one skeleton: sleep-refresh the target URL first, capture, raise ProtocolException on EMPTY payloads ("page not finished loading"); downloads need set_download_behavior("allow") latched first — which DownloadExpectation later restores (zendriver, patterns only).
- `./evaluate-dual-return-modes.md` — one Runtime.evaluate call serving two serialization modes: by-value returns JSON-round-tripped data while `return_by_value=False` flips to deep SerializationOptions (caps 10/10, shadow trees all) that survives cyclic/DOM graphs; fixed user_gesture+CSP-bypass flags; the hinted tuple return is DEAD — only js_dumps returns tuples (zendriver, patterns only).
- `./cdp-transaction-generator-protocol.md` — cdp method factories are SINGLE-YIELD generators; `Transaction` consumes the first yield to learn method/params and completes the asyncio future by resuming that same yield with `send(result)` — StopIteration.value IS the typed result; keyword-only `__call__`; `error` key → ProtocolException; two-yield adapter generators provably degrade to "could not parse"; GLOBAL_DELAY constant recorded dead (zendriver, patterns only).
- `./websocket-dispatch-ledger.md` — one websocket multiplexes concurrent commands via a mutex-guarded id counter plus a mapper ledger; the reader loop POPS each transaction BEFORE completing it (upstream memory-leak fix), tolerates reserved `-2`/unknown ids as silent continues, fans events out to `(event, connection)`-or-`(event)` callbacks with sync ones pushed to threads, and exposes `feed_cdp` sync fire-and-forget for blocking interception; MAX_SIZE/PING_TIMEOUT socket constants pinned (zendriver, patterns only).
- `./connection-class-armor-send-pipeline.md` — `CantTouchThis` metaclass turns class-level attribute writes into PermissionError-family raises (except `__annotations__`) while instance state stays per-object; every send lazily re-checks socket, LATCHED `_prepare_expert`/`_prepare_headless` preps, and listener health before writing; constructor `**kwargs` land silently as instance attrs; target info delegates through ~12 None-safe read-only properties refreshed by `sleep`/`wait` (zendriver, patterns only).
- `./easy-apply-button-sentinel.md` — Easy Apply button detection with a `False` sentinel consumed by IDENTITY check (`is not False`); text-verified multi-match beats class-only locators; the SAME sentinel projects the ledger's `attempted` column so the audit trail can't lie.
- `./trackingid-bytes-twins.md` — mint client-side 16-random-byte trackingIds per mutation; raw chr-join charString vs base64-slice twins for binary-tolerant vs text-only transports; server-issued search-hit ids are a DIFFERENT contract (private-api).
- `./working-hours-interval-algebra.md` — scope-nested weekly schedule test + closest-date composition (intersect vs inverted-merge of global schedule) with deterministic-seeded daily timeshift jitter and the all-day-and-night short-circuit that must run BEFORE shift math.
- `./aggregate-versioned-event-kernel.md` — event-sourced aggregates with dual-path versioning: optimistic bump+pending for new events vs strict no-gap check for persisted ones; guard-before-apply, freeze-on-snapshot, loud unknown-event handling.
- `./streaming-csv-import-gate.md` — streaming CSV import: first-chunk header checksum + foreign-file gate BEFORE any insert, per-row reasoned failures instead of aborts, backpressure on both hops, row-index checkpoints.
- `./legacy-template-variant-migration.md` — migrate flat `{valueParts, variables}` templates into a variants AST; the REVERSED if-then-else options indexing (`options[1]`=then, `options[0]`=else) is the named trap.
- `./derived-column-descriptors.md` — repo-descriptor `toColumn`/`fromRow` converters recompute derived flags (dayAndNight from start/end, bool↔INTEGER) at every boundary crossing so write and read can never drift.
- `./busy-retry-transaction-kernel.md` — closed-code-list SQLITE_BUSY retry replaying WHOLE transactions at fixed delay; one `_executeInTransaction` entry hides SQLite raw BEGIN levels vs PG isolation mapping; savepoint-wrapped nested ops, rollback-before-rethrow, ≤999-param insert chunking, empty-IN→TRUE/FALSE, centralized `_e`/toFuzzy escaping (lh-basis, patterns only).
- `./sqlite-init-phase-pipeline.md` — bootstrap as an ordered list of small transactional-retryable phases executed through the SAME per-source queue as business traffic; clearDB wipes DATA not schema (FKs off → triggers/views dropped → reverse-order DELETE skipping the version row), dropDB is one multi-statement DROP TABLE batch; both queued (lh-basis, patterns only).
- `./cqrs-context-composition.md` — bolt CQRS handlers onto a legacy source: token-keyed DI bindings per entity, `{commands, queries, events, legacy-guards}` facade as the single entry, transaction provider REBOUND after host DB init so both planes share one scope; write ordering guard→versioned append→save→publish; guards are query handlers throwing typed domain errors (lh-basis, patterns only).
- `./packaged-keyseq-devtools-latch.md` — packaged Electron builds gate DevTools/unlock/recovery behind prefix-matched typed keyword sequences (`launcher`/`auto`/`bbb`/`unlock`) with self-disarming latches and preventDefault hiding; dev builds fire instantly; actions hang off injectable globals (linked-helper-extract, patterns only).
- `./invitation-shared-secret-lifecycle.md` — list invitations, then answer with the server-minted sharedSecret that ONLY the listing supplies; view-envelope unwrap + fail-soft empty-list ambiguity (open-linkedin-api).
- `./job-detail-urn-plane.md` — job postings + skills where HTTP 200 can hide an embedded application error and the skills URN rides as a percent-encoded PATH segment (open-linkedin-api).
- `./universal-name-entity-lookup.md` — companies AND schools resolve through ONE /organization/companies?q=universalName endpoint keyed by public slug; first-row take needs the emptiness guard the original lacks (open-linkedin-api).
- `./update-extractor-family.md` — flatten scattered update-envelope fragments into post rows with a last-row-or-new-row streaming assembler, positional composite-URN splits, and a sentinel content ladder (open-linkedin-api).
- `./conversation-participant-resolution.md` — find THE thread with one profile by hand-inlining List() into the URI (params dict mis-serializes); miss returns {} and ids derive from entityUrn (open-linkedin-api).
- `./company-search-vertical-projection.md` — specialize blended search for a vertical at TWO layers: wire resultType filter + client-side trackingUrn kind gate, null-safe flattening after (open-linkedin-api).
- `./profile-me-card-variant-family.md` — read per-profile identity cards (privacy/badges/networkinfo) through ONE template: slug URI + explicit normalized+json accept (default headers omit it) + status-gated {} + data unwrap (open-linkedin-api).
- `./me-cache-memoization.md` — lazy /me read-through memo on the SHARED Client.metadata dict with force-refresh toggle; cookies-direct construction permanently skips the auth metadata scrape (open-linkedin-api).
- `./auxiliary-request-planes.md` — two non-voyager planes off one transport pair: /li/track telemetry beacon as JSON-under-text/plain on the bare www host (base_request=True), and action-verb unfollowByEntityUrn write on the API plane; inverted error bools both (open-linkedin-api).
- `./wvmp-guarded-descent.md` — seven-hop fixed-key $type descent into profile-view numViews with ZERO guards is the negative template; port as guarded .get()/length-checked walk with versionable $type constants (open-linkedin-api).
- `./member-share-feed-scoping.md` — scope feed reads to ONE member via paired (endpoint, moduleKey, identity-key) triples across profileUpdatesV2 (fsd_profile URN, token paging) vs /feed/updates (profileId set-literal quirk, ratio-wall recursion) (open-linkedin-api).
- `./visible-card-worklist-harvest.md` — build a virtualized-list work list by scrolling the container FIRST (Selenium only sees mounted cards), RE-collecting after the ladder, filtering Applied-badge/blacklist/data-job-id-sentinel at collection time, and bounding each session with a wall-clock budget; documents the stalled-offset trap (`start=` never increments).
- `./randomized-search-combo-walk.md` — enumerate positions×locations pairs in RANDOM rejection-sampled order (each exactly once, >500 hard break) so query patterns never look like sequential sweeps.
- `./path-first-browser-binary-discovery.md` — resolve chromedriver AND chromium/google-chrome from PATH with fail-fast FileNotFoundError BEFORE any network code; explicit Service + binary_location binding beats download-manager hope on NixOS-style hosts.
- `./offscreen-window-scrape-mode.md` — park the browser at 1×1 off-monitor between sessions and restore/maximize when scraping: headed rendering fidelity without desktop obstruction.
- `./yaml-entry-gate-none-tolerant-defaults.md` — startup config gate: presence asserts → one shape-specific exception naming the exact YAML edit ('-' list vs dict) → secret-redacted echo → `.get` defaults plus per-entry None filtering.
- `./lookup-exception-topology.md` — match each Selenium lookup's REAL exception: find_elements never raises (len>0 probes), plain find_element raises NoSuchElementException even under implicit waits, only until() raises TimeoutException; start_linkedin's except-TimeoutException soft-fail is structurally dead, so missing login fields crash the constructor.
- `./browser-title-ledger-identity.md` — ledger identity split positionally out of the tab title ('Job | Company | LinkedIn' — index IS the field) with per-part regex normalization; the unguarded [1] IndexErrors on separator-less titles and the print-and-continue island silently drops that row while the run survives.
- `./async-retry-disarm-trap.md` — opt-in exception-tuple exponential-backoff retry decorator whose only in-repo usage swallows the exact tuple member and returns False, structurally disarming itself: decorate only bodies that let tuple exceptions propagate.
- `./job-topcard-middot-triplet.md` — location/posted-date/applicant-count read from ONE unified-top-card container split on '·' where parts[0]/[1]/[2] IS the field identity, plus guarded vocabulary fallbacks (length caps, $ exclusion, title-equality exclusion).
- `./session-producer-manual-login-poll.md` — headed browser + human login + 1s is_logged_in poll with deadline → save_session artifact that every headless tool and test keys on; completion predicate shared with consumers.
- `./auth-session-verdict-tiers.md` — cookie-login verification decides by EVIDENCE TYPE in three tiers: observed death (redirect to login/authwall) throws typed AuthenticationError, unverifiable (0.5s×10 poll inconclusive) warns "Proceeding anyway" and continues, other causes wrap into that same typed error; contrasted with load_session's by-construction write-only `_is_authenticated=True`.
- `./integration-skip-gate-markers.md` — live-session tests gated by a file-existence fixture that pytest.skips WITH reason, strict-marker taxonomy, and selector rot recorded as unconditional documented skips; unit plane stays headless-green offline.
- `./model-dump-contract-validator-placement.md` — validate ONLY canonical-URL identity fields at construction (Job '/jobs', Company '/company/'), keep URN-keyed Post validator-free, and expose to_dict/to_json as total model_dump delegates.
- `./job-search-honest-empty-flow.md` — no-results returns [] through the success path after a first-card wait gate; scroll budget sized per surface (3 vs feeds' 10); limit check inside the collection loop.
- `./company-about-section-probe.md` — find label-less About text by scanning sections for 'About us' within the first 50 chars then taking the first <p>; descriptive failures degrade to None, always-formatted names degrade to sentinel values.
- `./cdp-active-lifecycle-fastpage.md` — force every scraped tab out of background throttling at CREATION time: recycle the initial about:blank tab, setBypassCSP(true), then one CDP session sends Page.enable + Page.setWebLifecycleState 'active' before any navigation.
- `./warm-browser-service-contract.md` — setup-once/run-many lifecycle: keepAlive trades memory for warm sessions, each run gets its own hardened page so Promise.all fan-out shares ONE browser, any failure closes everything, scraperSessionId tags concurrent logs.
- `./networkidle2-readiness-gate.md` — waitUntil is a data-completeness invariant: domcontentloaded silently yields null-filled sections on late-hydrating profiles; networkidle2 (≤2 inflight) is the minimum gate before scroll/expand/parse.
- `./timezone-pinned-time-tests.md` — pin moment.tz.setDefault INSIDE the test module before any NOW-dependent assertion ('Present'→now formatting, inclusive day diffs) or CI offsets flake the suite.
- `./constructor-option-gate.md` — validate user options BEFORE merging onto readonly defaults; wrong types throw synchronously with byte-stable 'Error during setup.' messages that tests assert verbatim.
- `./warm-scraper-http-recovery-ladder.md` — module-scope warm scraper behind one Express GET: setup-before-listen + env-fed `li_at`; documents the terminal failure chain (Express 4 drops async-handler rejections; run()'s catch tears down THEN rethrows ⇒ unhandledRejection or browser=null zombie) and the try/catch→status→re-setup ladder a porter must add.
- `./single-typed-error-budget.md` — exactly ONE typed error repo-wide (SessionExpired: name stamp, captureStackTrace frame elision, message-as-remediation-doc); error-type budget = number of distinct caller reactions; everything else stays plain sync throws or identity-preserving rethrows after cleanup.
- `./loader-latch-navigation-kernel.md` — fire-and-forget swallowed `gotoUrl` behind a two-phase spinner latch (appear → `.fade-load`, unbounded second wait) and ±1 s jitter on EVERY sleep; navigation errors can never crash an action because the latch layer owns readiness (linvo).
- `./querystring-repagination-router.md` — re-page saved search URLs by rewriting ONLY the query component (canonical page-1 deletion, separator-preserving `#` vs `?` re-stringify) behind a >100-page empty-success wall, routing `/sales/` vs feed and bypassing with precomputed CSV urls (linvo).
- `./endorse-unchecked-popover-close.md` — bulk endorse via unchecked-only ID collection then click-by-id, closing each artdeco hoverable before the next click; zero unchecked = idempotent success; identity envelope returned even on error (linvo).
- `./profile-views-private-mode-schema.md` — drain the who-viewed list with an interval scroller THEN one-pass evaluate where anonymous viewers degrade to Private Mode count rows so row count always equals card count (linvo).
- `./accepted-connections-snapshot.md` — one-viewport accepted-invite export gated by a 10 s selector wait; trailing slashes stripped INSIDE `page.evaluate`; miss yields `[]`, never a throw (linvo).
- `./conversation-queue-fanout-readback.md` — batch message queue across threads: about:blank SPA reset per conversation, count-delta send proof per message, and a return value produced by the READ path — a writer never certifies its own writes (linvo).
- `./li-at-mint-login.md` — form login as token minting: typed delays, success latch on the global search typeahead, `{user, token: li_at}` out, exactly ONE typed INVALID_CREDENTIALS failure carrying a scheduler route (linvo).
- `./wheel-bracketed-topcard-reader.md` — read nine top-card identity fields through per-era selector fallback chains inside ONE evaluate, bracketed by balanced real-wheel down/up steps that wake lazy media then restore position; fields degrade independently to undefined (linvo).
- `./conversation-switch-gate-walk.md` — harvest conversations one-by-one behind a name-inequality switch gate ("topcard ≠ previous thread") + loader-hidden history latch; shift-consume recursion omits failed threads but always advances; contrast trap: a waitForFunction returning a mapped ARRAY is truthy even empty (linvo).
- `./sentiment-transcript-twins.md` — speaker-segmented transcript reduce exists twice: guarded-append twin (port this) vs unguarded-replace twin that can emit literal "undefined"; header event opens a row, body events extend it; AFINN sentiment per message with a four-point divergence table (linvo).
- `./mouse-glide-wheel-hover.md` — one inherited primitive: measure the element's rect, glide a ghost cursor to its bottom edge at random x, wheel by ITS OWN top+offset, optional padded hover; halves fail independently and silently (linvo).
- `./sales-nav-warmup-projection.md` — warm up the Sales Nav shell first (index→/sales/home redirect latch), then block on THE response via a dual JSON/HTML predicate whose HTML arm demands SIX co-occurring markers; project fs_salesProfile rows off-DOM with a 2500-result clamp (linvo).
- `./post-return-stranded-dom-walk.md` — executability census: both scraper services return from their response planes early, leaving ENTIRE DOM-walk halves (wheel bursts, card relocation, pagination reads, workOnResults/scrollTo/elements) unreachable below the return — verify reachability before citing or porting (linvo).
- `./thread-href-connectivity-oracle.md` — prove connectivity by scanning for any `/messaging/thread` href on the visited profile; miss throws the typed not-connected error, hit hands the found thread URL to the delegated sender while the canonical profile URL stays in info.url; contact-supplied name overrides scraped name in BOTH identity slots (linvo).
- `./url-queue-file-roundtrip.md` — per-run search-URL plan file: 'w'-truncate then generate INSIDE the open handle (mid-crash = partial queue faithfully consumed), exists-guarded makedirs, honest-empty reader returning [] behind a loud operator message.
- `./chromedriver-explicit-path-relaunch.md` — WinError-193 repair ladder: rebuild the webdriver-manager install path as dir+chromedriver.exe explicitly, warn-gated fallback re-runs the SAME manager (repair ladder, not discovery ladder — contrast path-first-browser-binary-discovery).
- `./topcard-button-none-sentinel.md` — Easy Apply probed via ONE container-scoped XPath (jobs-apply-button--top-card class does the scoping), None consumed by identity check — but the else-branch books every buttonless job as "Already applied" (conflation trap); dead selector constants rot beside live inline literals.
- `./chrome-profile-path-split-normalization.md` — one cross-platform profile path → normalize BOTH separators onto os.sep, rfind-split into --user-data-dir=<parent> + --profile-directory=<basename>; the no-separator branch emits an EMPTY user-data-dir (live-executed trap).
- `./pipe-row-three-island-extraction.md` — ledger rows from fragile DOM reads: three independent try-islands degrade fields to "" (ragged-tail rows), location chips pre-pipe themselves, and middot→pipe folding collides content WITH the delimiter (column-shift trap).
- `./paging-base-url-accumulation.md` — page loop appends `&start=` onto the SAME binding it iterates: offsets accumulate (`&start=0&start=25&start=50`); correctness rides on undocumented duplicate-key semantics — always rebuild paging URLs from an immutable base.

- `./gspread-service-account-bootstrap.md` — script-relative `__file__`-anchored service-account key with a fail-fast existence gate BEFORE browser startup; authorize once, `open_by_key` lazily per destination.
- `./sheets-header-retrofit-guard.md` — header retrofit trichotomy on a shared sheet: empty→append, unheaded-nonempty→insert-at-1 (push down), headed-by-sentinel→no-op; existing rows are never overwritten.
- `./sheet-switch-batch-reset.md` — operator rotates output destination every 2 pages: validated prompt cadence, reopen ONLY on identity change, batch accumulators reset inside the same switch branch.
- `./in-sheet-page-set-marker.md` — distinct sorted page-set rendered as a 3-row separator INSIDE the data sheet: a human-resumable cursor that cannot drift from the rows it describes (+ dead-counter warning).
- `./cdp-attach-run-shell-ownership.md` — attach-mode run shell over `connect_over_cdp`: manual `.start()`, adopt `contexts[0]`/`pages[0]`, never close what another process launched; interrupt/fault collapse into a finally summary.
- `./utf8-stdout-guard.md` — reconfigure stdout to utf-8 at import time so emoji status vocabularies survive cp1252 consoles (behavioral RED/GREEN executed).
- `./bounded-element-read-budget.md` — every per-element DOM read in a Sales Nav row carries timeout=1000 with `or ""` coercion while page gates stay 10–20 s: one bad card costs ≤~5 s, never a default-timeout stall — the latency twin of the per-row exception island.
- `./panel-bot-subprocess-supervisor.md` — local web panel owns the bot child process: single-flight lock, poll-based liveness that self-cleans tracking+PID files, detached process-group spawn with TERM→wait(5s)→KILL escalation (Windows taskkill /F /T split), truncate-per-run log + byte-offset tail API whose cursor restarts when truncation is detected.
- `./pristine-defaults-snapshot-reload.md` — capture untouched config defaults under import-time overrides: stub the JSON-overlay loader to return {}, importlib.reload each config module, harvest schema-declared keys, restore the loader in finally; effective views are deepcopy(defaults) re-overlaid from a fresh disk read every call.
- `./stuck-modal-loop-breaker.md` — bound the Easy Apply Next-page loop with a ≥15 counter that either resets after a human-takeover pause or screenshots+raises "stuck"; a three-value errored vocabulary ("", "nose", "stuck") governs whether the finally-block may pause, whether submit failure escalates, and when the job discards cleanly.
- `./search-facet-cycle-rotation.md` — infinite re-runs stay fresh by rotating the date-posted window and toggling Most relevant↔Most recent AROUND each cycle (plus per-run term shuffle); documents that stop_date_cycle_at_24hr's clamp comparison is unreachable so it jumps straight to Past 24 hours instead of advancing one step per cycle.
- `./csv-cell-safety-pair.md` — CSV ledger cell safety is a PAIR: raise the interpreter cap once at import (csv.field_size_limit(1000000)) and funnel every cell through a never-raising stringifier capped well below it with a visible "...[TRUNCATED]" marker; header-once via tell()==0 on append handles.
- `./relative-posted-time-normalization.md` — "3 weeks ago (Reposted)" → sortable datetime: strip decorations into flags first, one case-insensitive regex over a declarative unit table (month≈30d, year≈365d), None-on-unparseable recorded as "Unknown" rather than guessed.
- `./blacklist-goodword-shortcircuit.md` — employer red-lines enforced whitelist-FIRST (one good word vetoes the blocklist scan), verdicts remembered in session-scoped rejected/blacklisted sets consulted at card level, and the raised ValueError carries the full about-company text plus offending word as its evidence payload.
- `./loud-log-mirror-alert-kernel.md` — console lines mirror to logs/log.txt; a dead target raises a blocking GUI alert guarded by from_critical against recursion — with executed failure-mode proof that a deleted logs/ dir blocks the process (and the pytest suite) inside tkinter until the startup make_directories contract is honored.
- `./job-detail-field-islands.md` — job TITLE / company NAME vs company URL / DESCRIPTION getters under one selector family with DIFFERENT rules per role: text-quality scan skips logo-only links for names while .first-href query-strip→absolutize carries identity; "About the job" heading-anchored ancestor::article climb beats generic article (linkedin_scraper).
- `./exception-taxonomy-wiring.md` — single-root 7-class error hierarchy where each subsystem raises ONE leaf (auth→AuthenticationError everywhere; throttle→RateLimitError with TIERED suggested_wait_time 3600/1800/300) and the docstring-promised ProfileNotFoundError is never raised — documented contracts can lie (linkedin_scraper).
- `./profile-header-extraction.md` — #OPEN_TO_WORK rides the avatar img title attribute, not a visible badge: attribute-carried state flags with conservative False defaults and case-insensitive substring predicates survive redesigns that keep moving visible elements (linkedin_scraper).
- `./scrape-orchestration-template.md` — every page scraper shares the five-beat ceremony on_start→navigate→throttle-gate→islanded-getters→construct-once; missing NAME degrades to sentinel strings while everything else degrades to None — identity needs a label, content does not (linkedin_scraper).

## Capsule map
- **Session & transport** — `voyager-api-client`, `cookie-session-persistence`, `browser-login`, `browser-lifecycle`, `cookie-session-bootstrap`, `li-at-mint-login`: authenticate once, persist loudly, classify every failure state, own the browser stack safely — including the no-login-scripting path: plant a hand-extracted `li_at` cookie and verify liveness with a `/login` redirect-probe that throws typed SessionExpired, plus linvo's MINT side: form login returning `li_at` as a portable token with one typed credential failure; `md5-pickle-cookie-jar` adds the Selenium flavor — md5(email)-keyed pickle jar with delete-before-replay restore and a build-unstable ember-id liveness probe.
- **Browser ops (Puppeteer flavor)** — `puppeteer-flag-stack`, `cdp-active-lifecycle-fastpage`, `networkidle2-readiness-gate`, `request-interception-budget`, `warm-browser-service-contract`, `constructor-option-gate`, `zombie-browser-teardown`: container-survivable launch floor (and which flags are cargo cult), per-page CDP forcing of the active web lifecycle plus initial-tab recycle, idle-network navigation readiness so late-hydrating sections never parse as empty, two-tier TYPE/HOST abort budget that never blocks stylesheets, the setup-once/run-many keepAlive service triad with per-run page isolation, validate-before-merge option gates with byte-tested messages, catch-path teardown with treeKill(SIGKILL) so no orphaned Chrome survives the process; `warm-scraper-http-recovery-ladder` rides the same facade behind HTTP — setup-before-listen, one thin async route, and the terminal failure chain (Express 4 drops async-handler rejections, run()'s catch tears down then rethrows ⇒ unhandledRejection crash or browser=null zombie) with the try/catch→status→re-setup ladder a porter must add.
- **CDP browser kernel (zendriver, patterns only)** — `idle-breathe-readiness`, `lazy-domain-enablement`, `headless-expert-prep-latches`, `cdp-transaction-generator-protocol`, `websocket-dispatch-ledger`, `connection-class-armor-send-pipeline`, `cloudflare-shadow-checkbox-solver`, `browser-launch-flag-assembly`, `pattern-filtered-cookie-jar`, `react-controlled-input-fix`, `stale-node-retry-ladder`, `humanized-scroll-gesture`, `cdp-keyboard-event-machinery`, `network-expectation-context`, `browser-lifecycle-teardown`, `text-find-best-match-ladder`, `js-dumps-two-strategy-ladder`, `contra-dict-attr-mapping`, `quads-center-mouse-kit`, `page-capture-download-gate`, `evaluate-dual-return-modes`: the general CDP-automation primitives under every browser scraper — sliding-window quiet detection, lazy domain enablement with a manual-intent lane, one-shot headless/expert stealth latches, the single-yield generator→future transaction protocol over one websocket, the pop-before-complete dispatch ledger that multiplexes concurrent commands and tolerates reserved ids, metaclass class-state armor plus latched send-path preps, the CF shadow-DOM checkbox solver, typed flag assembly, regex-subset cookie jar, React-controlled-input fix, bounded stale-node recovery, humanized scroll, the full keyboard-event machinery, network expectation/interception contexts, graceful-then-kill lifecycle, server-side text-find with best-match-by-length and find-as-wait semantics, hostile-JS object export via the two-strategy dump ladder, the `__dict__`-aliasing attribute-dict kernel, content-quads geometry with real-coordinate mouse actions, the page-capture/download-behavior skeleton, and the dual-mode evaluate contract (by-value vs deep serialization with its dead tuple hint). AGPL-3.0 — patterns only, never verbatim.
- **Profile parse discipline (Puppeteer)** — `raw-clean-two-phase-parse`, `expand-then-parse`, `duration-with-present`, `geo-string-classifier`, `timezone-pinned-time-tests`: dumb browser / smart Node split (evaluate emits nullable raw text, pure functions parse), expanders-then-clampers click ladder with settle waits between families, 'Present'→now + inclusive day diff date kernel, comma-split location resolved by part-count plus ISO predicates, and a test-local timezone pin keeping NOW-anchored assertions CI-reproducible.
- **Evasion & throttling** — `rate-limit-detection`, `browser-fingerprint-stealth`, `humanization-scroll`, `throttle-classification-ladder`, `declarative-delay-validation`: detect-and-backoff beats retry-harder; fingerprint via flags/profiles; every scroll terminates and bounds itself; classify WHICH limit hit into typed pause durations for account schedulers; per-action `{min,max}` delay configs validated at load with cross-field max≥min rules and human-readable action labels.
- **Navigation & readiness (linvo)** — `loader-latch-navigation-kernel`, `querystring-repagination-router`: the kernel under every linvo action — fire-and-forget swallowed navigation behind a two-phase loader latch with jitter-by-construction pacing, and query-only repagination of stored search URLs with canonical page-1 deletion and a hard page wall.
- **Data acquisition** — `sales-nav-pagination`, `voyager-pagination`, `response-interception`, `search-url-builder`, `feed-post-urn-scan`, `voyager-graphql-queryid`, `voyager-search-facet-encoding`, `embedded-json-search-parse`, `sales-nav-lead-identity`: page DOM or API until a real stop condition; prefer intercepting structured payloads over parsing cards — including the raw response buffer's embedded `<code>` JSON when no API client exists; filters as URLs OR as typed interfaces OR as API facet grammar; URN-scan identity for feeds; GraphQL queryId components; dual-collection feed sort (order + content rejoined client-side); Sales Nav composite-URN → lead-href → raw-href identity ladder to canonical `/in/<id>`; search hits rehydrated by URN joins — single-hop for people/companies, two-hop hit→posting→company for jobs (`search-blended-hit-joins`); `displayed-count-page-budget` turns the DOM "N jobs" label into a capped page count with its uncapped bare-int trap; analytics/network DOM harvests keep the same stop-condition honesty — profile-views drains lazy lists then maps ONCE with schema-total Private Mode rows (`profile-views-private-mode-schema`), accepted-connections snapshots one viewport with in-evaluate slash normalization and `[]`-on-miss (`accepted-connections-snapshot`); EasyApplyJobsBot's plan-file and paging honesty: `url-queue-file-roundtrip` regenerates a 'w'-truncated URL plan per run behind an honest-empty reader, and `paging-base-url-accumulation` documents why appending `&start=` onto the iterated binding (offsets piling into duplicate query keys) must become rebuild-from-immutable-base; linvo's Sales Nav plane completes the group — `sales-nav-warmup-projection` warms the shell (index→/sales/home redirect latch) before blocking on THE response via a dual JSON/HTML predicate whose HTML arm demands SIX co-occurring markers, then projects fs_salesProfile rows off-DOM under a 2500 clamp, and `post-return-stranded-dom-walk` is the executability census proving both linvo scraper services' DOM-walk halves sit unreachable below their response-plane returns (verify reachability before citing or porting).
- **API pagination primitives** — `created-before-scroller`, `skip-limit-scroller`, `search-scroller-twins`: two abstract scroller state machines — timestamp-cursor paging with a LIFO scroll-back stack (+1000 seed, empty-page no-op), and offset/limit paging with a sticky end-of-results latch — plus the subclass discipline: entity scrollers add ONLY a typed fetch closure + config, never their own cursor logic.
- **Profile deep-dive** — `profile-schema`, `profile-section-expansion`, `nested-position-parsing`, `tabbed-overlay-section-walking`, `overlay-pushstate-trigger`: one identity, three encodings, validated canonical URL; main-page-first-then-details fallback; structural nested detection; click-then-read tablist + overlay walk OR route-state overlay summon without clicks; wheel-bracketed top-card reading where every field resolves through dual-era fallback chains and degrades independently (`wheel-bracketed-topcard-reader`); recruiter state flags read from machine-readable attribute channels with conservative defaults (`profile-header-extraction`).
- **Action services & humanized interaction** — `linvo-service-taxonomy`, `ghost-cursor-click-ladder`, `connect-outreach-ladder`, `feed-engagement-caps`, `linkedin-url-canonicalization`, `visit-personalize-delegate`, `visit-resolve-identity`, `like-with-recovery-envelope`: every action behind ONE `process(page, cdp, data)` interface with typed schedulable errors; clicking degrades ghost-cursor → coordinate mouse event → DOM click, never the reverse; invites guard pending-state before mutating and engagement caps volume at collection time with traction-gated comments; all external links pass one total canonicalizer (shape-gate → parse → path-only) whose failure value is `''`; composites visit-personalize-guard then delegate to leaf services; a bare visit RESOLVES identity from the post-settle redirect href; like actions return one uniform identity envelope on success/empty/error with only the click region guarded; endorsement collects UNCHECKED buttons once and closes each hoverable popover between clicks so bulk actions can't block themselves (`endorse-unchecked-popover-close`); `mouse-glide-wheel-hover` is the inherited pre-click primitive under those services — measure the element's rect, glide a ghost cursor to its bottom edge at random x, wheel by ITS OWN top+offset, optional padded hover, halves failing independently and silently.
- **Messaging & outreach** — `message-send-guard-chain`, `inbox-harvest-shuffle`, `message-template-substitution`, `messaging-read-path-scrollers`: sends are policy-gated (reply state, fuzzy duplicates), verified by DOM delta, templated through a sanitized substituter; inbox enumeration is budgeted and shuffled because visit ORDER is a bot signature; the read side pages conversations AND per-conversation messages with one timestamp-cursor machine parameterized per entity; the batch QUEUE writer resets SPA state per thread, proves sends by list-count delta, and returns the READER's harvest instead of its own claim (`conversation-queue-fanout-readback`); linvo pass-2 read-side depth: `conversation-switch-gate-walk` walks shuffled threads behind a name-inequality switch gate (topcard ≠ previous thread) with shift-consume recursion that omits failed reads but always advances, `sentiment-transcript-twins` pairs the two speaker-segmented transcript reduces (port the guarded-append twin; the replace twin can emit literal "undefined") with AFINN scoring per message, and `thread-href-connectivity-oracle` gates sends on existence of any `/messaging/thread` href — miss throws typed not-connected, hit hands the found URL to the delegated sender.
- **Data contracts** — `profile-schema`: most-recent-first positional semantics.
- **Easy Apply automation** — `easy-apply-modal`, `form-question-answering`, `job-run-orchestration`, `selenium-click-finder-ladder`, `config-validation-ladder`, `langgraph-form-answer-pipeline`, `easy-apply-button-sentinel`: button-priority modal walker, label-routed answers with safe fallbacks, crash-isolated run loop with caps and ledgers, tolerant Selenium click helpers, fail-fast config validation; the AI answer layer compiles ONE LangGraph with conditional select/text routing, exact→ci→substring→passthrough option snapping, and a structured-output JSON fallback; absence of the Easy Apply button is a single identity-checked sentinel that also writes the audit trail's attempted flag. The EasyApplyJobsBot deep pass adds the per-job pipeline internals: `stale-proof-two-pass-harvest`, `progress-percent-step-budget`, `blacklist-inline-annotation-gate`, `phone-field-selector-ladder`, `preferred-resume-picker`; its pass-3 deepening adds `topcard-button-none-sentinel` (container-scoped probe whose None else-branch conflates already-applied with external/broken pages), `pipe-row-three-island-extraction` (per-field degrade-to-empty row assembly where middot→pipe folding collides content with the delimiter), and `chromedriver-explicit-path-relaunch` (dir+exe path repair whose fallback re-runs the same manager).
- **Control panel & config surface** — `schema-driven-config-panel`, `config-override-noop-layer`, `schema-typed-coercion-ladder`, `applied-jobs-csv-json-remap`, `chrome-profile-degradation-ladder`, `panel-bot-subprocess-supervisor`, `pristine-defaults-snapshot-reload`: a browser settings UI whose schema is the single source of truth for rendering AND validation; a JSON overlay that can never inject names or break defaults; coercion with the bool-before-number trap handled; a machine CSV ledger projected to JSON with both consumers preserved; browser sessions that degrade to guest profiles instead of dying (with EasyApplyJobsBot's `chrome-profile-path-split-normalization` owning the cross-platform parent/basename split that feeds such attaches — including its empty-user-data-dir bare-name branch); the panel also owns the bot as a subprocess (single-flight start, whole-process-group TERM→KILL ladder, byte-offset log tailing with truncation restart) and shows honest defaults via a loader-swap+reload snapshot.
- **Safety & self-limiting** — `dry-run-run-cap-gates`: dry-run branches at every irreversible verb and a post-increment cap stops the run before daily limits are approached; both gates keep dry vs live logs comparable through one outcome channel.
- **Answering intelligence** — `qa-memory-ladder`: persistent Q&A memory overrides a keyword ladder which overrides nothing — unknowns surface loudly for human takeover and are remembered on the next run; `string-outcome-channel`: one closed string vocabulary per decision drives logs + ledger + counters; `packaged-keyseq-devtools-latch`: packaged desktop apps hide debug/recovery surfaces behind typed multi-keyword sequences with self-disarming prefix latches while dev builds stay instant.
- **Sales Nav acquisition (DOM lane)** — `sales-nav-lead-identity`, `na-preserving-row-extraction`: composite-URN → lead-href → raw-href identity ladder to canonical `/in/<id>`; schema-total rows where failures degrade values ("NA") instead of dropping prospects; enabled/disabled/absent Next trichotomy ends pagination honestly.
- **Voyager mutations** — `voyager-mutation-endpoints`: POST-vs-GET split, inverted error-state bool, `action`+`decorationId` mutation shape.
- **Session & transport (API auth)** — `voyager-password-auth-metadata`: seed-JSESSIONID-then-POST, `login_result == "PASS"` gate, android-header auth split, meta-tag metadata bootstrap.
- **Easy Apply automation (config lane)** — `session-summary-ledger-append`: a human-readable end-of-run summary coexisting with the machine ledger in the SAME dated file — banner-delimited append-mode writes vs rewrite-on-strip row history.
- **Resilient helpers** — `playwright-resilient-helpers`: severity-split wait (self-diagnosing) vs safe-default extract/close; poll-based manual-login wait; `url-roundtrip-extraction`: normalize-then-dedupe ordering for canonical URL harvesting from DOM listings.
- **Redesign-proof extraction (joeyism)** — `overview-vocabulary-classifier`: when a redesign removes DOM labels, classify unlabeled chips by content vocabulary with an all-None seed dict and a zero-progress-only structural fallback.
- **State & scale** — `dedupe-applied-tracking`, `scraper-base-callbacks`, `queue-cache-decorators`, `chunked-export-iterator`, `voyager-dual-collection-sort`, `durable-named-cursor-continuation`, `channel-fetcher-contract`, `ledger-contrast-csv-vs-summary`: cross-run dedupe sets, observable thin scrapers, serialized account actions, streamed bounded exports, feed order+content stitched by URN-collision replay, composite (timestamp, id) durable cursors, channel fetchers, row-vs-summary ledger contrast.
- **Domain & data plumbing (lh-basis/open-linkedin-api, patterns only)** — `aggregate-versioned-event-kernel`, `streaming-csv-import-gate`, `legacy-template-variant-migration`, `derived-column-descriptors`, `working-hours-interval-algebra`: the production core around the scrapers — event-sourced campaign/account state with snapshot hydration, memory-bounded CSV imports gated by header checksums before any insert, legacy template formats migrated to variant trees (reversed then/else indexing documented), derived storage flags kept consistent via descriptor converters, and schedule algebra (scope-nested intervals + deterministic daily jitter) deciding when an account may act; plus the production persistence kernel: busy-retry transactions with closed retryable code lists and platform-split BEGIN dialects (`busy-retry-transaction-kernel`), ordered phase-pipeline schema init/reset where data resets preserve the version row (`sqlite-init-phase-pipeline`), and campaign-context CQRS composition with rebound transaction providers and guards-as-query-handlers (`cqrs-context-composition`).
- **Private-API client architecture (linkedin-private-api)** — `included-envelope-hydration`, `voyager-star-pointer-joins`, `scroller-factory-repositories`, `collection-response-envelope`, `client-field-precedence-wiring`: the kernel ideas for headless API scraping — `$type`-filter→transform→keyBy hydration of normalized-JSON envelopes typed by ONE `LinkedInCollectionResponse<T,I,M>` generic (elements point, included defines), the pointer-side grammar that hydration consumes (`*field` URN references each with its own unwrap — messagingMember tuple-split, scheme-stripped miniProfile ids, direction-dependent member fields), repositories as factories returning live scrollers around bound fetch closures, convenience endpoints as filter rewrites over one wire path, and composite-client field wiring whose children must defer parent reads to call time — plus the ONE-instance dual-return HTTP kernel every request rides (`full-response-transport-kernel`: slash-less base-relative endpoints, `fullResponse:true` header escape hatch used exactly twice for auth, wholesale `defaults.headers` persona swap, zero built-in error handling) and the asymmetric invitation mailbox reads (`invitation-mailbox-read-grammar`: selector-style received view vs type-filter sent V2 view, restli start/count rename, verbatim CONNECTION token).
- **API pagination primitives** — `created-before-scroller`, `skip-limit-scroller`, `search-scroller-twins`: two abstract scroller state machines — timestamp-cursor paging with a LIFO scroll-back stack (+1000 seed, empty-page no-op), and offset/limit paging with a sticky end-of-results latch, two-limit rewind, and counter-based "no previous page" answer — plus the subclass discipline: entity scrollers add ONLY a typed fetch closure + config, never their own cursor logic.
- **API session & headers** — `dual-persona-auth-headers`, `sessions-json-cache`, `cookie-jar-phase-taxonomy`: app-fingerprint headers authenticate while web-fingerprint headers call the API (csrf = raw JSESSIONID); multi-account username-keyed JSON jar with read-through-create and spread-merge writes; cookie names document auth phases (anonymous required-six vs authenticated optional superset) and set-cookie arrays parse through a first-key-wins kernel.
- **API query grammar** — `voyager-filter-grammar-serializer`, `decoration-id-response-shaping`, `typed-search-filter-contracts`: structured filters encoded as `List(key->v1|v2)` with identity final-encode; versioned decorationIds select server projections — parse by `$type` so bumps degrade instead of crash; the same protocol surfaced as typed filter interfaces with letter/digit wire enums and Omit-based scope locking.
- **API writes** — `messaging-write-path-duality`, `send-then-verify-invitation`, `trackingid-bytes-twins`: branch-on-context-id messaging payloads under one path namespace; body-less writes materialized by newest-row read-back with direction-dependent join fields; client-minted 16-byte trackingIds ride mutation payloads in two byte-compatible encodings (charString vs base64) chosen per transport.
- **Identity & enrichment** — `own-profile-bootstrap`: me→miniProfile→full-profile chain with null-on-miss; single-envelope company join by headline substring crosswalk.
- **Domain-model kernel (lh-basis, patterns only)** — `source-carrying-model-serialization`, `live-collection-views`, `structural-duck-type-guards`: hydrated models re-attach live data sources through a WeakMap ambient context (serialize throws if source-less); collections are lazy query facades where size/iteration/indexing are all live queries; deserialized shapes are admitted through presence+method duck-type guards that accept entity-or-id unions and never throw.
- **Easy Apply automation** — `easy-apply-modal`, `form-question-answering`, `job-run-orchestration`, `selenium-click-finder-ladder`, `config-validation-ladder`: button-priority modal walker, label-routed answers with safe fallbacks, crash-isolated run loop with caps and ledgers, tolerant Selenium click helpers, fail-fast config validation; the AI answer layer compiles one LangGraph with conditional select/text routing, option snapping (exact→ci→substring→passthrough), and a structured-output JSON fallback (`langgraph-form-answer-pipeline`).
- **Testability** — `testdouble-mock-harness`: replace→dynamic-import→stub-by-wire-shape→assert-parsed-output fixture ladder that pins auth headers, paging math, and envelope joins end-to-end without live sessions.
- **Easy Apply bot kernel (LinkedIn-Easy-Apply-Bot, dedicated lane)** — `visible-card-worklist-harvest`, `randomized-search-combo-walk`, `path-first-browser-binary-discovery`, `offscreen-window-scrape-mode`, `yaml-entry-gate-none-tolerant-defaults`, `lookup-exception-topology`, `browser-title-ledger-identity`: the single-file Selenium bot's previously uncited seams — post-scroll re-collection feeding a {jobId: status} work dict under a wall-clock session budget (BOTH progress traps documented: the stalled offset with no increment site AND the write-dead status channel where apply_loop's `== applied` comparison never assigns), randomized exactly-once search-pair enumeration, PATH-first binary resolution whose failures are constructor failures, geometric window hiding with maximize-on-work restore, a minimal yaml entry gate whose shape error names the exact fix, the lookup-exception topology whose one wrong-type except clause turns the login soft-fail into an uncaught constructor crash (find_elements never raises / find_element raises NoSuchElementException / only until() raises TimeoutException), and tab-title-derived ledger identity via positional `' | '` split with per-part regex normalization and its per-row IndexError degradation. Complements the four seams this repo already contributed (`easy-apply-modal`, `qa-memory-ladder`, `easy-apply-button-sentinel`, `string-outcome-channel`).
- **Job & company page extraction (joeyism/linkedin_scraper lanes)** — `job-topcard-middot-triplet`, `job-search-honest-empty-flow`, `company-about-section-probe`, `job-detail-field-islands`: one unified-top-card container split on '·' whose part index IS the field identity (location/date/applicants) with guarded vocabulary fallbacks; search flows that return [] through the success path when no cards mount and budget scrolling per surface; label-less About sections found by a heading-prefix window then first paragraph; the four judgment getters — text-quality name scan vs first-href identity URL, heading-anchored description climb.
- **Retry, models & test plane (joeyism, dedicated lane)** — `async-retry-disarm-trap`, `model-dump-contract-validator-placement`, `integration-skip-gate-markers`, `session-producer-manual-login-poll`, `auth-session-verdict-tiers`: decorator retry that only sees RAISED exceptions and the swallow-to-sentinel body that silently disarms it; pydantic validators placed ONLY on canonical-URL identity fields while URN-keyed models stay open and dumps stay total; live-session tests skip-with-reason behind a file-existence fixture under strict markers; one human login converted into the session artifact by a shared-predicate poll loop; session verdicts branch on EVIDENCE TYPE — negative redirects fail closed with the typed error, unverifiable polls proceed loudly, other causes wrap into that one type (while load_session's by-construction authenticated flag stays a write-only counterexample).
- **Error taxonomy & run ceremony (linkedin_scraper / FAC-59)** — `exception-taxonomy-wiring`, `scrape-orchestration-template`: one-root exception hierarchy with subsystem-owned leaf classes and tiered scheduler payloads on the throttle error (plus the never-raised docstring trap); the shared five-beat scrape ceremony whose only legal variation is degradation policy — sentinel strings for identity labels, None for content fields; `single-typed-error-budget` (linkedin-profile-scraper-api) holds the minimal-budget counterpoint: exactly ONE typed error where exactly ONE caller reaction exists (SessionExpired, name-stamped + captureStackTrace frame elision), everything else plain sync throws or teardown-preserving rethrows.

- **API read planes (open-linkedin-api)** — `invitation-shared-secret-lifecycle`, `job-detail-urn-plane`, `universal-name-entity-lookup`, `update-extractor-family`, `conversation-participant-resolution`, `company-search-vertical-projection`: the read-side contracts this repo adds beyond its mutation/pagination coverage — invitation answers credentialized by a server-minted sharedSecret round-tripped from the listing's view envelope; embedded-status error envelopes checked even inside HTTP 200 plus full-URN-as-path-segment addressing on dash sub-services; slug-keyed universalName lookup where schools ARE companies in one endpoint/projection; scattered update fragments assembled into ordered post rows by a last-row-or-new-row kernel with sentinel content ladder; single-thread participant lookup whose List() filter must ride raw in the URI with {} misses; and vertical search specialization double-gated by wire filter + trackingUrn kind check with null-safe flattening. Pass 2 adds the identity/me plane: `profile-me-card-variant-family` (one card template: slug URI + explicit normalized accept + status-gated {} + data unwrap), `me-cache-memoization` (lazy /me read-through on the shared Client.metadata dict; cookies-direct construction skips the auth scrape), `auxiliary-request-planes` (/li/track beacon as JSON-under-text/plain on the bare www host via base_request=True, action-verb unfollowByEntityUrn write on the API plane), `wvmp-guarded-descent` (seven-hop unguarded $type descent as the negative template → guarded-descent porting discipline), and `member-share-feed-scoping` (memberShareFeed across profileUpdatesV2 vs /feed/updates as atomic (endpoint, moduleKey, identity-key) triples).

- **Sheets destination & attach-run-shell plane (hassan, patterns only)** — `gspread-service-account-bootstrap`, `sheets-header-retrofit-guard`, `sheet-switch-batch-reset`, `in-sheet-page-set-marker`, `cdp-attach-run-shell-ownership`, `utf8-stdout-guard`, `bounded-element-read-budget`: the output-side kernel no other suite repo has — script-relative service-account auth failing fast before any browser work, header retrofit that never clobbers shared-sheet data, mid-run destination rotation with atomic batch-accounting resets, a human-resumable distinct-page-set marker co-located with the rows it describes, ownership-inverted CDP attach (adopt the managed context/page, never close the launcher's browser), the utf-8 console precondition under the emoji status vocabulary, and the two-tier wait budget whose 1 s per-element reads bound a stuck card's wall-clock cost while the row island bounds its failure scope.

- **Bot-run kernel (Auto_job_applier deep pass)** — `stuck-modal-loop-breaker`, `search-facet-cycle-rotation`, `blacklist-goodword-shortcircuit`, `csv-cell-safety-pair`, `relative-posted-time-normalization`, `loud-log-mirror-alert-kernel`: the unattended-run survival kit around job-run-orchestration — bounded modal loops with human-reset latches and an errored vocabulary gating the finally-block, facet rotation (plus its unreachable-clamp bug) keeping infinite runs fed, whitelist-first blacklist scans whose exceptions carry their own evidence and whose verdicts persist per session, paired interpreter-cap/truncation-marker CSV cell safety, declared-approximation posted-time parsing, and a log mirror that fails loudly but never recursively.

## Extending the foundation
Add one `./<seam>.md` capsule for one graph-selected, source-confirmed porting question from any suite repo (`Auto_job_applier_linkedIn`, `EasyApplyJobsBot`, `LinkedIn-Easy-Apply-Bot`, `hassan-sales-nav-profiles-scraper`, `joeyism-linkedin-scraper`/`linkedin_scraper`, `linvo-scraper`, `open-linkedin-api`, `linkedin-private-api`, `linkedin-profile-scraper-api`, `linked-helper-extract`, `lh-basis`). The retired `maximo3k-sales-nav-scraper` mirror (index dropped 2026-09) is covered by the retained capsules; do not re-query it. Cite the BEST implementation instance and name the runner-ups in the Flow/Verdict. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf. AGPL/no-license sources (lh-basis, linked-helper-extract, hassan): record patterns, never copy code.

## Provenance
Suite roots under `$REFERENCE_ROOT/linkedin/` (graph projects at top-level symlinks): Auto_job_applier_linkedIn MIT `main@0ca5550`; EasyApplyJobsBot CC-BY-NC `main@70fe7484`; LinkedIn-Easy-Apply-Bot Apache-2.0 `master@8471c58`; hassan-sales-nav NO-LICENSE file (README §License carries a bare "MIT License" mention with no license text — kept learn-only/pattern-only pending real LICENSE text; pass-25 observation) `main@e294ac09c9b9`; joeyism-linkedin-scraper GPL-3 `master@b1cdc1c` ≡ linkedin_scraper GPL-3 (identical trees); linvo-scraper ISC `main@cfbe910`; open-linkedin-api MIT `main@5feee36`; **linkedin-private-api MIT `master@e083f37`**; linkedin-profile-scraper-api MIT `master@9fc7125` (128n/213e full mode); linked-helper-extract + lh-basis NO-LICENSE extracts (learn-only; lh-basis core/models package.json declares ISC — patterns still only, dist-compiled tree; blocked-hosts.ts vendored MVPS data CC BY-NC-SA — pattern-only). Codebase Memory projects of the same names (full mode unless noted); lh-basis decorators indexed via project `lh-basis-source`; lh-basis `core/models` plane EXCLUDED from indexing by design (`not_indexed.dirs`) → source-read probes only. ext-zendriver AGPL-3.0 `main@2c6d9c7` (graph project `ext-zendriver`, root `$REFERENCE_ROOT/external/zendriver`, 4120n/28298e full mode) mined patterns-only into the CDP kernel group. Pass 24 adds citations into `linked-helper-extract-dist` (69n/137e, root `app-src/dist`) and the source-read plane `lh-basis core/local-source/dist/{helpers,DB}` — outside every project root by design, byte-exact file probes stand in.

## Full view (memory graph)
Revalidate before porting: `index_status --project <name> --verbose`, then `search_graph`/`get_code_snippet` per cited symbol. Recorded 2026-08-23 pass 1: all 10 surviving projects `ready` in full mode — open-linkedin-api 187n/524e, joeyism-linkedin-scraper 487n/1949e (≡ linkedin_scraper), linvo-scraper 239n/597e, open-linkedin-api HEAD `5feee36`, Auto_job_applier 548n/1850e HEAD `0ca5550`, EasyApplyJobsBot 184n/454e, LinkedIn-Easy-Apply-Bot 90n/189e, hassan 47n/56e, lh-basis 1601n/2833e (+`lh-basis-source` 3218n/11445e, +`lh-basis-migrations` 1146n/2740e). Pass 2 (this run): re-entered joeyism `person.py`/`company_posts.py`, open-linkedin-api `linkedin.py` tail (:451–1787), Auto_job_applier `clickers_and_finders.py`/`validator.py` — 8 new capsules (19→27). Pass 3 (this run): re-entered open-linkedin-api `linkedin.py` search encoders (:305–656) + `client.py`/`cookie_repository.py`/`settings.py`, joeyism `core/utils.py`/`core/auth.py`/`core/exceptions.py` — 3 new capsules (27→30). Pass 4 (this run): verified browser-lifecycle/scraper-base-callbacks against source (closed); mined open-linkedin-api `_get_list_feed_posts_and_list_feed_urns` + `utils/helpers.py` sort family → voyager-dual-collection-sort (30→31). `check_index_coverage` stdin-JSON on every cited path = `no_recorded_issue`+`metadata_match` EXCEPT ExportIterator.js (`freshness: missing` — outside its project root) and lh-basis cache.js under `lh-basis` (`excluded not_tracked` — lives in the Source project). Coverage caveats: most suite repos ship NO tests (each capsule states which probe is source-grounded vs test-pinned); joeyism tests are integration-gated behind a live `linkedin_session.json` fixture except model/lifecycle units. Pass 5 (this run): pins re-verified unchanged upstream (all three gate HEADs identical) → fresh-eyes sweep of the never-entered linvo core (`linkedin.abstract.service.ts` 497L whole-file, errors enum, service registry, email/visit/login services, helpers) → 4 new capsules (31→35): linvo-service-taxonomy (one-interface registry + typed schedulable errors), throttle-classification-ladder (probe-table→DELAY-with-minutes), ghost-cursor-click-ladder (ordered degradation), overlay-pushstate-trigger (route-state overlay summon + pre-capture href). Coverage on cited paths = `no_recorded_issue`+`metadata_match` EXCEPT linkedin.service.ts (`freshness: missing`, covered by direct whole-file read). Pass 6 (this run): mechanical citation-vs-inventory grep found 17 linvo files with zero citations → entered the never-entered messaging/outreach cluster whole-file (linkedin.message.service.ts 288L, linkedin.connect.service.ts 275L, linkedin.messages.from.chat.ts 241L, helpers/messages.service.ts 54L) plus linkedin.engagement.service.ts and linkedin.page.service.ts pagesTask (:78–172 embedded `<code>` buffer parse; elements() :10–76 confirmed DEAD, zero callers repo-wide) → 6 new capsules (35→41): connect-outreach-ladder, message-send-guard-chain, inbox-harvest-shuffle, embedded-json-search-parse, feed-engagement-caps, message-template-substitution. Probes: 43 source needles + 14 line-range anchors green after one quote-style fix (pass-5 smart-quote lesson re-applied); wrong-project retrieval returns 0 hits, all six linvo anchors resolve uniquely. Pass 7 (this run): mechanical citation-vs-inventory grep across the NON-linvo suite repos (EasyApplyJobsBot config.py/constants.py/additionalQuestions.yaml + LinkedIn-Easy-Apply-Bot config.yaml/qa.csv + lh-basis ActionSettings validation surface + both sales-nav scrapers) → 6 new capsules (41→47): dry-run-run-cap-gates (config.dryRun consulted at :235/:261/:477 + maxApplicationsPerRun post-increment cap :247/:270 triple-break), qa-memory-ladder (ans_question 16-branch ladder :601–653 over qa.csv append-only memory, dict hydrated at construction so memory overrides heuristics; dead `if "Yes" or "No"` branch and list.find_element crash documented as do-not-port), string-outcome-channel (apply_to_job string_easy+bool classify-once contract, EasyApplyJobsBot substring counters, per-job exception island), sales-nav-lead-identity (hassan fs_salesProfile:( split(',')[0] ladder :175–214 — row attribute BEFORE lead href BEFORE raw; committed AdsPower API key flagged as never-port), na-preserving-row-extraction (maximo3k five NA defaults + tell()==0 header probe + index re-find after scroll + Next enabled/disabled/absent trichotomy), declarative-delay-validation (lh-basis MinMax {min,max} IsMaxNotLessThanMin cross-field rule with human-label error messages, whitelist-strip transform, >20 action keys each carrying @IsValidDelay). Licenses pinned: EasyApplyJobsBot CC BY-NC-SA 4.0 (patterns only), LinkedIn-Easy-Apply-Bot Apache-2.0, maximo3k GPL-3, hassan no-license (patterns only). Pass 8 (this run): executed BOTH queued targets — lh-basis Analytics/Cursors plane mined whole (TimestampBigIntCodec composite `time|id` zero-padded cursor with loud validated decode; analyticsCursorRepo UNIQUE(li_account_id,name) opaque-position storage; EventsBatchProvider fetch→consume→commit at-least-once split; DataFetcher twins incl. fan-out MessageReplyReceived carrying parent cursor fields) → durable-named-cursor-continuation + channel-fetcher-contract; ledger contrast Auto_job_applier submitted_jobs(:802)/failed_job(:765) DictWriter tell()==0 append vs EasyApplyJobsBot writeResults(:97) daily rewrite-on-write numbered txt → ledger-contrast-csv-vs-summary (47→50). Coverage caveat: the Analytics plane sits OUTSIDE both indexed roots (`lh-basis` excludes core/local-source/dist; `lh-basis-source` roots at dist/Source) — probes are direct source paths. Concurrent-lane collision recorded: sibling drain commit 1c06388 wired 2 loader lines for these in-flight capsules mid-authoring; duplicates deduped keeping the richer lines. Pass 9 (this run, dedicated suite lane): the full-coverage assignment queued linkedin-private-api to this lane - entered it as the 11th suite repo (graph 2,389n/3,387e full mode, HEAD e083f37). Read the whole src tree (core/login/request/client, all requests/repositories/scrollers/entities, config) + test suites; mined the API-client architecture plane into 12 new capsules (50->62): included-envelope-hydration ($type-filter -> transform -> keyBy -> undefined-safe join over data/included envelopes; getProfilesFromResponse = 14 inbound callers), scroller-factory-repositories (repos return live scrollers around bound fetch closures; searchOwnConnections/searchConnectionsOf are filter rewrites network=F/connectionOf over ONE fetchPeople path), created-before-scroller (+1000 seed, LIFO pageIndexes stack, last-element cursor advance, empty-page no-op), skip-limit-scroller (hitEndOfResults latch + two-limit rewind + scrollNextCounter===1 answer; restart() latch leak recorded as quirk), dual-persona-auth-headers (iOS-app authHeaders for /uas/authenticate vs desktop requestHeaders with x-restli-protocol-version + normalized+json accept; csrf-token=raw JSESSIONID incl ajax: prefix - contrast voyager-api-client which strips), sessions-json-cache (username-keyed sessions.json read-through-create, spread-merge writes preserving sibling accounts, useCache:false opt-out, un-awaited writes noted), voyager-filter-grammar-serializer (List(key->v1|v2) grammar via custom paramsSerializer + identity final-encode + caller-side keyword escaping), decoration-id-response-shaping (FullProfileWithEntities-35 / WebJobSearchHitLite-14 versioned projections; parse-by-$type degrades on bumps), messaging-write-path-duality (branch-on-conversationId: /events+originToken body vs conversations+recipients envelope; typed MessageCreate key verbatim), send-then-verify-invitation (POST growth/normInvitations then fetchSent(limit:1)[0]; *toMember/*fromMember direction-dependent join keyed by RAW entityUrn), own-profile-bootstrap (/me->miniProfile->getProfile delegation, null-on-miss, company join by headline.includes(name) heuristic), testdouble-mock-harness (replace fs/promises -> dynamic-import Client -> stub by exact URL+params -> assert parsed output; factories emit valid $type-tagged envelopes). Probes: 46-source-needle battery green first run; direct tests pinned per capsule (login.spec header merge + zero-call cache hits, search spec paging matrix start=0/10/20 + skip=100 override, message spec scrollBack LIFO :200-259, invitation spec read-back :580-660); check_index_coverage stdin-JSON on cited paths = no_recorded_issue + metadata_match. Suite now 62 refs across 11 repos - ALL assigned repos have foundations. Squeezed-to-last-drop for cycle. Pass 10 (this run, dedicated suite lane): pins re-verified UNCHANGED on all git repos → mandated fresh citation-vs-inventory grep rerun at FILE granularity over all 12 suite repos against leaf+62 capsules → triaged signal from dist/re-export noise → 10 new capsules (62->72): Auto_job_applier control-panel plane whole-file (config_schema.py SCHEMA/_f/valid_keys + config/_overrides.py + app.py _coerce/COLUMN_MAPPING + open_chrome.py createChromeSession) -> schema-driven-config-panel + config-override-noop-layer + schema-typed-coercion-ladder (bool-guard-BEFORE-number: Python True is an int) + applied-jobs-csv-json-remap + chrome-profile-degradation-ladder (real profile -> temp/guest retry ladder); linkedin-private-api messaging READ path (conversation/message scrollers override fieldName lastActivityAt vs createdAt; invitation.scroller SkipLimit twin; conversation.request q=participants + keyVersion LEGACY_INBOX) -> messaging-read-path-scrollers; lh-basis core/models kernel (DeserializationContext WeakMap singleton + SerializableWithSource Exclude/Expose/Transform source property + SuperJSONTransform throw-on-sourceless serialize + serializableModelsRegistry name-keyed ctor map + MessagesCollection live lazy views w/ prev-id chained asyncIterator) -> source-carrying-model-serialization + live-collection-views (plane EXCLUDED from graph by design - source-read probes only); linvo create.linkedin.url.ts TOTAL canonicalizer + linkedin.message.with.view.ts composite -> linkedin-url-canonicalization + visit-personalize-delegate (/messaging/thread href probe = relationship precondition). Direct probes: test_config_overrides.py 7 tests + test_app_integration.py coercion/reject/remap matrix; pa message/conversation spec scrollBack blocks; coverage no_recorded_issue on all 5 cited pa paths. OMITTED-WITH-REASON this pass: joeyism/linkedin_scraper scrapers/job_search.py + samples/* (pure composition of search-url-builder + scraper-base-callbacks + trivial href-clean), setup.py (packaging), Auto_job_applier __deprecated__/resume_generator + modules/resumes/extractor (dead product code), unfollow_companies.js (moveAndClick-loop family), open_chrome.py driver-download plumbing (covered by ladder capsule), linvo accepted.connection.request.service (scrapeProfile = captured idioms), lh-basis dist re-export stubs (~900 of 1,096 uncited files are 6-line barrels or compiled twins of mined planes), OwnerModelsRepo ActualAt merge strategy (minified single-line dist, below bar). Suite now 72 refs / 12 repos incl. linkedin_scraper mirror. Pass 11 (this run, dedicated suite lane): all pins re-verified UNCHANGED → upgraded the sweep to SYMBOL granularity (mcp-lane lesson: file-level checks treat whole files as covered when one symbol inside was cited). Genuinely never-cited symbols found and mined whole-file into 7 new capsules (72→79): linkedin-private-api search plane — SearchRepository.fetchJobs (:192-235) two-hop URN join hit→jobPosting→company vs single-hop fetchPeople/fetchCompanies targetUrn joins + SearchRequest.searchBlended/searchJobs endpoint split (search/blended q=all TYPEAHEAD_ESCAPE_HATCH vs search/hits q=jserpFilters) → search-blended-hit-joins; PeopleSearchFilters/JobSearchFilters interfaces + letter/digit wire enums (f_E-style '1'..'6', job-type F/C/P/T/I/V/O, network F/S/O) + Omit-based scope locking on searchOwnConnections/searchConnectionsOf → typed-search-filter-contracts; PeopleSearchScroller/JobSearchScroller/CompanySearchScroller twins (subclass = typed closure + config only, zero paging logic of their own) → search-scroller-twins. Auto_job_applier AI answer layer — modules/ai/connections.py whole (298L: _resolve_provider openai-compatible collapse via base_url, _msg_text content-block handling, create_ai_client None-on-off-or-fail, extract_skills with_structured_output→JSON-parse fallback, _build_answer_graph LangGraph build_prompt→generate→route→format_text|select_option with exact→ci→substring→passthrough option snapping, answer_question None-safe "" failure) + prompts.py + test_ai_connections.py (13 stub-model tests) → langgraph-form-answer-pipeline (pass-10's "dead product code" ruling REFUTED at current HEAD — live layer behind runAiBot.py). EasyApplyJobsBot printSessionSummary (:125-161) append-mode banner-delimited summary coexisting with writeResults rewrite-on-strip ledger in the SAME dated file → session-summary-ledger-append. lh-basis public-methods models guard kit (objects.js presence/method predicates, dbItem positive-id gate, isIPerson 30-prop+33-method contract, entity-or-id union acceptance; patterns only, plane outside indexed roots → source-read probes) → structural-duck-type-guards. joeyism job_search._extract_job_urls (:103-145) normalize-then-dedupe ordering (query-strip→absolutize→dedupe-AFTER-normalization) promoted from pass-10's composition ruling because the ORDERING is a distinct porting trap → url-roundtrip-extraction. OMITTED-WITH-REASON this pass: linkedin-private-api responses/* (type aliases over already-mined envelopes), entities/index barrels, examples/* (thin usage demos), config/index.ts constants (covered by dual-persona-auth-headers); Auto_job_applier modules/resumes/generator.py+extractor.py (docx/fpdf template filler, dead product path per pass 10, confirmed); EasyApplyJobsBot utils prRed/prGreen/prYellow/donate/urlToKeywords/jobsToPages (trivial or covered by search-url-builder/ledger-contrast); lh-basis listData tuple guards (composition of duck-type-guards primitives). Probes: needle battery green across all 7 capsules (keyBy(jobPostings,'entityUrn'), Omit<PeopleSearchFilters,'network'>, with_structured_output, select_option, objectHasProperties, "Session Summary" append-mode, seen_urls split('?') — each resolves to its cited file); spec pins verified at :592/:611 (job posting/company $type assertions); parity mechanical-green (79 refs on disk = 79 unique loader lines = map entries; grep -rL capsule-v2 empty). MID-PASS SIBLING LANE: a second linkedin lane landed 8 capsule-v2 refs for the never-assigned 13th suite repo linkedin-profile-scraper-api (MIT, master@9fc7125, 128n/213e full mode) into this leaf's ./ mid-run (cookie-session-bootstrap, puppeteer-flag-stack, request-interception-budget, raw-clean-two-phase-parse, duration-with-present, expand-then-parse, geo-string-classifier, zombie-browser-teardown). This pass verified each against source before wiring (checkIfLoggedIn redirect-probe :465-493 + typed SessionExpired; triple-dash flag no-op; stylesheet-block warning; getCleanText/getLocationFromText/formatDate pure-function pins in src/utils/index.test.ts describes; treeKill SIGKILL behind browser.close at close() :416-460; graph probes resolve checkIfLoggedIn/setup/utils trio; coverage no_recorded_issue on all 4 cited paths), wired all 8 loader lines + 3 new map groups (Browser ops Puppeteer / Profile parse discipline / cookie-session-bootstrap folded into Session & transport), and re-ran parity: 87 refs = 87 unique loader lines = map entries. Suite now 87 refs / 13 repos. Pass 13 (this run, dedicated suite lane): pins re-verified UNCHANGED on all git suite repos; folder census found JobSpy + linkedin-mcp-server (both owned by sibling foundations jobspy/linkedin-mcp — NOT suite material); linked-helper-extract "new files" triaged as .idea/.pi IDE metadata only (source unchanged, learn-only ruling stands) → mandated fresh citation-vs-inventory grep rerun at FILE granularity over all 13 repos against leaf+87 capsules → 5 new capsules (87→92 for this lane's slice): linkedin-private-api response-typing plane whole-file (all 10 src/responses/*.ts + linkedin-collection-response.entity.ts: LinkedInCollectionResponse<T,I,M> generic — data.elements URN pointers vs sibling included[] entity definitions, per-endpoint M = paginationToken/unreadCount/invitationType/totalResultCount, SendMessageResponse literal included:[] as write-marker, GetOwnProfileResponse *miniProfile pointer shape, conversation.response.get direct-data variant) → collection-response-envelope; core/client.ts+linkedin-request.ts two-layer composite (field initializers capture this BEFORE constructor body assigns request; children store-and-defer) → client-field-precedence-wiring; joeyism company.py _get_overview (:118-208) content-vocabulary classifier with zero-progress dt/dd fallback + test_company_scraper.py unit/model pins → overview-vocabulary-classifier (pass-10 composition ruling refined: classification ladder is a distinct porting contract); linvo visit.service.ts (:15-35 post-settle href read-back as identity resolver) + like.service.ts (:18-77 identity-first envelope, about:blank SPA reset, slice(0,1) volume bound, try-wrapped click region) → visit-resolve-identity + like-with-recovery-envelope. OMITTED-WITH-REASON this pass: private-api ~35 remaining uncited entities/types (passive interface mirrors of mined envelopes), enums Country/Language (~250-line ISO tables, rot-prone wire vocab), BlendedSearchFilters union alias, ClientInterface type alias, invitation.scroller.ts SkipLimit twin (re-cited by messaging-read-path-scrollers), examples/* thin demos; joeyism samples/* (composition, pass-10 ruling stands), models/{post,job}.py (flat dataclasses consumed by mined scrapers), __init__ barrels; linvo endorse.service.ts/profileViews.service.ts/global.page.service.ts (moveAndClick/autoScroll idioms captured in ghost-cursor-click-ladder + inbox-harvest-shuffle), helpers capitalize/colors/make.id/gotoUrl/show.mouse (trivial or covered); open-linkedin-api __init__ barrels; Auto_job_applier config/resume.py commented-out stub + config/questions.py pure user-answer data feeding mined answer ladders. Probes: 15-source-needle battery green first run incl. negative cross-repo check (dt/dd xpath unique to joeyism tree); graph probes resolve all five seams at matching line ranges; coverage no_recorded_issue+metadata_match on cited pa paths. This lane's slice now 92 refs across 13 repos; leaf total 104 at wiring time including a CONCURRENT zendriver lane mining ext-zendriver into this same leaf (its 12 capsule-v2 files verified wired by its own pass, not this lane's). Pass 15 (2026-08-24, dedicated suite lane, ext-zendriver deepening at UNCHANGED pin `main@2c6d9c7` = base_sha; graph 4120n/28298e FULL mode ready, zero parse_partial): executed the queued ext-zendriver target — whole-file re-reads of tab.py/expect.py/_contradict.py/cloudflare.py/util.py + element.py public surface vs the 12 rescued refs exposed five genuinely uncited seams → text-find-best-match-ladder (DOM.perform_search + discard + text-node→parent hop + best-match-by-length + find-as-wait poll loops), js-dumps-two-strategy-ladder (prototype-chain key harvest depth-cap-2 dumper with exception-gated WeakSet fallback, deliberately bypassing Tab.evaluate), contra-dict-attr-mapping (`__dict__`-aliasing dict subclass, data-over-methods precedence, silent=True at both call sites; DOCSTRING-VS-CODE erratum: promised snake_case normalization does NOT exist in code), quads-center-mouse-kit (get_content_quads→Position center math, viewport-vs-page +scroll correction, press/release click_count=1 pairing, stepped drag interpolation w/ asyncio.sleep(0) yields, skip-not-crash missing-geometry semantics), page-capture-download-gate (sleep-refresh→capture→empty-guard skeleton for MHTML/screenshot/PDF, capture_beyond_viewport full-page, set_download_behavior allow-latch feeding DownloadExpectation restore). Probes: 20-needle battery green incl. cross-repo adversarial 0 hits (js_dumps in joeyism project); coverage no_recorded_issue+metadata_match ×7 cited paths. verify_cf loop internals ruled DUPLICATE of cloudflare-shadow-checkbox-solver (same seam); element.mouse_click docstring itself flags it as unreliable — recorded inside quads capsule. Leaf total now **109 capsule-v2** (92 suite + 17 zendriver). Pass 16 (2026-08-24, dedicated suite lane): pins re-verified UNCHANGED on all 11 git suite repos (dirty flags on linkedin-private-api/linvo-scraper triaged = untracked `.cgcignore` tool artifacts only); no new folder-census repo → executed queued conditional target #3 ext-zendriver deepening at unchanged pin `main@2c6d9c7` — `Tab.evaluate` deep-serialization semantics mined whole (:737-769) → evaluate-dual-return-modes: single-send dual-mode contract where `return_by_value=False` builds SerializationOptions(deep, max_depth=10, maxNodeDepth=10, includeShadowTree:"all") and returns DeepSerializedValue.value while by-value returns remote_object.value; fixed user_gesture=True + allow_unsafe_eval_blocked_by_csp both modes; identical ProtocolException error contract; TYPE-HINT ERRATUM recorded (hinted Tuple[RemoteObject,ExceptionDetails] arm is DEAD — only js_dumps :939-942 returns tuples, with its own upstream TODO); direct test pinned test_tab.py::test_evaluate_complex_object_no_error :470-477 over the deliberate-circular-ref fixture complex_object.html; js_dumps' deliberate evaluate-bypass (:933 infinite-loop comment) cross-referenced. Probes: needle battery green first run (deep :747, maxNodeDepth :749, user_gesture :755, csp-bypass :758, deep_serialized_value :769, bypass-comment :933, TODO :940); graph probes resolve Tab.evaluate 737-769 + DeepSerializedValue 70-108; coverage no_recorded_issue+metadata_match ×4 cited paths; adversarial wrong-repo check n/a (seam unique to zendriver tree). Leaf total now **110 capsule-v2** (92 suite + 18 zendriver). OMITTED-WITH-REASON pass 16: remaining ext-zendriver residuals stay closed per pass-15 rulings (cdp-generated stubs, examples/, tutorials/, scripts/ product surface; Connection send/receive internals beyond lazy-domain-enablement remain a named-only target). Pass 17 (2026-08-24, dedicated suite lane drain-lane-linkedin-combined, cron a461a6981132 successor fire): pins re-verified UNCHANGED on all 11 git suite repos + learn-only dirs untouched; folder census clean (15 dirs = 13 suite + sibling-owned JobSpy/linkedin-mcp-server) → executed the pass-16 named-only target — Connection send/receive transport core mined whole-file (connection.py 893L) at unchanged pin `main@2c6d9c7` (graph ext-zendriver ready FULL 4120n/28298e, base==head, generation_matches, zero parse_partial; coverage no_recorded_issue+metadata_match on connection.py+util.py) → THREE new capsules: cdp-transaction-generator-protocol (cdp factories are SINGLE-YIELD generators; Transaction consumes the first yield for method/params and completes its asyncio future by resuming that yield via send(result_dict) — StopIteration.value IS the typed result; ProtocolException parses dict/to_json/plain error shapes; keyword-only __call__ = tx(**message); ADVERSARIAL PROBE PROVEN: two-yield adapter generators degrade even well-formed responses to "could not parse"; ERRATUM: GLOBAL_DELAY :38 is dead — zero refs repo-wide), websocket-dispatch-ledger (send pipeline: mutex-guarded itertools.count ids reset when mapper empties, mapper insert→websocket write→await future with command/params context appended to re-raised ProtocolExceptions; listener_loop POPS each transaction BEFORE completing it — upstream-cited zxsleebu memory-leak fix; reserved -2 oneshot slot survives across frames, unknown ids silent-continue; events become EventTransactions inserted into the SAME mapper then fan out to (event,connection)-or-(event) callbacks, sync ones via asyncio.to_thread, one bad callback logged+re-raised without killing the loop; feed_cdp sync ensure_future fire-and-forget exists FOR blocking fetch.RequestPaused interception; MAX_SIZE 2**28 / PING_TIMEOUT 900s pinned; ERRATUM: Listener.history deque(maxlen 1000) declared but NEVER appended — dead debug buffer), connection-class-armor-send-pipeline (CantTouchThis metaclass raises SettingClassVarNotAllowedException on ANY class-level write except __annotations__ while instance writes stay free — class attrs are SHARED declarations; every send lazily re-checks socket open + LATCHED _prepare_expert/_prepare_headless preps + listener health; constructor **kwargs land silently as instance attrs; target setter validates TypeError; ~12 None-safe target delegation properties refreshed by sleep()/wait() via update_target(_is_update=True); Listener() ctor REQUIRES a running loop because create_task fires in __init__). Probes: REAL EXECUTION gate-5 battery 9/9 green incl. full round-trip through the REAL auto-started listener_loop over a FakeWS (command response pops mapper + completes awaiting future) and the two-yield adversarial case — no browser needed via import-by-path recipe (stub zendriver.cdp.{target,page,browser} + util.cdp_get_module in sys.modules; ambient python3 has websockets 15.0.1 but NOT `deprecated`, which only the cdp surface needs); probe iterations themselves surfaced two behavioral truths now IN the capsule (keyword-only completion; running-loop ctor requirement). Graph probes line-exact ×5 anchors (Transaction 83-95/__call__ 110-128, feed_cdp 462-475, _send_oneshot 710-723, send 535-585, listener_loop 773-886). SIBLING COORDINATION: parallel lane committed pass-16 evaluate-dual-return-modes mid-run (b149a1c [DONE:227]); this lane's slice was disjoint (transport core vs evaluate semantics), drafted without touching the leaf, wired after the sibling commit landed; parity set-diff verified 113 disk = 113 loader lines = map entries incl. all three new names, grep -rL 'capsule-v2' EMPTY. OMITTED-WITH-REASON pass 17: Position.to_viewport scale>1 ruled THIN PASS-THROUGH (element.py :1245-1253 forwards x/y/w/h into cdp.page.Viewport verbatim; callers always scale=1 — captured in quads/capture capsules, no separate contract); Transaction.__repr__/EventTransaction.__repr__ (debug formatting); Connection.sleep default 0.25s (covered by idle-breathe-readiness); add_handler module-walk internals (owned by lazy-domain-enablement); websockets library semantics (external dep). Leaf total now **113 capsule-v2** (92 suite + 21 zendriver). Pass 18 (2026-08-24, dedicated suite lane drain-lane-linkedin-deep, cron e7e02e9d1499 successor fire): pins re-verified UNCHANGED on all 11 git suite repos AND origin-polled (rev-list HEAD..@{u} = 0 on every repo incl. ext-zendriver — upstream genuinely silent, not unpulled); folder census clean; learn-only dirs re-triaged clean. Mandated fresh sweep at file+dir granularity over the three never-symbol-swept repos plus a full lh-basis dir-census vs citations → SEVEN new capsule-v2: open-linkedin-api generate_trackingId twins (16 randrange(256) bytes; chr-join charString for binary-tolerant mutation payloads vs base64[2:-1] printable twin; client-minted correlation id NOT derived from target — contrast private-api server-issued search-hit ids) → trackingid-bytes-twins; LinkedIn-Easy-Apply-Bot get_easy_apply_button False-SENTINEL (:396–415, single init site; text filter 'Easy Apply' over ALL jobs-apply-button matches beats class-only because premium buttons reuse the class; consumer gate `button is not False` identity check :339 projects write_to_file's `attempted` audit column :380) → easy-apply-button-sentinel; lh-basis WorkingHours plane whole (Source.WorkingHours.js + Source.WorkingHoursAdjustments.js + helpers/adjustments.js): scope-filtered minute-window interval test with all-day-and-night short-circuit BEFORE adjustment math, deterministic UTC-midnight-seeded daily timeshift jitter (prando keyed by date+account+dow+ids, shift=round(timeshift*(1−pct/100)), zero when interval too short), closest-date composition intersect-vs-INVERTED-merge of global schedule, adjustments UNIQUE(li_account_id,campaign_id,action_id,working_week_day) migration 169 backfilled only from non-dayAndNight bounded rows → working-hours-interval-algebra; lh-basis kernel DDD plane whole-file (contexts/kernel BaseAggregate dual-path versioning: apply() optimistic bump+pendingEvents vs applyPersisted() strict no-gap check throwing Invalid event version sequence; handler-map-only mutation; Campaign guard-before-apply _assertCampaignIsEditable/Visible; SSI migrateEvents UnsupportedEventMigrationError version gates; TimePoint/TypedId primitives; AccountSSIHistory ensureNew monotonic occurredAt; snapshot freeze + SQLSnapshotStore/SQLEventsStore persistEvents aggregateVersion=fromVersion+i+1) → aggregate-versioned-event-kernel; lh-basis CSVParser plane whole (ParseCSVTransform papaparse step-stream w/ abort-on-broken-first-row + two-hop backpressure; SaveEntitiesFromBatchedRowsStream first-chunk header latch + lastProcessedRowIndex checkpoints; campaigns.js header-checksum + foreign-file markers company_id/public_id BEFORE any insert; per-row classify-not-throw reason enum MISSING_QUOTES…FAILED_TO_CREATE_CAMPAIGN; empty-cell→null mapping) → streaming-csv-import-gate; lh-basis MessageTemplate/LegacyMessageTemplate.js whole (structural sniff per config slot; valueParts[i]-then-variables[i] positional interleave; if-then-else REVERSED options indexing options[1]=THEN options[0]=ELSE as THE named trap; wrap-in-single-variant envelope; migrated ?? cfg identity-preserving no-op for modern configs) → legacy-template-variant-migration; lh-basis WeekWorkingIntervalRepo/WorkingHoursAdjustments descriptor converters (dayAndNight toColumn/fromRow BOTH calling WeekWorkingSchedule.isDayAndNight on source fields {start,end}/{started_at,ended_at}; isEnabled bool↔INTEGER pair; DBModelsRepo ctor PK-loud-throw + auto-dates partitioning; storage twin day_and_night INTEGER NOT NULL DEFAULT 0 in migrations 34/81/155/169) → derived-column-descriptors. Probes: byte-exact needles verified pre-authoring ×14 (randrange(256)=2 in helpers.py; trackingId import+consumer :22/:1292; EasyApplyButton=False count 1; is-not-False sites :339/:380; day_and_night DEFAULT 0 in migration 34; options\[1\] count exactly 1; isFirstChunkParsed count 3; company_id count 1; UNIQUE key grammar in 169; toColumn pairs ×2 repos); graph probes: lh-basis BM25 returns 0 on dist files (minified single-line) so Retrieve pins semantic-query on project lh-basis-migrations (resolves numbered migrate Functions 42/44/169) and lh-basis-source 'adjustments timeshift' (_getRandomWorkingHoursTimeshift resolves line-exact); check_index_coverage stdin-JSON on WeekWorkingIntervalRepo/LegacyMessageTemplate/ParseCSVTransform under project lh-basis = excluded not_tracked (dist plane outside indexed root BY DESIGN — direct source probes, consistent with sibling lh-basis capsules). Sweep closure rulings this pass: open-linkedin-api utils/ now FULLY cited (URN helpers→profile-schema, sort family→voyager-dual-collection-sort, trackingId→new capsule); LinkedIn-Easy-Apply-Bot production tree fully covered (setupLogger trivial two-handler logging; fill_data window-offscreen cosmetic; avoid_lock pyautogui banned by Boundaries; load_page owned by humanization-scroll; remaining methods cited by easy-apply-modal/string-outcome-channel/dedupe/job-run capsules); hassan fully covered by sales-nav-lead-identity supporting seams + browser-fingerprint-stealth (AdsPower attach) — sheet-switching/header-bootstrap interactive UX ruled product surface; lh-basis remaining uncited dirs triaged: contexts/* application/mappers + composition bindings (inversify DI wiring shells), LhUser/lhUsersRepo (thin descriptor repo of captured pattern), CSVParser/{constants,helpers,index} (support chunks of mined pipeline), Deduplication/types.js + SearchBuilder/types.js + ImportPeople/OrganizationsFromUrls.js + ExportProfiles.js (empty barrels or 6-line re-exports), migrations/217-numbered family (schema history consumed via mined anchors). Leaf total now **120 capsule-v2** (92 suite + 21 zendriver + 7 pass-18). Pass 23 (2026-08-24, dedicated suite lane drain-lane-linkedin-deep, cron e7e02e9d1499 successor fire): pins re-verified UNCHANGED on all 11 git suite repos AND first origin-fetched poll = 0-behind ×11 (upstream genuinely silent); folder census unchanged (15 dirs = 13 suite + sibling-owned JobSpy/linkedin-mcp-server) → mandated fresh citation-vs-inventory sweep at FILE granularity over 10 corpus repos against leaf+120 → triaged: joeyism samples/* (demo composition, ruling stands); lh-basis core/public-methods/source/**Source.js plane READ WHOLE (every *Source.js = sentry-debugIds IIFE stub + sourceMappingURL, zero logic — the standing CQRS read-model conditional resolves OMIT-BY-STRUCTURE, later lanes must not retry); linvo accepted.connection.request.service (pass-10 ruling stands) but linkedin.messages.service.ts PLURAL ruled distinct from the mined linkedin.message.service.ts yet a composition of captured idioms (about:blank SPA reset, timer pacing, contenteditable send with msg-s-message-list count-delta verify, transcript return via messagesFromChat) → omitted-with-reason; private-api cookie taxonomy + test factories surfaced as TWO genuinely uncited seams, mined whole-file: voyager-star-pointer-joins (`*field` values are pointer REFERENCES with per-grammar unwraps — urn:li:fs_messagingMember:(n,id) → split(',')[1], scheme-stripped miniProfile ids keyed by TRANSFORMED profileId not raw entityUrn, direction-dependent member fields sent→*toMember / received→*fromMember; conversation spec pins BOTH suites :123–137 scroller + :314–329 direct-get with entityUrn+profileId asserts ×4 each; invitation-spec orderBy('createdAt') sort assert recorded as DEGENERATE stability-check quirk NOT to copy) and cookie-jar-phase-taxonomy (AnonymousCookies required-six vs AuthCookies optional superset are PHASE DOCS not runtime validators — live jar is whatever set-cookie delivered ({JSESSIONID, authenticated}); parseCookies first-key-wins kernel pickBy(k===Object.keys(parsed)[0]) drops attribute tails or the jar poisons; partial {JSESSIONID} jar valid via userCookie; login.spec pins header merge ×3, cache write shape, zero-call short-circuit :80/:196, partial-jar accept :152, useCache:false bypass :217). Probes EXECUTION-FIRST: v1 battery 13/24 exposed invented expectations (side-count compositions, line-vs-count swaps) → every RED re-derived against live grep BEFORE writing → capsule Probe paragraphs repaired pre-commit → final battery 31/31 GREEN byte-exact from repo root; graph probes participantToProfileId twins message.repository :14-15 + conversation.repository :11 rank#1×2, parseCookies :12-19 rank#1; adversarial wrong-project retrieval joeyism+linvo mixed-seam queries total:0 ×2 with positive control resolving; coverage stdin-JSON ×4 cited paths no_recorded_issue+metadata_match @ gen 2026-08-23T00:12:08Z; parity disk=loader=map 122=122=122 bidirectional set-equality all line-1 v2. GATES NOTE: mid-run check-integrity transiently FAILED on a COMMITTED conflict-marker packs.json blob at HEAD (rallly-lane rebase/autostash crash class, markers inside the committed JSON) — resolved by a sibling repair commit within minutes; untouched by this lane per transient-sibling-state protocol, quality-gate exit0 zero linkedin findings, membership unchanged. OMITTED-WITH-REASON pass 23: linvo linkedin.messages.service.ts (composition of captured idioms), lh-basis source/*Source.js (empty compiled stubs), private-api remaining entities/responses/types (standing passive-mirror rulings), test/*-factories.ts (covered AS FIXTURE EVIDENCE inside the two new capsules rather than as a standalone seam). NEXT-PASS TARGETS unchanged from pass 22: (1) any suite pin advance → diff-first per repo WITH origin fetch every poll; (2) NEW repo under inspo/linkedin/ not owned by siblings → folder census FIRST; (3) lh-basis CQRS query handlers ONLY on explicit porting question; (4) else cycle COMPLETE at these pins — rotate lane. Pass 24 (this run): pins UNCHANGED x11 and origin-polled 0-behind x11 → unique-seam re-entry mined the never-cited persistence/launcher planes: lh-basis `core/local-source/dist/helpers/{sql,functions}.js` (retryOnBusy SQLITE_BUSY-only full-batch replay; _executeInTransaction SQLite-raw-BEGIN vs PG isolation split; withSavepoint; ≤999-param chunks; empty-IN→TRUE/FALSE) — outside all graph roots, source-read probes stand in; lh-basis `Source/Source/SqliteSource.js` + DB init phases (queued clearDB/dropDB choreography: FKs off → triggers/views dropped → reverse-order DELETE skipping version row; EXCLUSIVE view rebuild; resetDBPhases re-run); lh-basis campaign context CQRS composition (bind/setup facade, INFRA_DB_TRANSACTION_PROVIDER rebindSync after host DB init, ChangeHiddenFlagHandler guard→versioned-append→save→publish ordering, AssertCollectionNotReadonlyHandler guards-as-query-handlers) closing the standing pass-22/23 conditional #3; linked-helper-extract-dist Window.js packaged key-sequence DevTools latch (`launcher`/`auto`/`bbb`/`unlock`, prefix-match self-disarming latch, preventDefault hiding, dev-instant branch). 4 new capsule-v2 (122→126); lh-basis-source anchors live-resolved pre-write (SqliteSource.clearDB/dropDB rank#1/#2; Source.execInQueue); umbrella-`lh-basis` and helpers-plane zero-result retrievals documented as BY-CONSTRUCTION (roots exclude those planes).

Dedicated-lane pass (miner-LinkedIn-Easy-Apply-Bot, 2026-08-25): project re-verified `ready` at HEAD `8471c58b39e2a3bb3f4a2deb1e3c410e7fda7e0e` (90n/189e unchanged, parse_partial 0) → whole-source internalization of `easyapplybot.py` (743L) + artifacts → boundary reads against every adjacent capsule → 5 new capsules from genuinely uncited seams; probes executed byte-for-byte at HEAD (no upstream tests exist — recorded block). Work record: `$REFERENCE_ROOT/LinkedIn-Easy-Apply-Bot-work/`. Pass 2 (same lane, same pin re-verified byte-for-byte): leaf grew ~46 capsules since pass-1 boundary reads → conditional NEXT-PASS TARGETS re-adjudicated → 2 new capsules (`lookup-exception-topology`: start_linkedin's except-TimeoutException soft-fail proven structurally dead — zero implicitly_wait sites, unversioned selenium pin, doc-verified exception semantics, live-WebDriver probe blocked-by-environment and recorded; `browser-title-ledger-identity`: write_to_file's positional `' | '` title split promoted per target #3 now that richer parser twins landed — regex probe executed verbatim incl. the [1] IndexError row-loss trap bounded by applications_loop's print-and-continue island :313–314) + 1 substantive refactor (visible-card-worklist-harvest gains the second progress trap: apply_loop :323 `jobIDs[jobID] == applied` comparison never assigns).

Dedicated-lane pass (miner-joeyism-linkedin-scraper, 2026-08-25): project re-verified `ready` at HEAD `b1cdc1c0e85bee8764d62565d229c682e5eb81bb` (487n/1949e unchanged, parse_partial pytest.ini only — read directly, clean INI) → whole-repo internalization of the async-Playwright library layering (core/models/scrapers/callbacks/samples/tests) + every test file → coverage check on all 15 cited paths (no_recorded_issue ×15) → 7 new capsules from genuinely uncited seams. Runner established this pass (pytest+pytest-asyncio+playwright installed): repo's own `pytest -m unit` = 7 passed / 15 deselected incl. session save/load roundtrip; `-m integration` without linkedin_session.json = honest fixture skips (3 skipped). Live probe: safe_click(missing) → False in 1.00s proving the disarm trap. Work record: `$REFERENCE_ROOT/joeyism-linkedin-scraper-work/`. Pass 2 (same lane, same pin re-verified live: 487n/1949e, HEAD byte-identical, checkout clean): symbol-by-symbol closure sweep — all 186 Function/Method nodes adjudicated against every citing capsule incl. the FAC-59 linkedin_scraper twins (≡ identical tree) → 1 new capsule (`auth-session-verdict-tiers`: login_with_cookie's three-tier verdict — negative redirect ⇒ typed AuthenticationError, unverifiable 0.5s×10 poll ⇒ loud "Proceeding anyway" proceed, other causes wrapped with typed passthrough; contrasted with BrowserManager.load_session's by-construction write-only `_is_authenticated=True`); trace_path proved extract_list_items/element_exists/count_elements/wait_for_navigation_complete/login_with_cookie have ZERO internal callers (consumer-only API tier); Locator.all await-trap hypothesis DISPROVEN by executed probe (iscoroutinefunction=True on repo-pinned playwright>=1.40); former targets #2–#5 adjudicated covered-in-place with sources/tests named. Fewer-than-five closure PROVEN at this pin: remaining symbols all owned or omit-reasoned in research.md pass-2 note. Pass 26 (FAC-66 lane miner-open-linkedin-api, main@5feee360ec26 re-verified vs checkout HEAD): dedicated open-linkedin-api batch — seam triage against all 13 citing capsules → 6 new capsules (invitation-shared-secret-lifecycle, job-detail-urn-plane, universal-name-entity-lookup, update-extractor-family, conversation-participant-resolution, company-search-vertical-projection); overlaps deliberately NOT duplicated: mutation shapes (reply_invitation/react/follow/seen) stay in voyager-mutation-endpoints, LEGACY_INBOX paging stays in messaging-read-path-scrollers (private-api twin), loop family stays in voyager-pagination. Coverage checks on linkedin.py + utils/helpers.py = no_recorded_issue+metadata_match; repo ships no tests → deterministic byte-exact probes recorded per capsule.

Dedicated-lane pass (miner-linkedin-private-api / FAC-60, 2026-08-25): project re-verified `ready` at HEAD `e083f370c331ed643348158b8c64f905de477626` (2389n/3387e full mode, parse_partial 0 / skipped 0); whole-repo squeeze against the 21 capsules already citing linkedin-private-api proved the src plane near-exhausted at this pin → 2 new capsules BY THIS LANE (shared-leaf totals move independently; sibling lanes active same day): `full-response-transport-kernel` (src/core/request.ts was uncited: slash-less buildUrl base resolution, fullResponse dual-return used exactly twice for auth, wholesale setHeaders persona swap, deliberate absence of error handling) and `invitation-mailbox-read-grammar` (src/requests/invitation.request.ts mailbox READ GETs were uncited: selector-style received view vs type-filter sentInvitationViewsV2, start/count rename, verbatim CONNECTION token); dual-persona-auth-headers gained the hardcoded authHeaders Content-Length trap. Runner block: jest suite present but node_modules absent in checkout (install exceeds lane allowed-file list) — probes recorded as source+spec-text evidence, never fabricated runs.

Dedicated-lane pass 2 (miner-linkedin-private-api / FAC-60, 2026-08-25): pin re-verified UNCHANGED (`ready`, head==base==`e083f370c331…`, 2389n/3387e, parse_partial 0 / skipped 0; node_modules still absent → runner blocker stands) → both queued NEXT-PASS TARGETS delivered as source-confirmed refactors of THIS lane's pass-1 neighborhood: `voyager-star-pointer-joins` invariant corrected from its single-form claim ("keyed by TRANSFORMED profileId") to the join-key FORM duality table proven across all seven join sites — messagingMember tuple pointers consume the profileId-stripped default (conversation :69→25, message :51→56) while plain URN pointers require an explicit RE-KEY by `'entityUrn'` (search fetchPeople :147→154, invitation parseInvitationResponse :20→26, fetchCompanies :181→188, fetchJobs two raw keyBys :213–214→217–218); wrong form = silent undefined joins; drifted citation :24–32 fixed to :14–31. `included-envelope-hydration` gained the full NINE-constant exported `$type` registry (exact wire strings + graph-USAGE consumers), including the dash/non-dash company trap (`COMPANY_TYPE`=voyager.dash.organization.Company vs `BASE_COMPANY_TYPE`=voyager.organization.Company) previously uncited anywhere in the leaf. Coverage no_recorded_issue+metadata_match on all 14 cited paths; capsule Retrieve calls re-executed live (getProfilesFromResponse :20–28; both participantToProfileId twins) plus adversarial probe ("fetchCompanies MiniCompany company search" → SearchRepository.fetchCompanies :158–190 top rank).

Dedicated-lane pass (miner-linvo-scraper, 2026-08-25): project re-verified `ready` at HEAD `cfbe91080c7347591dee44a26f55d74bba734da2` (239n/597e FULL, head==base==pin, parse_partial 0) → whole-repo internalization: composition root registry (14 services), helpers/messages.service → LinkedinAbstractService (497L) inheritance spine, navigation/readiness kernel, messaging write/read duality, scraping router → coverage adjudication of every file against prior suite waves → 8 new capsules from genuinely uncited seams. Repo ships NO tests (scripts start/watch/build only; start target missing) — deterministic byte-exact source anchors executed at HEAD instead; runner absence recorded as block. Work record: `$REFERENCE_ROOT/linvo-scraper-work/`.

Dedicated-lane pass 2 (miner-linvo-scraper / FAC-65, 2026-08-25): same pin re-verified live (head==base==cfbe910, 239n/597e unchanged; coverage check on all 9 cited paths = no_recorded_issue+metadata_match) → mined the recorded NEXT-PASS targets → +6 capsule-v2 (`conversation-switch-gate-walk`, `sentiment-transcript-twins`, `mouse-glide-wheel-hover`, `sales-nav-warmup-projection`, `post-return-stranded-dom-walk`, `thread-href-connectivity-oracle`) + humanization-scroll precision refactor (its linvo pagesTask-wheel/scrollTo instance citations annotated DEAD-AT-PIN). Key discovery: both scraper services return from their response planes early leaving entire DOM-walk halves unreachable below the return (sales :118–273 cluster incl. workOnResults+scrollTo; page :132–171 incl. elements; global Promise.race remnant :115–149); queue-writer→continueGetAllMessagesFromChat delegation confirmed in source (:80, module singleton — graph lacks the dynamic edge); engagement internals adjudicated COVERED by feed-engagement-caps (no duplicate). Runner blocks stand (no upstream tests, no agent runner); fabric_mesh lease surface unavailable → shared-ledger row written as exact own-row edit with degraded-lock note in the work record.


Dedicated-lane pass (miner-hassan-sales-nav-profiles-scraper / FAC-63, 2026-08-25): project re-verified `ready` at HEAD `e294ac09c9b94bfcc8030079f31734bc9ae30dac` == pin (47n/56e FULL, head==base, parse_partial 0 / skipped 0; only .git + linkedin_scraper.exe excluded by design; coverage stdin-check `linkedin_scraper.py`+`README.md` = no_recorded_issue+metadata_match @ gen 2026-08-18T02:08:06Z) → whole-file internalization of the never-dedicated 262-line module (symbols: main :33–259 fan-out 7 via trace_path hop-1 to start_adspower_browser :17–31) mined its UNCITED destination/attach planes → 6 new capsule-v2 (gspread-service-account-bootstrap, sheets-header-retrofit-guard, sheet-switch-batch-reset, in-sheet-page-set-marker, cdp-attach-run-shell-ownership, utf8-stdout-guard); identity/pagination/name seams left cited-in-place (sales-nav-lead-identity primary, sales-nav-pagination variant) with deep-dive pointers added instead of duplication. Probes EXECUTED byte-exact: grep line-maps :14–15, :67, :77–78, :87–89/:121/:130/:133–135, :138/:140/:143, append_row ⇒ exactly :141/:216/:231–233, :220–221/:229/:232/:234–235, :100/:101/:253/:255/:257/:259; py_compile exit 0; utf8-guard behavioral RED (PYTHONIOENCODING=latin-1 print 🌟 ⇒ UnicodeEncodeError exit 1) vs GREEN (repo's verbatim two lines first ⇒ emoji printed, exit 0). Retrieval honesty: this project graph indexes SYMBOL NAMES ONLY (47 nodes; bodies/literals are not nodes) so body-keyword BM25 returns 0 BY CONSTRUCTION — every Retrieve block pins a resolving symbol anchor (query 'main' rank#1 main :33–259; name_pattern ^(main|start_adspower_browser)$ resolves both, executed) and byte-exact greps stand in as source-read evidence per pass-23 precedent. Adversarial absence verified by grep on real sibling roots: marker/connect_over_cdp/current_sheet_choice/gspread ABSENT from maximo3k, linvo lib, joeyism, Auto_job_applier, EasyApplyJobsBot. No agent runner exists (standing infrastructure block) → deterministic retrieval/content/probe battery stands in for RED/GREEN. OMITTED-WITH-REASON: AdsPower endpoint/key details (leaf Boundary omits vendor-specific AdsPower endpoints; attach-stealth angle already owned by browser-fingerprint-stealth), records_since_separator write-only dead counter (documented inside in-sheet-page-set-marker), committed credentials.json + hardcoded AdsPower key + hardcoded sheet IDs (security boundary — recorded, never mined). NEXT-PASS TARGETS: any pin advance → diff-first re-mine of linkedin_scraper.py only; upstream LICENSE-file landing reopens the excerpt-length ruling; linkedin_scraper.exe stays unexamined.

Dedicated-lane pass (miner-hassan-sales-nav-profiles-scraper / FAC-63 pass 2, 2026-08-25): pin re-verified UNCHANGED (`ready`, head==base==`e294ac09c9b9`, 47n/56e, parse_partial 0, coverage `no_recorded_issue`+`metadata_match` on linkedin_scraper.py + README.md) → whole-file re-adjudication of the 262-line module against all eight capsules citing this repo proved it near-exhausted at this pin → 1 new capsule-v2 (`bounded-element-read-budget`: five `timeout=1000` per-element reads :178/:183/:207/:211/:214 with `or ""` coercion vs page-level 20 s/10 s gates — latency-isolation twin of the row island) + substantive deepen of `sales-nav-pagination.md` (hassan two-stage readiness: DCL goto compensated by the 20 s anchor data gate; three observed-state terminations :149–152/:168–170/:243–248; collect-once `.all()` snapshot after scroll+settle) + render-typo fix in sales-nav-lead-identity.md heading. Evidenced fewer-than-five path: pass-2 census in the work record omits URL-empty guard/pair-print/`\r` progress counter (below quality bar) and records `date_str` (:57) as a SECOND dead binding beside records_since_separator. Probes executed byte-exact this pass: timeout=1000 ⇒ exactly 5 sites; or "" ⇒ :178/:183; wait_until= ⇒ :116; range(10) ⇒ :159; time.sleep(5) ⇒ :164; No profiles found ⇒ :169; Retrieve name_pattern ^main$ resolves main :33–259.

Dedicated-lane pass (miner-Auto_job_applier_linkedIn / FAC-62, 2026-08-25): project re-verified `ready` at HEAD `0ca5550f8aa80027621cfc17a30fceba05705f84` == pin (548n/1850e FULL, canonical root $REFERENCE_ROOT/Auto_job_applier_linkedIn == git worktree_root; parse_partial pytest.ini:6 only; not_indexed by design = gitignored secrets config/personals.py+config/secrets.py + PNGs) → whole-repo internalization of all 23 Python files (~4.9k LOC) against the 14 capsules already citing this repo → mined its UNCITED run-kernel planes → 8 new capsule-v2 (panel-bot-subprocess-supervisor, pristine-defaults-snapshot-reload, stuck-modal-loop-breaker, search-facet-cycle-rotation, csv-cell-safety-pair, relative-posted-time-normalization, blacklist-goodword-shortcircuit, loud-log-mirror-alert-kernel); connections.py/clickers_and_finders.py/config plane/app.py panel+CSV endpoints left cited-in-place (langgraph-form-answer-pipeline, selenium-click-finder-ladder, schema-driven-config-panel family) instead of duplication. Probes EXECUTED: full pytest suite green under Xvfb + isolated venv — 57 ran / 56 passed / 0 failed / 1 live-skipped (OPENAI_API_KEY-gated smoke), repo tree byte-clean after; loud-log failure-mode experiment executed both ways (rm logs/ ⇒ pytest blocks in pymsgbox._alertTkinter ← helpers.print_lg:137 per faulthandler; mkdir logs/ per make_directories contract ⇒ suite green) as the loud-log-mirror-alert-kernel RED/GREEN; stop-date clamp arithmetic verified unsatisfiable ((idx+1)>4 impossible for len==4). Coverage stdin-check on every cited path (app.py, runAiBot.py, modules/helpers.py, tests/test_app_integration.py, tests/test_helpers.py) = no_recorded_issue+metadata_match @ gen 2026-08-23T00:12:13Z. Retrieval honesty: every Retrieve block pins a resolving symbol anchor executed this pass (_terminate :185–218/api_run/api_logs; _load_defaults/_effective_config; apply_to_jobs/answer_questions; failed_job/submitted_jobs/truncate_for_csv; calculate_date_posted+test; check_blacklist single-hit; print_lg/critical_error_log); rotation query honestly surfaces bm25 token neighbors (calculate_date_posted) while main()/run() are read directly at :1116–1186. No agent runner exists (standing infrastructure block) → deterministic retrieval/content/probe checks stand in per pass-24 precedent.

Dedicated-lane pass 2 (miner-open-linkedin-api / FAC-66 scheduled, 2026-08-25): pin re-verified UNCHANGED (`ready`, head==base==`5feee360ec26…`, 187n/524e FULL, parse_partial 0 / skipped 0; checkout porcelain empty) → resumed from pass-1 NEXT-PASS TARGETS and delivered all four: identity me-card trio template, /me memoization lifecycle (incl. the cookies-direct scrape-bypass invariant), auxiliary request planes (track beacon + unfollow action verb), wvmp guarded-descent contract, plus memberShareFeed two-endpoint scoping discovered mid-probe → 5 new capsule-v2 (profile-me-card-variant-family, me-cache-memoization, auxiliary-request-planes, wvmp-guarded-descent, member-share-feed-scoping); overlaps deliberately NOT duplicated: base_request mechanism stays in voyager-api-client, auth-time _fetch_metadata scrape in voyager-password-auth-metadata, skills/experiences queryId reads in voyager-graphql-queryid, paging machines in voyager-pagination. With thin rulings recorded (get_profile_connections one-line delegation, get_conversation bare fetch, _cookies/_headers accessors) every Linkedin method is now mined/covered/omitted-with-reason at this pin. Probes executed byte-exact (accept-header 9 sites :632…:1621; base_request=True ⇒ :1483 only; set literal ⇒ :1083; moduleKey pair ⇒ :128/:1085; wvmp chain ⇒ :1120–1130); coverage no_recorded_issue+metadata_match on linkedin.py/client.py/settings.py; repo ships no tests — standing runner block.

Dedicated-lane passes (miner-linkedin-profile-scraper-api / FAC-67, 2026-08-25): project re-verified `ready` at head==base==`9fc71258725bad7c1f3c146ce2d587fc7debdc4c` == pin (128n/213e FULL, parse_partial 0 / skipped 0, generation 2026-08-23T00:14:02Z unchanged across both passes; checkout porcelain-empty before/after every probe). Pass 1: whole-file internalization of the lifecycle facade → 5 capsule-v2 (cdp-active-lifecycle-fastpage, warm-browser-service-contract, networkidle2-readiness-gate, timezone-pinned-time-tests, constructor-option-gate) + 40/40 upstream tests green via staged lockfile toolchain outside the read-only checkout. Pass 2 (deep-learning re-entry): symbol-plane census CLOSED — all 8 methods + 10 utils functions now cited-in-place; all four queued NEXT targets resolved by adjudication rather than duplication: drift audit DECIDED pinned-era-baseline attribution (package.json self-declares "Works in 2020."; photo OR-chain `.pv-top-card__photo || .profile-photo-edit__preview` :596 is the file's only internal hedge; redesign defense stays attributed to overview-vocabulary-classifier/wheel-bracketed-topcard-reader/profile-header-extraction) wired into raw-clean-two-phase-parse; peopleAlsoViewed/similarProfiles/accomplishments ABSENCE confirmed at this pin (token grep zero hits; run() returns exactly five sections); server.ts error-middleware gap ELEVATED to NEW warm-scraper-http-recovery-ladder (express ^4.17.1 async-route rejection bypass ⇒ unhandledRejection crash or browser=null zombie after run()'s teardown-then-rethrow catch); interception↔cdp-fastpage cross-ref WIRED. Plus NEW single-typed-error-budget from query_graph THROWS census (exactly ONE typed throw repo-wide: checkIfLoggedIn→SessionExpired) and duration-with-present flag-vs-sentinel divergence trap (:651/:779 ternary defaults missing end-dates to 'Present' while formatDate :31 special-cases that string regardless of the flag). Both new seams testless upstream — deterministic source-pin probes only. Work record: `$REFERENCE_ROOT/linkedin-profile-scraper-api-work/`.

## Boundaries
Adopt the pure contracts: auth/session ladders, pagination stop conditions, schema validation, modal state machines, dedupe sets, chunk iterators. Adapt selectors, aria-labels, facet code tables, jitter ranges, and ledger schemas — they rot against live LinkedIn and MUST be re-verified before production use. Omit: hard-coded credentials/API keys/tokens found in sources, pyautogui desktop hacks, donate/sponsor nags, vendor-specific AdsPower endpoints, and ALL code from no-license sources (patterns only). These bots operate against LinkedIn's ToS — educational pattern study; rate-limit respect seams are part of the contract, not optional.

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`accepted-connections-snapshot.md`](./accepted-connections-snapshot.md)
- [`aggregate-versioned-event-kernel.md`](./aggregate-versioned-event-kernel.md)
- [`applied-jobs-csv-json-remap.md`](./applied-jobs-csv-json-remap.md)
- [`async-retry-disarm-trap.md`](./async-retry-disarm-trap.md)
- [`auth-session-verdict-tiers.md`](./auth-session-verdict-tiers.md)
- [`auxiliary-request-planes.md`](./auxiliary-request-planes.md)
- [`blacklist-goodword-shortcircuit.md`](./blacklist-goodword-shortcircuit.md)
- [`blacklist-inline-annotation-gate.md`](./blacklist-inline-annotation-gate.md)
- [`bounded-element-read-budget.md`](./bounded-element-read-budget.md)
- [`browser-fingerprint-stealth.md`](./browser-fingerprint-stealth.md)
- [`browser-launch-flag-assembly.md`](./browser-launch-flag-assembly.md)
- [`browser-lifecycle-teardown.md`](./browser-lifecycle-teardown.md)
- [`browser-lifecycle.md`](./browser-lifecycle.md)
- [`browser-login.md`](./browser-login.md)
- [`browser-title-ledger-identity.md`](./browser-title-ledger-identity.md)
- [`busy-retry-transaction-kernel.md`](./busy-retry-transaction-kernel.md)
- [`cdp-active-lifecycle-fastpage.md`](./cdp-active-lifecycle-fastpage.md)
- [`cdp-attach-run-shell-ownership.md`](./cdp-attach-run-shell-ownership.md)
- [`cdp-keyboard-event-machinery.md`](./cdp-keyboard-event-machinery.md)
- [`cdp-transaction-generator-protocol.md`](./cdp-transaction-generator-protocol.md)
- [`channel-fetcher-contract.md`](./channel-fetcher-contract.md)
- [`chrome-profile-degradation-ladder.md`](./chrome-profile-degradation-ladder.md)
- [`chrome-profile-path-split-normalization.md`](./chrome-profile-path-split-normalization.md)
- [`chromedriver-explicit-path-relaunch.md`](./chromedriver-explicit-path-relaunch.md)
- [`chunked-export-iterator.md`](./chunked-export-iterator.md)
- [`client-field-precedence-wiring.md`](./client-field-precedence-wiring.md)
- [`cloudflare-shadow-checkbox-solver.md`](./cloudflare-shadow-checkbox-solver.md)
- [`collection-response-envelope.md`](./collection-response-envelope.md)
- [`company-about-section-probe.md`](./company-about-section-probe.md)
- [`company-search-vertical-projection.md`](./company-search-vertical-projection.md)
- [`config-override-noop-layer.md`](./config-override-noop-layer.md)
- [`config-validation-ladder.md`](./config-validation-ladder.md)
- [`connect-outreach-ladder.md`](./connect-outreach-ladder.md)
- [`connection-class-armor-send-pipeline.md`](./connection-class-armor-send-pipeline.md)
- [`constructor-option-gate.md`](./constructor-option-gate.md)
- [`contra-dict-attr-mapping.md`](./contra-dict-attr-mapping.md)
- [`conversation-participant-resolution.md`](./conversation-participant-resolution.md)
- [`conversation-queue-fanout-readback.md`](./conversation-queue-fanout-readback.md)
- [`conversation-switch-gate-walk.md`](./conversation-switch-gate-walk.md)
- [`cookie-jar-phase-taxonomy.md`](./cookie-jar-phase-taxonomy.md)
- [`cookie-session-bootstrap.md`](./cookie-session-bootstrap.md)
- [`cookie-session-persistence.md`](./cookie-session-persistence.md)
- [`cqrs-context-composition.md`](./cqrs-context-composition.md)
- [`created-before-scroller.md`](./created-before-scroller.md)
- [`csv-cell-safety-pair.md`](./csv-cell-safety-pair.md)
- [`declarative-delay-validation.md`](./declarative-delay-validation.md)
- [`decoration-id-response-shaping.md`](./decoration-id-response-shaping.md)
- [`dedupe-applied-tracking.md`](./dedupe-applied-tracking.md)
- [`derived-column-descriptors.md`](./derived-column-descriptors.md)
- [`displayed-count-page-budget.md`](./displayed-count-page-budget.md)
- [`dry-run-run-cap-gates.md`](./dry-run-run-cap-gates.md)
- [`dual-persona-auth-headers.md`](./dual-persona-auth-headers.md)
- [`durable-named-cursor-continuation.md`](./durable-named-cursor-continuation.md)
- [`duration-with-present.md`](./duration-with-present.md)
- [`easy-apply-button-sentinel.md`](./easy-apply-button-sentinel.md)
- [`easy-apply-modal.md`](./easy-apply-modal.md)
- [`embedded-json-search-parse.md`](./embedded-json-search-parse.md)
- [`endorse-unchecked-popover-close.md`](./endorse-unchecked-popover-close.md)
- [`evaluate-dual-return-modes.md`](./evaluate-dual-return-modes.md)
- [`exception-taxonomy-wiring.md`](./exception-taxonomy-wiring.md)
- [`expand-then-parse.md`](./expand-then-parse.md)
- [`feed-engagement-caps.md`](./feed-engagement-caps.md)
- [`feed-post-urn-scan.md`](./feed-post-urn-scan.md)
- [`form-question-answering.md`](./form-question-answering.md)
- [`full-response-transport-kernel.md`](./full-response-transport-kernel.md)
- [`geo-string-classifier.md`](./geo-string-classifier.md)
- [`ghost-cursor-click-ladder.md`](./ghost-cursor-click-ladder.md)
- [`gspread-service-account-bootstrap.md`](./gspread-service-account-bootstrap.md)
- [`headless-expert-prep-latches.md`](./headless-expert-prep-latches.md)
- [`humanization-scroll.md`](./humanization-scroll.md)
- [`humanized-scroll-gesture.md`](./humanized-scroll-gesture.md)
- [`idle-breathe-readiness.md`](./idle-breathe-readiness.md)
- [`in-sheet-page-set-marker.md`](./in-sheet-page-set-marker.md)
- [`inbox-harvest-shuffle.md`](./inbox-harvest-shuffle.md)
- [`included-envelope-hydration.md`](./included-envelope-hydration.md)
- [`integration-skip-gate-markers.md`](./integration-skip-gate-markers.md)
- [`invitation-mailbox-read-grammar.md`](./invitation-mailbox-read-grammar.md)
- [`invitation-shared-secret-lifecycle.md`](./invitation-shared-secret-lifecycle.md)
- [`job-detail-field-islands.md`](./job-detail-field-islands.md)
- [`job-detail-urn-plane.md`](./job-detail-urn-plane.md)
- [`job-run-orchestration.md`](./job-run-orchestration.md)
- [`job-search-honest-empty-flow.md`](./job-search-honest-empty-flow.md)
- [`job-topcard-middot-triplet.md`](./job-topcard-middot-triplet.md)
- [`js-dumps-two-strategy-ladder.md`](./js-dumps-two-strategy-ladder.md)
- [`langgraph-form-answer-pipeline.md`](./langgraph-form-answer-pipeline.md)
- [`lazy-domain-enablement.md`](./lazy-domain-enablement.md)
- [`ledger-contrast-csv-vs-summary.md`](./ledger-contrast-csv-vs-summary.md)
- [`legacy-template-variant-migration.md`](./legacy-template-variant-migration.md)
- [`li-at-mint-login.md`](./li-at-mint-login.md)
- [`like-with-recovery-envelope.md`](./like-with-recovery-envelope.md)
- [`linkedin-url-canonicalization.md`](./linkedin-url-canonicalization.md)
- [`linvo-service-taxonomy.md`](./linvo-service-taxonomy.md)
- [`live-collection-views.md`](./live-collection-views.md)
- [`loader-latch-navigation-kernel.md`](./loader-latch-navigation-kernel.md)
- [`lookup-exception-topology.md`](./lookup-exception-topology.md)
- [`loud-log-mirror-alert-kernel.md`](./loud-log-mirror-alert-kernel.md)
- [`md5-pickle-cookie-jar.md`](./md5-pickle-cookie-jar.md)
- [`me-cache-memoization.md`](./me-cache-memoization.md)
- [`member-share-feed-scoping.md`](./member-share-feed-scoping.md)
- [`message-send-guard-chain.md`](./message-send-guard-chain.md)
- [`message-template-substitution.md`](./message-template-substitution.md)
- [`messaging-read-path-scrollers.md`](./messaging-read-path-scrollers.md)
- [`messaging-write-path-duality.md`](./messaging-write-path-duality.md)
- [`model-dump-contract-validator-placement.md`](./model-dump-contract-validator-placement.md)
- [`mouse-glide-wheel-hover.md`](./mouse-glide-wheel-hover.md)
- [`na-preserving-row-extraction.md`](./na-preserving-row-extraction.md)
- [`nested-position-parsing.md`](./nested-position-parsing.md)
- [`network-expectation-context.md`](./network-expectation-context.md)
- [`networkidle2-readiness-gate.md`](./networkidle2-readiness-gate.md)
- [`offscreen-window-scrape-mode.md`](./offscreen-window-scrape-mode.md)
- [`overlay-pushstate-trigger.md`](./overlay-pushstate-trigger.md)
- [`overview-vocabulary-classifier.md`](./overview-vocabulary-classifier.md)
- [`own-profile-bootstrap.md`](./own-profile-bootstrap.md)
- [`packaged-keyseq-devtools-latch.md`](./packaged-keyseq-devtools-latch.md)
- [`page-capture-download-gate.md`](./page-capture-download-gate.md)
- [`paging-base-url-accumulation.md`](./paging-base-url-accumulation.md)
- [`panel-bot-subprocess-supervisor.md`](./panel-bot-subprocess-supervisor.md)
- [`path-first-browser-binary-discovery.md`](./path-first-browser-binary-discovery.md)
- [`pattern-filtered-cookie-jar.md`](./pattern-filtered-cookie-jar.md)
- [`phone-field-selector-ladder.md`](./phone-field-selector-ladder.md)
- [`pipe-row-three-island-extraction.md`](./pipe-row-three-island-extraction.md)
- [`playwright-resilient-helpers.md`](./playwright-resilient-helpers.md)
- [`post-return-stranded-dom-walk.md`](./post-return-stranded-dom-walk.md)
- [`preferred-resume-picker.md`](./preferred-resume-picker.md)
- [`pristine-defaults-snapshot-reload.md`](./pristine-defaults-snapshot-reload.md)
- [`profile-header-extraction.md`](./profile-header-extraction.md)
- [`profile-me-card-variant-family.md`](./profile-me-card-variant-family.md)
- [`profile-schema.md`](./profile-schema.md)
- [`profile-section-expansion.md`](./profile-section-expansion.md)
- [`profile-views-private-mode-schema.md`](./profile-views-private-mode-schema.md)
- [`progress-percent-step-budget.md`](./progress-percent-step-budget.md)
- [`puppeteer-flag-stack.md`](./puppeteer-flag-stack.md)
- [`qa-memory-ladder.md`](./qa-memory-ladder.md)
- [`quads-center-mouse-kit.md`](./quads-center-mouse-kit.md)
- [`querystring-repagination-router.md`](./querystring-repagination-router.md)
- [`queue-cache-decorators.md`](./queue-cache-decorators.md)
- [`randomized-search-combo-walk.md`](./randomized-search-combo-walk.md)
- [`rate-limit-detection.md`](./rate-limit-detection.md)
- [`raw-clean-two-phase-parse.md`](./raw-clean-two-phase-parse.md)
- [`react-controlled-input-fix.md`](./react-controlled-input-fix.md)
- [`relative-posted-time-normalization.md`](./relative-posted-time-normalization.md)
- [`request-interception-budget.md`](./request-interception-budget.md)
- [`response-interception.md`](./response-interception.md)
- [`sales-nav-lead-identity.md`](./sales-nav-lead-identity.md)
- [`sales-nav-pagination.md`](./sales-nav-pagination.md)
- [`sales-nav-warmup-projection.md`](./sales-nav-warmup-projection.md)
- [`schema-driven-config-panel.md`](./schema-driven-config-panel.md)
- [`schema-typed-coercion-ladder.md`](./schema-typed-coercion-ladder.md)
- [`scrape-orchestration-template.md`](./scrape-orchestration-template.md)
- [`scraper-base-callbacks.md`](./scraper-base-callbacks.md)
- [`scroller-factory-repositories.md`](./scroller-factory-repositories.md)
- [`search-blended-hit-joins.md`](./search-blended-hit-joins.md)
- [`search-facet-cycle-rotation.md`](./search-facet-cycle-rotation.md)
- [`search-scroller-twins.md`](./search-scroller-twins.md)
- [`search-url-builder.md`](./search-url-builder.md)
- [`selenium-click-finder-ladder.md`](./selenium-click-finder-ladder.md)
- [`send-then-verify-invitation.md`](./send-then-verify-invitation.md)
- [`sentiment-transcript-twins.md`](./sentiment-transcript-twins.md)
- [`session-producer-manual-login-poll.md`](./session-producer-manual-login-poll.md)
- [`session-summary-ledger-append.md`](./session-summary-ledger-append.md)
- [`sessions-json-cache.md`](./sessions-json-cache.md)
- [`sheet-switch-batch-reset.md`](./sheet-switch-batch-reset.md)
- [`sheets-header-retrofit-guard.md`](./sheets-header-retrofit-guard.md)
- [`single-typed-error-budget.md`](./single-typed-error-budget.md)
- [`skip-limit-scroller.md`](./skip-limit-scroller.md)
- [`source-carrying-model-serialization.md`](./source-carrying-model-serialization.md)
- [`sqlite-init-phase-pipeline.md`](./sqlite-init-phase-pipeline.md)
- [`stale-node-retry-ladder.md`](./stale-node-retry-ladder.md)
- [`stale-proof-two-pass-harvest.md`](./stale-proof-two-pass-harvest.md)
- [`streaming-csv-import-gate.md`](./streaming-csv-import-gate.md)
- [`string-outcome-channel.md`](./string-outcome-channel.md)
- [`structural-duck-type-guards.md`](./structural-duck-type-guards.md)
- [`stuck-modal-loop-breaker.md`](./stuck-modal-loop-breaker.md)
- [`tabbed-overlay-section-walking.md`](./tabbed-overlay-section-walking.md)
- [`testdouble-mock-harness.md`](./testdouble-mock-harness.md)
- [`text-find-best-match-ladder.md`](./text-find-best-match-ladder.md)
- [`thread-href-connectivity-oracle.md`](./thread-href-connectivity-oracle.md)
- [`throttle-classification-ladder.md`](./throttle-classification-ladder.md)
- [`timezone-pinned-time-tests.md`](./timezone-pinned-time-tests.md)
- [`topcard-button-none-sentinel.md`](./topcard-button-none-sentinel.md)
- [`trackingid-bytes-twins.md`](./trackingid-bytes-twins.md)
- [`typed-search-filter-contracts.md`](./typed-search-filter-contracts.md)
- [`universal-name-entity-lookup.md`](./universal-name-entity-lookup.md)
- [`update-extractor-family.md`](./update-extractor-family.md)
- [`url-queue-file-roundtrip.md`](./url-queue-file-roundtrip.md)
- [`url-roundtrip-extraction.md`](./url-roundtrip-extraction.md)
- [`utf8-stdout-guard.md`](./utf8-stdout-guard.md)
- [`visible-card-worklist-harvest.md`](./visible-card-worklist-harvest.md)
- [`visit-personalize-delegate.md`](./visit-personalize-delegate.md)
- [`visit-resolve-identity.md`](./visit-resolve-identity.md)
- [`voyager-api-client.md`](./voyager-api-client.md)
- [`voyager-dual-collection-sort.md`](./voyager-dual-collection-sort.md)
- [`voyager-filter-grammar-serializer.md`](./voyager-filter-grammar-serializer.md)
- [`voyager-graphql-queryid.md`](./voyager-graphql-queryid.md)
- [`voyager-mutation-endpoints.md`](./voyager-mutation-endpoints.md)
- [`voyager-pagination.md`](./voyager-pagination.md)
- [`voyager-password-auth-metadata.md`](./voyager-password-auth-metadata.md)
- [`voyager-search-facet-encoding.md`](./voyager-search-facet-encoding.md)
- [`voyager-star-pointer-joins.md`](./voyager-star-pointer-joins.md)
- [`warm-browser-service-contract.md`](./warm-browser-service-contract.md)
- [`warm-scraper-http-recovery-ladder.md`](./warm-scraper-http-recovery-ladder.md)
- [`websocket-dispatch-ledger.md`](./websocket-dispatch-ledger.md)
- [`wheel-bracketed-topcard-reader.md`](./wheel-bracketed-topcard-reader.md)
- [`working-hours-interval-algebra.md`](./working-hours-interval-algebra.md)
- [`wvmp-guarded-descent.md`](./wvmp-guarded-descent.md)
- [`yaml-entry-gate-none-tolerant-defaults.md`](./yaml-entry-gate-none-tolerant-defaults.md)
- [`zombie-browser-teardown.md`](./zombie-browser-teardown.md)
