---
name: growchief-foundation
description: "Use when building durable multi-account outreach automation schedulers or browser-supervised social-media bots — Growchief outreach-automation contracts: per-bot durable Temporal throttler with signal-driven queues, restriction defer-and-swap, seven-way browser race supervision, storageState session persistence, response-interception extraction, provider @Tool registry, and the delay/repeatJob/endWorkflow outcome vocabulary."
---

# Growchief: Durable outreach-bot orchestration foundation

## Use this for
Use when building durable multi-account automation schedulers (Temporal or equivalent), browser-supervised social-media bots with human-like input, rate-limit/restriction handling with persistent ledgers, email→profile enrichment failover ladders, or any system where jobs must survive restarts while respecting per-account daily gaps, working hours, and platform-imposed limits. Source code and direct tests are ground truth; references carry decisive excerpts and graph retrieval. AGPL-3.0 source: learn patterns and contracts only, never copy code verbatim.

## Load the matching source dump
- `references/per-bot-durable-throttler.md` — how does one long-lived workflow serialize all actions for one account without dropping queued work across restarts?
- `references/throttler-queue-sorting-ladder.md` — how does a priority queue avoid both starvation and deterministic thundering herds?
- `references/restriction-defer-swap.md` — when the head job hits a platform limit, how does the queue keep other action types flowing?
- `references/throttler-continue-as-new-economics.md` — where does an infinite workflow cut its event history, and why the 1-tick sleep?
- `references/working-hours-signal-invalidation.md` — how does a sleeping workflow learn that working hours changed mid-day?
- `references/step-completion-protocol.md` — how do sequential campaign steps execute through a shared per-bot queue without a distributed lock?
- `references/campaign-enrichment-bifurcation.md` — how does one lead fan out to known-URL nodes instantly and unknown-email nodes after enrichment?
- `references/enrichment-provider-failover.md` — how do multiple paid providers share load with per-provider backoff and per-item memory of failures?
- `references/browser-run-race-heptathlon.md` — what seven concurrent watchers supervise one browser job, and who wins?
- `references/job-time-budget-heartbeat.md` — how do hard job deadlines, activity heartbeats, and kill broadcasts compose?
- `references/storage-state-session-persistence.md` — when is login state saved, and which outcomes must NOT save it?
- `references/progress-response-vocabulary.md` — what does the single job-outcome triple {delay, repeatJob, endWorkflow} mean at every return site?
- `references/humanization-kit.md` — what makes clicks/typing human-like, and which parts are actually live vs dead code?
- `references/cdp-stack-freeze-pass.md` — how can page JS be hardened against stack-based CDP leak checks (and is it wired)?
- `references/pausing-page-proxy.md` — how would a Page object gain pause/resume + audit logging via Proxy, and why is it disabled here?
- `references/provider-tool-contracts.md` — how do decorator metadata turn provider methods into a portable tool/plugin catalog?
- `references/response-interception-extraction.md` — how are authenticated JSON APIs mined via network interception plus in-page fetch replay?
- `references/account-limited-restriction-ledger.md` — how do page-text limit banners become dated ledger rows that gates consult later?
- `references/temporal-multitenant-control-plane.md` — how do search attributes, deterministic IDs, and listWorkflows queries form the admin control plane?
- `references/x-provider-vision-engagement-funnel.md` — how does a provider turn a live feed into exactly ONE AI-chosen engagement without repeating past actions?
- `references/ai-prompt-contract-trio.md` — what reliability contract wraps each LLM call: strict allowlist, retry-blind comment, fail-open vision caption?
- `references/degree-gated-action-guards.md` — where is follow-graph state enforced so tools never act on ineligible leads?
- `references/duplicate-send-similarity-guard.md` — why is "already messaged" decided by fuzzy compare over the visible thread, not the database?
- `references/feed-scroll-candidate-ladder.md` — how does feed automation reconcile an API-snapshot choice with what is actually rendered on screen?
- `references/enrichment-twin-workflow.md` — how does the singleton enrichment queue splice mid-flight and keep provider clocks honest across snapshots?
- `references/random-plugs-ambient-workflow.md` — how does ambient activity share one account queue with campaigns via priority −1 ghost enqueues?
- `references/campaign-fan-out-join.md` — what join contract (identifier echo, random child ids, per-child search attributes) keeps parallel leads coherent?
- `references/provider-registry-activation.md` — how do plugin-style provider lists become runtime-filtered, name-addressable activity catalogs?
- `references/voyager-payload-extraction.md` — how are profile identities parsed from authenticated-API payloads without brittle DOM scraping?
- `references/follow-button-detach-handshake.md` — what wait-ladder turns a fragile UI click into a confirmed state transition?
- `references/queue-retraction-signals.md` — how do queued-but-not-run jobs get retracted when a campaign node/workflow is edited or deleted mid-flight?
- `references/auth-org-context-middleware.md` — how does one middleware resolve user + active organization (+ superadmin impersonation) per request?
- `references/api-key-org-principal-auth.md` — how does a raw API key become an organization principal, and when does the billing gate apply?
- `references/email-transport-funnel.md` — why does one email path drain through a durable singleton while the other sends direct, and who picks the provider?
- `references/bot-status-projection.md` — how does one honest bot status emerge from DB rows, restriction ledgers, workflow handles, and live queue queries?
- `references/logout-freeze-backchannel.md` — how does a mid-run logout detection freeze every queued job without dropping them, and what un-freezes?
- `references/step-details-reporting-join.md` — where does the "current step" name come from when projecting bot status?
- `references/notification-org-fanout.md` — how does one backend event become a per-user inbox row (and optional email) for a whole organization?
- `references/throttler-replay-patching.md` — how does an immortal workflow adopt new behavior without corrupting in-flight histories?
- `references/campaign-launch-guards.md` — what stands between "user clicked start" and a Temporal run, and who owns retries?
- `references/lead-ingestion-dual-lane.md` — how does one "upload leads" call become per-account campaign jobs or search-lead scrapes?

## Capsule map
- **Orchestration** — `per-bot-durable-throttler`: one immortal workflow per bot owns a mutex-guarded queue; GAP_MS gap enforced durably via `nextAllowedAt` carried across `continueAsNew`.
- **Orchestration** — `throttler-queue-sorting-ladder`: priority asc → stable random weight per workflowId → date asc → id tiebreak.
- **Orchestration** — `restriction-defer-swap`: blocked head job either waits alone (condition with deadline) or rotates another action type to the front.
- **Orchestration** — `throttler-continue-as-new-economics`: snapshot at empty-or-50-deep; `await sleep(1)` lets late signals join before snapshot.
- **Scheduling** — `working-hours-signal-invalidation`: cached hours + refetch flag invalidated by `workingHoursUpdated`; condition wakes early on signal else at window open.
- **Orchestration** — `step-completion-protocol`: campaign signals `enqueue`, throttler answers `stepCompleted(stepId)` only on final success; `cancelAll` cancels a CancellationScope.
- **Orchestration** — `campaign-enrichment-bifurcation`: found leads → immediate child bot-jobs; missing leads → singleton enrichment workflow + per-step signal await.
- **Enrichment** — `enrichment-provider-failover`: per-provider `limitsDelay`, per-item `testedProviders`, 429→delay backoff, credit-zero silent drop.
- **Browser** — `browser-run-race-heptathlon`: scope-escape, runner, logout watcher, process timer, proxy watcher, limit poller raced; losers leak forever by design.
- **Browser** — `job-time-budget-heartbeat`: outer `Promise.any` with heartbeat-only-when-activity, AbortController fan-out, `killEverything$` broadcast; observable mode returns screenshots stream instead.
- **Browser** — `storage-state-session-persistence`: storageState loaded unless `login`; saved only on clean outcomes inside an upsert transaction keyed by platform+internalId.
- **Contracts** — `progress-response-vocabulary`: `{delay, repeatJob, endWorkflow}` semantics at all 12+ return sites incl. repeat-cap 3 and no-save paths.
- **Browser** — `humanization-kit`: ghost-cursor bezier from remembered position, scrollend races, cookie polling; typing typo engine is DEAD CODE (write() = plain keyboard.type).
- **Browser** — `cdp-stack-freeze-pass`: init script locks Error.prototype.stack + freezes instances; NOT invoked anywhere at this pin — adopt the technique, wire it yourself.
- **Browser** — `pausing-page-proxy`: Proxy wrapper adds pause-gate + retry + method logging to async Page calls; currently bypassed by an early `return page`.
- **Extensibility** — `provider-tool-contracts`: @Tool/@Plug decorators append to prototype metadata arrays; getTools/getPlugins expose catalogs, serializing RegExp as {source, flags}.
- **Extraction** — `response-interception-extraction`: waitForResponse(queryId) captures request, in-page fetch replays it with rewritten pagination, DOM twin raced as fallback.
- **Evasion-adjacent** — `account-limited-restriction-ledger`: body-text banner table → weekly restriction rows (`until` = end-of-week+2d UTC) consulted before every dispatch.
- **Ops** — `temporal-multitenant-control-plane`: boot-time search-attribute registration, deterministic workflow IDs, listWorkflows-driven signal/terminate fan-out.
- **Engagement** — `x-provider-vision-engagement-funnel`: DOM-harvest × API-relationship join → ledger dedupe → vision captions → policy gate → ONE shuffled pick → act → persist.
- **AI** — `ai-prompt-contract-trio`: subjectsAi strict-mode allowlist (all-allowed short-circuit), commentAI 3× retry-or-undefined, pictureToText fail-open `''`; `random()` template lines resolved per generation.
- **Guards** — `degree-gated-action-guards`: pure extractors normalize degree/pending (LinkedIn defaults optimistic to 1, X pessimistic to 0); tools early-return endWorkflow triples.
- **Messaging** — `duplicate-send-similarity-guard`: dice-coefficient ≥0.95 over rendered thread texts ⇒ skip with endWorkflow:false (campaign continues).
- **Engagement** — `feed-scroll-candidate-ladder`: visible-selector map, bail-if-unrendered, shift-until-selected scroll walk; ≤5 JSON refetches with bottom-scroll.
- **Enrichment** — `enrichment-twin-workflow`: reverse-index batch splices under mutex; provider clocks reseed on length≠provider-count; snapshot at empty-or-200.
- **Ambient** — `random-plugs-ambient-workflow`: random plug as priority −1 ghost enqueue (`'ignore'` id suffix, ignoreLead), stepCompleted join, uniform 20–60 min jitter, >50-run snapshot.
- **Orchestration** — `campaign-fan-out-join`: per-node TypedSearchAttributes + makeId child ids + ABANDON parenting; per-step condition-join on enrichment identifier echo.
- **Extensibility** — `provider-registry-activation`: module-singleton priority-sorted lists; lazy env-getter apiKey = activation; string-name addressing across the activity boundary (recurs in billing/proxies).
- **Extraction** — `voyager-payload-extraction`: $type-suffix matching, widest-artifact vectorImage pick, URN-tail ids, lenient optional chains, start-offset replay pagination.
- **Browser** — `follow-button-detach-handshake`: visible-wait → settle timer → humanized click → physics-matched post-proof (detached for toggles, disabled for sends).
- **Orchestration** — `queue-retraction-signals`: edit emits per-node, delete emits per-workflow retraction signals (incl. the literal `enrichment` handle); reverse-index descending splice under the SAME enqueue mutex; terminate tail only for whole-workflow deletes.
- **Auth** — `auth-org-context-middleware`: verifyJWT → superadmin viewas impersonation (flag preserved) → activated gate → password strip → DB-derived non-disabled memberships → showorg hint else first; ONE broad catch = 401; dead `!organization` guard documented.
- **Auth** — `api-key-org-principal-auth`: raw Authorization key → `getOrgByApiKey`; subscription gate exists iff `STRIPE_SECRET_KEY` set; synthesized `SUPERADMIN` org member; json-401 vs thrown-403 asymmetry.
- **Contracts** — `email-transport-funnel`: provider selected once at boot (`resend|nodemailer|EmptyProvider`) with boot-time env-key audit; async path signals durable `'send-emails'` singleton, sync path direct; BOTH share fail-soft @/sender-env guards.
- **Ops** — `bot-status-projection`: DB-first merge order (hours defaults → restrictions → describe on derived throttler id → silent tenant denial → live `botJobsQueries` head-job join); each plane degrades independently.
- **Orchestration** — `logout-freeze-backchannel`: `botLoggedSignal(false)` fanned from both logout branches, `true` after login persists storage; per-iteration DB re-sync makes the signal a push and the column the truth; deadline-less `condition(() => active && logged)` gate = freeze-not-drop.
- **Ops** — `step-details-reporting-join`: triple tenant filter + soft-delete guard re-resolves the live step; user-authored `data.label` wins (derived `_getStepDisplayName` is commented out = dead scaffolding).
- **Ops** — `notification-org-fanout`: list org members once → concurrent inbox rows (`Promise.all`) → sequential provider emails; throttler reaches it only via a patched-gated Temporal activity, API side calls direct.
- **Orchestration** — `throttler-replay-patching`: every post-deploy side effect in an immortal workflow is history-gated by `patched('<id>')`; the date string is a patch name, not logic.
- **Orchestration** — `campaign-launch-guards`: exists→active guard ladder returns typed envelopes (not throws); multi-run ids with random suffixes; `maximumAttempts:1` pushes retry ownership into the queue's outcome vocabulary; pause = DB flag gating future starts only.
- **Ingestion** — `lead-ingestion-dual-lane`: per-account lane choice driven by provider regex metadata (`urlRegex` vs `searchURL.regex`); campaign lane org-keyed, scrape lane triple-keyed; `importURLList` advertises the same regexes as `{source, flags}` data.

## Extending the foundation
Add one `references/<seam>.md` capsule for one graph-selected, source-confirmed porting question. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf.

## Provenance
growchief (AGPL-3.0), `main@abb1e37a6f5595d8d105aef5871a2eeb0c22a1dc` (= base_sha, HEAD unchanged since index); Codebase Memory project `growchief` — root `/mnt/hdd/utopia/inspo/growchief` (canonical worktree `/mnt/hdd/utopia/inspo/platforms/growchief`), branch main, full mode, 2,567 nodes / 7,146 edges, indexed 2026-08-23T00:00Z, parse_partial = 2 files (frontend working-hours.component.tsx :182; var/docker/nginx.conf), skipped = 0, not_indexed = .git + 17 image/suffix files by design. Pass 3 (pin unchanged): index_status re-verified head = base_sha = git HEAD `abb1e37a…`, ready, 2,567 nodes / 7,146 edges; `check_index_coverage` returned `no_recorded_issue` + `metadata_match` for all 12 newly cited paths.

## Full view (memory graph)
Revalidate `growchief` before porting: run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, and `get_code_snippet`. Pass 1 (this wave): `index_status --project growchief --verbose` confirmed HEAD `abb1e37a…` = base_sha, mode full, generation 2026-08-23T00:00:18Z. stdin-JSON `check_index_coverage` over all 30 cited source paths returned `no_recorded_issue` + `metadata_match` ×30. Live `search_graph` probes resolved every seam line-exact: "userWorkflowThrottler" → workflow.throttle.ts:101-404, "sortFunction" → :76-99, "ensureWithinWorkingHours" → working.hours.manager.ts:33-53, "createPageWrapper" → pausing.page.ts:10-66, "_checkForLoginElement"/"_isProxyUnavailable"/"_maximumJobTime" → bot.tools.ts:132-157/:37-67/:115-129, "LinkedinProvider.accountLimited/connectionRequest/leadList" → linkedin.provider.ts:137-158/:297-477/:66-135, "workflowEnrichment" → workflow.enrichment.ts:31-198, "botJobsQueries" Variable → bot.jobs.queries.ts:9-11, "saveRestriction/getStepRestrictions" ×3 layers each, "extractConnectionTarget" → extra.person.profile.ts:1-34, "mapNodes" → workflows.service.ts:31-44. Pass 2 (this wave, pin unchanged): live `search_graph` resolved every NEW seam rank#1 line-exact: "likeAndCommentOnAPost pictureToText" → x.provider.ts:294-500 + picture.to.text.ts:6-57, "subjectsAi getAllowedSubjects structured output" → bot.cursor.ts:261-268 + subjects.ai.ts:14-71, "commentAI generateComment parseComment" ×3 → comment.ai.ts, "extractUserData degree relationship" → extract.person.profile.ts:8-36, "createCursor setDataVariables getData", "checkActions savedActions findMany" → bots.repository.ts:76-122 (+ schema.prisma SavedActions), "workflowPlugs random plug enqueue throttler" → workflow.plugs.ts:29-134, "enrichments activity providerList apiKey filter" → enrichment.activity.ts:16-18, "extractMyProfile bestVectorUrl vectorImage" ×2, "giveLike giveComment shuffle reactions" → linkedin.provider.ts:990-1024/:1026-1067, "workflowEmail queue mutex shift" → workflow.email.ts:24-43 (mined as pattern-twin only), "workflowCampaign startChild botJobs search attributes" → workflow.campaign.ts:42-185. NO upstream test suite exists (zero *.spec/*.test files) — every Probe below is a deterministic source/grep pin with an explicit coverage caveat. AGPL: mine behavior contracts; never lift code verbatim.

Pass 3 (this wave, pin unchanged): live `search_graph` resolved every NEW seam rank#1 line-exact: "removeNodesFromQueueByNodeIdSignal" → throttle :148/:163 + workflows.service :293/:321/:341, "AuthMiddleware use showorg viewas" → auth.middleware.ts:15-64, "getOrgByApiKey PublicAuthMiddleware STRIPE_SECRET_KEY SUPERADMIN" → public.auth.middleware.ts:9-36, "sendEmailSync selectProvider EmptyProvider signalWorkflow send-emails" → email.service.ts:9-137 + temporal.client.subscription.register.ts:8-31, "getBotStatus botJobsQueries" → bots.service.ts:425-510 (:467 tenant check read directly, :471 live query), "botLoggedSignal loggedOut saveStorageAndActions" → bots.service.ts:214-251/:281-304 (signal sites :230/:295) + throttle :133/:137/:242/:259. Upstream test count re-verified ZERO (find '*.spec.ts'/'*.test.ts' = 0) — every Probe stays a deterministic source/grep pin.

## Boundaries
Adopt the pure contracts: per-account durable queue choreography (signals, gap carry-over, continueAsNew snapshots), the restriction ledger gate, the outcome vocabulary, provider failover economics, the race-supervision shape, storageState save/skip policy, decorator-metadata tool registries, interception-then-replay extraction, the engagement funnel order (harvest→dedupe→gate→pick-one→persist), per-call-site LLM failure semantics, degree/pending guard ladders, render-truth message dedupe, ghost-priority ambient enqueues, identifier-echo joins. Adapt host-specific mechanics: Temporal specifics (signals/queries/search attributes) to your scheduler, patchright/ghost-cursor to your driver, Prisma repos to your store, zod+LangChain AI plugs to your LLM stack. Omit product surface: LinkedIn/X selector tables and voyager queryId hashes (rotating product secrets), Stripe billing flows, NestJS controllers/frontend UI, brightdata proxy vendor plumbing, the hardcoded postiz.com notification URL, workflow.email.ts send mechanics (pattern-twin of mined queue shapes only). Never copy code verbatim (AGPL-3.0) — these capsules document behavior to reimplement independently.

## Pass-2 sweep ledger (pin unchanged)
Mined pass 2: both providers whole-file (linkedin.provider.ts 1,067L incl. giveLike/giveComment tails; x.provider.ts 501L), all extractor twins, plugs/ai/* (6 files whole), bot.cursor.ts + bot.tools.ts + bot.manager.ts residual planes (_isProxyUnavailable/_navigateOutSideOfScope/_scaleCoordinates/_checkForLoginElement/_functionToRun/_findRestrictions/_unsubscribeAll — folded into heptathlon/vocabulary capsules where pre-existing), enrichment providers ×4 + interface + provider.list, enrichment/campaign/plugs/email/bot.jobs workflows whole, savedActions/checkActions repository plane, working.hours.manager + time.functions (already-mined seams re-verified). OMITTED-WITH-REASON pass 2: shared/server/upload/* storage abstraction (thin S3/local interface, no porting tension beyond obvious), proxies/providers/brightdata.provider.ts internals (standing vendor omission), apps/backend controllers/auth surface (NestJS product shell, standing boundary), frontend/ entirely (product UI), workflow.subscription.deactivate.ts (billing lifecycle below porting bar), typing.tool internals beyond humanization-kit's dead-code finding (re-verified unchanged).

## Pass-3 deep pass (pin unchanged)
Mined pass 3 (uncited backend/transport planes, 6 new capsule-v2 refs): workflows.service edit/delete queue-retraction fan-out + throttler reverse-index splice handlers; AuthMiddleware org-context resolution with superadmin viewas impersonation; PublicAuthMiddleware API-key org-principal plane with existence-conditional billing gate; EmailService dual-path transport funnel + 'send-emails' boot registration + provider interface; BotsService.getBotStatus cross-plane status projection ladder; loggedOut/saveStorageAndActions bidirectional logout freeze back-channel. OMITTED-with-reason pass 3: brightdata zone internals (standing vendor omission; only a tenant-scoped assignProxy updateMany sits behind it), storage upload abstraction (re-examined: LocalStorage date-partitioned public mount vs Cloudflare R2 public-read object share one result shape — no non-obvious tension; consciously not padded into the batch), frontend/analytics/teams product shell, typing.tool internals (dead-code finding stands), subscription-deactivate choreography (already summarized in temporal-multitenant-control-plane Flow). Wave/module history: /mnt/hdd/utopia/inspo/growchief-work/{state,research,verification}.md.

## Pass-4 deep pass (pin unchanged)
Mined pass 4 (backend kernel tail, 5 new capsule-v2 refs): step-details-reporting-join (BotsRepository.getWorkflowStepDetails triple-filter join + dead _getStepDisplayName), notification-org-fanout (list-once → concurrent rows → sequential emails, dual dispatch planes), throttler-replay-patching (patched() history gates inside the immortal throttler), campaign-launch-guards (exists→active envelopes, multi-run ids, retry maximumAttempts:1, pause-vs-cancel), lead-ingestion-dual-lane (per-account provider-regex lane choice, org vs triple-keyed search attributes, importURLList {source,flags} advertising). A prior invocation of this pass was interrupted after persisting three of the five references; the completing run re-derived both missing capsules from fresh MCP evidence chains and wired all five loader/map entries. OMITTED-with-reason pass 4: EncryptionService (static bcrypt cost-10 wrappers + AUTH_SECRET JWT without expiry — textbook wrapper, no porter question), ProxiesManager (brightdata CRUD shell behind standing vendor omission), analytics/newsletter/organizations services (product shell), frontend entirely (standing), leads.service dedupe internals (only worth a capsule if a porter needs the exact uniqueness columns — recorded as next-pass candidate). Wave/module history: /mnt/hdd/utopia/inspo/growchief-work/{state,research,verification}.md.
