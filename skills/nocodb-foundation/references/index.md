<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->


# NocoDB Foundation

## Use this for
Use when building job-queue infrastructure, background-job processors, versioned one-time migration jobs, worker/primary instance coordination over pub-sub, DB-transaction-safe cache bypass, streaming CSV/JSON/Excel imports with deferred link resolution, streamed exports with charset fallback and concurrent upload, thumbnail generation under memory caps, or orphaned-file garbage collection. Source code and direct tests are ground truth; references carry decisive excerpts and graph retrieval.

## Load the matching source dump
- `./fallback-queue.md` — the in-memory Bull-compatible queue: PQueue-backed execution, cron repeat scheduling, delay timers, idempotent jobId, reset().
- `./jobs-processor.md` — the Bull processor admission gate: unknown-job/version-mismatch/local-concurrency requeue, exponential backoff budget, long-run watchdog.
- `./jobs-service.md` — the add() contract: circular-safe payload, nc_jobs meta row upsert, `_jobVersion` stamping, SKIP_STORING_JOB_META fast path.
- `./jobs-events.md` — status/log event fan-out: pub-sub result publish + DB status update + in-process EventEmitter, terminal-state dedup.
- `./jobs-polling.md` — long-poll job status endpoint: per-job listener rooms, `_mid` incremental message log, 30s refresh, closed-job tombstone.
- `./migration-jobs.md` — versioned one-time migration runner: single-instance lock, 5s confirm, stall watchdog, one-migration-per-run cascade.
- `./cache-bypass.md` — AsyncLocalStorage cache-bypass scope: read/write no-op inside DB transactions, invalidation/counters pass through.
- `./redis-jobs-service.md` — the Redis/Bull enqueue path: lazy primary pause/resume via workerCount(), meta-row id == Bull job id, removeOnFail ring.
- `./instance-command-bus.md` — primary↔worker pub-sub command bus: colon-packed frames, callback-map dispatch, publish-ack worker counting.
- `./cache-envelope.md` — `{value, parentKeys, timestamp}` envelope: back-pointer writes, list-TTL skew (-60s), grace-TTL lazy refresh.
- `./cache-list-selfheal.md` — list corruption self-heal: NONE sentinel semantics, null-mget ⇒ deepDel parents, empty-vs-miss contract.
- `./cache-gate-matrix.md` — which ops stop under disable/bypass and which pass through: invalidate+counters always; lock-style setIfNotExist bypasses both gates.
- `./import-streaming.md` — parser→AsyncQueue→async-generator bridge with held-resume backpressure and swap-before-await 1000-row flushes.
- `./import-error-ladder.md` — batch-failure classification: typed row-level errors retry one-by-one, system failures abort after a budget of 20.
- `./import-ltar-links.md` — deferred link phase: threshold flushing, self-reference detection defers all flushing, negative-caching display-value resolution.
- `./import-column-mapping.md` — scalar/link destination split, create-column dedup keyed on TITLE only, per-field failure isolation.
- `./import-dual-mode.md` — core run() plus thin job(): synchronous callers get typed results; no-op default logger; error sanitization at the boundary.
- `./import-handler-registry.md` — two-method parser interface (preview/streamRows) with eager registry and badRequest on unknown types.
- `./import-type-detection.md` — capped string-sample column typing boundary: handlers collect ≤500 string rows + header synthesis; detection sees original cell text only.
- `./csv-detector-stub-contract.md` — detectColumnTypes* are deliberate no-op stubs at f7513664 (all-SingleLineText out); the heuristic helpers below them are unreachable dead code — never reconnect them.
- `./import-progress-frames.md` — machine-readable progress/completed JSON frames with cumulative counters riding the prose log stream.
- `./import-error-formatter.md` — two-tier error attribution: batch-level generic, single-row retry passes the row for value→column matching.
- `./import-attachment-stream.md` — temp-file stream lifecycle: finally-block cleanup, warn-only failure, sync-caller opt-out flag.
- `./jobs-log-fanout.md` — emit-only log micro-service: string-keyed ids, fire-and-forget EventEmitter2, dual sendLog+debugLog closure.
- `./export-stream-upload.md` — concurrent pump/upload race: consumer starts first, shared error var thrown post-join, push(null) on producer error.
- `./export-charset-fallback.md` — bounded-decision charset Transform: round-trip lossiness probe within 64KB, whole-file UTF-8+BOM fallback, single stateful encoder.
- `./export-cleanup.md` — date-folder GC: lexicographic YYYY-MM-DD compare on path segments, serialized deletes, resolve-after-onIdle.
- `./export-presign.md` — deterministic dated filenames, uniform 3h presign for local-path vs remote-url branches, no encoding for binary xlsx.
- `./thumbnail-bomb-guard.md` — format-aware input pixel caps checked from cheap header metadata before decode; finite limitInputPixels backstop; clone-per-size.
- `./thumbnail-batch.md` — result-mapped batch thumbnails keyed to original references; sharp-absent degrades to empty results; two-source path derivation.
- `./attachment-orphan-gc.md` — HAVING COUNT(*)=COUNT(deleted) orphan scan, adapter ownership check, blobs-before-meta deletion order.
- `./duplicate-compensation.md` — serialize-in-dependency-order (interfaces last), threaded idMap, softDelete base / delete tables compensation split.
- `./duplicate-backfill.md` — external belongs-to backfill: field-filtered re-export, async header mapping with parser pause/resume, bulk-update id rewrite.
- `./export-idmap.md` — the accumulating source→dest id map created by importModels and extended by every later phase importer.
- `./import-link-stream.md` — mm links as side-channel CSV stream with cross-model handledMmList dedup; links strictly after data rows.
- `./at-import-pipeline.md` — Airtable import phase order (schema→data→links) over one lexical-scope state bundle; staged lookup/rollup materialization.
- `./at-import-failure-policy.md` — two-tier failure policy: schema-phase throws delete this run's tables then rethrow (EE audit pair shares parentAuditId); data-phase errors are swallowed inside syncData; module-scoped namesRef reserves names across jobs; process-shared concurrency-1 attachment queue.
- `./import-dual-listener-fanout.md` — readAndProcessData choreography: one paused Readable, row+link consumers attached before resume, ONE shared PQueue, swallow-and-resume error policy, onIdle→flush→await-ltarPromise termination ladder.
- `./import-record-coercion-ladder.md` — nocoBaseDataProcessing_v2: alias rewrite → falsy gate with virtual-column exception → per-uidt coercion switch → ncRecordId/ncRecordHash stamp; SingleSelect empty-string quirk documented.
- `./import-virtual-column-resolution.md` — staged link/lookup/rollup materialization: mm-pair minting with `_2` rename anchor, hm-direct vs mm-inverted fk matching, progress-guarded fixpoint over deferred columns, rollup function whitelist.
- `./import-view-config-family.md` — grid/form/gallery creators over fetched view JSON: default-view reuse, nested-filter skip, select-option id remap through sMap, sys-columns force-hidden behind imported order, gallery content deliberately dropped.
- `./airtable-backpressure.md` — dual-gate backpressure: remote paging pauses at 100 unconsumed records (hysteresis /2), local queue gate at 20, byte-aware batching.
- `./airtable-id-remap.md` — first-sight integer id assignment shared by data and link phases so forward references resolve deterministically.
- `./airtable-assoc-dedup.md` — junction-table claiming via insertedAssocRef before meta resolution; mirror-column skip; 500-tuple flush.
- `./entity-map.md` — in-memory SQLite mapping store: dynamic ALTER TABLE columns, JSON:: tagged object values, case-folded key encoding.
- `./airtable-fetch.md` — shareId session layer: initialize-once/read-many, exp-template branch, fetch stats feeding perf counters.
- `./airtable-type-map.md` — explicit remote→UITypes literal table; unmapped types log-and-fallback; deliberate collapse pairs (count→Rollup, barcode→text).
- `./import-naming.md` — dual-track naming: sanitized/truncated column_name vs unique display title, table-scoped generators, alias registry.
- `./webhook-decoupling.md` — id-only webhook payloads rehydrated at execution; missing entities quiet-skip; addJob re-injection for hook recursion.
- `./use-worker-rpc.md` — name-based allowlisted service RPC: {service, method, args} envelope with __original decorator unwrap to prevent re-enqueue loops.
- `./source-create-compensation.md` — error-in-result contract → baseDelete the half-created source then rethrow; strip config secrets from meta sources.
- `./source-delete-idempotent.md` — minimal delegate-to-idempotent-service processor; no TOCTOU pre-checks; original req carried for attribution.
- `./meta-sync-broadcast.md` — complete-then-broadcast with socket-id exclusion; preview/apply split via diff-only sibling method.
- `./attachment-url-upload.md` — thin processor delegating URL fetch to service; payload self-sufficient; this type IS meta-stored.
- `./jobs-module-wiring.md` — single 'JobsService' DI token swaps CE fallback / EE Redis behind IJobsService; processors stay plain classes.
- `./export-column-projection.md` — server-composed `_fieldIds` projection intersected with model columns; BELONGS_TO link columns rewritten onto their FK child via btMap.
- `./export-cell-serialization.md` — per-uidt cell normalization (attachment JSON, user→emails, formula/lookup dropped, `__nc_empty_string__` sentinel) + CWE-1236 formula-escape scoped to text cells/header on user-facing exports only.
- `./export-recursive-stream.md` — offset/limit recursion over a Readable: single `push(null)` at the true last page, first-page-only header, wrote-aware header threading on the mm side stream.
- `./import-csv-stream.md` — papaparse step → idMap header translation (BT→FK child), 1000-row pause/resume bulk insert, `skipDuplicates` object-hash, best-effort pg sequence realignment after verbatim-id inserts.
- `./import-link-buckets.md` — mm link rows bucketed per junction table with lazy first-sight column resolution, flush-on-new-column, threaded handledLinks dedup.
- `./migration-job-example.md` — nc_job_008 as a worked versioned-runner instance: batched meta-scan loop with in-flight guard, dialect-aware rename SQL, collision-avoiding suffixes, physical+meta write cohesion.
- `./export-composite-ids.md` — positional `::`-joined hierarchical export ids: base→source→model→child segments minted in walk order, late-bound fk_target_view_id mints on demand.
- `./export-col-options-policy.md` — four-class colOptions rewrite (remap fk_*/preserve workspace keys/transform formulas+output_column_ids/strip identity) with dual formula representation and pg cdf quote-trim.
- `./export-view-extras.md` — per-view-type extras remap: kanban double-keyed meta stacks, calendar/timeline range twins, gantt date dependencies, list levels; type-gated with map-or-null optional endpoints.
- `./export-dashboard-widgets.md` — dashboards export through a polymorphic widget-handler contract carrying the live idMap; exporter owns the generic widget-filter envelope only.
- `./export-json-excel.md` — JSON/Excel format forks: stream-framed array vs accumulated whole-buffer workbook; view-visible field derivation; typed-cells-no-CWE-1236-escape rule.
- `./export-ics-calendar.md` — paged VEVENT streaming: property-per-column assembly, anonymous visible-columns intersection, empty-pk UID guard with offset-index fallback.
- `./export-base-fanout.md` — whole-source export: schema.json + per-table data CSVs + one tee'd merged link stream; shared handledMmList dedup; post-join error rethrow.
- `./migration-trash-backfill.md` — nc_job_012: temp-table resume ledger (failed=visited), DISTINCT-tuple delete-event reconstruction, onConflict-ignore bulk inserts, original-date retention math.
- `./migration-orphan-links.md` — nc_job_013: JOIN-based orphan scan gated on the OWNING column's soft-delete state (restore-pending vs garbage), dependents-before-link reaping, dialect early-exit.
- `./migration-ee-ce-skew.md` — EE/CE versioned-migration skew: same version slot runs different services per edition via DI-time isEE selection over no-op fillers.
- `./migration-searchpath-grandfather.md` — nc_job_015 grandfathering: pin pre-fix behavior INTO rows via a pure four-null decision fn, keyset walk with creation-time race bound, update-then-resetSource pairing.
- `./keyset-backfill-walk.md` — id-cursor keyset batching (not OFFSET), per-page JOIN hydration, startedAt bound required only because nanoid ids are random.
- `./migration-bootstrap-ordering.md` — migrations enqueue as the FIRST queue item, last line of onModuleInit, after RESUME/PAUSE_LOCAL callbacks register.
- `./migration-ce-stub-parity.md` — nc_job_016 CE no-op stub: edition-paired empty slots keep version numbering aligned across forks.
- `./ncmeta-now-contract.md` — client-aware meta timestamps: never bind raw Dates against nc_meta string times (SQLite storage-class comparisons silently false).
- `./connection-reset-protocol.md` — delete-ref-THEN-bump-and-sync ordering, lazy staleness checks, and which callers may bump without local reset.
- `./config-merge-override-discipline.md` — getConfig ladder: decrypt → presence-gated merge → post-validate searchPath → single mssql-normalize funnel.
- `./presence-gated-override.md` — the general contract the partialExtract bug violated: absent keys mean inherit, never erase.
- `./driver-coercion-funnel.md` — tedious strict typing normalized at one chokepoint; conservative 'true'/'false' flipping; defaults never override explicit values.
- `./source-update-order-preservation.md` — both-sides-missing order backfill with order-1 reserved for the default source.
- `./jobs-dispatch-table.md` — JobsMap name→{instance, fn} injectable registry with default-method fallback and requeue-on-miss admission.
- `./jobs-relay-write-side.md` — sendJobStatus/sendJobLog: 20-message replay ring, monotonic `_mid`, refresh-hint-only pub-sub, finally-block leak-proof cleanup.
- `./jobs-listen-shutdown-flush.md` — onModuleDestroy answers every parked long-poller with one refresh hint before sockets die.
- `./export-import-rest-trigger.md` — anchor-derived ids (viewId→fk_model_id), controller-owned transport defaults (BOM), handle-only acks.
- `./export-data-processor-v2.md` — DataExport job wrapper: orchestration-only split, Excel encoding exclusion, non-Error identity envelope.
- `./export-cleanup-job-lifecycle.md` — scan-stream→serialized-delete-queue with segment-search date parse; producer shipped disabled upstream.
- `./repeat-job-registration-gap.md` — registration ≠ scheduling: identical commented producers in BOTH queue services mark intentional deprecation.
- `./import-result-aggregation.md` — DataImport wrapper samples the first error across per-table results while full lists flow through the job value.
- `./duplicate-granularity-dispatch.md` — method-per-granularity routing over flag-switching; ids-only payloads; fast-ack controllers.
- `./metasync-dual-method-bodies.md` — sync vs diff bodies: 'all' string sentinel, socket-excluded broadcast on mutation, diff returns the changeset.
- `./source-create-delete-processors.md` — error-in-result compensation ladder (delete-then-rethrow), meta-config scrub, attribution fallback chain.
- `./webhook-rehydration-guards.md` — missing hook/model = log + successful return; addJob injected into invokeWebhook for recursion.
- `./internal-event-job-enqueue.md` — webhook/url-upload jobs enqueue through service-layer emit-handlers on the SAME queue surface as REST.
- `./url-upload-passthrough.md` — the 15-line processor: why pass-through delegation deserves its own JobType.
- `./thumbnail-processor-path-algebra.md` — dual-shape path derivation (download/-strip vs URL-extract), read-key ≠ write-key normalization, sharp-absent degradation.
- `./upload-error-triage.md` — log-everything/throw-first-typed with instanceof whitelist masking foreign errors as generic 500s.
- `./baseuser-roster-query.md` — three-table roster join: inner-workspace/left-base asymmetry, keep-deleted-for-references, shared-cache mutation discipline.
- `./export-users-roster.md` — serializeUsers: email-keyed dual-role payload, `(user as any)` workspace_roles carry-through, export-without-import asymmetry.
- `./baseuser-service-actors.md` — NOCO_SERVICE_USERS appended post-cache with deleted:true so validators match while pickers filter.
- `./cache-setlist-projection.md` — setList projected child keying ['base_id','id'], dual-gate suppression, query-shaped reads never seed canonical cache.
- `./job-timing-helpers.md` — initTime/elapsedTime self-reanchoring dual-clock stopwatch with debug-channel split logging.
- `./widget-handler-contract.md` — getWidgetHandler factory indirection over shape-correct CE no-ops carrying WidgetDependencies declarations.
- `./attachment-migration-ledgers.md` — nc_job_001: storage scan ledger + per-model offset ledger; pause/resume ingest, late-thrown stream errors, cells rewritten with minted FileReference ids.
- `./thumbnail-backfill-migration.md` — nc_job_002: existence short-circuit via getDirectoryList sentinel trio + direct ThumbnailGeneratorProcessor.job() reuse at concurrency 1.
- `./link-recovery-witness.md` — nc_job_003: rebuild lost LTAR colOptions from lookup/rollup/link witnesses by inverse-type probing; unrecoverable columns deleted; one transaction.
- `./order-column-backfill.md` — nc_job_005 (+ unregistered _014 twin): dialect-keyed ROW_NUMBER templates, auto-increment copy fast path, partitioned junction variant wiring order ids onto relation rows.
- `./audit-id-migration.md` — nc_job_009: PK-keyset pagination (O(log N)/batch) + uuidv7({msecs}) preserving chronology regardless of copy order; in-trx dedup recheck for parallel runners.
- `./soft-delete-upgrader-mode.md` — nc_job_010: Upgrader instance + upgraderMode Source clones stage DDL; flush through a second unflagged driver so live reads survive the window.
- `./soft-delete-unique-conversion.md` — nc_job_010: plain UNIQUE → partial unique index around __nc_deleted; pk/ai exempt; NEW_COLUMN before UPDATE_COLUMN emission order.
- `./soft-delete-meta-fastpath.md` — nc_job_010: allowlisted direct nc_columns_v2 insert + per-table multi-row view-column INSERTs replacing N+1 service calls for system columns.
- `./migration-queue-backpressure.md` — nc_job_010: gate the enqueue loop on queue.size not queue.pending; NOT EXISTS anti-join beats NOT IN on 200k-row ledgers.
- `./sqlite-boolean-default-repair.md` — nc_job_011: repairs _010's SQLite 'false' string default — drop index → DDL alter → recreate index → normalize values LAST (copy-dance overwrites earlier fixes).
- `./orphan-view-column-gc.md` — nc_job_014: constant-table NOT EXISTS anti-join deletes across six view-column tables, warn-and-continue; no-op version slots are contract, not dead code.
- `./json-import-streaming.md` — peek/unshift array detection, bracket-wrap object roots, mandatory pipe-error forwarding, preview-vs-import truncation tolerance split.
- `./excel-import-streaming.md` — WorkbookReader minimal options, resolveCellValue union ladder before typing, includeEmpty index stability, non-target sheet draining.
- `./cross-instance-migration-stream.md` — one octet-stream POST of typed JSON frames in duplicate.processor phase order; consumer-first axios start; guaranteed pushStream(null) terminator.
- `./at-engine-dual-mode.md` — ATImportEngine extends ATMockImportEngine selected by isPlayWrightNode(); module-global aTbl→ncId mapTbl; DEBUG_MOCK_AIRTABLE_IMPORT swaps atBase to mocks.
- `./job-producer-controllers.md` — fast-ack {id} producers with controller-owned guards and option shaping (BOM/isPublicExport flags, filter confinement, JOB-status placeholder base).
- `./data-table-single-bulk-funnel.md` — one service serves single row AND bulk array: wrap-in-array → bulkInsert → unwrap[0], shape-derived trace op names, resolved-source is_data_readonly re-assertion, title/name/id pk fallback.
- `./data-table-duplicate-guard.md` — pre-flight Set rejection of duplicated pks in bulk update/delete BEFORE any DB work; composite keys join with ___ over _-escaped components; pk-required checked after dup.
- `./data-table-nested-dispatch.md` — five-way LTAR ladder (bt-like-v2-junction / mm-or-mm-like / hm / oo-non-bt / bt) plus refContext resolution and mutate-in-place restrictNestedLinkQuery before getAst compiles data+count.
- `./data-table-link-swap.md` — copy/paste/deleteAll becomes a computed {link[],unlink[]} diff deposited via captureForTrace for an outer undo scope, applied once through removeLinks→addLinks; recorded ops carry RESOLVED pks and are self-inverse.
- `./data-table-link-by-display.md` — display-value linking: group entries by column, two-step resolve (case-sensitive eq batch then lowercased-equality like fallback), set-diff vs current mmList, silent skip of unmatched values, first-match-only for single-link columns.
- `./ltar-cross-base-context.md` — memoized four-context derivation (ref/mm/parent/child) for links whose related table or junction lives in another base; permissions:undefined on re-base prevents cross-base visibility leak; accessors re-base onto the LINK's own base_id.
- `./ltar-update-placeholder.md` — LinkToAnotherRecordColumn.update() is an underscore-param placeholder; real mutations go straight to ncMeta.metaUpdate on COL_RELATIONS followed by dual COL_RELATION+COLUMN cache eviction.
- `./meta-diff-splice-ladder.md` — consume-on-match splice diff between introspection and stored columns makes ADD/TYPE_CHANGE/PROPS_CHANGED/REMOVE mutually exclusive by construction; dialect-gated type compare (mysql dtxp; PG native enums via udt_name); virtual/system columns exempt from REMOVE.
- `./meta-diff-postsync-pk-gate.md` — relation-removal detection must PREDICT post-sync pk state (stored pk OR colListRef pk) or it undoes the pk restoration happening in the same sync; creation-side twin refuses relations when either side lacks pk.
- `./meta-sync-deferred-virtual-groups.md` — TABLE_RELATION_ADD enqueues thunks grouped by target table: same-table inserts run sequentially (unique titles observe siblings), different tables parallelize; removal-type changes sort first to dodge FK errors.
- `./meta-sync-pk-ratchet.md` — asymmetric pk diff (regressions don't fire) + OR-fusion write-back make a manual pk flag on a UNIQUE-NOT-NULL external column survive every later sync; rqd/un/ai/unique stay symmetric.
- `./meta-sync-mm-junction-heuristic.md` — bridge-table detection (2 belongs-to + <5 normal cols + 2 pks), existence probe with cross-wired id equality before minting each side, markAsMmTable promotion/demotion, hm twins promoted to system fields.
- `./shared-view-query-confinement.md` — anonymous where/sort/filterArr/fields confined per-term to view-exposed columns; alias-complete key enumeration (?w= bypass incident documented); strip-don't-reject except group-by; scope hoisted once for bulk entries.
- `./public-form-insert-gates.md` — unauthenticated form insert: scheduling gate → ANONYMOUS_USER attribution + ncSharedViewId stamp BEFORE audits → visible-fields-only write filter → multipart+url attachment ingestion with url-uploads first.
- `./public-nested-link-guards.md` — anonymous /mm//hm//picker stack three independent gates: parent-row visible under applyViewFilters, link column show-flag, predicate confinement; form-picker fields allowlist = pk+pv+display+visible-lookup targets; trashed-table null-model guard.
- `./public-gantt-links-ltar-duality.md` — linksAsLtar must flip BOTH the AST (projection) and listArgs (SQL fetch) together or nocoExecute silently strips nested rows; flag only ever SET server-side per view type, never read from caller query.
- `./hook-filter-tree-flatten.md` — getFilterObject rebuilds hierarchy by grouping on fk_parent_id, sorting siblings by order (Infinity last), parent-first DFS flatten; creation order explicitly abandoned (documented import/duplicate corruption); twin lives in Filter.ts.
- `./model-role-visibility-cache.md` — per-(view,role) disabled ACL: pair-keyed object cache inside a base-scoped projected list; deletes deepDel CHILD_TO_PARENT; deleteByBaseId deliberately loops per-row deletes to keep cache eviction exact.
- `./jobs-meta-list-window.md` — jobs listing shows rows updated within 1h OR still live (ACTIVE/WAITING/DELAYED); other users' rows appear but with result stripped; timestamps formatted through ncMeta string-time discipline.
- `./api-token-sso-scoping.md` — token listing branches on sso_client_id identity (SSO users see both families); create stamps fk_sso_client_id + base confinement (null = account-wide); foreign-token delete returns the SAME notFound as missing — no ownership oracle.
- `./datas-alias-entry-funnel.md` — v1/v2 alias-entry wrappers own identity validation before delegating: linkColumnId must reference THIS model as LTAR else fieldNotFound, the link's fk_target_view_id substitutes the view, form-scheduling gates both insert entries, external deletes ride delByPk's internal link cascade (pre-guard deliberately removed).
- `./datas-getdatalist-engine.md` — the shared read engine every list route funnels into: getAst once → silent JSON coercion of filterArr/sortArr → Promise.all(count, list+nocoExecute) → typed-error rethrow funnel (NcBaseError/NcSDKErrorV2 pass, everything else logged+masked 500) → PagedResponse with limitOverride spread AFTER query; skipSubstitutingColumnIds reads raw query for the AST but AND-ed with v3 context for the SQL fetch.
- `./datas-grouped-dual-path.md` — grouped listings fork on `source.type === 'pg' && query.opt === 'true'`: the optimized path resolves nested cols/rollups directly in SQL (AST only for dependencyFields) while the fallback projects through nocoExecute({key:1,value:ast}); per-key counts join with ?? 0; response contract identical both paths — that is the point of the opt-in gate.
- `./nested-link-query-restriction.md` — ONE sanitizer serves all six nested-link routes: gate ladder (shared-view access ALWAYS restricts → caller-threaded hasLimitedAccess → conservative cross-base/no-visibility default), exposure = pk+pv+link display column, leaf-level where strip with empty-group pruning (never drop the whole where = fail-open), unresolvable refs left alone, mutation happens BEFORE both fetch and count read it.
- `./nested-link-count-asymmetry.md` — nested list family shares the synthetic-one-key-AST fetch + dedicated count primitive, but hmList alone returns `totalRows:` while four siblings return `count:` + query spread — a wire-contract split consumed by different client generations; primitive param names drift (parentId/id/cid/pid) and swapping them silently returns wrong sets; ooExcludedList forks mm-primitives for LinkV2 columns.
- `./byviewid-misnomer-contract.md` — the *ByViewId write/read quartet resolves its id as a MODEL id (legacy route, no view semantics); only dataListByViewId and dataInsertByViewId load a real View; treating the misnomer as a bug and "fixing" either name or resolution breaks existing consumers.
- `./shared-metas-trashed-model-guard.md` — public viewMetaGet guards BOTH soft-delete cases (cache-miss ⇒ view.model unset, cache-hit ⇒ stale .deleted flag) so a share UUID outliving its table returns clean 4xx not 500; rangeColumns exempted BEFORE the visibility filter or calendar/timeline shares render positionless; lock_type forced Collaborative for anonymous viewers.
- `./related-metas-projection-fixpoint.md` — extractRelatedMetas walks LTAR (+mm junction) and lookup chains recursively attaching FULL foreign schemas, then projectRelatedMetas cuts each model to pk+pv+needed-columns+transitive same-model dependency closure at a bounded fixpoint (≤6 passes) via replace-with-copy (cache-shared Models never mutated) — metadata must agree with the data path's pkAndPvOnly envelope or the frontend renders fields the API refuses to return.
- `./shared-metas-secret-strip.md` — serialization boundary strips secrets by prototype-preserving shallow copy (`Object.assign(Object.create(Object.getPrototypeOf(view)), view, {password: undefined})`) explicitly so correctness survives future caching, plus a fresh nested-spread copy for the form email recipient map; never `delete` on the loaded instance.
- `./filter-hookfilter-twin-diff.md` — Filter and HookFilter carry byte-similar getFilterObject trees (identical reorder-fix NOTE, group-by-parent/order-sort/DFS-flatten) that drift in three places: cache key shape (bare-id fallback chain vs explicit [FilterCacheScope.VIEW, id]), Filter's OWN condition ladder vs cache-scope ladder disagree on precedence (parentColId+viewId caller = DB keyed one way, cached under VIEW = latent cross-scope pollution), and only Filter adds DB-side `orderBy: order asc`.
- `./filter-rls-tree-failopen.md` — RLS enforcement links flat rows into an in-memory tree (linkFilterRowsIntoTree) BEFORE dynamic-value substitution: unpopulated children make getChildren() reload RAW rows from cache post-substitution, so `{currentUser.x}` reaches SQL literally and negated operators invert it into match-everything; no depth cap BY DESIGN (truncation fails OPEN); fk_parent_id cycles are unreachable by construction so no visited-set is needed.
- `./filter-insert-scope-ladder.md` — one insert path serves all seven filter scopes: a first-match FK ladder derives source_id (the view branch explicitly yields when fk_parent_column_id is also set) and keys metaGetNextOrder per-scope ordering; children recurse re-stamped with the ROOT's scope key; caller-supplied order is honored ONLY under isReplay() (undo→redo round-trips).
- `./filter-cache-multiscope-fanout.md` — redisPostInsert registers one row under up to five list caches derived from the row's own FKs (VIEW/HOOK/PARENT_COLUMN/COLUMN/RLS_POLICY/BUTTON_COLUMN + parent-nested twins + unconditional PARENT entry), admits with a mandatory id+primary-scope 400 gate (HookFilter twin: logger.error + 500), then clears the view's optimized single-query cache with a concurrent-delete null-view guard that skips instead of throwing.
- `./filter-update-delete-lifecycle.md` — Filter.update attaches its NocoCache.update INSIDE the active knex transaction (ncMeta.knex.attachToTransaction) so rollback drops the cache write (HookFilter twin: bare await); delete guards double-delete AND self-parent cycles (id === fk_parent_id brake HookFilter lacks), recurses child-first with deepDel CHILD_TO_PARENT, and ends with exactly one View.clearSingleQueryCache for view-scoped trees only (hook/RLS/parent-col/button variants skip it).
- `./filter-hook-list-scope-mismatch.md` — rootFilterListByHook READS the [FilterCacheScope.VIEW, hookId] list but WRITES [HOOK, hookId]: DB truth keyed fk_hook_id keeps every caller correct while the mis-keyed read degrades to miss-always + redundant setLists; port the LESSON (derive cache keys mechanically from the same expression as the DB condition) and know the four absorbing call sites (Hook.getFilters, filters.service.hookFilterList, hooks.service snapshot + delete-reinsert flows).
- `./alias-services-boundary-correction.md` — CORRECTS the prior pure-delegation record: data-alias-nested carries its own boundary (restrictNestedLinkQueryForColumn before BOTH consumers ×7, an explicit view-show-vs-column-level-ACL DESIGN NOTE on mmList, LTAR type-check on list methods only, and a live relationDataAdd-vs-Remove Source-resolution asymmetry) and bulk-data-alias dispatches typed option tuples via `Parameters<(typeof BaseModelSqlv2.prototype)[T]>` reflection with validateV1V2DataPayloadLimit (API-token Array-body cap, default 100) applied to four of six ops — never UpdateAll/DeleteAll.
- `./filter-deferred-composition.md` — parseConditionV2 never mutates the live builder: every path returns a deferred `{clause, rootApply}` pair applied clause-first at the call site; skipped filters are EMPTY no-op closures (never undefined) so parent composition stays type-stable; disabled filters drop before recursion; aliasCount is one shared mutable counter threaded through recursion.
- `./filter-dialect-null-ladder.md` — the generic comparison switch's hidden corrections: every negation op re-admits NULL (neq/not append orWhereNull on ALL dialects), MySQL eq needs BINARY for case-sensitive text, Oracle stores '' as NULL so notempty becomes `1 = 1` and blank drops its empty-string arm, nlike keeps '%%' degenerate-pattern special case, gt/lt INVERT under customWhereClause role swap, Formula operands swap before wildcard wrapping, allof/nanyof family does wrapped-comma CSV membership with enum/set trimEnd.
- `./filter-groupby-textcast.md` — formulaToTextCast's four-way table (pg ::text / mysql CAST AS CHAR / mssql NVARCHAR(MAX) because T-SQL has no valid AS TEXT / oracle VARCHAR2(4000)) makes `= ''` type-safe against JSON_EXTRACT-typed formula output (#12695); gb_eq/gb_null fold to eq/blank for real columns but build literal-vs-aggregated-subquery comparisons for Lookup/LTAR, with QrCode/Barcode unwrapped to their value column first.
- `./dynamic-filter-exists.md` — fk_value_col_id dynamic filters: same-table swaps filter.value to a knex.ref and continues the normal switch; cross-table builds EXISTS(SELECT 1 FROM related __nc_dfN WHERE pk = _crossTableRowId AND softDelete AND sourceCol op related.col) by delegating back into parseConditionV2 with the SOURCE column kept as subject (operator direction sacred); virtual value columns and missing row context silently skip.
- `./rollup-correlated-aggregate.md` — genRollupSelectv2 compiles HM/OO/MM rollups into correlated aggregate subqueries (soft-delete filtered per shape, btLikeV2 junction LIMIT 1) whose failure contract is an 'ERR' sentinel cell (Oracle adds FROM dual), never a throw; formula/Created/Modified operands materialize through toQuery() with `\?` RE-ESCAPED because knex.raw(rawObj) strips the inner escapes and corrupts outer bindings; pg bool::integer, MSSQL bit→FLOAT + derived-table agg wrap, Oracle CLOB→VARCHAR2(4000) corrections.
- `./lookup-nested-join-agg.md` — generateLookupSelectQuery walks nested lookup chains adding JOINs per level (soft-delete excluded at EVERY level) then aggregates ONLY when some level was non-BT (all-BT chains skip aggregation entirely): pg json_agg::text (+junction link-order default when no explicit lookup config), mysql JSON_ARRAYAGG-as-NCHAR, sqlite group_concat('___'), MSSQL hand-built JSON_QUERY/STRING_AGG array string, Oracle JSON_ARRAYAGG NULL ON NULL RETURNING VARCHAR2(4000) with CLOB pre-shortening.
- `./lookup-pk-in-limit.md` — per-lookup top-N is expressed as a correlated pk-IN subquery (qb CLONE carries the exact relation correlation) instead of LIMIT so it composes with any consumer aggregate/EXISTS; "last N" flips the sort inside the pk-IN then presents un-flipped; the pk is always the final tiebreaker so boundary ties resolve identically across display/filter/formula; the filter variant ranks with ROW_NUMBER() PARTITION BY fk because the filter set is global parent-IN(qb); EE-license-gated, PG-only.
- `./sort-dialect-unsortable.md` — sortV2 intercepts LOB sorts at the ORCHESTRATOR level because the check keys on column.dt which per-type handlers can't see: MSSQL text/ntext/image/xml → bounded NVARCHAR(4000) (MAX spills tempdb), Oracle clob/nclob → DBMS_LOB.SUBSTR(col,2000,1); prefix-truncation accepted for display keys; direction maps to NULLS FIRST/LAST in exactly one place passed down to handlers.
- `./formula-tree-alias-registry.md` — the v2 formula compiler entry: `aliasToColumn` is a lazy registry of builder factories seeded for EVERY column before compilation (Formula/Button recurse with a null-seeded self-entry as the circular-ref breaker; Lookup/LTAR/Rollup route to lookupOrLtarBuilder or genRollupSelectv2; CreatedTime/DateTime get per-dialect tz normalization; User-family gets memoized email REPLACE chains; AI LongText per-dialect JSON extraction), threaded with one shared monotonic alias counter and a CircularRefContext clone chain.
- `./mssql-formula-literal-binding.md` — mssql-only Literal branch inlines strings as N'…' (quotes doubled, `\?` escaped) and booleans as 1/0 because bound placeholders get shifted by the outer single-query `.toQuery()` composition (a pk landing in TOP(…) = SQL Server 1060) and nvarchar typing is lost through toQuery(); every other client keeps normal `?` bindings.
- `./formula-dryrun-transient-doctrine.md` — validation dry-runs execute the compiled expression against the REAL table, then `isTransientError` decides everything: transient failures latch `formulaDryRunFailed` (single guarded write site) and rethrow, latched runs throw the FORMULA_DRY_RUN_SKIPPED_MESSAGE sentinel which is CONTROL FLOW never a persisted column.error, and only non-transient failures under validateFormula/circular-ref rewrite column metadata via cache:false updates plus explicit cache-map clearing.
- `./formula-output-guardrails.md` — two post-compilation gates: compile-time SQL length >500k chars refuses with telemetry, runtime wraps STRING-typed non-raw-JSON trees at the OUTERMOST call only in per-dialect SUBSTR caps (pg ::text / mssql NVARCHAR(MAX) / oracle TO_CLOB / sqlite TEXT) where the length is an INLINED integer literal — a second `?` binding would steal a positional slot from the inner subquery's bindings.
- `./formula-binary-operator-coercion.md` — binaryExpressionBuilder rewrites operators into other tree nodes BEFORE SQL exists (`&`/string-`+`→CONCAT, BLANK() equality→ISBLANK/ISNULL swaps, mixed-type ==→STRING() both sides, /→FLOAT both operands + NULLIF(x,0)), then materializes operands via toQuery() and applies per-dialect corrections on the string: ordering comparisons coerce the text side through NULL-safe numerics (pg btrim+regex CASE, mssql TRY_CAST, oracle BINARY_DOUBLE DEFAULT NULL ON CONVERSION ERROR; mysql/sqlite left implicit), Date-vs-'' collapses to IS [NOT] NULL, standalone comparisons CASE-materialize to 1/0 (mssql/oracle) gated by prevBinaryOp∉{AND,OR}, and mssql numeric +−* re-casts FLOAT against tedious string returns; exit re-escapes `\?` once and paren-wraps on precedence change.
- `./formula-call-expression-assembly.md` — callExpressionBuilder's switch does TREE REWRITES only: ADD/SUM fold right into COALESCE-guarded `+` chains threading pt.dataType (dropping it regresses mssql numerics to tedious strings); CONCAT forks by client (sqlite `||`, oracle TO_CLOB chaining past ORA-01489, mysql2 IFNULL-per-arg vs pg ignore-NULL); URL() becomes a CONCAT of paren-escaped segments whose space padding is load-bearing for the nc-gui URI::( … ) regex; everything else falls to mapFunctionName and the generic assembler joins already-materialized arg strings into one knex.raw with a final `\?` re-escape.
- `./formula-unary-fold-precedence.md` — fn()'s UnaryExpression branch folds `-`/`+` over NUMERIC arguments into ONE bound constant (`(op=='-'?-1:1) * argument.value`) because the SDK validator admits only `-` over numeric literals (everything else throws NOT_SUPPORTED pre-compile), making the LiteralNode cast validation-guaranteed; the prefix-splice fallback arm threads pt.operator down as prevBinaryOp so mixed-operator children paren-wrap; node itself wraps on operator≠prevBinaryOp. Extend validator + compiler arms in lockstep or `.value` reads crash on non-literals.
- `./formula-aggregate-thunk-dispatch.md` — Identifier resolution's duck-test protocol: when aliasToColumn returns a FUNCTION builder, call it with the parent formula fnName (stamped onto args pre-recursion by the CallExpression pass) to get MIN/SUM/FIRST over the captured multi-row lookup subquery via getAggregateFn; dispatch table surprises: AVG deliberately routes to .sum() (true-average exists only as commented todo), FIRST is select().limit(1) relying on the subquery's ORDER BY nc_order, every arm clears 'select' first; databricks gets string surgery — strip one trailing `)` and append `LIMIT 1)` because raws can't carry knex .limit().
- `./formula-string-cast-user-substitution.md` — a node with cast===STRING never compiles itself: it re-dispatches as arg #1 of a synthetic STRING() call with cast:null ON THE CLONE (original keeps annotation → recursion terminates); runs BEFORE CallExpression handling so functions wrap whole; the idempotent `if (arg.fnName) return` stamp guard preserves inner-call names. User/CreatedBy/LastModifiedBy build lazily with closure-memoized roster (include_internal_user), resolve storage column via getColumnName (system=false columns have none of their own), and render pg/sqlite delimited-token replace vs everyone else's nested REPLACE() ladder materialized per hop with .toQuery().
- `./groupby-derived-table-fork.md` — one grouped-list implementation, two shapes: pg/mysql/sqlite GROUP BY select aliases directly; mssql/oracle can't (alias ORA-00904 <23c, subquery-valued alias ORA-22818) so keys project into a `__nc_grp_src__` derived table and ALL aggregation moves outward (inner COUNT would be ORA-00937); both end in `WITH grouped AS (...) SELECT * FROM grouped g`; final `__nc_group_alias` wrap is default-path-only (T-SQL forbids CTE-in-derived-table; Oracle unquoted idents can't start with `_`); table alias syntax deferred to DBQueryClient.tableAlias (Oracle forbids AS on table aliases).
- `./groupby-key-projection-ladder.md` — processColumn compiles each type to a group key under the display alias: errored meta → 400; Attachment/Button → 400; QR/Barcode swap to value column KEEPING original alias (asId); rollup/lookup reuse genRollupSelectv2/generateLookupSelectQuery; JSON projects `(col)::jsonb` on pg and SILENTLY DROPS the key on every other dialect; plain columns wrap in sqlNullIfBlank; returns .toQuery() because the sub-group path consumes the string.
- `./groupby-blank-null-buckets.md` — sqlNullIfBlank makes '' and NULL share one bucket four ways: pg non-string gates on pg_typeof(text/varchar/char) before ::text='' check; mssql CASTs to NVARCHAR(MAX); oracle arm is a NO-OP ('' ≡ NULL already; NULLIF(col,'') raises ORA-00932 on NUMBER); mysql/sqlite get NULLIF(col,''). isStringType=true skips the type-gated variants.
- `./groupby-datetime-minute-bucketing.md` — DateTime/CreatedTime/LastModifiedTime keys truncate to MINUTE per dialect and MUST stay in lockstep between list() and count(): pg date_trunc+interval noop; mysql CONVERT_TZ-to-UTC BEFORE stripping SECOND(); sqlite strftime (count twin APPENDS an offset suffix re-parsed from chars 20+); mssql DATETRUNC on 2022+/CONVERT style-120 VARCHAR(16) older; oracle TRUNC(CAST(... AS DATE),'MI') with SYS_EXTRACT_UTC ONLY for dt containing 'time zone' (plain TIMESTAMP raises ORA-30175).
- `./groupby-subgroup-count-placements.md` — sub-group counts place COUNT(DISTINCT COALESCE(blank→NULL(key),'__null__')) where the dialect allows aggregates: inline `__sub_group_count__` (pg/mysql/sqlite) vs projecting `__nc_sub_group_col__` (mssql CAST NVARCHAR(MAX), oracle TO_CHAR) for the outer query to aggregate; leaf expression sanitized BEFORE composition AND AGAIN after each toQuery() hop (formatQuery unescapes \? → bare ? breaks knex binding count; the oracle-column bug shipped NaN subgroup counts).
- `./groupby-mincount-sqlite-bind-trap.md` — duplicates-only HAVING COUNT(pk||'*') >= minCount REQUIRES Number() coercion of the query-string arg: SQLite compares INTEGER < TEXT by storage class so binding '2' always-false → 0 groups (pg/mysql/oracle coerce); HAVING rides the inner qb on default path, moves to the outer derived table (COUNT(*)) for mssql/oracle.
- `./groupby-mssql-pagination-orderby.md` — T-SQL requires ORDER BY whenever OFFSET/FETCH present (applyPaginate always sets offset) but the sort loop emits keys only for grouped columns, so sortless pages die with "Invalid usage of NEXT in FETCH": append trailing constant orderByRaw('(SELECT NULL)') AFTER user sorts on mssql only; gated by NC_DISABLE_GROUP_BY_LIMIT alongside pagination itself.
- `./groupby-nulls-sort-contract.md` — grouped sorts apply on the OUTER CTE referencing g.<alias>, skip sorts whose column isn't a group key, treat count-asc/count-desc as first-class directions emitting TWO keys (g.count first w/ nulls LAST-for-desc/FIRST-else, key second); pg NULLS semantics are canonical (desc→LAST), MSSQL's inverted default is compensated with an explicit CASE WHEN key IS NULL THEN 1 ELSE 0 END bucket; mapping lives in exactly one place.
- `./groupby-user-display-sort.md` — sorting groups by a User/CreatedBy/LastModifiedBy key translates stored user ids to display names INSIDE SQL over the already-grouped g.<alias>: pg/sqlite via DBQueryClient.replaceDelimitedWithKeyValue keyed map over the BaseUser roster; others via folded nested REPLACE(acc,?,?) chains materialized through toQuery(); display_name||email fallback; roster loaded once per sort.
- `./groupby-rls-entry-funnel.md` — row-level-security conditions enter groupBy/groupByCount ONLY at the BaseModelSqlv2 wrappers, PREPENDED into args.filterArr as one is_group Filter ahead of caller filters (the module itself never queries RLS); contrast groupByAndAggregate which builds a dedicated conditionV2 group — dropping the prepend leaks out-of-policy rows into grouped counts.
- `./groupby-aggregate-dispatch.md` — groupByAndAggregate validates the client-supplied fn by REFLECTION (`typeof qb[aggregateFn] === 'function'` else throw) and never interpolates it into SQL; flat single query (no processColumn virtual keys, no CTE shell), output aliased `${fn}__${column}`, RLS as its own conditionV2 group; knex upgrades silently widen the accepted fn set — add an allowlist if untrusted callers reach it.
- `./formula-lookup-ltar-relation-fork.md` — lookupOrLtarBuilder's BT/HM/MM first-hop fork inside a formula: two-shape return (QueryBuilder vs `(fn) => Raw` aggregate closure), OO direction from `column.meta?.bt`, errored lookups degrade to `knex.raw('?', [null])` never throw, correlation via `??` identifier bindings.
- `./formula-nested-lookup-chain-walker.md` — the while-loop that flattens lookup-of-lookup chains into one JOIN spine: prevAlias/lookupContext cursor pair, per-level link filters + soft-delete at THAT level's alias ("see the mm-lookup filter fix" regression comment), PG inner limits per level, MM joins junction-first on the child side.
- `./formula-terminal-aggregate-dispatch.md` — terminal switch by uidt: isArray flips builder→aggregate function capturing selectQb BEFORE reassignment; display col resolved from the TERMINAL LTAR not hop one; Formula arm recurses via `_formulaQueryBuilder` with CircularRefContext.cloneAndAdd cycle guard; PG attachment concat uses jsonb_agg+lateral explode.
- `./formula-lookup-limit-placement.md` — outer pk-IN limit runs while selectQb is still a plain builder (`typeof clone === 'function'` duck-guard), inner correlated limits ride each nested BT/HM level with BOTH correlation columns, all PG-only, config read from the CURRENT level's column.
- `./formula-mm-link-order-stash.md` — junction Order column stashed as `_ncLinkOrderRef` side-channel property for the aggregate stage to ORDER BY drag order; skipped when the lookup HAS its own sort/limit config (config-wins precedence), absent for non-PG/v1/external by design.
- `./handler-dispatch-core.md` — HANDLER_REGISTRY two-key routing (uidt × dbClient with _default fallback); applyFilters skips orphaned-column filters while verifyFiltersSafe FAILS them (RLS-leak guard); getLogicalOpMethod maps or/and/not onto qb methods; rootApply composed separately in index order.
- `./generic-filter-op-table.md` — every generic comparison op's exact SQL: three-gate ''-vs-NULL matrix (isEmptyStringIncompatible types × Oracle ''≡NULL × native pg enum), negations re-admit NULL, Oracle notempty short-circuits to `1 = 1`, is/isnot keyword-routing as complements, btw/nbtw comma-split restored post-migration.
- `./dynamic-field-ref-like-patterns.md` — field-to-field LIKE: isRawInstance detection, dialect wildcard concatenation table (CONCAT / `+` / `||`) keeping refs as bindings, Oracle UPPER-wrap case parity.
- `./negated-op-exists-mapping.md` — negatedMapping flips nlike/neq/blank/null/notchecked/nanyof/nallof to POSITIVE ops inside the subquery then inverts containment outer (whereNotIn); vacuous-NULL arm differs per relation type; flipping at the wrong layer double-negates.
- `./lookup-filter-subquery-forks.md` — LookupGeneralHandler HM/BT/MM containment shapes + orphaned-lookup empty-clause policy + allof collection semantics (AND-of-anyofs across related rows) + MM pair-ranked ROW_NUMBER window with child-FK tiebreaker + Postgres 42703 link-filter alias incident.
- `./ltar-filter-count-family.md` — LTAR filters delegate parseConditionV2 over the related table with ltarSubField resolution; blank/notblank/checked become correlated COUNT(...)=0 subqueries with soft-delete on the counted alias; MM blank must join+filter the parent or deleted links stay "not blank"; knex raw-ref `as any` casts documented.
- `./datetime-relative-op-machine.md` — relative sub-op anchor algebra (startOf day vs minute under groupby), eq = BETWEEN [+24h−1ms], neq = outside-or-NULL, time-component sniffing switches gt/gte/lt/lte to instants, null-family ops route AROUND date parsing (empty-clause all-rows bug), `in` normalizes offset tokens for Oracle NLS.
- `./datetime-write-format-twins.md` — Date pins YYYY-MM-DD comparisons + ISO-prefix normalization against ±1day tz shift; Time pins HH:mm:ss with 1999-01-01 parse sentinel; MySQL wraps comparisons AND group-by in symmetric CONVERT_TZ; PG writes via AT TIME ZONE CURRENT_SETTING; TimeMysqlHandler alone re-widens format.
- `./user-display-name-substitution.md` — like/nlike pre-filter the user roster IN JS by wildcard semantics before folding REPLACE() chains into SQL; sort uses the full roster; CreatedBy name resolution never mutates cached Columns; PG/SQLite swap the primitive for replace_delimited_with_keyvalue through composition shells.
- `./json-blank-dialect-forks.md` — JSON blank = NULL ∪ {} ∪ [] (+ ::text='' on pg); pg dual-cast equality (::jsonb semantic vs ::text literal); mysql JSON_UNQUOTE everywhere; fieldExpr(column) hook documents per-type cast override; NC_MAX_TEXT_LENGTH checked before JSON validity.
- `./formula-filter-sort-routing.md` — Formula columns compile once then delegate: verifyFilter clones the Column with dataType→uidt mapping and re-enters the dispatcher; filter passes compiled builder as customWhereClause with numeric-bound values; pure-literal formulas ORDER BY 1.
- `./links-v2-duality-dispatch.md` — Links columns pivot on isBtLikeV2Junction: MO/OO → LtarGeneralHandler (display-value semantics), OM/MM+v1 → RollupGeneralHandler (count semantics, numeric-bound values); composition over inheritance; ComputedFieldHandler makes writes no-op undefined.
- `./attachment-blank-write-ledger.md` — attachment blank = NULL ∪ '[]' ∪ 'null'; empty like/nlike chips mean blank/notblank; parseUserInput merges old-row metadata over client patches (server authority), mints temp_ ids, GCs removed FileReferences write-time, enforces per-cell file caps.
- `./checkbox-rating-null-folds.md` — checkbox notchecked folds NULL ∪ false (NULL = "never set"), sqlite repairs 1/0↔bool on read; rating lt/lte/gte add asymmetric orWhereNull arms around zero; duration/year/currency/colour parse ladders; numeric family borrows general's op table via instance-delegation shells.
- `./longtext-ai-projection-case-rules.md` — LongText family (Email/URL/Phone/SingleLineText inherit) projects AI-prompt `.value` via per-dialect JSON extraction for both filter LHS and sort; PG delegates like/nlike to ILIKE; MySQL pins BINARY eq/neq for legacy parity.
- `./select-sort-uuid-casts.md` — MySQL select sorts CONCAT()-wrap enum/set so labels order alphabetically not by DDL position (silent-wrong-order trap); UuidPgHandler casts EVERY comparison ::text so partial-value filters avoid uuid parse errors at the cost of index use.
- `./engine-generic-overrides.md` — GenericPgFieldHandler coerces ::text + ilike broadly (with the recorded nlike operator-flip regression restoring `= ''` re-admission); MySQL/SQLite narrow overrides to the membership family only; enum/set trimEnd gated on dt.
- `./handler-interface-computed-mixin.md` — FilterOperationResult deferred protocol + the null/empty/blank split rationale docstring; withComputedParseUserInput mixin gives CreatedBy/LastModifiedBy User-family SQL with write rejection; LastModifiedBy exports are literal aliases of CreatedBy.
- `./formula-blank-nan-semantics.md` — capture operand types BEFORE the `/` FLOAT()-rewrite (rewritten nodes carry no dataType); pg-only blank→0 COALESCE applies in EVERY mode (partial adoption renders blanks as numbers but sorts them as NULL); NULLIF-NaN strip fires only inside ordering comparisons using the SAME helper as the filter layer; `/` and `%` dispatch unconditionally to the IEEE forms on pg.
- `./pg-ieee-case-ladders.md` — pure SQL-text composer library typed `'double precision'` (pg<14 numeric cannot hold Infinity): `abs(x) < 'Infinity'` finite test mentions the operand ONCE (mention-count is the complexity metric under nesting), division tests only the divisor letting `x * 'Infinity'` resolve sign/zero/NULL cases, MOD guards divisor≠0 AND finite-left because it routes through numeric, sqrt/log `< 0`/`<= 0` guards leave NULL and NaN in the ELSE where the fn maps them to themselves, two-arg LOG negates the domain test so NULL falls to ELSE.
- `./pg-math-function-guards.md` — MAX strips NaN from operands then COALESCE-falls-back to unstripped greatest so an all-NaN list still yields NaN (LEAST needs no guard — NaN can never win a minimum); POWER/SQRT/LOG/MOD wrap their args in IEEE guards; ROUND adds an isFinite CASE because ::numeric casts raise at pg<14; GREATEST→MAX and POW→POWER twin entries exist because string-alias mappings PERSIST the rewritten lowercase callee name in old columns' trees ('sqrt' needs no twin — it uppercases back).
- `./non-finite-aggregate-exclusion.md` — exactly {sum,sumDistinct,avg,avgDistinct,min,max} drop non-finite rows at three consumer sites with three binding styles (genRollupSelectv2 composes excluded SQL TEXT because re-binding would strip `\?`; applyAggregation double-binds `??`; type-changer re-applies escapes post-toQuery and gates on destination numeric-ness); the count family deliberately keeps every row — an Infinity cell is not an empty cell.
- `./nan-sort-rank-agreement.md` — boolean `(expr <> 'NaN')` rank key rides the value sort at ALL THREE sites (cell applySort + both group-by call sites incl. the count-direction branch) so the group list and its contents agree on where NaN goes; filters strip NaN only for gt/lt/gte/lte/ge/le because eq/neq compare displayed tokens; non-finite filter VALUES bind `?::double precision` and verify() admits them `{isValid:true}` before the Decimal verifier 422s `eq NaN`.
- `./non-finite-wire-contract.md` — 'Infinity'|'-Infinity'|'NaN' STRING tokens exist because JSON.stringify collapses float8 specials to null indistinguishable from real NULL; convertFormulaNonFinite runs in execAndParse keyed by getAs() (=asId||id, never raw id), pg-only display-path-only; parsePlainCellValue passes tokens through untouched (governs CSV export); swagger declares pg numeric formulas oneOf [number,string] + nullable.
- `./sdk-operator-admission.md` — `%` is undocumented (never in the function list or UI suggestions) yet jsep parses it and builders lower it, so the SDK ArithmeticOperators tuple — the vocabulary persisted artifacts validate against — had to admit it or OLD stored formulas break upstream of any builder fix.
- `./lookup-filtered-nested-conditions.md` — every nested lookup level applies ITS OWN link conditions onto its relation sub-query via extractLinkRelFiltersAndApply, capturing the related baseModel PER RELATION SHAPE inside each branch (HM→parentBaseModel, BT/OO→childBaseModel, MM→junction parentBaseModel): outer-only application silently drops inner filters (#10150); LTAR hops stay ungated.
- `./link-v2-conversion-guards.md` — convertLinkToV2 pre-dispatch ladder rejects system columns (auto-created HM links whose upgrade corrupts the parent M2M's junction metadata), columns living ON junction tables, and any V1 column whose paired side — matched by swapped FK columns for MM-family (self-ref safe) or equal parent/child ids for direct relations — is already V2 (both sides transition together or fail loudly instead of compounding metadata corruption).
- `./display-value-admission-ladder.md` — Airtable import nocoSetPrimary: eligibility = ¬pk ∧ ¬system ∧ supported ∧ ¬createdOrLastModifiedTime ∧ ¬(Formula ∧ '""' placeholder); ladder primary → first eligible column (warn with reason) → mint Title_N SingleLineText against a lowercase taken-set of titles∪column_names (deliberately NOT nc_getSanitizedColumnName — its generator keys off the pre-create table name); everything inside per-table try/catch logWarning because a cosmetic throw reaches the job's outer catch and drops every created table.
- `./locale-minus-normalization.md` — normalize U+2212→'-' BEFORE stripping (Intl emits typographic minus in 36 locales), resolve sign AFTER stripping by leading-minus position ("-$1"/"$-1" = sign, "100-50" = noise) matching extractDecimalFromString so paste and typing agree; en-US currency fast path must KEEP '-'. Direct jest specs pin U+2212, $-99.5, and 100-50 cases.
- `./view-section-copy-inheritance.md` — duplicateView inherits the source section ONLY when the caller left fk_view_section_id strictly undefined; explicit null means top level and WINS — falsy checks would make explicit-null inexpressible; allowlist gains the key before inheritance fills it.
- `./credit-hold-scoped-keys.md` — reservation uniqueness moves from global request_ref to composite (scope, fk_scope_id, request_ref): a foreign-scope collision made hold swallow-and-run UNRESERVED, and unscoped deleteByRequestRef could release ANOTHER tenant's hold — safety that depends on caller convention becomes schema.
- `./sqlclient-factory-dispatch.md` — two-level factory dispatch keys on wire protocol (`client`) then managed flavor (`meta.dbtype`: tidb/vitess→mysql twins, yugabyte→pg twin); the async default export pre-resolves SSL cert files; NcConnectionMgrv2 passes an EXISTING knex handle via `connectionConfig.knex` so introspection never opens a second pool.
- `./knex-placeholder-escape-contract.md` — sanitize escapes every unescaped `?` run (`([^\\]|^)(\?+)`), genQuery opts in per call site, and any composed SQL string fed to another raw()/toQuery() must be re-escaped ON EXIT or its literal placeholders become bind slots of the next query.
- `./default-value-function-allowlist.md` — bare zero-arg function DEFAULTs pass a deny-by-default Set (25 members across date/time, random, uuid families) compared case-insensitively; a generic `\w+()` match would execute version()/current_user() during INSERT and leak results through the API; misses reject badRequest for the schema editor.
- `./updown-statement-duality.md` — every DDL op executes AND returns `{upStatement:[{sql}],downStatement:[{sql}]}` frames prefixed `/* xc */\n`, built from SEPARATE builder invocations per direction; create keys on `non_unique` while delete keys on `non_unique_original` (original kind decides drop verb); base relationDelete double-executes (recorded trap — PG override canonical).
- `./pg-alter-column-ladder.md` — alterTableColumn emits RENAME (guarded against accidental renames) → DROP DEFAULT → optional dateConversionFunction → TYPE USING cast (AutoNumber backfill = `0::bigint`) → NOT NULL → DEFAULT → CREATE SEQUENCE OWNED BY + `nextval(?)` regclass-string → partial-vs-full unique; AI-removal must DROP NOT NULL because serial's hidden constraint is invisible to the rqd diff.
- `./pg-enum-introspection-roundtrip.md` — columnList binds native enum identity into internal_meta (pg_enum_type_name + pg_enum_schema_name, BOTH required — partial metadata forces schema guessing); findColumnsUsingType scans pg_attribute/pg_class/pg_type over relkind r|p, attnum>0, ¬attisdropped, with an all-three-or-none self-exclusion trio, deciding in-place ALTER TYPE vs fork.
- `./pg-typecast-composers.md` — generateCastQuery turns arbitrary existing cell text into any UITypes via pure-SQL total functions: extractNumber sentinel-swap laundering (leading-sign protect → inner dashes strip → FIRST dot becomes decimal), min−1/max+1 bounding sentinels (out-of-domain ⇒ NULL never clipped), LOWER token-set booleans, format×time CASE ladders calling external to_date_time_safe, duration format-id semantics (0=h:mm single=minutes).
- `./version-keyed-query-dispatch.md` — _getQuery lazily latches {version,key} where key is STRING concatenation primary+major ('57','80'); catalogs are `{func:{default|key:{sql}}}`; mysql rows arrive as `[rows,fields]` (guard `response.length===2`) with UPPERCASE keys lowercased on fetch; MariaDB quoted-'NULL' defaults coerced to real null (#4625).
- `./mysql-unique-index-ladder.md` — CHANGE COLUMN requires both names; unique constraints exist ONLY as indexes (DROP INDEX verbs, INDEX_NAME from STATISTICS NON_UNIQUE=0, 64-char cap); tableUpdate sanitizes the whole comma-joined body then re-binds just the table name; tableDelete's down is verbatim SHOW CREATE TABLE output; timestamp DEFAULT_GENERATED extras recombine during introspection.
- `./sqlite-table-update-machine.md` — column edits become four-step rename-to-nanoid-suffix/copy/drop ladders; dependent user indexes (pragma origin='c') drop FIRST; transaction runs under foreign_keys-OFF + legacy_alter_table-ON with a SELECT-warmup hack against duplicate-column errors; splitQueries is quote-state-aware so trigger bodies survive; down-migrations for edits are structurally `';'`.
- `./pg-lifecycle-connection-ladder.md` — testConnection/createDatabaseIfNotExists/dropDatabase all build throwaway postgres-db pools (deepClone, pool min0/max1, destroy in finally); "database does not exist" from the fallback is SUCCESS carrying the ORIGINAL error message; schema creation probes schemata first because IF NOT EXISTS needs CREATE ON DATABASE; drop terminates backends then swaps this.sqlClient.
- `./connection-manager-reuse.md` — composite `(base_id:id)` cache key (bare ids cross-wired bases); LRU(500) destroys evicted XKnex handles; Redis SOURCE_CONN_VER staleness check runs before every get with delete-before-bump reset ordering; pool-level mysql typeCast converts BIT buffers→int, other buffers→hex, NEWDECIMAL→float; getSqlClient wraps the SAME handle for SqlClientFactory.

- `./extract-ids-entity-funnel.md` — one param-precedence funnel (~25 id params, first-match-wins) resolves base/source/context with eager typed 404s BEFORE handlers; internal-API escape skips base resolution.
- `./extract-ids-view-binding-split.md` — personal/locked views attach under Symbol.for('nc:view') ONLY for real view branches; share uuids set shareViewType separately so anonymous requests never gain personal-view ACL gates.
- `./legacy-extract-table-pin.md` — v1 funnel pins ncTableId for table-visibility ACL (14 assignments → one store), late-recovers query-param views behind instanceof View guards, falls back to default workspace by @Acl scope.
- `./acl-evaluation-ladder.md` — eleven ordered aclFn gates: implicit guard bootstrap (console-logged failures), public-base stamp, personal-view owner/editor restrictions BEFORE role checks, token-class blocks, include/exclude resolution, self-leave escape LAST, source-readonly tail.
- `./acl-inheritance-boot-compile.md` — rolePermissions compiles at module load: includes inherit UP role order per scope, excludes DOWN reversed; five boot-time validators throw; SUPER_ADMIN='*'; org VIEWER collapses to CREATOR.
- `./share-access-source-ladder.md` — resolveShareAccessSource maps share params→SHARED_FORM/VIEW/BASE/DOC (publicDataUuid≡sharedView); isSharedViewAccess reads positively because shared bases also set is_public but keep full access.
- `./frame-guard-embeddable-ladder.md` — setFrameGuardHeaders omits BOTH headers on embeddable share paths (route regex + /p/ redirect + legacy hash shell + public API prefixes), emits XFO+CSP elsewhere; redirect hops must stay header-free.
- `./global-request-stamping.md` — outermost middleware stamps site-url/full-url/tab-id/dashboard before ExtractIds; tab id UUID-validated BEFORE storage because it keys per-tab undo stacks.
- `./body-parser-matrix.md` — type-scoped parsers: json (json+scim+json, NC_REQUEST_BODY_SIZE default 50mb), raw */* for signature verification, urlencoded extended; first parser consumes the stream — never stack.
- `./rate-limiter-guard-trio.md` — three dependency-free CanActivate shells partition data/meta/public surfaces so a public flood can't starve authenticated schema ops; budget logic lives in one shared limiter, never in the guards.
- `./auth-token-base-scoping.md` — xc-token strategy rejects base-scoped tokens hitting another base ('Invalid token', no oracle); account-wide tokens skip; userless legacy tokens get EDITOR roles via getWithRoles otherwise.
- `./oauth-bearer-route-confinement.md` — OAuth bearers pass a route allowlist (/mcp, /api/v3/, /auth/user/me) then granted-resource matching that FAILS CLOSED on missing context (CWE-863); deleted client kills tokens (CWE-613).
- `./shared-base-pseudo-user.md` — xc-shared-base-id authenticates as pseudo-user from SHARE's roles after ordered guards: friendly private-base error only when share row exists, opaque 401 on uuid mismatch.
- `./google-strategy-verification.md` — Google sign-in rejects unverified emails pre-lookup via collect-all-flags checker (passport profile + ID-token shapes); fails closed ONLY when a flag is present-and-false.
- `./pollable-decorator-swap.md` — @Pollable swaps method → enqueue-and-return-{id} wrapper; __original stash lets workers bypass without re-enqueue loops; serializeWorkerArgs throws on function args at the CALL SITE.
- `./use-worker-env-gates.md` — @UseWorker evaluates topology gates at DECORATION time and subscribes worker:job:<id> BEFORE enqueueing so completion can't outrun the listener; blocking RPC unlike Pollable's {id}.
- `./nc-upgrader-ratchet.md` — app migrations run under ONE meta transaction over zero-padded version list, re-writing NC_CONFIG_MAIN after each step; fresh installs short-circuit when STORE table absent.
- `./undo-scope-resolution.md` — undo stacks partition by ScopeType stamped at forward-record time (inverse ops inherit); ancestor scopes prune stranded undone rows; SIDEBAR_FIELDS routes renames to base stack vs content edits to entity stack.
- `./capture-bag-side-effect-ledger.md` — 21 typed capture slots record side effects AT CREATION POINTS so undo/redo replay id-stably; macro ops auto-transcribe nested traced children with resolvedExtra snapshots.
- `./operation-catalog-contracts.md` — 169-name OperationName enum lives in CE; name@version keys registry lookup AND changelog event column; versions coexist until old rows drain; inverse-pair ops carry doc-comment specs.
- `./ce-stub-parity-trace.md` — TraceCommand/Untraced/NcCache/OperationRegistry ship as semantic no-op stubs keeping FULL types so EE overrides swap implementations without touching call sites.
- `./gui-shell-serving.md` — tri-state disable (split-frontend URL / missing dist / missing index.html) resolved in constructor; Accept-header branch between static fall-through and branded shell; frame-guard set BEFORE any branch.
- `./connect-time-db-ssrf-validation.md` — SSRF moves INTO the driver's socket setup (knex connection.stream factory): pg wraps connect() translating positional→options form, mysql2 must return an ALREADY-connecting socket (it never calls .connect() on supplied streams); validatingLookup fails CLOSED if ANY Happy-Eyeballs address is blocked; IP literals validated directly; IPv4-in-IPv6 transition encodings normalized first; file copied VERBATIM into nc-sql-executor — no app imports allowed.
- `./ssrf-policy-gate.md` — ONE source-keyed enable function: Cloud always enforces (env bypass ignored for tenants), then global NC_DISABLE_SSRF_PROTECTION, then per-source NC_ALLOW_LOCAL_* (HOOKS/EXTERNAL_DBS/DATA_IMPORT); disabled getFilteredAgents returns {} so call sites spread unconditionally.
- `./ssl-filepath-timing-oracle.md` — three stacked defenses vs ssl.{ca,key,cert}FilePath probing: reject BEFORE any disk access (no timing/error signal), pad both success and failure to a common 1500ms floor via hrtime try/finally, and size the floor above driver connect-timeout or the timeout tail still leaks.
- `./telemetry-only-socket-gateway.md` — websocket safety by SURFACE MINIMALITY not auth: JWT guard failure swallowed (connection accepted) but only 'page'/'event' telemetry handlers exist, so anonymous connections reach nothing sensitive; attribution id = md5(server UUID + user id); adding any real channel converts this to a bypass.
- `./socket-redis-adapter-mock-twin.md` — RedisIoAdapter falls back to ioredis-mock when no Redis URL: single-node installs run the IDENTICAL @socket.io/redis-adapter code path as clusters (pub client + duplicate() sub client required); app code never branches on deployment shape.
- `./ce-noop-realtime-shell.md` — NocoSocket/NocoPresence ship as signature-preserving no-op stubs so 34 consumer services compile unchanged in CE; EE swaps implementations with identical static API (underscore params mark intentionally-unused args).
- `./fire-and-forget-event-bus.md` — Emittery emit returns a promise that rejects if ANY listener rejects; discarding it = process-fatal unhandledRejection from one throwing webhook listener; the catch lives INSIDE the emitter (emit keeps sync void signature), logging the event name.
- `./refcounted-pubsub-demux.md` — ONE shared redis 'message' listener demuxes via Map<channel, Set<handler>> (no MaxListenersExceeded at N channels); wire-level UNSUBSCRIBE only when the LAST local handler leaves AND keepRedisChannel=false (restart cycles can leave the channel warm); per-handler try/catch isolates throwers.
- `./lru-map-eviction-hooks.md` — O(1) LRU on Map insertion order (get promotes via delete+reinsert; keys().next().value IS the victim); EVERY removal path fires onEvict exactly once; asyncDelete/asyncClear remove from map BEFORE awaiting cleanup so concurrent readers never see mid-teardown values.
- `./throttler-crosspod-rollup.md` — rate-limit logging across pods without locks: every pod INCRBYs a shared expiring counter, SETNX elects one leader to log; leader TTL = 1.5× window absorbs clock skew (a duplicate would fire at exactly 1×); cache-down degrades to local-count logging, never signal loss; Symbol.for singleton survives hot reload.
- `./signup-claim-race-guard.md` — serialize account creation across the bcrypt gap with a short-TTL (10s = crash net lifetime) SETNX claim per canonical email; wrap ONLY the create branch (invite path updates existing rows and must not reject); release delete errors swallowed because TTL releases anyway.
- `./slot-mapped-dependency-table.md` — fixed physical schema serves heterogeneous event types via a logical-field→slot mapping: ≤3 indexed queryable columns + meta JSON; typed coercion at extraction, hydration drops unmapped keys, getSlotId returns NULL for meta fields (unqueryable by construction); integration-store slot_0..9 twins the pattern.
- `./per-request-mcp-server.md` — stateless HTTP MCP: fresh McpServer + sessionIdGenerator:undefined transport PER REQUEST, closed on res close; authorization is STRUCTURAL — write tools registered only for EDITOR+ users, aggregate tool only in CE, so viewers never receive definitions that could error.
- `./mcp-attachment-allowlist.md` — LLM-supplied attachment paths are checked against MetaTable.FILE_REFERENCES for THIS base (deleted:false) before streaming; trust boundary = recorded uploads, not caller strings; per-file failures become {error} data entries grouped into one markdown response.
- `./redis-url-tier-ladder.md` — three independent Redis addresses resolve through ONE getter: CACHE/JOB/THROTTLER tiers each fall back to NC_REDIS_URL; availability checks and connections MUST use the same tier or planes disagree about whether Redis exists.
- `./limit-offset-admission.md` — invalid limit/offset NEVER error: non-positive/non-integer → default 25, clamped [env-min, env-max=1000]; page beats raw offset ((page−1)×limit); limitOverride applies LAST and UNCLAMPED as the trusted-caller escape hatch.
- `./pagination-envelope-duality.md` — v2 offset math degrades fractional pages to explicit offset DELETING pageInfo.page rather than lying; v3 HATEOAS links build ONLY when flags confirm neighbors and round-trip an explicit allowlist of query params (dropping sort/where would change next-page content silently).
- `./plugin-manager-lifecycle.md` — DB rows own ACTIVE config, code classes own schema/builder; init seeds 19 plugins (duplicate-id boot check) then ENV activation overwrites UI-saved input each boot (NC_S3_*/NC_SMTP_*); storage falls back to Local adapter, emailAdapter returns null-for-invite but throws-for-webhook.
- `./boot-permission-reflection.md` — boot-time pg has_database_privilege probe downgrades instead of crash-looping restricted deployments: missing CREATE ⇒ set NC_DISABLE_PG_DATA_REFLECTION=true + warn, downstream minimal-databases features read the flag rather than re-probing.
- `./datasource-encryption-backfill.md` — first-boot-with-key sweeps SOURCES (own scope) + INTEGRATIONS (workspace scope) where is_encrypted false/null: per-row isolation (invalid JSON logged, siblings continue), commit on partial success, rollback only when EVERY attempted row failed.
- `./env-admin-reconciliation.md` — NC_ADMIN_EMAIL/PASSWORD upsert runs four branches under one transaction: first-user insert; email-change migrates the collision account's base memberships keeping HIGHER role (numeric ladder compare) THEN deletes it; same-email re-hashes against stored salt; no-super upgrade-or-create; token_version rotates on every credential write.
- `./public-query-key-blocklist.md` — shared-view sanitization deletes exactly TWO keys (getHiddenColumn, nested) because they are pure privilege switches; compositional surfaces (where/sort/filterArr/group-by) strip per leaf/term instead so multi-field searches degrade, never fail open.
- `./comment-body-sanitize-allowlist.md` — comments sanitize at WRITE time via exported DOMPurify config (20 tags, href pinned to http(s)/mailto, form/svg/math explicitly forbidden against parser confusion); config-as-data lets documents/notifications share the identical policy.
- `./stage-timer-null-profiler.md` — zero-overhead instrumentation: StageTimer.start returns null when NC_PERF_LOG off so timer?.mark() collapses to a null check; marks measure deltas since LAST mark (phases tile the timeline); end() emits at INFO regardless of global debug level.
- `./boot-order-version-ratchet.md` — meta provider refuses version <100002 (and legacy-projects-without-config) BEFORE migrations touch data; ordering rationale: admin-from-env precedes upgrader (migrations assume super user), encryption backfill follows it (new columns exist), firstEeLoad computed from migration-table trio.

- `./query-client-dialect-factory.md` — DBQueryClient.get() is total-by-undefined (no default arm; unsupported ClientType surfaces as TypeError at the caller) while fromKnex() throws loudly on unknown knex clients; MYSQL falls through into MYSQL_LEGACY; dbVersion stamped post-construction.
- `./client-type-identity-admission-split.md` — clientType getter is IDENTITY (registry dispatch), validateClientType is ADMISSION (accepts engine aliases); mysql widens to ['mysql','mysql2'] but keeps canonical error text.
- `./query-client-sql-micro-abstractions.md` — temporaryTableRaw binds (VALUES ...) AS alias(cols) with values-before-identifiers in ONE positional array; tableAlias is a METHOD because Oracle rejects AS on table aliases (ORA-00907); concat/simpleCast per dialect.
- `./concat-cast-required-pair.md` — concat + simpleCast are the two abstract members every dialect must implement; call sites speak standard vocab (TEXT) and mysql translates TEXT→CHAR internally.
- `./batch-pk-update-case-funnel.md` — batchUpdate emits per-column CASE WHEN over a deduped pk IN-list in one statement; three early null returns; undefined-valued rows dropped per column (MSSQL binding-validation quote pinned); Oracle excluded because its CASE types from first THEN (ORA-00932).
- `./batch-update-admission-gate.md` — BaseModelSqlv2 takes the fast path only when primaryKeys.length===1 AND dialect ∈ {pg,mysql,sqlite,mssql}, built fromKnex(transaction) so the CASE participates in rollback.
- `./pagination-orderby-hook.md` — ensurePaginationOrderBy is an IDEMPOTENT last-resort hook before LIMIT/OFFSET: base no-op (pg/mysql/sqlite), mssql overrides for T-SQL OFFSET/FETCH ordering rule.
- `./aggregation-strategy-registry.md` — AGGREGATION_HANDLER_REGISTRY maps five ClientTypes to handler classes; GenericAggregationHandler.generate runs buildContext → category dispatch → postProcess → wrap(COALESCE-exempt dates, alias, toQuery).
- `./registry-stub-registration-doctrine.md` — mssql/oracle are REGISTERED CE stubs that throw EE_ONLY so errors self-classify (registered-but-EE vs unregistered-gap); same import paths resolve to EE overrides in EE builds.
- `./query-client-ee-stub-doctrine.md` — CE stub doctrine for query clients: identical module paths both builds, single canonical throw message per class, pure constants (SINGLE_QUERY_DEFAULT_VIEW 'nc_default_view') stay in CE files; stub doc preserves the 42703 hash-vs-set lesson.
- `./single-query-hash-grouping.md` — compiled single-query plans live as FIELDS of one Redis HASH per (model,view): invalidation is ONE atomic DEL; SET-indexed predecessor stranded variants → PG 42703 incident (documented in the CE stub itself).
- `./exec-parse-bulkaggregate-contract.md` — five client methods throw 'Not implemented' as loud cross-build surface; execAndParse({bulkAggregate:true}) force-skips record converters then JSON.parses '{'-prefixed strings — the consumer half of the JSON row selectors.
- `./category-default-unsupported-degradation.md` — category methods default to undefined (=unsupported); dialect-gaps degrade per-column silently while user-picked invalid combos throw 501 — two channels chosen by who can fix it.
- `./coalesce-exemption-wrap.md` — wrap() COALESCEs everything to 0 EXCEPT EarliestDate/LatestDate, then applies alias AFTER coalesce; new NULL-legit aggregations must edit this list.
- `./aggregation-context-contract.md` — AggregationGeneratorParams (caller→handler) and AggregationSqlContext (dialect-extended) carry everything category methods need; handlers instantiate per call with zero shared state under Promise.all fan-out.
- `./client-handler-delegation-split.md` — subclasses no longer override generateAggregateQuery: the base resolves getAggregationHandler(this.clientType); aggregate/bulkAggregate delegate to curried orchestration closures taking the client — adding a dialect touches nothing shared.
- `./aggregation-prelude-gates.md` — applyAggregation order: silent skips (missing pair, stored colOptions.error) → SDK validateAggregationColType (false/unknown ⇒ notImplemented throw) → Barcode/QrCode value-column swap keeping original id → virtual-column expression via getColumnNameQuery → pg numeric-formula IEEE double-bind → dispatch via fromKnex.
- `./virtual-column-identity-substance-split.md` — Barcode/QR aggregate their VALUE column but keep the requested column's id ({...valueCol, id: column.id} spread order) so stats attach to the visible cell.
- `./non-finite-binding-style-triptych.md` — excludeNonFiniteSql applies three ways: builder ⇒ wrap+double-bind (applyAggregation), escaped text ⇒ compose inline never re-bind (genRollupSelectv2), toQuery output ⇒ re-escape; count family keeps Infinity rows by design.
- `./aggregate-column-resolution-precedence.md` — resolveAggregateColumns: explicit pairs override+narrow view config when non-empty; gc.show gates before type resolution; system fields dropped unless show_system_fields; system Links/LTAR always excluded; no-view path requires explicit pairs.
- `./aggregate-filter-stack-order.md` — four ANDed groups in fixed order (RLS group, view-root when viewId, args.filterArr, xwhere) + soft-delete appended separately; selectors aliased col.id under Promise.all; result remapped id→title with ?? passthrough.
- `./xwhere-listargs-parity.md` — aggregations parse where-strings through the SAME _getListArgs + extractFilterFromXwhere machinery as list endpoints against the SAME columns snapshot — parity by construction.
- `./rls-first-opaque-group.md` — RLS conditions enter FIRST wrapped as ONE opaque Filter group so policy structure survives conditionV2 tree-walking and no later group can widen visibility.
- `./soft-delete-post-filter-append.md` — getSoftDeleteFilter rides a raw qb.where AFTER conditionV2 (it's a fragment, not a Filter node); conjunctive append means no user filter can resurrect deleted rows.
- `./selector-fanout-vs-bucket-serialization.md` — single-mode fans selector generation across columns under Promise.all (generation is IO); bulk-mode serializes buckets because each tQb builder is consumed by its own row selector.
- `./orchestration-error-asymmetry.md` — aggregate rethrows after logging (single footer fails loudly); bulkAggregate swallows into {} (dashboard renders N buckets); user-input filter validation sits OUTSIDE bulk's swallowing try.
- `./bulk-aggregate-bucket-funnel.md` — bulkFilterList filterArrJson parsed BEFORE the swallowing catch (malformed bucket = 400, never silently-unfiltered aggregation); six stacked groups per bucket; expressions generated PER set with baseQuery=tQb ("Phase 2 correctness invariant"); outer qb.limit(1).
- `./bulk-row-selector-json-packers.md` — pg JSON_BUILD_OBJECT / mysql JSON_UNQUOTE(JSON_OBJECT)+limit(1) / sqlite json_object, all wrapped (tQb) as alias; mysql's UNQUOTE+limit are load-bearing (scalar subqueries multi-row without it).
- `./aggregate-id-title-remap.md` — SQL aliases are stable ids, API keys are mutable titles; unknown keys pass through on remap (driver case-folding defense); bulk nests {bucketAlias: {colId: value}}.
- `./pg-empty-cell-sentinel-ladder.md` — condnValue sentinel ladder: '' default, NULL for typed columns incl. NATIVE PG ENUMS (pg_enum_type_name escape — '' raises on enums), 0 for Rating; interpolated raw into SQL.
- `./dialect-sentinel-list-diff.md` — pg's typed-sentinel list has 16 entries vs mysql/sqlite 14 (UUID/AutoNumber only typed on pg where ''-comparison breaks); sentinel list ≠ filled-test list by design.
- `./filled-vs-sentinel-list-asymmetry.md` — two distinct membership rules: sentinel list = "''-comparison BREAKS", typed-filled list = "''-comparison POINTLESS" (adds JSON/Lookup/LTAR); pg filled list 19 entries vs sentinel 16.
- `./json-empty-cell-probe-trio.md` — CountEmpty JSON branches: pg IS NULL (jsonb can't compare strings), mysql JSON_LENGTH IS NULL, sqlite json_array_length IS NULL — empty arrays stay FILLED everywhere (= '{}' nowhere in codebase).
- `./json-typed-list-membership-rule.md` — two-question test for new types: does sentinel-comparison break? does IS NOT NULL suffice for filled? JSON answers yes/yes (dedicated branch + typed list), Rating answers no/no (0 sentinel + bespoke arms).
- `./filter-vs-case-count-parity-matrix.md` — FILTER↔SUM(CASE) and NULLIF↔IFNULL translation matrix across the common family; JSON predicates are engine-specific and never cross-copied; denominators use COUNT(*) of the filtered set.
- `./percent-denominator-zero-safe-shape.md` — percent recipe: float-literal *100.0 BEFORE division, total-row COUNT(*) denominator (per-row not per-filled-cell), NULLIF(pg/mysql)/IFNULL(sqlite) guard, wrap COALESCEs empty-set results to 0.
- `./rating-zero-as-empty-numerics.md` — Rating zero-rule is PER-FUNCTION: Avg/Min/Stddev filter !=0, Sum/Max don't; pg Range comment pins FILTER-binds-to-adjacent-aggregate and SDK reducer parity (max includes zeros, min excludes).
- `./sentinel-binding-vs-interpolation-split.md` — numerical rating arms BIND condnValue as parameter (pg FILTER positions); common-family predicates INTERPOLATE it as compile-time constant text — never mix, never interpolate runtime data.
- `./date-range-calendar-month-parity.md` — MonthRange is CALENDAR-month diff everywhere (pg year*12+month arithmetic deliberately NOT AGE(); mysql PERIOD_DIFF; sqlite strftime*12+month); Earliest/Latest are the only NULL-allowed names in wrap.
- `./date-range-cast-first-vs-diff-first.md` — DateRange semantics split: pg casts ::date THEN subtracts (calendar days), mysql TIMESTAMPDIFF(DAY)/sqlite JULIANDAY floor elapsed time — intra-day spans differ by one day BY DESIGN; unit contract integer-days.
- `./median-window-vs-limit-offset.md` — median without percentile_cont: mysql8 ROW_NUMBER window averaging middle-two rows (FLOOR((N+1)/2),(N+2)/2)), sqlite LIMIT 2-(N%2) OFFSET (N-1)/2 over non-null ordered subset; even-N interpolation contract exact.
- `./stddev-population-fallback-composition.md` — sqlite hand-rolls population stddev SQRT(Σ(x-avg)²/N) over a two-level derived table with rating filter injected into BOTH avg and variance scans + COUNT>0 NULL guard; pg stddev_pop native; mysql STDDEV native.
- `./filtered-derived-table-materialization.md` — engines without FILTER re-materialize caller filters: baseQuery.clone().clearSelect().select((expr) as nc_val) becomes FROM-source nc_agg_sub for median/stddev/attachment subqueries; raw-table fallback without baseQuery.
- `./attachment-size-json-array-sum.md` — attachment size unwinds JSON arrays natively: pg jsonb_array_elements with COALESCE inner sum, mysql JSON_TABLE lateral over CAST AS JSON, sqlite json_each; all self-contained scalar subqueries over filtered sets.
- `./aggregate-service-entry-gates.md` — service entries: grid-only gate BEFORE parsing (data-table badRequest; public twin notFound to hide existence), tolerant JSON.parse of filterArrJson/aggregation, dispatch DBQueryClient.get(source.type); internal UiPost module shares the same path.

- `./median-stddev-per-dialect-recipes.md` — per-engine percentile/percentile-adjacent recipes: pg percentile_cont(0.5), mysql ROW_NUMBER window, sqlite LIMIT/OFFSET pair-average; Rating Range is MAX(all)-MIN(nonzero) with the FILTER-binds-to-adjacent-aggregate comment; bulkAggregateRowSelector JSON packers per dialect.
- `./delimited-token-order-preserving-rewrite.md` — replaceDelimitedWithKeyValue rewrites delimited id lists to display names IN ORDER via pg WITH ORDINALITY / sqlite json_each.key position threading through string_agg/GROUP_CONCAT order-by; without it aggregate order shifts when the user stack changes, silently corrupting User/CreatedBy sorts.
- `./bucket-vs-global-filter-stratification.md` — bulk buckets stack SIX filter groups: global args.where AND bucket f.where stay SEPARATE layers extracted from the same aliasColObjMap; validated JSON rides last; bucket filters are additive so no slice can widen the view's scope.
- `./ast-depth-cycle-guards.md` — getAst caps client-nested LTAR expansion at depth 8 (truncation returns an empty AST subtree) while extractDependencies stops Lookup cycles with a visited Set — the guard that prevents Knex's self-containing-SELECT infinite loop; lookup dependency buckets normalize missing nested/fieldsSet before mutation.
- `./ast-strategy-chain-ordering.md` — twelve named strategies evaluated first-match-wins reproduce the legacy if/else ladder exactly; each match encodes only its own rung because array order supplies exclusion; allowRequestedHiddenFields bypasses the view-show gate opt-in-only, never public.
- `./ast-rowcolor-button-deps.md` — row-color (SELECT-mode meta vs FILTER-mode conditions) and button-filter collectors dedupe referenced column ids into a dedicated early strategy rung, the sanctioned way view cosmetics widen payloads; button filters scope to view-visible buttons before reading their filters.
- `./populate-meta-introspection-spine.md` — external-source→metadata funnel runs four ordered waves (relations snapshot → tables via PQueue(5) → deferred virtual/LTAR columns → views) where virtual columns MUST wait for every real column because their closures resolve sibling models' column ids; executeOperations is stop-on-first-error with queued tasks no-oping after any failure and exactly errors[0] rethrown.
- `./system-column-remap-gate.md` — a table qualifies as NocoDB-created only when ALL SIX marker columns exist together (created_at/updated_at/created_by/updated_by/nc_order/__nc_meta); then — and only then — they remap to semantic UITypes + system:true in place BEFORE display-value selection; partial matches never convert, so user tables with a plain created_at stay untouched.
- `./ag-inference-ladder.md` — mssql PK auto-generation inference needs THREE guards `pk && !cdf && !ai` because IDENTITY columns report no column_default (`!cdf` alone mis-tags every IDENTITY table as NocoDB-generated); databricks keeps the two-way test.
- `./mm-junction-heuristic.md` — a junction table = exactly 2 belongsTo FKs covering exactly the composite PK + <5 normal columns; MM insertion is idempotent via a three-id probe (fk_mm_model_id + fk_child_column_id + fk_mm_child_column_id), non-matching tables get DEMOTED (markAsMmTable false), and each side's HAS_MANY twin becomes a system field; meta-diffs.service carries a deliberately diverged twin with corrupted-data guards.
- `./views-prefix-copy-bug.md` — recorded latent defect: for prefixed meta sources `views = tables.filter(...)` copies the TABLES list over views, so viewsCount counts tables and view Models double-insert as VIEW type; porters must choose bug-compatibility vs fix consciously.
- `./virtual-title-dedup-ladder.md` — duplicate hm/bt titles dedupe per batch with zero-based unsuffixed/1/2 suffixes via `c || ''` coercion (never "Title0"), scoped to titles minted in the current wave only — real columns from prior waves are not consulted, unlike getUniqueColumnAliasName.
- `./execute-operations-queue.md` — NcHelp.executeOperations = PQueue concurrency NC_EXECUTE_OPERATIONS_CONCURRENCY||5, skip-after-first-error, rethrow errors[0] after onIdle; `_dbType` param unused (dialect lives inside closures).
- `./single-query-cache-invalidator.md` — compiled single-query SQL embeds joined models' physical names; referrer discovery is a reverse transitive closure: seed link columns targeting the changed model, expand Lookups/Rollups to fixpoint (relation-hop OR target-column-already-embedding), map columns→models, delete self; EE-gated before ANY discovery query; formulas explicitly out of scope.
- `./cross-base-cache-invalidation.md` — base-scoped discovery misses other bases' referrers; inbound cross-base links read workspace-wide via raw knex then PER REFERRING BASE rebuild context `{...ctx, base_id}` and rerun the closure there — invalidating under the changed model's context silently no-ops; best-effort log-and-continue so a cache miss never 500s a committed rename.
- `./rename-gate-invalidation-dispatch.md` — Model.update/Column.update gate on PHYSICAL name inequality only (table_name/column_name, never title): rename ⇒ transitive graph walk, otherwise cheap one-hop scan (lookup/rollup metadata references ids, so only renames bake stale strings into compiled SQL).
- `./ltar-pair-insert-contract.md` — one link = two Column.insert rows under EACH side's own base context; dr/ur hardcoded 'NO ACTION' to keep shouldCascadeLinkCleanup truthful; self-referencing links forced system; custom links stamped version:1; oo discriminates sides via meta.bt:true on the child only.
- `./rollup-lookup-validation-ladder.md` — rollup/lookup creation validates existence→shape-gate→aggregatability (with suggestive eligible-fields error text)→function availability; `isMMOrMMLike ? 'mm' : relation.type` forces junction columns down the mm arm; circular lookup detection walks fk_lookup_column chains AFTER obtaining the relation's refContext (cross-base safe).
- `./constraint-name-generators.md` — FK/index names = prefix + 10-char sanitized stems ×2 + 10-char nanoid tail ≤40 chars against dialect 64-char limits; companion getMMColumnNames truncates junction FK columns to slice(0,30)_id with a 29-char+1 self-ref disambiguator.
- `./column-sanitize-alias-resolution.md` — sanitizeColumnName is opt-OUT via two env kill-switches, preserves INTERNATIONAL letters (SDK regex classes), turns all-underscore into 'field', lowercases on databricks; getRefColumnIfAlias maps CreatedTime/LastModifiedTime/CreatedBy/LastModifiedBy aliases to SYSTEM twins by uidt equality; deleteColumnSystemPropsFromRequest strips 13 DDL props with per-OperationSource system-flag matrix (AT_IMPORT keeps ncRecord* system flags, SYNC honors caller flags).
- `./system-column-roster.md` — write-side truth for managed tables: id(ID)/created_at/updated_at/created_by/updated_by/nc_order + __nc_deleted (isMeta-gated) + META_COL_NAME (isMetaColSupport-gated); allowNonSystem marks which names users may still claim; must stay name-parity with populateMeta's read-side six-name map.
- `./links-count-auto-column.md` — creating a Links field auto-inserts a Links-count rollup (`rollup_function:'count'`, pk-or-first-column fallback, `<Related> Count` title) and hides the RELATION's grid entry in views[0], not the new count column.
- `./formula-reference-scan.md` — pre-delete dependency scan walks formulaJsep ASTs matching Identifier nodes equal to column.id OR title (both forms persist); CallExpression checks arguments ONLY (callee names ignored); Buttons qualify only when type==='url'.
- `./ajv-validation-funnel.md` — ONE shared Ajv instance (allErrors, ajv-errors, addFormats) serves both the express middleware (400 JSON {message,errors}) and service-level validatePayload (throws ajvValidationError; unknown schema = 404-style genericNotFound); getApiTokenFromHeader prefers trimmed xc-token then case-insensitive 'bearer ' Authorization prefix.
- `./command-palette-base-list.md` — baseListAll caches per-user rosters (NO_ACCESS excluded, soft-deleted bases filtered) wrapped in one synthetic workspace, then registers its key into the palette's ws/user lists so EXISTING cleanup piggybacks — no dedicated eviction exists; PAT scoping post-filters the cached envelope by Set membership.
- `./get-cache-middleware.md` — entire module: GET-only `Cache-Control: public, max-age=2592000` (30-day default); method gate keeps mutations uncacheable.
- `./lookup-travel-revtype.md` — travelLookupColumn recurses lookup chains through each relation's refContext returning the terminal column, degrading broken chains (stored error / missing relation) to null instead of throwing; getRevType inverts exactly bt↔hm and many_to_one↔one_to_many while mm/oo pass through unchanged.
- `./required-field-presence-split.md` — validateRequiredField is the boolean counterpart of validateParams: presence = key-in-payload AND ≠undefined AND ≠null (empty-string/false pass); use when absence needs branching rather than an immediate throw.
- `./db-error-extractor-dispatch.md` — DBErrorExtractor fingerprints the client from error-code SHAPE (ER_*/ORA-/SQLITE_/5-char SQLSTATE/tedious codes, ordered most-specific first) then dispatches to a per-dialect registry; unknown shapes trial all five extractors and fall to an outside-map default unless ignoreDefault.
- `./pg-extractor-sqlstate-table.md` — the PG SQLSTATE ladder turns detail/hint into user-safe templates (23505 parses Key(col)=(val), 23503 strips physical table names, XX000 deliberately returns undefined so backend internals never reach user toasts).
- `./mysql-sqlite-extractor-twins.md` — mysql switches on ER_* driver codes while sqlite squeezes seven message-shape regexes out of SQLITE_ERROR alone; both legs ship NO details field on the wire unlike pg.
- `./transient-error-taxonomy.md` — isTransientError's four layers (NcBaseErrorv2 types → socket codes → per-driver code/number sets incl. the deliberate EREQUEST hole → length-guarded message phrases) decide which failures self-heal vs poison stored state.
- `./unique-constraint-funnel.md` — handleUniqueConstraintError detects 23505 at every nesting level (stringified deep search as backstop), resolves the offending column via detail-parse→payload→live-DB-probe ladder, relabels through form-view custom labels, and ALWAYS throws.
- `./ltar-text-link-conversion.md` — text↔link conversion is a five-step ordering contract (paged snapshot → temp-titled LTAR create → hard-delete source → rename BEFORE backfill → append-only backfill) with fresh-reuse-per-subop meta-cache armor and circular-safe broadcasts.
- `./nc-error-version-taxonomy.md` — NcError.get picks V1/V3 from context.api_version; the whole v3 behavioral diff is one 14-entry codex table re-registering not-founds as 422s plus two method overrides.
- `./webhook-condition-evaluator.md` — validateCondition is the JS-side twin of the SQL filter grammar: dayjs day-granularity date sub-ops, user-field set logic, recursive-array blank detection, three-valued or/and/not fold.
- `./meta-dependency-cascade.md` — MetaDependencyEventHandler repairs dependents in ONE lazy transaction under a forced-suppression next-context (the recursion guard); getAffectedDependency gates whether any tx opens at all.
- `./sql-sanitize-ddl-guards.md` — three DDL interpolation guards: sanitize/unsanitize ?-escape codec, pgQuoteLiteral for the PG positions that reject bind params, and the dtxp allowlist whose absence would let `1) CHECK(1=0` write persistent schema injection.
- `./disposable-email-gate.md` — two-tier matcher: exact match against ~3.6k domains plus dot-prefixed wildcard parents (the '.' separator is load-bearing); returns undefined-not-false.
- `./notifications-longpoll-registry.md` — held per-user HTTP responses woken cross-pod via PubSubRedis channel; refcounted unsubscribe keeps the channel while any local connection remains; 30s refresh + shutdown flush ladders.
- `./notification-triple-delivery.md` — durable row THEN pubsub publish THEN same-pod fan-out; AppHooks-driven producers (PROJECT_INVITE/WELCOME); PagedResponse third-arg {unreadCount} envelope.
- `./meta-json-column-codec.md` — prepareForDb/prepareForResponse: idempotent stringify/parse with swallow-and-fallback, applied only to props present on the object (absence survives round-trips).
- `./audit-fire-forget-insert.md` — Audit.insert never awaits outside tests (TEST env default), .catch-logs errors, NC_DISABLE_AUDIT kill-switch; recordAuditList reads via id-cursor keyset with limit+1 pop lookahead.
- `./oauth-code-validate-then-claim.md` — authorization-code single-use CAS (rowcount WHERE is_used=false) deferred until EVERY precondition passes so failed exchanges don't burn codes; mandatory is_oauth_token JWT discriminator.
- `./oauth-refresh-revokeifactive-cas.md` — refresh rotation mints BEFORE revoking so a signing failure can't destroy the presented chain (GHSA-353r); concurrent refreshes arbitrated by the CAS loser.
- `./mail-send-gates-audit-ledger.md` — adapter soft-null once-warn, NC_SITE_URL refusal gate with once-per-install super-admin nag, then send-capture-ledger rows (status/error≤8000/sent_at-null) with privacy skip-set.

## Capsule map
- **Queue core** — `fallback-queue`: a process-local PQueue (concurrency 2) with a memory job registry; add() is idempotent by jobId, repeat jobs reschedule only after completion, delayed jobs use unref'd timers, static reset() cancels pending timers for test isolation.
- **Worker admission** — `jobs-processor`: every job passes a three-gate admission (registered name, resolvable fn, version match) else requeues; local per-type concurrency caps; 60-attempt exponential backoff (5s→60s, ~57min) then FAILED; 10-min long-run watchdog.
- **Enqueue contract** — `jobs-service`: add() strips cycles via getTrueCircularReplacer, upserts the `nc_jobs` meta row (WAITING), stamps `_jobVersion` from JobVersions, and skips meta entirely for SKIP_STORING_JOB_META types (nanoid id only).
- **Event fan-out** — `jobs-events`: active/failed/completed each publish the result to pub-sub, update the nc_jobs row, and emit an in-process EventEmitter STATUS event; REQUEUED is a non-terminal special case.
- **Long-poll surface** — `jobs-polling`: `/jobs/listen` holds HTTP responses in per-job rooms, replays `_mid`-incremented cached messages, sends 30s refresh keepalives, tombstones closed jobs for 60s, and always frees per-job state in a finally.
- **Versioned migrations** — `migration-jobs`, `migration-job-example`, `migration-trash-backfill`, `migration-orphan-links`, `migration-ee-ce-skew`, `attachment-migration-ledgers` (_001), `thumbnail-backfill-migration` (_002), `link-recovery-witness` (_003), `order-column-backfill` (_005/_014), `audit-id-migration` (_009), `soft-delete-upgrader-mode`/`soft-delete-unique-conversion`/`soft-delete-meta-fastpath`/`migration-queue-backpressure` (_010), `sqlite-boolean-default-repair` (_011), `orphan-view-column-gc` (_014-cleanup; _004/no_op are deliberate empty slots): a versioned list runs one migration per job invocation; a store-backed lock (5s confirm + 10-min stall release + 5-min heartbeat) prevents concurrent runners; each success re-enqueues the next; concrete instances show the batched meta-scan with dialect rename SQL (nc_job_008), the resumable DISTINCT-tuple trash backfill (nc_job_012), the owning-column-gated orphan reaper (nc_job_013), and the EE/CE same-version different-service skew.
- **Cache safety** — `cache-bypass`: runWithoutCache(fn) scopes cache reads/writes to no-ops via AsyncLocalStorage; invalidation (`del`) and counters still pass through; per-async-tree, nested-safe.
- **Redis queue** — `redis-jobs-service`: Bull add() keeps meta-row id == Bull job id; toggleQueue() lazily resumes the primary when workerCount()==0 and pauses it when workers exist; removeOnFail=1000 ring buffer.
- **Instance commands** — `instance-command-bus`: two role channels (`<env>-primary/-worker`) carry colon-packed command frames dispatched through static callback maps; workerCount() = subscriber count of a probe publish.
- **Cache envelope** — `cache-envelope`: every cached value carries parentKeys back-pointers + timestamp; lists are SETs of child keys expiring NC_REDIS_TTL−60s (never outlive children); reads older than grace TTL refresh themselves.
- **Cache self-heal** — `cache-list-selfheal`: any null child in mget poisons the list → deepDel every referenced parent → rebuild from source of truth; `'NONE'` member caches "definitely empty".
- **Cache gate matrix** — `cache-gate-matrix`: read/write ops respect disable+bypass; del/deepDel/incr* pass through bypass (transaction invalidations survive); setIfNotExist/hash-lock ops bypass even bypass.
- **Import pipeline** — `import-streaming`, `import-handler-registry`: papaparse/xlsx handlers expose preview()+async streamRows(); AsyncQueue holds each row's resume until consumed; 1000-row flushes swap buffers before awaiting.
- **JSON/Excel streaming handlers** — `json-import-streaming`, `excel-import-streaming`: peek/unshift array detection + bracket-wrap object roots + mandatory pipe-error forwarding; WorkbookReader minimal options + cell-value union unwrapping before typing + non-target-sheet draining.
- **Import failure policy** — `import-error-ladder`, `import-error-formatter`: SDK-typed row errors salvage good rows one-by-one; 20 system errors abort; formatter attributes embedded values to columns only in single-row retries.
- **Import links** — `import-ltar-links`, `import-link-stream`, `import-link-buckets`, `import-csv-stream`: display values resolve post-insert against cross-flush negative caches (chunks of 200, concurrency 25); duplicate flows ship mm tuples on a dedicated link stream deduped by handledLinks; link rows bucket per junction table with lazy column resolution; the data stream translates headers through the idMap and realigns pg sequences.
- **Import schema** — `import-column-mapping`, `import-naming`, `import-type-detection`, `csv-detector-stub-contract`: explicit mappings split scalar vs link destinations; created columns dedupe on TITLE; handlers collect capped string samples but the detector is a DELIBERATE STUB at f7513664 — all columns ship SingleLineText and the heuristic ladder below it is unreachable dead code.
- **Import UX** — `import-progress-frames`, `import-dual-mode`, `import-attachment-stream`: JSON progress frames with cumulative counters; run() serves sync callers with typed results and sanitized errors; temp files die in finally.
- **Job logging** — `jobs-log-fanout`: sendLog emits `{id: job.id.toString(), message}` on an EventEmitter; processors wrap it with debug mirrors; transport lives in jobs-events.
- **Export** — `export-stream-upload`, `export-charset-fallback`, `export-presign`, `export-cleanup`, `export-column-projection`, `export-cell-serialization`, `export-recursive-stream`: upload starts before the pump; legacy charsets fall back to UTF-8+BOM within a 64KB decision window; artifacts get deterministic names + 3h presigns; date-partitioned files die by path-string compare; columns are server-composed (BT→FK child rewrite); cells normalize per uidt with CWE-1236 escape on user-facing exports; pages recurse with single `push(null)`.
- **Export schema serialization** — `export-composite-ids`, `export-col-options-policy`, `export-view-extras`, `export-dashboard-widgets`, `export-idmap`: ids are positional ancestor chains minted in walk order; colOptions follow the remap/preserve/transform/strip policy with dual formula bodies; per-view-type extras (kanban stacks, calendar/timeline ranges, gantt deps) are type-gated rewrites; dashboards serialize through widget handlers carrying the live idMap.
- **Export data formats** — `export-json-excel`, `export-ics-calendar`, `export-base-fanout`, `export-recursive-stream`: JSON frames a streamed array while Excel accumulates and writes one buffer (typed cells ⇒ no formula escape); ICS narrows descriptions to view-visible columns on anonymous exports; whole-source export tees every table's link stream into one merged CSV with post-join error rethrow.
- **Cross-instance migration** — `cross-instance-migration-stream`: one octet-stream POST of typed JSON frames in duplicate.processor phase order (base→users→schema→warnings→extras→scripts→documents→dashboards→workflows→interfaces→per-model data/link); consumer-first axios start, guaranteed pushStream(null) terminator, email-keyed user resolution, shared handledMmList dedup.
- **Thumbnails** — `thumbnail-bomb-guard`, `thumbnail-batch`: header-checked format-aware pixel caps precede decode; batches return per-attachment results keyed to originals and degrade gracefully without sharp.
- **File GC** — `attachment-orphan-gc`: SQL HAVING finds fully-tombstoned references; retention window + adapter check guard deletes; thumbnails removed alongside; meta rows last.
- **Duplication** — `duplicate-compensation`, `duplicate-backfill`, `export-idmap`: serialize models→scripts→workflows→dashboards→interfaces(last); one accumulating idMap threads all phases; failures soft-delete bases or hard-delete created tables; external bt-links get bulk-updated with new ids.
- **Airtable import** — `at-import-pipeline`, `at-import-failure-policy`, `import-dual-listener-fanout`, `import-record-coercion-ladder`, `import-virtual-column-resolution`, `import-view-config-family`, `airtable-backpressure`, `airtable-id-remap`, `airtable-assoc-dedup`, `entity-map`, `airtable-fetch`, `airtable-type-map`, `import-naming`, `at-engine-dual-mode`: phased schema→data→links over closure-shared maps; two-tier failure policy (schema throws compensate by deleting created tables, data errors swallow); one paused stream fanned to row+link consumers over ONE shared queue; per-record alias→coercion→sys-stamp ladder; staged link/lookup/rollup fixpoint; per-type view creators; dual hysteresis backpressure; deterministic id remap; junction claiming; shareId session; explicit type table.
- **Processor patterns** — `webhook-decoupling`, `use-worker-rpc`, `source-create-compensation`, `source-delete-idempotent`, `meta-sync-broadcast`, `attachment-url-upload`, `jobs-module-wiring`, `job-producer-controllers`: id-only payloads rehydrated at execution, allowlisted name-based RPC, compensate-and-rethrow creation, idempotent delegation, broadcast-with-exclusion, thin delegation, and the single-token CE/EE backend swap.
- **Migration machinery** — `migration-searchpath-grandfather`, `keyset-backfill-walk`, `migration-bootstrap-ordering`, `migration-ce-stub-parity`, `ncmeta-now-contract`, `connection-reset-protocol`, `source-update-order-preservation`: grandfathering writes old behavior into rows via a four-null decision fn; keyset walks bound mid-run creations with ncMeta.now() snapshots (never raw Dates); migrations enqueue first-in-queue after worker callbacks register; CE stubs keep version slots aligned; config-changing callers must resetSource after update; order backfill reserves slot 1 for the default source.
- **Config resolution** — `config-merge-override-discipline`, `presence-gated-override`, `driver-coercion-funnel`: decrypt → presence-gated two-key override → post-merge validation → single mssql-normalize funnel; absent keys mean inherit, never erase.
- **Job dispatch & relay** — `jobs-dispatch-table`, `jobs-relay-write-side`, `jobs-listen-shutdown-flush`, `job-timing-helpers`: injectable {this, fn} registry requeues on miss; the write half keeps a 20-message `_mid` ring mirrored to Redis with hint-only pub-sub and finally-block cleanup; shutdown flushes parked pollers with refresh hints; hrtime stopwatch laps chain through one mutated struct.
- **Processor wrappers** — `export-data-processor-v2`, `import-result-aggregation`, `duplicate-granularity-dispatch`, `metasync-dual-method-bodies`, `source-create-delete-processors`, `webhook-rehydration-guards`, `url-upload-passthrough`, `internal-event-job-enqueue`, `thumbnail-processor-path-algebra`, `upload-error-triage`, `export-import-rest-trigger`, `export-cleanup-job-lifecycle`, `repeat-job-registration-gap`: processors orchestrate only (streams live in services), sample errors at the boundary, route granularity by method name, compensate before rethrow, quiet-skip vanished entities, pass payloads through untouched, gate thumbnails producer-side with ROOT context, and audit registration-vs-scheduling before adopting periodic jobs.
- **User roster & cache lists** — `baseuser-roster-query`, `export-users-roster`, `baseuser-service-actors`, `cache-setlist-projection`: inner-workspace/left-base join asymmetry keeps soft-deleted users for references; exports travel email-keyed with workspace_roles carry-through; service actors append post-cache flagged deleted:true so validators match while pickers filter; list caches key children by projected props behind both gates.
- **Widget seam** — `widget-handler-contract`: factory indirection over shape-correct CE no-ops; the porting point for per-type dispatch.
- **Record data plane** — `data-table-single-bulk-funnel`, `data-table-duplicate-guard`, `jobs-meta-list-window`: one funnel serves single+bulk with shape-derived trace ops and a resolved-source readonly re-assertion; bulk mutations pre-flight duplicate pks (composite keys ___-joined); job listings window to 1h-or-live and strip other users' results.
- **Link (LTAR) engine** — `data-table-nested-dispatch`, `data-table-link-swap`, `data-table-link-by-display`, `ltar-cross-base-context`, `ltar-update-placeholder`: five-way relation dispatch behind sanitized queries; copy/paste becomes one self-inverse resolved-pk op deposited for outer undo; display values batch-resolve exact-then-case-insensitive; cross-base links derive four contexts with permissions dropped on re-base; the model's update() is a deliberate placeholder.
- **Meta sync** — `meta-diff-splice-ladder`, `meta-diff-postsync-pk-gate`, `meta-sync-deferred-virtual-groups`, `meta-sync-pk-ratchet`, `meta-sync-mm-junction-heuristic`: splice-diff vocabulary with dialect-gated type compare; pk prediction keeps relation removals from undoing same-run recovery; virtual-column inserts serialize per target table for title uniqueness; manual pk flags ratchet one-way; bridge tables promote into mm pairs via shape heuristic + existence probe.
- **Public/shared surface** — `shared-view-query-confinement`, `public-form-insert-gates`, `public-nested-link-guards`, `public-gantt-links-ltar-duality`, `api-token-sso-scoping`: access-source-gated query confinement with alias-complete keys; anonymous inserts attribute to service users before auditing and write only view-visible fields; nested link routes stack parent-visibility + column-show + predicate gates; dual-layer shape flags flip together; token ownership violations read as notFound.
- **Filter & ACL models** — `hook-filter-tree-flatten`, `model-role-visibility-cache`: hierarchical filters flatten via group-by-parent + order-sort + parent-first DFS (creation order abandoned by documented incident); per-(view,role) visibility rows cache pairwise and cascade-delete per-row to keep list caches exact.
- **Filter write/lifecycle plane** — `filter-insert-scope-ladder`, `filter-cache-multiscope-fanout`, `filter-update-delete-lifecycle`: one insert path serves seven scopes via a first-match FK ladder that derives source_id and keys per-scope ordering (children re-stamped with the root's scope key; caller order honored only under isReplay); redisPostInsert fans one row out to up to five list memberships plus parent twins behind a mandatory-scope 400 gate, then single-query-invalidates with a null-view skip guard; updates attach the cache write to the active knex transaction while deletes are double-delete/self-parent-cycle guarded, child-first, and end in exactly one view-scoped single-query invalidation.
- **Filter RLS enforcement** — `filter-rls-tree-failopen`, `filter-hook-list-scope-mismatch`: RLS must link rows into an in-memory tree BEFORE dynamic-value substitution or unpopulated children reload raw `{currentUser.x}` rows post-substitution and negated operators invert them into match-everything (no depth cap: truncation fails open; cycles unreachable by construction); and a live read/write cache-scope divergence inside rootFilterListByHook (reads VIEW-keyed list, writes HOOK-keyed) shows why cache keys must be derived mechanically from the DB condition expression.
- **Data alias entry & read engine** — `datas-alias-entry-funnel`, `datas-getdatalist-engine`, `datas-grouped-dual-path`: wrappers validate identity (linkColumnId→this model, link target-view substitution) before one shared engine serves every list route with parallel count+fetch, a typed-error funnel, and a dialect-gated grouped fast path whose response contract never forks.
- **Alias boundary correction** — `alias-services-boundary-correction`: the two remaining "alias wrapper" services are boundaries, not pass-throughs — data-alias-nested re-applies the nested-link sanitizer before both consumers and documents the view-show vs column-level-ACL split, while bulk-data-alias reflectively dispatches typed option tuples behind an API-token bulk cap that four of six ops enforce (never the *All variants).
- **Nested-link routes** — `nested-link-query-restriction`, `nested-link-count-asymmetry`: six nested routes share one before-both-consumers sanitizer (shared-view always restricts, exposure = pk+pv+display, strip leaves not the whole where) over synthetic-AST fetches with dedicated count primitives; hmList's `totalRows` vs siblings' `count` envelope split is contract, not drift to fix.
- **Legacy ByViewId surface** — `byviewid-misnomer-contract`: the write/read quartet resolves its "viewId" as a MODEL id by legacy contract; only two of five load real views.
- **Public meta surface** — `shared-metas-trashed-model-guard`, `related-metas-projection-fixpoint`, `shared-metas-secret-strip`: viewMetaGet guards both soft-delete cache cases, projects relatedMetas to a data-agreeing fixpoint envelope via replace-with-copy, and strips password/email-map at the serialization boundary with prototype-preserving copies so future caching can't leak.
- **Filter twin diff** — `filter-hookfilter-twin-diff`: Filter/HookFilter getFilterObject trees are copy-invariants in flatten logic but legitimately diverge on cache key shape, DB ordering, and — a live latent trap — Filter's internal condition-vs-cache-scope precedence disagreement.
- **SQL compilation plane (db/)** — `filter-deferred-composition`, `filter-dialect-null-ladder`, `filter-groupby-textcast`, `dynamic-filter-exists`, `rollup-correlated-aggregate`, `lookup-nested-join-agg`, `lookup-pk-in-limit`, `sort-dialect-unsortable`: conditionV2 returns deferred {clause, rootApply} pairs (empty-closure skips, never undefined) whose generic switch hides dialect corrections (negation ops re-admit NULL everywhere, MySQL BINARY eq, Oracle ''≡NULL branches incl. `1 = 1`, CWC direction inversion); formula empty-string checks cast through a four-way dialect table while gb_eq/gb_null fold or build literal-vs-aggregated-subquery comparisons; fk_value_col_id dynamic filters swap to knex.ref same-table or build pk-pinned EXISTS cross-table preserving operator direction; genRollupSelectv2 compiles correlated aggregates with sentinel-cell failure contract and re-escapes `\?` after toQuery() materialization; generateLookupSelectQuery walks nested joins (soft-delete every level) and aggregates only when some level was non-BT, emitting per-dialect JSON-array strings; per-lookup top-N is a clone-carried pk-IN subquery (flip-sort-take-head for last N, ROW_NUMBER partition for filters); sortV2 rescues LOB keys orchestrator-side via bounded NVARCHAR / DBMS_LOB.SUBSTR casts keyed on column.dt.
- **Grouped aggregation plane (group-by.ts)** — `groupby-derived-table-fork`, `groupby-key-projection-ladder`, `groupby-blank-null-buckets`, `groupby-datetime-minute-bucketing`, `groupby-subgroup-count-placements`, `groupby-mincount-sqlite-bind-trap`, `groupby-mssql-pagination-orderby`, `groupby-nulls-sort-contract`, `groupby-user-display-sort`, `groupby-rls-entry-funnel`, `groupby-aggregate-dispatch`: one grouped list/count implementation forks into two shapes because mssql/oracle can't GROUP BY select aliases or subquery-valued keys (ORA-00904/ORA-22818) — their keys project into derived tables and ALL aggregation moves outward (inner COUNT ⇒ ORA-00937); every key type compiles per dialect under the display alias (QR/Barcode swap to value column keeping alias, JSON silently dropped off-pg, datetime minute-truncated in lockstep between list and count); '' vs NULL buckets unify through sqlNullIfBlank's four-way table (oracle arm a deliberate no-op); minCount HAVING binds MUST be Number()-coerced or SQLite storage-class comparison returns zero groups; sub-group counts place COUNT(DISTINCT COALESCE(...,'__null__')) where aggregates are legal and re-sanitize composed strings after every toQuery() hop; T-SQL needs a trailing `(SELECT NULL)` ORDER BY whenever OFFSET/FETCH rides a sortless query; grouped sorts reference g.<alias> on the outer CTE with pg-canonical NULLS placement (MSSQL CASE-bucket compensation), count-desc as a two-key first-class direction, and User-family keys translated to display names inside SQL (replaceDelimitedWithKeyValue map vs folded REPLACE chains); RLS enters ONLY at the BaseModelSqlv2 wrappers prepended into filterArr, while groupByAndAggregate dispatches client aggregate fn names through a qb-method reflection gate on a flat raw-column query.
- **Formula compiler plane (db/formulav2/)** — `formula-tree-alias-registry`, `mssql-formula-literal-binding`, `formula-dryrun-transient-doctrine`, `formula-output-guardrails`, `formula-binary-operator-coercion`, `formula-call-expression-assembly`: formulaQueryBuilderv2 seeds a lazy aliasToColumn registry of per-column builder factories (null-seeded self-entries break circular refs) threaded with one shared monotonic alias counter; mssql literals inline as N'…'/1|0 because single-final-toQuery composition shifts bound placeholders; validation dry-runs against the real table split outcomes through isTransientError (transient latch → FORMULA_DRY_RUN_SKIPPED_MESSAGE sentinel = control flow, never a persisted column.error); post-compilation gates refuse >500k-char SQL and SUBSTR-cap STRING outputs outermost-only with inlined lengths (a second `?` binding would steal inner subquery slots); parsed-tree builders rewrite operators/functions into other nodes before SQL exists then apply per-dialect coercions on materialized strings (NULL-safe ordering rescues, CASE-materialized comparisons, mssql FLOAT arithmetic casts, `\?` re-escape at every exit).

- **Formula dispatch kernel plane (fn() internals, pass 17)** — `formula-unary-fold-precedence`, `formula-aggregate-thunk-dispatch`, `formula-string-cast-user-substitution`: the recursive fn() dispatcher folds unaries into single bound constants under the SDK validator's `-numeric-literal`-only gate (prefix-splice fallback threads prevBinaryOp for precedence), resolves Identifiers through the function-builder thunk protocol (multi-row lookup terminals return `(fnName) => aggregate(subquery)`; getAggregateFn maps AVG→.sum(), FIRST→limit(1)-over-display-order, databricks gets trailing-paren LIMIT-1 surgery), and forces string context by re-dispatching STRING-cast nodes as synthetic STRING() arguments with cast:null clones while User-family identifiers lazily substitute memoized email REPLACE chains (pg/sqlite delimited primitive vs nested-REPLACE ladder).

- **Formula lookup/LTAR plane (lookup-or-ltar-builder.ts, 878L whole-file)** — `formula-lookup-ltar-relation-fork`, `formula-nested-lookup-chain-walker`, `formula-terminal-aggregate-dispatch`, `formula-lookup-limit-placement`, `formula-mm-link-order-stash`: a Lookup/LTAR column inside a formula compiles into ONE correlated expression through a BT/HM/MM fork whose return shape itself forks (plain QueryBuilder for single-row relations vs `(fn) => Raw` aggregate closure for multi-row); nested lookups flatten via a while-loop JOIN spine with per-level link filters and soft-delete pinned to each level's own alias (the mm-lookup filter-fix regression); the terminal uidt decides select-vs-aggregate with the Formula arm recursing through `_formulaQueryBuilder` under CircularRefContext cycle guards; PG-only sort/limit configs apply outer-pk-IN before the loop and correlated top-N per nested level; MM junction drag-order stashes `_ncLinkOrderRef` on the builder as an aggregate-stage hint unless an explicit config wins.
- **Field-handler dispatch plane (db/field-handler/, 5,702L whole-tree)** — `handler-dispatch-core`, `generic-filter-op-table`, `dynamic-field-ref-like-patterns`, `negated-op-exists-mapping`, `lookup-filter-subquery-forks`, `ltar-filter-count-family`, `datetime-relative-op-machine`, `datetime-write-format-twins`, `user-display-name-substitution`, `json-blank-dialect-forks`, `formula-filter-sort-routing`, `links-v2-duality-dispatch`, `attachment-blank-write-ledger`, `checkbox-rating-null-folds`, `longtext-ai-projection-case-rules`, `select-sort-uuid-casts`, `engine-generic-overrides`, `handler-interface-computed-mixin`: HANDLER_REGISTRY routes every (uidt × dbClient) to type/dialect handler classes returning deferred {clause, rootApply} pairs — reads SKIP orphaned filters while verification FAILS them so RLS policies can't silently vanish; the generic op table encodes a three-gate ''-vs-NULL matrix (type-compatibility × Oracle ''≡NULL × native pg enum) where every negation re-admits NULL and Oracle notempty collapses to `1 = 1`; Lookup containment builds per-relation subqueries whose negated ops flip to positive-inside/NOT-IN-outside (or rows matching both polarities slip through) while allof means set-membership across related rows split into AND-of-anyofs; LTAR count ops become correlated COUNT(...)=0 subqueries with soft-delete on the counted alias; DateTime relative ops anchor windows in UTC (eq = BETWEEN +24h−1ms, minute buckets under groupby) and route NULL-family ops AROUND date parsing or they match all rows; User columns pre-filter the roster by wildcard semantics in JS then fold REPLACE() display-name chains into SQL (PG/SQLite swap in replace_delimited_with_keyvalue); JSON blank spans NULL∪{}∪[] with pg dual-cast equality; Links columns pivot on junction shape between display-value (BT-like) and count (rollup) semantics; attachment writes give old-row metadata authority over client patches; MySQL CONCAT-wraps select sorts out of DDL-position ordering; UuidPgHandler casts everything ::text; PG nlike carries a recorded operator-flip regression comment restoring empty-string re-admission.
- **PG IEEE formula plane (drift pass 15, develop@640fe3b0)** — `pg-ieee-case-ladders`, `formula-blank-nan-semantics`, `pg-math-function-guards`, `non-finite-aggregate-exclusion`, `nan-sort-rank-agreement`, `non-finite-wire-contract`, `sdk-operator-admission`: pg formulas now compile Airtable IEEE semantics (x/0→±Infinity, 0/0→NaN) through one pure SQL-text composer library typed `'double precision'` because pg<14 numeric cannot hold Infinity; every helper minimizes operand mentions (the nesting complexity metric) and proves domain membership before falling into pg's own operator; binary operands COALESCE blanks to 0 in every mode and strip NaN only inside ordering comparisons (eq/neq must still match the displayed token); MAX strips-then-COALESCE-falls-back so all-NaN stays NaN while LEAST needs nothing (NaN never wins a minimum), and formerly-string-mapped functions need lowercase twin entries because alias rewrites PERSIST into stored trees; numeric aggregates {sum/avg/min/max±Distinct} drop non-finite rows at each consumer site (count family keeps them — an Infinity cell is not empty) while cell sort, filter ordering ops, and BOTH group-by sort sites emit a boolean `(x <> 'NaN')` rank key so group lists and row contents agree; float8 specials serialize as string tokens ('Infinity'/'-Infinity'/'NaN') keyed by getAs() because JSON.stringify would otherwise erase them into indistinguishable nulls, swagger admits number-or-string via oneOf, and the undocumented `%` operator had to enter the SDK ArithmeticOperators tuple or persisted old columns break upstream of the builders.
- **Drift hardening plane (pass 15 misc)** — `lookup-filtered-nested-conditions`: generateLookupSelectQuery captures each level's related baseModel per relation shape and applies THAT level's link conditions onto its own sub-query (outer-only application silently drops inner filters, #10150). `link-v2-conversion-guards`: convertLinkToV2 refuses system columns, junction-table columns, and half-converted pairs (matched by swapped MM FKs or equal direct-FK ids) so one-sided V2 upgrades fail loudly instead of corrupting Rollup/Lookup metadata. `display-value-admission-ladder`: Airtable import picks a usable display value (primary → first eligible → mint Title_N) inside warn-not-throw per-table guards because a cosmetic throw aborts the whole import. `locale-minus-normalization`: paste serializer normalizes U+2212 before stripping and resolves sign AFTER by leading-minus position (jest-pinned). `view-section-copy-inheritance`: duplicates inherit the source section only when the caller left the key strictly undefined (explicit null = top level wins). `credit-hold-scoped-keys`: reservation uniqueness moves to composite scope keys when caller-convention safety becomes cross-tenant hazard.

- **SQL-client introspection plane (db/sql-client/, 13,960L whole-tree, pass 16)** — `sqlclient-factory-dispatch`, `knex-placeholder-escape-contract`, `default-value-function-allowlist`, `updown-statement-duality`, `pg-alter-column-ladder`, `pg-enum-introspection-roundtrip`, `pg-typecast-composers`, `version-keyed-query-dispatch`, `mysql-unique-index-ladder`, `sqlite-table-update-machine`, `pg-lifecycle-connection-ladder`, `connection-manager-reuse`: the factory dispatches wire-protocol-first then meta.dbtype (tidb/vitess/yugabyte twins) over ONE shared knex handle handed in by NcConnectionMgrv2 (composite base_id:id LRU key, Redis staleness, pool-level BIT/hex/NEWDECIMAL typeCast); KnexClient owns the cross-dialect contracts — placeholder sanitize/unsanitize with re-escape-on-every-materialization, the deny-by-default zero-arg function allowlist for column DEFAULTs, and execute-plus-return up/down frames built from separate builder invocations; per-dialect ladders differ where engines differ: pg orders RENAME→TYPE-USING-cast→NULL→DEFAULT→sequence-OWNED-BY→unique (with serial's hidden NOT NULL compensated on AI removal and enum identity bound into internal_meta via findColumnsUsingType), mysql treats unique constraints as INDEXES only and regenerates downs from SHOW CREATE TABLE, sqlite rebuilds columns through rename-copy-drop inside a PRAGMA-choreographed quote-aware transaction; pg type-cast composers are total functions (NULL on unparseable, sentinel bounding, never a failed migration).

- **Request pipeline & ACL plane (middlewares/guards/strategies/utils, pass 18)** — `extract-ids-entity-funnel`, `extract-ids-view-binding-split`, `legacy-extract-table-pin`, `acl-evaluation-ladder`, `acl-inheritance-boot-compile`, `share-access-source-ladder`, `frame-guard-embeddable-ladder`, `global-request-stamping`, `body-parser-matrix`, `rate-limiter-guard-trio`: one param-precedence funnel (~25 id params, mcpToken>integration>table>view>…) turns any route into an authenticated context with eager typed 404s BEFORE handlers; personal/locked views attach under Symbol.for('nc:view') while share uuids deliberately set ONLY shareViewType (merging the bindings changes anonymous ACL class); the v1 funnel pins ncTableId for table-visibility ACL behind instanceof-View guards; aclFn runs eleven ordered gates with personal-view restrictions BEFORE role checks and the no-access self-leave escape LAST; rolePermissions compiles at module load — includes inherit UP each scope's role array, excludes DOWN the reversed array, five boot-time validators throw, org VIEWER collapses to CREATOR; isSharedViewAccess reads POSITIVELY off access_source because shared bases also set is_public but keep full access; setFrameGuardHeaders omits BOTH headers on embeddable share paths and emits XFO+CSP everywhere else; tab id UUID-validates before storage; type-scoped body parsers must never stack on one route; three dependency-free guard shells partition data/meta/public rate-limit surfaces.

- **Auth strategies & token planes (strategies/, pass 18)** — `auth-token-base-scoping`, `oauth-bearer-route-confinement`, `shared-base-pseudo-user`, `google-strategy-verification`: xc-token auth rejects base-scoped tokens hitting a DIFFERENT base outright ('Invalid token', no oracle) while account-wide tokens skip the check and legacy userless tokens get EDITOR; OAuth bearers face a route allowlist (/mcp, /api/v3/, /auth/user/me) BEFORE granted-resource matching that FAILS CLOSED on missing context (undefined ≠ base_id is true — CWE-863) plus a client-existence guard (CWE-613); shared-base requests authenticate as pseudo-users from the SHARE's roles after two ordered guards (friendly private-base message only when a share row exists, opaque mismatch otherwise); Google sign-in rejects unverified emails via a collect-all-flags checker understanding both passport-profile and ID-token shapes, failing closed ONLY when a flag is present-and-false.

- **Decorator & command-registry kernel (decorators/command-registry/, pass 18)** — `pollable-decorator-swap`: @Pollable swaps the method for an enqueue-and-return-{id} wrapper whose __original stash lets the worker call the real body without re-enqueueing, args fail fast at the call site (functions throw, objects JSON-roundtrip); `use-worker-env-gates`: @UseWorker evaluates BOTH topology gates at DECORATION time (no PubSubRedis or not NC_WORKER_CONTAINER==='false' ⇒ plain method) and subscribes to worker:job:<id> BEFORE enqueueing so completion can't outrun the listener; `nc-upgrader-ratchet`: app migrations run under ONE meta transaction over a zero-padded version list, re-writing NC_CONFIG_MAIN after each step so progress logs inside the rollback boundary; `undo-scope-resolution`: undo stacks partition by ScopeType stamped at forward-record time, ancestor scopes prune stranded undone rows, and SIDEBAR_FIELDS enumeration routes renames to the base stack vs content edits to entity stacks; `capture-bag-side-effect-ledger`: 21 typed capture slots record side effects AT CREATION POINTS so inverses replay id-stably (convertedText/convertedLink reuse original ids, baseSectionChildren carries order, upsert inserts ride rotated trashIds); `operation-catalog-contracts`: 169-name OperationName enum lives in CE, name@version keys both registry lookup and changelog rows, versions coexist until old rows drain; `ce-stub-parity-trace`: TraceCommand/Untraced/NcCache/OperationRegistry ship as semantic no-op stubs keeping full types so EE overrides swap without touching call sites.

- **Shell serving plane (gui middleware, pass 18)** — `gui-shell-serving`: tri-state disable (split-frontend URL / missing dist env / missing index.html) resolved in the constructor once, then Accept-header branching between express.static fall-through and best-effort branded shell — frame-guard headers set BEFORE any branch because this middleware terminates everything it serves.

- **Query-client kernel (dbQueryClient)** — `query-client-dialect-factory`, `client-type-identity-admission-split`, `query-client-sql-micro-abstractions`, `concat-cast-required-pair`, `batch-pk-update-case-funnel`, `batch-update-admission-gate`, `pagination-orderby-hook`: a static factory maps enum/knex-string identities to per-dialect clients (get total-by-undefined vs fromKnex throwing; mysql2 alias widening; shared MYSQL_LEGACY fallthrough); identity (clientType getter) is separate from admission (validateClientType); VALUES-injection flattens values-before-identifiers in one positional bind array; table aliasing is a method because Oracle forbids AS (ORA-00907); concat/simpleCast are the required abstract pair with TEXT→CHAR keyword maps; batchUpdate packs per-column CASE WHEN over deduped pks gated at the CALLER by single-pk+dialect inside a transaction (Oracle excluded — CASE types from first THEN, ORA-00932); pagination ordering is an idempotent last-resort hook no-oping off T-SQL.
- **Aggregation strategy plane (aggregations/)** — `aggregation-strategy-registry`, `registry-stub-registration-doctrine`, `query-client-ee-stub-doctrine`, `single-query-hash-grouping`, `exec-parse-bulkaggregate-contract`, `category-default-unsupported-degradation`, `coalesce-exemption-wrap`, `aggregation-context-contract`, `client-handler-delegation-split`, `aggregation-prelude-gates`, `virtual-column-identity-substance-split`, `non-finite-binding-style-triptych`: a registry resolves per-dialect handler classes through buildContext → category-dispatch → postProcess → wrap pipelines (subclasses stopped overriding generateAggregateQuery; handlers instantiate stateless per call); mssql/oracle ride REGISTERED throwing stubs whose canonical EE_ONLY message self-classifies failures while constants stay in CE paths; category defaults return undefined so platform gaps degrade per-column while user-invalid combos throw 501; wrap COALESCEs everything except earliest/latest date and applies alias after; params/context interfaces make handlers total functions of their inputs; applyAggregation orders silent-skip → SDK-validate-throw → barcode/QR identity-substance swap → virtual expression resolve → pg IEEE double-bind → dispatch; excludeNonFiniteSql has THREE binding styles keyed by how the expression arrives.
- **Dialect SQL recipes (pg/mysql/sqlite handlers)** — `pg-empty-cell-sentinel-ladder`, `dialect-sentinel-list-diff`, `filled-vs-sentinel-list-asymmetry`, `json-empty-cell-probe-trio`, `json-typed-list-membership-rule`, `filter-vs-case-count-parity-matrix`, `percent-denominator-zero-safe-shape`, `rating-zero-as-empty-numerics`, `sentinel-binding-vs-interpolation-split`, `date-range-calendar-month-parity`, `date-range-cast-first-vs-diff-first`, `median-window-vs-limit-offset`, `stddev-population-fallback-composition`, `filtered-derived-table-materialization`, `attachment-size-json-array-sum`: empty-cell sentinels tier ''/NULL/0 per column class with pg-only native-enum escape and 16-vs-14 entry list diffs driven by storage typing; two distinct membership lists gate ''-comparisons (breaks vs pointless); JSON emptiness probes are engine-specific with empty-arrays-stay-FILLED invariant; FILTER↔SUM(CASE) and NULLIF↔IFNULL translate mechanically; percentages multiply by 100.0 before dividing by guarded COUNT(*); Rating filters zeros per-function (Avg/Min/Stddev yes, Sum/Max no) matching the SDK JS reducer; MonthRange is calendar-month parity everywhere (never AGE()); DateRange splits cast-first (pg calendar days) vs diff-first (elapsed floor); median averages middle-two via ROW_NUMBER window or LIMIT/OFFSET arithmetic (`median-window-vs-limit-offset`, `median-stddev-per-dialect-recipes`); population stddev hand-rolls on sqlite with double rating-filter injection; FILTER-less engines re-materialize caller filters as nc_val derived tables feeding scalar-subquery aggregates including attachment-size array sums.
- **Delimited rewrite & cross-cutting** — `delimited-token-order-preserving-rewrite`, `bucket-vs-global-filter-stratification`: User/CreatedBy id-lists rewrite to display names positionally (ordinality/json_each keys pinned into aggregate ORDER BY) while bulk buckets keep global-vs-local where layers additive.
- **Orchestration & entry (cross-db-utils + services)** — `aggregate-column-resolution-precedence`, `aggregate-filter-stack-order`, `xwhere-listargs-parity`, `rls-first-opaque-group`, `soft-delete-post-filter-append`, `selector-fanout-vs-bucket-serialization`, `orchestration-error-asymmetry`, `bulk-aggregate-bucket-funnel`, `bulk-row-selector-json-packers`, `aggregate-id-title-remap`, `aggregate-service-entry-gates`: column resolution layers explicit pairs over view config behind show/system gates; the WHERE stack fixes group order (RLS opaque-group first, view-root, request filters, xwhere, bucket-local, validated JSON) with soft-delete appended conjunctively outside the tree; xwhere parses through the same machinery as list endpoints against the same snapshot; single mode fans generation under Promise.all while bulk serializes bucket builders; single rethrows, bulk swallows to {} with input validation hoisted OUTSIDE the catch; per-bucket expressions bind baseQuery=tQb so medians honor each set's filters; row selectors pack per-set JSON (UNQUOTE+limit(1) load-bearing on mysql); results key SQL-side by col.id and remap to titles with passthrough; services gate grid-only before parsing (public twin notFounds to hide existence).
- **Infra & security kernel plane (helpers/utils/redis/gateways/mcp, pass 21)** — `connect-time-db-ssrf-validation`, `ssrf-policy-gate`, `ssl-filepath-timing-oracle`, `telemetry-only-socket-gateway`, `socket-redis-adapter-mock-twin`, `ce-noop-realtime-shell`, `fire-and-forget-event-bus`, `refcounted-pubsub-demux`, `lru-map-eviction-hooks`, `throttler-crosspod-rollup`, `signup-claim-race-guard`, `slot-mapped-dependency-table`, `per-request-mcp-server`, `mcp-attachment-allowlist`, `redis-url-tier-ladder`, `limit-offset-admission`, `pagination-envelope-duality`, `plugin-manager-lifecycle`, `boot-permission-reflection`, `datasource-encryption-backfill`, `env-admin-reconciliation`, `public-query-key-blocklist`, `comment-body-sanitize-allowlist`, `stage-timer-null-profiler`, `boot-order-version-ratchet`: SSRF validation rides the driver's socket setup (TOCTOU-closed) behind one source-keyed policy gate with response-padding for cert-file oracles; websockets are safe-by-surface-minimality with a mock-twin Redis adapter and CE no-op shells; the event bus catches its own discarded emit promise while pubsub demuxes refcounted; LRU eviction hooks fire exactly once with remove-before-await teardown; cross-pod throttle logging elects skew-tolerant leaders without locks; signup claims bridge the bcrypt gap; slot mappings give heterogeneous events indexed columns without DDL; MCP servers are per-request throwaways whose authz is structural registration; pagination admits silently but paginates honestly; boot reconciles env admins transactionally, backfills encryption idempotently, reflects pg privileges into degradation flags, and ratchets versions before migrations.
- **Meta-projection & cache-invalidation plane (helpers/, pass 22)** — `populate-meta-introspection-spine`, `system-column-remap-gate`, `ag-inference-ladder`, `mm-junction-heuristic`, `views-prefix-copy-bug`, `virtual-title-dedup-ladder`, `execute-operations-queue`, `single-query-cache-invalidator`, `cross-base-cache-invalidation`, `rename-gate-invalidation-dispatch`, `ltar-pair-insert-contract`, `rollup-lookup-validation-ladder`, `constraint-name-generators`, `column-sanitize-alias-resolution`, `system-column-roster`, `links-count-auto-column`, `formula-reference-scan`, `ajv-validation-funnel`, `command-palette-base-list`, `get-cache-middleware`, `lookup-travel-revtype`, `required-field-presence-split`: external-source introspection runs four ordered waves whose virtual-column wave MUST follow every real column (stop-on-first-error PQueue(5) rethrowing errors[0]); NocoDB-created tables are recognized by an all-six marker-column conjunction before system remap; mssql AG inference needs pk+!cdf+!ai because IDENTITY hides column_default; junction tables pass a 2-bt-FK/composite-PK/<5-columns shape gate with id-based idempotent MM probes and demote-else; single-query cache invalidation is a reverse transitive closure over embedding columns (seed links targeting the model, expand lookups/rollups to fixpoint) gated on PHYSICAL renames only, swept per-referring-BASE context cross-base best-effort; LTAR creation inserts two rows under each side's own base context with hardcoded NO ACTION rules and self-ref system forcing; rollup/lookup validation ladders end in suggestive error text with mm-forced shape dispatch and refContext-first circularity walks; constraint names budget 10+10+10 chars against 64-char dialect caps; column sanitization is opt-out international-letter-preserving with per-operation-source system-flag stripping; the write-side system roster must keep name parity with the read-side recognition map; Links fields auto-mint `<Related> Count` rollups hiding the relation's grid entry; formula dependency scans match Identifier id-or-title ignoring callee names; one shared Ajv instance serves both 400-json middleware and throwing validatePayload with xc-token→Bearer token precedence; base rosters cache under piggyback list registration so palette cleanup covers them free.
- **AST & response-projection plane (helpers/getAst*, pass 22)** — `ast-depth-cycle-guards`, `ast-strategy-chain-ordering`, `ast-rowcolor-button-deps`: getAst caps client-nested LTAR expansion at depth 8 returning an EMPTY subtree on breach while extractDependencies stops Lookup cycles with a visited Set (the guard against Knex's production self-containing-SELECT infinite loop) and normalizes lookup dependency buckets before fieldsSet mutation; twelve named column-inclusion strategies evaluate first-match-wins so ARRAY ORDER IS the legacy if/else ladder (each match encodes only its own rung, terminal catch-all stays last, allowRequestedHiddenFields bypasses the view-show gate opt-in-only never public); row-color collectors split SELECT-mode view.meta reads from FILTER-mode condition walks and button filters scope to view-visible buttons BEFORE reading their FILTER_EXP rows, feeding one dedicated early rung — the sanctioned route for view cosmetics to widen data payloads.
- **Error-translation & validation kernel plane (helpers/db-error + uniqueConstraintErrorHandler + ncError* + sqlSanitize + webhookHelpers + isDisposableEmail, pass 23)** — `db-error-extractor-dispatch`, `pg-extractor-sqlstate-table`, `mysql-sqlite-extractor-twins`, `transient-error-taxonomy`, `unique-constraint-funnel`, `nc-error-version-taxonomy`, `webhook-condition-evaluator`, `sql-sanitize-ddl-guards`, `disposable-email-gate`: raw driver errors become typed user-safe extractions through a client-fingerprinting dispatcher whose per-dialect tables parse detail/hint into templates while deliberately returning undefined for XX000 internals; isTransientError's four-layer taxonomy (with its deliberate EREQUEST hole) gates which failures self-heal vs poison stored columns; the 1,044-line unique-constraint funnel detects violations at every nesting level then resolves column→label before always throwing; NcError's versioned singleton pair moves the entire v1/v3 behavioral diff into one codex table; the JS-side webhook filter evaluator mirrors SQL semantics with day-granularity date algebra; and three DDL interpolation guards (?-escape codec, pgQuoteLiteral, dtxp allowlist) plus the two-tier disposable-email matcher close the input-validation boundary.
- **LTAR text↔link conversion plane (helpers/ltarColumnConversion, pass 23)** — `ltar-text-link-conversion`: the five-step ordering contract (paged snapshot → temp-titled create → hard-delete → rename-before-backfill → append-only backfill), fresh-reuse-per-subop meta-cache armor, replay-aware row-cap bypass, and circular-safe broadcasts for source+related tables.
- **Meta-dependency cascade plane (services/meta-dependency, pass 23)** — `meta-dependency-cascade`: boot-registered handler map over ten meta events; forced-suppression next-context as THE recursion guard; lazy shared transaction opened only when a handler claims affected dependencies, committed once or rolled back atomically.
- **User-eventing & token plane (pass W1, work-record epoch 1)** — `notifications-longpoll-registry`, `notification-triple-delivery`, `meta-json-column-codec`: plain-HTTP multi-instance push via held responses + `notification:<userId>` pubsub channel; production rides the AppEvents bus while mail stays inline; every meta-table JSON column round-trips the swallow-and-fallback codec. `audit-fire-forget-insert`: observability never taxes the hot path (unawaited + kill-switch) and its reader pins the id-cursor keyset/+1-pop page contract. `oauth-code-validate-then-claim`, `oauth-refresh-revokeifactive-cas`: both single-use guarantees are affected-row-count CAS updates — the code claim sits AFTER all preconditions (failed exchange must not consume), the rotation revoke sits AFTER minting (failed mint must not burn the chain; GHSA-353r); access JWTs carry mandatory `is_oauth_token:true` because they share the first-party secret (GHSA-xmfr-pc8j-4xh5). `mail-send-gates-audit-ledger`: config-state gates refuse sends safely and nc_mail_sends records attempts truthfully without ever breaking the boolean sendMail contract.

## Extending the foundation
Add one `./<seam>.md` capsule per new porting question (e.g. export.service serialize internals per entity type, import.service csv-stream parsing details, or migration-jobs individual migrations). Add one matching loader line and one grouped map entry; keep evidence in the capsule, not this leaf.

## Provenance
PASS W1 (factory FAC-79, dedicated lane miner-nocodb; work-record epoch 1 — /mnt/hdd/utopia/inspo/nocodb-work/{state,research,verification}.md CREATED this pass; ledger row repaired from stale "pass 0/refs 0"): user-eventing & token plane — whole-file reads of notifications.{controller,service}.ts + models/Notification.ts + utils/modelUtils.ts + models/Audit.ts + modules/oauth/{oauth-token.service,oauth-token.claims,oauth-token.queries}.ts + OAuth{Token,AuthorizationCode} CAS ranges + services/mail/mail.service.ts (+ context reads app-hooks-listener.service.ts, workspace-users.service.ts) → **+7 capsule-v2 (362→369)** in ONE new map group. Evidence: graph `nocodb` ready FULL 198591n/345602e head==base==pin 640fe3b0, origin/develop rev-list=0 BOTH directions (zero drift); probe anchors derived from live grep BEFORE writing then byte-exact executed post-write; check_index_coverage no_recorded_issue ×5 cited files. GRAPH-PROJECT ADJUDICATION: passes 15–22 ordered citing the path-slugged TWIN (`mnt-hdd-utopia-inspo-platforms-nocodb`, root platforms/nocodb) while the short name served stale content THEN; this lane's contract pins project `nocodb` @FULL 198591 nodes and live index_status satisfies exactly that with line-exact symbol resolution on every seam this pass — Retrieve blocks cite `nocodb`; twin untouched (other checkout path, out of lane scope). NEXT-PASS TARGETS recorded in nocodb-work/state.md: workspace-invite batch semantics (file fully read), mail template/branding plane, OAuthClient lifecycle, telemetry listener plane, mention-event family producers.
NocoDB (Sustainable Use License, develop branch) `develop@640fe3b06fb26c9d000e2258477001c0d5e62c73` (pass-21 infra/security-kernel plane 2026-08-24, dedicated nocodb cron lane drain-lane-nocodb e031734db924; pass-20 sibling-audit verification; pass-19 aggregate-dispatch plane; pass-18 fresh-eyes re-entry; pass-15 drift re-entry; passes 1–14 pinned `f7513664f3f3`); Codebase Memory project **`mnt-hdd-utopia-inspo-platforms-nocodb`** — the path-slugged TWIN, permanently adopted after the 2026-08-24 in-place re-index landed there while the short-name `nocodb` project kept serving stale pre-drift content (ieeeDivisionSql resolves on the twin only); CITE THE TWIN in every Retrieve block (full mode, ready @640fe3b0 head==base==pin, origin/develop rev-list=0 ZERO DRIFT this pass; skipped = 9 SQL insert dumps + pnpm-lock; assets excluded by design). PASS-21 (2026-08-24): executed the fresh-eyes re-entry on planes never entered by passes 1–20 — gateways/socket (socket.gateway.ts 96L, RedisIoAdapter.ts 32L), socket stubs (NocoSocket/NocoPresence), event-emitter module, redis/pubsub-redis.ts 128L, utils LRUMap/NcConcurrent (NcConcurrent = ZERO consumers, dead code, omitted-with-reason), mcp/mcp.service.ts 767L whole + mcp.controller.ts, integrations/integration.store.ts, helpers cluster {dbSsrfLookup 264L, validateDbConnectionHost 99L, utils/ssrf.ts 65L, withMinResponseTime, stageTimer, throttlerLogger, signupClaim, sanitizeCommentBody, publicQuerySanitizer, PagedResponse, extractLimitAndOffset, getUniqueName (trivial loops, below capsule bar — omit), initAdminFromEnv 295L, initBaseBehaviour 89L, initDataSourceEncryption 117L, NcPluginMgrv2 338L, metaCacheInvalidator (thin wrapper over mined View.clearSingleQueryCache — omit), patResourceFilter/replayScope/sandboxGuards (CE stubs covered by ce-stub doctrine — omit), DependencySlotMapper 310L} + providers/init-meta-service.provider.ts 180L → **+25 capsule-v2 (301→326)** in ONE new map group (Infra & security kernel plane). Evidence: gate-1 index_status twin ready @640fe3b0 generation_matches=true; probe battery scratch-ncpass21-battery.sh FIRST RUN 53/58 with 5 RED invented counts re-derived against live grep PRE-COMMIT (keepRedisChannel 4→5 incl type×2; unref 2→1 comment lacks token; rolesLevel 5→3 decl+2 operands; PERF_LOG_ENABLED 3→2 doc uses {@link}; NocoSocket consumers ≥8→34 files) → FINAL SWEEP 58/58 GREEN byte-exact from repo root; search_graph line-exact ×10 symbol groups on the TWIN (PubSubRedis.ensureMessageListener :34–55, McpService.handleRequest/registerTools :37–60/:62–756, ThrottlerLogger.flush :35–81, LRUMap.asyncDelete :74–81, validatingLookup/mysql2StreamFactory/pgStreamFactory/applyDbSsrfProtection :109–132/:194–225/:146–177/:249–264, SocketGateway.constructor :41–45, RedisIoAdapter :11–25/:27–31, withSignupClaim :26–58, sanitizePublicQuery :12–21, withMinResponseTime/hasSslFilePath); adversarial retrieval teable 'PubSubRedis ensureMessageListener' total:0 + short-name-nocodb ssrf query total:0 (wrong-plane adjudication: short-name IS stale for these symbols); check_index_coverage stdin-JSON ×14 cited paths ALL no_recorded_issue + metadata_match generation_matches=true; parity disk=loader=map SET-EQUAL bidirectional 326=326 (map 329 stems incl 3 pre-existing prose citations); v2 marker per-file head-check clean ×25. OMITTED-WITH-REASON pass 21: NcConcurrent (dead code, zero consumers), createCoalescer (dead code, zero consumers), getUniqueName (trivial while-loops), metaCacheInvalidator/patResourceFilter/replayScope/sandboxGuards (CE no-op/thin wrappers already represented by ce-stub-parity-trace doctrine), schema/swagger*.json (generated artifacts), run/* docker entry shells, public/js swagger-ui-bundle (vendored), nc-gui frontend. PASS-19 (2026-08-24, platforms-rover cron lane drain-lane-platforms-rover successor fire): executed pass-18 target #2 — the named aggregate-dispatch porting question — reading ALL 20 dbQueryClient/** files whole top-to-bottom (~2.9kL: index/generic/types, pg/mysql/sqlite/mssql/oracle clients, aggregations/{index,interface,generic,pg,mysql,sqlite,mssql}, cross-db-utils/{aggregate,bulk-aggregate,applyAggregation,single-query-cache}) plus consumers (data-table/public-datas services, UiPost.operations, BaseModelSqlv2 batchUpdate site :4499–4516, group-by tableAlias/replaceDelimited sites, genRollupSelectv2 exclusion site) → 47 capsule-v2 (253→300) in FOUR new map groups (query-client kernel, aggregation strategy plane, dialect SQL recipes, orchestration & entry). Evidence: search_graph resolved getAggregationHandler (:29–39), resolveAggregateColumns (:167–218), GenericAggregationHandler.generate/wrap (:28–59/:101–122), MysqlAggregationHandler.buildContext/numerical (:17–74/:276–360), SqliteAggregationHandler family (:76–455), PgAggregationHandler.buildContext (:17–57), DBQueryClient.fromKnex (:47–68), batchUpdate (:93–164), temporaryTableRaw (:37–66), ensurePaginationOrderBy (:243–245), applyAggregation (:38–136), aggregate (:25–154), MssqlAggregationHandler.generate (:11–13), replaceDelimitedWithKeyValue (generic stub :197–204 + pg :40–86), bulkAggregateRowSelector trio, singleQueryCacheKey (:25–30) line-exact on the twin; stdin-JSON check_index_coverage no_recorded_issue+metadata_match ×19 cited paths generation_matches=true; zero upstream specs touch the plane (109 spec files grepped) — no-test caveat recorded per capsule. GATE5: probe battery /tmp/nccron344_probe_battery_v6.sh executed byte-exact from repo root = 136/136 GREEN after the FIRST draft ran 99/137 — 38 expectation failures caught by execution and each re-derived against source PRE-COMMIT (dominant defect class: grep -c counts LINES not occurrences — e.g. pg FILTER-clause count 13→24, mysql SUM(CASE 10→12, IFNULL/NULLIF 6→9 each, 100.0 6→9; several sed windows off-by-a-line; TWO semantic repairs: public-data aggregate gates via NcError.notFound('Not found') NOT badRequest, AGE() appears once in a comment so =0 was wrong; EE_ONLY counts mssql=4/oracle=5 lines). PASS-18 (2026-08-24, dedicated drain-lane-nocodb): zero drift at origin/develop (rev-list HEAD..origin/develop = 0) → citation-vs-inventory census over packages/nocodb/src + nocodb-sdk/src (1,417 prod files vs 128 cited) exposed the never-entered request-pipeline plane — middlewares/, guards/, strategies/, decorators/, command-registry/, version-upgrader/NcUpgrader.ts, helpers/{accessSource,frameGuard}, utils/acl.ts read whole-file top-to-bottom → 21 capsule-v2 in five NEW map groups. Evidence: search_graph resolved markPersonalViewIfNeeded/checkIsPersonalViewOwner/aclFn (:1073–:1305)/legacyExtractIds (:476–:1053)/dynamicScope/getScopeAncestors/setFrameGuardHeaders/resolveShareAccessSource/Pollable/UseWorker/NcUpgrader.upgrade/OperationContract/CaptureBag line-exact on the twin; stdin-JSON check_index_coverage no_recorded_issue+metadata_match ×16 cited paths generation_matches=true; gate-5 probe battery /tmp/nclane331_probe_battery_v2.sh executed byte-exact from repo root = 44/44 GREEN after SEVEN first-draft expectation failures were re-derived against source and repaired PRE-COMMIT (oauth pins 3→4 incl. allowlist decl+use, frame-ref 2→3, cestub descriptor sites, sharebase UnauthorizedException import counted, gui next() single site, gui env pair comment+read ×2, google-strategy capsule REWRITTEN — no tokeninfo call exists, the gate is isGoogleEmailUnverified profile-flag checking with present-and-false fail-closed semantics); strategy/jwt/local/authtoken spec files verified construction-only stubs (coverage caveat in-capsule); hiddenFieldMatrix access-source invariant suite referenced by comments NOT present at pin (recorded gap). PASS-17 (dedicated drain-lane-nocodb): executed the LAST standing conditional — formulaQueryBuilderv2.ts fn() dispatch region :376–491 whole → formula-unary-fold-precedence/formula-aggregate-thunk-dispatch/formula-string-cast-user-substitution; SDK validator guard validate-extract-tree.ts :1115–1129 makes the LiteralNode cast validation-guaranteed; commit swept into sibling locoagent lane's 6931389f (erratum recorded, content verified byte-exact at that sha). PASS-15 (2026-08-24, frontier-platforms lane): fetch-first diff sweep found real upstream drift for the first time since f7513664 — five planes entered from the diff hunks whole: pg-ieee.ts (NEW 117L composer library), parsed-tree-builder.ts blank/NaN semantics + `/``%` dispatch, functionMappings/pg.ts guarded math functions with persisted-name twins, three non-finite aggregate-exclusion consumers, NaN sort-rank agreement across cell+group-by, non-finite wire contract (execAndParse hook + SDK tokens + swagger oneOf), `%` operator admission; plus drift-hardening set: generateLookupSelectQuery per-level link conditions (#10150), convertLinkToV2 half-converted-pair guards, Airtable display-value admission ladder, view-section copy inheritance, credit-hold composite-key migration, U+2212 paste normalization. Direct tests: NEW upstream jest specs Decimal.spec.ts (:60/:67/:71) + serializer.spec.ts (:61/:206/:256) pin the sign plane; IEEE-SQL plane has no upstream suite (runner-blocked caveat recorded per capsule, never fabricated). Evidence: search_graph resolved ieeeDivisionSql/coalesceNumericOperand/isFiniteSql/stripNaNSql/excludeNonFiniteSql/convertFormulaNonFinite/mapNonFiniteToString/serializeDecimalValue/applyIeeeNanRank/findPairedLinkColumn line-exact on the twin; stdin-JSON check_index_coverage returned no_recorded_issue ×14 cited paths; probe battery executed live against source (grep/sed pins in every capsule). PASS-16 (2026-08-24, dedicated drain-lane-nocodb): zero drift at origin/develop (HEAD still 640fe3b06f) → executed the never-entered db/sql-client tree (13,960L whole-tree read top-to-bottom: SqlClient.ts 118L interface stubs, SqlClientFactory 44L, KnexClient 1,961L whole-file incl. allowedDefaultFunctions/sanitize/genQuery/up-down frames/relationDelete double-execution trap, PgClient 3,890L whole-file, MysqlClient 2,834L whole-file, SqliteClient 2,302L whole-file, Tidb/Vitess/Yugabyte vendor twins, typeCast/constants/data.helper/order helpers, plus consumer NcConnectionMgrv2.ts) → 12 capsule-v2 in a NEW "SQL-client introspection plane" map group. Evidence: search_graph resolved alterTableColumn line-exact in all three dialect clients (3157–3432/2668–2748/2150–2255) + NcConnectionMgrv2 symbols line-exact; stdin-JSON check_index_coverage no_recorded_issue+metadata_match ×14 cited paths (generation complete, generation_matches=true); probe battery = deterministic sed/grep pins byte-verified pre-authoring; zero upstream unit specs import any sql-client symbol (109 spec files grepped) — runner-blocked caveat recorded per capsule, never fabricated. Prior-pass evidence trail: parse_partial noise is SQL sakila dumps / scss / helm charts / pnpm-lock, plus one real file `readAndProcessData.ts` whose flagged ranges were read directly from source; skipped = 9 SQL insert dumps; 1,085 assets excluded by design). No in-repo unit tests exist for these seams — every capsule carries an explicit no-test caveat and a source-grounded probe. Pass-13 (formula plane, commits 2e3a52d + rescue 291ce15) wired parsed-tree-builder capsules authored by an interrupted sibling. Pass-14 additions (this pass, formula lookup/LTAR plane + field-handler dispatch plane) read whole-file at the SAME unchanged pin: formulav2/lookup-or-ltar-builder.ts (878L) and the entire db/field-handler tree (5,702L: index.ts registry/orchestrator 719L, generic.ts op table 1,069L, handlerUtils.ts 449L, field-handler.interface.ts 243L, lookup/ltar/links/formula/rollup/computed handlers, date-time family + pg/mysql/sqlite/mssql/oracle twins, user family incl. created-modified-by mixin, json family, long-text family, attachment pair, checkbox/decimal/number/rating/duration/year/currency/colour/percent chains, multi-select/single-select, time quartet, uuid.pg); every newly-cited path verified via stdin-JSON check_index_coverage (`no_recorded_issue` + `metadata_match`, generation_matches=true) and search_graph live line-exact resolution; HEAD unchanged at pin f7513664f3f3 (zero drift). Earlier passes: pass-5 verified via search_graph live resolution + stdin-JSON check_index_coverage over all seven newly-cited files. Pass-8 additions (record data plane, meta-sync engine, public/shared surface, LTAR model) re-verified the same way: search_graph resolved every newly-cited symbol line-exact and stdin-JSON check_index_coverage returned `no_recorded_issue` for all seven newly-cited paths (data-table.service.ts, meta-diffs.service.ts, public-datas.service.ts, pk-preservation.ts, LinkToAnotherRecordColumn.ts, HookFilter.ts, ModelRoleVisibility.ts); HEAD unchanged at the pin. Pass-9 additions (data alias entry + read engine, nested-link routes, ByViewId surface, public metas, filter twin diff) whole-file read at the unchanged pin: datas.service.ts (1,386L), public-metas.service.ts (396L), nestedLinkQueryHelpers.ts (389L), relatedMetaProjection.ts (213L), PagedResponse.ts, dataHelpers.ts, plus Filter.ts/HookFilter.ts twin regions — both service spec files verified construction-only stubs (coverage caveats in-capsule). Pass-10 additions (filter write/lifecycle + RLS plane, alias boundary correction) whole-file read at the SAME unchanged pin: Filter.ts (1,589L) and HookFilter.ts (514L) full twins, data-alias-nested.service.ts (426L), bulk-data-alias.service.ts (217L), plus call sites Hook.ts / filters.service.ts / hooks.service.ts and cache plumbing NocoCache.getList + CacheMgr.getList + FilterCacheScope enum — search_graph resolved every newly-cited symbol line-exact and stdin-JSON check_index_coverage returned `no_recorded_issue` for all twelve cited paths; both alias specs re-verified construction-only (`should be defined`). Pass-11 additions (db/ SQL-compilation plane) whole-file read at the SAME unchanged pin: conditionV2.ts (1,078L), sortV2.ts (106L), genRollupSelectv2.ts (565L), generateLookupSelectQuery.ts (920L), lookupSortLimit.ts (414L) — search_graph resolved every newly-cited symbol line-exact (parseConditionV2 145-886, resolveDynamicFilterValue 901-954, genRollupSelectv2 32-565 incl. applyFunction 149-370 + wrapMssqlNestedAgg 375-389, generateLookupSelectQuery 128-920, getDisplayValueOfRefTable 39-63, getRefTableColumnForFilter 80-121, sortV2 9-106, applyLookupPkInLimit 146-211, buildNestedLookupLevelLimit 248-320) and stdin-JSON check_index_coverage returned `no_recorded_issue` + `metadata_match` for all five cited files; caller fan-out verified by grep (conditionV2 consumed from BaseModelSqlv2 + delete/group-by/relation-data-fetcher/dbQueryClient aggregate paths). Pass-13 additions (formula compiler plane) whole-file read at the SAME unchanged pin (HEAD still f7513664f3f3, zero src drift): formula-query-builder.helpers.ts (185L) + the four previously-unwired committed capsules wired into leaf/map, plus two NEW capsules from parsed-tree-builder.ts (725L whole-file): binaryExpressionBuilder 333-725 + callExpressionBuilder 35-331 — search_graph resolved every cited symbol line-exact (wrapFormulaWithMaxLength 83-128, getFormulaOutputMaxLength 70-76, _formulaQueryBuilder 56-494, isTransientError helpers/db-error/utils.ts 40-210, binaryExpressionBuilder, callExpressionBuilder), FORMULA_DRY_RUN_SKIPPED_MESSAGE/formulaDryRunFailed verified by source grep (:53/:614-615/:653-659/:707 sole write site, second consumer select-object.ts), stdin-JSON check_index_coverage `no_recorded_issue`+`metadata_match` ×3 cited files.
PASS-22 (2026-08-24, platforms-rover lane cron 3bceab6b2e9a): executed the unconditional census target — citation-vs-inventory over src/helpers (25 of 110 files cited) → whole-file reads of populateMeta.ts 665L + singleQueryCacheInvalidator.ts 531L (the two named targets) plus columnHelpers 898L / apiHelpers / baseListAllHelpers / formulaHelpers / cacheHelpers / utils-NcHelp + Model.ts:1055 & Column.ts:1711/:1970 call sites → **+22 capsule-v2 (326→348)** in ONE new map group (Meta-projection & cache-invalidation plane). Evidence: probe battery 26/26 byte-exact GREEN from repo root after 2 pre-commit repairs (constraint pattern shell-mangled → re-derived `_\${randomID()}`=2; allowNonSystem invented 14 → true 8, lines-vs-occurrences); parity disk=loader=map set-equal incl. 5 mid-run sibling refs; gates integrity OK / quality rc0 zero nocodb findings / hygiene foreign-only. Recorded latent DEFECT: populateMeta views-wave prefix filter copies TABLES into `views` (:587–591) — bug-compatibility decision point preserved as views-prefix-copy-bug capsule. OMITTED-WITH-REASON pass 22: helpers/{coalescer+createCoalescer (dead), deepClone/extractProps/getAst (already cited), aliasing one-liners (getUniqueName class, below bar), db-error/* (mined pass 13)}; remaining product-surface services per pass-8/9 lists; nc-gui boundary.
PASS-22b (2026-08-24, dedicated drain-lane-nocodb e031734db924 successor fire, same-repo concurrent-lane pass): ran the mandated census independently over src/helpers+src/services; whole-file reads of getAst.ts 593L + getAstColumnStrategy.ts 301L (plus populateMeta/singleQueryCacheInvalidator) → authored 5 capsule-v2, then adjudicated against the platforms-rover lane's SAME-PASS meta-projection group landing mid-run (ref mtimes 17:29–17:34 vs mine 17:42+) → SELF-DEDUPED my two broad twins (`meta-projection-populate-meta`, `single-query-cache-rename-closure` deleted pre-commit, never committed; delta content preserved HERE: NcHelp.executeOperations is DIALECT-KEYED batching (sqlite/mssql transaction-wrapped) so bare Promise.all changes tx grouping; mm dedup runs isMMRelationExist comparing fk_mm_model_id + crossed child/parent ids (:51-78) before inserting either side and markAsSystemField's the matched HAS_MANY twin :215-236) and kept **+3 novel capsule-v2** in NEW map group "AST & response-projection plane": `ast-depth-cycle-guards`, `ast-strategy-chain-ordering`, `ast-rowcolor-button-deps` — the rover census had ruled getAst "already cited" but NO capsule covered the depth cap, visited-set cycle guard, strategy-chain ordering semantics, or row-color/button collectors, so all three are purely additive (326 base + rover 22 + these 3 = 351 when both slices land). Evidence: gate-1 twin ready 197,005n/345,370e @640fe3b0 origin/develop rev-list=0 ZERO drift; probe anchors derived from live grep BEFORE writing then executed (GET_AST_MAX_DEPTH decl+check :34/:94, visited-cycle warn :470, never-defaults-bucket comment :518, strategy names grep -c = 12, must-stay-last :267, views-prefix quirk :588, !column.cdf count 2, EE gates :65/:131/:200/:392, rename gates Model.ts:1054/Column.ts:1710); search_graph live-resolved line-exact ×6 query groups on the TWIN (populateMeta family ranks 1-4, expandEmbeddingColumns/clearSingleQueryCacheForReferencingModels :468-500/:60-109, extractDependencies rank-1 :452-492, clearCrossBaseReferringModels total:1 :386-460, resolveColumnAst total:1 :288-301, getButtonFilterFields total:1 :409-450); coverage caveat: zero unit specs import any of these helpers (109 specs grepped; jest bin absent — runner-blocked stands), probes are source-greps honestly labeled. Concurrent-lane protocol held: no adoption, no deletion of sibling files, own-slice-only commit; reconciliation queue = verify combined parity after BOTH slices commit.
PASS-23 (2026-08-24, dedicated drain-lane-nocodb e031734db924 next fire): executed target #1 reconciliation — git-blob archaeology found pass-22b's SKILL.md provenance hunk swallowed by autostash `5fc00387` ("On foundations-sync: autostash", 18:16 +0800) which ALSO carried sibling grist (+3 loader lines — landed via later lane) and duckdb (title rework — still stash-only, NOT mine to restore; erratum recorded); restored MY hunk verbatim from `git show stash@{0}:…` (read-only archaeology, no stash pop per absolute boundary). Target #2 census over src/helpers+src/services (328 TS files vs refs): ~200 product-surface services remain uncited per standing pass-8/9 omit lists, but exposed NEVER-ENTERED kernel planes → whole-file reads of db-error/* family (extractor 131L dispatch + pg 393L SQLSTATE table + mysql 195L + sqlite 146L message-shapes + utils.ts isTransientError taxonomy 210L), uniqueConstraintErrorHandler.ts 1,044L whole, ltarColumnConversion.ts 1,038L whole, ncError trio (ncError 399L statics + NcErrorV1 143L + ncErrorV3 105L codex table), webhookHelpers.ts 653L validateCondition evaluator, sqlSanitize.ts 90L DDL guards, isDisposableEmail.ts 4,031L two-tier matcher, meta-dependency/event-handler.service.ts 100L + provider → **+11 capsule-v2 (351→362)** in THREE new map groups (error-translation & validation kernel plane ×9 incl. LTAR conversion + meta-dependency cascade as separate groups). Evidence: probe anchors derived from live grep BEFORE writing then byte-exact executed from repo root post-write 11/11 GREEN after THREE live repairs (webhook date-format expectation was lines-vs-occurrences — both formats share one line, switched to grep -o|wc -l; meta-dependency trxNcMeta window off-by-one AND count 1→2 (:80 probe + :83 tx-open); sqlSanitize case-sensitivity widened match set — re-derived true count 4 with -ciE and pinned honestly); search_graph live-resolved rank-1 line-exact ×8 query groups on TWIN (detectClientType :57-99/extractDbError :101-130 + consumer catchError.extractDBError :26-41; handleUniqueConstraintError :205-1044 + SDK UniqueConstraintViolationError :66-76; ltar factory ranks 1-3 incl ColumnsService delegator :6405-6426; MetaDependencyEventHandler.handleEvent :60-99 + concrete handlers; NcErrorV1.recordNotFound :66-111 + NcErrorV3.constructor :23-83; webhook invokeWebhook/validateCondition :453-472/:66-396 with nc-gui FormFilters twin adjudicated wrong-plane; sqlSanitize trio :25-30/:51-70/:72-90 total:3; isDisposableEmail :4019-4031 total:1); adversarial teable error-plane query total:0 with positive controls green same-run; check_index_coverage stdin-JSON ×13 cited paths ALL no_recorded_issue + metadata_match generation_matches=true. OMITTED-WITH-REASON pass 23: services/* product surface (~200 files, standing pass-8/9 ruling — controllers/services are thin route layers over mined kernels), api-docs/swagger generators (schema artifacts), populateSamplePayload.ts 530L (template payload builder below kernel bar, candidates for a named question), meta-dependency handler/* bodies (16 handlers follow the mined dispatcher contract individually), remaining helpers one-liners (linkHelpers/metaColumnHelpers/responseHelpers <20L glue). NEXT-PASS TARGETS (pass 24+, ONLY past 640fe3b0 drift or named question): (1) diff-first `git log --oneline 640fe3b0..origin/develop` + citation grep FIRST vs 362 refs; rewrite views-prefix-copy-bug defect→diff record if upstream patches it; (2) verify duckdb/grist autostash erratum reached their owning lanes; (3) conditional: populateSamplePayload + webhook-invoker WebhookInvoker class under a hooks-fidelity porting question; (4) watch upstream specs landing for db-error/unique-constraint planes (drop grep-pinned caveats when suites exist); (5) else squeezed-to-last-drop cycle at 362/362 v2 @ 640fe3b0.

## Full view (memory graph)
PASS-22 addendum: helpers/{populateMeta, singleQueryCacheInvalidator, columnHelpers, apiHelpers, baseListAllHelpers, formulaHelpers, cacheHelpers} + utils/NcHelp + models/{Model,Column} call sites fully indexed on the twin; `populateMeta`, `expandEmbeddingColumns`, `clearSingleQueryCacheForReferencingModels`, `extractAndGenerateManyToManyRelations` resolve rank-1 line-exact; short-name `nocodb` project now agrees with the twin at pin 640fe3b0 (both serve populateMeta :244–665) but the twin remains canonical per standing adjudication.
Revalidate before porting: run `index_status --project mnt-hdd-utopia-inspo-platforms-nocodb --verbose`, `check_index_coverage` (stdin JSON), `search_graph`, `trace_path`, and `get_code_snippet`. Root `/mnt/hdd/utopia/inspo/platforms/nocodb`, branch `develop@640fe3b06fb2`, full mode, ready @640fe3b0 (head==base==pin; origin/develop rev-list=0 at pass-21). The short-name project `nocodb` remains STALE for drifted symbols (ieeeDivisionSql and the pass-21 infra plane resolve on the TWIN only) — cite the twin. Source and direct tests decide shipped claims. PASS-21 addendum: helpers/{dbSsrfLookup,signupClaim,throttlerLogger,initDataSourceEncryption,...}, redis/pubsub-redis.ts, gateways/*, socket stubs, mcp/mcp.service.ts, providers/init-meta-service.provider.ts fully indexed (coverage ×14 clean); BM25 resolves every new kernel symbol line-exact. PASS-19 addendum: dbQueryClient/** fully indexed (coverage ×19 clean); BM25 resolves every kernel symbol line-exact.

## Boundaries
Adopt the Bull-compatible fallback queue, the three-gate worker admission with exponential requeue, the circular-safe enqueue contract, the status/log event fan-out, the `_mid` long-poll protocol, the versioned single-lock migration runner, the AsyncLocalStorage cache-bypass scope, the cache envelope/self-heal/gate matrix, the backpressured streaming import pipeline with deferred LTAR links, the concurrent pump-upload export with charset fallback, memory-capped thumbnails, and orphan-GC HAVING scans; adapt queue names, concurrency, backoff constants, meta table, cache scope, type maps, and storage adapters to host. Omit the nc-gui frontend, SQL sakila fixtures, EE-only worker-group routing, and the Airtable private-API wire specifics unless migrating from Airtable itself. NocoDB pass-4 note: pass-3's remaining seams are now mined — per-entity/dashboard schema serialization internals, the JSON/ICS/Excel format variants, exportBase fan-out, and migration jobs 012/013 plus the EE/CE version skew. NocoDB pass-5 note (this pass): all three pass-4 targets executed — serializeUsers/BaseUser roster internals (3 capsules), nc_job_015 search-path grandfathering with its config-merge/connection-reset/ncMeta.now machinery (8 capsules), plus a fresh-eyes sweep that found the never-entered jobs.controller write-half, JobsMap dispatch table, and every processor wrapper layer (19 more). nc_job_016 confirmed an 11-line CE stub (no second resume ledger exists; the ledger pattern stays unique to 012). Remaining unmined mass is product surface: serializeScripts (flat field-copy list) and stub serializers (Documents/Workflows/Interfaces return []), nc_job_001-011/014 attachment/thumbnail/order/link-recovery variants of already-mined patterns, and controllers/services outside the job surface. Pass-6 targets only if a porting question emerges: `jobs-log.service.ts` (38 L) + `use-worker.service.ts` details beyond the mined RPC contract, `at-import` processor shell beyond the pipeline internals, or diff `develop` against `f751366` for new seams. NocoDB pass-8 note (this pass): the never-attempted full `src/services/**` + `src/models/**` sweep executed — data-table/meta-diffs/public-datas services, pk-preservation helper, and LinkToAnotherRecordColumn/HookFilter/ModelRoleVisibility models entered whole-file for the first time (20 capsule-v2). Still unmined at this pin (product surface / thin CRUD): datas.service.ts + data-alias-nested/bulk-data-alias (aliases over the same baseModel calls), public-metas/shared-bases/org-users/workspace-users/plugins/sql-views/calendars/galleries/grids/kanbans/forms/forms-columns/view-columns/sorts/filters/views/columns/documents/web-bookmark/smart-text/link-placeholder/dependency/telemetry/integrations/base-integrations/extensions/command-palette/data-import/calendar-datas/duplicate-detection/formula-column-type-changer/column-data-backup-handler services, remaining ~45 models (mostly thin cache-wrapped CRUD like SelectOption — omit-with-reason), bulk-data-op.service.ts (empty comment-only stub), noco-jobs.service.ts (pure pass-through to IJobsService). Re-enter ONLY past f7513664 with a diff-driven sweep. NocoDB pass-9 note (this pass): the two highest-value named unmined services entered whole-file at the unchanged pin — datas.service.ts (the aliased v1/v2 data entry: alias-entry funnel, shared getDataList engine, pg-opt grouped dual path, six nested-link routes sharing one before-both-consumers sanitizer, totalRows-vs-count envelope asymmetry, ByViewId misnomer contract) and public-metas.service.ts (trashed-model dual-case guard, relatedMetas fixpoint projection agreeing with pkAndPvOnly, prototype-preserving secret strip), plus the Filter-vs-HookFilter getFilterObject twin diff (three legitimate drifts + one latent precedence trap) and their never-cited helpers (nestedLinkQueryHelpers, relatedMetaProjection, PagedResponse envelope). Both spec files are construction-only stubs — no behavioral tests exist for these seams. Still unmined at this pin (product surface / thin CRUD per pass-8 list): remaining ~40 product-surface services (shared-bases/org-users/workspace-users/plugins/sql-views/view-type CRUDs/documents/command-palette/etc.), ~45 thin cache-CRUD models. NocoDB pass-10 note (this pass): executed pass-9's named conditional targets at the unchanged pin — (1) Filter-vs-HookFilter full-file twin diff beyond getFilterObject surfaced the write/lifecycle plane (multi-scope cache fan-out, transaction-attached update, guarded recursive delete, single-query invalidation) plus a NEW live find: rootFilterListByHook reads the VIEW-scoped list but writes HOOK-scoped (miss-always degradation absorbed by four callers); (2) RLS tree-vs-list fail-open doctrine mined (linkFilterRowsIntoTree + rootFilterTreeByRlsPolicy + BaseModelSqlv2.getRlsConditions clone-per-read seam); (3) CORRECTED the record: data-alias-nested/bulk-data-alias are NOT pure delegation — alias boundary correction capsule supersedes the earlier omit-with-reason. NEXT-PASS: re-enter ONLY past f7513664 (`git log --oneline f7513664..HEAD -- packages/nocodb/src` FIRST); FIRST grep whether upstream patched rootFilterListByHook's read key; remaining unmined stays product-surface services + thin models per pass-8/9 lists. NocoDB pass-15 note (this pass): the queued diff-driven sweep EXECUTED against real drift f7513664→640fe3b0 (dark-mode/UI mass skipped by design as frontend boundary) — all backend hunks mined into 13 capsules across the PG-IEEE formula plane and drift-hardening plane; rootFilterListByHook read-key question NOT touched by upstream (grep of diff shows no Filter.ts/HookFilter.ts change — standing conditional remains); graph project migrated to the path-slugged twin `mnt-hdd-utopia-inspo-platforms-nocodb` (short-name `nocodb` stale, never retry refresh-in-place). NEXT-PASS TARGETS (pass 16+, ONLY past `640fe3b0` drift or on a fresh question): 1. `git log --oneline 640fe3b0..origin/develop -- packages/nocodb packages/nocodb-sdk` FIRST — diff-driven seams only; also watch whether upstream ships unit specs for pg-ieee/formulav2 (drop runner-blocked caveats when they land). 2. Conditional: nc-gui dark-theme/uno.config migration ONLY if a CSS-build porting question emerges (frontend boundary otherwise). 3. Conditional: workflow selectOptions chips plumbing (sdk interface.ts + nc-gui form-builder) ONLY on a workflow-condition-builder question. 4. Standing conditionals from earlier passes remain (formulaQueryBuilderv2 fn() dispatch region; ~~db/sql-client introspection surface~~ EXECUTED pass 16 — 12 capsules; re-enter only on fresh drift or a dialect-porting question). NocoDB pass-16 note (this pass): the sql-client tree is now mined-to-its-edges — every file in db/sql-client/ read whole (SqlClient/SqlClientFactory/KnexClient/PgClient/MysqlClient/SqliteClient/Tidb/Vitess/Yugabyte/typeCast/constants/data.helper/order) plus consumer NcConnectionMgrv2; recorded quirks preserved as capsules include the base relationDelete double-execution trap, MysqlClient.schemaCreate's triggerList func-key aliasing (works by accident — fix, never copy), sqlite's down-migration `';'` stubs, and faker/seed methods (KnexClient.fakerColumns* + MysqlClient.seedInit/mapFieldWithSuggestedFakerFn levelling) left as legacy-surface omit-with-reason (no callers outside the dead xc-tooling surface). Remaining unmined at this pin: formulaQueryBuilderv2 fn() dispatch region :376–491 (standing conditional), product-surface services/thin models per pass-8/9 lists, nc-gui frontend. PASS-17 (2026-08-24, dedicated lane drain-lane-nocodb, zero upstream drift): the standing fn()-dispatch conditional EXECUTED — three capsule-v2 (231 total) covering the dispatcher kernel: UnaryExpression fold under the SDK `-numeric-literal`-only validator gate (validate-extract-tree.ts:1115-1129 makes the LiteralNode cast safe), Identifier aggregate-thunk resolution with getAggregateFn's AVG→sum/FIRST→limit(1) surprises + databricks LIMIT-1 paren surgery, and STRING-cast re-dispatch with User-family memoized email substitution; formulaQueryBuilderv2.ts now mined line-range complete (:56-724 across 8 capsules). Remaining unmined at this pin: product-surface services/thin models per pass-8/9 lists, nc-gui frontend. PASS-18 (2026-08-24, dedicated lane drain-lane-nocodb, zero upstream drift): fresh-eyes citation-vs-inventory census (1,417 prod files vs 128 cited) exposed and MINED the never-entered request-pipeline plane — extract-ids funnel pair, AclMiddleware ladder + acl.ts boot-compile, share/access-source + frame-guard helpers, global/gui/body middlewares, three guard shells, auth-token/oauth-token/base-view/google strategies, Pollable/UseWorker/NcCache/TraceCommand decorators, command-registry kernel (OperationName 169 names, OperationContract/CaptureBag types, scope.ts builders), NcUpgrader — 21 capsule-v2 (252 total) in five new map groups. OMITTED-WITH-REASON pass 18: version-upgrader/upgraders/*.ts bodies 0100002–0258003 (one-shot legacy data migrations; the RUNNER contract is mined — individual upgraders are variant instances of already-mined migration patterns), basic.strategy.ts + local.strategy.ts + jwt.strategy.ts (thin passport wrappers over User.verify/me plumbing already covered by user-roster capsules), strategies/google.strategy.ts tail beyond the verification gate (find-or-provision user = standard ladder), middlewares/extract-ids.helpers.ts permission-list BODIES beyond the three exported sets (pure string arrays, counts pinned in-capsule), dbQueryClient/* per-dialect client classes (aggregation entry shells over the ALREADY-mined BaseModelSqlv2/groupby/formulav2 planes — enter only on a dedicated aggregation-dispatch question), meta/api + controllers product surface per pass-8/9 lists, nc-gui frontend (standing boundary). PASS-19 note: dbQueryClient/* mined whole — OMITTED-WITH-REASON within the plane: none (every file entered; EE-only bodies absent from CE by design and documented via stub capsules). NEXT-PASS TARGETS (pass 20+, ONLY past `640fe3b0` drift or a NEW named question): 1. diff-first `git log --oneline 640fe3b0..origin/develop -- packages/nocodb packages/nocodb-sdk`. 2. Individual ncUpgrader bodies ONLY on a legacy-data-migration porting question. 3. Watch for upstream unit specs covering middleware/strategy/dbQueryClient planes — drop construction-only caveats when they land. 4. Conditional: EE single-query client surface ONLY if an EE-build porting question emerges (CE side shows only throw-stubs + cache-key helpers). 5. Else squeezed-to-last-drop for cycle at this pin relative to mined scope. PASS-21 NEXT-PASS TARGETS (pass 22+, ONLY past `640fe3b0` drift or a NEW named question; citation-vs-inventory grep FIRST vs all 326 refs): 1. diff-first git log --oneline 640fe3b0..origin/develop — zero drift this pass, so any new commits are fresh seams; re-run citation-vs-inventory over src/helpers + src/services (the pass-21 census found the infra plane by exactly this method). 2. helpers/populateMeta.ts 665L + singleQueryCacheInvalidator.ts 531L whole-file IF a meta-projection/single-query-invalidation porting question emerges (adjacent-mined via meta-diffs capsules today). 3. helpers/apiHelpers.ts/dataHelpers.ts/exportImportHelpers.ts/formulaHelpers.ts cluster on a named v3-API-surface question. 4. Watch for upstream unit specs touching gateways/mcp/helpers planes (109 spec files currently construction-or-absent) — drop grep-pinned caveats when real suites land. 5. Conditional: nc-gui frontend plane remains a standing boundary unless the user names a GUI porting question. 6. Else squeezed-to-last-drop for cycle at 326/326 v2 @ 640fe3b0 relative to mined scope.

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`acl-evaluation-ladder.md`](./acl-evaluation-ladder.md)
- [`acl-inheritance-boot-compile.md`](./acl-inheritance-boot-compile.md)
- [`ag-inference-ladder.md`](./ag-inference-ladder.md)
- [`aggregate-column-resolution-precedence.md`](./aggregate-column-resolution-precedence.md)
- [`aggregate-filter-stack-order.md`](./aggregate-filter-stack-order.md)
- [`aggregate-id-title-remap.md`](./aggregate-id-title-remap.md)
- [`aggregate-service-entry-gates.md`](./aggregate-service-entry-gates.md)
- [`aggregation-context-contract.md`](./aggregation-context-contract.md)
- [`aggregation-prelude-gates.md`](./aggregation-prelude-gates.md)
- [`aggregation-strategy-registry.md`](./aggregation-strategy-registry.md)
- [`airtable-assoc-dedup.md`](./airtable-assoc-dedup.md)
- [`airtable-backpressure.md`](./airtable-backpressure.md)
- [`airtable-fetch.md`](./airtable-fetch.md)
- [`airtable-id-remap.md`](./airtable-id-remap.md)
- [`airtable-type-map.md`](./airtable-type-map.md)
- [`ajv-validation-funnel.md`](./ajv-validation-funnel.md)
- [`alias-services-boundary-correction.md`](./alias-services-boundary-correction.md)
- [`api-token-sso-scoping.md`](./api-token-sso-scoping.md)
- [`ast-depth-cycle-guards.md`](./ast-depth-cycle-guards.md)
- [`ast-rowcolor-button-deps.md`](./ast-rowcolor-button-deps.md)
- [`ast-strategy-chain-ordering.md`](./ast-strategy-chain-ordering.md)
- [`at-engine-dual-mode.md`](./at-engine-dual-mode.md)
- [`at-import-failure-policy.md`](./at-import-failure-policy.md)
- [`at-import-pipeline.md`](./at-import-pipeline.md)
- [`attachment-blank-write-ledger.md`](./attachment-blank-write-ledger.md)
- [`attachment-migration-ledgers.md`](./attachment-migration-ledgers.md)
- [`attachment-orphan-gc.md`](./attachment-orphan-gc.md)
- [`attachment-size-json-array-sum.md`](./attachment-size-json-array-sum.md)
- [`attachment-url-upload.md`](./attachment-url-upload.md)
- [`audit-fire-forget-insert.md`](./audit-fire-forget-insert.md)
- [`audit-id-migration.md`](./audit-id-migration.md)
- [`auth-token-base-scoping.md`](./auth-token-base-scoping.md)
- [`baseuser-roster-query.md`](./baseuser-roster-query.md)
- [`baseuser-service-actors.md`](./baseuser-service-actors.md)
- [`batch-pk-update-case-funnel.md`](./batch-pk-update-case-funnel.md)
- [`batch-update-admission-gate.md`](./batch-update-admission-gate.md)
- [`body-parser-matrix.md`](./body-parser-matrix.md)
- [`boot-order-version-ratchet.md`](./boot-order-version-ratchet.md)
- [`boot-permission-reflection.md`](./boot-permission-reflection.md)
- [`bucket-vs-global-filter-stratification.md`](./bucket-vs-global-filter-stratification.md)
- [`bulk-aggregate-bucket-funnel.md`](./bulk-aggregate-bucket-funnel.md)
- [`bulk-row-selector-json-packers.md`](./bulk-row-selector-json-packers.md)
- [`byviewid-misnomer-contract.md`](./byviewid-misnomer-contract.md)
- [`cache-bypass.md`](./cache-bypass.md)
- [`cache-envelope.md`](./cache-envelope.md)
- [`cache-gate-matrix.md`](./cache-gate-matrix.md)
- [`cache-list-selfheal.md`](./cache-list-selfheal.md)
- [`cache-setlist-projection.md`](./cache-setlist-projection.md)
- [`capture-bag-side-effect-ledger.md`](./capture-bag-side-effect-ledger.md)
- [`category-default-unsupported-degradation.md`](./category-default-unsupported-degradation.md)
- [`ce-noop-realtime-shell.md`](./ce-noop-realtime-shell.md)
- [`ce-stub-parity-trace.md`](./ce-stub-parity-trace.md)
- [`checkbox-rating-null-folds.md`](./checkbox-rating-null-folds.md)
- [`client-handler-delegation-split.md`](./client-handler-delegation-split.md)
- [`client-type-identity-admission-split.md`](./client-type-identity-admission-split.md)
- [`coalesce-exemption-wrap.md`](./coalesce-exemption-wrap.md)
- [`column-sanitize-alias-resolution.md`](./column-sanitize-alias-resolution.md)
- [`command-palette-base-list.md`](./command-palette-base-list.md)
- [`comment-body-sanitize-allowlist.md`](./comment-body-sanitize-allowlist.md)
- [`concat-cast-required-pair.md`](./concat-cast-required-pair.md)
- [`config-merge-override-discipline.md`](./config-merge-override-discipline.md)
- [`connect-time-db-ssrf-validation.md`](./connect-time-db-ssrf-validation.md)
- [`connection-manager-reuse.md`](./connection-manager-reuse.md)
- [`connection-reset-protocol.md`](./connection-reset-protocol.md)
- [`constraint-name-generators.md`](./constraint-name-generators.md)
- [`credit-hold-scoped-keys.md`](./credit-hold-scoped-keys.md)
- [`cross-base-cache-invalidation.md`](./cross-base-cache-invalidation.md)
- [`cross-instance-migration-stream.md`](./cross-instance-migration-stream.md)
- [`csv-detector-stub-contract.md`](./csv-detector-stub-contract.md)
- [`data-table-duplicate-guard.md`](./data-table-duplicate-guard.md)
- [`data-table-link-by-display.md`](./data-table-link-by-display.md)
- [`data-table-link-swap.md`](./data-table-link-swap.md)
- [`data-table-nested-dispatch.md`](./data-table-nested-dispatch.md)
- [`data-table-single-bulk-funnel.md`](./data-table-single-bulk-funnel.md)
- [`datas-alias-entry-funnel.md`](./datas-alias-entry-funnel.md)
- [`datas-getdatalist-engine.md`](./datas-getdatalist-engine.md)
- [`datas-grouped-dual-path.md`](./datas-grouped-dual-path.md)
- [`datasource-encryption-backfill.md`](./datasource-encryption-backfill.md)
- [`date-range-calendar-month-parity.md`](./date-range-calendar-month-parity.md)
- [`date-range-cast-first-vs-diff-first.md`](./date-range-cast-first-vs-diff-first.md)
- [`datetime-relative-op-machine.md`](./datetime-relative-op-machine.md)
- [`datetime-write-format-twins.md`](./datetime-write-format-twins.md)
- [`db-error-extractor-dispatch.md`](./db-error-extractor-dispatch.md)
- [`default-value-function-allowlist.md`](./default-value-function-allowlist.md)
- [`delimited-token-order-preserving-rewrite.md`](./delimited-token-order-preserving-rewrite.md)
- [`dialect-sentinel-list-diff.md`](./dialect-sentinel-list-diff.md)
- [`display-value-admission-ladder.md`](./display-value-admission-ladder.md)
- [`disposable-email-gate.md`](./disposable-email-gate.md)
- [`driver-coercion-funnel.md`](./driver-coercion-funnel.md)
- [`duplicate-backfill.md`](./duplicate-backfill.md)
- [`duplicate-compensation.md`](./duplicate-compensation.md)
- [`duplicate-granularity-dispatch.md`](./duplicate-granularity-dispatch.md)
- [`dynamic-field-ref-like-patterns.md`](./dynamic-field-ref-like-patterns.md)
- [`dynamic-filter-exists.md`](./dynamic-filter-exists.md)
- [`engine-generic-overrides.md`](./engine-generic-overrides.md)
- [`entity-map.md`](./entity-map.md)
- [`env-admin-reconciliation.md`](./env-admin-reconciliation.md)
- [`excel-import-streaming.md`](./excel-import-streaming.md)
- [`exec-parse-bulkaggregate-contract.md`](./exec-parse-bulkaggregate-contract.md)
- [`execute-operations-queue.md`](./execute-operations-queue.md)
- [`export-base-fanout.md`](./export-base-fanout.md)
- [`export-cell-serialization.md`](./export-cell-serialization.md)
- [`export-charset-fallback.md`](./export-charset-fallback.md)
- [`export-cleanup-job-lifecycle.md`](./export-cleanup-job-lifecycle.md)
- [`export-cleanup.md`](./export-cleanup.md)
- [`export-col-options-policy.md`](./export-col-options-policy.md)
- [`export-column-projection.md`](./export-column-projection.md)
- [`export-composite-ids.md`](./export-composite-ids.md)
- [`export-dashboard-widgets.md`](./export-dashboard-widgets.md)
- [`export-data-processor-v2.md`](./export-data-processor-v2.md)
- [`export-ics-calendar.md`](./export-ics-calendar.md)
- [`export-idmap.md`](./export-idmap.md)
- [`export-import-rest-trigger.md`](./export-import-rest-trigger.md)
- [`export-json-excel.md`](./export-json-excel.md)
- [`export-presign.md`](./export-presign.md)
- [`export-recursive-stream.md`](./export-recursive-stream.md)
- [`export-stream-upload.md`](./export-stream-upload.md)
- [`export-users-roster.md`](./export-users-roster.md)
- [`export-view-extras.md`](./export-view-extras.md)
- [`extract-ids-entity-funnel.md`](./extract-ids-entity-funnel.md)
- [`extract-ids-view-binding-split.md`](./extract-ids-view-binding-split.md)
- [`fallback-queue.md`](./fallback-queue.md)
- [`filled-vs-sentinel-list-asymmetry.md`](./filled-vs-sentinel-list-asymmetry.md)
- [`filter-cache-multiscope-fanout.md`](./filter-cache-multiscope-fanout.md)
- [`filter-deferred-composition.md`](./filter-deferred-composition.md)
- [`filter-dialect-null-ladder.md`](./filter-dialect-null-ladder.md)
- [`filter-groupby-textcast.md`](./filter-groupby-textcast.md)
- [`filter-hook-list-scope-mismatch.md`](./filter-hook-list-scope-mismatch.md)
- [`filter-hookfilter-twin-diff.md`](./filter-hookfilter-twin-diff.md)
- [`filter-insert-scope-ladder.md`](./filter-insert-scope-ladder.md)
- [`filter-rls-tree-failopen.md`](./filter-rls-tree-failopen.md)
- [`filter-update-delete-lifecycle.md`](./filter-update-delete-lifecycle.md)
- [`filter-vs-case-count-parity-matrix.md`](./filter-vs-case-count-parity-matrix.md)
- [`filtered-derived-table-materialization.md`](./filtered-derived-table-materialization.md)
- [`fire-and-forget-event-bus.md`](./fire-and-forget-event-bus.md)
- [`formula-aggregate-thunk-dispatch.md`](./formula-aggregate-thunk-dispatch.md)
- [`formula-binary-operator-coercion.md`](./formula-binary-operator-coercion.md)
- [`formula-blank-nan-semantics.md`](./formula-blank-nan-semantics.md)
- [`formula-call-expression-assembly.md`](./formula-call-expression-assembly.md)
- [`formula-dryrun-transient-doctrine.md`](./formula-dryrun-transient-doctrine.md)
- [`formula-filter-sort-routing.md`](./formula-filter-sort-routing.md)
- [`formula-lookup-limit-placement.md`](./formula-lookup-limit-placement.md)
- [`formula-lookup-ltar-relation-fork.md`](./formula-lookup-ltar-relation-fork.md)
- [`formula-mm-link-order-stash.md`](./formula-mm-link-order-stash.md)
- [`formula-nested-lookup-chain-walker.md`](./formula-nested-lookup-chain-walker.md)
- [`formula-output-guardrails.md`](./formula-output-guardrails.md)
- [`formula-reference-scan.md`](./formula-reference-scan.md)
- [`formula-string-cast-user-substitution.md`](./formula-string-cast-user-substitution.md)
- [`formula-terminal-aggregate-dispatch.md`](./formula-terminal-aggregate-dispatch.md)
- [`formula-tree-alias-registry.md`](./formula-tree-alias-registry.md)
- [`formula-unary-fold-precedence.md`](./formula-unary-fold-precedence.md)
- [`frame-guard-embeddable-ladder.md`](./frame-guard-embeddable-ladder.md)
- [`generic-filter-op-table.md`](./generic-filter-op-table.md)
- [`get-cache-middleware.md`](./get-cache-middleware.md)
- [`global-request-stamping.md`](./global-request-stamping.md)
- [`google-strategy-verification.md`](./google-strategy-verification.md)
- [`groupby-aggregate-dispatch.md`](./groupby-aggregate-dispatch.md)
- [`groupby-blank-null-buckets.md`](./groupby-blank-null-buckets.md)
- [`groupby-datetime-minute-bucketing.md`](./groupby-datetime-minute-bucketing.md)
- [`groupby-derived-table-fork.md`](./groupby-derived-table-fork.md)
- [`groupby-key-projection-ladder.md`](./groupby-key-projection-ladder.md)
- [`groupby-mincount-sqlite-bind-trap.md`](./groupby-mincount-sqlite-bind-trap.md)
- [`groupby-mssql-pagination-orderby.md`](./groupby-mssql-pagination-orderby.md)
- [`groupby-nulls-sort-contract.md`](./groupby-nulls-sort-contract.md)
- [`groupby-rls-entry-funnel.md`](./groupby-rls-entry-funnel.md)
- [`groupby-subgroup-count-placements.md`](./groupby-subgroup-count-placements.md)
- [`groupby-user-display-sort.md`](./groupby-user-display-sort.md)
- [`gui-shell-serving.md`](./gui-shell-serving.md)
- [`handler-dispatch-core.md`](./handler-dispatch-core.md)
- [`handler-interface-computed-mixin.md`](./handler-interface-computed-mixin.md)
- [`hook-filter-tree-flatten.md`](./hook-filter-tree-flatten.md)
- [`import-attachment-stream.md`](./import-attachment-stream.md)
- [`import-column-mapping.md`](./import-column-mapping.md)
- [`import-csv-stream.md`](./import-csv-stream.md)
- [`import-dual-listener-fanout.md`](./import-dual-listener-fanout.md)
- [`import-dual-mode.md`](./import-dual-mode.md)
- [`import-error-formatter.md`](./import-error-formatter.md)
- [`import-error-ladder.md`](./import-error-ladder.md)
- [`import-handler-registry.md`](./import-handler-registry.md)
- [`import-link-buckets.md`](./import-link-buckets.md)
- [`import-link-stream.md`](./import-link-stream.md)
- [`import-ltar-links.md`](./import-ltar-links.md)
- [`import-naming.md`](./import-naming.md)
- [`import-progress-frames.md`](./import-progress-frames.md)
- [`import-record-coercion-ladder.md`](./import-record-coercion-ladder.md)
- [`import-result-aggregation.md`](./import-result-aggregation.md)
- [`import-streaming.md`](./import-streaming.md)
- [`import-type-detection.md`](./import-type-detection.md)
- [`import-view-config-family.md`](./import-view-config-family.md)
- [`import-virtual-column-resolution.md`](./import-virtual-column-resolution.md)
- [`instance-command-bus.md`](./instance-command-bus.md)
- [`internal-event-job-enqueue.md`](./internal-event-job-enqueue.md)
- [`job-producer-controllers.md`](./job-producer-controllers.md)
- [`job-timing-helpers.md`](./job-timing-helpers.md)
- [`jobs-dispatch-table.md`](./jobs-dispatch-table.md)
- [`jobs-events.md`](./jobs-events.md)
- [`jobs-listen-shutdown-flush.md`](./jobs-listen-shutdown-flush.md)
- [`jobs-log-fanout.md`](./jobs-log-fanout.md)
- [`jobs-meta-list-window.md`](./jobs-meta-list-window.md)
- [`jobs-module-wiring.md`](./jobs-module-wiring.md)
- [`jobs-polling.md`](./jobs-polling.md)
- [`jobs-processor.md`](./jobs-processor.md)
- [`jobs-relay-write-side.md`](./jobs-relay-write-side.md)
- [`jobs-service.md`](./jobs-service.md)
- [`json-blank-dialect-forks.md`](./json-blank-dialect-forks.md)
- [`json-empty-cell-probe-trio.md`](./json-empty-cell-probe-trio.md)
- [`json-import-streaming.md`](./json-import-streaming.md)
- [`json-typed-list-membership-rule.md`](./json-typed-list-membership-rule.md)
- [`keyset-backfill-walk.md`](./keyset-backfill-walk.md)
- [`knex-placeholder-escape-contract.md`](./knex-placeholder-escape-contract.md)
- [`legacy-extract-table-pin.md`](./legacy-extract-table-pin.md)
- [`limit-offset-admission.md`](./limit-offset-admission.md)
- [`link-recovery-witness.md`](./link-recovery-witness.md)
- [`link-v2-conversion-guards.md`](./link-v2-conversion-guards.md)
- [`links-count-auto-column.md`](./links-count-auto-column.md)
- [`links-v2-duality-dispatch.md`](./links-v2-duality-dispatch.md)
- [`locale-minus-normalization.md`](./locale-minus-normalization.md)
- [`longtext-ai-projection-case-rules.md`](./longtext-ai-projection-case-rules.md)
- [`lookup-filter-subquery-forks.md`](./lookup-filter-subquery-forks.md)
- [`lookup-filtered-nested-conditions.md`](./lookup-filtered-nested-conditions.md)
- [`lookup-nested-join-agg.md`](./lookup-nested-join-agg.md)
- [`lookup-pk-in-limit.md`](./lookup-pk-in-limit.md)
- [`lookup-travel-revtype.md`](./lookup-travel-revtype.md)
- [`lru-map-eviction-hooks.md`](./lru-map-eviction-hooks.md)
- [`ltar-cross-base-context.md`](./ltar-cross-base-context.md)
- [`ltar-filter-count-family.md`](./ltar-filter-count-family.md)
- [`ltar-pair-insert-contract.md`](./ltar-pair-insert-contract.md)
- [`ltar-text-link-conversion.md`](./ltar-text-link-conversion.md)
- [`ltar-update-placeholder.md`](./ltar-update-placeholder.md)
- [`mail-send-gates-audit-ledger.md`](./mail-send-gates-audit-ledger.md)
- [`mcp-attachment-allowlist.md`](./mcp-attachment-allowlist.md)
- [`median-stddev-per-dialect-recipes.md`](./median-stddev-per-dialect-recipes.md)
- [`median-window-vs-limit-offset.md`](./median-window-vs-limit-offset.md)
- [`meta-dependency-cascade.md`](./meta-dependency-cascade.md)
- [`meta-diff-postsync-pk-gate.md`](./meta-diff-postsync-pk-gate.md)
- [`meta-diff-splice-ladder.md`](./meta-diff-splice-ladder.md)
- [`meta-json-column-codec.md`](./meta-json-column-codec.md)
- [`meta-sync-broadcast.md`](./meta-sync-broadcast.md)
- [`meta-sync-deferred-virtual-groups.md`](./meta-sync-deferred-virtual-groups.md)
- [`meta-sync-mm-junction-heuristic.md`](./meta-sync-mm-junction-heuristic.md)
- [`meta-sync-pk-ratchet.md`](./meta-sync-pk-ratchet.md)
- [`metasync-dual-method-bodies.md`](./metasync-dual-method-bodies.md)
- [`migration-bootstrap-ordering.md`](./migration-bootstrap-ordering.md)
- [`migration-ce-stub-parity.md`](./migration-ce-stub-parity.md)
- [`migration-ee-ce-skew.md`](./migration-ee-ce-skew.md)
- [`migration-job-example.md`](./migration-job-example.md)
- [`migration-jobs.md`](./migration-jobs.md)
- [`migration-orphan-links.md`](./migration-orphan-links.md)
- [`migration-queue-backpressure.md`](./migration-queue-backpressure.md)
- [`migration-searchpath-grandfather.md`](./migration-searchpath-grandfather.md)
- [`migration-trash-backfill.md`](./migration-trash-backfill.md)
- [`mm-junction-heuristic.md`](./mm-junction-heuristic.md)
- [`model-role-visibility-cache.md`](./model-role-visibility-cache.md)
- [`mssql-formula-literal-binding.md`](./mssql-formula-literal-binding.md)
- [`mysql-sqlite-extractor-twins.md`](./mysql-sqlite-extractor-twins.md)
- [`mysql-unique-index-ladder.md`](./mysql-unique-index-ladder.md)
- [`nan-sort-rank-agreement.md`](./nan-sort-rank-agreement.md)
- [`nc-error-version-taxonomy.md`](./nc-error-version-taxonomy.md)
- [`nc-upgrader-ratchet.md`](./nc-upgrader-ratchet.md)
- [`ncmeta-now-contract.md`](./ncmeta-now-contract.md)
- [`negated-op-exists-mapping.md`](./negated-op-exists-mapping.md)
- [`nested-link-count-asymmetry.md`](./nested-link-count-asymmetry.md)
- [`nested-link-query-restriction.md`](./nested-link-query-restriction.md)
- [`non-finite-aggregate-exclusion.md`](./non-finite-aggregate-exclusion.md)
- [`non-finite-binding-style-triptych.md`](./non-finite-binding-style-triptych.md)
- [`non-finite-wire-contract.md`](./non-finite-wire-contract.md)
- [`notification-triple-delivery.md`](./notification-triple-delivery.md)
- [`notifications-longpoll-registry.md`](./notifications-longpoll-registry.md)
- [`oauth-bearer-route-confinement.md`](./oauth-bearer-route-confinement.md)
- [`oauth-code-validate-then-claim.md`](./oauth-code-validate-then-claim.md)
- [`oauth-refresh-revokeifactive-cas.md`](./oauth-refresh-revokeifactive-cas.md)
- [`operation-catalog-contracts.md`](./operation-catalog-contracts.md)
- [`orchestration-error-asymmetry.md`](./orchestration-error-asymmetry.md)
- [`order-column-backfill.md`](./order-column-backfill.md)
- [`orphan-view-column-gc.md`](./orphan-view-column-gc.md)
- [`pagination-envelope-duality.md`](./pagination-envelope-duality.md)
- [`pagination-orderby-hook.md`](./pagination-orderby-hook.md)
- [`per-request-mcp-server.md`](./per-request-mcp-server.md)
- [`percent-denominator-zero-safe-shape.md`](./percent-denominator-zero-safe-shape.md)
- [`pg-alter-column-ladder.md`](./pg-alter-column-ladder.md)
- [`pg-empty-cell-sentinel-ladder.md`](./pg-empty-cell-sentinel-ladder.md)
- [`pg-enum-introspection-roundtrip.md`](./pg-enum-introspection-roundtrip.md)
- [`pg-extractor-sqlstate-table.md`](./pg-extractor-sqlstate-table.md)
- [`pg-ieee-case-ladders.md`](./pg-ieee-case-ladders.md)
- [`pg-lifecycle-connection-ladder.md`](./pg-lifecycle-connection-ladder.md)
- [`pg-math-function-guards.md`](./pg-math-function-guards.md)
- [`pg-typecast-composers.md`](./pg-typecast-composers.md)
- [`plugin-manager-lifecycle.md`](./plugin-manager-lifecycle.md)
- [`pollable-decorator-swap.md`](./pollable-decorator-swap.md)
- [`populate-meta-introspection-spine.md`](./populate-meta-introspection-spine.md)
- [`presence-gated-override.md`](./presence-gated-override.md)
- [`public-form-insert-gates.md`](./public-form-insert-gates.md)
- [`public-gantt-links-ltar-duality.md`](./public-gantt-links-ltar-duality.md)
- [`public-nested-link-guards.md`](./public-nested-link-guards.md)
- [`public-query-key-blocklist.md`](./public-query-key-blocklist.md)
- [`query-client-dialect-factory.md`](./query-client-dialect-factory.md)
- [`query-client-ee-stub-doctrine.md`](./query-client-ee-stub-doctrine.md)
- [`query-client-sql-micro-abstractions.md`](./query-client-sql-micro-abstractions.md)
- [`rate-limiter-guard-trio.md`](./rate-limiter-guard-trio.md)
- [`rating-zero-as-empty-numerics.md`](./rating-zero-as-empty-numerics.md)
- [`redis-jobs-service.md`](./redis-jobs-service.md)
- [`redis-url-tier-ladder.md`](./redis-url-tier-ladder.md)
- [`refcounted-pubsub-demux.md`](./refcounted-pubsub-demux.md)
- [`registry-stub-registration-doctrine.md`](./registry-stub-registration-doctrine.md)
- [`related-metas-projection-fixpoint.md`](./related-metas-projection-fixpoint.md)
- [`rename-gate-invalidation-dispatch.md`](./rename-gate-invalidation-dispatch.md)
- [`repeat-job-registration-gap.md`](./repeat-job-registration-gap.md)
- [`required-field-presence-split.md`](./required-field-presence-split.md)
- [`rls-first-opaque-group.md`](./rls-first-opaque-group.md)
- [`rollup-correlated-aggregate.md`](./rollup-correlated-aggregate.md)
- [`rollup-lookup-validation-ladder.md`](./rollup-lookup-validation-ladder.md)
- [`sdk-operator-admission.md`](./sdk-operator-admission.md)
- [`select-sort-uuid-casts.md`](./select-sort-uuid-casts.md)
- [`selector-fanout-vs-bucket-serialization.md`](./selector-fanout-vs-bucket-serialization.md)
- [`sentinel-binding-vs-interpolation-split.md`](./sentinel-binding-vs-interpolation-split.md)
- [`share-access-source-ladder.md`](./share-access-source-ladder.md)
- [`shared-base-pseudo-user.md`](./shared-base-pseudo-user.md)
- [`shared-metas-secret-strip.md`](./shared-metas-secret-strip.md)
- [`shared-metas-trashed-model-guard.md`](./shared-metas-trashed-model-guard.md)
- [`shared-view-query-confinement.md`](./shared-view-query-confinement.md)
- [`signup-claim-race-guard.md`](./signup-claim-race-guard.md)
- [`single-query-cache-invalidator.md`](./single-query-cache-invalidator.md)
- [`single-query-hash-grouping.md`](./single-query-hash-grouping.md)
- [`slot-mapped-dependency-table.md`](./slot-mapped-dependency-table.md)
- [`socket-redis-adapter-mock-twin.md`](./socket-redis-adapter-mock-twin.md)
- [`soft-delete-meta-fastpath.md`](./soft-delete-meta-fastpath.md)
- [`soft-delete-post-filter-append.md`](./soft-delete-post-filter-append.md)
- [`soft-delete-unique-conversion.md`](./soft-delete-unique-conversion.md)
- [`soft-delete-upgrader-mode.md`](./soft-delete-upgrader-mode.md)
- [`sort-dialect-unsortable.md`](./sort-dialect-unsortable.md)
- [`source-create-compensation.md`](./source-create-compensation.md)
- [`source-create-delete-processors.md`](./source-create-delete-processors.md)
- [`source-delete-idempotent.md`](./source-delete-idempotent.md)
- [`source-update-order-preservation.md`](./source-update-order-preservation.md)
- [`sql-sanitize-ddl-guards.md`](./sql-sanitize-ddl-guards.md)
- [`sqlclient-factory-dispatch.md`](./sqlclient-factory-dispatch.md)
- [`sqlite-boolean-default-repair.md`](./sqlite-boolean-default-repair.md)
- [`sqlite-table-update-machine.md`](./sqlite-table-update-machine.md)
- [`ssl-filepath-timing-oracle.md`](./ssl-filepath-timing-oracle.md)
- [`ssrf-policy-gate.md`](./ssrf-policy-gate.md)
- [`stage-timer-null-profiler.md`](./stage-timer-null-profiler.md)
- [`stddev-population-fallback-composition.md`](./stddev-population-fallback-composition.md)
- [`system-column-remap-gate.md`](./system-column-remap-gate.md)
- [`system-column-roster.md`](./system-column-roster.md)
- [`telemetry-only-socket-gateway.md`](./telemetry-only-socket-gateway.md)
- [`throttler-crosspod-rollup.md`](./throttler-crosspod-rollup.md)
- [`thumbnail-backfill-migration.md`](./thumbnail-backfill-migration.md)
- [`thumbnail-batch.md`](./thumbnail-batch.md)
- [`thumbnail-bomb-guard.md`](./thumbnail-bomb-guard.md)
- [`thumbnail-processor-path-algebra.md`](./thumbnail-processor-path-algebra.md)
- [`transient-error-taxonomy.md`](./transient-error-taxonomy.md)
- [`undo-scope-resolution.md`](./undo-scope-resolution.md)
- [`unique-constraint-funnel.md`](./unique-constraint-funnel.md)
- [`updown-statement-duality.md`](./updown-statement-duality.md)
- [`upload-error-triage.md`](./upload-error-triage.md)
- [`url-upload-passthrough.md`](./url-upload-passthrough.md)
- [`use-worker-env-gates.md`](./use-worker-env-gates.md)
- [`use-worker-rpc.md`](./use-worker-rpc.md)
- [`user-display-name-substitution.md`](./user-display-name-substitution.md)
- [`version-keyed-query-dispatch.md`](./version-keyed-query-dispatch.md)
- [`view-section-copy-inheritance.md`](./view-section-copy-inheritance.md)
- [`views-prefix-copy-bug.md`](./views-prefix-copy-bug.md)
- [`virtual-column-identity-substance-split.md`](./virtual-column-identity-substance-split.md)
- [`virtual-title-dedup-ladder.md`](./virtual-title-dedup-ladder.md)
- [`webhook-condition-evaluator.md`](./webhook-condition-evaluator.md)
- [`webhook-decoupling.md`](./webhook-decoupling.md)
- [`webhook-rehydration-guards.md`](./webhook-rehydration-guards.md)
- [`widget-handler-contract.md`](./widget-handler-contract.md)
- [`xwhere-listargs-parity.md`](./xwhere-listargs-parity.md)
