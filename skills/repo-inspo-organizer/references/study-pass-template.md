# Compatibility pointer.

The canonical shared study skeletons are under `~/.dsh/template/study/`, outside the skill package (`00-overview.md` through `04-verification.md`). Copy those files into each source `study/` folder and fill them in order.
