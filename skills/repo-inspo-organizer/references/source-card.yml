# Compatibility pointer.

The canonical shared source card is `~/.agents/templates/source.yml`, outside the skill package. Copy it to each source sidecar, then fill every placeholder with a pinned value or an explicit reason. Host mirrors (e.g. `/home/utopia/.dsh/template/work/catalog/source.yml`) are derived copies; fix drift in `~/.agents`.
