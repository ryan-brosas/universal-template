<!-- capsule-v2 -->
**Source:** dub `movePartnersToGroup` @ pin `29df217a29`
**Question:** How do you move partners between groups without duplicating reward/discount fan-out or blocking the HTTP response?
**Path/Symbol:** `apps/web/lib/api/groups/move-partners-to-group.ts` — `movePartnersToGroup({workspaceId, programId, partnerIds, userId, group, isGroupDeleted?, groupMoveDisabledAt?})`
**Signature:** returns `Promise<number>` = updateMany count; `group` is a Pick of PartnerGroup id/name + the FIVE denormalized columns copied onto enrollments (`clickRewardId, leadRewardId, saleRewardId, referralRewardId, discountId`).
**Data Shape:** ProgramEnrollment carries group state DENORMALIZED (groupId + 5 reward/discount ids), so a move is one `updateMany`, not per-row logic.
**Decisive source:** :50-74 — enrollment snapshot FIRST (`findMany` select id/partnerId/status/partnerGroup), then `partnerIds = programEnrollments.map(({partnerId}) => partnerId)` :74 REBINDS the input to enrolled-only ids: non-enrolled ids silently vanish from every downstream side effect (links fetch, bounty trigger, notify) because there are no enrollment rows to update. :76-92 updateMany spreads the five columns; `...(groupMoveDisabledAt !== undefined && { groupMoveDisabledAt })` :90 means `undefined` = leave column untouched while explicit `null` clears it.
**Flow/Invariant:** Empty input → return 0 BEFORE any query; zero enrollments → return 0; count===0 → return 0 with NO waitUntil work. All post-write work (link refetch, fresh-enrollment read for change sets, owner-attribution fallback, five-way `Promise.allSettled`: remap-default-links qstash / draft-bounty trigger / tinybird recordLink / partner notification / activity log) runs inside ONE `waitUntil(async () => ...)` :98-201 so the route returns immediately. Activity logs pair pre-update vs re-read enrollments by enrollment id (:136-153).
**Probe (direct test):** from repo root `$REFERENCE_ROOT/platforms/dub`: `grep -c 'return 0;' apps/web/lib/api/groups/move-partners-to-group.ts` → `3`; `grep -n 'partnerIds = programEnrollments.map' apps/web/lib/api/groups/move-partners-to-group.ts | cut -d: -f1` → `74`; `grep -c 'Promise.allSettled' apps/web/lib/api/groups/move-partners-to-group.ts` → `1`.
**Retrieve:** `echo '{"project":"mnt-hdd-utopia-inspo-platforms-dub","query":"movePartnersToGroup","limit":5}' | codebase-memory-mcp cli search_graph`
**Verdict:** adopt — the rebinding + waitUntil-allSettled choreography is the whole contract; naive ports either block the request or leak side effects to unenrolled ids.
