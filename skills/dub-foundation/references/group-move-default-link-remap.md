<!-- capsule-v2 -->
**Source:** dub `remapPartnerGroupDefaultLinks` + route @ pin `29df217a29`
**Question:** When a partner changes groups, what happens to their default links, and why is the mapping match keyed on normalized URLs?
**Path/Symbol:** `apps/web/app/(ee)/api/cron/groups/remap-default-links/route.ts` (POST, qstash-signed) + `apps/web/app/(ee)/api/cron/groups/remap-default-links/utils.ts` — `remapPartnerGroupDefaultLinks({partnerId, partnerLinks, newGroupDefaultLinks}) → {linksToCreate, linksToUpdate, linksToRemoveMapping}`
**Signature:** pure function; identity = `normalizeUrl(url)` on BOTH sides; matching default links are consumed from the Map (`delete`) so one old link claims exactly one new default.
**Data Shape:** three disjoint buckets per partner: create {domain,url,partnerId,partnerGroupDefaultLinkId} / update {id,newDefaultLinkId} / removeMapping [linkId].
**Decisive source:** route docstring :31-43 defines the algorithm. :91-105 the isGroupDeleted branch: DELETE /groups cascades `partnerGroupDefaultLinkId → null` (Prisma SetNull), so the cron must take ALL partner links and re-map them, while a normal group move filters to already-mapped links only. :145-205 creation goes through generatePartnerLink with AppsFlyer parameters loaded ONLY when any new-group default URL matches `isAppsFlyerTrackingUrl`; per-link failures drop out via allSettled+isFulfilled before bulkCreateLinks. :208-252 updates batched per target default-link id via updateMany; removal is one updateMany to null.
**Flow/Invariant:** Chain continues: this cron ALWAYS schedules sync-utm (:254-260) then remap-discount-codes (:266-274) with the same body flags — the group-change pipeline is remap-links → sync-utm → remap-discounts in qstash order.
**Probe (direct test):** from repo root: `grep -c 'normalizeUrl' apps/web/app/(ee)/api/cron/groups/remap-default-links/utils.ts` → `3`; `grep -n 'newDefaultLinksByUrl.delete' apps/web/app/(ee)/api/cron/groups/remap-default-links/utils.ts | cut -d: -f1` → `56`; `grep -c 'verifyQstashSignature' apps/web/app/(ee)/api/cron/groups/remap-default-links/route.ts` → `2`; `grep -c 'isFulfilled' apps/web/app/(ee)/api/cron/groups/remap-default-links/route.ts` → `2`.
**Retrieve:** `echo '{"project":"mnt-hdd-utopia-inspo-platforms-dub","query":"remapPartnerGroupDefaultLinks","limit":5}' | codebase-memory-mcp cli search_graph`
**Verdict:** adopt — normalize-keyed consume-on-match + SetNull-aware dual mode is the porting trap.
