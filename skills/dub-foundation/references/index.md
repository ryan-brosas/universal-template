<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->


# Dub Foundation

## Use this for
Use when wiring a serverless background-job pipeline (publish, outbox fallback, signed executor), designing one stable API error envelope, offering cursor pagination with a bounded offset escape hatch, centralizing rate-limit policies, adding safe distributed locks and retry/batch utilities, or porting a partner-payout platform end-to-end: eligibility predicates, invoice claim + charge, settlement-gated multi-rail money movement, provider-id-keyed lifecycle closures, amount reconciliation, referral reward engines, and partner-owned event callbacks. Source code and direct tests are ground truth; references carry decisive excerpts and graph retrieval.

## Load the matching source dump
- `./job-dispatch.md` — defineJob/dispatchJobs: publish-over-QStash with per-chunk batching, exponential publish retries, and a DB-outbox `deferred` fallback that never loses a job.
- `./job-executor.md` — one signed HTTP executor route where 2xx means permanent (stop retrying) and 500 means transient (queue retries); static-import handler registry with name cross-checks.
- `./webhook-signature.md` — raw-body-first HMAC signature verification with explicit dev bypass and library-error translation into the app's error class.
- `./api-error-envelope.md` — closed-code `DubApiError` + central mapper ordering Zod → domain → Prisma P2025 → opaque fallback; code→status/docs-anchor derivation; SDK-facing OpenAPI error factory.
- `./dual-pagination.md` — presence-of-cursor mode selection, mutual-exclusion validations, unique-id cursor with signed take + skip:1, and MAX_OFFSET_PAGE=1000 cap.
- `./policy-rate-limit.md` — declarative policy table (`{attempts, window, keyPrefix, message}`) + single assert gate composing `[keyPrefix, ...ids].join(":")` keys with human retry-after formatting.
- `./redis-lock.md` — NX+EX acquire with a unique token value and Lua compare-and-delete release; null-means-busy signaling.
- `./batched-processing.md` — processInBatches count-driven loop with truthful `hasMore` budget-exhaustion signal (direct test present).
- `./fetch-retry.md` — fetchWithRetry: per-attempt AbortController timeouts, 429/5xx-only retry whitelist, quadratic backoff shared across status and network failures.

### Pass 2 additions (webhook delivery lifecycle · redirect edge · bulk link writes)
- `./webhook-dispatch-fanout.md` — sendWebhooks: Promise.allSettled fan-out to N subscriber URLs returning per-webhook `{webhookId, ok, messageId?|error}` rows that never reject.
- `./webhook-delivery-callback.md` — QStash delivery-status callback: base64 body echo, `status>=400||−1` failure test, atomic increment-then-branch ladder (notify@5/10/15 → disable@20 → workspace flag sync), reset-on-first-success.
- `./webhook-receiver-dialects.md` — exact-hostname receiver classification (`user|zapier|make|slack|segment`), transform-before-sign payload dialects, per-receiver transport headers.
- `./webhook-click-fastpath.md` — derived Redis set of click-subscribing workspaces recomputed from Prisma on every webhook mutation; hot click stream checks membership before any DB read.
- `./webhook-event-envelope.md` — the public webhook body contract: `evt_<nanoid25>` ids minted in one function, enum'd triggers, parse-at-creation envelope with `any` data slot guarded by family schemas.
- `./link-redirect-ladder.md` — LinkMiddleware's ordered guard table (inspect→password→banned→disabled→expired→bot/proxy→URI-scheme→cloak→ios/android/geo→regular), rewrite-vs-redirect per branch, `_root`=301 policy.
- `./click-attribution-continuity.md` — three-tier clickId resolution (cookie → identity-hash Redis → fresh nanoid) attached as cookies on every terminal response; 1h dedup vs 1w A/B stickiness lifetimes.
- `./link-destination-assembly.md` — getFinalUrl: visitor-wins param merge with an internal-param denylist enforced at merge AND post-merge; Stripe `client_reference_id` swap protocol.
- `./link-bot-detection.md` — ordered bot signals (param→HEAD→UA-with-false-positive-scrub→referer→IP/CIDR) and the bot∧proxy rewrite-not-redirect metadata contract.
- `./link-abtest-routing.md` — weighted variant draw scaled to weight sum, cookie stickiness validated against current variants, fail-open nulls past two entry gates.
- `./link-request-parsing.md` — parse(): domain/key vs fullKey/fullPath derivation, decode-before-normalize ordering, preview-host collapse, `_root` + inspect conventions.
- `./bulk-create-ordering.md` — bulkCreateLinks: shortLink→index map before createMany(skipDuplicates), refetch-by-identity, relation backfill through the same map, explicit order-restoring sort.
- `./bulk-update-validation-split.md` — bulk PATCH two-tier failures (workspace refs throw, item problems become result rows), nested deleteMany+create relation swaps, shared R2 image key per call.
- `./bulk-delete-counter-tx.md` — discount-cleanup-first, deleteMany+counter-decrement in ONE interactive tx using real affected count, batched allSettled side effects gated on deletedCount>0.
- `./link-cache-tiering.md` — LRU(10k/5s) → Redis(24h) → Vercel cache(5min, error-gated only) → MySQL; miss≠error control flow, asymmetric key derivation, throw-under-outage guard.

### Pass 3 additions (single-link write path · click ingestion · list composition)
- `./process-link-validation-gate.md` — processLink funnel returning `{link, error, code}` tuples (never throws): URL+UTM folding, plan/domain policy ladder, server-forced projectId/userId, skip-flag re-entry so updates can rerun the gate.
- `./key-admission-ladder.md` — pure processKey normalizer (`null`=reject, `_` prefix reserved, domain-scoped NFD diacritic strip + punycode) then keyChecks' ordered ladder incl. phantom-occupancy reserved/redirect tables and length-tiered plan gates.
- `./single-row-write-propagation.md` — createLink/updateLink: DB commit → waitUntil(Promise.allSettled) fan-out (cache mset w/ partner enrichment, Tinybird recordLink, R2 upload w/ optimistic image swap, anon-link 30-min TTL cron, AB-test scheduling); compensating cache delete ONLY on key/domain change; nonced image URLs.
- `./link-update-route-choreography.md` — PATCH /links/[linkId]: body-onto-row merge where absent UTMs mean preserve, `_root` address pinned, deepEqual no-op short-circuit, computed skipKeyChecks/skipExternalIdChecks from the actual diff, blocked cross-workspace transfer naming /transfer, schema-validated link.updated webhook after response.
- `./case-sensitive-key-codec.md` — XOR+base64 reversible keyspace transform gated by CASE_SENSITIVE_DOMAINS so case-folding storage serves `Foo`≠`foo`; explicitly not security; every read/write path must wrap.
- `./tombstone-metadata-ingest.md` — recordLink over a typed Tinybird ingest endpoint: full-row re-emit per mutation, `deleted:1` tombstones (append-only can't UPDATE), null→"" coercion, keys decoded before warehouse landing.
- `./click-ingest-failclosed.md` — recordClick presence-gate ladder (clickId → dub-no-track → bot → 1h identity dedup) with FAIL-CLOSED semantics: Redis errors DROP the click; dedup key arms only after recording; EU IPs never persisted; clickIdCache Redis mirror bridges Tinybird ingest lag.
- `./hard-delete-choreography.md` — deleteLink: snapshot-with-relations BEFORE delete, dependents-first single `$transaction`, ownership-predicated blob GC (`startsWith(R2/images/<id>)`), atomic counter decrement OUTSIDE the tx, idempotent eventual cleanups under allSettled.
- `./workspace-list-composition.md` — getLinksForWorkspace: flat tenancy equality + AND-intersected OR branches; folder visibility = permitted-folderIds UNION `folderId:null` so unfiled rows stay visible; tagId/tagIds > tagNames precedence; exact-mode pivots shortLink-vs-key on `https://` prefix.
- `./link-serializer-boundary.md` — transformLink: the single API exit shape renaming internal columns (projectId→prefixed workspaceId), folding joins to arrays, synthesizing back-compat stubs (`identifier:null`, first-tag mirror), deriving qrCode at read time, skipDecodeKey escape hatch.
- `./archive-flag-semantics.md` — archive = bare boolean column toggled via PATCH; enforced at exactly ONE list-side expression (`showArchived ? undefined : false`); archived ≠ deleted (analytics/cache survive).
- `./cursor-identity-validation.md` — cursors resolved + tenancy-checked BEFORE paging with an intentionally uniform "does not exist" message (kills the existence oracle); getLinkOrThrow alias ladder (id / `ext_` compound lookup / domain→_root collapse) with self-correcting error codes (bad_request hint < not_found < unauthorized).

### Pass 4 additions (durable workflow engine · account merge · drift re-pins, upstream `873edc5a` → `e3a558d1`)
- `./durable-workflow-step-contract.md` — serve()+context.run step contract: every durable step named, idempotent, self-revalidating on retry; skip paths also durable; failureFunction clears the entry-gate cache.
- `./overlap-first-merge-plan.md` — merge-partner-accounts plan: overlap enrollments fold BEFORE transfers move; every mutation WHERE-scoped to the source partner so lost races abort instead of stealing; tenantId copied only when unclaimed.
- `./batched-row-transfer-ladder.md` — count-driven limited-updateMany loop (exit ONLY when count < limit) for high-volume ownership transfers + oldest-survivor payout fold (update survivor → re-point children → delete donors).
- `./workflow-operator-table.md` — WORKFLOW_OPERATORS co-locate validate (loud throw → 400) and evaluate (silent false on any mismatch); zod superRefine and the evaluator consume the SAME records.
- `./workflow-attribute-registry.md` — attribute definitions carry operators allowlist + data-cost (`requires`) + exclusivity; schemas, validation, and fetch-planning all derive from ONE registry; per-type overlays narrow by spread.
- `./enrollment-window-resolution.md` — scheduled "enrolled ≥ N days" is a SQL createdAt window sized to the cron period (+12h), then STRIPPED from JS evaluation to avoid double-filtering false-negatives; sent-ledger dedup before sends.
- `./discount-code-deletion-funnel.md` — one deleteDiscountCodes entry point: soft (disabledAt stamp) vs hard branch, webhooks under waitUntil, provider revocation enqueued only for known non-custom providers; orphans still die locally.
- `./ai-usage-credit-reserve.md` — quota gate = single conditional UPDATE (`SET aiUsage = aiUsage + 1 WHERE aiUsage < aiLimit`, count 0 ⇒ forbidden); refund decrements floored at zero.
- `./named-ratelimit-policies.md` — policy table grows actor/target DUAL policies for attackable pairs (email-change, DNS instructions) and frozen verbatim error contracts UI string-matches.

### Pass 5 additions (workflow engine core · merge internals · onboarding & commission pipelines)
- `./workflow-trigger-dispatch.md` — executeWorkflows funnel: JSON-path prefilter → parse-and-skip corrupt rows → lazy commission aggregate → catch-and-continue handler loop → flush-before-return.
- `./condition-validate-vs-evaluate.md` — loud numbered-error checker (per-type overlay) vs silent-false run-time predicate, including the deliberate empty-conditions asymmetry.
- `./workflow-config-parse-gate.md` — re-parse stored JSON at execution time; empty conditions/actions throw; `actions[0]` only (multi-action configs silently truncate).
- `./workflow-attribute-data-requirements.md` — attribute registry carries `requires` (fetch planning), `scheduled`, `exclusive` flags; superset table + per-type overlays never mutate each other.
- `./move-group-guard-ladder.md` — fresh-read → same-target skip → groupMoveDisabledAt override → evaluate on fresh groupId → NX dedup lock released in finally.
- `./award-bounty-accumulation-gate.md` — upsert-increment draft submission, condition gated on POST-increment total, terminal flip WHERE-guarded to `status:"draft"`.
- `./campaign-dual-mode-audience.md` — optional-context dual mode (single partner vs scheduled cohort); enrolledDays enforced ONLY as a cron-sized SQL window; DB sent-ledger + per-user Idempotency-Key double dedup.
- `./partner-link-stats-kernel.md` — zero-object-over-null link-stat aggregation with single-site bigint→cents normalization.
- `./merge-plan-dynamic-steps.md` — merge plan as first-step output; per-enrollment step names (`merge-enrollment-<id>`) make crash-resume replay-safe; skip paths clear the verification cache durably.
- `./unique-row-merge-arbitration.md` — three-tier table strategy (batched ≤250 high-volume / single low-volume / transfer-else-delete unique) with the strict `count < limit` exit.
- `./enrollment-merge-tx.md` — overlap tx: raise-only status promotion, application-id severance, tenant copy with existence pre-check; transfers WHERE-scoped to the source partner.
- `./payout-fold-finalize-sync.md` — oldest-survivor payout fold (widen envelope → re-point commissions in batches → delete donors); finalize collects allSettled then THROWS so idempotent syncs retry.
- `./merge-teardown-ordering.md` — rewinds → user (only if workspace-less) → fraud events (count===2 auto-resolve rule) → raw-SQL `DELETE FROM Partner` LAST.
- `./partner-approved-choreography.md` — seven guarded durable steps; network-program early-return before side effects; enrollment-keyed batch-email idempotency; step outputs hand off created links to the webhook payload.
- `./commission-eligibility-ladder.md` — ordered gates (fraud/canceled first-commission → lead-once → max-duration incl. original-reward one-time → spend-limit clamp) ending in a P2002-as-dedup write.
- `./commission-hold-gating.md` — dual-source hold decision (triggered rules OR pending partner-level group), pending-guarded flip with refetch fallback, per-effect predicates in an allSettled fan-out.
- `./partner-identity-sync-discipline.md` — cross-namespace email conflict checks routing to the merge flow, copy-on-sync image keys, undefined-vs-null partial-update semantics.
- `./force-withdrawal-orchestration.md` — per-partner NX lock with loud-fail contention, rail dispatch on default method, 90-day sweeper reusing the same force path in batches of 20.
- `./cutoff-period-splitting.md` — splitPayouts partitions commissions at a cutoff instant: original payout keeps its id with narrowed envelope, post-cutoff rows move to a new `po_` payout; >1000 eligible payouts refuse cutoffs.

### Pass 6 additions (partner payouts plane · referral engine · postbacks · importers · cron kernel)
- `./payout-aggregation-claim.md` — aggregateDueCommissionsForPartner: create-empty-payout → raw-SQL guarded claim (Prisma updateMany drops MySQL predicates, issue #28840) → recompute amount from DB truth; mutable-status JOIN closes the confirm race.
- `./payout-processing-funnel.md` — processPayouts: eligibility-filter updateMany claim → hybrid external re-mode → mode sums → fee waiver → FX markup → PaymentIntent keyed `process-payout-invoice/${invoiceId}`; confirm action's pre-flight guard ladder lives in `payout-confirm-guards`.
- `./payout-mode-eligibility.md` — getPayoutEligibilityFilter's internal/external/hybrid branches + NOT-wrapped Tremendous window; read-side getEffectivePayoutMode derives legacy rows' mode from payoutsEnabledAt.
- `./payout-charge-router.md` — charge-succeeded route: raw method backfill → card fund-settlement probe (`available_on`+15min, self-republish w/ dedup+parallelism 1) → allSettled rail fan-out; balance-leaving rails gated on fundsAvailable.
- `./payout-connect-transfer.md` — createStripeTransfer two-bucket fold, capability-gate downgrade, below-min defer-or-$0.50-fee, and source_transaction attached ONLY when no stranded backlog exists; content-addressed idempotency keys.
- `./payout-stablecoin-ladder.md` — fee-netted floor-rounded outbound payments via Stripe Financial Account pre-funding; closed-account vs missing-capability degradation split.
- `./payout-paypal-batch.md` — Redis-cached OAuth token with expiry buffer, invoice-keyed batch ids, failed-only transactional retry flip with revert-on-error.
- `./payout-tremendous-giftcards.md` — fold-to-one-order with deterministic external_id; non-EXECUTED orders release payouts to processed (not failed) for safe retry inside the $5–$2000 window.
- `./payout-method-recompute.md` — recomputePartnerPayoutState: capability-checked activity matrix (stablecoin/connect/paypal/tremendous), sticky default, payoutsEnabledAt reset exactly on default change.
- `./payout-amount-reconciliation.md` — every-minute sweeper: token NX lock w/ Lua owner-release, 2-min lookback keyset scan, tx delete-empty-or-restamp guarded by MUTABLE_PAYOUT_STATUSES.
- `./payout-lifecycle-webhooks.md` — payout.failed/paid map provider payout ids onto rows; balance-available auto-withdrawal re-tags sent-unpaid + previously-failed rows and floors HUF/TWD to hundreds.
- `./payout-status-machine.md` — the full transition graph (pending→processing→sent→completed, processed-as-release, failed-retryable, canceled-mutable) with the owning writer per edge.
- `./payout-confirm-guards.md` — validate-then-create ladder incl. EXTERNAL_WEBHOOK_REQUIRED fatal gate and mandate-fail detach self-healing.
- `./referral-commission-engine.md` — four-trigger taxonomy (saleRecorded/commissionEarned percentages, partnerApproved/threshold flats) with synthetic `referral:`-prefixed invoiceIds riding the (invoiceId,programId) unique constraint; P2002-as-dedup.
- `./referral-attribute-loop-guard.md` — claim-shaped conditional upsert (`referredByPartnerId:null` in where) + bounded ancestor walk that breaks on pre-existing cycles; fabricated funnel timestamps.
- `./referral-queue-fanout.md` — payout-driven enqueue: sent/completed gate, per-sale jobs vs ONE threshold job, network-bonus fallthrough.
- `./network-referral-bonus.md` — recruiting bonus estimated as 50% of an assumed-3% fee share, recorded as a synthetic "Partner payout fee" sale in the network program's customer namespace.
- `./referral-backfill.md` — retroactive reward cron re-validates attribution then reuses the same idempotent workers per trigger family.
- `./partner-postback-dispatch.md` — selector → enricher registry (snake→camel, UTC-forced timestamps) → receiver adapters with TRANSFORM-BEFORE-SIGN HMAC over QStash delivery+failure callbacks logging attempts (-1⇒503).
- `./partner-postback-lifecycle.md` — once-shown `pbsec_` secrets, hostname-classified receivers, cap=5, trigger-scoped sample-event tests skipping enrichment.
- `./provider-import-continuation.md` — five-provider importer pattern: MAX_BATCHES ceiling → cursor-in-payload self-requeue → terminal credential teardown → next-action chaining; ±1h transition-window dedup.
- `./cron-dual-auth.md` — withCron method-keyed verifiers (Vercel Bearer GETs / Upstash Receiver POSTs), VERCEL≠1 dev bypass, raw-body clone discipline, qstashWithoutBypass trust boundary.
- `./side-effect-walker.md` — recursive audit-log/email walker: take 100 + skip:1 cursor + full-page requeue; card-vs-debit email suppression.
- `./workflow-trigger-entry.md` — triggerQStashWorkflow choke point: retries 5, flowControl defaults (type-keyed parallelism 15) vs caller-overridden serial partner keys, correlated trigger-failure logs.
- `./jobs-outbox-deepening.md` — dispatchJobs published-vs-deferred contract (DB persist only after publish failure) + minute-cron replay deleting rows on VERIFIED messageIds, attempt-capped at 10.
- `./batch-email-idempotency.md` — queueBatchEmail dual-layer keys (QStash deduplicationId AND Resend Idempotency-Key identical, `-batch-${i}` suffixes), empty-input success.
- `./fx-conversion-dual-path.md` — cached-Rates best-effort conversion for imports vs live Stripe fx_quotes (rate>0 validated, markup inside round) for charges; zero-decimal ×100 rule.
- `./payout-fee-waiver-math.md` — three-term fee composition (non-waivable hard cost + waivable plan rate on charged slice + flat fast-ACH) with usage-counter depletion after success.
- `./payout-list-read-path.md` — tenant-alias access path throwing on misses, relation-scoped group NOT-IN filters, page-wide derived-mode projection.
- `./payout-count-eligibility.md` — count route's eligibility switch sharing THE claim predicate plus server-side enum zero-fill for stable tabs.
- `./payout-detail-projection.md` — [payoutId] read-time derivation (mode/traceId/tenantId) with PayoutResponseSchema.parse as the whitelist projection.
- `./payout-export-split.md` — ≤1000 inline CSV else 202 + QStash worker paging via async generator, 100k remaining-budget cap, R2 signed link emailed.
- `./payout-reminder-cadence.md` — partner nudges gated IN the selection predicate (3-day last-reminded stamp captured pre-send), status set limited to claimable money.
- `./payout-owner-reminders.md` — month-end weekday-window owner digest with per-program custom-minimum tiers and 14-day recent-invoice suppression.
- `./payout-owner-nudges.md` — treasury sweep withdrawing netBalance − owed-payouts − $30k reserve with negative-pending netting.
- `./payout-count-detail-read.md` — dashboard tab counts (groupBy+zero-fill) and single-payout detail derivation sharing the eligibility filter.
- `./payout-methods-surface.md` — settings projection `{type,label,default,connected,identifier}` with per-rail masking (wallet 6••••4, routing••••last4) driven by live probes.
- `./stripe-v2-preview-client.md` — schema-typed @better-fetch registry for /v2 money-management endpoints + version-pinned raw-fetch fx_quotes sibling; Stripe-Context header scoping.

### Pass 7 additions (fraud rule engine · commission holds · group lifecycle)
- `./fraud-detection-entry-gates.md` — detectAndRecordFraudEvent funnel: zod safeParse → inactive-enrollment → riskMonitoringDisabledAt → per-rule try/catch loop; every skip returns `[]`, one broken rule never blocks the rest.
- `./fraud-rule-registry-stubs.md` — Record<FraudRuleType, defineFraudRule> total registry; partner-level enum members registered as never-trigger stubs so conversion dispatch can't double-fire them; unknown type throws.
- `./fraud-rule-config-merge.md` — global catalog iterated, program rows override per type; NO row means enabled-by-default (opt-out model); name/description always catalog-owned.
- `./fraud-event-hash-identity.md` — per-type identity projection into sha256-base64url(24) sorted-key hash; customer-level rules require customerId (fail-closed throw); three metadata keys sanitized pre-persist.
- `./fraud-event-group-continuity.md` — ONE pending group per (programId,partnerId,type): in-memory hash dedupe → pending-status-qualified DB dedupe → reuse-or-create groups → createMany frg_/fre_ ids → allSettled counter bumps refreshing lastEventAt.
- `./fraud-rule-email-match-tiers.md` — exact → generic-mail firewall → same-domain → historical-domain (endsWith probe scoped program+partner, gated by isFirstConversion presence) ladder with matchType metadata per tier.
- `./fraud-rule-click-signals.md` — paid-traffic param-table fingerprint (7 platforms) with google campaign allowlist escape hatch; referral-source ban over BOTH referer fields via case-insensitive minimatch on www-normalized domains.
- `./fraud-rule-disposable-domain.md` — single Redis SISMEMBER against curated set with FAIL-OPEN outage posture (contrast: click dedup fails closed).
- `./fraud-partner-application-risks.md` — application-time risk matrix: two state-backed booleans (pending partner-level groups) + four pure profile heuristics, ranked through FRAUD_SEVERITY_CONFIG ranks.
- `./fraud-duplicate-account-detectors.md` — Veriff-identity / payout-method XOR-wallet sweeps sharing one skeleton: shared-key enrollments → rule-disabled filter → >1-multiplicity gate → full n×n directed-pair events → best-effort holds.
- `./fraud-commission-hold-plane.md` — plan-gated bulk hold ladders (pending / processed+payout.detach+retally) on chunk(50)×while(true)×LIMIT=250 with refetch-actually-held reconciliation after every re-guarded updateMany.
- `./fraud-hold-release-ladder.md` — partner-level survivor vetoes ALL releases; customer-level groups block only their customers (custom commissions exempt); batched hold→pending flips fan out four allSettled side effects with status-re-stamped log rows.
- `./fraud-group-resolution-expiry.md` — human resolve action (role+plan gates, partnerComment, optional monitoring kill-switch) vs daily expiry sweeper (lastEventAt < now−30d, BATCH_SIZE=250) converging on one QStash release queue; crossProgramBan never expires.
- `./fraud-network-ban-propagation.md` — issuing program excluded, active+monitored enrollments of every OTHER program receive sourceProgramId-attributed events unless their rule is disabled; immediate holds per receiving program.
- `./fraud-conversion-hold-integration.md` — sole detection call site inside createPartnerCommission: dual-source hold (triggered-now OR pre-existing partner-level group), exemptions for explicit status imports/clawbacks/non-pending, catch-refetch race recovery.
- `./fraud-schema-and-test-harness.md` — four-table Prisma shape (@@unique([programId,type]) overrides, composite group index, append-only hashed events) + poll-until-event verifyFraudEvent harness pinning full event shape.
- `./richtext-provider-feature-gates.md` — RichTextProvider turns a features array into the exact TipTap extension set (StarterKit link ALWAYS off, immediatelyRender:false for SSR, editorRef stale-closure guard).
- `./richtext-mention-variable-fallback.md` — Mention.extend adds a fallback attr rendered losslessly as data-fallback HTML AND `{{id | fallback}}` text; two-stage picker with PartnerName-only fallback input, 10-item cap.
- `./richtext-safe-link-normalize-ladder.md` — normalizeLinkHref prefixes https:// then isSafeLinkHref verifies via URL-parse protocol allowlist {http:,https:,mailto:}; normalize FIRST, verify the normalized value, toast-and-stay on failure.
- `./richtext-link-modal-selection-replay.md` — deferred link dialogs freeze `{from,to,text,href}` and replay setTextSelection before every mutation; unchanged label setLink vs rewritten label insertContent-with-link-mark; empty label falls back to href-as-text.
- `./richtext-link-click-to-edit.md` — handleClick consumes clicks on `a[href]` descendants (consumer handlers first), repositions caret, extendMarkRange("link") grows to the whole mark span; openOnClick:false + inclusive:false are both load-bearing.
- `./richtext-link-hover-tooltip.md` — delegated mouseover/mouseout on editor.view.dom (PM owns those anchors, React cannot wrap them); anchor→anchor moves keep the tooltip; capture-phase scroll clears; modal state suppresses render.
- `./richtext-file-handler-paste-guard.md` — FileHandler MIME allowlist {png,jpeg,gif,webp}; paste WITH htmlContent returns false so default paste wins (no double-insert); drop inserts at drop pos, bare paste at selection anchor.
- `./richtext-image-alt-node-view.md` — NodeView chrome islands wrapped contentEditable={false} + stopEvent mousedown; onOpenAutoFocus preventDefault steers focus into the alt input; trim-to-null saves keep hasAlt honest; popover force-closes when isEditable flips false.
- `./richtext-campaign-image-link-plane.md` — image NODE carries sanitized href attr: parseHTML hoists from closest <a> via isSafeLinkHref, renderHTML emits ONLY data-linked-image marker, cmd/ctrl-click plugin re-verifies then window.open noopener.
- `./richtext-toolbar-image-link-branch.md` — one Link button dispatches text-mark modal vs image-attr prompt; nodePos captured BEFORE window.prompt blurs; cancel/empty(unlink)/unsafe(toast)/safe quadruple each re-selects the node after.
- `./richtext-suggestion-menu-lifecycle.md` — ReactRenderer session contract: body-append + posToDOMRect floating positioning, keydown bridge through useImperativeHandle (consume-or-pass), element.remove() BEFORE destroy() or the menu div leaks.
- `./richtext-provider-consumer-wiring.md` — chat plane (condensed, markdown mode, getMarkdown, bold/italic/links only, cmd+Enter + colon-emoji consumer keydowns) vs campaign plane (relaxed, JSON mode, getJSON, imageControls + variables) off ONE provider.
- `./messages-panel-group-compaction.md` — parent-computed isGroupedWithPrevious = !firstFromSender && !newTime threads down as prop; single `-mt-1.5` token in BOTH live and campaign branches pairs with prose `[&_p+p]:mt-2`.
- `./callout-variant-table.md` — flat variant table {icon, containerClassName, iconClassName} ×5; warn/error share TriangleWarning and differ only by palette; text color on container, min-w-0 content wrapper.
- `./message-markdown-link-tooltip.md` — read-plane links wrap Tooltip at the react-markdown component map (the DUAL of delegated DOM events when React owns the nodes); prose margins collapsed to p+p adjacent spacing.

- `./group-move-core-choreography.md` — movePartnersToGroup: enrollment snapshot REBINDS partnerIds to enrolled-only ids; one updateMany copies the five denormalized reward/discount columns; every side effect rides ONE waitUntil allSettled fan-out.
- `./group-move-rule-overlap-lattice.md` — findGroupsWithMatchingRules three-gate overlap: disjoint eq/in partnerGroup id-sets short-circuit false, zero shared metric attributes short-circuit false, else EVERY shared metric must interval-overlap (gte/between closed-interval test).
- `./group-move-rules-upsert-tri-state.md` — upsertGroupMoveRules tri-state contract: [] + existing workflow deletes it (null), field absent returns undefined as a strict no-op, provided rules = plan gate -> validate -> conflict sweep -> create/update.
- `./group-delete-drain-and-reward-gc.md` — DELETE /groups: while(true) <=100-enrollment drain into the default group with isGroupDeleted:true, commission-gated reward GC inside the final tx, discount deliberately left for remap-discount-codes.
- `./group-move-default-link-remap.md` — remapPartnerGroupDefaultLinks matches on normalizeUrl keys consuming each new default once (create/update/removeMapping buckets); isGroupDeleted mode takes ALL links because DELETE cascades partnerGroupDefaultLinkId SetNull.
- `./group-move-discount-code-repartition.md` — remap-discount-codes partitions by isDiscountEquivalent (re-point vs provider-side delete), auto-provisions missing codes with a fail-open break on four provider error classes, then GCs the old discount at zero remaining codes.
- `./group-move-rule-scrub-on-delete.md` — removeGroupIdFromMoveRules scrubs dead ids with a JSON.stringify change gate; an in[] emptied by the scrub DROPS the whole condition instead of leaving a match-nothing rule.

## Capsule map
- **Job dispatch** — `job-dispatch`: envelope `{name, dispatchedAt, payload}`, kebab-case `-job` naming, chunked batch publishing; failed publishes persist to an outbox table and are republished by cron — published|deferred|lost is an explicit tri-state. Deepened by `jobs-outbox-deepening` (verified-messageId deletion, attempt cap).
- **Job execution** — `job-executor`: single `/api/jobs/process/[jobName]` route verifies signature, validates the envelope, rejects URL/envelope mismatch, lazy-loads handlers from a code-split registry; retryability is decided by status code alone (ZodError ⇒ 2xx non-retryable).
- **Webhook trust** — `webhook-signature`: verify the raw bytes before parsing; missing header = bad_request, invalid = unauthorized, non-production bypass is an explicit env check.
- **Error contract** — `api-error-envelope`: every thrown error funnels through one handler producing `{error:{code,message,doc_url}}`; unhandled errors never leak their message.
- **Listing** — `dual-pagination`: cursor mode only on unique columns with negative-take backward walks; offset mode capped at page 1000 with an upgrade hint in the rejection message.
- **Abuse control** — `policy-rate-limit`: named policies keep endpoint budgets isolated by key prefix; limiter timeout capped at 1 s so availability beats accuracy; env kill-switch checked before any Redis call.
- **Concurrency** — `redis-lock`: ownership-token locks make expired-lock takeovers safe; contention is a return value, not an exception. Token-less variant in `force-withdrawal-orchestration`, upgraded variant in `payout-amount-reconciliation`.
- **Throughput utils** — `batched-processing`, `fetch-retry`: bounded batch loops with honest hasMore, and per-attempt-timeout HTTP retries with a strict status whitelist.
- **Webhook delivery lifecycle** — `webhook-dispatch-fanout` (per-recipient result objects, never rejects), `webhook-delivery-callback` (queue callbacks → atomic failure ladder → auto-disable), `webhook-receiver-dialects` (classify→transform→sign ordering), `webhook-click-fastpath` (derived Redis eligibility set), `webhook-event-envelope` (`evt_` ids, parse-at-creation).
- **Redirect edge** — `link-request-parsing`, `link-redirect-ladder` (ordered guard table; rewrite keeps short domain, redirect leaves it), `click-attribution-continuity` (cookie→identity-cache→mint clickId threading), `link-destination-assembly` (param denylist merge), `link-bot-detection` (preview rewrites for scrapers), `link-abtest-routing` (sticky weighted draw), `click-ingest-failclosed` (fail-closed click dedup + Tinybird fan-out), `case-sensitive-key-codec` (per-domain reversible key encoding).
- **Bulk link writes** — `bulk-create-ordering` (identity-map + refetch + order restoration), `bulk-update-validation-split` (throw vs per-item rows), `bulk-delete-counter-tx` (real-count decrement tx), `link-cache-tiering` (LRU/Redis/CDN degradation ladder feeding the redirect edge).
- **Single-link write path** — `process-link-validation-gate` (error-tuple funnel every write reuses), `key-admission-ladder` (normalize-then-authorize slug admission), `single-row-write-propagation` (post-commit allSettled fan-out w/ compensating cache deletes), `link-update-route-choreography` (PATCH merge + deepEqual no-op + computed skip flags), `hard-delete-choreography` (funnel-first dependent removal, then bare link delete, eventual cross-system cleanups), `archive-flag-semantics` (on-row boolean soft-delete enforced at one read-side switch).
- **Durable workflow engine** — `durable-workflow-step-contract` (named idempotent steps + failureFunction cache clear), `workflow-operator-table` (loud-validate vs silent-false-evaluate from one record), `workflow-attribute-registry` (operators+data-cost+exclusivity drive schema, validation, and fetch planning), `enrollment-window-resolution` (cron-sized SQL window replaces JS day-diff), `overlap-first-merge-plan` (fold overlaps before transfers; ownership-scoped WHEREs), `batched-row-transfer-ladder` (count-driven limited updateMany loop + oldest-survivor payout fold). Entry surface: `workflow-trigger-entry` (flow-control keys as concurrency contract).
- **Metering & policy plane** — `discount-code-deletion-funnel` (one entry point: soft/hard branch, webhooks, provider-known revocation queueing), `ai-usage-credit-reserve` (conditional atomic increment quota gate + zero-floored refund), `named-ratelimit-policies` (actor/target dual budgets + frozen verbatim error contracts).
- **Workflow engine core** — `workflow-trigger-dispatch` (prefilter→parse-skip→lazy-aggregate→isolated-loop funnel), `condition-validate-vs-evaluate` (loud collector vs silent-false predicate; empty asymmetry), `workflow-config-parse-gate` (re-parse stored JSON, first-action-only), `workflow-attribute-data-requirements` (`requires`/`scheduled`/`exclusive` registry drives fetch planning), `partner-link-stats-kernel` (zero-object stats with single-site bigint normalization).
- **Workflow actions** — `move-group-guard-ladder` (fresh-read skips → user-opt-out → NX dedup lock in try/finally), `award-bounty-accumulation-gate` (upsert-increment → post-increment gate → draft-guarded terminal flip), `campaign-dual-mode-audience` (single-vs-cohort dispatch, SQL-window scheduling attribute, ledger+Idempotency-Key dedup).
- **Account merge internals** — `merge-plan-dynamic-steps` (plan-as-data + per-enrollment step names + durable skip/cache-clear), `unique-row-merge-arbitration` (batched/single/transfer-else-delete matrix, strict `count<limit` exit), `enrollment-merge-tx` (raise-only promotion, tenant existence pre-check, source-scoped WHEREs), `payout-fold-finalize-sync` (oldest-survivor fold + collect-then-throw reconciliation), `merge-teardown-ordering` (children → user → fraud events w/ count===2 resolve → raw-SQL partner delete last).
- **Onboarding & commission pipelines** — `partner-approved-choreography` (seven guarded durable steps, network early-return, enrollment-keyed email idempotency), `commission-eligibility-ladder` (ordered gates ending P2002-as-dedup), `commission-hold-gating` (dual-source hold, pending-guarded flip, per-effect fan-out predicates), `partner-identity-sync-discipline` (dual-namespace email conflicts, copy-on-sync images, undefined-vs-null updates).
- **Read side & analytics sync** — `workspace-list-composition` (tenancy-flat where-builder + unfiled-visible folder union), `cursor-identity-validation` (pre-query cursor tenancy check + ext_-aliased loader), `link-serializer-boundary` (one API exit shape, back-compat stubs), `tombstone-metadata-ingest` (full-row re-emit warehouse sync).
- **Partner payouts plane** — `payout-mode-eligibility` (mode-branched WHERE + derived read mode), `payout-confirm-guards` (validate-then-create incl. webhook-required + mandate detach), `payout-processing-funnel` (claim→hybrid→fees→FX→idempotent charge), `payout-aggregation-claim` (raw-SQL guarded commission claims), `cutoff-period-splitting` (partition-and-repoint preserving row ids), `payout-charge-router` (settlement-gated rail fan-out + self-republish delay), `payout-connect-transfer` (two-bucket fold, source_transaction rule), `payout-stablecoin-ladder` (fee-netted FA-funded outbound payments), `payout-paypal-batch` (cached token, invoice batch ids, guarded retries), `payout-tremendous-giftcards` (one folded order, EXECUTED-or-release), `payout-method-recompute` (capability matrix + sticky default), `payout-methods-surface` (masked settings rows), `payout-lifecycle-webhooks` (provider-id closures + auto-withdrawal re-tagging), `payout-status-machine` (the whole edge graph), `payout-amount-reconciliation` (locked convergent sweeper), `force-withdrawal-orchestration` (loud-fail locks + 90-day sweep), `payout-fee-waiver-math` (three-term composition), `fx-conversion-dual-path` (cached vs quoted tolerance classes), `stripe-v2-preview-client` (schema-typed preview APIs), `payout-owner-nudges` (liability-aware treasury sweep).
- **Payout read & export plane** — `payout-list-read-path` (tenant alias + NOT-IN groups + uniform projection), `payout-count-eligibility` (shared-predicate badges + zero-fill), `payout-detail-projection` (parse-as-whitelist), `payout-count-detail-read` (tabs + detail together), `payout-export-split` (202 threshold + budget-trimmed worker), `payout-reminder-cadence` (predicate-level 3-day nudge cadence), `payout-owner-reminders` (month-end weekday owner digest with custom-minimum tiers).
- **Referral engine** — `referral-commission-engine` (trigger taxonomy + synthetic-invoiceId idempotency), `referral-attribute-loop-guard` (claim upsert + ancestor walk), `referral-queue-fanout` (status-gated granularity-matched jobs), `network-referral-bonus` (fee-share estimate as synthetic sale), `referral-backfill` (re-validating retro rewards).
- **Partner postbacks** — `partner-postback-dispatch` (selector→enricher→adapters, transform-before-sign, delivery callbacks), `partner-postback-lifecycle` (secret lifecycle, hostname receivers, scoped tests).
- **Importer framework** — `provider-import-continuation` (ceiling loop, cursor self-requeue, credential teardown, action chaining, fuzzy transition dedup).
- **Cron & queue kernel** — `cron-dual-auth` (method-keyed verifiers, dev bypass, bypass-secret split), `side-effect-walker` (skip:1 cursor requeue), `jobs-outbox-deepening` (publish-first outbox), `batch-email-idempotency` (dual-layer keys).
- **Fraud rule engine & commission holds** — `fraud-detection-entry-gates` (silent-skip funnel + per-rule isolation catch), `fraud-rule-registry-stubs` (total enum registry, partner-level stubs), `fraud-rule-config-merge` (opt-out override merge), `fraud-event-hash-identity` (per-type identity hashing + metadata sanitization), `fraud-event-group-continuity` (one-pending-group-per-composite-key choreography), `fraud-rule-email-match-tiers` (exact→domain-firewall→historical ladder), `fraud-rule-click-signals` (paid-traffic param fingerprint + referral minimatch gate), `fraud-rule-disposable-domain` (fail-open Redis set probe), `fraud-partner-application-risks` (state-backed + pure-profile risk matrix with severity ranks), `fraud-duplicate-account-detectors` (Veriff/payout-method directed-pair sweeps), `fraud-commission-hold-plane` (plan-gated re-verified bulk holds incl. payout detach+retally), `fraud-hold-release-ladder` (partner-veto / customer-block release model + four-fan-out), `fraud-group-resolution-expiry` (resolve action + 30-day sweeper, immortal bans), `fraud-network-ban-propagation` (cross-program opt-out alerts), `fraud-conversion-hold-integration` (detect-then-hold inside commission creation), `fraud-schema-and-test-harness` (Prisma shape + poll-based E2E helper).
- **Rich-text messaging plane** — `richtext-provider-feature-gates` (features array → conditional extensions; always-off StarterKit link), `richtext-mention-variable-fallback` (template variables with `{{id | fallback}}` round-trip), `richtext-safe-link-normalize-ladder` (normalize→verify scheme allowlist), `richtext-link-modal-selection-replay` (frozen range + replay-before-mutate), `richtext-link-click-to-edit` (handleClick consume + extendMarkRange), `richtext-link-hover-tooltip` (delegated DOM events over PM-owned anchors), `richtext-file-handler-paste-guard` (htmlContent escape hatch + channel-specific insertion points), `richtext-image-alt-node-view` (contentEditable={false} control islands), `richtext-campaign-image-link-plane` (parse-sanitize/render-strip/modifier-click triple boundary), `richtext-toolbar-image-link-branch` (prompt-around-blur nodePos capture), `richtext-suggestion-menu-lifecycle` (ReactRenderer remove-then-destroy), `richtext-provider-consumer-wiring` (markdown chat vs JSON campaign off one engine), `messages-panel-group-compaction` (sender-group negative margin), `callout-variant-table` (five-variant notice shape), `message-markdown-link-tooltip` (read-plane component-wrap dual).

- **Group-move & deletion plane** — `group-move-core-choreography` (enrollment-snapshot rebind + denormalized five-column updateMany + single waitUntil fan-out), `group-move-rule-overlap-lattice` (partnerGroup identity short-circuit + shared-metric interval conjunction keeps at most one matcher per values-set), `group-move-rules-upsert-tri-state` ([]=delete / absent=no-op / provided=create-or-update + conflict sweep), `group-delete-drain-and-reward-gc` (page-drain to default group + commission-gated rewards + deferred discount handoff), `group-move-default-link-remap` (normalizeUrl consume-on-match link mapping, SetNull-aware deleted-group mode), `group-move-discount-code-repartition` (equivalence repartition + provider-error fail-open provisioning + zero-code old-discount GC), `group-move-rule-scrub-on-delete` (JSON-diff scrub where an emptied in[] drops the condition).
- **Reward arithmetic kernel** — `reward-selection-and-context` (event-column map + computed-stats-wins context merge), `reward-modifier-override-merge` (null-clearing amount overlay; three absence semantics), `reward-per-product-fanout` (products∧productId-modifier dual gate), `reward-condition-group-evaluation` (undefined short-circuit → every/some → highest-amount wins), `reward-condition-entity-matrix` (complementary per-entity attribute/metadata maps), `reward-condition-operator-ladder` (12 fail-closed operators incl. not_in empty-array asymmetry), `reward-metadata-coercion` (operator-class typing of loose JSON), `reward-amount-kernels` (flat/percentage pick + Decimal→Number + bigint cents bridge), `reward-admission-validation` (event×type×amount legality gates + derived referral triggers), `reward-version-ledger` (Redis INCR staleness keys, strict `<`), `reward-sweep-job-envelope` (version mint-vs-passthrough, messageId-or-throw), `reward-sweep-cron-walker` (three-arm switch + count-gated notify + 300/batch cursor + P2025-swallow), `reward-surface-helpers` (stable event-order sort with MAX_SAFE_INTEGER demotion).
- **PayPal webhook receiver & connect** — `paypal-webhook-signature-plane` (cert-host allowlist, cache-aside cert fetch, CRC32 message grammar), `paypal-webhook-event-funnel` (9-event set → two handlers; unknown ACK 200), `paypal-payout-success-closure` (invoice-id retry-suffix split, completed idempotency, paidAt preserve, chunk-250 commission flips), `paypal-payout-failure-mapping` (5-of-8 failed map, write-through transferId, email-only-on-failed), `paypal-oauth-callback-connect` (verified-email gate, recompute-before-write, P2002 error param). Completes the receiver half that pass-6's payout-paypal-batch recorded as unmined.
- **Workflow move-group executor** — `workflow-move-group-executor` (fresh-read guards + opt-out honor + NX ex:10 try/finally lock feeding movePartnersToGroup single-partner form; executor-side twin of pass-5's route-side guard ladder).
- **Analytics read plane** — `analytics-alltime-fastpath` (guarded MySQL SUM bypass), `analytics-pipe-router` (three-way v4 pipe map + dual filter channels), `analytics-filter-wire-format` (ParsedFilter negation/comma algebra + closed field whitelist), `analytics-granularity-window` (start>interval precedence + silent swap + width-driven buckets), `analytics-top-hydration` (drop-on-miss Prisma hydration + singular rename), `analytics-metadata-query-dsl` (fail-closed user-typed filter grammar), `embedded-analytics-hostname-gate` (opt-in origin allowlist on track ingest).
- **Event feed & export plane** — `analytics-event-feed-funnel` (v4_events funnel + drop-link/placeholder-customer hydration), `analytics-export-zip-orchestrator` (sequential skip-set endpoint walker), `csv-formula-neutralization` (serializer-level formula-injection guard), `folder-rbac-scope-selection` (counter short-circuit + sentinel folder scope), `partner-earnings-timeseries` (enrollment-scoped Commission zero-fill join), `analytics-wire-schema` (parse-time ParsedFilter transforms + pipe-param twin), `analytics-response-whitelists` (endpoint-keyed exit-gate shapes).
- **Export defer & partner-facing analytics surface** — `threshold-export-defer` (count-probe inline-or-202 + worker-side authorization re-derivation + full-page generator paging), `partner-enrollment-filter-materialization` (membership-validated NOT IN→IN materialization over trusted enrollment ids, bounded enumeration with coarse partnerId fallback), `partner-privacy-projection` (post-auth ip strip + flag-gated email obfuscation enforced by strict parse), `public-analytics-dashboard-gate` (per-row dashboard boolean as sole public authz + 60s whole-param Redis cache), `client-earnings-timeseries-plane` (SQL bucket fold + explicit-window zero-fill + SWR keepPreviousData pairing), `first-conversion-heuristic` (counter-based first-sale predicate shared by all 13 commission-minting callers).
- **Export worker twins & partner-profile read plane** — `events-export-worker-plane` (worker-side authorization re-derivation at two depths + privacy projection inside the CSV loop), `partner-customer-read-surface` (LARGE_PROGRAM gate + attribution-scoped existence oracle + firstSaleAt relation fold), `partner-referral-read-surface` (referredByPartnerId list + separate groupBy earnings Map with ?? 0 fill), `partner-earnings-list-kernel` (auth-free kernel, sharing flag threaded per caller), `program-export-worker-twins` (program-scoped no-budget-cap paging vs payouts budget trim), `workspace-links-read-demotions` (size-driven sort/search demotions + count second-chance folder materialization).
- **Workspace customers plane & count grammar** — `customer-count-groupby-self-exclusion` (grouped COUNT drops its own grouping dimension; builder-extracted form of the platform count grammar), `count-badge-groupby-grammar` (gate → self-excluding where → three-arm switch; five deliberate divergence axes: pivot / hydration tolerance / privacy / response shape / bounding), `customer-identity-ext-prefix-loader` (prefix-dispatched dual-key addressing over a compound tenant-scoped unique), `race-safe-get-or-create-funnel` (find→create→catch-P2002→re-find identity funnel for every conversion rail), `workspace-customers-export-defer-twin` (threshold-defer publisher + payouts-style 100k row-cap worker hybrid), `customer-avatar-compensating-media` (mint-URL-then-upload-after-response with null-column compensation and success-ordered blob GC), `partner-program-sibling-sweep-verdicts` (thin-vs-hidden-invariant sweep: triple-scoped oracle, in-transaction last-owner guard, self-removal permission bypass).
- **Customers list kernel & bounties plane** — `customers-list-cursor-identity` (three-member cursor-identity family with uniform missing-vs-foreign 422 + scope-column adaptation; stale offset-mode comment trap), `partner-bounties-visibility-plane` (see-vs-submit predicate split over one eligibility core; empty-means-open audience grammar; per-partner relative windows; SQL+JS double enforcement), `bounty-social-metrics-sync-plane` (asymmetric negative/positive scrape cache + single-writer draft→submitted transition + exhaust-before-stamp sync bookkeeping), `at-search-ladder-grammar` ("@" ⇒ exact email else sanitized full-text; raw-input test before sanitizing; 9-site duplication census).
- **Bounty write/admin plane** — `bounty-submission-status-machine` (four-state one-way review ladder: drafts unreviewable, approved terminal both ways, rejected reopenable; guard-in-WHERE idempotent commission link-back; DB unique key as last word on per-period dedupe), `draft-submission-upsert-cron` (pure planner + race-guarded writer; clamp-at-threshold storage; was-expired-and-now-active reopen predicate), `bounty-creation-validation-plane` (mode-dependent timing gates; type-conditional zeroing; state-escalating immutability; count-gated delete refusal).
- **Bounty file-upload & notification plane** — `bounty-submission-upload-admission` (seven-gate mint ladder with the rate limit BEFORE the DB lookup so abuse self-throttles per (bounty, partner); admitted Content-Length/Content-Type bound into the presigned signature; not_found-for-ineligible), `bounty-upload-submit-path-pairing` (issuer and validator each re-derive the same actor-scoped path template from their own context; prefix+origin check, nanoid carries no security), `bounty-notify-partners-cron` (QStash self-requeueing 100-row id-cursor walker; per-batch Resend idempotency keys; 200-on-terminal-state; success-gated notificationEmail ledger; 2s/30s delay ladder).
- **Customer Stripe integration surface** — `customer-stripe-invoice-walker` (per-workspace mode resolution over the installed-integration settings row with a per-mode secret map; connected-account header on every call; best-effort charges read degrades refund detection instead of failing; refund truth derived per read from the payments expand × charges join; resource_missing translated to an actionable 400; program-scoped invoiceId→commission ledger join; single-page 100-invoice horizon), `stripe-customer-search-bridge` (provider-directory search joined to local rows as a nullable dubCustomerId — absence is a value, not an oracle; null-safe projection defaults + strict boundary parse).
- **Partner-network discovery plane** — `network-partner-ranking-kernel` (subquery-first funnel cutting ~1.5M→~5k candidates before the join fan-out, with the discoverable-partner filter re-applied inside every aggregate subquery via one alias-parameterized helper; finalScore = profile 500 + trusted 200 + similarity 0-50 + program-match 0-15 so the two dominance bonuses always outrank performance; p.id ASC deterministic tiebreak; MySQL JSON_ARRAYAGG hydration repaired back to Prisma types), `network-status-facet-grammar` (one lifecycle grammar across three dialects — composable listing parts for list+count routes, const statusWheres record where absence of a DiscoveredPartner row is a valid discover state, skip-undefined parallel facet counts, advertised-but-unimplemented groupBy arms throwing raw Error, invitedAt∨messagedAt billing-window invite meter re-enforced by both consuming actions), `marketplace-program-selection` (one cache()-wrapped fetch + in-memory selection passes — featured shuffle, popularity with invoice-total tiebreak, recency, two-pass category backfill — all under one shared usedIds set; count twin drops each facet filter for its own groupBy dimension).

### Pass 10 additions (reward arithmetic kernel · PayPal webhook receiver & OAuth connect · move-group executor)
- `./reward-selection-and-context.md` — event→reward-column mapping, missing-reward early null, computed-stats-wins context merge, bigint-safe cents normalization.
- `./reward-modifier-override-merge.md` — matched-condition overlay with three absence semantics: falsy-fallback type, explicit-null-clearing amounts, undefined-preserving maxDuration.
- `./reward-per-product-fanout.md` — products AND productId-modifier dual gate for Stripe line-item rewards; null-filtered ProductReward collection.
- `./reward-condition-group-evaluation.md` — undefined-field short-circuit before every/some operator logic; winner chosen by computed amount descending, not declaration order.
- `./reward-condition-entity-matrix.md` — complementary entity maps: partner/customer scalars-only, lead metadata-only, sale both; whitespace metadataField fails closed.
- `./reward-condition-operator-ladder.md` — twelve operators, fail-closed type guards, not_in empty-array asymmetry, strict-equality typing.
- `./reward-metadata-coercion.md` — operator-class-driven normalization of loose JSON metadata (string patterns / parsed numerics / condition-value-typed equality).
- `./reward-amount-kernels.md` — getRewardAmount flat-vs-percentage pick, serializeReward's Decimal→Number boundary (server-only), toCentsNumber bigint bridge.
- `./reward-admission-validation.md` — four-section create/update gate with verbatim messages and config-derived referral trigger allowlists.
- `./reward-version-ledger.md` — Redis INCR+sliding-EXPIRE version keys per (group,event); strict-less-than staleness; missing key = fresh.
- `./reward-sweep-job-envelope.md` — rewardJobSchema shape, first-call mints version / recursion passes it through, messageId-or-throw publish contract.
- `./reward-sweep-cron-walker.md` — created/updated/deleted three-arm switch, count-gated vs unconditional notification split, id-cursor 300/batch chaining, P2025-as-success hard delete.
- `./reward-surface-helpers.md` — copy-then-stable-sort with MAX_SAFE_INTEGER demotion for unknown events; null-filtering group rewards triple.
- `./paypal-webhook-signature-plane.md` — cert-host allowlist as SSRF/trust boundary, Redis cache-aside cert fetch (7d), CRC32 pipe-joined message grammar over raw body.
- `./paypal-webhook-event-funnel.md` — 9-event set collapsing 8 non-success flavors into one handler; unknown events ACK 200 by design.
- `./paypal-payout-success-closure.md` — retry-suffix invoice-id split, completed idempotency gate, paidAt preservation, chunk-250 commission flips.
- `./paypal-payout-failure-mapping.md` — explicit 5-of-8 failed map; write-through transferId for ALL flavors; partner email only on real failures.
- `./paypal-oauth-callback-connect.md` — verified-email gate, recompute-before-write capability derivation, P2002 ⇒ account-already-in-use redirect param.
- `./workflow-move-group-executor.md` — fresh-read revalidation, opt-out honoring, NX ex:10 lock in try/finally around single-partner moves.

### Pass 11 additions (analytics read plane · Tinybird query funnel · ingest hostname gate)
- `./analytics-alltime-fastpath.md` — five-clause guard (linkId + count + interval=all + no window + zero dimensional filters) lets a dashboard count SUM denormalized Link columns in MySQL instead of ClickHouse; composite events fold all four metrics; guard IS the contract.
- `./analytics-pipe-router.md` — three-way pipe map (v4_count/v4_timeseries vs v4_group_by_link_metadata for six metadata top_* endpoints vs v4_group_by) plus the dual filter-channel split: identity filters as top-level IN|NOT IN params applied at Tinybird's workspace_links node, event dims behind a closed 15-field whitelist in ONE JSON filters param.
- `./analytics-filter-wire-format.md` — ParsedFilter algebra {IS|IS_NOT|IS_ONE_OF|IS_NOT_ONE_OF, IN|NOT IN, values}: leading `-` negates the WHOLE comma list, empty input → undefined, string→ParsedFilter lift, qr→trigger compat fold, "US-CA" region split, closed SUPPORTED_FIELDS table.
- `./analytics-granularity-window.md` — custom start beats interval; interval=all degrades dataAvailableFrom → founding date; reversed ranges swap silently; ≤2d→hour, >180d→month else day; fixed presets hard-code their own granularity (24h hour … 1y/ytd/all month).
- `./analytics-top-hydration.md` — batched findMany({id in}) hydration where a missing MySQL row DROPS the warehouse row silently (drift tolerance ×6 guards); keys decoded per case-sensitive domains, puny-encoded shortLinks rebuilt; non-top endpoints rename groupByField via SINGULAR_ANALYTICS_ENDPOINTS.
- `./analytics-metadata-query-dsl.md` — never-throws parser for metadata['key.path']:value filters; [a-zA-Z0-9_.] key charset fail-closed; value token-stripping (; \ -- */ /*); unsupported operators degrade to equals; direct pure-unit test present.
- `./embedded-analytics-hostname-gate.md` — verifyAnalyticsAllowedHostnames gates the four /api/track/* INGEST routes: empty allowlist = allow-all (opt-in restriction), referer||origin www-stripped hostname, exact match first, *.dom matches ONLY proper subdomains.

### Pass 12 additions (event feed · export plane · wire schemas · partner earnings)
- `./analytics-event-feed-funnel.md` — getEvents: ONE v4_events pipe with per-eventType endpoint schemas + offset pagination; dual Prisma hydration maps where a missing link DROPS the row but a missing customer gets a "Deleted Customer" placeholder; MySQL-wins domain/key repair; per-event response whitelists.
- `./analytics-export-zip-orchestrator.md` — exportAnalyticsToZip: skip-set endpoint algebra over VALID_ANALYTICS_ENDPOINTS, strictly sequential getAnalytics walk (test pins call order), composite folding, empty-response skip (no file), formatRows hook, JSZip nodebuffer; pure-unit direct test.
- `./csv-formula-neutralization.md` — convertToCSV parseValue hook: null→"", Date→ISO, strings starting with =+-@ or tab/CR/LF apostrophe-prefixed at the SERIALIZER level so no export path can forget it.
- `./folder-rbac-scope-selection.md` — getFolderIdsToFilter tri-branch: foldersUsage===0→undefined (NOT []), plan-split user-scoped getFolders(1000) vs workspace findMany, both appending the "" unsorted sentinel that consumers strip last.
- `./partner-earnings-timeseries.md` — /api/partners/analytics: enrollment-scoped ParsedFilter link scoping, frozen earnings eligibility triple (pending/processed/paid + amount>0 + sale) in all three branches, raw-SQL CONVERT_TZ buckets zero-fill-joined onto warehouse rows, cross-workspace demoted to not_found.
- `./analytics-wire-schema.md` — analyticsQuerySchema transforms ~20 fields via parseFilterValue AT PARSE TIME (+capitalize device/browser/os, DUB_FOUNDING_DATE floor); deprecated folds (qr booleanQuerySchema→trigger, tagIds→tagId, order→sortOrder); analyticsFilterTB pipe twin = split(",") unions + 8 IN|NOT IN operator pairs + single JSON filters param for dims.
- `./analytics-response-whitelists.md` — endpoint-keyed zod record as THE exit gate: metric .default(0), centsSchemaWithDefault preprocess with codegen-introspectable inner default, geo drill-down as literal-* depth encoding, partner earnings via .extend(earningsSchema.shape).

### Pass 13 additions (export defer plane · public dashboards · partner projection)
- `./threshold-export-defer.md` — count-probe → inline CSV ≤1000 else 202 + QStash worker that RE-DERIVES authorization from ids; full-page-continuation async generator paging.
- `./partner-enrollment-filter-materialization.md` — shared partner-route prelude: membership-validate linkId against enrollment links, materialize NOT IN → explicit IN over trusted ids, bounded enumeration with coarse partnerId fallback.
- `./partner-privacy-projection.md` — post-auth per-row stripping (`ip`) + flag-gated email obfuscation with synthetic fillers, enforced by strict zod parse at the boundary.
- `./public-analytics-dashboard-gate.md` — unauthenticated analytics served solely on a per-row `dashboard` boolean; demo-link short-circuit; plan gates apply to anonymous traffic; 60s whole-param Redis cache.
- `./client-earnings-timeseries-plane.md` — SQL bucket fold + explicit-window zero-fill with pre-seeded groupBy keys; SWR keepPreviousData + 60s dedup pairing.
- `./first-conversion-heuristic.md` — counter-based two-line predicate (sales===0 OR cross-link ⇒ first) with the accepted false-positive documented in-code; single source of truth across all 13 commission-minting callers.
### Pass 14 additions (export worker twins · partner-profile read surfaces · links read demotions)
- `./events-export-worker-plane.md` — how does a deferred export worker re-derive authorization and enforce partner privacy OUTSIDE the HTTP boundary.
- `./partner-customer-read-surface.md` — how do you show a partner THEIR customers without leaking tenant data or subsidizing unbounded reads.
- `./partner-earnings-list-kernel.md` — how does one auth-free kernel serve both embed and partner-profile callers with different privacy flags.
- `./partner-referral-read-surface.md` — how do you list the partners I recruited, with their earned referral rewards, without N+1 queries or leaking emails.
- `./program-export-worker-twins.md` — how do partner/commission exports differ from the payout export that trims to a budget.
- `./workspace-links-read-demotions.md` — how does a list endpoint degrade sorting and search as the workspace grows past six and seven figures of links.

### Pass 15 additions (workspace customers plane · partner-profile sibling sweep · count grammar)
- `./count-badge-groupby-grammar.md` — what is the platform-wide shape of grouped-count endpoints, and where do the members diverge.
- `./customer-avatar-compensating-media.md` — how does a post-response blob upload keep the DB row honest when it fails.
- `./customer-count-groupby-self-exclusion.md` — why must a grouped COUNT drop the filter on the dimension it groups by.
- `./customer-identity-ext-prefix-loader.md` — how does one path parameter address two identity keys (row id vs external id) without ambiguity.
- `./partner-program-sibling-sweep-verdicts.md` — which enrollment-scoped read routes are truly thin, and what invariants hide in the ones that look thin.
- `./race-safe-get-or-create-funnel.md` — why does Prisma upsert still throw P2002 on MySQL, and what is the documented fallback.
- `./workspace-customers-export-defer-twin.md` — what does the THIRD member of the threshold-defer family do differently from links/events.

### Pass 16 additions (customers list kernel · partner bounties plane · @-search grammar)
- `./customers-list-cursor-identity.md` — how does a list endpoint validate a cursor, and which list kernels share the check (three-member family, uniform 422, scope-column adaptation).
- `./partner-bounties-visibility-plane.md` — how do you show a partner only the bounties they can act on, while keeping their past submissions visible.
- `./bounty-social-metrics-sync-plane.md` — how do you keep third-party social metrics fresh for submissions without hammering the scraper.
- `./at-search-ladder-grammar.md` — how do you implement dual-mode search where "@" means exact email, across a whole platform.

### Pass 17 additions (bounty write/admin plane · draft-upsert cron · count-route quirk)
- `./bounty-submission-status-machine.md` — how do you model a reviewable submission lifecycle where approval is the only money door.
- `./draft-submission-upsert-cron.md` — how do you machine-create "in progress" rows for lifetime performance goals, and re-open them after expiry.
- `./bounty-creation-validation-plane.md` — how do you admit a two-type incentive object whose timing fields mean different things per mode.

### Pass 18 additions (bounty file-upload plane · notify-partners cron)
- `./bounty-submission-upload-admission.md` — how do you issue presigned upload URLs that cannot be abused (pre-lookup self-throttling quota, signature-bound declared size/type, not_found-for-ineligible).
- `./bounty-upload-submit-path-pairing.md` — how do you keep a presigned-URL issuer and its submit-time validator honest with each other (one path template, dual derivation, prefix+origin check).
- `./bounty-notify-partners-cron.md` — how do you fan a new-bounty email campaign out over an unbounded audience without double-sends or provider abuse (per-batch idempotency keys, 200-on-terminal, success-gated ledger, delay ladder).

### Pass 19 additions (customer Stripe integration surface)
- `./customer-stripe-invoice-walker.md` — how do you read a connected Stripe Connect read plane into your own commission ledger without trusting the connection (per-workspace mode resolution, connected-account header, best-effort optional scopes, derived refund truth, program-scoped ledger join, single-page horizon).
- `./stripe-customer-search-bridge.md` — how do you search a provider's directory and join it to your own rows without an existence oracle (provider→local nullable-id join, null-safe projection defaults, strict boundary parse).

### Pass 20 additions (partner-network discovery plane)
- `./network-partner-ranking-kernel.md` — how do you rank 1.5M network partners for discovery without trusting per-program performance alone (subquery-first funnel with an alias-parameterized eligibility helper re-applied at every join; additive score with two hard dominance bonuses; deterministic id tiebreak; raw-SQL JSON hydration repair).
- `./network-status-facet-grammar.md` — how do you keep list, count-facet, and metering views of one lifecycle consistent across three query dialects (parts-split where grammar, skip-undefined facet counts, absence-of-record as a valid discover state, two-timestamp billing meter).
- `./marketplace-program-selection.md` — how do you build a storefront home page from one query without N+1 or duplicate rows (one fetch + in-memory selection passes under a shared used-id ledger, two-pass category backfill, groupBy-conditional facet filters, JS-side ordering incl. the featured shuffle).
## Extending the foundation
Add one `./<seam>.md` capsule per new porting question (e.g. Tinybird analytics query builders or the reward arithmetic cluster). Add one matching loader line and one grouped map entry; keep evidence in the capsule, not this leaf. The pass-7 fraud rule engine & commission holds group completed the last queued fraud target — remaining omissions are listed in Boundaries.

## Provenance
dub (AGPL-3.0-or-later; `apps/web/app/(ee)` dirs under a separate EE license) `main@e3a558d1cf5d7aa10c366fb631ff7bc4295d8192` (advanced from pass 1–3 pin `873edc5a` in drift pass 4; passes 5 AND 6 re-entered at the SAME pin — upstream zero-drift confirmed by fetch-first `git rev-list HEAD..origin/main --count` = 0 at pass-6 start). Codebase Memory project `dub` (ready; root `$REFERENCE_ROOT/platforms/dub`, 24,796 nodes / 125,570 edges; parse_partial = 6 files with single-line gaps in ee/analytics routes + one ui/support tsx; skipped = 0). All cited source/test paths — passes 1–4 (33 paths), pass 5's 17 more, and pass 6's 51 more (aggregate/process/split/update payout crons, charge-succeeded tree incl. queue-* + utils, send-stripe-payout, create-stripe-transfer/-stablecoin-payout, paypal quartet, tremendous sender/constants, recompute/get-partner-payout-methods/idempotency-key, reconcile-pair, referrals trio of lib files + queue/create/backfill crons, postback tree + callback route, tolt pair, confirm-payouts/force-withdrawal actions, export pair + generator, get-payouts/get-eligible/get-effective/eligibility-filter/payout-id-selection-where, constants/payouts, cutoff-period, queue-batch-email, jobs/index + queue/retry, with-cron/verify-qstash/enqueue-batch-jobs/qstash-workflow, stripe v2 client + fx-quote + fund/outbound/recipient helpers, reminders partners/program-owners, trigger-withdrawal, balance-available, payout-failed/paid, count/[payoutId]/export routes) — reported `no_recorded_issue` + `metadata_match` on stdin-JSON check_index_coverage (`generation_matches: true`). Pass 7 re-entered at the SAME pin (origin fetch rev-list HEAD..origin/main = 0 at run start) and added the fraud plane: lib/api/fraud/* (17 files whole-file incl. rules/), apps/web/prisma/schema/fraud.prisma, lib/zod/schemas/schemas.ts + partners.ts (INACTIVE_ENROLLMENT_STATUSES :43-47), lib/plan-capabilities.ts, lib/actions/fraud/{resolve-fraud-group,bulk-resolve-fraud-groups}.ts, cron routes release-hold/release-all/expired-fraud-groups, create-partner-commission route detection region (:540-680), tests/fraud/* + tests/utils/verify-fraud-event.ts — 10 decisive paths stdin-JSON coverage no_recorded_issue + metadata_match generation_matches=true. Pass-7 graph probes live-resolved line-exact for detectAndRecordFraudEvent :10-97, createFraudEvents :17-178, releaseHoldCommissions :35-237 + queueReleaseHoldCommissions :241-301, holdProcessedCommissions :16-170, detectDuplicateIdentityFraud :17-144, executeFraudRule :30-46, getMergedFraudRules :7-40, resolveFraudGroups :6-55, reportNetworkLevelBan :12-91; checkCustomerEmailMatch has no BM25 Function node (const object literal — resolved via search_code Module node + source read). Pass 8 re-entered at ADVANCED pin `main@29df217a29631ced4041882a28d2327cc4546f27` (+10 upstream commits: aug21-messaging/callout/rich-text-area waves touching ZERO lib/api|prisma files — fraud/engine capsules stand unchanged) after an in-place `index_repository --repo-path` re-registration created path-slugged TWIN `mnt-hdd-utopia-inspo-platforms-dub` (24,816n/125,689e, generation 2026-08-24T04:23Z, parse_partial still the same 6 files); adoption verified by content (normalizeLinkHref/RichTextLinkHoverTooltip resolve line-exact :18-24/:12-76 on the twin while short-name `dub` returns total:0) — pass-8 gates ran against and all Retrieve blocks cite THE TWIN; coverage stdin-JSON ×14 newly-cited paths no_recorded_issue + metadata_match, generation_matches=true. Pass 9 (frontier-platforms lane cron edc64ce02773 successor fire, 2026-08-24) re-entered at pin `29df217a29` UNCHANGED (origin fetch rev-list HEAD..origin/main = 0 at run start; graph twin mnt-hdd-utopia-inspo-platforms-dub ready 24,816n/125,689e head==base==pin) and fired standing target #2 by posing the reward-swap porting question over the group lifecycle: lib/api/groups/* whole-file (move-partners-to-group 204L, find-groups-with-matching-rules 192L, upsert-group-move-rules 115L, remove-group-id-from-move-rules 97L) + workflows/move-group/{schema,types}.ts + execute.ts cross-checked against pass-5 move-group-guard-ladder (deduped, not re-authored) + groups/[groupIdOrSlug]/route.ts 420L whole (PATCH/DELETE handlers) + cron/groups/remap-default-links route+utils + remap-discount-codes route -> 7 new capsule-v2 (133 -> 140).


Pass 10 (platforms rover lane, 2026-08-24) re-entered at pin `29df217a29` UNCHANGED (origin fetch rev-list HEAD..origin/main = 0 at run start; twin `mnt-hdd-utopia-inspo-platforms-dub` ready 24,816n/125,689e unchanged) and fired standing targets #3+#5: reward arithmetic cluster whole-file — lib/partners/{determine-partner-reward,evaluate-reward-conditions,sort-rewards-by-event-order,get-group-rewards-and-discount,get-reward-amount,aggregate-partner-links-stats}.ts, lib/api/rewards/* (validate-reward, queue-reward-processing, reward-condition-metadata, reward-version, reward-spend-limit-window), api/cron/rewards/process/route.ts, api/partners/serialize-reward.ts, lib/zod/schemas/rewards.ts operator tables (:270-320, REWARD_EVENT_COLUMN_MAPPING :515), lib/partner-referrals/constants.ts trigger config, tests/rewards/* (2,484L conditions suite + click/lead/sale) → 13 capsule-v2; PayPal webhook receiver & OAuth connect whole-tree — app/(ee)/api/paypal/{webhook/{route,verify-signature,payouts-item-succeeded,payouts-item-failed,utils},callback/route}.ts + lib/paypal/env.ts → 5 capsule-v2 (completing the receiver half pass-6 recorded as unmined); plus workflows/move-group/execute.ts executor-side twin → 1 capsule-v2 (140 -> 159). Graph battery pre-authoring 20/20 rank-1 line-exact on the twin; schema/const symbols (payoutsItemSchema, rewardJobSchema, PAYPAL_TO_DUB_STATUS, CERT_CACHE_KEY_PREFIX, REWARD_VERSION_TTL_SECONDS) BM25-invisible by construction (const/Variable class), documented in-capsule with grep probes. Coverage stdin-JSON ×22 newly-cited paths all no_recorded_issue + metadata_match generation_matches=true. Probe battery byte-exact ×2 passes ALL GREEN (expectations derived from live grep BEFORE writing). Runner stays offline-blocked (vitest needs PG+Redis+QStash cloud, standing since pass 4); direct-test anchors pinned by line (reward-conditions.test.ts :2288/:2313/:2340/:2370/:2411/:2453 etc.). CBM exact-build admission gate flapped mid-pass (cache-dir clash error under sibling load) AFTER the graph battery completed — adversarial wrong-project control re-run pending; prior passes established the twin-vs-short-name adjudication.

Pass 11 (miner-dub lane, 2026-08-25) ran at pin `29df217a2963` UNCHANGED against the repo's NEW canonical root `$REFERENCE_ROOT/dub` (relocated from platforms/dub; live Codebase Memory project renamed to `dub`, twin `mnt-hdd-utopia-inspo-platforms-dub` retired): ready, 24,816 nodes / 125,686 edges, generation 2026-08-24T14:04:54Z, mode FULL, skipped 0, parse_partial 6 single-line-gap files none under lib/analytics; check_index_coverage scope `apps/web/lib/analytics` → no_recorded_issue, generation_matches true. Fired the leaf's own anticipated "Tinybird analytics query builders" target: whole-file reads of get-analytics.ts (:1-401), filter-helpers.ts (:1-195), utils/get-start-end-dates.ts + get-interval-data.ts, metadata-query-parser.ts (:1-120), verify-analytics-allowed-hostnames.ts (:1-59), packages/utils/src/functions/parse-filter-value.ts (via snippet :25-55), constants.ts (:1-224), plus trace_path inbound on verifyAnalyticsAllowedHostnames (4 track routes) — 7 capsule-v2 outcomes (159 → 166 refs). Graph battery rank-1 observed pre-authoring; probe battery byte-exact executed live. Runner stays offline-blocked (no node_modules in checkout; integration suites additionally require PG+Redis+QStash cloud — standing since pass 4); direct-test anchors pinned by line (metadata-query-parser.test.ts :5-78, advanced-filter-helpers.test.ts :13-111, get-analytics.test.ts :8 CI-gated).

Pass 12 (miner-dub lane, 2026-08-25) re-entered at pin `29df217a2963` UNCHANGED (project `dub`, canonical root $REFERENCE_ROOT/dub; head_sha=base_sha == checkout git rev-parse HEAD; ready, 24,816 nodes / 125,686 edges, generation 2026-08-24T14:04:54Z, mode FULL, parse_partial 6 — ONE newly in scope, app/(ee)/api/partners/analytics/route.ts :126, flagged range read directly from source; skipped 0). Fired ALL SIX pass-11 NEXT-PASS TARGETS: whole-file reads of get-events.ts (:1-307) + tests/analytics/get-events.test.ts head (:1-120), export plane quartet (export-analytics-to-zip.ts :1-93 + PURE-UNIT test :1-177, events-export-helpers.ts :1-40, format-analytics-export.ts :1-29, convert-to-csv.ts :1-28), get-folder-ids-to-filter.ts (:1-44) + consumer validate-links-query-filters.ts (:1-68), partners/analytics/route.ts (:1-212) + types.ts :70-114 + tests/partners/analytics.test.ts :1-34, lib/zod/schemas/analytics.ts whole-file (:1-557), analytics-response.ts decisive ranges (:1-160, :245-294, :460-503) + schemas/utils.ts whole-file + schemas/misc.ts booleanQuerySchema + partners.ts :820-879 — 7 capsule-v2 outcomes (166 → 173 refs). Graph battery rank-1 observed pre-authoring; probe battery byte-exact executed live BEFORE authoring (one self-correction: operator pairs = 8, not 9). Runner stays offline-blocked (no node_modules; integration suites additionally require PG+Redis+QStash cloud — standing since pass 4); direct-test anchors pinned by line.

Pass 13 (miner-dub lane, 2026-08-25) re-entered at pin `29df217a2963` UNCHANGED (project `dub`, canonical root $REFERENCE_ROOT/dub; head_sha=base_sha == checkout git rev-parse HEAD; ready, 24,816 nodes / 125,686 edges, generation 2026-08-24T14:04:54Z, mode FULL; parse_partial 6 — none in pass-13 scope; skipped 0). Fired ALL SIX pass-12 NEXT-PASS TARGETS: links/events export threshold-defer pair (app/api/links/export/route.ts :1-96 whole, app/(ee)/api/events/export/route.ts :20-181, cron/export/links/route.ts :1-156 whole + fetch-links-batch.ts :1-28 whole), partner-profile analytics/export + events routes (:1-143 and :1-146 whole) with lib/constants/partner-profile.ts gate constants, public dashboard route (:1-187 whole) + its CI-gated test :1-33, client earnings plane (get-partner-earnings-timeseries.ts via snippet :10-137 + use-partner-earnings-timeseries.ts :1-54 whole), is-first-conversion.ts (:1-23 whole) with 13-caller inbound trace — 6 capsule-v2 outcomes (173 → 179 refs). Production authored by an earlier same-day invocation that died before durable-state delivery; THIS close-out run independently re-executed every capsule's graph retrieval, decisive source read, coverage check, and probe anchors before recording completion (one correction applied: fetchLinksBatch default pageSize is 1000, not 100). Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated); direct-test anchors pinned by line.

Pass 14 (miner-dub lane, 2026-08-26) re-entered at pin `29df217a2963` UNCHANGED (project `dub`, canonical root $REFERENCE_ROOT/dub; head_sha=base_sha == checkout git rev-parse HEAD; ready, 24,816 nodes / 125,686 edges, generation 2026-08-24T14:04:54Z, mode FULL; parse_partial 6 — none in pass-14 scope; skipped 0) and fired ALL FIVE pass-13 NEXT-PASS TARGETS: event export worker twins (cron/export/events/workspace :31-126 + partner :39-220 + fetch-events-batch.ts), partner-profile customers list/detail + get-customer-events (+TB twin), referrals route, get-earnings-for-partner kernel, links + links/count read demotions vs validate-links-query-filters/get-folder-ids-to-filter, cron/export/partners+commissions publishers/workers/generators — 6 capsule-v2 outcomes (179 → 185 refs); check_index_coverage ×20 newly-cited paths no_recorded_issue + metadata_match generation_matches true; deterministic GREEN×6 + RED adversarial ×2 battery. NOTE: the pass-14 SKILL.md wiring was only partially persisted (loader lines landed under the pass-13 heading; the grouped Capsule-map entry, provenance ¶, and full-view sentence were lost) — repaired structurally during the pass-15 close-out below.

Pass 15 (miner-dub lane, 2026-08-26) completed at pin `29df217a2963` UNCHANGED (git rev-parse HEAD == recorded head_sha from the pass-11..14 index_status runs; checkout clean except the two standing untracked daemon-log dirs). EVIDENCE PATH DEGRADED AND RECORDED: Codebase Memory MCP was NOT connected in this session, so graph search/trace/snippet/coverage were substituted by direct whole-file source reads of every cited path per the AGENTS.md fallback (customers POST/PATCH/DELETE routes, transform-customer.ts, lib/storage.ts isStored, partner-profile submitted-leads/activity-logs/users routes, workspace programs/[programId]/submitted-leads, referrals/customers/earnings count routes, workspace customers/count, customer-count-where.ts) plus an executed deterministic grep probe battery (all GREEN incl. negative probes: LARGE_PROGRAM_IDS absent from earnings/count; filter(Boolean) absent from earnings/count). A predecessor same-day invocation authored the pass-15 learning note + 4 capsules (customer-count-groupby-self-exclusion, workspace-customers-export-defer-twin, race-safe-get-or-create-funnel, customer-identity-ext-prefix-loader) and died before durable-state delivery; THIS run independently re-verified those four against source, then delivered the remaining 3 planned seams (customer-avatar-compensating-media, partner-program-sibling-sweep-verdicts, count-badge-groupby-grammar) → 7 capsule-v2 outcomes total (185 → 192 refs). Parity 192 loader == 192 disk bidirectional ∅∅, all v2; Capsule map repaired into proper grouped entries (pass-14 + pass-15 groups). Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated); no direct test exists for the mined plane (tests/**/*customer* = ∅; nearest anchor tests/tracks/track-lead.test.ts :80 idempotency pin, CI-gated).

Pass 16 (miner-dub lane, 2026-08-26) completed at pin `29df217a2963` UNCHANGED (git rev-parse HEAD == recorded head_sha; checkout clean except the two standing untracked daemon-log dirs). EVIDENCE PATH DEGRADED AND RECORDED: Codebase Memory MCP was NOT connected in this session, so graph search/trace/snippet/coverage were substituted by direct whole-file source reads of every cited path per the AGENTS.md fallback (get-customers.ts :1-115 whole, get-links-for-workspace.ts :41-75, get-commissions.ts :1-80, pagination.ts :1-89 whole, customers route GET :23-54 + count route :1-85 whole, schemas/customers.ts whole, misc.ts pagination schemas, partner-profile bounties list/detail/social-content-stats routes whole, bounty-availability.ts :1-277 whole, get-bounties-for-partner.ts :1-123 whole, get-bounty-or-throw.ts :1-38 whole, bounty-period.ts :1-140 whole, lib/bounty/utils.ts whole, social-content.ts :1-50, get-social-content.ts :1-248 decisive ranges, sync-social-metrics cron :1-238 whole, get-social-metrics-updates.ts :1-95 whole, program-enrollment-query.ts :1-40, prisma/index.ts sanitizer, upsert-draft test head) plus an executed deterministic grep probe battery (~30 anchors incl. negative probes: "Invalid cursor" census = exactly 3 family members all unprocessable_entity — REFUTING the pass-15 note's "links used not_found" claim; includes("@") census = exactly 9 search sites + 3 non-search; partnerId groupBy arm ABSENT from the customers count route). Fired pass-15 NEXT-PASS TARGETS #1 (customers list kernel), #3 (bounties plane, as two capsules), and #4 (@-search grammar); target #2 (Stripe surface) carried — its named-question condition unmet. 4 capsule-v2 outcomes (192 → 196 refs). Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated); no direct test exists for the mined plane (tests/bounties/ holds only the draft-upsert kernel suite, not the read surface).

Pass 17 (miner-dub lane, 2026-08-26) completed at pin `29df217a2963` UNCHANGED (git rev-parse HEAD == recorded head_sha; checkout clean except the two standing untracked daemon-log dirs). EVIDENCE PATH DEGRADED AND RECORDED: Codebase Memory MCP was NOT connected in this session, so graph search/trace/snippet/coverage were substituted by direct whole-file source reads of every cited path per the AGENTS.md fallback (bounties POST/GET route :1-384 whole, [bountyId] GET/PATCH/DELETE route :1-476 whole, submissions list/approve/reject routes whole, approve/reject/create-bounty-submission kernels whole, queue-partner-commission-creation.ts whole, create-partner-commission workflow set-bounty-commission step, upsert-draft-bounty-submissions.ts :1-124 whole + its PURE-UNIT test :1-275 whole (14 cases line-pinned), upsert-draft-submissions cron route :1-258 whole, trigger-draft-bounty-submissions.ts whole, validate-bounty.ts :1-131 whole, bounty-period.ts :1-140 whole, generate-performance-bounty-name.ts whole, performance-bounty-scope-attributes.ts whole, prisma/schema/bounty.prisma Bounty+BountySubmission models, schemas/bounties.ts decisive ranges, customers/count route :1-85 whole) plus an executed deterministic grep probe battery (~25 anchors incl. negative probes: partnerId groupBy arm ABSENT from the count route; commissionId:null guard in the link-back updateMany; status:"draft" race guard in the upsert update WHERE; AWARD_BOUNTY_OPERATORS = {gte} only). Fired pass-16 NEXT-PASS TARGETS #1 (submission status machine), #2 (draft-upsert cron — direct pure-unit test exists and is line-pinned), #3 (partnerId groupBy fallthrough quirk — CORRECTION to count-badge-groupby-grammar as a sixth divergence axis), and #5 (creation/validation plane); target #4 (Stripe surface) carried — its named-question condition unmet. 4 outcomes (3 new capsules + 1 correction; refs 196 → 199). Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated); the draft-upsert kernel's direct test is pure-unit and offline-blocked here, line-pinned.

Pass 19 (miner-dub lane, 2026-08-28) completed at pin `29df217a2963` UNCHANGED (git rev-parse HEAD == recorded head_sha; no re-index needed). RECOVERY FIRST: an external git operation had reverted the leaf to its pass-17 state — the three pass-18 reference files were absent from disk and SKILL.md carried 199 loader refs. The complete pass-18 state survived in the catalog repo's stash (`stash@{1}` SKILL.md + untracked-tree `stash@{1}^3` holding all three capsule files byte-identical to the pass-18 record); this run restored SKILL.md from the stash and re-wrote the three capsules, then RE-VERIFIED every pass-18 anchor against source at the pin before accepting the restore (ratelimit :79 < getBountyOrThrow :91 gate order; key template :141 == validateFiles expectedPath :420 modulo nanoid; allHeaders :132; idempotencyKey :189; delay ladder :212-215; publisher :326-329/:354-363). Pass-18 evidence chain re-executed in full; no content divergence found. NEW WORK — fired the four-times-carried Stripe target after a named porting question landed ("how do you read a user-connected payment account without trusting the connection"): direct whole-file source reads of app/(ee)/api/customers/[id]/stripe-invoices/route.ts :1-39, customers/search-stripe/route.ts :1-91, lib/api/customers/get-customer-stripe-invoices.ts :1-179, lib/stripe/index.ts :1-32, lib/integrations/stripe/schema.ts :1-24, lib/zod/schemas/customers.ts StripeCustomer/InvoiceSchema (:204-220), getDefaultProgramIdOrThrow :1-16, plus the consumer side create-manual-commissions.ts :100-175/:400-470/:470-560. Executed deterministic probe battery (~25 anchors incl. negative probes: no autoPaging/has_more/starting_after in the walker — single-page horizon; no test references either seam; exactly two callers of getCustomerStripeInvoices; dubCustomerId null-in-band never thrown). 2 new capsule-v2 outcomes (customer-stripe-invoice-walker, stripe-customer-search-bridge; refs 202 → 204). Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated). Parity 204==204 bidirectional ∅∅ all v2; validator P0=0 P1=0 for dub-foundation.

Pass 20 (miner-dub lane, 2026-08-28) completed at pin `29df217a2963` UNCHANGED (git rev-parse HEAD == recorded head_sha; checkout clean except the two standing untracked daemon dirs; no re-index needed). CLOSURE AUDIT pass: census of the repo's API/lib surface vs the 204-capsule map + omission ledger found ONE uncited reusable subsystem — the partner-network discovery plane — firing it instead of closing the repo. EVIDENCE PATH DEGRADED AND RECORDED: Codebase Memory MCP not connected this session → direct whole-file source reads of lib/api/network/calculate-partner-ranking.ts :1-381, lib/api/network/partner-network-listing-where.ts :1-64, app/(ee)/api/network/partners/route.ts :1-165, partners/count/route.ts :1-161, partners/invites-usage/route.ts :1-19, programs/route.ts :1-139, programs/count/route.ts :1-133, programs/summary/route.ts :1-10, lib/fetchers/get-marketplace-programs-summary.ts :1-231, lib/api/partners/get-network-invites-usage.ts :1-25, lib/zod/schemas/partner-network.ts :1-80, lib/constants/program.ts :7, prisma/schema/network.prisma DiscoveredPartner :42-61, plus caller census (invite-partner-from-network :29, message-partner :115). Executed deterministic probe battery (42 anchors incl. negative probes: exactly one caller of calculatePartnerRanking; zero tests reference any network seam; zero groupBy=platform/subscribers arms in the count route). 3 new capsule-v2 outcomes (network-partner-ranking-kernel, network-status-facet-grammar, marketplace-program-selection; refs 204 → 207). Bounty read serializers re-adjudicated a FIFTH time during the audit — still no independent invariant; omission stands. Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated).

Pass 18 (miner-dub lane, 2026-08-26) completed at pin `29df217a2963` UNCHANGED (git rev-parse HEAD == recorded head_sha; checkout clean except the two standing untracked daemon-log dirs). EVIDENCE PATH DEGRADED AND RECORDED: Codebase Memory MCP was NOT connected in this session, so graph search/trace/snippet/coverage were substituted by direct whole-file source reads of every cited path per the AGENTS.md fallback (get-bounty-submission-upload-url.ts :1-158 whole, upload-bounty-submission-file.ts :1-47 whole, embed/referrals/bounties/[bountyId]/upload/route.ts :1-48 whole, lib/storage.ts getSignedUploadUrl/getSignedUrl decisive ranges (:110-166), create-bounty-submission.ts validateFiles (:413-435) + pipeline order (:100), cron/bounties/notify-partners/route.ts :1-245 whole, bounties/route.ts publisher region (:320-384), packages/email/src/index.ts sendBatchEmail (:31-60) + send-via-resend.ts (:74-130), cron/utils.ts logAndRespond (:1-21), lib/upstash/ratelimit.ts :1-17, embed/referrals/auth.ts withReferralsEmbedToken (:33-110), ui/partners/bounties/claim-bounty-sheet.tsx handleUpload (:55-150), app.dub.co/embed/referrals/bounties/submission-fields.tsx handleUpload (:55-100)) plus an executed deterministic grep probe battery (~20 anchors incl. negative probes: no test references either seam; gate line-order census confirming ratelimit :79 precedes getBountyOrThrow :91; embed auth context excludes programPartnerTags; single publisher of the notify-partners URL). Fired pass-17 NEXT-PASS TARGETS #1 (file-upload plane — TWO capsules: admission kernel + issuer/validator path pairing) and #2 (notify-partners cron); target #3 (Stripe surface) carried — its named-question condition unmet (third carry). 3 capsule-v2 outcomes (refs 199 → 202). Runner stays offline-blocked (no node_modules; integration suites PG+Redis+QStash cloud-gated); no direct test exists for either mined seam (tests/bounties/ holds only the draft-upsert kernel suite).

## Full view (memory graph)
Pass 11 revalidation (project `dub`, root $REFERENCE_ROOT/dub): index_status verbose → branch main, head_sha/base_sha 29df217a29631ced4041882a28d2327cc4546f27 (matches checkout git rev-parse), nodes 24,816 / edges 125,686, status ready. Analytics-plane direct tests discovered via graph: tests/analytics/{get-analytics,get-analytics-advanced,advanced-filter-helpers,metadata-query-parser,get-events,export-analytics-to-zip,partner-analytics,public-analytics-dashboard}.test.ts (+ tests/partners/analytics.test.ts); all CI-gated/offline-blocked here. Pass 12 revalidation (same project/root/pin): index_status ready unchanged (24,816n/125,686e); check_index_coverage on all ten newly-cited paths no_recorded_issue EXCEPT app/(ee)/api/partners/analytics/route.ts = partial (parse_partial :126 — read directly); export-analytics-to-zip.test.ts identified as pure-unit (vi.mock, no CI gate) though still offline-blocked. Pass 13 revalidation (same project/root/pin): index_status ready unchanged (24,816n/125,686e); check_index_coverage on all 12 newly-cited paths no_recorded_issue + metadata_match, generation_matches true; public-analytics-dashboard.test.ts confirmed describe.runIf(env.CI)-gated with strict top_links parse + folderId assertion read directly. Pass 15 revalidation (2026-08-26): pin `29df217a29631ced4041882a28d2327cc4546f27` re-verified via checkout `git rev-parse HEAD` == recorded head_sha of the pass-11..14 index_status runs (Codebase Memory MCP not connected this session — graph coverage checks substituted by direct whole-file reads of every cited path; parse_partial set unchanged at 6 files, none in pass-15 scope); no direct tests exist for the workspace customers plane or the partner-profile count/sibling routes (tests/**/*customer* = ∅), so those capsules carry executed deterministic grep probes as the evidence class. Pass 16 revalidation (2026-08-26): pin `29df217a29631ced4041882a28d2327cc4546f27` re-verified via checkout `git rev-parse HEAD` == recorded head_sha (Codebase Memory MCP not connected this session — graph coverage checks substituted by direct whole-file reads of every cited path; parse_partial set unchanged at 6 files, none in pass-16 scope); no direct tests exist for the customers list/count routes or the partner-profile bounties read surface (tests/bounties/ contains only upsert-draft-bounty-submissions.test.ts pinning the draft-upsert kernel), so pass-16 capsules carry executed deterministic grep probes as the evidence class. Pass 17 revalidation (2026-08-26): pin `29df217a29631ced4041882a28d2327cc4546f27` re-verified via checkout `git rev-parse HEAD` == recorded head_sha (Codebase Memory MCP not connected this session — graph coverage checks substituted by direct whole-file reads of every cited path; parse_partial set unchanged at 6 files, none in pass-17 scope); the draft-upsert kernel HAS a direct pure-unit test (tests/bounties/upsert-draft-bounty-submissions.test.ts, 14 cases) which is offline-blocked here and line-pinned, while approve/reject/create kernels and validateBounty/resolveBountyTiming have no direct test (glob tests/**/*bounty* = only the upsert suite), so pass-17 capsules carry executed deterministic grep probes as the evidence class. Pass 18 revalidation (2026-08-26): pin `29df217a29631ced4041882a28d2327cc4546f27` re-verified via checkout `git rev-parse HEAD` == recorded head_sha (Codebase Memory MCP not connected this session — graph coverage checks substituted by direct whole-file reads of every cited path; parse_partial set unchanged at 6 files, none in pass-18 scope); no direct tests exist for the file-upload plane or the notify-partners cron (grep tests/ for both seam names = ∅), so pass-18 capsules carry executed deterministic grep probes as the evidence class. Pass 19 revalidation (2026-08-28): pin `29df217a29631ced4041882a28d2327cc4546f27` re-verified via checkout `git rev-parse HEAD` == recorded head_sha (Codebase Memory MCP not connected this session — graph coverage checks substituted by direct whole-file reads of every cited path; parse_partial set unchanged at 6 files, none in pass-19 scope); no direct tests exist for the Stripe customer surface (grep apps/web/tests/ for getCustomerStripeInvoices | search-stripe = ∅), so pass-19 capsules carry executed deterministic grep probes as the evidence class. Pass 20 revalidation (2026-08-28): pin `29df217a29631ced4041882a28d2327cc4546f27` re-verified via checkout `git rev-parse HEAD` == recorded head_sha (Codebase Memory MCP not connected this session — graph coverage checks substituted by direct whole-file reads of every cited path; parse_partial set unchanged at 6 files, none in pass-20 scope); no direct tests exist for the partner-network discovery plane (grep tests/ for calculatePartnerRanking | partnerNetworkListing | getMarketplaceProgramsSummary = ∅), so pass-20 capsules carry executed deterministic grep probes as the evidence class.
Revalidate `dub` before porting: run `index_status --project dub --verbose`, `check_index_coverage` (stdin JSON), `search_graph`, `trace_path`, and `get_code_snippet`. Root `$REFERENCE_ROOT/platforms/dub`, branch `main@e3a558d1`, 24,796 nodes / 125,570 edges (re-indexed 2026-08-23 pass 4 at the advanced HEAD after ff-merging origin/main; passes 1-3 ran at `873edc5a` = 24,197 nodes / 121,291 edges; passes 5-6 confirmed the SAME generation serves this HEAD — no stale-twin). Direct-test coverage: `processInBatches` (`tests/misc/process-in-batches.test.ts`), the webhook ENQUEUE side (`tests/webhooks/index.test.ts` — 12-trigger integration pinning envelope/callback params/body schemas), the link CRUD surface (`tests/links/create-link.test.ts` 20 feature tests, `create-link-error.test.ts` table-driven error envelopes, `update-link.test.ts` incl. UTM ladder :205-275, `delete-link.test.ts` incl. root-delete refusal :38, `list-links.test.ts` pagination pins :77-215); pass 5 added the workflow test plane: operator semantics (`tests/workflows/comparison-operators.test.ts`), move-group lifecycle (789L sequential suite), award-bounty flow (275L + verify helpers), merge-partner-accounts e2e (:34-61, :63-113, :128-156). Pass 7 added the fraud E2E plane: `tests/fraud/index.test.ts` (six rule-trigger flows — emailMatch exact/domain/historical tiers, suspiciousDomain, referralSourceBanned referer-header flow, paidTrafficDetected gclid URL flow) + `tests/fraud/fraud-groups.test.ts` (group list/filter-by-status/type/partner/groupId + schema parse) + `tests/utils/verify-fraud-event.ts` poll helper; suites need PG+Redis+QStash cloud (standing offline block since pass 4) so capsules carry deterministic grep probes alongside recorded test anchors. The pass-6 payouts/postbacks/importers plane is source-grounded with recorded no-test caveats in each capsule (runner requires PG+Redis+QStash cloud — standing block since pass 4). Graph fan-in evidence: `sendWebhooks` 57 inbound callers; `bulkDeleteLinks` 29; `executeWorkflows` invoked from ≥10 trigger sites; pass-6 symbols (processPayouts, createStripeTransfer, createStablecoinPayout, sendTremendousPayouts, recomputePartnerPayoutState, createReferralCommission, sendPartnerPostback, withRedisLock, calculatePayoutFeeWithWaiver, dispatchJobs, queueBatchEmail, importCommissions ×5 providers) each resolve ≥1 node line-exact via BM25 search_graph on project `dub`. Pass-8 symbols resolve on twin `mnt-hdd-utopia-inspo-platforms-dub`: normalizeLinkHref :18-24 rank#1, RichTextLinkHoverTooltip :12-76, useRichTextContext :379-388, LinkButton :142-214, RichTextToolbar :18-140, isSafeLinkHref urls.ts :20-32, suggestions/updatePosition variables.tsx :42-111/:20-40, configureCampaignEditorImage :132-138, ImageAltNodeView :11-136, Callout apps/web/ui/shared/callout.tsx :33-64, handleSave :48-82, MessagesPanel :29-310; PROSE_STYLES/RichTextProvider/RichTextLinkModalState/DEFAULT_RICH_TEXT_FEATURES resolve via name_pattern Variable/Interface rows (BM25 const-invisible); method-local openLinkModal/handleImageUpload graph-invisible by construction (grep fallback documented in capsules). Adversarial wrong-project retrieval on stale `dub` (normalizeLinkHref): total:0. No upstream vitest suite targets the rich-text-area package (standing PG+Redis+QStash runner block since pass 4) — pass-8 capsules carry deterministic grep probes as the evidence class. Pass 9 direct-test pins: `apps/web/tests/workflows/find-groups-with-matching-rules.test.ts` (pure unit suite, 4 cases: disjoint eq/in identity no-conflict, in-overlap conflict, one-sided partnerGroup falls through to metric overlap, ne/notIn unparseable falls through) plus the move-group E2E suite's rule-config cases (workflow created on configured rules / deleted on removed rules); vitest stays offline-blocked (PG+Redis+QStash cloud harness, standing since pass 4), so pass-9 capsules carry byte-exact grep probes derived pre-write.

## Boundaries
Adopt (pass 11) the analytics read plane as a SYSTEM: guarded denormalized fast path, three-way pipe routing with the identity-vs-event dual filter channels over a closed whitelist, the ParsedFilter negation algebra, precedence-driven window/granularity resolution, drop-on-miss hydration with strict response whitelists, the fail-closed metadata query DSL, and the opt-in origin allowlist guarding track ingest.
Adopt the job envelope + outbox-fallback dispatch, status-code-keyed retryability, raw-body signature gating, closed-code error mapping, cursor-first dual pagination with the offset cap, policy-table rate limiting, ownership-token locks, the two throughput utils, the webhook delivery lifecycle (fan-out results, callback failure ladder, receiver dialects, derived click-eligibility set), the redirect-edge ladder with its attribution/param/bot/AB companions, the three bulk-write contracts (order-preserving create, throw-vs-per-item update split, real-count delete transaction), the single-link write path (error-tuple validation gate, key admission ladder, post-commit propagation with compensating cache deletes, PATCH choreography with computed skip flags, hard-delete snapshot-then-eventual-cleanups, boolean archive) plus the read side (tenancy-flat list composition with unfiled-visible folder union, pre-query cursor identity checks, one-shape serializer, tombstone warehouse sync, fail-closed click ingestion). Adopt (pass 4+5) the durable workflow engine end-to-end: trigger dispatch funnel, validate-vs-evaluate posture split, config re-parse gate, attribute registry with fetch-cost declarations, the three action executors (move-group guard ladder, award-bounty accumulation gate, campaign dual-mode audience), merge internals (dynamic step names, unique-row arbitration, enrollment tx, payout fold, teardown ordering with raw-SQL finale), the partner-approved onboarding choreography, the commission eligibility ladder + hold gating, and identity-sync discipline. Adopt (pass 6) the partner-payouts plane as a SYSTEM: eligibility predicate + derived read modes, confirm-guard ladder, aggregation claim (raw-SQL guarded), invoice claim + fee waiver + FX + idempotent charge, settlement-gated rail router, per-rail senders (connect fold/source_transaction, stablecoin fee-netting, paypal cached-token batches, tremendous windowed gift orders), provider-id lifecycle webhooks, convergent amount reconciliation, force-withdrawal locks, reminder cadences, treasury sweep, and the referral engine (attribution loop guards, queue fan-outs, network bonus, backfill), plus partner postbacks (dispatch adapters, secret lifecycle), the five-provider importer continuation pattern, and the cron kernel pieces (dual-auth wrapper, side-effect walker, workflow trigger entry, deepened jobs outbox, batch-email idempotency). Adapt transports (QStash/Upstash → your queue/Redis), the ORM shapes (Prisma), Stripe/PayPal/Tremendous SDK specifics, policy values, constants, and error-code enums to host. Adopt (pass 7) the fraud rule engine & commission holds as a SYSTEM: silent-skip detection funnel with per-rule isolation, total enum registry with partner-level stubs, opt-out rule merge, per-type hash identity + metadata sanitization, one-pending-group continuity, the four conversion rules (email-match tiers with generic-mail firewall, click-signal pair, disposable-domain fail-open probe), application-risk matrix, duplicate-account directed-pair detectors, plan-gated re-verified commission holds (incl. payout detach+retally), two-tier release blocking with four-fan-out side effects, resolve/expiry lifecycle converging on one QStash queue with immortal cross-program bans, network ban propagation, conversion-time hold integration, and the schema/test harness. Omit dub's product domains beyond the mined seams (Stripe billing internals, email templates, Tinybird pipes beyond the mined ingest shapes, domain management `lib/domains`, workspace/auth surfaces, EE admin surfaces incl. fraud-alerts admin routes and fraud-risks UI tables, campaign render/interpolation plane, bounty/swag fulfillment, QR codegen, google-ads/appsflyer/singular integrations, customers/beehiiv scripts incl. its fraud-checks variant) unless a target needs them — each omitted seam has a recorded reason in the work record and remains a valid future capsule target. Adopt (pass 8) the rich-text messaging plane as a SYSTEM: feature-gated provider assembly over one TipTap engine, template variables with fallback-proof `{{id | fallback}}` serialization, the normalize-then-verify safe-link ladder, selection-replay deferred dialogs, click-to-edit + hover-preview duality across editor/read planes, guarded paste/drop ingestion with the htmlContent escape hatch, NodeView control islands (contentEditable={false}), linked-image parse-sanitize/render-strip/modifier-click triple boundary, prompt-around-blur toolbar branching, ReactRenderer suggestion lifecycle (remove-then-destroy), and the markdown-chat/JSON-campaign consumer wiring pair. Adopt (pass 9) the group-move & deletion plane as a system: enrollment-snapshot rebinding with denormalized five-column moves under one waitUntil fan-out, the three-gate rule-overlap lattice guaranteeing a single matcher, tri-state move-rule upserts, page-drain deletion with commission-gated reward GC and the deferred discount handoff, normalizeUrl consume-on-match default-link remapping with its SetNull-aware deleted-group mode, equivalence-partitioned discount-code migration with fail-open provider errors, and delete-time rule scrubbing whose emptied in[] drops the condition. Adopt (pass 10) the reward arithmetic kernel as a SYSTEM: event-column selection with computed-context enrichment, modifier overlays whose null-vs-undefined semantics clear or preserve base fields, per-product fan-out behind the products∧productId-modifier dual gate, group evaluation with undefined short-circuit and highest-amount arbitration, complementary entity matrices, fail-closed operator ladder, operator-class metadata coercion, cents/Decimal boundary kernels, four-section admission validation with config-derived referral triggers, Redis version ledger + version-passing job envelope + three-arm cron walker (count-gated vs unconditional notify, 300/batch cursor, P2025-swallowing delete), and the PayPal webhook receiver & OAuth connect completing the payout rail's callback half (cert-allowlist signature plane, 9-event funnel, success closure with retry-suffix id split, 5-of-8 failure mapping with email-only-on-failed, verified-email OAuth connect), plus the move-group workflow executor twin. Adopt (pass 13) the export-defer & partner-facing analytics surface as a SYSTEM: count-probe→inline-or-202 threshold split with raw-params QStash handoff, worker-side authorization re-derivation (RBAC recomputed from ids at execution time) with full-page-continuation generator paging, negate-then-materialize link filtering against the trusted enrollment set (membership checks read as scoped not_found, bounded enumeration falling back to coarse partnerId scope), post-auth privacy projection (ip strip + flag-gated email obfuscation + synthetic fillers behind a strict boundary parse), per-row opt-in `dashboard` boolean as complete public authz with plan gates and 60s whole-param Redis cache applied to anonymous traffic, the SQL-bucket zero-fill earnings timeseries consumed through keepPreviousData/60s-dedup SWR pairing, and the counter-based first-conversion predicate whose cross-link false positive is accepted in-code. Adopt (pass 14) the export worker twins & partner-profile read plane as a system: worker-side authorization re-derivation at two depths (shallow user/workspace re-fetch vs full enrollment materialization replay with per-batch privacy projection inside the CSV loop), program-scoped no-budget-cap paging twins vs the payouts budget trim, LARGE_PROGRAM-gated customer/referral read surfaces with attribution-scoped existence oracles, the auth-free earnings kernel with caller-threaded sharing flags, and size-driven links read demotions with count second-chance folder materialization. Adopt (pass 15) the workspace customers plane & count grammar as a system: program-pinned customer scope (query programId never honored workspace-side), the count-where builder's grouping-dimension self-exclusion as the extracted form of the platform-wide count-badge groupBy grammar (gate → self-excluding where → three-arm switch, with pivot / hydration-tolerance / privacy / response-shape / bounding as explicit per-endpoint decisions), prefix-dispatched ext_ dual-key identity loading over a compound tenant-scoped unique, the race-safe find→create→catch-P2002→re-find identity funnel for every conversion rail, the threshold-defer + 100k-row-cap export hybrid with honest capping logs, compensating avatar media writes (null-column compensation on upload failure, success-ordered old-blob GC, post-delete blob cleanup, serialization-side name null-repair), and the partner-program sibling sweep verdicts (triple-scoped existence oracle before log reads, in-transaction last-owner guard, self-removal permission bypass with guard retention, membership-timestamp-preserving merge order). Adopt (pass 16) the customers list kernel & bounties plane as a system: the three-member pre-query cursor-identity family (uniform missing-vs-foreign 422 with per-resource scope column; id-ordered skip:1 negative-take cursor branch vs [sortBy]-honoring offset branch — ignore the stale "order by id only" comment), opt-in expanded-field includes with route-boundary response-schema switching, the see-vs-submit bounty predicate split over one shared eligibility core (submissions keep a bounty visible past expiry; active-enrollment gate for submitting), the empty-means-open group-AND-tag audience grammar with enrollment-group fallback, per-partner relative windows anchored on enrollment createdAt with new-partner-only exclusion, SQL+JS double enforcement kept semantically identical, and double oracle suppression (cross-scope and ineligible both report not_found); plus the social-metrics sync plane (asymmetric scrape cache — 30-day negative TTL for 404s vs 1-hour positive, transient errors never cached — single-writer draft→submitted transition gated on stored minCount with completedAt in the same update, exhaust-before-stamp sync bookkeeping, per-partner inline rate gate on the on-demand probe) and the @-search ladder grammar ("@" ⇒ exact email tested on RAW input with the exact arm bypassing sanitization, else operator-char-stripped full-text over name fields) whose 9-site duplication is the anti-pattern to consolidate. Adopt (pass 17) the bounty write/admin plane as a system: the four-state one-way submission review ladder (drafts unreviewable "in progress", approved terminal for both kernels, rejected reopenable because no money moved yet; three-tier not_found oracle across program/bounty scopes; reviewer stamping reviewedAt+userId; approve clears rejection fields, reject severs commissionId), the queue-first reward wiring whose commission link-back is idempotent BY GUARD (updateMany WHERE status:"approved" AND commissionId:null under per-partner flowControl parallelism 1), the nine-step submission handler pipeline whose per-period uniqueness is backed by the DB unique key rather than JS, the pure-planner/race-guarded-writer draft-upsert split (clamp-at-threshold progress storage, auto-submit at the gte threshold, status:"draft" predicate IN the update WHERE so concurrent finalization wins, was-expired-and-now-active reopen with future-reschedule tolerance, QStash full-page self-requeue), and the mode-dependent creation/validation ladder (relative⇒startsAt-null vs absolute-defaults-now, endsAt⊕endsAfterDays, lifetime+relative forbidden, type-conditional field zeroing at the route, creation-only schema omission for type/performanceScope, soft-refusal of prize-rule changes once submissions exist, count-gated delete refusal with the archive alternative, pre-minted-id same-transaction workflow companion). Adopt (pass 18) the bounty file-upload & notification plane as a system: the seven-gate presigned-URL mint ladder whose rate limit sits BEFORE the DB lookup so abuse self-throttles per (bounty, partner), the admitted Content-Length/Content-Type bound into the presigned signature as second enforcement, not_found-for-ineligible oracle suppression, the issuer/validator dual-derivation path pairing (one actor-scoped template, prefix+origin check, nanoid carries no security), and the QStash self-requeueing notify-partners walker (per-batch Resend idempotency keys keyed by cursor position, 200-on-terminal-state, success-gated index-paired notificationEmail ledger, 2s/30s delay ladder, relative-start skip). Adopt (pass 19) the customer Stripe integration surface as a system: per-workspace mode resolution (installed-integration settings row → stripeMode default live → per-mode secret map) with the connected-account header on every call, best-effort degradation for optional scopes (charges.read missing ⇒ refund detection silently degrades, core read stands), derived refund truth (payments expand × charges page joined on payment_intent, computed per read rather than stored), resource_missing translated into an actionable 400 that names the stale stored pointer, the program-scoped invoiceId→commission ledger join, the manual-import consumer contract (importStripeInvoices XOR manual sale fields, invoiceId_programId uniqueness as the dedupe backstop, refunded status propagated onto the minted commission, ascending createdAt ordering with the first invoice anchoring the lead event), and the search-then-join directory bridge (provider rows enriched with a nullable workspace-scoped dubCustomerId; null-safe projection defaults behind a strict boundary parse). Adopt (pass 20) the partner-network discovery plane as a system: the subquery-first ranking funnel with an alias-parameterized eligibility helper re-applied at every aggregate join, the additive discovery score whose two dominance bonuses (profile 500, trusted 200) always outrank the 0-65 performance band, the deterministic id tiebreak, raw-SQL JSON hydration repair back to Prisma types, one lifecycle grammar served across three query dialects (composable listing parts, const statusWheres with absence-of-record as a valid discover state, skip-undefined facet counts, groupBy-conditional facet filters in the count twin), the invitedAt∨messagedAt billing-window invite meter re-enforced by both consuming actions, and the one-fetch marketplace selection kernel (shared used-id ledger, two-pass category backfill, JS-side ordering with the featured shuffle). Omit the bounty read serializers (get-bounty-with-details/transform-bounty) unless a target needs them — each omitted seam has a recorded reason in the work record and remains a valid future capsule target.

## Revise closure (2026-09-02)
The foundation-pack migration review marked this leaf revise for a checkout
beyond the recorded pin. Re-verified at the recorded advanced pin: checkout
git rev-parse HEAD == 29df217a29631ced4041882a28d2327cc4546f27 == recorded
head_sha, origin/main behind = 0, 207 refs on disk == 207 loader lines,
foundation-validator 0 fail. Disposition: retained at the exact recorded pin.

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`ai-usage-credit-reserve.md`](./ai-usage-credit-reserve.md)
- [`analytics-alltime-fastpath.md`](./analytics-alltime-fastpath.md)
- [`analytics-event-feed-funnel.md`](./analytics-event-feed-funnel.md)
- [`analytics-export-zip-orchestrator.md`](./analytics-export-zip-orchestrator.md)
- [`analytics-filter-wire-format.md`](./analytics-filter-wire-format.md)
- [`analytics-granularity-window.md`](./analytics-granularity-window.md)
- [`analytics-metadata-query-dsl.md`](./analytics-metadata-query-dsl.md)
- [`analytics-pipe-router.md`](./analytics-pipe-router.md)
- [`analytics-response-whitelists.md`](./analytics-response-whitelists.md)
- [`analytics-top-hydration.md`](./analytics-top-hydration.md)
- [`analytics-wire-schema.md`](./analytics-wire-schema.md)
- [`api-error-envelope.md`](./api-error-envelope.md)
- [`archive-flag-semantics.md`](./archive-flag-semantics.md)
- [`at-search-ladder-grammar.md`](./at-search-ladder-grammar.md)
- [`award-bounty-accumulation-gate.md`](./award-bounty-accumulation-gate.md)
- [`batch-email-idempotency.md`](./batch-email-idempotency.md)
- [`batched-processing.md`](./batched-processing.md)
- [`batched-row-transfer-ladder.md`](./batched-row-transfer-ladder.md)
- [`bounty-creation-validation-plane.md`](./bounty-creation-validation-plane.md)
- [`bounty-notify-partners-cron.md`](./bounty-notify-partners-cron.md)
- [`bounty-social-metrics-sync-plane.md`](./bounty-social-metrics-sync-plane.md)
- [`bounty-submission-status-machine.md`](./bounty-submission-status-machine.md)
- [`bounty-submission-upload-admission.md`](./bounty-submission-upload-admission.md)
- [`bounty-upload-submit-path-pairing.md`](./bounty-upload-submit-path-pairing.md)
- [`bulk-create-ordering.md`](./bulk-create-ordering.md)
- [`bulk-delete-counter-tx.md`](./bulk-delete-counter-tx.md)
- [`bulk-update-validation-split.md`](./bulk-update-validation-split.md)
- [`callout-variant-table.md`](./callout-variant-table.md)
- [`campaign-dual-mode-audience.md`](./campaign-dual-mode-audience.md)
- [`case-sensitive-key-codec.md`](./case-sensitive-key-codec.md)
- [`click-attribution-continuity.md`](./click-attribution-continuity.md)
- [`click-ingest-failclosed.md`](./click-ingest-failclosed.md)
- [`client-earnings-timeseries-plane.md`](./client-earnings-timeseries-plane.md)
- [`commission-eligibility-ladder.md`](./commission-eligibility-ladder.md)
- [`commission-hold-gating.md`](./commission-hold-gating.md)
- [`condition-validate-vs-evaluate.md`](./condition-validate-vs-evaluate.md)
- [`count-badge-groupby-grammar.md`](./count-badge-groupby-grammar.md)
- [`cron-dual-auth.md`](./cron-dual-auth.md)
- [`csv-formula-neutralization.md`](./csv-formula-neutralization.md)
- [`cursor-identity-validation.md`](./cursor-identity-validation.md)
- [`customer-avatar-compensating-media.md`](./customer-avatar-compensating-media.md)
- [`customer-count-groupby-self-exclusion.md`](./customer-count-groupby-self-exclusion.md)
- [`customer-identity-ext-prefix-loader.md`](./customer-identity-ext-prefix-loader.md)
- [`customer-stripe-invoice-walker.md`](./customer-stripe-invoice-walker.md)
- [`customers-list-cursor-identity.md`](./customers-list-cursor-identity.md)
- [`cutoff-period-splitting.md`](./cutoff-period-splitting.md)
- [`discount-code-deletion-funnel.md`](./discount-code-deletion-funnel.md)
- [`draft-submission-upsert-cron.md`](./draft-submission-upsert-cron.md)
- [`dual-pagination.md`](./dual-pagination.md)
- [`durable-workflow-step-contract.md`](./durable-workflow-step-contract.md)
- [`embedded-analytics-hostname-gate.md`](./embedded-analytics-hostname-gate.md)
- [`enrollment-merge-tx.md`](./enrollment-merge-tx.md)
- [`enrollment-window-resolution.md`](./enrollment-window-resolution.md)
- [`events-export-worker-plane.md`](./events-export-worker-plane.md)
- [`fetch-retry.md`](./fetch-retry.md)
- [`first-conversion-heuristic.md`](./first-conversion-heuristic.md)
- [`folder-rbac-scope-selection.md`](./folder-rbac-scope-selection.md)
- [`force-withdrawal-orchestration.md`](./force-withdrawal-orchestration.md)
- [`fraud-commission-hold-plane.md`](./fraud-commission-hold-plane.md)
- [`fraud-conversion-hold-integration.md`](./fraud-conversion-hold-integration.md)
- [`fraud-detection-entry-gates.md`](./fraud-detection-entry-gates.md)
- [`fraud-duplicate-account-detectors.md`](./fraud-duplicate-account-detectors.md)
- [`fraud-event-group-continuity.md`](./fraud-event-group-continuity.md)
- [`fraud-event-hash-identity.md`](./fraud-event-hash-identity.md)
- [`fraud-group-resolution-expiry.md`](./fraud-group-resolution-expiry.md)
- [`fraud-hold-release-ladder.md`](./fraud-hold-release-ladder.md)
- [`fraud-network-ban-propagation.md`](./fraud-network-ban-propagation.md)
- [`fraud-partner-application-risks.md`](./fraud-partner-application-risks.md)
- [`fraud-rule-click-signals.md`](./fraud-rule-click-signals.md)
- [`fraud-rule-config-merge.md`](./fraud-rule-config-merge.md)
- [`fraud-rule-disposable-domain.md`](./fraud-rule-disposable-domain.md)
- [`fraud-rule-email-match-tiers.md`](./fraud-rule-email-match-tiers.md)
- [`fraud-rule-registry-stubs.md`](./fraud-rule-registry-stubs.md)
- [`fraud-schema-and-test-harness.md`](./fraud-schema-and-test-harness.md)
- [`fx-conversion-dual-path.md`](./fx-conversion-dual-path.md)
- [`group-delete-drain-and-reward-gc.md`](./group-delete-drain-and-reward-gc.md)
- [`group-move-core-choreography.md`](./group-move-core-choreography.md)
- [`group-move-default-link-remap.md`](./group-move-default-link-remap.md)
- [`group-move-discount-code-repartition.md`](./group-move-discount-code-repartition.md)
- [`group-move-rule-overlap-lattice.md`](./group-move-rule-overlap-lattice.md)
- [`group-move-rule-scrub-on-delete.md`](./group-move-rule-scrub-on-delete.md)
- [`group-move-rules-upsert-tri-state.md`](./group-move-rules-upsert-tri-state.md)
- [`hard-delete-choreography.md`](./hard-delete-choreography.md)
- [`job-dispatch.md`](./job-dispatch.md)
- [`job-executor.md`](./job-executor.md)
- [`jobs-outbox-deepening.md`](./jobs-outbox-deepening.md)
- [`key-admission-ladder.md`](./key-admission-ladder.md)
- [`link-abtest-routing.md`](./link-abtest-routing.md)
- [`link-bot-detection.md`](./link-bot-detection.md)
- [`link-cache-tiering.md`](./link-cache-tiering.md)
- [`link-destination-assembly.md`](./link-destination-assembly.md)
- [`link-redirect-ladder.md`](./link-redirect-ladder.md)
- [`link-request-parsing.md`](./link-request-parsing.md)
- [`link-serializer-boundary.md`](./link-serializer-boundary.md)
- [`link-update-route-choreography.md`](./link-update-route-choreography.md)
- [`marketplace-program-selection.md`](./marketplace-program-selection.md)
- [`merge-plan-dynamic-steps.md`](./merge-plan-dynamic-steps.md)
- [`merge-teardown-ordering.md`](./merge-teardown-ordering.md)
- [`message-markdown-link-tooltip.md`](./message-markdown-link-tooltip.md)
- [`messages-panel-group-compaction.md`](./messages-panel-group-compaction.md)
- [`move-group-guard-ladder.md`](./move-group-guard-ladder.md)
- [`named-ratelimit-policies.md`](./named-ratelimit-policies.md)
- [`network-partner-ranking-kernel.md`](./network-partner-ranking-kernel.md)
- [`network-referral-bonus.md`](./network-referral-bonus.md)
- [`network-status-facet-grammar.md`](./network-status-facet-grammar.md)
- [`overlap-first-merge-plan.md`](./overlap-first-merge-plan.md)
- [`partner-approved-choreography.md`](./partner-approved-choreography.md)
- [`partner-bounties-visibility-plane.md`](./partner-bounties-visibility-plane.md)
- [`partner-customer-read-surface.md`](./partner-customer-read-surface.md)
- [`partner-earnings-list-kernel.md`](./partner-earnings-list-kernel.md)
- [`partner-earnings-timeseries.md`](./partner-earnings-timeseries.md)
- [`partner-enrollment-filter-materialization.md`](./partner-enrollment-filter-materialization.md)
- [`partner-identity-sync-discipline.md`](./partner-identity-sync-discipline.md)
- [`partner-link-stats-kernel.md`](./partner-link-stats-kernel.md)
- [`partner-postback-dispatch.md`](./partner-postback-dispatch.md)
- [`partner-postback-lifecycle.md`](./partner-postback-lifecycle.md)
- [`partner-privacy-projection.md`](./partner-privacy-projection.md)
- [`partner-program-sibling-sweep-verdicts.md`](./partner-program-sibling-sweep-verdicts.md)
- [`partner-referral-read-surface.md`](./partner-referral-read-surface.md)
- [`payout-aggregation-claim.md`](./payout-aggregation-claim.md)
- [`payout-amount-reconciliation.md`](./payout-amount-reconciliation.md)
- [`payout-charge-router.md`](./payout-charge-router.md)
- [`payout-confirm-guards.md`](./payout-confirm-guards.md)
- [`payout-connect-transfer.md`](./payout-connect-transfer.md)
- [`payout-count-detail-read.md`](./payout-count-detail-read.md)
- [`payout-count-eligibility.md`](./payout-count-eligibility.md)
- [`payout-detail-projection.md`](./payout-detail-projection.md)
- [`payout-export-split.md`](./payout-export-split.md)
- [`payout-fee-waiver-math.md`](./payout-fee-waiver-math.md)
- [`payout-fold-finalize-sync.md`](./payout-fold-finalize-sync.md)
- [`payout-lifecycle-webhooks.md`](./payout-lifecycle-webhooks.md)
- [`payout-list-read-path.md`](./payout-list-read-path.md)
- [`payout-method-recompute.md`](./payout-method-recompute.md)
- [`payout-methods-surface.md`](./payout-methods-surface.md)
- [`payout-mode-eligibility.md`](./payout-mode-eligibility.md)
- [`payout-owner-nudges.md`](./payout-owner-nudges.md)
- [`payout-owner-reminders.md`](./payout-owner-reminders.md)
- [`payout-paypal-batch.md`](./payout-paypal-batch.md)
- [`payout-processing-funnel.md`](./payout-processing-funnel.md)
- [`payout-reminder-cadence.md`](./payout-reminder-cadence.md)
- [`payout-stablecoin-ladder.md`](./payout-stablecoin-ladder.md)
- [`payout-status-machine.md`](./payout-status-machine.md)
- [`payout-tremendous-giftcards.md`](./payout-tremendous-giftcards.md)
- [`paypal-oauth-callback-connect.md`](./paypal-oauth-callback-connect.md)
- [`paypal-payout-failure-mapping.md`](./paypal-payout-failure-mapping.md)
- [`paypal-payout-success-closure.md`](./paypal-payout-success-closure.md)
- [`paypal-webhook-event-funnel.md`](./paypal-webhook-event-funnel.md)
- [`paypal-webhook-signature-plane.md`](./paypal-webhook-signature-plane.md)
- [`policy-rate-limit.md`](./policy-rate-limit.md)
- [`process-link-validation-gate.md`](./process-link-validation-gate.md)
- [`program-export-worker-twins.md`](./program-export-worker-twins.md)
- [`provider-import-continuation.md`](./provider-import-continuation.md)
- [`public-analytics-dashboard-gate.md`](./public-analytics-dashboard-gate.md)
- [`race-safe-get-or-create-funnel.md`](./race-safe-get-or-create-funnel.md)
- [`redis-lock.md`](./redis-lock.md)
- [`referral-attribute-loop-guard.md`](./referral-attribute-loop-guard.md)
- [`referral-backfill.md`](./referral-backfill.md)
- [`referral-commission-engine.md`](./referral-commission-engine.md)
- [`referral-queue-fanout.md`](./referral-queue-fanout.md)
- [`reward-admission-validation.md`](./reward-admission-validation.md)
- [`reward-amount-kernels.md`](./reward-amount-kernels.md)
- [`reward-condition-entity-matrix.md`](./reward-condition-entity-matrix.md)
- [`reward-condition-group-evaluation.md`](./reward-condition-group-evaluation.md)
- [`reward-condition-operator-ladder.md`](./reward-condition-operator-ladder.md)
- [`reward-metadata-coercion.md`](./reward-metadata-coercion.md)
- [`reward-modifier-override-merge.md`](./reward-modifier-override-merge.md)
- [`reward-per-product-fanout.md`](./reward-per-product-fanout.md)
- [`reward-selection-and-context.md`](./reward-selection-and-context.md)
- [`reward-surface-helpers.md`](./reward-surface-helpers.md)
- [`reward-sweep-cron-walker.md`](./reward-sweep-cron-walker.md)
- [`reward-sweep-job-envelope.md`](./reward-sweep-job-envelope.md)
- [`reward-version-ledger.md`](./reward-version-ledger.md)
- [`richtext-campaign-image-link-plane.md`](./richtext-campaign-image-link-plane.md)
- [`richtext-file-handler-paste-guard.md`](./richtext-file-handler-paste-guard.md)
- [`richtext-image-alt-node-view.md`](./richtext-image-alt-node-view.md)
- [`richtext-link-click-to-edit.md`](./richtext-link-click-to-edit.md)
- [`richtext-link-hover-tooltip.md`](./richtext-link-hover-tooltip.md)
- [`richtext-link-modal-selection-replay.md`](./richtext-link-modal-selection-replay.md)
- [`richtext-mention-variable-fallback.md`](./richtext-mention-variable-fallback.md)
- [`richtext-provider-consumer-wiring.md`](./richtext-provider-consumer-wiring.md)
- [`richtext-provider-feature-gates.md`](./richtext-provider-feature-gates.md)
- [`richtext-safe-link-normalize-ladder.md`](./richtext-safe-link-normalize-ladder.md)
- [`richtext-suggestion-menu-lifecycle.md`](./richtext-suggestion-menu-lifecycle.md)
- [`richtext-toolbar-image-link-branch.md`](./richtext-toolbar-image-link-branch.md)
- [`side-effect-walker.md`](./side-effect-walker.md)
- [`single-row-write-propagation.md`](./single-row-write-propagation.md)
- [`stripe-customer-search-bridge.md`](./stripe-customer-search-bridge.md)
- [`stripe-v2-preview-client.md`](./stripe-v2-preview-client.md)
- [`threshold-export-defer.md`](./threshold-export-defer.md)
- [`tombstone-metadata-ingest.md`](./tombstone-metadata-ingest.md)
- [`unique-row-merge-arbitration.md`](./unique-row-merge-arbitration.md)
- [`webhook-click-fastpath.md`](./webhook-click-fastpath.md)
- [`webhook-delivery-callback.md`](./webhook-delivery-callback.md)
- [`webhook-dispatch-fanout.md`](./webhook-dispatch-fanout.md)
- [`webhook-event-envelope.md`](./webhook-event-envelope.md)
- [`webhook-receiver-dialects.md`](./webhook-receiver-dialects.md)
- [`webhook-signature.md`](./webhook-signature.md)
- [`workflow-attribute-data-requirements.md`](./workflow-attribute-data-requirements.md)
- [`workflow-attribute-registry.md`](./workflow-attribute-registry.md)
- [`workflow-config-parse-gate.md`](./workflow-config-parse-gate.md)
- [`workflow-move-group-executor.md`](./workflow-move-group-executor.md)
- [`workflow-operator-table.md`](./workflow-operator-table.md)
- [`workflow-trigger-dispatch.md`](./workflow-trigger-dispatch.md)
- [`workflow-trigger-entry.md`](./workflow-trigger-entry.md)
- [`workspace-customers-export-defer-twin.md`](./workspace-customers-export-defer-twin.md)
- [`workspace-links-read-demotions.md`](./workspace-links-read-demotions.md)
- [`workspace-list-composition.md`](./workspace-list-composition.md)
