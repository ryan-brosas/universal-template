<!-- capsule-v2 -->
**Source:** dub `findGroupsWithMatchingRules` @ pin `29df217a29`
**Question:** How do you guarantee two groups' auto-move rules can never both fire for the same partner?
**Path/Symbol:** `apps/web/lib/api/groups/find-groups-with-matching-rules.ts` — `findGroupsWithMatchingRules({groups, currentRules, currentGroupId})`; helpers `doRuleSetsOverlap`, `doPartnerGroupConditionsOverlap`, `partnerGroupConditionToIdSet`, `conditionToInterval`, `doConditionsOverlap`
**Signature:** returns `Array<{id, name}>` of conflicting groups; rules are `WorkflowCondition[]` with attribute ∈ {totalLeads, totalConversions, totalSaleAmount, totalCommissions, partnerGroup}.
**Data Shape:** rule set = flat condition array; overlap check builds per-attribute Maps (LAST duplicate wins on `Map.set`).
**Decisive source:** :35-36 states the contract: "Two rule sets conflict if there exists ANY set of attribute values that would satisfy both simultaneously. This ensures that for any given set of attribute values, at most one group rule set will match." The three-gate ladder: (1) partnerGroup eq/in sets from BOTH rules, when both parse to id Sets, DISJOINT ⇒ immediate false :54-64; (2) metric attributes shared between the two sets — none ⇒ false ("one checks conversions, the other checks leads — they're independent" :71-72); (3) EVERY shared metric attribute must interval-overlap (`gte→[v,+∞)`, `between→{min,max}`, closed-interval test `i1.min <= i2.max && i2.min <= i1.max` :191) else false.
**Flow/Invariant:** partnerGroup is a SET-IDENTITY gate (eq→singleton Set, in→Set, ne/notIn/other → `null` = cannot adjudicate) while metrics are INTERVAL gates; null-adjudicable identity + any shared overlapping metric still conflicts (test case 4). Self-exclusion is by id at the filter level (:27), not in the overlap math.
**Probe (direct test):** from repo root: `grep -c 'return false;' apps/web/lib/api/groups/find-groups-with-matching-rules.ts` → `7`; `grep -c 'return null;' apps/web/lib/api/groups/find-groups-with-matching-rules.ts` → `9`; direct suite `apps/web/tests/workflows/find-groups-with-matching-rules.test.ts` pins 4 cases (disjoint eq/in no-conflict; in-overlap conflict; one-sided partnerGroup falls through to metric overlap; ne/notIn unparseable falls through). Runner caveat: vitest needs node_modules (standing block in inspo clone).
**Retrieve:** `echo '{"project":"mnt-hdd-utopia-inspo-platforms-dub","query":"doRuleSetsOverlap findGroupsWithMatchingRules","limit":5}' | codebase-memory-mcp cli search_graph`
**Verdict:** adopt — the disjoint-identity short-circuit is the subtle part; porters who only compare metric intervals will allow conflicting group rules.
