<!-- capsule-v2 -->
**Source:** dub `upsertGroupMoveRules` @ pin `29df217a29`
**Question:** What is the full lifecycle of a group's auto-move workflow, and which no-op inputs must stay no-ops?
**Path/Symbol:** `apps/web/lib/api/groups/upsert-group-move-rules.ts` — `upsertGroupMoveRules({workspace, group, moveRules?}) → {workflowId}`
**Signature:** returns `{workflowId: string}` on create/update; `null` after delete; `undefined` when nothing was provided — the caller writes whatever comes back straight into the group row.
**Decisive source:** :24-32 plan gate `canUseGroupMoveRule` fires ONLY when moveRules were provided (`if (moveRules && !canUseGroupMoveRule)`) so reading/updating other fields never trips it. :34-44 `moveRules?.length === 0 && group.workflowId` deletes the Workflow and returns null. :47-51 `!moveRules` (field absent) returns undefined and does NOTHING. Between those: `validateWorkflowConditions({conditions, workflowType: "moveGroup", context: {programId, groupId}})` :53-60 then the conflict sweep against every OTHER program group's rules (:62-75) throwing bad_request naming conflicting groups.
**Flow/Invariant:** The three-way return (null / undefined / id) is load-bearing: PATCH stores `workflowId` unconditionally in its update payload, so a porter who collapses "absent" with "empty" either deletes workflows on unrelated updates or resurrects deleted ones. One group = at most one workflow (group.workflowId 1:1); action shape `{type: "moveGroup", data: {groupId}}`.
**Probe (direct test):** from repo root: `grep -c 'canUseGroupMoveRule' apps/web/lib/api/groups/upsert-group-move-rules.ts` → `2`; `grep -n 'workflowId: null' apps/web/lib/api/groups/upsert-group-move-rules.ts | cut -d: -f1` → `42`; `grep -n 'workflowId: undefined' apps/web/lib/api/groups/upsert-group-move-rules.ts | cut -d: -f1` → `49`; E2E pins lifecycle in `apps/web/tests/workflows/move-group-workflow.test.ts` ("Workflow is created when move rules are configured" / "Workflow is deleted when move rules are removed" — live-server harness).
**Retrieve:** `echo '{"project":"mnt-hdd-utopia-inspo-platforms-dub","query":"upsertGroupMoveRules canUseGroupMoveRule","limit":5}' | codebase-memory-mcp cli search_graph`
**Verdict:** adopt — tri-state workflowId semantics + plan-gate placement are exactly what a naive port gets wrong.
