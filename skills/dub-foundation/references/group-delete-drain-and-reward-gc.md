<!-- capsule-v2 -->
**Source:** dub group DELETE route @ pin `29df217a29`
**Question:** How do you safely delete a group whose members carry denormalized rewards, default links, and discount codes?
**Path/Symbol:** `apps/web/app/(ee)/api/groups/[groupIdOrSlug]/route.ts` DELETE handler; `getGroupOrThrow`; `DEFAULT_PARTNER_GROUP`
**Signature:** id-or-slug resolution `groupIdOrSlug.startsWith("grp_") ? {id} : {programId_slug}` (:269-281); cross-program slug hit → forbidden "not found in your program" :294-299.
**Decisive source:** :301-306 default-group deletion forbidden. :308-327 `while (true)` drain: fetch ≤100 enrollments of the dying group, movePartnersToGroup(..., group: defaultGroup, isGroupDeleted: true), loop until a page returns empty — bounded pages keep each move's fan-out small. :329-351 reward GC is COMMISSION-GATED: count commissions over the group's four reward ids (take:100 suffices — any hit flips the flag); zero ⇒ rewards deleteMany inside the final tx. :365-367 comment: the group's Discount is deliberately NOT deleted here because `remap-discount-codes` still needs it; that cron deletes it once no remaining discount codes reference it.
**Flow/Invariant:** Final tx order = delete rewards (gated) → delete move workflow → delete group; audit log + removeGroupIdFromMoveRules ride waitUntil AFTER commit. The two-phase handoff (move first, discount later) is what prevents orphaned provider-side coupons.
**Probe (direct test):** from repo root: `grep -c 'DEFAULT_PARTNER_GROUP.slug' 'apps/web/app/(ee)/api/groups/[groupIdOrSlug]/route.ts'` → `3`; `grep -n 'while (true)' 'apps/web/app/(ee)/api/groups/[groupIdOrSlug]/route.ts' | cut -d: -f1` → `308`; `grep -c 'not found in your program' 'apps/web/app/(ee)/api/groups/[groupIdOrSlug]/route.ts'` → `1`; E2E cleanup helper in move-group-workflow.test.ts exercises DELETE /groups/:id on every case.
**Retrieve:** anonymous `DELETE` export is graph-invisible by construction — use search_code with a decisive in-route needle: `echo '{"project":"mnt-hdd-utopia-inspo-platforms-dub","pattern":"You cannot delete the default group","limit":5}' | codebase-memory-mcp cli search_code` → resolves the route's DELETE Variable node (`[groupIdOrSlug]/route.ts` 263-420).
**Verdict:** adopt — commission-gated reward GC + deferred-discount handoff is the deletion-ordering contract.
