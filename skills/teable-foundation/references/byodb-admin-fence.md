<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` + `space.controller.ts` @ pin `06a4461e`
**Question:** How is the whole migration API fenced so tenants can't start, observe, or roll back each other's database moves?
**Path/Symbol:** `assertCrossSpaceReferenceFlagOff` (:988-994), `assertNoActiveMigrationForSpaces` (:5387-5424), `jobStatusIncludesSpace` (:4073-4079), `getRelatedSpaceIdsForCopy`/`assertRequestedSpaceIsCopySource` (:5566-5593), `rejectSpaceDataDbMigrationFromSpaceApi` (space.controller.ts :84-89), admin-gate test `space-data-db-admin-gate.test.ts`
**Signature:** start path asserts: cross-space-reference flag OFF (feature mutually exclusive with migration — new links could invalidate the closure mid-run) → targetMode === 'migrate-space' → url present → related closure → no active job for ANY member space (direct spaceId column OR inventory JSON scan fallback) → same physical DB fingerprint for already-BYODB members → same url+schema fingerprint → requested space still on default (copy source).
**Data Shape:** fingerprints split identity: `fingerprintDatabaseUrl(url)` = host+db identity for "same physical server" checks; `fingerprintDataDbConnection(url, schema)` = full connection identity for binding uniqueness. Status VO redacts: only displayHost/displayDatabase/capabilities/schemaVersion exposed — never the URL.
**Decisive source:** controller :196-226 — EVERY migration route in the space API throws RESTRICTED_RESOURCE with SPACE_DATA_DB_ADMIN_ONLY ('rejects migrate-space preflight requests from the non-admin space API' etc.); the ONLY entry is DataDbBindingService.updateBindingForSpace with allowSpaceMigration=true from the admin panel. :1104-1106 — after job creation inside one meta tx (connection upsert status 'migrating' + job insert), the client cache is invalidated by connectionId so no pod serves stale routing.
**Flow/Invariant:** membership checks treat inventory as truth: a grouped job covers every related space, so cancel/status/rollback resolve ANY member space via getInventorySpaceIds (spaceIds or primarySpaceId). Cancellation only pre-validation (`cancelableStates = pre-copy + 'copying'`); validation-onward returns CANCEL_CONFLICT because the write gate makes post-copy cancellation unsafe.
**Probe (direct test):** specs 'rejects concurrent active migration for the same space' (:470), 'starts and switches a migration for cross-space linked spaces together' (:1438), 'starts a repair migration for default spaces linked to an existing target-bound space' (:1621), 'rejects a repair migration when linked BYODB spaces use another physical database' (:1827), admin-gate trio (:79/:92/:105); live: `grep -c 'assertCrossSpaceReferenceFlagOff()' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `3`.
**Retrieve:** `echo '{"project":"teable","pattern":"assertNoActiveMigrationForSpaces","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt admin-only surface + component-wide mutual exclusion + dual-fingerprint identity; adapt error codes to your taxonomy.
