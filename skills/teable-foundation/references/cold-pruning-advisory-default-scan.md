<!-- capsule-v2 -->
**Source:** teable `record-history-cold-read.service.ts` pruning @ pin `06a4461e`
**Question:** Which part-pruning signals are sound, and what happens when stats are missing or capped?
**Path/Symbol:** `pruneByRecordId`, `statsAllowPart`, `setsIntersect`, STATS_SET_CAP=500 interplay
**Signature:** key-level: keep parts with `minRecordId <= recordId` (NOT just the last one). Stats-level per entry: recordId outside [minRecordId,maxRecordId] → prune; bloom says absent → prune; boundary.t < minCreatedTime → prune; startIso > maxCreatedTime / endIso < minCreatedTime → prune; field/creator sets present and disjoint from query → prune.
**Decisive source:** :470-478 — "a heavily edited record can exceed the part size budget and spill across consecutive parts (each starting AT that record), so keeping only the last min<=recordId part per group would drop the earlier spilled rows." Advisory ladder: "entry sets are advisory: unknown (null) or no filter → cannot prune" (`if (!entrySet || !queryList?.length) return true`); "stats are advisory: no entry → must scan". STATS_SET_CAP=500 = the product's column maximum so fieldIds NEVER degrades to null in practice (:90-97).
**Flow/Invariant:** Every pruner is a NEGATIVE filter — false negatives are impossible by construction (bloom no-false-negatives + advisory defaults to scan). Wrong-direction pruning would silently drop history rows, so each signal is fail-open.
**Probe (direct test):** spec "serves record-level queries via minRecordId pruning and field filters" pins byRecord=['rh2'], byField=['rh3','rh1'], empty-projection→0 rows; live: `grep -c 'if (!entrySet || !queryList?.length) return true;\|const entry = stats?.parts\[part\.key\];' apps/nestjs-backend/src/features/record-history-cold/record-history-cold-read.service.ts` → `2` (both advisory anchors :488/:495 — the `part.key` brackets MUST stay backslash-escaped, an unescaped `[...]` is a BRE bracket-class matching one char and counts only the first anchor); `grep -oE 'STATS_SET_CAP = [0-9]+' apps/nestjs-backend/src/features/record-history-cold/part-codec.ts` → `500`.
**Retrieve:** `echo '{"project":"teable","pattern":"pruneByRecordId","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — fail-open pruning taxonomy with spill-across-parts subtlety.
