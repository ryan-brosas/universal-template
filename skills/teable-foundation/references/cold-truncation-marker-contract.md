<!-- capsule-v2 -->
**Source:** teable `part-codec.ts` truncation + `record-history-flusher.service.ts` SQL marker @ pin `06a4461e`
**Question:** How are legacy multi-MB before/after values neutralized at every entry point without losing renderability?
**Path/Symbol:** `truncateColdValue(raw, maxUnits)`, `truncateColdRow(row, maxUnits)`, `coldTruncatedMarker(units)`; flusher `readBatch` CASE/`char_length` SQL; consumer `coldTruncated === true` checks
**Signature:** marker = `{"data":"[value too large, cold-truncated (N chars)]","coldTruncated":true,"units":N}` — keeps a NON-NULLISH string `data` so getRecordHistory renders "value truncated" instead of a blank cell; units measured in UTF-16 length of the WHOLE {meta,data} envelope (O(1) proxy for V8 heap cost).
**Data Shape:** `maxUnits <= 0` disables; `truncateColdRow` returns the SAME ref when nothing oversized (identity preserved for cheap skip).
**Decisive source:** part-codec :68-76 config doc + :211-232 impl; the SAME marker string is rebuilt in SQL inside readBatch (:887-897): `CASE WHEN ${cap} > 0 AND "bl" > ${cap} THEN '{"data":"[value too large, cold-truncated (' || "bl" || ' chars)]",...}' ELSE "before" END` with per-side flags `beforeTruncated`/`afterTruncated` — "Over-cap before/after are still replaced with a marker IN SQL so a multi-MB value never crosses the wire".
**Flow/Invariant:** THREE enforcement layers: (1) SQL-side cap at buffer read, (2) JS `truncateColdRow` when folding EXISTING parts back in (feeder ensureInitialized / compactor mergeInputs heal legacy parts on rewrite), (3) consumer guard. Threshold interlock: default 1MB sits above a legit max cell (256KB data envelope) so nothing valid is ever truncated.
**Probe (direct test):** spec "oversized value truncation" x4 cases incl. feeder healing a 2MB legacy value (`expect(healedBefore.coldTruncated).toBe(true)`); live: `grep -cF 'value too large, cold-truncated (' apps/nestjs-backend/src/features/record-history-cold/part-codec.ts` → `1`; same pattern in record-history-flusher.service.ts → `1`; `grep -c 'coldTruncated === true' apps/nestjs-backend/src/features/record/open-api/record-open-api.service.ts` → `2`.
**Retrieve:** `echo '{"project":"teable","pattern":"truncateColdValue","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — dual JS+SQL enforcement with a render-safe marker is the reusable pattern.
