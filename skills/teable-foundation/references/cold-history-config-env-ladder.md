<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/record-history-cold` @ main `06a4461e2bc53055182d4df0a72dffa26fd99210`
**Question:** How do operators turn the cold-history migration on/off without breaking reads or stranding rows?
**Path/Symbol:** `record-history-cold.config.ts` — `readBoolEnv`, `readPositiveIntEnv`, `readNonNegativeIntEnv`, `IRecordHistoryColdConfig`, `recordHistoryColdConfig()`
**Signature:** `readBoolEnv(name): boolean` (accepts `'1'|'true'|'on'`, trimmed+lowercased); `readPositiveIntEnv(name, default)` floors finite >0 values else returns default; `readNonNegativeIntEnv` same with `>= 0` ("0 disables").
**Data Shape:** ~14 knobs incl. `flushHorizonMs` (24h), `backfillDayWindowMs` (30d), `partUncompressedBytes` (32MB), `tableConcurrency` (4), `maxRowsPerRun` (2M), `catchupDelayMs` (5s), `readBatchSize` (5000), `sortMemoryBudgetBytes` (64MB), `sortMergeFanIn` (16), `truncateValueUnits` (1MB), `s3ReadTimeoutMs` (10s), `bufferLagAlarmMs` (72h). All read per-call from `process.env` (`BACKEND_RECORD_HISTORY_COLD_*`), NOT captured at module load.
**Decisive source:** :99-138 — `const disabled = readBoolEnv('BACKEND_RECORD_HISTORY_COLD_DISABLED'); return { flushSchedulerEnabled: !disabled, compactSchedulerEnabled: !disabled, deleteEnabled: !disabled, ... }`.
**Flow/Invariant:** ONE kill switch gates exactly three things (flush scheduler, compaction scheduler, buffer deletion). Merged reads are UNCONDITIONAL — reading is how migrated data stays visible; a staging env sharing prod's DB must keep the switch ON and let exactly one environment own the migration (:83-98 doc). The threshold knobs interlock: `truncateValueUnits` must sit ABOVE a valid max cell envelope (256KB data) but BELOW legacy multi-MB rows; `maxRowsPerRun=0` disables the budget (backfill marathon mode). Config is re-read at every use site so env files loading after module evaluation still apply.
**Probe (direct test):** `grep -cE '^const read.*Env = ' apps/nestjs-backend/src/features/record-history-cold/record-history-cold.config.ts` → `3`; `grep -c 'BACKEND_RECORD_HISTORY_COLD_DISABLED' apps/nestjs-backend/src/features/record-history-cold/record-history-cold-read.service.ts` → `0` (the read plane has no gate — anchored at repo root).
**Retrieve:** `echo '{"project":"teable","pattern":"recordHistoryColdConfig","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — the config ladder is the porting skeleton for any tiered-storage feature flag.
