<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do you detect that a target database can host the source's data BEFORE committing to the migration?
**Path/Symbol:** `inspectSourcePostgresExtensionDependencies` (:6180-6274), `assertTargetSupportsSourceDependencies` (:6397-6454), `inspectMissingTargetExtensionDependencies` (:6514-6566), `inspectTargetDiskCapacity` (:1521-1581), `inspectTargetConflicts`/`pushConflictCount` (:6568-6698)
**Signature:** dependency scan = one CTE over pg_index/pg_class/pg_namespace WHERE definition contains 'gin_trgm_ops' → `{extensionName:'pg_trgm', objectType:'operator_class', schemaName, objectName:'gin_trgm_ops', accessMethod:'gin', sourceObjects[] (capped 20)}` persisted in inventory.
**Data Shape:** target check queries pg_opclass JOINed to current_schemas(true): an opclass is MISSING only when absent from BOTH the expected schema AND the search path (`available` vs `visible` sets, :6555-6565). Install attempt ladder: per dependency try `CREATE EXTENSION IF NOT EXISTS pg_trgm WITH SCHEMA <dep.schema>`, then fallback schema `'extensions'`; install errors are tail-capped at 500 chars and attached to the final error's remediation text.
**Decisive source:** :1533-1548 — disk capacity is measured ON THE TARGET SERVER by shelling out through the DB: `COPY __teable_target_disk_capacity FROM PROGRAM 'bash -lc … df -B1 -P <mount>'` after reading `current_setting('data_directory')`, parsing `mount|total|used|avail`. Failure is DATA ({checked:false, reason}) never a throw — "does not block migrate-space when target disk capacity cannot be inspected" spec :772. Required = max(estimatedTotalBytes × multiplier 2, 1GB min free).
**Flow/Invariant:** target conflict scan probes each shared table with to_regclass existence then scoped COUNT > 0 (schema-name collision on base ids too); every target operation is wrapped by runTargetPreflightOperation which converts raw failures into either preflight errors or TARGET_CONFLICT with sanitized messages (raw URL and postgres://user:pass@ redacted, 1000-char cap; read-only-transaction errors get a dedicated Supabase-pooler remediation message).
**Probe (direct test):** specs 'rejects migrate-space when the source needs pg_trgm but the target cannot create it' (:1075), 'starts migrate-space after creating the required pg_trgm operator class on target' (:1146), 'accepts pg_trgm installed in the target extensions schema when it is search-path visible' (:1219), 'rejects migrate-space when target disk capacity is known to be insufficient' (:735), 'does not treat equivalent extension dependency objects as changed when JSON key order differs' (:5686); live: `grep -c 'current_schemas(true)' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"inspectTargetDiskCapacity","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt capability-by-probe doctrine (never version sniffing); COPY FROM PROGRAM requires superuser — adapt for managed hosts but keep fail-open-with-evidence semantics.
