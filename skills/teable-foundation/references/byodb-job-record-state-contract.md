<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How is the migration job record itself used as the progress/audit store without corrupting state under concurrent writers?
**Path/Symbol:** `IMigrationJobRecord.copyStats` writes (:1617-1711, :3001-3078, :4794-4934), `normalizeInventory` family (:5803-5912), `compareInventoryForCopy` (:5957-6072), `completeFailedMigrationJob` (:5489-5509)
**Signature:** copyStats = `{phase, progress, <per-phase payload>, optional: delta|schemaOperations|backgroundWriters|computedDrain|targetCleanup|staleRecovery|canceled|worker}` — every phase write SPREADS the previous stats (`...(asRecord(job.copyStats) ?? {})`) and refreshes only its own key + lastError:null; per-phase heartbeat writes are fire-and-forget, terminal writes are awaited.
**Data Shape:** inventory persisted as JSON on the job and re-normalized defensively on every read (37 normalizeInventory call sites): missing fields get defaults (`mode:'default'`, cacheKey 'meta-fallback', sharedTableIds←tableIds, relatedSharedTableIds←union of related spaces' tables) so jobs written by older code versions still migrate.
**Decisive source:** :5489-5499 terminal-state guard — completeFailedMigrationJob refuses to overwrite succeeded/canceled/rolled_back and CAS-updates via `updateMany({where:{id, state: current.state}})`: a cancel racing a failure can never resurrect a canceled run. Inventory comparison uses stableJsonStringify with RECURSIVE KEY SORTING (:880-897) plus sorted-list diffs capped at 5 added/removed samples — "does not treat equivalent extension dependency objects as changed when JSON key order differs" (spec :5686). compareSharedScope flag: start-time retry checks SKIP shared-scope comparison (pre-copy churn expected); freeze-time checks INCLUDE it.
**Flow/Invariant:** every drain loop iteration re-reads job state via assertMigrationNotCanceled BEFORE continuing; cancellation throws MIGRATION_CANCELED which the catch block classifies via isProcessCancelErrorForJob so canceled runs are never mislabeled failed. Job status API returns sanitized VO (no encrypted URL).
**Probe (direct test):** specs 'fails before copy when deleted-table shared scope changed after freeze' (:5620), 'returns null when no pending migration job exists for a worker' (:1972), 'preserves canceled state when a base schema copy process is killed by cancel' (:2860); live: `grep -c 'normalizeInventory(' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `37`.
**Retrieve:** `echo '{"project":"teable","pattern":"compareInventoryForCopy","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt spread-merge stats + defensive inventory normalization + terminal-state CAS; adapt the phase taxonomy to your runner.
