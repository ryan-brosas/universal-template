<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` + `space-data-db-copy-plan.ts` @ pin `06a4461e`
**Question:** How do base schemas move between two live Postgres instances without a shared filesystem, and when must streaming be given up?
**Path/Symbol:** `copyBaseSchemasForJob` (:1587-1717), `buildBaseSchemaCopyContext` (:1838-1865), `SpaceDataDbCopyService.copyBaseSchemas`; plan builders `buildBaseSchemaDumpStreamRestorePlan`, `buildBaseSchemaDumpRestorePlan`, `buildBaseSchemaPgcopydbPlan`
**Signature:** strategies `'pg_dump_stream_restore' | 'pg_dump_restore' | 'pgcopydb'`; jobs clamped `min(max(1,jobs), maxJobs=4)`; timeout default 24h.
**Data Shape:** stream plan = pg_dump `--format=custom --no-owner --no-acl [--snapshot <id>] --schema <q>... <url>` piped into pg_restore `--no-owner --no-acl --exit-on-error --dbname <url>` (source stdout → target stdin via `.pipe`). dump/restore plan adds `--file dumpFile` + optional filtered `--use-list`. pgcopydb writes an `[include-only-schema]` filter ini and runs with `--skip-large-objects --fail-fast --restart`.
**Decisive source:** :1849-1852 — `strategy: requestedStrategy === 'pg_dump_stream_restore' && inventory.outOfScopeForeignKeys.length ? 'pg_dump_restore' : requestedStrategy`; copy.service :202-205 repeats the same demotion. pgcopydb REFUSES snapshot ids AND out-of-space FKs (:207-216) — the two capabilities that require file-based planning.
**Flow/Invariant:** URL hygiene is load-bearing: `postgresToolUrl` strips Prisma-only params (`schema`, `statement_cache_size`, `connection_limit`, `pool_timeout`, `pgbouncer`) and maps node-pg's `sslmode=no-verify` to libpq's `require` — libpq tools REJECT `no-verify` (plan comment :73-77). User info is percent-encoded once, never double-encoded. Out-of-scope FKs (constraints referencing schemas outside the migrated bases) are excluded by token-matching `FK CONSTRAINT` lines in `pg_restore --list` output (schema+table+constraint tokens all present).
**Probe (direct test):** specs 'caps copy jobs before passing them to pg_dump and pg_restore' (:6112), 'rejects pgcopydb when out-of-space foreign keys need filtered restore entries' (copy.service.spec :429), 'filters out-of-space foreign keys from pg_restore list before restore' (:223), 'maps the node-pg-only sslmode=no-verify to require for PostgreSQL client tools' (copy-plan.spec :54); live: `grep -c "sslmode === 'no-verify'" apps/nestjs-backend/src/features/space/space-data-db-copy-plan.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"buildBaseSchemaDumpStreamRestorePlan","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt strategy ladder + URL translation wholesale; adapt tool set if your hosts differ (tool availability pre-checked with `<tool> --version`, 5s cap).
