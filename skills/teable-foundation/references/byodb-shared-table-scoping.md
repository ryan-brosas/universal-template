<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do shared instance-wide tables (history, trash, outbox, undo log) move per-space without dragging the whole tenant along?
**Path/Symbol:** `sharedTables` map (:680-689), `getDeltaCaptureScopeBySharedTable` (:2217-2237), `buildSharedTableCountPlansForScope` (:7367-7441), `buildMigrationSharedTableDefinitions` (copy-plan.ts :491-585)
**Signature:** 8 shared tables: record_history, table_trash, record_trash (scope column table_id), computed_update_outbox/dead_letter (base_id), computed_update_outbox_seed (table_id + task_id IN outbox-of-base subquery), computed_update_pause_scope (OR of space/base/table scope rows), __undo_log (`split_part("table_name",'.',1) = ANY(baseIds)`).
**Data Shape:** every copy plan REQUIRES an explicit column list and a non-empty scoped WHERE — buildSharedTableCopyPlan throws otherwise (:338-343). Deleted-table handling: when a table is deleted its history/trash rows must still move — sharedTableIds falls back to ALL table ids of the copied spaces for those tables only (:2222-2224, :2559-2561), spec-pinned 'uses deleted-table shared scope only for shared rows that reference table_id directly' (copy-plan.spec :473).
**Decisive source:** :2195-2197 — "Shared tables live in the source's public schema and receive writes from every space on the instance. Scope their capture triggers to this job's rows … so a migration does not have to log and then page through the whole instance's shared-table churn." pause_scope + undo_log stay UNSCOPED at trigger level (composite/derived keys not expressible cheaply; churn negligible) and get filtered at replay by scope_type/space|base|table membership and base-prefix parsing respectively.
**Flow/Invariant:** repair migrations from default→BYODB sources copy only the DEFAULT subset (includeSpacePauseScopes=false drops space-scoped pause rows — binding.service owns them); validation counts use copy-scope plans while the out-of-scope check uses RELATED-space plans so pre-existing neighbor rows don't fail the run ('allows existing related target rows during repair validation'). FDW variants generate one schema+server PER TABLE (`${prefix}_${index}`, IMPORT FOREIGN SCHEMA LIMIT TO) inside a single psql transaction that also drops them.
**Probe (direct test):** specs 'copies shared rows only for the default subset during repair migrations' (:3096), 'pauses target computed claims after pause scopes are copied and before target outbox rows' (:3018), 'rejects unscoped shared table copy plans' (copy-plan.spec :531), 'builds scoped postgres_fdw plans for all migration shared tables' (:502); live: `grep -c "task_id\" IN (SELECT \"id\" FROM" apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"buildMigrationSharedTablePsqlCopyPlans","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt scope-column taxonomy + explicit-columns/no-unscoped-plan contract; adapt table set but keep dependents-before-sources ordering.
