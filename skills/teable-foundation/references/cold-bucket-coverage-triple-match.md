<!-- capsule-v2 -->
**Source:** teable `record-history-flusher.service.ts` planBucketCoverage @ pin `06a4461e`
**Question:** How do upload-only→delete-enabled transitions avoid re-uploading already-persisted buckets?
**Path/Symbol:** `planBucketCoverage(tableId, qualified, cutoff, dayWindowStart)` → {coveredRows, streamRanges?}, `groupStatsByBucket`, `listPartsByBucket`, `isBucketCovered`, `bucketRange`
**Signature:** per-bucket PG aggregate (count/min/max grouped by yyyymm+day-window CASE) vs `_stats.json` bucket aggregation vs LIVE part listing; covered iff key-set sizes equal AND every stats key listed AND rows equal AND min AND max ISO timestamps equal (triple match).
**Decisive source:** :692-701 — "'Fully persisted' is judged by an exact triple match ... AND a live listing that corroborates the stats keys one-to-one (stats alone are advisory; skipping an upload on stale stats would lose rows at the delete step). Buffer rows are insert-only with db-stamped timestamps and uploads came from this very buffer, so a triple match implies set equality for our write pattern." Fallback guard :750-756 — more than 64 uncovered ranges → warn + FULL rewrite ("exceed the predicate cap").
**Flow/Invariant:** Return contract: `streamRanges === undefined` → stream everything; `[]` → nothing to stream (flushTable's `streamNothing` skips the loop entirely so reconciled tables cost zero reads); else OR-clause over canonical clamped ranges (day buckets clamp to [max(dayStart, windowStart), min(dayEnd, cutoff)]; month buckets to month start vs window/cutoff). Reconciled rows count toward the ROW BUDGET only when their delete actually happened — otherwise "re-counted every run, burning the budget on the same rows forever" (:276-287).
**Probe (direct test):** `grep -c 'agg.rows === Number(bucket.count) &&' apps/nestjs-backend/src/features/record-history-cold/record-history-flusher.service.ts` → `1` (the triple-match conjunction line); `grep -c 'falling back to a full rewrite' ...` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"planBucketCoverage","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — corroborated-stats skip logic for idempotent tiered flushes.
