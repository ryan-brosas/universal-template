<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/data-db-url-secret.ts` + `data-db-internal-schema.ts` + `data-db-preflight.service.ts` @ pin `06a4461e`
**Question:** How are BYODB connection URLs stored, identified, and validated without leaking credentials?
**Path/Symbol:** `encryptDataDbUrl`/`decryptDataDbUrl` (url-secret :16-20), `generateDataDbInternalSchema`/`resolveDataDbInternalSchema` (internal-schema :8-21), `fingerprintDatabaseUrl`, `getDatabaseUrlDisplayParts`, `DataDbPreflightService.preflight` classification ladder (preflight.service, 29-test suite)
**Signature:** AES encryptor keyed by BACKEND_DATA_DB_URL_ENCRYPTION_KEY → BACKEND_ACCESS_TOKEN_ENCRYPTION_KEY → sha256(SECRET_KEY|'teable-data-db-url-secret')[:16] with parallel IV fallback chain; algorithm default aes-128-cbc. Identity = sha256(`host:port/db`)[:16] under prefix BYODB_DATA_DB_INTERNAL_SCHEMA_PREFIX ('teable') — the SAME URL always maps to the SAME schema, making target identity deterministic across pods.
**Data Shape:** stored row keeps ONLY {encryptedUrl, urlFingerprint(url+schema), displayHost, displayDatabase, internalSchema, schemaVersion, capabilities JSON}; related-space summaries recompute database fingerprint from DECRYPTED url to compare physical servers while never emitting the url itself.
**Decisive source:** preflight suite pins the acceptance taxonomy: pooler-compatible cluster+db identity query (:174), empty db usable (:207), compatible teable data db (:222), partial teable data db INCOMPATIBLE (:260), non-empty public allowed because BYODB uses internal schemas (:275), unknown object inside internal schema rejected (:307), missing DDL/table/index/function privileges reported granularly (:322-411), read-only replica reported as business error (:336) and ALTER-failure mapped to read-only class (:355), readonly-sharing capability reported SEPARATELY from core usability (:451), no password in any connection error (:475).
**Flow/Invariant:** preflight runs BEFORE baseline initialization on every start/preflight path; runTargetPreflightOperation wraps each destructive probe so failures surface as classified preflight errors, never raw driver messages. Display parts derive from the parsed URL once and are the only host info persisted alongside ciphertext.
**Probe (direct test):** specs 'masks database URL passwords' (:152), 'creates a stable fingerprint without exposing the URL' (:156), 'rejects a partial Teable data database as incompatible' (:260), 'reports read-only target connections as a business preflight error' (:336); live: `grep -c 'aes-128-cbc' apps/nestjs-backend/src/features/space/data-db-url-secret.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"fingerprintDataDbConnection","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt encrypt-at-rest + deterministic schema minting + capability-classification preflight as a unit; adapt cipher/key sourcing to your KMS.
