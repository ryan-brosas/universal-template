<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-related-spaces.ts` @ pin `06a4461e`
**Question:** Before migrating a space to its own database, how do you discover the FULL set of spaces whose data must move together?
**Path/Symbol:** `resolveSpaceDataDbRelatedSpaces`, `collectComponentGraph`, `findLinkFieldsReferencingTables`, `applyCandidateEdge` (:136-392)
**Signature:** `resolveSpaceDataDbRelatedSpaces(prismaService, primarySpaceId) → {primarySpaceId, hasCrossSpaceLinks, spaces[], links[]}`
**Data Shape:** BFS over SPACES (not tables): frontier = space ids; edges come from link-ish fields (`FieldType.Link` non-lookup, `ConditionalRollup`, or lookup+conditional) whose options/lookupOptions JSON carries `"foreignTableId":"<id>"`.
**Decisive source:** :128-135 comment — the inbound scan is a COARSE SQL prefilter: `options LIKE ANY (SELECT ('%"foreignTableId":"' || frontier.table_id || '"%'))`; "Nested occurrences keep the prefilter a superset of the truth (false positives only). Prefer strpos + unnest over regexp_matches(..., 'g') — production traces showed the regex prefilter alone dominating GET data-db (~2.5s of a ~3.3s admin summary). `extractForeignTableId` (real JSON parsing) stays the source of truth for every returned row."
**Flow/Invariant:** per frontier round: load outbound fields of frontier spaces + prefiltered inbound candidates → batch-resolve unknown foreign table→space ids → `applyCandidateEdge` adds an edge ONLY if at least one endpoint is already in the component (prefilter false positives can never fabricate an edge, :255-257) and pushes newly touched spaces onto the next frontier. Loop until frontier empty; final links list filtered to component members and sorted. Deleted tables/spaces never join (deletedTime:null in every query).
**Probe (direct test):** spec `space-data-db-related-spaces.spec.ts` — 'ignores prefilter false positives whose real edge does not touch the component' (:230), 'discovers transitive outbound components across spaces' (:144), 'finds inbound links whose real target follows a nested foreignTableId occurrence' (:264); live: `grep -c 'LIKE ANY' apps/nestjs-backend/src/features/space/space-data-db-related-spaces.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"resolveSpaceDataDbRelatedSpaces","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — migration must move the whole connected component or cross-space link cells break; porting only the requested space is the classic wrong port.
