<!-- capsule-v2 -->
**Source:** teable `record-history-flusher.service.ts` filterKnownTables @ pin `06a4461e`
**Question:** Which buffered tables are orphans vs served-elsewhere, and why does the distinction decide deletion?
**Path/Symbol:** `filterKnownTables(tableIds, {excludeByodbBound?, spaceIds?})` → {keep, orphans}; `deleteOrphanBufferRows`
**Signature:** orphan = buffered table_id with NO table_meta row ANYWHERE (hard-deleted). byodb-routed table KEEPS its table_meta and is merely served from another db — "those are never orphaned or deleted here".
**Decisive source:** :420-430 — shared-group filtering must "mirror the DatabaseRouter exactly, which never falls back to the shared db for mode='byodb' (ready/migrating/error route to the tenant connection, anything else throws). Flushing a shared-db copy the router would not serve corrupts an active migration's row-count checks (copy/validate), and for error/disabled it would operate on the wrong database entirely. Those rows simply wait untiered until the binding is repaired or reset." Space-scoped runs RE-CHECK existence UNFILTERED: "re-check existence unfiltered to avoid misclassifying an other-space table as an orphan" (:465-476).
**Flow/Invariant:** Orphan delete runs on the SAME client that listed the rows (deleted tables have no metadata to route through), unqualified `record_history`, bounded by cutoff so mid-create/restore tables keep recent rows; gated on deleteEnabled ("a read-only environment sharing the db never mutates it"); failures are caught + logged + return 0 — "one stuck orphan set must not stall otherwise-healthy tables" (discovery-phase isolation, spec-pinned ×3).
**Probe (direct test):** spec trio "sweeps orphan rows..., never deletes when the cleanup gate is off, isolates a failed orphan delete" pins groups===[{kind:'shared',tableIds:['tblLive']}], params[0]=['tblOrphan'], zero executions when disabled; live: `grep -cF "binding.mode === 'default'" apps/nestjs-backend/src/features/record-history-cold/record-history-flusher.service.ts` → `1`; `grep -c 'misclassifying an other-space table as an orphan' ...` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"filterKnownTables","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — orphan-vs-routed taxonomy prevents cross-database corruption.
