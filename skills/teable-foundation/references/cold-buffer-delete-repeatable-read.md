<!-- capsule-v2 -->
**Source:** teable `record-history-flusher.service.ts` reconcileAndDelete @ pin `06a4461e`
**Question:** Why is buffer deletion a count-gated single DELETE inside REPEATABLE READ?
**Path/Symbol:** `reconcileAndDelete(tableId, qualified, cutoff, flushedRows)`, `deleteFlushedRows`, deleteSkippedReason values
**Signature:** pre-check count outside the tx (`count !== flushedRows` → skip with reason); then ONE transaction {isolationLevel: 'RepeatableRead', timeout: 30min, maxWait 30s} re-counting and throwing on mismatch before executing `DELETE ... WHERE table_id AND created_time < cutoff`.
**Decisive source:** :1011-1028 — "count and delete run inside one REPEATABLE READ transaction, so a history row whose transaction opened before the cutoff but commits between the two statements is invisible to the delete and survives for the next run — the range predicate alone would remove it without it ever having been uploaded. (This is why the delete is NOT split into separately-committed batches...)". Performance archaeology: "A SINGLE DELETE ... one O(n) pass over the (table_id, created_time) index (~4 min for 7.8M rows). The previous ctid-LIMIT batching loop re-scanned the not-yet-vacuumable dead tuples every iteration ... O(n^2), which timed out the 30-min transaction on 10M+ row tables (the 2026-07-09 cn stall — two tables at 15M / 7.8M rows whose delete rolled back every run, so they never drained)."
**Flow/Invariant:** Two-layer late-write defense: outer reconciliation catches listener-backlog stragglers (defer, "next run re-flushes"); inner snapshot count makes the read-set atomic. Serialization failure/timeout → skippedReason 'delete-deferred' (rows stay buffered; nothing lost).
**Probe (direct test):** `grep -c "isolationLevel: 'RepeatableRead', timeout: 30 \* 60_000" apps/nestjs-backend/src/features/record-history-cold/record-history-flusher.service.ts` → `1`; `grep -c 'count-mismatch buffered=' ...` → `1`; `grep -c 'count !== flushedRows\|count !== expectedRows' ...` → `2` (outer + inner gates).
**Retrieve:** `echo '{"project":"teable","pattern":"deleteFlushedRows","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — snapshot-consistent delete + O(n²) batching lesson.
