<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration-worker.service.ts` + `space-data-db-migration.constants.ts` @ pin `06a4461e`
**Question:** What is the minimal worker loop that turns queued migration rows into supervised runs?
**Path/Symbol:** `SpaceDataDbMigrationWorkerService` (:22-101), `runOnce` (:32-59), `runForever` (:61-95), constants state lists (:7-26)
**Signature:** runOnce: recoverStaleActiveMigrationJobs(workerId) FIRST (logs each recovery) → claimNextPendingMigrationJob → runMigrationJob → return {jobId, status:'succeeded'|'failed', error?} — NEVER throws; runForever polls (default 5s, BYODB_SPACE_DATA_DB_MIGRATION_WORKER_POLL_MS), sleeps errorBackoffMs (10s) after failed runs AND after loop exceptions, exits cleanly via stop() with a final log.
**Data Shape:** worker identity = env BYODB_SPACE_DATA_DB_MIGRATION_WORKER_ID ?? `${hostname()}:${process.pid}` — stamped into claims and stale-recovery records for audit trails. State machine: pending/waiting_worker → preflight → freezing_writes ⇄ copying → validating → switching → succeeded | canceled | rolled_back, failures land in any active state.
**Decisive source:** constants :17-26 — TWO cancelability tiers: preCopyCancelable = pending|waiting_worker|preflight|freezing_writes, cancelable adds 'copying' (cancel polls still honored mid-copy through shouldCancel); validation onward is NOT cancellable because the write gate is already closed. Error-code taxonomy (16 SPACE_DATA_DB_* codes) maps every terminal condition to a stable string for UI branching.
**Flow/Invariant:** the loop is deliberately single-job sequential — one migration per worker at a time keeps drain/lock accounting sane; recovery-before-claim means an abandoned job is failed and cleaned BEFORE a new claim can pick overlapping work. All 3 unit tests pin no-throw semantics ('logs and reports a failed claimed migration job without throwing').
**Probe (direct test):** REAL RUNNER vitest `space-data-db-migration-worker.service.spec.ts` 3/3 GREEN (standalone /var/tmp/tvp): 'runs a claimed migration job', 'returns null when there is no pending job'; live: `grep -c 'BYODB_SPACE_DATA_DB_MIGRATION_WORKER_ID' apps/nestjs-backend/src/features/space/space-data-db-migration-worker.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"SpaceDataDbMigrationWorkerService","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt recover→claim→run→backoff skeleton verbatim; it composes with any DB-backed queue table.
