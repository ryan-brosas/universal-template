<!-- Preserved from the pre-foundation-skill-v1 loader. Detail remains historical and revision-pinned. -->

# Teable Foundation

## Use this for
Use when porting Airtable-style table semantics onto plain Postgres — background recomputation for formula/link/lookup fields, per-view row ordering, row-level undo, cross-table dependency tracking, transactional realtime broadcast, durable schema operations — or when borrowing teable's v2 hexagonal architecture (Result-domain-errors core, symbol-token DI, Noop defaults). Source code and direct tests are ground truth; references carry decisive excerpts and graph retrieval.

## Load the matching source dump
- `./computed-update-outbox.md` — Postgres-row task queue: lease claims via try-or-skip advisory locks (never wait), stale reclaim before pending, owner-stamped renew/complete.
- `./record-order-fractional.md` — fractional midpoint ordering in per-view lazy `double precision` columns with a ROW_NUMBER() full-shuffle escape when gaps collapse.
- `./undo-capture-triggers.md` — row-level before/after images captured by DB triggers for every write path; savepoint-guarded self-install; fail-open to no-undo.
- `./field-dependency-graph.md` — typed same_record/cross_record dependency edges built purely from field options, traversed transitively across linked tables.
- `./domain-error-contract.md` — nine-tag frozen-plain-object DomainError on neverthrow Result rails; tags drive HTTP mapping and retry policy.
- `./undo-redo-stack.md` — versioned forward+inverse COMMANDS replayed through the production bus; mode-gated appends make undo-of-undo converge.
- `./unit-of-work.md` — ambient scope-bound transactions sharing one physical DB between meta/data scopes; state-machine afterCommit; tag-gated deadlock retries.
- `./distributed-lock.md` — best-effort Redis SET NX lock that fails OPEN and never releases foreign locks; guarded work must be idempotent by contract.
- `./schema-operation-lifecycle.md` — provision-state flip + operation ledger row committed atomically so every long-running DDL is resumable from its audit trail.
- `./di-tokens.md` — curated tsyringe re-export + unique-Symbol tokens + Noop default implementations keep the core package adapter-free at runtime.
- `./share-realtime-gate.md` — ShareDB submit gate (source-tag bypass, records-only) + ops buffered in CLS until the producing Prisma transaction commits.
- `./search-vector-lifecycle.md` — advisor→executor→reconciler that creates/validates/rebuilds a managed generated-column + GIN substring-search index, gated by advisory locks, managed-name ownership, and a large-table rewrite permission.
- `./record-search-where.md` — free-text search compiled into a per-cell-type Postgres predicate, routed to a generated full-text/substring access path only when it covers every resolved field.
- `./computed-update-planner.md` — dependency-graph planner turning an insert/update/delete into a topo-sorted set of recompute steps with symmetric-link refresh and cycle handling.
- `./computed-backfill.md` — sync/async/hybrid backfill of existing rows for a changed computed field, keyed on a pg_class row estimate, with sync-failure→outbox fallback.
- `./field-operation-plugin-pipeline.md` — enforce-grouped plugin phases (supports→prepare→guard→beforePersist→afterCommit) with detached contexts and fail-open afterCommit.
- `./record-write-plugin-pipeline.md` — record-write plugin runner that reuses previous prepared state across chunked writes and adds an optional scope hook.
- `./table-data-safety-limits.md` — env-driven nested resource-limit config with alias fallback and undefined-as-unset semantics.
- `./computed-plan-steps.md` — planStage step-building: propagation-mode ladder (precise filter vs before-image vs whole-table), merge-key edge dedup, oneMany insert pruning, delete seed-step retention, CTE same-table batching.
- `./outbox-worker-lifecycle.md` — claim→execute→stage→complete worker loop with phase-tagged failures, lock-miss control-flow requeue, stage-depth cap, and afterCommit event publication.
- `./task-lease-heartbeat.md` — serialized lease heartbeat that joins in-flight beats, diffs renewed ids, and blacklists locally lost tasks before each unit of work.
- `./computed-update-lock-ladder.md` — record→batch-shard→table advisory-lock escalation with sorted acquisition and md5→bit(64) keys keyed only by protected identity.
- `./task-failure-classifier.md` — four-kind failure taxonomy: code-first safety limits, deterministic timeouts/SQL-gen bugs dead-letter immediately, unknown stays retryable.
- `./pause-scopes.md` — hierarchical space/base/table pause scopes with scheduled resume, enforced as a claim-time NOT-EXISTS plus a post-claim space re-filter.
- `./update-from-select-before-images.md` — single UPDATE...FROM SELECT that splices a `__old` self-join for before-images, fail-closed on missing WHERE.
- `./search-scope-heat.md` — observation-window heat policy ranking hot search scopes (three-factor AND, capped log score) into coverage-gated scoped-index recommendations.
- `./search-vector-inventory.md` — catalog-derived inventory (pg_attribute/pg_index/COMMENT marker) collapsing stale-reason lists into ready/stale/invalid/missing without trusting the config table.
- `./search-access-path-capability.md` — one-query pg_bigm/pg_trgm capability probe (available→preloaded→installed→opclass→ready) that never runs CREATE EXTENSION.
- `./plan-validation-hypopg.md` — EXPLAIN allowlist (single SELECT/WITH, no params/semicolons) + HypoPG before/after cost-delta validation with always-reset hypothetical indexes.
- `./expected-index-derivation.md` — query-shape→expected-index derivation (composite btree, gin_trgm, expression) diffed against pg_indexes with strict operator-class matching.
- `./remediation-task-executor.md` — kind-dispatch remediation executor (manual/schema-maintenance/search-vector/plain index) with explicit manual-execution gating and tqops_-prefixed names.
- `./search-vector-executor-guards.md` — advisory-lock serialization, managed-name ownership assertion, large/unknown-rewrite permission gate, definition-change/stale rebuild requirements, cleanup-on-failure.
- `./search-vector-naming-sql.md` — stableHash-derived managed names (63-char caps), lower(coalesce||'\n'||..) document expression, versioned COMMENT marker, LIKE wildcard escaping.
- `./search-semantics-comparison.md` — ILIKE-baseline + per-strategy match-count/sample-overlap deltas with explicit LLM-review handoff and redacted probe.
- `./real-ddl-plan-gate.md` — post-build keep-gate over live evidence: EXPLAIN-after must use the candidate index, a symmetric two-direction EXCEPT probe must prove ILIKE result equality, and cost delta must be strictly negative; any miss throws into managed-object cleanup (hypothetical-mode twins return reason codes instead).
- `./search-vector-schema-maintenance.md` — advisory-xact-lock serialization + coalesce-or-insert rebuild task that updates the reason instead of piling up N tasks.
- `./search-vector-status-reader.md` — to_regclass existence guard + newest-active-config read + closed-enum string normalization (degrades to disabled pre-schema).
- `./query-ops-repositories.md` — additive observation upsert, open-partial-unique recommendation upsert, FOR UPDATE SKIP LOCKED task claim, owner-or-expiry lease renewal.
- `./query-ops-schema-bootstrap.md` — idempotent IF NOT EXISTS table/column/index bootstrap with partial-unique indexes and raw-SQL column backfills.
- `./query-ops-adapter-wiring.md` — meta/data dual-DB resolution with dataDb→metaDb fallback and one reconciler under two tokens.
- `./query-ops-physical-name-helpers.md` — Result-returning physical-name resolver, quote-doubling identifier escaper, schema-qualified table composer.
- `./table-operation-gate-runners.md` — table/view plugin runners split prepare-outside from guard-inside via the isTransactionBound context flag; no persist hooks.
- `./task-chunking-fanout.md` — oversized tasks replaced by record-bounded children BEFORE lock acquisition; fan-out split gated on linkTraversal plans and chunk-count refusal.
- `./obsolete-task-ladder.md` — NotFound resolves over the full referenced-table closure: deleted⇒mark done, paused/provisioning⇒requeue, real errors⇒failure ladder.
- `./insert-pruning-seeding.md` — insert plans drop un-set FK-held link fields (keeping explicit values + symmetric twins) while force-seeding conditional and context-free fields.
- `./schema-rule-contract.md` — ISchemaRule kernel: id-as-dependency-edge, plane-tagged lazy statement builders, Result-only errors, IF EXISTS/IF NOT EXISTS replay safety.
- `./schema-rule-resolution.md` — Kahn resolver with dangling-dependency tolerance, reverse-order teardown, closure-based single-rule targeting, stage-tagged plan errors.
- `./schema-checker-streaming.md` — async-generator check stream with setImmediate yields and the asymmetric optional-dependency gate.
- `./schema-repair-ladder.md` — validate→execute→re-validate repair ladder with dry-run previews and four-outcome vocabulary.
- `./manual-repair-form-schema.md` — WeakMap zod metadata serialized into UI repair forms; enum resolutions as the only legal strategies.
- `./junction-rule-family.md` — junction create/validate/backfill from surviving JSONB link values with coded manual-repair routing.
- `./constraint-backfill-ladder.md` — column→default-backfill→NOT NULL chain that keeps populated-column constraints repairable or honestly unrepairable.
- `./field-rules-factory.md` — field-type→PG-type table, three-branch link storage router, generated-vs-stored column duality.
- `./type-conversion-dispatch.md` — ordered strategy ladder from in-place ALTER USING to staged rename→recreate→migrate conversions.
- `./schema-update-visitor.md` — spec-funnel DDL with ordered search-index choreography and version-bumping record collectors.
- `./schema-update-cascade.md` — post-DDL cycle gate → backfill → level-ordered cascade with conditional DISTINCT skipping and deferred replay.
- `./reference-meta-rules.md` — meta-plane reference rows with delete-vs-convert down() asymmetry and merge-don't-overwrite meta patches.
- `./system-table-rules.md` — system-column validity rules plus convention-based orphaned link-storage GC with per-schema promise caching.
- `./dependency-gate-visitors.md` — reverse-allowlist spec classification for cycle checks, self-backfills, and storage-type-drift detection.
- `./field-duplicate-sql.md` — three-way duplication strategy: value copy, computed skip, storage-routed relationship copy.
- `./select-options-minting.md` — dual-plane choice minting with fail-closed length guards and append-only merging.
- `./text-to-user-conversion.md` — tokenize→map→single-UPDATE text-to-user resolution with part-index-preserving aggregation.
- `./conditional-ddl-guard.md` — compile-valid statements whose execute() re-introspects before applying potentially-applied DDL.
- `./unique-index-duplicate-remediation.md` — quantified duplicate detection feeding confirm-gated keep-first-null-rest cleanup.
- `./fk-equivalence-orphan-preflight.md` — shape-based FK equivalence, orphan-count preflight, and idempotent DO-block creation.
- `./deferred-fk-provisioning.md` — two-pass batch table creation withholding prefix-keyed FK rules until all shells exist.
- `./create-builder-fusion.md` — rule-to-inline-column classification with skip sets and cross-field helper-column dedup during CREATE TABLE.
- `./formula-migration-matrix.md` — typed formula-value migration matrix where null means incompatible-by-design, never error.
- `./query-shape-literal-exclusion.md` — recursive forbidden-key veto BEFORE zod validation keeps user values out of shapes entirely.
- `./query-shape-stable-hash.md` — execution-shape-blind sorted-keys hashing gives queries a stable structural identity.
- `./query-risk-policy-ladder.md` — ten additive risk rules capped at 100 with policyVersion stamping so threshold changes invalidate old recommendations.
- `./query-recommendation-state-machine.md` — immutable aggregates where accept/dismiss/start/fail are guard-clause Results and executor failure becomes task state.
- `./query-ops-safetry-rail.md` — the neverthrow safeTry generator funnel composing sync parses and async reads in one short-circuiting rail.
- `./query-ops-di-noop-wiring.md` — isRegistered-gated Noop defaults plus class-token projection wiring keep optional adapters truly optional.
- `./query-ops-runner-leasing.md` — interval runners coordinated by TTL-equals-interval leases with fail-open error semantics and per-item error continuation.
- `./query-filter-shape-parser.md` — max-depth filter-tree reduction into operator families with formula-source rewriting and structural-only value inspection.
- `./query-observation-decorator.md` — marker-guarded repository decorator emitting finally-block observation windows from find AND findStream.
- `./query-sql-diagnostics-collector.md` — save/restore context-slot SQL fingerprinting with samples off by default and bounded diagnostics per window.
- `./query-config-observation-twins.md` — saved-view and relation-field configs become zero-duration synthetic observations with provenance fingerprints.
- `./query-search-definition-builder.md` — semantics↔provider mutual exclusion and derived-not-supplied physical names compose a trustworthy definitionKey.
- `./query-search-validation-gate.md` — tri-state candidate verdict: correctness veto → evidence sufficiency → plan-improvement + timing-regression gates.
- `./query-index-set-planner.md` — five-way classification, order-insensitive canonical grouping, dual absolute/percentage thresholds, total-order ranking with audited rejections.
- `./query-schema-maintenance-projection.md` — warn-don't-fail field-event projection mapping event classes to coalescing maintenance reasons.
- `./query-sql-error-diagnostics.md` — FNV-1a-64 fingerprints, quote-aware literal redaction, and position-windowed SQL samples on driver errors with parametersCaptured:false.
- `./query-schema-operation-ledger.md` — idempotency-key operation ledger where attempts count inside ON CONFLICT and claims come from a three-branch stale CTE with SKIP LOCKED.
- `./query-row-limit-plugin.md` — policy-resolved row ceilings double-checked at guard and beforePersist phases with string-bigint count handling.
- `./query-table-cte-insert.md` — conditional data-modifying CTE chain inserting table+fields+views atomically with ambient-tx fork and post-commit meta backfill.
- `./query-restore-batch-stamp.md` — shared deletion timestamp as restore batch id plus to_regclass-guarded permanent purge of optional trash tables.
- `./query-version-touch-algebra.md` — touch-order × final-version arithmetic reconstructs per-field version chains without intermediate snapshots.
- `./query-lateral-hydration.md` — selectNoFrom jsonArrayFrom laterals load tables in one round trip under a primary-first field ordering contract.
- `./query-v1-filter-migration.md` — read-time V1→V2 filter migration with uniform value-shape normalization and date-range item splitting.
- `./query-provision-ledger-recording.md` — bulk provision flips write one schema_operation row per table via length-aware deterministic idempotency keys.
- `./query-field-deserialize-ladder.md` — degrade-don't-fail field DTO deserialization trusting DB flags over options payloads with v1 conditional-twin splits.
- `./storage-type-visitor.md` — cellValueType→dbFieldType ladder (multiple/json→JSON; number REAL; dateTime DATETIME; boolean BOOLEAN; string TEXT) with autoNumber INTEGER and link JSON hard overrides.
- `./field-persistence-row-builder.md` — v2 domain fields serialize into v1-compatible `field` rows: options vs lookup_options column split, conditionalRollup flattening, conditionalLookup innerType masquerade, T6332 formula-expression strip, DTO-first/domain-fallback resolution.
- `./db-field-naming.md` — DB identifier minting: slugify (case-preserving) + unnamed/t-prefix repair + suffix disambiguation over a growing reserved set seeded with the seven `__*` system columns.
- `./table-where-spec-filter.md` — four provision-state constructor ladders compile table specs into table_meta WHERE clauses; incoming-reference EXISTS filters the referenced FIELD's soft-delete too; mutation specs refuse loudly instead of matching nothing.
- `./meta-update-two-depth.md` — every field update routes options-only vs full storage-metadata rewrite by whether derived columns could go stale; statements triple-pin id+table_id+deleted_time and bump version SQL-side; insert-or-revive upsert clears deleted_time.
- `./record-condition-where.md` — typed per-field condition specs compile to Postgres predicates: three-valued emptiness ladders, jsonb ?|/?&/@> list operators, null-preserving negation, 27-mode date-range taxonomy with format-preset-derived granularity, literal-vs-field-ref operand polymorphism.
- `./select-columns-order.md` — record SELECT lists accumulate in table-field order with conflict-on-duplicate db names and the record-id column always first.
- `./base-repository-tx-join.md` — scope-tagged join-or-open transaction pattern ('meta' never joins a 'data' ambient tx), computed-order subquery inside INSERT, non-throwing describeError serialization ladder.
- `./cell-value-mutate-visitor.md` — typed cell-value specs compile into one record UPDATE: system-column seeding, computed-field skip, changed-field ledger for propagation.
- `./link-mutation-routing.md` — link SET routes by relationship: junction delete+insert, FK-on-main, or foreign-table repoint, with the symmetric-FK (`__id`) guard.
- `./attachment-table-replace.md` — attachment cells replace via delete-then-insert on `attachments_table` keyed by (table,record,field), with batch tuple-IN dedupe.
- `./link-title-fill.md` — SQL-side COALESCE of missing link titles from the foreign table's lookup column, 20k pre-compile cap.
- `./field-value-visitors.md` — insert/delete/database/sql-literal per-field conversions; computed never written, JSONB stringified, explicit NULL casts.
- `./link-change-collection.md` — classify link add/remove/replace/reorder and collect exclusivity constraints for newly-added foreign records.
- `./is-persisted-as-generated-column.md` — which computed fields are real generated columns (lookup/conditionalLookup never), gating mutation writes.
- `./record-condition-where-compilation.md` — typed condition specs compile to raw Postgres predicates with NULL-passing negative operators and jsonb array/id extraction.
- `./date-range-resolution.md` — 27-mode date filter ranges with format-preset-derived granularity and timezone-aware half-open boundaries.
- `./record-query-read-plane.md` — find/findOne/findStream assembly: `__row_` order-column fallback, parallel rows+count, array_position record-id order, CTE source.
- `./record-read-model-mapping.md` — row→read-model mapping with system-column extraction and recursive legacy-avatar URL normalization.
- `./undo-capture-session.md` — trigger-based undo capture session with local-then-global batch-id fallback and idempotent finish/abort.
- `./attachment-lookup-service.md` — attachment lookups run in the meta db (BYODB safety) with optional-column drop-and-retry.
- `./stream-pagination-strategies.md` — offset vs cursor stream pagination selection with shared limit/exhaustion math.
- `./user-lookup-service.md` — identifier→user resolution across id/email/name in one OR query with avatar derivation.
- `./date-search-range.md` — free-text date search value → half-open range with format-preset unit gating and round-trip validation.
- `./build-record-where-clause.md` — spec→visitor→WHERE funnel converting the empty-where error to null.
- `./auto-number-sequence-resync.md` — auto-number sequence resync.
- `./computed-event-fanout.md` — computed event fanout.
- `./computed-seed-dispatch.md` — computed seed dispatch.
- `./cte-before-image-bulk-update.md` — CTE before-image bulk update.
- `./deferred-computed-drain.md` — deferred computed drain.
- `./empty-table-seed-analyze.md` — empty table seed analyze.
- `./generated-column-insert-strip.md` — generated column insert strip.
- `./hybrid-sync-budget-splitting.md` — hybrid sync budget splitting.
- `./impact-hint-normalization.md` — impact hint normalization.
- `./incoming-link-delete-choreography.md` — incoming link delete choreography.
- `./insert-batch-bind-ceiling.md` — insert batch bind ceiling.
- `./link-exclusivity-validation.md` — link exclusivity validation.
- `./link-seed-batch-loading.md` — link seed batch loading.
- `./linked-record-advisory-ladder.md` — linked record advisory ladder.
- `./meta-touch-monotonic-clock.md` — meta touch monotonic clock.
- `./missing-host-table-tolerance.md` — missing host table tolerance.
- `./noop-write-guard.md` — noop write guard.
- `./physical-row-duplication-plan.md` — physical row duplication plan.
- `./record-write-funnel.md` — record write funnel.
- `./restore-identity-choreography.md` — restore identity choreography.
- `./seed-field-expansion.md` — seed field expansion.
- `./sparse-batch-update-engine.md` — sparse batch update engine.
- `./trash-restore-cleanup.md` — trash restore cleanup.
- `./undo-row-snapshot-reconstruction.md` — undo row snapshot reconstruction.
- `./view-order-bootstrap.md` — view order bootstrap.
- `./inline-outbox-drain-loop.md` — inline outbox drain loop.
- `./sync-dirty-seed-cascade.md` — sync dirty seed cascade.
- `./insert-builder-dual-api.md` — RecordInsertBuilder dual API: compile a record INSERT plus link/attachment side-effect SQL without executing.
- `./bulk-insert-reuse.md` — bulkInsert fans N records through ONE builder instance; additional statements drain only after all main rows exist and locks are held.
- `./explain-builder-reuse.md` — EXPLAIN analyzers reuse the same insert builders read-only because the builder must stay side-effect-free and deterministic.
- `./insert-exclusivity-choreography.md` — repository-side ordering: validate exclusivity BEFORE the INSERT, strip physically-generated columns, take foreign locks AFTER own row but BEFORE junction/FK statements.
- `./insert-bind-limit-batching.md` — dynamic insert batching under PostgreSQL's 65,535-parameter ceiling (columns × rows, clamped [1,500]) with cross-batch RETURNING accumulation.
- `./generated-column-insert-strip-meta.md` — meta-level visitor probe drops system-computed fields from explicit INSERTs so GENERATED ALWAYS columns never collide.
- `./generated-audit-column-strip.md` — physical catalog re-check strips legacy GENERATED ALWAYS audit columns from assembled values and gates auto-number setval() resync.
- `./insert-restore-identity-dedup.md` — restore-identity resolution degrades warn-only to `{}` names and NEVER probes users on the transaction connection (25P02).
- `./link-title-fill-sql.md` — INSERT-time COALESCE JOIN fills missing link titles inside the main INSERT instead of a post-insert UPDATE pass.
- `./update-builder-impact-plan.md` — RecordUpdateBuilder splits SET clauses from the collected link-diff impact plan that drives locks, exclusivity, seeding, and computed propagation.
- `./update-one-mutation-applied.md` — updateOne tri-state: zero-row UPDATE returns mutationApplied:false as SUCCESS data, aborting the undo capture session first so no rows leak.
- `./physical-row-duplication.md` — table clone copies rows server-side via ordered INSERT…SELECT (creation-order preserving) plus junction pair clones with per-side order-column guards.
- `./batch-update-aggregation.md` — BatchRecordUpdateBuilder transposes N per-record mutation specs into one per-column payload with deduped sorted locks and version never carried per-record.
- `./batch-sql-composition-seam.md` — buildBatchUpdateSql emits the single UPDATE…FROM(VALUES): presence CASE decides explicit-null-clear vs omitted-keep; version bump lives only in SET.
- `./batch-existing-links-bulk-reader.md` — loadExistingLinkRecordIdsBatch prefetches every record's current link ids in ONE IN-query per relationship shape under an all-keys-present pre-seeding contract.
- `./before-image-merging.md` — buildBeforeImageRecordsFromStepChanges + earliest-wins mergeBeforeImageRecords keep ORIGINAL filter-field old values intact across multi-stage sync→outbox recomputes.
- `./pg-capability-identifier-hash.md` — Postgres capability by EXECUTION (probe pg_input_is_valid, never version-sniff) + FNV-1a hash suffixing keeps >63-char identifiers collision-free and deterministic.
- `./query-builder-mode-manager.md` — one manager interface switches reads between computed (lateral) and stored modes; prepare(deps)-before-build() pins output shape and system columns (`__row_${string}`).
- `./same-table-cte-formula-batch.md` — many same-table formula recomputes compile into level-ordered CTEs with shared-subexpression reuse via SQL plan objects (fragments → CSE bindings → leveled CTEs).
- `./stored-ordering-contract.md` — raw-column stored reads reproduce v1/computed sort semantics through three-tier ORDER BY resolution (nulls, choice labels, user-like snapshots, date-only fields).
- `./dirty-frontier-propagation.md` — generation-keyed dirty frontier in pg_temp.tmp_computed_dirty: per-generation edge SELECTs dedup by sql+params, one UNION ALL INSERT..ON CONFLICT DO NOTHING per pass, stop at zero inserted rows.
- `./conditional-before-image-propagation.md` — conditionalFiltered edges widen to all-target on four named runtime fallbacks; targets go dirty when the filter matches the CURRENT row OR a jsonb-overlaid BEFORE-IMAGE reconstruction.
- `./same-table-collapse-guards.md` — multi-level same-table formula batches collapse to one CTE step only when purely same-record + formula-only + non-JSON targets + within 1000/500-id chunk limits.
- `./dirty-target-lock-choreography.md` — locks are taken on POST-propagation dirty groups (not just seeds) inside the write transaction by reusing the seed lock planner with extraSeedRecords.
- `./computed-cell-safety-limit.md` — computed cell byte ceiling fails closed EXCEPT junction-projected link values; field-chunked collection bumps __version once per changed record via an explicit id-set UPDATE.
- `./dirty-set-join-placement.md` — the tmp_computed_dirty INNER JOIN must precede lateral joins AND repeat inside every set-based host source; recordId slices ride = ANY(text[]).
- `./set-based-rollup-ladder.md` — four join modes (lateral/inner/hostLeft/hostKey) chosen by relationship shape, order-sensitivity, condition complexity, and text-type host keys; host-driven grouping keeps zero-match hosts emitting COUNT/SUM-zero rows.
- `./link-join-ladder.md` — FK-location truth table shared by read joins, propagation selects, and aggregates; link titles project as jsonb_strip_nulls({id,title}) with deterministic tie-breakers and jsonb-only [0] subscripting.
- `./lookup-rollup-json-aggregation.md` — storage-aware lookup aggregation: single-level flatten only for plain unfiltered lookups, recursive WITH RECURSIVE otherwise; rollup numerics coerce hostile text via strip→prefix→validate→NULL instead of erroring.
- `./distinct-host-key-probe.md` — 1000–50k dirty conditional aggregate steps may skip id chunking only after a bounded COUNT(DISTINCT host_key) probe confirms ≤5,000 keys.
- `./field-change-impact-closure.md` — field-definition changes invalidate whole tables via a lazy ALL_RECORDS sentinel (never eager id materialization) and re-admit conditional-sort dependents even when reference rows are missing.
- `./link-cascade-frontier-bfs.md` — link propagation is an iterative frontier BFS over junction tables (500-id chunks, ALL_RECORDS saturation short-circuit, `::text` casts) that never scans full edge tables.
- `./reference-graph-cte-queries.md` — four SQL shapes enumerate dependents: recursive reference CTE UNION changed-link self rows; lookup_options JSON link-field probe; options/lookup_options sort-field extraction for conditional-rollup dependents; LEFT JOIN/HAVING COUNT=0 dependency-free formulas — all on the tx client.
- `./conditional-failsafe-ladder.md` — conditional rollups abandon precise targeting through an eight-rung widening ladder (>10k ids, missing filter/refs, cross-table refs, unknown fields, JSON filter columns) and re-probe with typed CAST-per-column VALUES derived tables when new values must be simulated pre-commit.
- `./symmetric-planned-seeding.md` — link cell writes pre-seed foreign tables with BOTH old and new link-target ids so removals refresh stale symmetric display values junction-walking would miss.
- `./dual-phase-update-orchestration.md` — inside one transaction: collect impact → lock impacted records sorted by dbTableName (capability-gated) → run caller update → evaluate+publish raw versionless ShareDB ops; base-changed fields excluded EXCEPT track-all audit fields.
- `./all-records-fixpoint-bfs.md` — record-impact iteration is monotone growth to fixpoint: any dirty conditional match triggers a FULL link-closure re-run and re-enqueue of grown tables plus their link dependents; Set→ALL is one-way.
- `./layered-evaluation-ordering.md` — Kahn-level topo layers order evaluation so lookups materialize before formulas reading them; stored-column preference engages only in later layers; cycles fall back to ONE flat layer instead of failing.
- `./raw-op-publication-ladder.md` — publishing distinguishes evaluated-null from not-projected via hasOwnProperty, always sends oldCellValue:null, prefers __prev_version as op version, and saves RAW ops without DB writes or version bumps.
- `./auto-number-cursor-paging.md` — >10k-record recomputes page by auto-number cursor with clone-per-page builders, JS re-sort BEFORE handling, null-cursor advance guard, and short-page termination; strategy table falls through to cursor as default.
- `./errored-field-update-gating.md` — UPDATE...FROM SELECT SET-lists keep errored lookups/rollups (typed NULLs clear stale values) but drop errored non-generated formulas; track-all timestamps stay RETURNING while generated audit users are skipped.
- `./persisted-backfill-reentry.md` — maintenance backfills ride the field-creation pipeline via a no-op update callback after self-healing CLS context, never a bespoke bulk writer.
- `./trigger-role-config.md` — producer/consumer split defaults BOTH-on with truthy-list booleans ('1','true','yes','on') and silent positive-int fallbacks so single-node deployments need zero env.
- `./base-admission-lua-lease.md` — per-base concurrency caps live in one atomic Lua zset script (Redis TIME, capacity check+insert, TTL≥2×lease); renewal is ZADD XX-only so expired owners cannot resurrect; permit.assertActive() throws at every await boundary.
- `./deferred-wakeup-reason-ladder.md` — claim-miss rescheduling encodes distributed facts per reason: eligible/concurrency=100ms transient, not_due trusts upstream retryAt verbatim, active_lease=max(transient,expiry), indefinite pause parks WITHOUT republish; deterministic time-bucketed ids converge duplicates with -r suffix escape.
- `./final-attempt-replay-rearm.md` — wakeup consumers Zod-validate first (invalid ⇒ UnrecoverableError, no retries) and on final broker attempt publish a 30s durable replay whose DB row outlives queue state.
- `./fire-forget-publish-recovery.md` — publishes use intentional fire-and-forget timeouts (a slow Redis that completes still delivers at-least-once), one background recovery circuit fails fast new publishes, and deterministic cuwr2-/cuwd- ids removeOnComplete immediately so replays stay addable.
- `./startup-redrive-reconciliation.md` — startup + 5-minute actionable reconcile re-arm durable tasks across meta-fallback AND every BYODB target under a pg advisory single-flight lock, keyed cuwr2-<taskId>-<updatedAt.attempts.nextRunAt.lockedAt> so state changes become fresh jobIds.
- `./role-aware-publish-capability.md` — consumer-only deployments emit replay wakeups through an AsyncLocalStorage consumer capability checked beside producerEnabled, keeping business-code publishes impossible.
- `./post-process-drain.md` — after any targeted task succeeds, drain remaining outbox in 50-task batches until empty poll or 500 cap, re-asserting the admission permit each iteration (restores continue-after-progress cascade semantics).
- `./anomaly-grouping-recovery.md` — dead-letter triage groups failures by kind+base+seedTable+first-500-error-chars with newest representative and bounded samples; dead/stale recovery reports delivery honestly as accepted-or-deferred.
- `./order-preserving-concurrency-map.md` — shared-cursor worker-pool map bounds concurrency while writing results by claimed index; ~18L portable primitive reused by redrive/monitor/anomaly sweeps.
- `./outbox-health-classification.md` — health composes reasons into critical only for queue_unavailable/consumer_unavailable (zero workers counts even on producer-only roles), degrades otherwise, coalesces concurrent snapshots onto the in-flight promise.
- `./wakeup-wire-contract.md` — wake-up payloads are versioned locators (literal schemaVersion, ISO datetimes, created/merged/retry/replay cause taxonomy) carrying no business data; validation failure parks poison messages.
- `./computed-module-wiring.md` — the computed pipeline exports only orchestrator+backfill (internal seams stay private) and the trigger splits async-registered producer vs V2-importing consumer modules around a symbol-token publisher.
- `./ref-cte-four-way-or.md` — the reference closure runs as a recursive CTE whose join matches all FOUR edge orientations so the directed table behaves undirected, identity edges excluded.
- `./topo-cycle-throw-contract.md` — dependency-first topo emission throws a localized validation error on cycle detection, and changed-field seeds missing from the graph are synthesized and prepended.
- `./link-fk-single-source-of-truth.md` — old link keys are parsed from FK storage (junction rows / FK columns), never from stale cell JSON, with per-relationship scalar/array shape validation.
- `./link-junction-order-reorder-ladder.md` — junction writes classify pure reorders (delete-all + ordered reinsert) vs membership deltas (pairwise diff + append after MAX(order)) to keep link order a stored attribute.
- `./link-plan-commit-split.md` — `planDerivateByLink` derives identical cell changes via in-memory symmetric patches without writing FKs; `commitForeignKeyChanges` persists later, and the persist path must RE-FETCH after writes.
- `./link-delete-context-dual-sweep.md` — record deletion builds unlink contexts from BOTH the reference graph and a raw options scan, sweeping self-side junction data directly to survive pre-existing inconsistency.
- `./batch-temp-table-update-ladder.md` — bulk updates stage values in a nanoid-suffixed TEMP table created/updated/dropped inside ONE transaction, translating unique/not-null violations into field-named localized errors.
- `./batch-cls-rawop-stash.md` — calculation writes push ShareDB raw ops into the CLS `tx.rawOpMaps` stash instead of submitting, keeping DB mutations and realtime ops atomic through commit.
- `./sysfield-tracked-lastmodified-dual-write.md` — system last-modified columns always stamp while tracked LastModified* fields update their own columns only for records whose changed fields intersect the tracked set (empty filter degrades to track-all).
- `./link-title-resolution-ladder.md` — link titles degrade gracefully: typed formatter → primitives → array join → well-known key ladder → record id, never throwing mid-write.
- `./calc-diamond-merge-compose.md` — diamond recomputation merges duplicate changes first-slot/last-value and composes same-path ops carrying the FIRST od with LAST oi, deleting od==oi no-ops.
- `./calc-errored-gate-auxiliary-context.md` — auxiliary data widens to lookup filter operands and ridden links BEFORE graph filtering, and errored (`hasError`) link fields are excluded from derivation contexts only.
- `./integrity-check-fix-dispatch.md` — integrity fixes never scan independently: check aggregates seven detectors (incl. cross-base inbound links) into one issue list that the fix switch routes type-by-type.
- `./integrity-link-json-backfill.md` — lost FK storage is rebuilt FROM surviving JSON link cells via conservative idempotent SQL (NULLIF extraction, MIN(self_id) dedup, NOT EXISTS pair inserts), PG jsonb vs SQLite json_each split.
- `./integrity-primary-promotion-ladder.md` — broken primaries demote inside one outer tx then resolve kept/promoted/mirror-formula, renaming to "(before-fix)" only in the fallback so downstream references survive.
- `./integrity-orphan-purge-guards.md` — dangling reference edges purge bidirectionally by count, and junction orphans delete via whereNotExists anti-join behind a hard `junction_` naming guard.
- `./integrity-v2-crossbase-hydration.md` — base integrity hydrates the cross-base referenced-table closure before verdicts (unknown-base tables retry via withoutBaseId specs) so valid remote lookups are not "repaired".
- `./integrity-v2-preflight-gate.md` — a grouped meta query counts active/primary fields before hydration: zero-field tables are excluded from specs yet reported as manual-mode synthetic issues yielded first.
- `./integrity-v2-sse-lifecycle.md` — long integrity streams ride SSE with comment-line heartbeats, connect/complete/error lifecycle events on one grammar, and flush-per-write under X-Accel-Buffering:no.
- `./integrity-v2-repair-telemetry.md` — repair streams capture result-level errors AND thrown exceptions to Sentry+spans then RE-THROW, so telemetry never converts failures into silent partial repairs.
- `./integrity-filter-operator-prune.md` — invalid filter operators are pruned recursively from options/lookupOptions with empty groups collapsing to NULL, applied only through OT field-property ops.
- `./integrity-uniqueindex-emptystring-twins.md` — missing UNIQUE indexes are rebuilt with canonical DDL naming branched on id prefix, and exact-empty-string text cells normalize to NULL on non-computed fields.
- `./integrity-symmetric-consistency-ladder.md` — structural existence checks gate semantic comparison; a vanished symmetric twin is repaired by declaring one-way, never by fabricating a twin.
- `./integrity-fk-column-restoration.md` — missing FK columns are re-created with relationship-exact constraints (OneOne unique, one-way OneMany composite pair-unique), tolerating 42P07, and always finish with JSON-cell backfill.
- `./agg-sql-compile-ladder.md` — statistics requests funnel through request-validation → builder state → per-field raw `SELECT <sql> AS alias` emission, validity enforced at BOTH the open-api and SQL layers.
- `./agg-mcv-jsonb-join.md` — multi-cell-value aggregates register a per-field CTE subquery and cross-join it (`MAX(mcv.value)`) while presence/count-class funcs are exempt and stay row-scoped.
- `./agg-hostile-numeric-coercion.md` — text jsonb elements coerce via strip→NULLIF→cast so SUM/AVG never error on hostile cell text.
- `./agg-percent-greatest-guard.md` — every percent aggregate divides by GREATEST(COUNT(*),1) with pre-division *1.0 float promotion.
- `./agg-user-unique-id.md` — Unique counts DISTINCT ->> 'id' only for single-valued user-family fields; everything else whole-cell distinct by design.
- `./agg-date-range-split.md` — DateRangeOfDays is pure SQL; DateRangeOfMonths ships "MAX,MIN" from SQL and converts with dayjs in the service.
- `./agg-attachment-size-boolean.md` — attachment size sums per-row jsonb elements inside an outer SUM; checkbox containment rides @> '[true]'::jsonb with asymmetric NULL handling between scalar/array twins.
- `./agg-selection-slice.md` — selection chips aggregate a [skip,take) slice by folding groupBy into orderBy, filtering collapsed groups, and forcing the paginated table path in BOTH layers.
- `./agg-group-points-fanout.md` — N-level groups re-aggregate prefix slices, hash full PATHS into group ids, and emit an 'unknown' remainder bucket when the cap truncates.
- `./agg-choice-order-sort.md` — grouped aggregates sort by choice-declaration order via ARRAY_POSITION CASE ladders with doubled placeholder bindings.
- `./agg-search-timeout-gate.md` — searches run under SET LOCAL statement_timeout with three-shape error duck-typing because the data-db Prisma runtime is a separate generated client.
- `./agg-searchindex-two-pass.md` — search-index resolution is pass-1 hit ids then pass-2 ROW_NUMBER() OVER () over the live view CTE, NaN-guarded against mid-flight deletes.
- `./agg-cache-me-key.md` — aggregation GETs cache for 1h versioned by table lastModifiedTime, inflating keys with currentUserId whenever any filter mentions the Me sentinel.
- `./agg-valid-func-table.md` — one pure getValidStatisticFunc matrix serves UI ordering AND both validation layers; user splices mid-array, attachment pulls Uniques and pushes TotalAttachmentSize.
- `./agg-calendar-spine.md` — calendar daily collection cross-joins a generate_series date spine pinned to the field's timezone with array_agg[1:10] id samples.
- `./agg-permission-projection.md` — projections collect statistic/group/filter/search fields then intersect enabledFieldIds with an all-allowed fallback, while filters on hidden fields keep working via augmented selection maps.
- `./agg-tql-view-merge.md` — TqlPipe mutates filterByTql→filter after zod, then view JSON filters merge with custom filters; ignoreViewQuery bypasses filter+sort+columnMeta together.
- `./agg-result-conversion.md` — driver values normalize through one bigint/date/string ladder with percent-family NULL→0 coercion, matched by alias-or-fieldId for wire compat.
- `./agg-rowcount-link-modes.md` — row count serves views/pickers/chips from ONE spine where selectedRecordIds polarity flips to whereNotIn exclusions in candidate mode.
- `./agg-symbol-di-token.md` — AGGREGATION_SERVICE_SYMBOL + Inject decorator decouple consumers from the concrete service class, mirroring the v2 di-tokens discipline.
- `./agg-group-points-cap.md` — maxGroupPoints LIMIT applies only under groupBy; the grid twin compares against a separate true row count and synthesizes the Unknown bucket.
- `./base-pagination-cte-split.md` — split pagination wraps the source in a BASE_<alias> CTE holding offset+limit rows with the outer query applying offset.
- `./aggregate-range-table-path-forcing.md` — a paginated [offset,offset+limit) aggregate forces the table path ('full' pagination mode) because tableCache skips base pagination and would silently aggregate the whole view.
- `./computed-pagination-fallback.md` — filter/sort referencing computed fields disables BASE pagination entirely while keeping record-id restriction — degrade to unpaged, never mis-page.
- `./query-state-manager-scoped-selection.md` — one mutable manager owns CTE/selection registries per query while ScopedSelectionState shares CTE reads but THROWS on every write.
- `./original-source-latch.md` — setMainTableSource latches originalMainSource on FIRST write so nested CTEs re-read the physical table even after pagination repoints mainSource at the BASE CTE.
- `./filter-augmented-selection.md` — buildFilter clones the visitor selectionMap then augments EVERY field with alias-qualified physical refs so hidden fields still participate in WHERE.
- `./sort-selection-gate.md` — buildSort restricts to selectionMap keys only (permission-respected) — the deliberate asymmetry vs filter augmentation.
- `./link-cte-emission-choreography.md` — per-link CTE generation is stack-guarded, pending-name registered, nested-dependency collected, emitted-once, then joined back exactly once via markCteJoined.
- `./materialized-aggregate-cte.md` — conditional rollup/lookup CTEs emit WITH...AS MATERIALIZED on PG so UPDATE...FROM re-joins don't re-run aggregates per outer row.
- `./conditional-equality-counts-plan.md` — 'field is {FieldRef}' conjuncts extract into a GROUP BY counts join plan (fail-closed on any other shape incl. date-like fields), residual filters ride inside the counts subquery.
- `./conditional-window-limit.md` — top-N per host key compiles ROW_NUMBER() OVER (PARTITION BY keys ORDER BY sort) filtered __cl_rank <= limit BEFORE aggregation.
- `./multi-value-lookup-jsonagg.md` — multi-value lookups compile to json_agg(sanitized ORDER BY stable-ladder) FILTER (WHERE EXISTS-sub AND NOT NULL) — both clauses are load-bearing.
- `./lookup-date-iso-normalize.md` — date-like targets under MULTI-value lookups normalize elements to ISO-UTC strings for lexicographic json filters; single-value lookups keep native types.
- `./link-cell-jsonb-strip-nulls.md` — link cells are jsonb_strip_nulls(jsonb_build_object('id',…,'title',…)) so null titles drop the KEY, aggregated with FILTER excluding vanished records.
- `./formula-expansion-context-swap.md` — nested formula expansion caches results, throws CircularReferenceError on cycles, and save/restores targetDbFieldType+timeZone around each recursion.
- `./question-mark-literal-split.md` — string literals containing ? split and rebuild via CONCAT so knex.raw never sees a bind placeholder; numeric regexes are likewise ? -free by comment-documented rule.
- `./pg-multivalue-scalar-aggregators.md` — 22 scalar functions over multi-value first args intercept dispatch into per-element STRING_AGG aggregators built from the SAME formulaQuery methods.
- `./binary-op-numeric-coercion.md` — comparisons/arithmetic cast string operands to numeric EXCEPT equality against blank-like values, which must stay text to preserve empty semantics.
- `./param-metadata-coercion-dispatch.md` — every function emit runs inside setCallMetadata(paramInfos)/finally-clear so emitters classify each argument slot; lookup-json slots force multi-value json treatment.
- `./loose-numeric-coercion.md` — never-throw numeric coercion: REGEXP_REPLACE strip -> NULLIF -> COLLATE "C" anchored match -> double precision, ELSE NULL; arrays sum matching elements.
- `./blank-aware-comparison.md` — blank literals or text-like sides route BOTH comparison operands through COALESCE(NULLIF(text,''),'') unifying '' and NULL; numeric pairs stay native.
- `./datetime-sanitize-tzwrap.md` — tzWrap sanitizes through BTRIM+'null/undefined'+default-pattern guard UNLESS metadata-trusted or already timestampish; tz always quote-doubled in AT TIME ZONE.
- `./month-diff-day-adjustment.md` — DATETIMEDIFF month/quarter/year uses hand-rolled baseMonths ± day-of-month adjustments with last-day guards, while FROMNOW/TONOW deliberately use AGE() months.
- `./truthiness-if-type-matrix.md` — IF() scores conditions via boolean/json/trusted-numeric/pg_typeof ladder then picks branch typing rung-by-rung: datetime -> numeric-with-blank -> target-numeric -> text.
- `./generated-column-immutability-contract.md` — generated-column SQL must be immutable: castToTimestamp falls back NULL::timestamp for non-datetime inputs and the validator vetoes computed/system field references pre-DDL.
- `./typed-null-error-degradation.md` — errored fields (even physical reads) emit dialect-typed NULLs per dbFieldType so UPDATE...FROM assignments stay valid — degradation is per-field, never a throw.
- `./rollup-function-support-matrix.md` — rollupAggregate compiles each fn with type guards: non-numeric SUM/AVG become constant SUM(0)/AVG(0); countall over MultipleSelect counts jsonb array length; xor is parity.
- `./flatten-nested-json-array.md` — arrays-of-arrays lookup payloads flatten via WITH RECURSIVE f(e) peeling until non-array leaves; single-value lookups return null to keep raw column access.
- `./formula-select-expression-fallback.md` — formulas read their generated column normally but recompile inline under rawProjection (schema-change safety) or when not persisted as generated columns.
- `./projection-dependency-closure.md` — projections expand BFS-style: lookups/rollups pull their link field, formulas pull reference fields (toggleable), output re-sorted to declared order before any CTE build.
- `./aggregate-group-choice-order.md` — group ordering uses ARRAY_POSITION over choice names with doubled bindings for multi-select (first-element probe) plus a secondary full-jsonb sort for determinism.
- `./user-snapshot-select-json.md` — CreatedBy/LastModifiedBy render join-free via buildUserJsonObjectFromSnapshot(snapshotRef,idFallbackRef) — snapshot first, system id fallback.
- `./link-cell-formatting-ladder.md` — FieldFormattingVisitor applies choice/number/date/user display formatting through dialect methods, formatting arrays per-element WITH ORDINALITY.
- `./conditional-computed-target-resolution.md` — filters/sorts targeting nested computed fields ensure their scope CTE exists then read it via correlated scalar subquery keyed on foreign record id, falling back to inline visitor SQL.
- `./conditional-aggregate-cast-envelope.md` — aggregates unwrap json-to-scalar then cast to the ROLLUP FIELD's declared dbFieldType before entering CTE columns — skipping the envelope breaks UPDATE...FROM assignment.
- `./select-expression-alias-binding.md` — every computed SELECT item emits as qb.select({[dbFieldName]: knex.raw(expr)}) because bare strings let knex parse '?' placeholders inside expressions.
- `./link-cte-column-contract.md` — link CTEs expose main_record_id/link_value/lookup_<id>/rollup_<id>; readers address these exact names and register them in selectionMap for WHERE reuse; link_value is ::jsonb-cast on PG.
- `./conditional-equality-fallback-coalesce.md` — equality-plan hosts with no foreign rows COALESCE to semantic defaults (0::double precision for count/sum/average; typed NULL for max/min) mirroring empty-set aggregate semantics.
- `./view-context-raw-selection.md` — view/tableCache contexts select stored columns directly (shouldSelectRaw) with NO CTE generation; cache failures log-and-degrade to the full table path.
- `./conditional-lookup-scalar-extractor.md` — scalar functions over multi-value args reduce to the first non-null element (ordinality order) through title->name->text preference, datetime-reformatted except in generated columns.
- `./conditional-rollup-ordering-functions.md` — array_join/concatenate/array_unique/array_compact honor sort+LIMIT on the ordered slice; ordering capability excludes them from the equality counts plan.
- `./link-relationship-join-matrix.md` — CTE body shape branches three ways: junction double-join + GROUP BY, one-many FK-in-foreign + GROUP BY, many-one/one-one direct join without grouping (symmetric fields swap keys).
- `./conditional-lookup-warning-degradation.md` — missing conditional CTEs warn loudly (generation-side AND reader-side messages listing available keys) then return typed NULL — nothing throws.
- `./aggregate-builder-composition-order.md` — aggregation appends group-blind (groupBy withheld from aggregationQuery), GroupQuery owns GROUP BY, then per-group choice-aware ORDER BYs land last.
- `./generated-column-countall-multivalue.md` — COUNTALL over jsonb arrays counts jsonb_array_length after NULLIF(x::jsonb,'null'::jsonb); scalars stay CASE 0/1 — spec-pinned byte-exact.
- `./fromnow-tonow-direction-contract.md` — FROMNOW subtracts date-from-now, TONOW keeps NOW() - date (past-positive); unit ladders divide epochs except month/year using AGE calendar arithmetic.
- `./select-error-function-semantics.md` — SELECT-side ERROR() emits a WHERE-FALSE subquery trick (mutable allowed on read paths) and IS_ERROR() is constant FALSE; neither may port into STORED DDL.
- `./conditional-filter-field-reference-context.md` — dual fieldReference maps resolve {HostField} tokens to mainAlias refs and bare ids to foreignAlias refs (first-wins host priority; same-table scopes alias everything through the foreign side).
- `./lookup-order-column-tiebreak.md` — deterministic lookup ordering: order-col IS NULL DESC -> ASC -> __id ASC (junction or foreign variant), degrading to bare record-id ordering; ts-pattern enforces exhaustiveness.
- `./conditional-lookup-dual-column-emission.md` — one conditional CTE emits BOTH conditional_lookup_<id> and conditional_rollup_<id> aliases when the field is dual-reader, so either visitor finds its expected column.
- `./formula-support-validator-family-vetoes.md` — six AST detector families (datetime concat, datetime slicing, logical-non-bool, numeric-over-text, logical fns, plus TypeInferVisitor) veto generated columns before per-function support checks.
- `./aggregate-selection-slice-projection.md` — buildAggregateSelect visits fields for REGISTRATION ONLY (results discarded) so filters/groups resolve expressions without emitting a row-level SELECT list.
- `./datetime-format-offset-suffix.md` — DATETIME_FORMAT renders TO_CHAR(pattern) plus a ±HH:MM offset derived from the SAME expression's UTC-vs-tz epoch difference, quote-doubling the zone name.
- `./cold-history-config-env-ladder.md` — one kill switch gates flush/compact/delete only; merged reads are UNCONDITIONAL so shared-DB environments never lose visibility; per-call env reads with positive/non-negative int ladders (0 disables).
- `./cold-part-key-grammar.md` — part keys embed minRecordId + optional run token (`{dd|m}-p{seq4}-[r{token}-]{minRecordId}.ndjson.{zst|gz}`); the grammar IS the pruning index and `_stats.json` deliberately fails to parse.
- `./cold-bloom-double-hash.md` — per-part record-id bloom (k=7 double hashing, 10 bits/el): h2 forced ODD via `| 1` AFTER unsigned coercion or the modulo goes negative and buffer writes become silent no-ops; false-only pruning.
- `./cold-ndjson-line-splitter.md` — readline's ConsString+regex flattening is an O(n²) OOM on multi-MB lines; accumulate partial chunks and concat exactly once per newline; destroy the stream in finally.
- `./cold-zstd-gzip-negotiation.md` — write format negotiable by env (mixed-node fleets force gzip), read capability strict per key suffix; checked per call because env files load after module eval.
- `./cold-part-writer-verify-delete.md` — stream-cut parts at 32MB uncompressed, verify each upload by re-download + 4-way count/sha/footer match, delete-on-fail so unverified parts never sit under the live prefix.
- `./cold-truncation-marker-contract.md` — over-cap before/after replaced IN SQL with a render-safe marker (string data + coldTruncated + units), re-healed on part rewrite; threshold must clear a legit max cell envelope.
- `./cold-spill-swap-before-await.md` — sorter run swap happens before ANY await so rows added mid-spill open the next run; first spill error poisons every later add/drain (owner must error, not emit partial sets).
- `./cold-fanin-floor-two.md` — merge fan-in floored at 2 (a 1→1 pass never converges); multi-pass groups keep inputs+outputs tracked mid-pass for throw-safe temp cleanup.
- `./cold-sort-memory-budget-global-eviction.md` — ONE byte budget across all bucket sorters (per-sorter caps multiply by bucket count — the cn-drain OOM); charges held until spill writes LAND; enforce waits on inflight as backpressure; drains freeze eviction via unregister+settle.
- `./cold-feeder-foldback-eof-init.md` — buckets merge existing parts back IN (never clobber) via full external sort because no input order can be trusted and half-open GETs deadlock HTTP clients; consumedKeys is the only healable set.
- `./cold-module-split-consumer-guard.md` — services-only CoreModule vs queue+Processor outer module stops auxiliary workers from silently becoming competing cold-queue consumers (the BYODB-migration-worker incident).
- `./cold-scheduler-add-only-no-removal.md` — schedulers are add-only across pods; kill-switched processes pause their worker and gate at execution time instead of removing shared schedules; no boot-time kick jobs.
- `./cold-catchup-chain-hop-id.md` — BullMQ rejects ':' in custom job ids but dedups against ANY stored job including the executing one → hop-numbered colon-free ids plus a pending-chain probe before chaining.
- `./cold-discovery-loose-index-scan.md` — distinct buffered tables via recursive loose index scan; BYODB dbs probed purely on main-db max(last_modified_time)-vs-bookmark so idle tenant dbs are never woken.
- `./cold-byodb-bookmark-monotonic.md` — bookmarks advance only after verified deletes with all four drain conditions met, monotonic via WHERE lt cutoff; monthly day-1 sweep ignores bookmarks as a safety net.
- `./cold-orphan-vs-byodb-exclusion.md` — no-table_meta orphans (hard-deleted) are deletable in place; byodb-routed tables keep metadata and are excluded to mirror DatabaseRouter exactly (else corruption/wrong-db).
- `./cold-read-batch-adaptive-limit.md` — keyset buffer reads through native pg (knex) sidestep Prisma's napi-string engine quirk; LIMIT adapts to ~8MB/batch from observed row weight with floor 1; UTC-naive timestamp binds.
- `./cold-bucket-coverage-triple-match.md` — already-persisted buckets skip re-upload only when stats AND live listing AND buffer aggregate triple-match (count+min+max); >64 uncovered ranges falls back to full rewrite.
- `./cold-storage-write-vs-read-cache-discipline.md` — write paths cache-free (cross-process rewrites make key-addressed caches lie); read path caches compressed bytes keyed key@etag from live listings; concurrent misses must subtract old bytes first.
- `./cold-topk-month-atomic-timeout.md` — S3 budget starts at the cold segment; months scan ATOMICALLY (partial scans contribute nothing), deadline checked within parts every 1024 rows; timed-out empty pages throw rather than read as end-of-history.
- `./cold-pruning-advisory-default-scan.md` — every pruner fails OPEN (no entry/no set → scan); minRecordId keeps ALL overlapping parts because fat records spill across consecutive part cuts.
- `./cold-cursor-dual-dialect-inclusive-legacy.md` — chs1: base64url cursors are exclusive; legacy prisma id cursors resolve INCLUSIVE when still buffered, restart when flushed; COLLATE "C" tie order keeps pagination stable across the seam.
- `./cold-merged-read-buffer-first-seam.md` — buffer-first shortfall fill preserves global order (buffer ⊇ everything newer than cutoff); seenIds dedup covers the overlap window; hot pages never touch S3.
- `./cold-missing-part-relist-retry-once.md` — vanished-under-rewrite parts get exactly one fresh re-list + rescan; second miss propagates (pairs with writer run tokens making duplication, never loss).
- `./cold-buffer-delete-repeatable-read.md` — count-gated single DELETE inside REPEATABLE READ makes late-committing rows survive for the next run; batched ctid deletes were O(n²) dead tuples (the 15M-row stall).
- `./cold-truncation-render-guard.md` — consumer checks coldTruncated === true BEFORE type-specific processing (attachment presign would fail on marker strings); meta-less import rows get synthetic field meta with degraded placeholder fallback.
- `./cold-compactor-force-repair.md` — monthly compaction folds day+existing month parts order-agnostically (the collation-order repair tool); force bypasses the no-day-parts skip; healing deletes only snapshot-consumed keys.
- `./cold-flush-run-budget-atomicity.md` — soft row budgets checked between table slices keep single tables atomic; mid-stream failures abort feeders (idempotent budget release); date-based bucketing gives fresh installs month-file backfill for free.
- `./history-buffer-capture-funnel.md` — v1 listener turns table.record.update ops into per-cell history rows OFF the request path: kill switch → cross-record field-id union → ONE batched field load → five-step filter ladder (malformed shape → missing oldValue/newValue keys → `_oldField ?? field` conversion latch → isEqual no-op → both-computed skip) → schema-qualified raw insert through the database router → unconditional RECORD_HISTORY_CREATE re-emit.
- `./history-envelope-dual-dialect.md` — same physical row carries TWO JSON dialects: full `{meta:{type,name,options,cellValueType},data}` envelopes from the listener and all v2 projections vs deliberately bare `{data}` envelopes from the createRecordsOnlySql fast path; readers treat missing meta as render-data-only.
- `./history-v2-background-deferral.md` — v2 projections defer writes through withoutTransaction(context) + a four-rung scheduler ladder (injected scheduleBackgroundTask → unref'd setTimeout → setImmediate → queueMicrotask → inline fallback), swallow-and-log failures, and attribute the actor from the EVENT snapshot because CLS at drain time belongs to an unrelated request.
- `./history-v2-projection-trio.md` — one writer shape over RecordUpdated / RecordsBatchCreated / RecordsBatchUpdated sharing extractFieldMeta (visitor-driven v1-compat metadata) and differing ONLY in skip ladders: computed-source gate absent on creates; empty-string values skipped on creates but recorded as real updates on edits.
- `./byodb-related-spaces-closure.md` — cross-space link graph BFS (frontier = spaces, edges = link-ish fields with JSON-prefiltered foreignTableId) computing the connected component that must migrate together; prefilter is a false-positive-only superset, real JSON parsing stays the edge truth.
- `./byodb-job-claim-cas.md` — oldest-first worker claim via conditional updateMany (count===1 CAS) + progress-aware stale recovery that fails abandoned jobs, resumes their pauses, and cleans their targets.
- `./byodb-base-schema-copy-ladder.md` — pg_dump|pg_restore strategy ladder: streaming pipe default, demotion to file+filtered TOC when out-of-scope FKs exist, pgcopydb refusing snapshots/FK filters, and libpq URL translation (Prisma params stripped, no-verify→require).
- `./byodb-copy-pipeline-supervisor.md` — subprocess pipeline kernel: shell:false spawns piped stdout→stdin, five event-timing fallbacks so half-delivered Node exit/close events can't hang a copy, single-flight progress polls with hang/failure kill, 1s cancel polling, secret-redacted bounded buffers.
- `./byodb-snapshot-delta-replay.md` — exported repeatable-read snapshot held across both copies; per-job WHEN-guarded delta triggers log scoped changes into one bigserial log; replay applies ON CONFLICT upserts with generated-column stripping and baseline-relative cursors.
- `./byodb-migration-progress-model.md` — weighted six-stage progress model (~40 phase→fraction positions), monotonic EWMA-smoothed tracker with byte throughput and velocity-based ETA clamped at 30 days.
- `./byodb-copy-validation-kernel.md` — copy verification: batched catalog signature comparison (columns/indexes/constraints/triggers with PG18 NOT NULL exclusion), order-independent multiset content hashing (extract(epoch) time normalization, N/V length-prefix encoding, FUNC_MAX_ARGS-safe || concatenation), shared-table parity plus zero-out-of-scope-target check.
- `./byodb-write-freeze-gate.md` — pre-copy drain taxonomy (schema operations, provisioning resources + BullMQ import jobs, computed outbox leases) with stale-row exclusion, initial-drain grace before snapshot, and the repeated full gate at final freeze.
- `./byodb-atomic-switch-rollback-proof.md` — cutover flips connection + all member bindings in ONE meta transaction after route smoke; rollback requires a zero-finding proof (inventory/counts/timestamps vs switchedAt) run under 'switching' state with fail-closed query errors.
- `./byodb-target-artifact-retry-lifecycle.md` — idempotent retry: same-fingerprint failed/dry-run predecessors get their base schemas dropped and shared rows scope-deleted (priority order dependents-before-sources) gated on unchanged inventory; TRUNCATE only when the target has zero bindings; never clean a switched target.
- `./byodb-target-capability-probes.md` — capability-by-execution probes: source gin_trgm_ops dependency scan, target opclass availability incl. search-path visibility with extensions-schema install fallback, server-side disk measurement via COPY FROM PROGRAM df, and scoped to_regclass conflict counts.
- `./byodb-write-guard-baseline.md` — app-wide write guards keyed by migration state (schema writes blocked on all active states, record writes only during freezing/switching of switch-jobs) with tx-client/root-client split and missing-table tolerance; checksummed advisory-locked data-DB baseline migrations with per-connection in-flight dedupe.
- `./byodb-shared-table-scoping.md` — eight shared tables each with a scope column grammar (table_id/base_id/pause-scope OR/undo-log schema prefix), deleted-table fallback to full table-id scope, explicit-columns-and-scoped-WHERE plan contract, and repair-mode default-subset narrowing.
- `./byodb-heartbeat-failure-forensics.md` — fire-and-forget heartbeats with awaited final beat, pg_stat_progress_copy sampling plus restore-side byte totals, failure forensics reconstructed from persisted stats for stale recovery, read-only-replica remediation messaging, and secrets redacted at capture.
- `./byodb-admin-fence.md` — admin-panel-only API surface (space routes throw RESTRICTED_RESOURCE), component-wide mutual exclusion against active jobs, dual fingerprints (physical DB vs url+schema), inventory-truth membership checks, and two-tier cancelability ending at validation.
- `./byodb-repair-mode-legacy-seqs.md` — repair migrations adopt an existing neighbor's internal schema and narrow the copy set; legacy public auto-number sequences are pre-created from pg_attrdef/pg_depend metadata then setval-synced with is_called at the final gate; public undo-capture function shim installed before restore.
- `./byodb-job-record-state-contract.md` — job row as audit store: spread-merge phase stats, defensively re-normalized JSON inventory (37 call sites), key-order-insensitive stable comparisons, terminal-state CAS so failures can't resurrect canceled runs.
- `./byodb-shared-copy-strategies.md` — psql COPY pipeline (snapshot-wrapped source → stdin ingest) vs postgres_fdw scripted insert with per-table schema/server lifecycle; COPY/INSERT row-count regexes feed honest progress; sequential execution with pause-after-scopes hook.
- `./byodb-worker-loop.md` — recover→claim→run→backoff poll loop (5s/10s), hostname:pid worker identity stamped into claims and recoveries, never-throws runOnce, clean stop.
- `./byodb-secret-and-preflight.md` — AES-encrypted-at-rest URLs with deterministic sha256 host:port/db internal-schema minting, dual fingerprint identity, and a capability-classification preflight ladder (empty/compatible/partial/read-only/privilege-granular) that never leaks credentials.
- `./history-select-minimize-twin.md` — select-field options are filtered to choices matching the VALUE at write time (null→empty choices, string equality, Set membership for arrays), duplicated line-for-line across v1/v2 engines by design; snapshot answers "what did the cell say", not "what did the palette look like".
- `./history-ops-to-event-pipeline.md` — ShareDB rawOps become events post-commit only (bindAfterTransaction reads AND clears tx.rawOpMaps), grouped `${tableId}_${eventName}`, bulk-merged with isBulk:true; oldField rides CLS onto every record-update event and create+edit merges produce {newValue}-only cells the listener must tolerate.
- `./history-id-gc-lifecycle.md` — rows are `rhi`+24 ids generated client-side by every writer; ONE env kill switch (RECORD_HISTORY_DISABLED==='true', config-load-time) gates all writers but no reader; table drop drains hot buffer best-effort FIRST and S3 prefixes strictly AFTER the purge commit — no TTL job exists on the buffer.
- `./cold-heal-consumed-keys-scoping.md` — heal deletes exactly consumedKeys − writtenKeys per touched bucket; concurrent same-bucket keys must survive (duplication until dedup is the accepted transient).
- `./airtable-import-phase-ladder.md` — Airtable base import as a strict phase ladder (tables→links→record streams→link fill→derived fields→view config) where any uncomputable field degrades to a reported issue instead of aborting.
- `./airtable-analyze-endpoint.md` — dual-mode analyze route: base discovery vs pure-planner dry run whose predicted issues match executed issues by construction.
- `./airtable-import-authz.md` — scope-parity guard on stored integration credentials plus concrete-target authorization (`base|table_import` vs `base|create`) before any stream opens.
- `./airtable-sse-import-stream.md` — SSE framing for long imports: header set incl. X-Accel-Buffering:no, comment-line heartbeats, closed-stream guards, in-stream typed error events.
- `./airtable-api-client-discipline.md` — third-party list-API client: 220 ms spacing throttle, per-request token provider, inactivity-based aborts (never whole-request deadlines), one retry budget over 429/5xx/network, typed iterator-expiry error.
- `./airtable-record-streaming.md` — page-streamed record import with reverse-batch creates (target assigns descending order), id-map-filtered restarts after iterator expiry, and memory kept only for link-target tables.
- `./airtable-bounded-concurrency-map.md` — shared-cursor worker-pool map (~18L): bounded concurrency, index-claimed writes preserve result order; used at concurrency 3 for attachment transfers.
- `./airtable-attachment-watchdog.md` — CDN→storage stream transfer: size preflight before opening the socket, chunk-refreshing stall watchdog, triple unhandled-rejection armor, socket release on every failure path.
- `./airtable-cell-conversion.md` — never-throw cell conversion: error/specialValue markers ⇒ empty, NUL stripping, array/scalar collapsing, email-keyed collaborator resolution with count-and-report drops.
- `./airtable-user-resolution.md` — collaborator identity maps resolve against the BASE's space membership (not the caller-passed space), keyed by lowercased email, once per import.
- `./airtable-field-mapping-ladder.md` — per-type mapping table with a 15-kind converter vocabulary decoupling target type from cell conversion; live-first/snapshot-fallback for computed fields; unknown types degrade to LongText snapshots; primary-field compatibility gate.
- `./airtable-link-planning.md` — two-way links pair with deterministic single-side ownership; creation is ManyMany except declared-single sides; oversized singles relax or truncate-and-report because foreign uniqueness can't be pre-guaranteed.
- `./airtable-link-row-spill.md` — JSONL part spill of link cells into deployment blob storage (4 MiB parts, env-named budget, tail-flush-before-read) keeps importer memory flat.
- `./airtable-link-fill.md` — streamed remap-and-fill of spilled link cells through old→new id maps with three-way accounting (dropped / truncated / missing field).
- `./airtable-derived-fields.md` — lookups/counts/rollups/formulas created in late passes; formulas retry in dependency passes until no progress, then report survivors.
- `./airtable-formula-translator.md` — translate-or-refuse token-level formula bridge with opaque `{field}`/string spans, TRUE()/FALSE() literalization, and refusal-as-feature snapshot fallback.
- `./airtable-rollup-mapper.md` — strict single-aggregation grammar (`FUNC(values)`) mapping 14 functions 1:1; compound aggregations return null so snapshots stay honest.
- `./airtable-share-client.md` — undocumented share-link endpoint access: typed-reason session scraping, verbatim session replay, stream-json assembly dropping huge branches at token level for flat memory.
- `./airtable-view-config-mapping.md` — drop-and-report filter/sort/group translation with host-side operator validity re-checks, select option-id→name mapping, kanban stack-from-groupLevel.
- `./airtable-view-config-apply.md` — ordered view pairing with type guards plus label-scoped per-aspect application isolation (filter/sort/group/options each catch independently).
- `./airtable-issues-ledger.md` — one shared issues array + severity-split structured summary make every degradation observable yet never fatal.
- `./airtable-ai-field-upgrade.md` — aiText columns upgrade to live AI fields only when an AI model exists; prompts rebuild through the id map with name fallback; isAutoFill:false keeps import generation-free.
- `./auth-permission-guard-ladder.md` — one NestJS guard resolves public→disabled→share-ceiling→anonymous→standard+PUBLIC-fallback in a fixed order; anyPermissions alternative groups preserve the PRIMARY error when all fail.
- `./auth-share-scope-ceiling.md` — base-share/share-view permissions derive from link metadata, always re-check resource membership (cross-base replay guard), scope node shares by cached subtree closure with a link-target escape hatch, gate edits to logged-in users minus exclusions, and validate share-view headers against an explicit method×path×permission allowlist.
- `./auth-session-store-dual-key.md` — express-session Store over Redis keeps `session-store:{sid}` + a `session-user:{userId}` index (+120s margin), prunes expired sids on write, self-heals orphans on read, and revokes user-wide via tombstone-before-delete.
- `./auth-strategy-chain-fallback.md` — AuthGuard tries session→PAT→JWT→anonymous in order; every validate re-reads deactivated/system state from DB; ensure-login redirects browsers instead of 401; stale deserialize deletes the session user instead of throwing.
- `./auth-jwt-internal-token-kinds.md` — internal JWTs discriminate User/App/Automation by payload shape; robot users ride the normal chain while tempAuthBaseId pins base scoping into CLS before authorization; enterprise override hook is an honest community no-op.
- `./auth-signin-lockout-attempts.md` — brute-force ladder gated on dual config knobs: atomic INCR (30s window) → threshold flips to minutes-long lockout key and expires the counter; generic invalid-credentials error masks all causes; turnstile pre-gate failures consume attempts too.
- `./auth-oauth-state-callback.md` — OAuth2 state is a cache-backed consume-once token (16-char id, 12h TTL, destructive verify) carrying redirect_uri; callbacks accept only path-only redirects via placeholder-origin URL parsing.

## Capsule map
- **Background computation** — `computed-update-outbox`, `field-dependency-graph`: changed cells enqueue merged outbox tasks; claims are leases (try-or-skip advisory locks, bounded stale reclaim, heartbeat renewal); the affected-field set comes from a pure edge-builder graph whose cross-record edges hop tables through link fields.
- **Row ordering** — `record-order-fractional`: insert K rows anywhere with midpoint math over lazy per-view order columns; float exhaustion triggers dense renumber-and-retry instead of failure.
- **Undo & history** — `undo-capture-triggers`, `undo-redo-stack`: DB triggers log row images transparently under batch GUCs; the application layer replays versioned commands through the real bus so permissions and side-effects apply identically.
- **Transactions & consistency** — `unit-of-work`, `schema-operation-lifecycle`, `base-repository-tx-join`: ambient per-scope transactions with sibling reuse and exact-once afterCommit; repositories join by scope tag so 'meta' writes never ride a 'data' transaction, opening their own only when absent; DDL tracked as paired state-flip + ledger row with idempotency keys and attempt scheduling.
- **Error model** — `domain-error-contract`: serializable tagged errors returned as Results end-to-end; one edge mapper converts tags→HTTP; infrastructure tag gates deadlock retries.
- **Runtime wiring** — `di-tokens`, `distributed-lock`: hexagonal boundary enforced by import graph via Symbol tokens and Noop defaults; cron/maintenance exclusivity via a deliberately fail-open SET NX lock.
- **Realtime** — `share-realtime-gate`: client OT submissions gated by source-tag and document type, then held per-request until their DB transaction commits.
- **Search & full-text access paths** — `search-vector-lifecycle`, `record-search-where`, `search-vector-inventory`, `search-access-path-capability`, `plan-validation-hypopg`, `expected-index-derivation`, `remediation-task-executor`, `search-vector-executor-guards`, `search-vector-naming-sql`, `search-semantics-comparison`, `real-ddl-plan-gate`, `search-vector-schema-maintenance`, `search-vector-status-reader`: a managed generated-column + GIN index is created/validated/rebuilt by an advisor→executor→reconciler (advisory-lock serialized, managed-name-guarded, large-rewrite-gated); the executor's inventory is catalog-derived (pg_attribute/pg_index/COMMENT marker) and collapses stale-reason lists into ready/stale/invalid/missing; provider capability is a one-query probe that never CREATEs an extension; plan validity is proven with an EXPLAIN allowlist + HypoPG before/after cost deltas, and real-DDL keeps are gated on used-index + symmetric-EXCEPT semantics + strictly-negative cost evidence; expected indexes derive from the query shape and diff against pg_indexes; the executor dispatches by task kind with managed-name + large-rewrite guards; names/expressions are stableHash-derived and length-capped; n-gram semantics are compared against the ILIKE baseline with an LLM-review handoff; schema changes coalesce into one rebuild task; free-text search then routes per-cell-type predicates onto that access path only when it covers every resolved field, else falls back to a default OR-of-ILIKE.
- **Query-ops infrastructure** — `query-ops-repositories`, `query-ops-schema-bootstrap`, `query-ops-adapter-wiring`, `query-ops-physical-name-helpers`: the meta tables are bootstrapped idempotently (IF NOT EXISTS + partial-unique indexes); observations accumulate additively, open recommendations upsert on a partial unique key, tasks claim via FOR UPDATE SKIP LOCKED with stale reclaim, leases renew owner-or-expiry; the adapter wires meta/data dual-DB (dataDb→metaDb fallback) and shares one reconciler under two tokens; physical names resolve via Result-returning helpers with quote-doubling identifier escaping.
- **Computed recompute planning** — `computed-update-planner`, `computed-backfill`: a dependency-graph planner turns insert/update/delete into a topo-sorted recompute step set (INSERT seeds all fields, DELETE promotes conditional cross-record sources, symmetric links refresh); backfill of existing rows picks sync/async/hybrid by a pg_class row estimate and falls back to the outbox on sync failure.
- **Plugin pipelines** — `field-operation-plugin-pipeline`, `record-write-plugin-pipeline`, `table-operation-gate-runners`: enforce-grouped phases (supports→prepare→guard→beforePersist→afterCommit) run in parallel within an enforce group and serially across groups, with detached contexts, fail-open afterCommit, and (record-write) prepared-state reuse across chunked writes; table/view runners are pure gatekeepers that prepare outside and guard inside the transaction.
- **Resource limits** — `table-data-safety-limits`: env-driven nested limit config with alias fallback and undefined-as-unset.
- **Outbox execution & reliability** — `outbox-worker-lifecycle`, `task-lease-heartbeat`, `computed-update-lock-ladder`, `task-failure-classifier`, `task-chunking-fanout`, `obsolete-task-ladder`, `pause-scopes`, `update-from-select-before-images`: the drain side of the task queue — phase-tagged execution with control-flow requeues (lock-miss, blocked tables) vs classified dead-letters, serialized lease heartbeats, record→batch→table advisory-lock escalation, pre-lock chunking/fan-out, hierarchical pause scopes enforced at claim time, single-statement recompute returning before-images.
- **Computed plan steps** — `computed-plan-steps`, `insert-pruning-seeding`: how the topo-sorted field set becomes ordered UPDATE steps via a propagation-mode ladder with merge-key edge dedup, delete retention, CTE batching, insert-time FK-location pruning, and forced seeding of scan-invisible fields.
- **Adaptive search indexing** — `search-scope-heat`: observation windows rank hot per-field-set search scopes (three-factor hot gate, capped log heat score) into plan-validated scoped-expression index recommendations.
- **Integrity: schema rules & repair** — `schema-rule-contract`, `schema-rule-resolution`, `schema-checker-streaming`, `schema-repair-ladder`, `manual-repair-form-schema`: atomic DDL capabilities declare up/down/isValid with dependency ids; a Kahn resolver orders and inverts them; a streaming checker yields progress with an asymmetric optional-dependency gate; the repairer re-validates after executing and gates destructive paths behind zod-declared confirmation forms.
- **Integrity: link storage rules** — `junction-rule-family`, `constraint-backfill-ladder`, `unique-index-duplicate-remediation`, `fk-equivalence-orphan-preflight`, `reference-meta-rules`: junction tables rebuild from surviving JSONB link values (manual confirm for missing host schemas/orphan rows); NOT NULL rides a default-backfill child rule; unique indexes quantify duplicates before consent-gated cleanup; FK validity is shape-based with orphan preflight; reference rows keep delete-vs-convert asymmetry on the meta plane.
- **Schema evolution** — `field-rules-factory`, `type-conversion-dispatch`, `formula-migration-matrix`, `select-options-minting`, `text-to-user-conversion`, `field-duplicate-sql`, `create-builder-fusion`, `deferred-fk-provisioning`, `conditional-ddl-guard`, `storage-type-visitor`, `field-persistence-row-builder`, `db-field-naming`: one factory maps 20 field types to rule sets (link routing junction vs FK vs unique-FK); conversions dispatch from in-place guarded ALTERs to staged rename→recreate→migrate spines preserving cell values; duplication, choice minting, user resolution, batch creation, second-pass FKs, storage type visitors, field row builders, and DB identifier minting each carry their own SQL contract.
- **Spec gating & orchestration** — `dependency-gate-visitors`, `schema-update-visitor`, `schema-update-cascade`, `system-table-rules`, `meta-update-two-depth`: reverse-allowlist collectors mark which specs need cycle checks or backfills; the update visitor choreographs DDL + trigram-index lifecycle + record-update capture, while the meta-plane sibling routes every field update through options-only vs storage-metadata depth selection with triple-pinned scopes and SQL-side version bumps; the repository sequences statements → cycle gate → backfill → level-ordered cascade → deferred replay over system-column-valid tables.
- **Query-ops domain kernel** — `query-shape-literal-exclusion`, `query-shape-stable-hash`, `query-risk-policy-ladder`, `query-recommendation-state-machine`: shapes carry counts/ids/enums never values (recursive forbidden-key veto before zod); structural identity comes from execution-shape-blind sorted-key hashing; a pure policy turns windows into capped additive risk scores stamped with policyVersion; recommendations and remediation tasks are immutable aggregates whose transitions are guard-clause Results (executor failure lands as task state, not thrown errors).
- **Query-ops application & runtime** — `query-ops-safetry-rail`, `query-ops-di-noop-wiring`, `query-ops-runner-leasing`: handlers compose sync parses and async reads through one safeTry generator rail; DI fills only unregistered tokens with Noops and wires the maintenance projection under its class token; interval analyzers/task-workers coordinate via TTL-equals-interval leases, continue past per-item errors, and dispatch through the real command bus.
- **Query observation pipeline** — `query-filter-shape-parser`, `query-observation-decorator`, `query-sql-diagnostics-collector`, `query-config-observation-twins`: filter trees reduce to max-depth stats + operator families with formula-source rewriting; a marker-guarded decorator wraps the record-query repository so find/findStream emit finally-block 300s windows; SQL rides context-slot collectors that fingerprint without storing samples by default; saved views and relation fields enter the same sink as zero-duration synthetic observations with provenance fingerprints.
- **Query-ops search definition & validation** — `query-search-definition-builder`, `query-search-validation-gate`, `query-index-set-planner`: definitions derive physical names from the domain into a composite definitionKey under semantics↔provider mutual exclusion; candidate verdicts run correctness → evidence-sufficiency → performance gates; validated candidates classify, group by order-insensitive canonical field sets, dual-threshold gate, and rank into one audited recommendation set.
- **Query-ops schema-change upkeep** — `query-schema-maintenance-projection`: field events map to coalescing maintenance reasons and schedule best-effort, warn-don't-fail.
- **Record condition compilation** — `record-condition-where`, `select-columns-order`, `table-where-spec-filter`: typed per-field condition specs compile into raw Postgres predicates (three-valued emptiness ladders, jsonb ?|/?&/@> list operators, null-preserving negation, 27-mode date ranges whose unit granularity derives from the field's formatting preset); record SELECT lists accumulate in table-field order with conflict-on-duplicate names and the record-id column pinned first; table-level selectors compile through four provision-state ladders where the incoming-reference EXISTS also filters the referenced FIELD's soft-delete and mutation specs refuse loudly rather than match nothing.
- **Record write/read value plane** — `cell-value-mutate-visitor`, `link-mutation-routing`, `attachment-table-replace`, `link-title-fill`, `field-value-visitors`, `link-change-collection`, `is-persisted-as-generated-column`, `record-condition-where-compilation`, `date-range-resolution`, `build-record-where-clause`, `auto-number-sequence-resync.md`, `computed-event-fanout.md`, `computed-seed-dispatch.md`, `cte-before-image-bulk-update.md`, `deferred-computed-drain.md`, `empty-table-seed-analyze.md`, `generated-column-insert-strip.md`, `hybrid-sync-budget-splitting.md`, `impact-hint-normalization.md`, `incoming-link-delete-choreography.md`, `insert-batch-bind-ceiling.md`, `link-exclusivity-validation.md`, `link-seed-batch-loading.md`, `linked-record-advisory-ladder.md`, `meta-touch-monotonic-clock.md`, `missing-host-table-tolerance.md`, `noop-write-guard.md`, `physical-row-duplication-plan.md`, `record-write-funnel.md`, `restore-identity-choreography.md`, `seed-field-expansion.md`, `sparse-batch-update-engine.md`, `trash-restore-cleanup.md`, `undo-row-snapshot-reconstruction.md`, `view-order-bootstrap.md`, `inline-outbox-drain-loop.md`, `sync-dirty-seed-cascade.md`: typed cell-value specs compile into one record UPDATE (system-column seeding, computed-field skip, changed-field ledger); link SETs route by relationship with the symmetric-FK `__id` guard; attachment cells replace via delete-then-insert keyed on (table,record,field); missing link titles fill in SQL from the foreign table; insert/delete/database/sql-literal value conversions share computed-never-written + JSON-stringify + explicit-NULL-cast invariants; link changes classify add/remove/replace/reorder and collect exclusivity constraints; condition specs compile to raw predicates with NULL-passing negative operators and format-derived date granularity; the where funnel converts the empty-where error to null; deep record pipelines handle batch binds, CTE before-images, seed expansion, sparse batch updates, view order bootstrap, inline outbox drains, and sync dirty-seed cascades.
- **Record read/query plane** — `record-query-read-plane`, `record-read-model-mapping`, `stream-pagination-strategies`, `attachment-lookup-service`, `user-lookup-service`, `date-search-range`: find/findOne/findStream assemble SELECTs with `__row_` order-column fallback, parallel rows+count, and array_position record-id ordering; rows map to read models with recursive legacy-avatar normalization; stream pagination selects offset vs cursor with shared limit math; attachment lookups run in the meta db with optional-column retry; users resolve across id/email/name; free-text date searches become half-open ranges gated by format presets.
- **Record write builders** — `insert-builder-dual-api`, `update-builder-impact-plan`, `explain-builder-reuse`: pure deterministic builders turn field values into compiled INSERT/UPDATE SQL plus side-effect statements and lock metadata WITHOUT executing — every field iteration ends in exactly one of value/statement/skip, and the same builders serve repositories and EXPLAIN analyzers byte-identically.
- **Read-path query builders** — `query-builder-mode-manager`, `same-table-cte-formula-batch`, `stored-ordering-contract`: a manager-created builder pair switches reads between computed laterals and stored columns behind prepare(deps)-before-build(); batched same-table formula recomputes compile into level-ordered CTEs with shared-subexpression reuse; stored reads reproduce v1 sort semantics through three-tier ORDER BY resolution.
- **Computed execution engine (updater internals)** — `dirty-frontier-propagation`, `conditional-before-image-propagation`, `same-table-collapse-guards`, `dirty-target-lock-choreography`, `computed-cell-safety-limit`, `distinct-host-key-probe`: ComputedFieldUpdater turns a plan into SQL by expanding seeds through generation-keyed frontier passes (identical edge selects dedup, stop at zero inserts), widening un-compilable conditional filters to all-target refresh while matching current OR jsonb-reconstructed before-image rows, collapsing pure same-record formula batches under formula-only/non-JSON/chunk guards, locking post-propagation dirty targets inside the write transaction, enforcing per-cell byte ceilings with link-projection exemption plus one deferred version bump, and probing distinct host-key cardinality before skipping record-id chunks.
- **Computed SELECT assembly (builder internals)** — `dirty-set-join-placement`, `set-based-rollup-ladder`, `link-join-ladder`, `lookup-rollup-json-aggregation`: ComputedTableRecordQueryBuilder filters dirty rows FIRST (outer join before laterals, repeated inside host sources), picks lateral/inner/hostLeft/hostKey join modes per relationship and condition shape with host-driven grouping preserving zero-match COUNT/SUM semantics, resolves link joins from one FK-location truth table shared across read/propagate/aggregate paths, and formats lookup/rollup values through storage-aware flattening and sanitize-then-coerce numeric ladders.
- **Computed app-layer orchestration (nestjs-backend services)** — `dual-phase-update-orchestration`, `field-change-impact-closure`, `all-records-fixpoint-bfs`, `link-cascade-frontier-bfs`, `reference-graph-cte-queries`, `conditional-failsafe-ladder`, `symmetric-planned-seeding`, `layered-evaluation-ordering`, `raw-op-publication-ladder`, `errored-field-update-gating`, `auto-number-cursor-paging`, `persisted-backfill-reentry`: the transaction orchestrator locks post-collection sorted targets, runs the caller's update, then publishes raw versionless ShareDB ops; impact closure grows monotonically to fixpoint over junction-table frontier BFS with a lazy ALL_RECORDS sentinel and an eight-rung widening ladder for conditional filters; evaluation runs in Kahn layers (lookups before formulas, flat-layer cycle fallback) paging by auto-number cursor and publishing hasOwnProperty-null raw ops.
- **Computed outbox trigger plane (v2 queue bridge)** — `wakeup-wire-contract`, `base-admission-lua-lease`, `deferred-wakeup-reason-ladder`, `post-process-drain`, `final-attempt-replay-rearm`, `fire-forget-publish-recovery`, `startup-redrive-reconciliation`, `role-aware-publish-capability`, `anomaly-grouping-recovery`, `outbox-health-classification`, `order-preserving-concurrency-map`, `trigger-role-config`, `computed-module-wiring`: versioned locator payloads ride BullMQ behind atomic Lua zset per-base admission leases; claim misses reschedule through a reason→delay ladder with deterministic time-bucketed ids while indefinite pauses park; final broker attempts re-arm durable 30s replays and startup/5-minute redrive sweeps rebuild delivery across meta-fallback plus every BYODB target under a pg advisory single-flight lock.

- **Insert execution choreography** — `bulk-insert-reuse`, `insert-exclusivity-choreography`, `insert-bind-limit-batching`, `generated-column-insert-strip-meta`, `generated-audit-column-strip`, `insert-restore-identity-dedup`, `link-title-fill-sql`, `physical-row-duplication`: bulk inserts reuse one builder then drain additional statements only after all main rows exist under foreign locks; exclusivity validates pre-INSERT while locks land post-INSERT pre-junction; batches respect the columns×rows parameter ceiling with cross-batch RETURNING accumulation; generated columns are stripped at both meta and physical levels; optional identity lookups never ride the transaction connection; missing link titles fill inside the main INSERT; whole-table duplication is a single ordered INSERT…SELECT.
- **Update execution semantics** — `update-one-mutation-applied`, `batch-update-aggregation`, `batch-existing-links-bulk-reader`, `batch-sql-composition-seam`: zero-row updates are success data (capture session aborted first); batch updates transpose per-record specs into ONE UPDATE…FROM(VALUES) with presence flags deciding clear-vs-keep and version bumped only in SET; existing link state prefetches one IN-query per relationship shape under a load-bearing all-keys-present contract; before-images extracted from step changes merge earliest-wins so multi-stage recomputes never lose the original filter old-values.
- **Record write execution plane** — `record-write-funnel`, `insert-batch-bind-ceiling`, `noop-write-guard`, `cte-before-image-bulk-update`, `generated-column-insert-strip`, `restore-identity-choreography`, `view-order-bootstrap`, `auto-number-sequence-resync`, `trash-restore-cleanup`, `computed-seed-dispatch`, `deferred-computed-drain`, `computed-event-fanout`, `seed-field-expansion`, `impact-hint-normalization`, `meta-touch-monotonic-clock`, `linked-record-advisory-ladder`, `link-exclusivity-validation`, `incoming-link-delete-choreography`, `missing-host-table-tolerance`, `link-seed-batch-loading`, `undo-row-snapshot-reconstruction`, `empty-table-seed-analyze`, `sparse-batch-update-engine`, `physical-row-duplication-plan`, `hybrid-sync-budget-splitting`, `inline-outbox-drain-loop`, `sync-dirty-seed-cascade`, `before-image-merging`: the pass-10 record/ repository sweep's stage-ordered write funnel (arm→DML→locks→statements→computed→finish), bind-ceiling batching, IS-DISTINCT-FROM noop guards, single-statement before-image bulk updates, dual-level generated-column strips, out-of-tx identity restores, lazy order-column bootstraps, sequence resyncs on restore, drain-until-empty computed runners, seed dispatch/expansion/normalization, monotonic meta-touch clocks, advisory-lock ladders, exclusivity validation, delete choreographies with self-referential carve-outs, recursive 42P01 tolerance, O(linkFields) seed loading, newest-first undo snapshot walks, conditional ANALYZE probes, VALUES-presence sparse updates, catalog-filtered duplication plans, hybrid budget splits with whole-stage requeue, delayed single-flight inline drains, sync dirty-seed fixpoints, and earliest-wins before-image merges (parity repair pass 14: wired to loader at HEAD without map entries).
- **Infra utilities & capability probes** — `pg-capability-identifier-hash`: Postgres features are detected by executing a probe (pg_input_is_valid), never by version sniffing, and over-long identifiers get deterministic FNV-1a base36 suffixes.
- **Undo capture** — `undo-capture-session`: record mutations arm the undo trigger through a local-then-global batch-id fallback and idempotent finish/abort that restore the previous batch id.
- **Meta-plane table repository** — `query-sql-error-diagnostics`, `query-table-cte-insert`, `query-restore-batch-stamp`, `query-version-touch-algebra`, `query-lateral-hydration`, `query-v1-filter-migration`, `query-field-deserialize-ladder`, `query-provision-ledger-recording`, `query-row-limit-plugin`, `query-schema-operation-ledger`: driver errors become redacted position-windowed diagnostics with FNV-1a fingerprints; inserts land as conditional CTE chains inside ambient or local transactions with post-commit db-name backfill; restore keys children by shared deletion timestamp while permanent delete probes optional trash tables via to_regclass; version chains reconstruct arithmetically from touch order × final versions; reads hydrate via jsonArrayFrom laterals under a primary-first ordering contract; legacy filters migrate at read time with uniform value-shape rules; field DTOs deserialize degrade-don't-fail trusting DB flags over payloads; bulk provision flips write per-table idempotency-keyed ledger rows; row ceilings enforce through policy-resolved plugins double-checked across phases; long-running schema work claims from a three-branch stale CTE ledger where attempts count in SQL.

- **V1 calculation & link derivation engine** — `ref-cte-four-way-or`, `topo-cycle-throw-contract`, `link-fk-single-source-of-truth`, `link-junction-order-reorder-ladder`, `link-plan-commit-split`, `link-delete-context-dual-sweep`, `batch-temp-table-update-ladder`, `batch-cls-rawop-stash`, `sysfield-tracked-lastmodified-dual-write`, `link-title-resolution-ladder`, `calc-diamond-merge-compose`, `calc-errored-gate-auxiliary-context`: the v1 features/calculation engine closes the field-reference graph with a four-orientation recursive CTE then dependency-first topo sorts that THROW localized errors on cycles and prepend synthesized seeds; link derivations trust FK storage as the only source of old keys, classify junction writes into pure-reorder (delete+ordered reinsert) vs membership-delta (pairwise diff, append after MAX(order)), split plan (in-memory symmetric patches) from commit (persist + re-fetch), sweep deletes through reference-graph AND raw-options channels incl. direct junction reads for inconsistent rows; bulk row updates stage in tx-scoped nanoid temp tables translating unique/not-null violations into field-named errors while raw ShareDB ops stash in CLS until commit; system last-modified columns always stamp but tracked LastModified* fields dual-write their own columns only on tracked-set intersection with empty-filter-degrades-to-all; link titles degrade formatter→primitives→array→key-ladder→id without throwing; diamond recomputes merge first-slot/last-value and compose same-path ops keeping FIRST od + LAST oi while deleting od==oi no-ops; auxiliary data widens to lookup filter operands before graph filtering and gates only derivation (not metadata) on errored fields.
- **Integrity check/repair plane** — `integrity-check-fix-dispatch`, `integrity-link-json-backfill`, `integrity-primary-promotion-ladder`, `integrity-orphan-purge-guards`, `integrity-filter-operator-prune`, `integrity-uniqueindex-emptystring-twins`, `integrity-symmetric-consistency-ladder`, `integrity-fk-column-restoration`: the repair loop re-runs one seven-detector check (per-field structural audit gated by host/column existence, cross-base inbound links included) and routes issues type-by-type so fixes never scan independently; lost FK storage rebuilds FROM surviving JSON cells via conservative idempotent SQL (NULLIF extraction, MIN(self_id) deterministic dedup, NOT EXISTS pair inserts across jsonb/json_each); broken primaries demote inside one outer tx to kept/promoted/mirror-formula outcomes preserving downstream references ("(before-fix)" rename only in fallback); dangling reference edges purge bidirectionally while junction orphans die by whereNotExists anti-join behind a hard junction_ naming guard; invalid filter operators prune recursively through OT ops with empty groups collapsing to NULL; UNIQUE indexes re-materialize under canonical DDL naming branched on id prefix as '' normalizes to NULL on non-computed text; vanished symmetric twins repair by declaring one-way; missing FK columns recreate with relationship-exact constraints (OneOne unique, one-way OneMany composite unique) tolerating 42P07 then always backfilling.
- **V2 integrity streaming plane** — `integrity-v2-crossbase-hydration`, `integrity-v2-preflight-gate`, `integrity-v2-sse-lifecycle`, `integrity-v2-repair-telemetry`: base-scoped verdicts hydrate the cross-base referenced-table closure first (unknown-base tables retry via withoutBaseId specs) so valid remote lookups are never "repaired"; a grouped meta preflight counts active/primary fields BEFORE hydration, excluding zero-field tables from specs while reporting them as manual-mode synthetic issues yielded first; long streams ride SSE with comment heartbeats and connect/complete/error lifecycle events on one grammar flushed per write; repair telemetry captures result errors and thrown exceptions to Sentry+spans then RE-THROWS — observability never converts failures into silent partial repairs.
- **Aggregation & statistics read plane** — `agg-sql-compile-ladder`, `agg-mcv-jsonb-join`, `agg-hostile-numeric-coercion`, `agg-percent-greatest-guard`, `agg-user-unique-id`, `agg-date-range-split`, `agg-attachment-size-boolean`, `agg-selection-slice`, `agg-group-points-fanout`, `agg-choice-order-sort`, `agg-search-timeout-gate`, `agg-searchindex-two-pass`, `agg-cache-me-key`, `agg-valid-func-table`, `agg-calendar-spine`, `agg-permission-projection`, `agg-tql-view-merge`, `agg-result-conversion`, `agg-rowcount-link-modes`, `agg-symbol-di-token`, `agg-group-points-cap`: the features/aggregation read surface compiles statistics requests through one builder (validity gated at BOTH api and SQL layers) into per-field raw SELECT expressions; multi-value jsonb cells aggregate via per-field CTE cross-joins with row-scoped exemptions and strip→NULLIF numeric coercion that can never error; percent family guards denominators with GREATEST(COUNT(*),1); user-family uniqueness extracts ids only when single-valued; date-range months split SQL-payload from dayjs decoding; selection chips slice [skip,take) by folding groupBy into orderBy over collapsed-group filters with the paginated table path forced at both layers; group points re-aggregate prefix slices hashing full paths into ids under a maxGroupPoints cap whose deficit becomes an explicit Unknown bucket sorted by choice-declaration ARRAY_POSITION ladders with doubled bindings; searches bound themselves with SET LOCAL statement_timeout duck-typed across three error shapes and resolve positions in a second ROW_NUMBER pass NaN-guarded against deletes; results cache 1h versioned by table mtime with Me-sentinel key inflation; the validity matrix, calendar generate_series spine, permission projection funnel, TQL/view-filter merge, result conversion ladder, link-cell row-count polarity modes, and symbol-token DI round out the plane.

- **V1 SELECT compiler stack (record query-builder + db-provider)** — `base-pagination-cte-split`, `aggregate-range-table-path-forcing`, `computed-pagination-fallback`, `query-state-manager-scoped-selection`, `original-source-latch`, `filter-augmented-selection`, `sort-selection-gate`, `link-cte-emission-choreography`, `materialized-aggregate-cte`, `conditional-equality-counts-plan`, `conditional-window-limit`, `multi-value-lookup-jsonagg`, `lookup-date-iso-normalize`, `link-cell-jsonb-strip-nulls`, `formula-expansion-context-swap`, `question-mark-literal-split`, `pg-multivalue-scalar-aggregators`, `binary-op-numeric-coercion`, `param-metadata-coercion-dispatch`, `loose-numeric-coercion`, `blank-aware-comparison`, `datetime-sanitize-tzwrap`, `month-diff-day-adjustment`, `truthiness-if-type-matrix`, `generated-column-immutability-contract`, `typed-null-error-degradation`, `rollup-function-support-matrix`, `flatten-nested-json-array`, `formula-select-expression-fallback`, `projection-dependency-closure`, `aggregate-group-choice-order`, `user-snapshot-select-json`, `link-cell-formatting-ladder`, `conditional-computed-target-resolution`, `conditional-aggregate-cast-envelope`, `select-expression-alias-binding`, `link-cte-column-contract`, `conditional-equality-fallback-coalesce`, `view-context-raw-selection`, `conditional-lookup-scalar-extractor`, `conditional-rollup-ordering-functions`, `link-relationship-join-matrix`, `conditional-lookup-warning-degradation`, `aggregate-builder-composition-order`, `generated-column-countall-multivalue`, `fromnow-tonow-direction-contract`, `select-error-function-semantics`, `conditional-filter-field-reference-context`, `lookup-order-column-tiebreak`, `conditional-lookup-dual-column-emission`, `formula-support-validator-family-vetoes`, `aggregate-selection-slice-projection`, `datetime-format-offset-suffix`: the v1 app-layer read compiler turns views/filters/sorts/aggregations into Postgres through a split BASE-pagination CTE (limit lands pre-CTE, offset post-CTE, 'full' mode for range aggregates; computed-in-predicate queries degrade to unpaged) driven by a single-writable manager whose scoped child throws on writes and whose original-source latch keeps nested link CTEs reading the paged physical table; per-link CTEs (main_record_id/link_value/lookup_/rollup_ columns, junction vs FK join matrix, MATERIALIZED conditional counts plans with ROW_NUMBER top-N and semantic fallback defaults) are joined back once and read through typed-null degradation, deterministic order ladders, ISO-UTC date normalization for array filters, and jsonb_strip_nulls {id,title} cells; the formula plane recompiles nested formulas with cached cycle-checked context swaps, splits '?' literals away from knex binds, intercepts 22 scalar functions into per-element STRING_AGG aggregators over pg_typeof-guarded jsonb coercion, coerces numbers never-throw via COLLATE "C" regex CASEs, unifies ''/NULL blanks in comparisons, trusts-or-sanitizes datetimes through BTRIM+pattern guards with hand-rolled month diffs and pinned FROMNOW/TONOW direction, and types IF()/SWITCH() branch-by-rung while the generated-column twin keeps everything immutable behind a six-family validator veto pipeline.
- **Record-history cold storage plane (buffer→S3 tiering)** — `cold-history-config-env-ladder`, `cold-part-key-grammar`, `cold-bloom-double-hash`, `cold-ndjson-line-splitter`, `cold-zstd-gzip-negotiation`, `cold-part-writer-verify-delete`, `cold-truncation-marker-contract`, `cold-spill-swap-before-await`, `cold-fanin-floor-two`, `cold-sort-memory-budget-global-eviction`, `cold-feeder-foldback-eof-init`, `cold-module-split-consumer-guard`, `cold-scheduler-add-only-no-removal`, `cold-catchup-chain-hop-id`, `cold-discovery-loose-index-scan`, `cold-byodb-bookmark-monotonic`, `cold-orphan-vs-byodb-exclusion`, `cold-read-batch-adaptive-limit`, `cold-bucket-coverage-triple-match`, `cold-storage-write-vs-read-cache-discipline`, `cold-topk-month-atomic-timeout`, `cold-pruning-advisory-default-scan`, `cold-cursor-dual-dialect-inclusive-legacy`, `cold-merged-read-buffer-first-seam`, `cold-missing-part-relist-retry-once`, `cold-buffer-delete-repeatable-read`, `cold-truncation-render-guard`, `cold-compactor-force-repair`, `cold-flush-run-budget-atomicity`, `cold-heal-consumed-keys-scoping`: the record_history write buffer tiers into zstd/gzip NDJSON parts under a minRecordId-bearing key grammar (day buckets in the 30d window, month files beyond) via daily flush jobs that discover work through a recursive loose index scan and BYODB bookmarks probed without waking idle tenant dbs; bucket rewrites fold existing parts back through a globally budgeted external sorter (sync-swap spills, floor-2 multi-pass merges, readline-free NDJSON splits) whose consumed-keys set is the ONLY authorization for healing deleted keys and rewriting `_stats.json` entries; uploads verify by re-download with 4-way count/sha/footer agreement before any buffer row is count-gated deleted inside one REPEATABLE READ transaction; merged reads serve buffer-first pages with COLLATE "C"-stable pagination, advisory fail-open pruning over key ranges/bloom filters/stats sets, atomic-month scans under an S3 time budget, and render-safe truncation markers honored end-to-end.

- **Record-history buffer write plane (capture → envelope → GC)** — `history-buffer-capture-funnel`, `history-envelope-dual-dialect`, `history-v2-background-deferral`, `history-v2-projection-trio`, `history-select-minimize-twin`, `history-ops-to-event-pipeline`, `history-id-gc-lifecycle`: every cell mutation reaches the append-only `record_history` buffer through one of three writers — the v1 ShareDB-op listener (post-commit, batched field loads, five-step filter ladder, schema-qualified raw insert) or two of the three v2 projection handlers (background-deferred, event-snapshot actor attribution, visitor-extracted v1-compat metadata) — all writing `{meta,data}` envelopes EXCEPT the createRecordsOnlySql fast path's bare `{data}` dialect; select-choice minimization, the `rhi`+24 id grammar, the single config-load-time kill switch, and the hot-buffer-then-S3 post-purge GC order complete the lifecycle.
- **BYODB space data-DB migration plane (features/space, pass 19)** — `byodb-related-spaces-closure`, `byodb-job-claim-cas`, `byodb-base-schema-copy-ladder`, `byodb-copy-pipeline-supervisor`, `byodb-snapshot-delta-replay`, `byodb-migration-progress-model`, `byodb-copy-validation-kernel`, `byodb-write-freeze-gate`, `byodb-atomic-switch-rollback-proof`, `byodb-target-artifact-retry-lifecycle`, `byodb-target-capability-probes`, `byodb-write-guard-baseline`, `byodb-shared-table-scoping`, `byodb-heartbeat-failure-forensics`, `byodb-admin-fence`, `byodb-repair-mode-legacy-seqs`, `byodb-job-record-state-contract`, `byodb-shared-copy-strategies`, `byodb-worker-loop`, `byodb-secret-and-preflight`: a 8,554-line orchestrator moves a whole cross-space component from shared/meta storage onto a tenant's own Postgres: related-spaces BFS fixes WHO moves; admin-only entry gates + dual fingerprints fix WHERE; capability probes (pg_trgm opclass availability with search-path fallback, server-side disk df via COPY FROM PROGRAM, scoped conflict counts) fix WHETHER the target qualifies; three drains (schema operations, provisioning+BullMQ import jobs, computed outbox leases) freeze writers with stale-row tolerance; an exported repeatable-read snapshot plus per-job WHEN-guarded delta triggers keep pg_dump/psql-COPY copies convergent through baseline-relative replay; batched catalog signatures and order-independent multiset content hashes prove equivalence before ONE transaction flips connection+bindings to byodb after route smoke; rollback demands a zero-finding proof (inventory, counts, timestamps vs switchedAt) run fail-closed; retries clean only same-fingerprint non-switched predecessors in dependents-before-sources priority with TRUNCATE gated on unbound targets.
- **Airtable import plane (features/airtable-import, pass 21)** — `airtable-import-phase-ladder`, `airtable-analyze-endpoint`, `airtable-import-authz`, `airtable-sse-import-stream`, `airtable-api-client-discipline`, `airtable-record-streaming`, `airtable-bounded-concurrency-map`, `airtable-attachment-watchdog`, `airtable-cell-conversion`, `airtable-user-resolution`, `airtable-field-mapping-ladder`, `airtable-link-planning`, `airtable-link-row-spill`, `airtable-link-fill`, `airtable-derived-fields`, `airtable-formula-translator`, `airtable-rollup-mapper`, `airtable-share-client`, `airtable-view-config-mapping`, `airtable-view-config-apply`, `airtable-issues-ledger`, `airtable-ai-field-upgrade`: one route pair moves an Airtable base into teable through a degrade-never-abort phase ladder — pure-planner analyze/dry-run and scope-parity authz up front; a throttled stall-guarded official-API client streams records in reverse-batch creates with id-map-filtered iterator restarts while attachments transfer CDN→storage under a chunk-refreshing watchdog at bounded ordered concurrency; link cells spill as JSONL parts into blob storage and fill back through old→new id maps with drop/truncate/missing accounting; lookups/counts/rollups/formulas materialize late (formulas via no-progress-bounded dependency passes) with translate-or-refuse formula/rollup bridges falling back to typed snapshots; opt-in view-config import scrapes the undocumented share endpoint with token-level branch dropping and maps filters/sorts/groups drop-and-report; every degradation lands in ONE issues ledger summarized severity-split.
- **Auth & permission plane (features/auth, pass 22, dedicated miner-teable lane)** — `auth-permission-guard-ladder`, `auth-share-scope-ceiling`, `auth-session-store-dual-key`, `auth-strategy-chain-fallback`, `auth-jwt-internal-token-kinds`, `auth-signin-lockout-attempts`, `auth-oauth-state-callback`: authentication is a passport strategy CHAIN (cookie session over a custom dual-key revocable store → Bearer PAT → Bearer JWT → explicit anonymous sentinel) where every validate re-reads account state from the DB, while authorization is one PermissionGuard ladder whose fixed order makes share links a ceiling for authenticated users, anonymous users resolve through template/USER gates, and standard checks fall back to PUBLIC share/template auth only when the decorator allows; share permission families re-verify resource membership against cross-base replay, scope node shares via cls-cached subtree closures with link-target escape hatches, gate edits behind logged-in identity minus an exclusion set, password-protect via JWT cookies checked against the live DB password, and confine X-Tea-Share-View to an explicit method×path×permission allowlist; internal JWT kinds carry base-scoped robot identities into CLS ahead of authorization; local sign-in throttles brute force through a dual-knob atomic-INCR lockout ladder that masks every cause behind one generic error; OAuth flows keep state consume-once in cache and validate callback redirects by placeholder-origin path parsing.

## Extending the foundation
Add one `./<seam>.md` capsule for one graph-selected, source-confirmed porting question. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf.

## Provenance
teable (AGPL), `develop@06a4461e2bc53055182d4df0a72dffa26fd99210`; Codebase Memory project `teable` (full index, generation 2026-08-23T00:04:59Z; parse_partial caveats confined to SQL-template-literal lines and migration SQL — see each capsule; `record-history-flusher.service.ts` flagged at :363 inside a SQL template literal — content verified against working tree). Pass 22 (2026-08-26, dedicated miner-teable lane FAC-105): first pass WITH a durable work record (inspo/teable-work/{state,research,verification}.md created; shared-ledger row reconciled from stale 0/0/0 against the real 388→395-ref leaf); mined the never-cited features/auth plane whole (+7 capsule-v2 in the new Auth & permission plane map group: guard ladder, share scope ceiling, session store dual-key revocation, multi-strategy identity chain, JWT internal robot tokens, sign-in lockout ladder, OAuth state/callback guards); graph re-verified pin-current live (head==base==06a4461e, ready FULL 95,388n/404,072e, zero drift vs Full view) with 18/18 cited paths no_recorded_issue at that generation; three direct spec files read whole (permission.guard.spec anyPermissions error-preservation trio, session-store.service.spec tombstone/index pins, local.strategy.spec lockout quintet); vitest runner still BLOCKED (root node_modules now partially provisioned but tooling-only — no vitest binary anywhere; installing into the checkout is lane-forbidden) → deterministic byte-check + behavioral probes per fallback protocol; server-side automation/webhook confirmed ABSENT at this pin (openapi stubs + frontend panel only), recorded as omitted-with-reason. Pass 21 (2026-08-24, dedicated teable→grist→dub lane cron 309d0085081b): all three rotation repos verified pin-current (fetch rev-list 0 on teable develop / grist main / dub main) so no drift plane existed anywhere; fresh-eyes citation census found the never-cited `features/airtable-import/` plane (13 prod files ~4.5kL, ZERO prior citations) and mined it whole-file → +22 capsule-v2 in the new Airtable import plane map group (335→357); probe-first discipline caught and repaired TWO invented expectations pre-commit (`byCode=` count and `buildAirtableImportPlan` count both re-derived against live grep); search_graph battery live-resolved rank-1 for every planned seam symbol; adversarial wrong-project probe (same query on grist-core) returned grist's own client-side AirtableImporter with positive control green in-project — project routing in Retrieve blocks is load-bearing; coverage stdin-JSON ×12 cited paths ALL no_recorded_issue metadata_match generation_matches=true; runner BLOCKED this window (repo ships no node_modules; CBM CLI search_code down with server-side "cannot create temp file (Disk quota exceeded)" under fleet /tmp pressure even with lane TMPDIR override) → deterministic probe battery executed byte-exact instead, per protocol. Pass 20 (2026-08-24, frontier-platforms lane): mined the never-cited `features/space/` BYODB plane — `space-data-db-migration.service.ts` 8,554L read whole plus all plane satellites (constants 51L, progress 250L + spec, related-spaces 392L + spec, copy-plan 647L + spec, copy-service 353L + spec, process-runner 1,167L + 26-test spec, worker 101L + spec, guard 290L, data-db-migration 318L, internal-schema/url-secret/baseline) → +20 capsule-v2 in the new BYODB space data-DB migration plane map group (328→348); REAL RUNNER vitest standalone venv (/var/tmp/tvp, vitest 3.2.4 + experimentalDecorators tsconfig): progress 11/11 + copy-plan 24/24 + copy-service 15/15 + process-runner 26/26 + worker 3/3 = 79 GREEN at pin (repo clone ships no node_modules; bun install EDQUOT on sheetjs CDN tarball — recorded); adversarial grist-core search_code total:0; coverage stdin-JSON ×9 cited paths no_recorded_issue metadata_match generation_matches=true. Pass 18 (2026-08-24, dedicated teable→grist→dub lane): executed the standing target — the never-cited `features/record-history-cold/` cold-storage plane (11 prod files ~3.7kL + 1,271L direct spec, ALL read whole) → +30 capsule-v2 in the new Record-history cold storage plane map group (298→328). Pass 19b (2026-08-24, same dedicated lane, successor fire): executed the pass-18 queued target #2 under the posed history-capture porting question — the never-cited record-history BUFFER write side mined whole-file (`event-emitter/listeners/record-history.listener.ts` 174L + `features/v2/v2-record-history.service.ts` 652L + its 313L vitest spec + the third writer `record.service.ts createRecordsOnlySql` :1397–1502 + emission side `event-emitter.service.ts` 421L / `share-db.service.ts` :80–93 + infra `packages/v2/core/src/ports/ExecutionContext.ts` scheduler ladder) → +7 capsule-v2 in the new Record-history buffer write plane map group (328→335); probes measured pre-write, spec-pinned deferral contract; runner BLOCKED (no node_modules — standing ruling), deterministic probe battery green ×2. Pass 16 (2026-08-24, deepening lane C [cron 17824924dcff]) mined the never-cited `features/aggregation/` plane whole-file (aggregation.service.ts 1,332L, interface/provider/symbol/module/index, open-api controller+service) plus its compiler substrate (`db-provider/aggregation-query/` abstract + postgres function/adapters, performance-cache trio, tql.pipe, filter-has-me, threshold.config) and the group-points twin in record.service.ts → +21 capsule-v2 in the new Aggregation & statistics read plane map group; retrieval caveat recorded: search_graph BM25 sits at the 50-hit noise floor on this repo, search_code resolves every cited symbol line-exact. Pass 17 (2026-08-24, dedicated teable→grist→dub lane): census-refuted closure — filename-granular citation-vs-inventory over apps/nestjs-backend/src + packages/v2 + packages/core/src exposed the never-entered v1 SELECT compiler stack (`features/record/query-builder/` ~13.4kL + `db-provider/select-query` 2,080L + `db-provider/generated-column-query` 1,645L + validator/dialect/utils planes) → +53 capsule-v2 in the new V1 SELECT compiler stack map group; direct upstream specs exist and were pinned (select-query.postgres.spec.ts, generated-column-query.postgres.spec.ts, pg-record-query-dialect.spec.ts, record-query-builder-group-quoting.spec.ts, formula-support-generated-column-validator.spec.ts). Pass 4 (2026-08-23) mined the never-cited `packages/v2/adapter-table-repository-postgres/src/schema/` plane whole-file (rules kernel/resolver/planner/checker/repairer, field rule family incl. JunctionTableRule 1251L, visitors incl. FieldTypeConversionVisitor 3251L + TableSchemaUpdateVisitor 2456L, PostgresTableSchemaRepository update pipeline) → 30→53 capsule-v2. Pass 5 (2026-08-23) mined the never-cited `packages/v2/adapter-table-query-ops-postgres/src/` plane whole-file (searchVector.ts 2906L, indexInspection.ts 526L, planValidation.ts 263L, executor.ts 205L, repositories.ts 462L, schema.ts 248L, searchAccessPathCapability.ts 112L, searchVectorStatus.ts 76L, searchVectorMaintenance.ts 131L, register.ts 166L, helpers.ts 26L) → 53→67 capsule-v2. Reconciliation addendum (2026-08-23, concurrent-lane overlap): an independent fire authored the same plane in parallel; 11 fully-overlapping capsules were deleted keeping the sibling's committed stronger-probed versions (f653ece), and ONE genuinely additive seam survived the symbol-level overlap check — `real-ddl-plan-gate` (validateRealDdlSearchVectorPlan / assertRealDdlPlanEvidenceReady / validateSubstringResultCompatibility / chooseNextAction, cited nowhere else) — wired into loader + map → 67→68 capsule-v2. Pass 6 (2026-08-23, platforms rover lane [DONE:150]) mined the never-cited `packages/v2/table-query-ops/src/` domain kernel whole-file (domain.ts 1383L, application.ts 472L, queryConfigShape.ts 418L, indexSetPlanner.ts 235L, recordQueryObservation.ts 270L, sqlDiagnostics.ts, runners.ts, di.ts, relationFieldConfigObservation.ts, savedViewConfigObservation.ts, searchVectorDefinition.ts, searchVectorValidation.ts, searchVectorSchemaMaintenance.ts) plus the never-cited meta-plane adapter seams (`adapter-db-postgres-shared/src/PostgresSqlExecutionError.ts` 160L, `adapter-repository-postgres/src/repositories/PostgresSchemaOperationRepository.ts` 509L + PostgresTableRowLimitPlugin.ts 185L + PostgresTableRepository.ts 2,346L read whole) → 68→93 capsule-v2. Pass 7 (2026-08-23, teable→grist→dub dedicated lane [DONE:157]) executed the recorded pass-6 targets: mechanical citation-vs-inventory grep over `adapter-repository-postgres/src`, `adapter-db-postgres-shared/src`, `table-query-ops/src` (de-noised suffix matching) exposed the never-cited repositories/visitors plane; whole-file reads of naming.ts, tableDbMeta.ts, PostgresBaseRepository.ts 179L, TableFieldPersistenceBuilder.ts 640L, FieldStorageTypeVisitor.ts 226L, TableWhereVisitor.ts 716L, TableMetaUpdateVisitor.ts 990L, TableRecordConditionWhereVisitor.ts 1,528L, TableRecordSelectColumnsVisitor.ts 172L, relationFieldConfigObservation.ts 240L → 8 new capsule-v2 (93→101). Symbol-level overlap adjudication per the [DONE:144] protocol: my authored `relation-config-observation` fully overlapped the sibling's committed `query-config-observation-twins` and was deleted keeping theirs; the sibling's three pre-wired loader lines for my in-flight filenames were kept and refined to match capsule content. OMITTED-WITH-REASON: `adapter-db-postgres-shared/src/config.ts` + `adapter-repository-postgres/src/config.ts` (19–47L env plumbing), `db/tableDbMeta.ts` (12L type aliases), `ports.ts` (289L thin hexagonal interfaces whose adapters are already cited), spec files of cited sources (fixtures for the pinned probes). DISCOVERED FOR NEXT PASS: `adapter-table-repository-postgres/src/record/visitors/TableRecordConditionWhereVisitor.ts` is a 2,400-line SUPERSET twin of the mined 1,528-line condition visitor (+872L diff: recursive effective-lookup unwrapping via resolveEffectiveLookupField, link display-value handling, v1-parity array coercion) — the record-side read plane (`PostgresTableRecordRepository.ts` 4,212L parse_partial at 8 ranges, `PostgresRecordOrderCalculator.ts`, undo capture infra) is the largest remaining unmined mass. → 101 capsule-v2. Pass 8 (2026-08-23, platforms rover lane) executed the queued citation-vs-inventory grep over `record/` FIRST — it exposed 62 never-cited files (~26.7k LOC) that the pass-7 sweep never entered. Mined whole-file: the `record/visitors/` value-mutation family (CellValueMutateVisitor 926L, FieldInsertValueVisitor, FieldDeleteValueVisitor, FieldDatabaseValueVisitor, FieldSqlLiteralVisitor, LinkChangeCollectorVisitor, LinkExclusivityConstraintCollector) + `record/attachments/attachmentTableMutations.ts` + `record/buildFilledLinkValueExpression.ts` + `record/computed/isPersistedAsGeneratedColumn.ts` + named target #2 `TableRecordConditionWhereVisitor.ts` (2,400L whole) + named target #3 read-plane (`PostgresTableRecordQueryRepository.ts` 1,056L, `PostgresRecordOrderCalculator.ts`, `PostgresRecordMutationSnapshotCaptureService.ts`, `PostgresAttachmentLookupService.ts`, `PostgresUserLookupService.ts`, `buildRecordWhereClause.ts`, `dateSearchRange.ts`, `CursorStreamPaginationStrategy.ts`, `OffsetStreamPaginationStrategy.ts`) → 101→118 capsule-v2. Probes pinned to the real spec/pglite suites. `record-order-fractional` already existed (leaf map) — the order-calculator seam was NOT duplicated. Loader/map parity 118=118=118. Pass 10 (2026-08-23, platforms rover lane) re-ran the mandated citation-vs-inventory grep over `record/` at the UNCHANGED pin `06a4461e` (95,388-node graph) — it exposed the never-cited `record/query-builder/**` plane plus the repository orchestrator and strategy trio. Whole-file reads: `PostgresTableRecordRepository.ts` ALL 5,298L (insert :1120–1348, insertMany :1380–1756, duplicatePhysicalRows :1762–1862, insertManyStream :1864–1974, updateOne :2068–2235, updateMany :2237–2476, updateManyStream :2478–2867, deleteMany :3049–3281, computed runners :3397–4062, module-level lock/exclusivity/delete-support helpers :4065–5298), `BatchUpdateSqlBuilder.ts` ALL 533L, `SyncInTransactionStrategy.ts` ALL 189L, `IUpdateStrategy.ts`, `HybridWithOutboxStrategy.ts` ALL 822L → 27 new capsule-v2 (118→145): record-write-funnel (arm→DML→locks→statements→computed→finish stage order, abort-before-error), insert-batch-bind-ceiling (union-width ÷ 30k clamped [1,500]), noop-write-guard (IS DISTINCT FROM OR-ladder + mutationApplied:false), cte-before-image-bulk-update (WITH matched UPDATE…FROM single-statement old+new), generated-column-insert-strip (T6146 information_schema is_generated probe), restore-identity-choreography (out-of-tx actor lookups, three-way fallback), view-order-bootstrap (lazy __row_ columns + hashed index names + max+index append), auto-number-sequence-resync (setval(GREATEST(max,1),true) gated on restore), trash-restore-cleanup (aggregate GC only when fully drained), computed-seed-dispatch (mode×changeType matrix, hybrid defers deletes, batch-by-ids sync-only unless forceOutbox), deferred-computed-drain (accumulate-then-settle streams, enqueue vs fire-and-forget), computed-event-fanout (derived newVersion=old+1, registerAfterCommit publish), seed-field-expansion (computed-dependents union + formula AST fallback), impact-hint-normalization (links unioned into values for planning while keeping raw split), meta-touch-monotonic-clock (GREATEST(now, prev+1ms)), linked-record-advisory-ladder (sorted dedup pg_advisory_xact_lock unnest), link-exclusivity-validation (batch-check + grouped probes, expected-vs-holder vs not-in-self asymmetry), incoming-link-delete-choreography (seed BEFORE cleanup, self-referential carve-out), missing-host-table-tolerance (preflight + recursive 42P01 classifier), link-seed-batch-loading (O(linkFields) queries, total-domain maps), undo-row-snapshot-reconstruction (newest-first first-parseable walk, version-missing hard error), empty-table-seed-analyze (EXISTS probe → one conditional ANALYZE), sparse-batch-update-engine (VALUES presence flags + CASE-keep SETs, BatchUpdateSqlBuilder whole), physical-row-duplication-plan (catalog-filtered INSERT…SELECT ORDER BY __auto_number), hybrid-sync-budget-splitting (dirty-budget level prefixes, lock-conflict ⇒ whole-stage outbox requeue returning partial ok), inline-outbox-drain-loop (delayed single-flight drain-until-empty, tx-stripped context), sync-dirty-seed-cascade (observe-dirty→replan→updatedFieldIds filter fixpoint). An EXTERNAL WRITER wired 26 of the 27 loader lines mid-flight (recorded pattern); parity closed to 145=145=145 mechanically. OMITTED-WITH-REASON: `query-builder/{insert,update}/RecordInsertBuilder.ts`+`RecordUpdateBuilder.ts`+`BatchRecordUpdateBuilder.ts` bodies (their OUTPUT contracts pinned via repository + builder-spec suites; builder internals remain a named next-pass target), `query-builder/stored/*`+`computed/ComputedTableRecordQueryBuilder.ts` family (SELECT-side query builders adjacent-mined via record-query-read-plane; distinct seam left for a read-builder pass), `ComputedFieldUpdater.ts`/`ComputedUpdatePlanner.ts` internals (already cited), `userSnapshotSql.ts`/`dateLikeOrderBy.ts` (small helpers consumed inside cited flows), spec files (probe fixtures). Gates green (check-integrity 6 packs, quality-gate no failures, hygiene clean for lane paths). → 145 capsule-v2. Pass 11 (2026-08-23, deepening lane C): mechanical parity grep exposed the pass-10/lane-B fallout — 15 authored capsule-v2 builder-plane refs sat UNTRACKED and unwired while the leaf claimed their seams omitted-with-reason; all 15 verified against source pins (`develop@06a4461e`) and wired into loader + three new map groups (Record write builders / Insert execution choreography / Update execution semantics) → 160=160=160. OMITTED-WITH-REASON after this pass: `query-builder/stored/*` + `ComputedTableRecordQueryBuilder.ts` (SELECT-side read-builder seam, distinct from the now-mined write builders), `ComputedFieldUpdater.ts` internals, `userSnapshotSql.ts`/`dateLikeOrderBy.ts` helpers consumed inside cited flows. Pass 12 (2026-08-23, platforms rover lane, [DONE:196]): scratch-recovery pass executing the [DONE:173] concurrent-lane-B addendum duty — diffed `~/.pi/work/foundations-deep-farm/scratch-teable-pass8-laneB/` (23 capsule-v2 authored by the parallel pass-8 lane-B fire) and mechanically adjudicated ALL 38 candidate drafts (23 scratch + 15 pass-10 fallout already rescued by lane C) against the 160-capsule inventory via symbol-level citation grep. 25 REJECTED as fully-covered duplicates (batch-insert-sizing-restore ⊂ insert-batch-bind-ceiling; cell-mutate-set-clauses ⊂ cell-value-mutate-visitor; serialization-visitor-quartet + user-snapshot-sql-ladder ⊂ field-value-visitors; link-change-classifier ⊂ link-change-collection; snapshot-capture-session ⊂ undo-capture-session + record-write-funnel; record-condition-where-v2-superset ⊂ record-condition-where-compilation; insert-value-two-phase ⊂ field-value-visitors; plus attachment-side-table-replace, batch-update-values-presence, date-search-range-ladder, empty-where-null-contract, generated-column-duality, computed-run-wakeup-context, delete-link-cleanup-descriptors, link-exclusivity-descriptors, link-title-fill-expression, sql-literal-cast-ladder, insert-restore-identity-dedup, bulk-insert-reuse, explain-builder-reuse, generated-column-insert-strip-meta, generated-audit-column-strip, insert-bind-limit-batching, insert-exclusivity-choreography, insert-builder-dual-api, update-one-mutation-applied, physical-row-duplication, link-title-fill-sql, update-builder-impact-plan, batch-existing-links-bulk-reader, batch-update-aggregation — each owned by its committed counterpart). 5 ADOPTED as genuinely additive seams cited NOWHERE in the prior pack: `before-image-merging` (ComputedBeforeImageFromChanges.ts 123L whole-file; extract-from-step-changes + earliest-wins merge keeps ORIGINAL filter old-values across sync→outbox stages; direct test __tests__/ComputedBeforeImageFromChanges.spec.ts), `pg-capability-identifier-hash` (utils/detectPgCapability.ts 69L capability-by-execution probe + shared/sqlIdentifiers.ts 61L FNV-1a base36 suffixing; direct test detectPgCapability.spec.ts), `query-builder-mode-manager` (TableRecordQueryBuilderManager.ts 120L prepare(deps)-before-build() computed/stored switch; ITableRecordQueryBuilder `__row_${string}` system columns), `same-table-cte-formula-batch` (SameTableBatchSqlPlan.ts 143L + SameTableBatchQueryBuilder.ts 838L fragments→CSE-bindings→leveled CTEs; direct tests SameTableBatchQueryBuilder.spec.ts + __tests__/SameTableBatch.spec.ts), `stored-ordering-contract` (StoredTableRecordQueryBuilder.ts 485L three-tier ORDER BY resolution incl. dateLikeOrderBy/userSnapshotSql helpers; direct test StoredTableRecordQueryBuilder.spec.ts) — every pinned symbol verified line-exact at pin `06a4461e` (loadExistingLinkRecordIdsBatch :4387, buildInsertData :192/build :442, stripPhysicallyGeneratedColumnsFromInsertValues :461, duplicatePhysicalRows :1762, updateOne :2068, hashIdentifier constants :10–13) before adoption → wired into loader + map (NEW Read-path query builders group; Update execution semantics + Infra utilities groups extended) → 160→165 = 165 loader = 165 map, all capsule-v2. This partially supersedes pass-11's stored-builder omit: the ORDER-BY half is mined; `ComputedTableRecordQueryBuilder.ts` internals stay omitted-with-reason (computed SELECT assembly distinct from the batching plan). Pass 13 (2026-08-24, dedicated teable→grist→dub lane): depth re-entry at the UNCHANGED pin — read BOTH standing giant omissions whole-file (`ComputedFieldUpdater.ts` 2,785L, `ComputedTableRecordQueryBuilder.ts` 2,968L) and mined their never-cited seams into 10 new capsule-v2 (165→175): dirty-frontier-propagation, conditional-before-image-propagation, same-table-collapse-guards, dirty-target-lock-choreography, computed-cell-safety-limit, distinct-host-key-probe (updater internals); dirty-set-join-placement, set-based-rollup-ladder, link-join-ladder, lookup-rollup-json-aggregation (builder internals). Every decisive anchor verified line-exact at pin; all Retrieve probes live-executed against the `teable` graph. Wired as two new map groups (Computed execution engine / Computed SELECT assembly). Pass 14 (2026-08-24, platforms rover lane): executed target #3 — the never-cited app-layer `record/computed/services/` orchestration plane (~4.0kL, all 9 files read whole) — into 12 capsule-v2 (dual-phase-update-orchestration publish-only doctrine + capability-gated sorted locking; field-change-impact-closure lazy ALL_RECORDS seeding; reference-graph-cte-queries four SQL shapes incl. dialect-split JSON accessors; conditional-failsafe-ladder eight-rung widening + typed CAST VALUES post-probe; symmetric-planned-seeding old∪new link targets; all-records-fixpoint-bfs monotone re-closure; layered-evaluation-ordering Kahn layers + flat-layer cycle fallback, DIRECT spec computed-evaluator.service.spec.ts pins stored-lookup inversion; raw-op-publication-ladder hasOwnProperty-null + __prev_version; errored-field-update-gating SET-list matrix; auto-number-cursor-paging JS-resort-before-handle; persisted-backfill-reentry no-op-callback pipeline reuse; trigger-role-config env contract) AND target extension — the never-cited `features/v2/computed-outbox-trigger/` BullMQ bridge plane (~2.4kL prod, 9 spec suites ×40 tests read) — into 12 more (wakeup-wire-contract versioned locator schema; base-admission-lua-lease atomic zset lease + XX renewal + permit.assertActive ladder, 12-spec direct suite; deferred-wakeup-reason-ladder reason→delay table + deterministic bucketed ids, 19-spec handler suite; post-process-drain T6191 continue-after-progress; final-attempt-replay-rearm UnrecoverableError validation gate + 30s durable replay; fire-forget-publish-recovery single recovery circuit + retention-by-id-class; startup-redrive-reconciliation cuwr2 revision-keyed replays across meta-fallback+BYODB under pg advisory single-flight; role-aware-publish-capability AsyncLocalStorage consumer scope; anomaly-grouping-recovery truncating composite group keys; outbox-health-classification critical/degraded composition; order-preserving-concurrency-map; computed-module-wiring narrow exports). Coverage stdin-JSON ×9 paths no_recorded_issue/metadata_match/generation_matches=true; adversarial wrong-project retrieval grist-core total:0; runner BLOCKED (clone ships no vitest/node_modules) → deterministic needle greps + live graph probes recorded per protocol. Wired as two new map groups (Computed app-layer orchestration / Computed outbox trigger plane). Pass 15 (2026-08-24, deepening lane C): mechanical citation-vs-inventory grep over `apps/nestjs-backend/src/features` (basename matching vs full reference corpus + leaf) exposed the never-entered v1 `features/calculation/` engine (~3.9kL prod) and `features/integrity/` repair plane (~3.5kL prod) — the whole app-layer cluster prior passes' packages/v2 focus had skipped; concurrent-sibling protocol executed (platforms rover landed its own pass-14 [DONE:244] mid-run with 3 name-overlapping but symbol-DISTINCT capsules; adjudicated at symbol level, zero deletions). 24 new capsule-v2 (200→224): calculation plane (ref-cte-four-way-or undirected closure via orientation permutations; topo-cycle-throw-contract throw-at-plan + prepend synthesized seeds; link-fk-single-source-of-truth storage-not-cell-JSON old keys w/ reverse-index duplicate rejection; link-junction-order-reorder-ladder pure-reorder delete+reinsert vs membership delta append-after-MAX(order); link-plan-commit-split planDerivateByLink in-memory patches vs commitForeignKeyChanges + mandatory re-fetch on persist path; link-delete-context-dual-sweep reference-graph+options-scan channels + getDirectForeignKeys inconsistency tolerance; batch-temp-table-update-ladder tx-scoped nanoid temp tables + metadata-driven constraint translation; batch-cls-rawop-stash CLS tx.rawOpMaps deferral; sysfield-tracked-lastmodified-dual-write tracked-set intersection gating + empty-filter-degrades-to-all + sanitized cloned audit snapshots; link-title-resolution-ladder formatter→primitives→array→key-ladder→id degrade-don't-throw; calc-diamond-merge-compose first-slot/last-value merge + FIRST-od/LAST-oi composition + no-op elimination; calc-errored-gate-auxiliary-context widen-before-filter auxiliary assembly + hasError derivation-only gating) and integrity planes (check-fix-dispatch single-work-generator switch incl. cross-base inbound scan; link-json-backfill conservative NULLIF/MIN-self_id/NOT-EXISTS SQL across jsonb/json_each; primary-promotion-ladder demote-in-outer-tx kept/promoted/mirror outcomes preserving references; orphan-purge-guards bidirectional reference purge + junction_-guarded anti-join deletes; filter-operator-prune recursive prune through OT ops with empty-collapses-to-null; uniqueindex-emptystring-twins canonical-naming index rebuilds + ''-to-NULL normalization; symmetric-consistency-ladder existence-gated checks + declare-one-way repair; fk-column-restoration relationship-exact constraint DDL + 42P07 tolerance + unconditional backfill tail; v2-crossbase-hydration closure-before-verdict + withoutBaseId unknown-base fallback; v2-preflight-gate count-before-hydrate + synthetic manual-mode issues yielded first; v2-sse-lifecycle comment heartbeats + lifecycle event grammar + flush-per-write; v2-repair-telemetry capture-and-rethrow dual-sink Sentry/span). Evidence: anchors line-exact ×24 pre-authoring (anchor-calc.sh/anchor-integ.sh sweeps); deterministic probe battery 24/24 byte-exact GREEN after re-deriving true counts from source (12 guessed counts corrected pre-write — no count-only verification); graph retrieval live-resolving ×14 against `teable` project (search_graph flag form); stdin-JSON check_index_coverage ×8 cited paths no_recorded_issue metadata_match generation_matches=true; direct tests pinned: dfs.spec.ts (DAG orders/cycles/pruneGraph), changes.spec.ts (:67 merge), compose-maps.spec.ts (same-p overwrite ×3, oi==od filter ×2), link.service.spec.ts :25–56 junction-reads-from-data-db routing pin, integrity-v2.service.spec.ts :325 cross-base hydration RED/GREEN pair + :496/:548 preflight pins + :628/:670 telemetry capture twins, link-field.service.spec.ts data-db routing; runner BLOCKED (repo ships no node_modules/vitest in this clone) → per protocol recorded, deterministic battery substituted. OMITTED-WITH-REASON this pass: features/auth|field|base|table|attachments|undo-redo|airtable-import|import|view|oauth|share|plugin|user|mail-sender|trash|selection|setting|canary|ai product surfaces (Boundaries; space/record-history-cold/aggregation since mined by passes 16-19), calculation/batch+link spec files' commented-out legacy suites (dead fixtures), integrity/integrity.controller.ts v1 thin routes (controller delegates to mined service), utils/name-console.ts debug helper. Wired as THREE new map groups (V1 calculation & link derivation engine / Integrity check/repair plane / V2 integrity streaming plane). Parity repaired to 224=224=224 bidirectional set-equality; grep -rL capsule-v2 EMPTY; membership UNCHANGED packs.json/router/manifest untouched.

## Full view (memory graph)
Revalidate before porting: project `teable`, root `$REFERENCE_ROOT/teable` (canonical `$REFERENCE_ROOT/platforms/teable`, live symlink), branch `develop@06a4461e2bc53055182d4df0a72dffa26fd99210`, status ready, full mode, 95,388 nodes / 404,072 edges. BM25 search_graph sits at a 50-hit noise floor on this repo — use `search_code --pattern '<symbol>'` (resolves Module/File nodes line-exact); every pass-18 cold-plane Retrieve primitive live-resolved line-exact at pin; every pass-19 buffer-plane and BYODB-plane Retrieve primitive live-resolved line-exact at pin; every pass-21 airtable-plane Retrieve primitive live-resolved rank-1 via BM25 search_graph ids mode at pin (pass-21 caveat: CBM CLI `search_code` was down with a server-side temp-file quota error during that window — its Retrieve blocks pin the working `search_graph` form). Adversarial wrong-project probes NEED positive controls on this fleet (same query on grist-core resolves grist's own client-side AirtableImporter — always route by the capsule's cited project token). parse_partial = 98 files (SQL migrations, pnpm-lock, ~15 large v2 adapter files flagged at single template-literal lines); skipped = 0; excluded by design: `.git`/.idea dirs + 257 ignored-suffix assets. Run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, `get_code_snippet`; source and direct tests decide shipped claims.

## Boundaries
Adopt the Result-domain-error discipline, symbol-token DI, outbox lease claiming, trigger-based undo capture, fractional ordering, dependency-edge semantics, command-replay undo/redo, sibling-reuse transactions, fail-open locking posture, ledger-backed schema operations, commit-time realtime buffering, the advisor→executor→reconciler managed search-index lifecycle, the per-cell-type search-predicate routing ladder, the computed recompute planner/backfill, and the enforce-grouped plugin pipelines; pass 15 supersedes its own aggregation product-shell ruling by mining the features/aggregation read surface (pass 16: SQL compiler ladder, MCV jsonb-join aggregation with hostile-text numeric coercion, percent GREATEST guards, user-id uniqueness, date-range SQL/JS split, attachment-size nested sums, selection slice choreography, group-point fan-out under the maxGroupPoints cap with Unknown-bucket honesty, choice-order sort ladders, DB-level search timeouts, two-pass search-index resolution, Me-sentinel cache keys, the validity matrix, calendar generate_series spines, permission projection funneling, TQL/view-filter merging, result conversion, row-count link-cell modes, and symbol-token DI); pass 4 adds the declarative schema-rule kernel (dependency-id rules, plane-tagged builders, Kahn resolution with reverse teardown), the self-verifying repair ladder (validate→execute→re-validate, zod-form manual gates, coded validation failures routing auto-vs-manual), the link-storage rule family (junction backfill from JSONB values, shape-based FK equivalence with orphan preflight, delete-vs-convert reference asymmetry), the conversion dispatch ladder (in-place guarded ALTER vs staged rename-migrate vs drop+create recreation with null-for-incompatible contracts), and the spec-gated update orchestration (reverse-allowlist collectors → DDL → cycle gate → backfill → level-ordered cascade → deferred replay); pass 5 adds the query-ops advisor/executor/reconciler deep seams (catalog-derived inventory with stale-reason collapse, one-query provider capability probing that never CREATEs an extension, EXPLAIN-allowlist + HypoPG plan validation, query-shape→expected-index derivation, kind-dispatch remediation executor, managed-name + large/unknown-rewrite guards, stableHash naming + versioned COMMENT markers, ILIKE-baseline n-gram semantics comparison with LLM-review handoff, schema-change task coalescing, status reader with to_regclass guard, additive observation/partial-unique recommendation/FOR UPDATE SKIP LOCKED claim/owner-or-expiry lease repositories, idempotent schema bootstrap, dual-DB adapter wiring, and physical-name helpers), plus the real-DDL keep-gate (used-index + symmetric-EXCEPT semantics + strictly-negative cost must all hold before a freshly built index is kept, else throw-to-cleanup); pass 6 adds the query-ops domain kernel (literal-exclusion shape validation, execution-shape-blind hashing, additive policyVersion-stamped risk ladder, guard-clause recommendation/task state machines, the safeTry handler rail, isRegistered-gated Noop wiring with class-token projection, TTL-equals-interval leased runners, max-depth filter parsing with formula-source rewriting, marker-guarded query-repository decoration with finally-block stream windows, context-slot SQL diagnostics, zero-duration config observations, derived definitionKeys, tri-state validation gates, canonical-group index-set planning, warn-don't-fail maintenance projection) and the meta-plane repository seams (FNV-1a redacted SQL-error diagnostics, conditional-CTE atomic table insert with ambient-tx fork, timestamp-batched restore + to_regclass-guarded purge, arithmetic version-touch reconstruction, primary-first lateral hydration, read-time v1→v2 filter migration, degrade-don't-fail field DTO deserialization, per-table provision-ledger keys, policy-resolved row limits, and the three-branch stale-claim operation ledger); adapt Postgres identifiers/GUC names, config knobs, tag/status tables, container choice, and scope vocabularies to host; omit teable's product surfaces (NestJS feature modules beyond cited seams, Next.js app, SDK/openapi/ui-lib packages, AI/chat/automation features), the formula-engine internals unless a target task needs them, and the ShareDB read side (queryPoll is a thin field-projection translation over the mined submit gate). Pass 11 adds the record-write builder kit: adopt the never-execute pure-builder split (compiled SQL + impact plan as data), the insert choreography order (exclusivity pre-INSERT → own row → foreign locks → junction/FK statements), the dual generated-column strip (meta visitor + physical catalog probe), the columns×rows bind ceiling with cross-batch RETURNING accumulation, out-of-transaction identity lookups, INSERT-time link-title fill, and the zero-row-update-is-success tri-state; adapt batch transposition (presence flags decide clear-vs-keep) and server-side table cloning to your schema conventions. Pass 12 adds the read-path builder half: adopt the capability-by-execution probe (never version-sniff Postgres features), FNV-1a identifier suffixing, and the prepare(deps)-before-build() computed/stored mode switch; adapt the level-ordered CTE formula batching and three-tier stored ORDER BY resolution to your sort conventions. Pass 13 adds the computed execution/assembly kit: adopt the generation-keyed dirty frontier with per-generation read scoping and PK-dedup inserts, the fail-safe widen-to-all-target ladder for un-compilable conditional filters, the current-OR-before-image match for filtered relations, post-propagation dirty-target locking inside the write transaction, the link-projection-exempt cell byte ceiling with one deferred version bump, filter-first dirty joins repeated inside host sources, the lateral/inner/hostLeft/hostKey rollup join-mode ladder with zero-match host semantics, the shared FK-location truth table, storage-aware lookup flattening, sanitize-then-coerce numeric rollups, and the bounded distinct-host-key probe before unchunked execution. Pass 14 adds the app-layer orchestration + queue-bridge kit: adopt the publish-only orchestrator doctrine (collect→sorted-lock→update→evaluate, raw versionless ShareDB ops), lazy ALL_RECORDS impact seeding with monotone fixpoint re-closure over junction frontier BFS, the widening-only conditional fail-safe ladder with typed CAST VALUES post-probes, old∪new symmetric link seeding, Kahn-layer evaluation with flat-layer cycle fallback and stored-lookup gating, hasOwnProperty-null raw op publication preferring __prev_version, errored-field SET-list gating, auto-number cursor paging with JS resort-before-handle, atomic Lua zset per-base admission leases with XX-only renewal and permit.assertActive ladders, reason-keyed deferred wakeup rescheduling with deterministic time-bucketed ids, final-attempt durable replay re-arms behind UnrecoverableError payload gates, fire-and-forget publish timeouts with a single recovery circuit, cuwr2-revision-keyed startup redrive across meta-fallback plus BYODB under pg advisory single-flight, AsyncLocalStorage consumer publish capability, truncating composite anomaly group keys, two-tier health composition, and order-preserving bounded-concurrency maps; adapt BullMQ specifics, Redis key namespacing, and env knob names to your broker. Pass 18 adds the cold-storage tiering kit: adopt the one-kill-switch config ladder (migration gated, reads unconditional), minRecordId-bearing part-key grammar with per-writer run tokens, verify-by-re-download before any buffer delete, the globally budgeted external sorter (sync-swap spills, floor-2 fan-in, in-flight-write backpressure), consumed-keys-scoped healing, count-gated REPEATABLE READ deletes, add-only schedulers with hop-chained colon-free catch-up jobs, bookmark-probe discovery that never wakes idle tenant dbs, fail-open advisory pruning over keys/blooms/stats, atomic-month S3 scans under a time budget, and the end-to-end truncation marker contract; adapt bucket/S3 specifics and the record_history schema to your store. Pass 19 adds the buffer-capture kit: adopt post-commit event emission that reads-AND-clears the op stash, cross-record field-id unions before batched metadata loads, five-step filter ladders with a conversion-time oldField latch, transaction-stripped background deferral with unref'd scheduler fallbacks, actor attribution from the EVENT snapshot (never drain-time CLS), per-engine duplicated select-choice minimization, and the hot-buffer-then-S3 post-purge GC order with a single config-load-time write-side kill switch; adapt the envelope dialect choice (full meta vs bare data) to your reader's tolerance.

## Reference-file inventory

Every preserved capsule/reference file in this foundation:

- [`agg-attachment-size-boolean.md`](./agg-attachment-size-boolean.md)
- [`agg-cache-me-key.md`](./agg-cache-me-key.md)
- [`agg-calendar-spine.md`](./agg-calendar-spine.md)
- [`agg-choice-order-sort.md`](./agg-choice-order-sort.md)
- [`agg-date-range-split.md`](./agg-date-range-split.md)
- [`agg-group-points-cap.md`](./agg-group-points-cap.md)
- [`agg-group-points-fanout.md`](./agg-group-points-fanout.md)
- [`agg-hostile-numeric-coercion.md`](./agg-hostile-numeric-coercion.md)
- [`agg-mcv-jsonb-join.md`](./agg-mcv-jsonb-join.md)
- [`agg-percent-greatest-guard.md`](./agg-percent-greatest-guard.md)
- [`agg-permission-projection.md`](./agg-permission-projection.md)
- [`agg-result-conversion.md`](./agg-result-conversion.md)
- [`agg-rowcount-link-modes.md`](./agg-rowcount-link-modes.md)
- [`agg-search-timeout-gate.md`](./agg-search-timeout-gate.md)
- [`agg-searchindex-two-pass.md`](./agg-searchindex-two-pass.md)
- [`agg-selection-slice.md`](./agg-selection-slice.md)
- [`agg-sql-compile-ladder.md`](./agg-sql-compile-ladder.md)
- [`agg-symbol-di-token.md`](./agg-symbol-di-token.md)
- [`agg-tql-view-merge.md`](./agg-tql-view-merge.md)
- [`agg-user-unique-id.md`](./agg-user-unique-id.md)
- [`agg-valid-func-table.md`](./agg-valid-func-table.md)
- [`aggregate-builder-composition-order.md`](./aggregate-builder-composition-order.md)
- [`aggregate-group-choice-order.md`](./aggregate-group-choice-order.md)
- [`aggregate-range-table-path-forcing.md`](./aggregate-range-table-path-forcing.md)
- [`aggregate-selection-slice-projection.md`](./aggregate-selection-slice-projection.md)
- [`airtable-ai-field-upgrade.md`](./airtable-ai-field-upgrade.md)
- [`airtable-analyze-endpoint.md`](./airtable-analyze-endpoint.md)
- [`airtable-api-client-discipline.md`](./airtable-api-client-discipline.md)
- [`airtable-attachment-watchdog.md`](./airtable-attachment-watchdog.md)
- [`airtable-bounded-concurrency-map.md`](./airtable-bounded-concurrency-map.md)
- [`airtable-cell-conversion.md`](./airtable-cell-conversion.md)
- [`airtable-derived-fields.md`](./airtable-derived-fields.md)
- [`airtable-field-mapping-ladder.md`](./airtable-field-mapping-ladder.md)
- [`airtable-formula-translator.md`](./airtable-formula-translator.md)
- [`airtable-import-authz.md`](./airtable-import-authz.md)
- [`airtable-import-phase-ladder.md`](./airtable-import-phase-ladder.md)
- [`airtable-issues-ledger.md`](./airtable-issues-ledger.md)
- [`airtable-link-fill.md`](./airtable-link-fill.md)
- [`airtable-link-planning.md`](./airtable-link-planning.md)
- [`airtable-link-row-spill.md`](./airtable-link-row-spill.md)
- [`airtable-record-streaming.md`](./airtable-record-streaming.md)
- [`airtable-rollup-mapper.md`](./airtable-rollup-mapper.md)
- [`airtable-share-client.md`](./airtable-share-client.md)
- [`airtable-sse-import-stream.md`](./airtable-sse-import-stream.md)
- [`airtable-user-resolution.md`](./airtable-user-resolution.md)
- [`airtable-view-config-apply.md`](./airtable-view-config-apply.md)
- [`airtable-view-config-mapping.md`](./airtable-view-config-mapping.md)
- [`all-records-fixpoint-bfs.md`](./all-records-fixpoint-bfs.md)
- [`anomaly-grouping-recovery.md`](./anomaly-grouping-recovery.md)
- [`attachment-lookup-service.md`](./attachment-lookup-service.md)
- [`attachment-table-replace.md`](./attachment-table-replace.md)
- [`auth-jwt-internal-token-kinds.md`](./auth-jwt-internal-token-kinds.md)
- [`auth-oauth-state-callback.md`](./auth-oauth-state-callback.md)
- [`auth-permission-guard-ladder.md`](./auth-permission-guard-ladder.md)
- [`auth-session-store-dual-key.md`](./auth-session-store-dual-key.md)
- [`auth-share-scope-ceiling.md`](./auth-share-scope-ceiling.md)
- [`auth-signin-lockout-attempts.md`](./auth-signin-lockout-attempts.md)
- [`auth-strategy-chain-fallback.md`](./auth-strategy-chain-fallback.md)
- [`auto-number-cursor-paging.md`](./auto-number-cursor-paging.md)
- [`auto-number-sequence-resync.md`](./auto-number-sequence-resync.md)
- [`base-admission-lua-lease.md`](./base-admission-lua-lease.md)
- [`base-pagination-cte-split.md`](./base-pagination-cte-split.md)
- [`base-repository-tx-join.md`](./base-repository-tx-join.md)
- [`batch-cls-rawop-stash.md`](./batch-cls-rawop-stash.md)
- [`batch-existing-links-bulk-reader.md`](./batch-existing-links-bulk-reader.md)
- [`batch-sql-composition-seam.md`](./batch-sql-composition-seam.md)
- [`batch-temp-table-update-ladder.md`](./batch-temp-table-update-ladder.md)
- [`batch-update-aggregation.md`](./batch-update-aggregation.md)
- [`before-image-merging.md`](./before-image-merging.md)
- [`binary-op-numeric-coercion.md`](./binary-op-numeric-coercion.md)
- [`blank-aware-comparison.md`](./blank-aware-comparison.md)
- [`build-record-where-clause.md`](./build-record-where-clause.md)
- [`bulk-insert-reuse.md`](./bulk-insert-reuse.md)
- [`byodb-admin-fence.md`](./byodb-admin-fence.md)
- [`byodb-atomic-switch-rollback-proof.md`](./byodb-atomic-switch-rollback-proof.md)
- [`byodb-base-schema-copy-ladder.md`](./byodb-base-schema-copy-ladder.md)
- [`byodb-copy-pipeline-supervisor.md`](./byodb-copy-pipeline-supervisor.md)
- [`byodb-copy-validation-kernel.md`](./byodb-copy-validation-kernel.md)
- [`byodb-heartbeat-failure-forensics.md`](./byodb-heartbeat-failure-forensics.md)
- [`byodb-job-claim-cas.md`](./byodb-job-claim-cas.md)
- [`byodb-job-record-state-contract.md`](./byodb-job-record-state-contract.md)
- [`byodb-migration-progress-model.md`](./byodb-migration-progress-model.md)
- [`byodb-related-spaces-closure.md`](./byodb-related-spaces-closure.md)
- [`byodb-repair-mode-legacy-seqs.md`](./byodb-repair-mode-legacy-seqs.md)
- [`byodb-secret-and-preflight.md`](./byodb-secret-and-preflight.md)
- [`byodb-shared-copy-strategies.md`](./byodb-shared-copy-strategies.md)
- [`byodb-shared-table-scoping.md`](./byodb-shared-table-scoping.md)
- [`byodb-snapshot-delta-replay.md`](./byodb-snapshot-delta-replay.md)
- [`byodb-target-artifact-retry-lifecycle.md`](./byodb-target-artifact-retry-lifecycle.md)
- [`byodb-target-capability-probes.md`](./byodb-target-capability-probes.md)
- [`byodb-worker-loop.md`](./byodb-worker-loop.md)
- [`byodb-write-freeze-gate.md`](./byodb-write-freeze-gate.md)
- [`byodb-write-guard-baseline.md`](./byodb-write-guard-baseline.md)
- [`calc-diamond-merge-compose.md`](./calc-diamond-merge-compose.md)
- [`calc-errored-gate-auxiliary-context.md`](./calc-errored-gate-auxiliary-context.md)
- [`cell-value-mutate-visitor.md`](./cell-value-mutate-visitor.md)
- [`cold-bloom-double-hash.md`](./cold-bloom-double-hash.md)
- [`cold-bucket-coverage-triple-match.md`](./cold-bucket-coverage-triple-match.md)
- [`cold-buffer-delete-repeatable-read.md`](./cold-buffer-delete-repeatable-read.md)
- [`cold-byodb-bookmark-monotonic.md`](./cold-byodb-bookmark-monotonic.md)
- [`cold-catchup-chain-hop-id.md`](./cold-catchup-chain-hop-id.md)
- [`cold-compactor-force-repair.md`](./cold-compactor-force-repair.md)
- [`cold-cursor-dual-dialect-inclusive-legacy.md`](./cold-cursor-dual-dialect-inclusive-legacy.md)
- [`cold-discovery-loose-index-scan.md`](./cold-discovery-loose-index-scan.md)
- [`cold-fanin-floor-two.md`](./cold-fanin-floor-two.md)
- [`cold-feeder-foldback-eof-init.md`](./cold-feeder-foldback-eof-init.md)
- [`cold-flush-run-budget-atomicity.md`](./cold-flush-run-budget-atomicity.md)
- [`cold-heal-consumed-keys-scoping.md`](./cold-heal-consumed-keys-scoping.md)
- [`cold-history-config-env-ladder.md`](./cold-history-config-env-ladder.md)
- [`cold-merged-read-buffer-first-seam.md`](./cold-merged-read-buffer-first-seam.md)
- [`cold-missing-part-relist-retry-once.md`](./cold-missing-part-relist-retry-once.md)
- [`cold-module-split-consumer-guard.md`](./cold-module-split-consumer-guard.md)
- [`cold-ndjson-line-splitter.md`](./cold-ndjson-line-splitter.md)
- [`cold-orphan-vs-byodb-exclusion.md`](./cold-orphan-vs-byodb-exclusion.md)
- [`cold-part-key-grammar.md`](./cold-part-key-grammar.md)
- [`cold-part-writer-verify-delete.md`](./cold-part-writer-verify-delete.md)
- [`cold-pruning-advisory-default-scan.md`](./cold-pruning-advisory-default-scan.md)
- [`cold-read-batch-adaptive-limit.md`](./cold-read-batch-adaptive-limit.md)
- [`cold-scheduler-add-only-no-removal.md`](./cold-scheduler-add-only-no-removal.md)
- [`cold-sort-memory-budget-global-eviction.md`](./cold-sort-memory-budget-global-eviction.md)
- [`cold-spill-swap-before-await.md`](./cold-spill-swap-before-await.md)
- [`cold-storage-write-vs-read-cache-discipline.md`](./cold-storage-write-vs-read-cache-discipline.md)
- [`cold-topk-month-atomic-timeout.md`](./cold-topk-month-atomic-timeout.md)
- [`cold-truncation-marker-contract.md`](./cold-truncation-marker-contract.md)
- [`cold-truncation-render-guard.md`](./cold-truncation-render-guard.md)
- [`cold-zstd-gzip-negotiation.md`](./cold-zstd-gzip-negotiation.md)
- [`computed-backfill.md`](./computed-backfill.md)
- [`computed-cell-safety-limit.md`](./computed-cell-safety-limit.md)
- [`computed-event-fanout.md`](./computed-event-fanout.md)
- [`computed-module-wiring.md`](./computed-module-wiring.md)
- [`computed-pagination-fallback.md`](./computed-pagination-fallback.md)
- [`computed-plan-steps.md`](./computed-plan-steps.md)
- [`computed-seed-dispatch.md`](./computed-seed-dispatch.md)
- [`computed-update-lock-ladder.md`](./computed-update-lock-ladder.md)
- [`computed-update-outbox.md`](./computed-update-outbox.md)
- [`computed-update-planner.md`](./computed-update-planner.md)
- [`conditional-aggregate-cast-envelope.md`](./conditional-aggregate-cast-envelope.md)
- [`conditional-before-image-propagation.md`](./conditional-before-image-propagation.md)
- [`conditional-computed-target-resolution.md`](./conditional-computed-target-resolution.md)
- [`conditional-ddl-guard.md`](./conditional-ddl-guard.md)
- [`conditional-equality-counts-plan.md`](./conditional-equality-counts-plan.md)
- [`conditional-equality-fallback-coalesce.md`](./conditional-equality-fallback-coalesce.md)
- [`conditional-failsafe-ladder.md`](./conditional-failsafe-ladder.md)
- [`conditional-filter-field-reference-context.md`](./conditional-filter-field-reference-context.md)
- [`conditional-lookup-dual-column-emission.md`](./conditional-lookup-dual-column-emission.md)
- [`conditional-lookup-scalar-extractor.md`](./conditional-lookup-scalar-extractor.md)
- [`conditional-lookup-warning-degradation.md`](./conditional-lookup-warning-degradation.md)
- [`conditional-rollup-ordering-functions.md`](./conditional-rollup-ordering-functions.md)
- [`conditional-window-limit.md`](./conditional-window-limit.md)
- [`constraint-backfill-ladder.md`](./constraint-backfill-ladder.md)
- [`create-builder-fusion.md`](./create-builder-fusion.md)
- [`cte-before-image-bulk-update.md`](./cte-before-image-bulk-update.md)
- [`date-range-resolution.md`](./date-range-resolution.md)
- [`date-search-range.md`](./date-search-range.md)
- [`datetime-format-offset-suffix.md`](./datetime-format-offset-suffix.md)
- [`datetime-sanitize-tzwrap.md`](./datetime-sanitize-tzwrap.md)
- [`db-field-naming.md`](./db-field-naming.md)
- [`deferred-computed-drain.md`](./deferred-computed-drain.md)
- [`deferred-fk-provisioning.md`](./deferred-fk-provisioning.md)
- [`deferred-wakeup-reason-ladder.md`](./deferred-wakeup-reason-ladder.md)
- [`dependency-gate-visitors.md`](./dependency-gate-visitors.md)
- [`di-tokens.md`](./di-tokens.md)
- [`dirty-frontier-propagation.md`](./dirty-frontier-propagation.md)
- [`dirty-set-join-placement.md`](./dirty-set-join-placement.md)
- [`dirty-target-lock-choreography.md`](./dirty-target-lock-choreography.md)
- [`distinct-host-key-probe.md`](./distinct-host-key-probe.md)
- [`distributed-lock.md`](./distributed-lock.md)
- [`domain-error-contract.md`](./domain-error-contract.md)
- [`dual-phase-update-orchestration.md`](./dual-phase-update-orchestration.md)
- [`empty-table-seed-analyze.md`](./empty-table-seed-analyze.md)
- [`errored-field-update-gating.md`](./errored-field-update-gating.md)
- [`expected-index-derivation.md`](./expected-index-derivation.md)
- [`explain-builder-reuse.md`](./explain-builder-reuse.md)
- [`field-change-impact-closure.md`](./field-change-impact-closure.md)
- [`field-dependency-graph.md`](./field-dependency-graph.md)
- [`field-duplicate-sql.md`](./field-duplicate-sql.md)
- [`field-operation-plugin-pipeline.md`](./field-operation-plugin-pipeline.md)
- [`field-persistence-row-builder.md`](./field-persistence-row-builder.md)
- [`field-rules-factory.md`](./field-rules-factory.md)
- [`field-value-visitors.md`](./field-value-visitors.md)
- [`filter-augmented-selection.md`](./filter-augmented-selection.md)
- [`final-attempt-replay-rearm.md`](./final-attempt-replay-rearm.md)
- [`fire-forget-publish-recovery.md`](./fire-forget-publish-recovery.md)
- [`fk-equivalence-orphan-preflight.md`](./fk-equivalence-orphan-preflight.md)
- [`flatten-nested-json-array.md`](./flatten-nested-json-array.md)
- [`formula-expansion-context-swap.md`](./formula-expansion-context-swap.md)
- [`formula-migration-matrix.md`](./formula-migration-matrix.md)
- [`formula-select-expression-fallback.md`](./formula-select-expression-fallback.md)
- [`formula-support-validator-family-vetoes.md`](./formula-support-validator-family-vetoes.md)
- [`fromnow-tonow-direction-contract.md`](./fromnow-tonow-direction-contract.md)
- [`generated-audit-column-strip.md`](./generated-audit-column-strip.md)
- [`generated-column-countall-multivalue.md`](./generated-column-countall-multivalue.md)
- [`generated-column-immutability-contract.md`](./generated-column-immutability-contract.md)
- [`generated-column-insert-strip-meta.md`](./generated-column-insert-strip-meta.md)
- [`generated-column-insert-strip.md`](./generated-column-insert-strip.md)
- [`history-buffer-capture-funnel.md`](./history-buffer-capture-funnel.md)
- [`history-envelope-dual-dialect.md`](./history-envelope-dual-dialect.md)
- [`history-id-gc-lifecycle.md`](./history-id-gc-lifecycle.md)
- [`history-ops-to-event-pipeline.md`](./history-ops-to-event-pipeline.md)
- [`history-select-minimize-twin.md`](./history-select-minimize-twin.md)
- [`history-v2-background-deferral.md`](./history-v2-background-deferral.md)
- [`history-v2-projection-trio.md`](./history-v2-projection-trio.md)
- [`hybrid-sync-budget-splitting.md`](./hybrid-sync-budget-splitting.md)
- [`impact-hint-normalization.md`](./impact-hint-normalization.md)
- [`incoming-link-delete-choreography.md`](./incoming-link-delete-choreography.md)
- [`inline-outbox-drain-loop.md`](./inline-outbox-drain-loop.md)
- [`insert-batch-bind-ceiling.md`](./insert-batch-bind-ceiling.md)
- [`insert-bind-limit-batching.md`](./insert-bind-limit-batching.md)
- [`insert-builder-dual-api.md`](./insert-builder-dual-api.md)
- [`insert-exclusivity-choreography.md`](./insert-exclusivity-choreography.md)
- [`insert-pruning-seeding.md`](./insert-pruning-seeding.md)
- [`insert-restore-identity-dedup.md`](./insert-restore-identity-dedup.md)
- [`integrity-check-fix-dispatch.md`](./integrity-check-fix-dispatch.md)
- [`integrity-filter-operator-prune.md`](./integrity-filter-operator-prune.md)
- [`integrity-fk-column-restoration.md`](./integrity-fk-column-restoration.md)
- [`integrity-link-json-backfill.md`](./integrity-link-json-backfill.md)
- [`integrity-orphan-purge-guards.md`](./integrity-orphan-purge-guards.md)
- [`integrity-primary-promotion-ladder.md`](./integrity-primary-promotion-ladder.md)
- [`integrity-symmetric-consistency-ladder.md`](./integrity-symmetric-consistency-ladder.md)
- [`integrity-uniqueindex-emptystring-twins.md`](./integrity-uniqueindex-emptystring-twins.md)
- [`integrity-v2-crossbase-hydration.md`](./integrity-v2-crossbase-hydration.md)
- [`integrity-v2-preflight-gate.md`](./integrity-v2-preflight-gate.md)
- [`integrity-v2-repair-telemetry.md`](./integrity-v2-repair-telemetry.md)
- [`integrity-v2-sse-lifecycle.md`](./integrity-v2-sse-lifecycle.md)
- [`is-persisted-as-generated-column.md`](./is-persisted-as-generated-column.md)
- [`junction-rule-family.md`](./junction-rule-family.md)
- [`layered-evaluation-ordering.md`](./layered-evaluation-ordering.md)
- [`link-cascade-frontier-bfs.md`](./link-cascade-frontier-bfs.md)
- [`link-cell-formatting-ladder.md`](./link-cell-formatting-ladder.md)
- [`link-cell-jsonb-strip-nulls.md`](./link-cell-jsonb-strip-nulls.md)
- [`link-change-collection.md`](./link-change-collection.md)
- [`link-cte-column-contract.md`](./link-cte-column-contract.md)
- [`link-cte-emission-choreography.md`](./link-cte-emission-choreography.md)
- [`link-delete-context-dual-sweep.md`](./link-delete-context-dual-sweep.md)
- [`link-exclusivity-validation.md`](./link-exclusivity-validation.md)
- [`link-fk-single-source-of-truth.md`](./link-fk-single-source-of-truth.md)
- [`link-join-ladder.md`](./link-join-ladder.md)
- [`link-junction-order-reorder-ladder.md`](./link-junction-order-reorder-ladder.md)
- [`link-mutation-routing.md`](./link-mutation-routing.md)
- [`link-plan-commit-split.md`](./link-plan-commit-split.md)
- [`link-relationship-join-matrix.md`](./link-relationship-join-matrix.md)
- [`link-seed-batch-loading.md`](./link-seed-batch-loading.md)
- [`link-title-fill-sql.md`](./link-title-fill-sql.md)
- [`link-title-fill.md`](./link-title-fill.md)
- [`link-title-resolution-ladder.md`](./link-title-resolution-ladder.md)
- [`linked-record-advisory-ladder.md`](./linked-record-advisory-ladder.md)
- [`lookup-date-iso-normalize.md`](./lookup-date-iso-normalize.md)
- [`lookup-order-column-tiebreak.md`](./lookup-order-column-tiebreak.md)
- [`lookup-rollup-json-aggregation.md`](./lookup-rollup-json-aggregation.md)
- [`loose-numeric-coercion.md`](./loose-numeric-coercion.md)
- [`manual-repair-form-schema.md`](./manual-repair-form-schema.md)
- [`materialized-aggregate-cte.md`](./materialized-aggregate-cte.md)
- [`meta-touch-monotonic-clock.md`](./meta-touch-monotonic-clock.md)
- [`meta-update-two-depth.md`](./meta-update-two-depth.md)
- [`missing-host-table-tolerance.md`](./missing-host-table-tolerance.md)
- [`month-diff-day-adjustment.md`](./month-diff-day-adjustment.md)
- [`multi-value-lookup-jsonagg.md`](./multi-value-lookup-jsonagg.md)
- [`noop-write-guard.md`](./noop-write-guard.md)
- [`obsolete-task-ladder.md`](./obsolete-task-ladder.md)
- [`order-preserving-concurrency-map.md`](./order-preserving-concurrency-map.md)
- [`original-source-latch.md`](./original-source-latch.md)
- [`outbox-health-classification.md`](./outbox-health-classification.md)
- [`outbox-worker-lifecycle.md`](./outbox-worker-lifecycle.md)
- [`param-metadata-coercion-dispatch.md`](./param-metadata-coercion-dispatch.md)
- [`pause-scopes.md`](./pause-scopes.md)
- [`persisted-backfill-reentry.md`](./persisted-backfill-reentry.md)
- [`pg-capability-identifier-hash.md`](./pg-capability-identifier-hash.md)
- [`pg-multivalue-scalar-aggregators.md`](./pg-multivalue-scalar-aggregators.md)
- [`physical-row-duplication-plan.md`](./physical-row-duplication-plan.md)
- [`physical-row-duplication.md`](./physical-row-duplication.md)
- [`plan-validation-hypopg.md`](./plan-validation-hypopg.md)
- [`post-process-drain.md`](./post-process-drain.md)
- [`projection-dependency-closure.md`](./projection-dependency-closure.md)
- [`query-builder-mode-manager.md`](./query-builder-mode-manager.md)
- [`query-config-observation-twins.md`](./query-config-observation-twins.md)
- [`query-field-deserialize-ladder.md`](./query-field-deserialize-ladder.md)
- [`query-filter-shape-parser.md`](./query-filter-shape-parser.md)
- [`query-index-set-planner.md`](./query-index-set-planner.md)
- [`query-lateral-hydration.md`](./query-lateral-hydration.md)
- [`query-observation-decorator.md`](./query-observation-decorator.md)
- [`query-ops-adapter-wiring.md`](./query-ops-adapter-wiring.md)
- [`query-ops-di-noop-wiring.md`](./query-ops-di-noop-wiring.md)
- [`query-ops-physical-name-helpers.md`](./query-ops-physical-name-helpers.md)
- [`query-ops-repositories.md`](./query-ops-repositories.md)
- [`query-ops-runner-leasing.md`](./query-ops-runner-leasing.md)
- [`query-ops-safetry-rail.md`](./query-ops-safetry-rail.md)
- [`query-ops-schema-bootstrap.md`](./query-ops-schema-bootstrap.md)
- [`query-provision-ledger-recording.md`](./query-provision-ledger-recording.md)
- [`query-recommendation-state-machine.md`](./query-recommendation-state-machine.md)
- [`query-restore-batch-stamp.md`](./query-restore-batch-stamp.md)
- [`query-risk-policy-ladder.md`](./query-risk-policy-ladder.md)
- [`query-row-limit-plugin.md`](./query-row-limit-plugin.md)
- [`query-schema-maintenance-projection.md`](./query-schema-maintenance-projection.md)
- [`query-schema-operation-ledger.md`](./query-schema-operation-ledger.md)
- [`query-search-definition-builder.md`](./query-search-definition-builder.md)
- [`query-search-validation-gate.md`](./query-search-validation-gate.md)
- [`query-shape-literal-exclusion.md`](./query-shape-literal-exclusion.md)
- [`query-shape-stable-hash.md`](./query-shape-stable-hash.md)
- [`query-sql-diagnostics-collector.md`](./query-sql-diagnostics-collector.md)
- [`query-sql-error-diagnostics.md`](./query-sql-error-diagnostics.md)
- [`query-state-manager-scoped-selection.md`](./query-state-manager-scoped-selection.md)
- [`query-table-cte-insert.md`](./query-table-cte-insert.md)
- [`query-v1-filter-migration.md`](./query-v1-filter-migration.md)
- [`query-version-touch-algebra.md`](./query-version-touch-algebra.md)
- [`question-mark-literal-split.md`](./question-mark-literal-split.md)
- [`raw-op-publication-ladder.md`](./raw-op-publication-ladder.md)
- [`real-ddl-plan-gate.md`](./real-ddl-plan-gate.md)
- [`record-condition-where-compilation.md`](./record-condition-where-compilation.md)
- [`record-condition-where.md`](./record-condition-where.md)
- [`record-order-fractional.md`](./record-order-fractional.md)
- [`record-query-read-plane.md`](./record-query-read-plane.md)
- [`record-read-model-mapping.md`](./record-read-model-mapping.md)
- [`record-search-where.md`](./record-search-where.md)
- [`record-write-funnel.md`](./record-write-funnel.md)
- [`record-write-plugin-pipeline.md`](./record-write-plugin-pipeline.md)
- [`ref-cte-four-way-or.md`](./ref-cte-four-way-or.md)
- [`reference-graph-cte-queries.md`](./reference-graph-cte-queries.md)
- [`reference-meta-rules.md`](./reference-meta-rules.md)
- [`remediation-task-executor.md`](./remediation-task-executor.md)
- [`restore-identity-choreography.md`](./restore-identity-choreography.md)
- [`role-aware-publish-capability.md`](./role-aware-publish-capability.md)
- [`rollup-function-support-matrix.md`](./rollup-function-support-matrix.md)
- [`same-table-collapse-guards.md`](./same-table-collapse-guards.md)
- [`same-table-cte-formula-batch.md`](./same-table-cte-formula-batch.md)
- [`schema-checker-streaming.md`](./schema-checker-streaming.md)
- [`schema-operation-lifecycle.md`](./schema-operation-lifecycle.md)
- [`schema-repair-ladder.md`](./schema-repair-ladder.md)
- [`schema-rule-contract.md`](./schema-rule-contract.md)
- [`schema-rule-resolution.md`](./schema-rule-resolution.md)
- [`schema-update-cascade.md`](./schema-update-cascade.md)
- [`schema-update-visitor.md`](./schema-update-visitor.md)
- [`search-access-path-capability.md`](./search-access-path-capability.md)
- [`search-scope-heat.md`](./search-scope-heat.md)
- [`search-semantics-comparison.md`](./search-semantics-comparison.md)
- [`search-vector-executor-guards.md`](./search-vector-executor-guards.md)
- [`search-vector-inventory.md`](./search-vector-inventory.md)
- [`search-vector-lifecycle.md`](./search-vector-lifecycle.md)
- [`search-vector-naming-sql.md`](./search-vector-naming-sql.md)
- [`search-vector-schema-maintenance.md`](./search-vector-schema-maintenance.md)
- [`search-vector-status-reader.md`](./search-vector-status-reader.md)
- [`seed-field-expansion.md`](./seed-field-expansion.md)
- [`select-columns-order.md`](./select-columns-order.md)
- [`select-error-function-semantics.md`](./select-error-function-semantics.md)
- [`select-expression-alias-binding.md`](./select-expression-alias-binding.md)
- [`select-options-minting.md`](./select-options-minting.md)
- [`set-based-rollup-ladder.md`](./set-based-rollup-ladder.md)
- [`share-realtime-gate.md`](./share-realtime-gate.md)
- [`sort-selection-gate.md`](./sort-selection-gate.md)
- [`sparse-batch-update-engine.md`](./sparse-batch-update-engine.md)
- [`startup-redrive-reconciliation.md`](./startup-redrive-reconciliation.md)
- [`storage-type-visitor.md`](./storage-type-visitor.md)
- [`stored-ordering-contract.md`](./stored-ordering-contract.md)
- [`stream-pagination-strategies.md`](./stream-pagination-strategies.md)
- [`symmetric-planned-seeding.md`](./symmetric-planned-seeding.md)
- [`sync-dirty-seed-cascade.md`](./sync-dirty-seed-cascade.md)
- [`sysfield-tracked-lastmodified-dual-write.md`](./sysfield-tracked-lastmodified-dual-write.md)
- [`system-table-rules.md`](./system-table-rules.md)
- [`table-data-safety-limits.md`](./table-data-safety-limits.md)
- [`table-operation-gate-runners.md`](./table-operation-gate-runners.md)
- [`table-where-spec-filter.md`](./table-where-spec-filter.md)
- [`task-chunking-fanout.md`](./task-chunking-fanout.md)
- [`task-failure-classifier.md`](./task-failure-classifier.md)
- [`task-lease-heartbeat.md`](./task-lease-heartbeat.md)
- [`text-to-user-conversion.md`](./text-to-user-conversion.md)
- [`topo-cycle-throw-contract.md`](./topo-cycle-throw-contract.md)
- [`trash-restore-cleanup.md`](./trash-restore-cleanup.md)
- [`trigger-role-config.md`](./trigger-role-config.md)
- [`truthiness-if-type-matrix.md`](./truthiness-if-type-matrix.md)
- [`type-conversion-dispatch.md`](./type-conversion-dispatch.md)
- [`typed-null-error-degradation.md`](./typed-null-error-degradation.md)
- [`undo-capture-session.md`](./undo-capture-session.md)
- [`undo-capture-triggers.md`](./undo-capture-triggers.md)
- [`undo-redo-stack.md`](./undo-redo-stack.md)
- [`undo-row-snapshot-reconstruction.md`](./undo-row-snapshot-reconstruction.md)
- [`unique-index-duplicate-remediation.md`](./unique-index-duplicate-remediation.md)
- [`unit-of-work.md`](./unit-of-work.md)
- [`update-builder-impact-plan.md`](./update-builder-impact-plan.md)
- [`update-from-select-before-images.md`](./update-from-select-before-images.md)
- [`update-one-mutation-applied.md`](./update-one-mutation-applied.md)
- [`user-lookup-service.md`](./user-lookup-service.md)
- [`user-snapshot-select-json.md`](./user-snapshot-select-json.md)
- [`view-context-raw-selection.md`](./view-context-raw-selection.md)
- [`view-order-bootstrap.md`](./view-order-bootstrap.md)
- [`wakeup-wire-contract.md`](./wakeup-wire-contract.md)
