<!-- capsule-v2 -->
**Source:** teable `record-history-flusher.service.ts` flushTable/flushGroup @ pin `06a4461e`
**Question:** How does one flush run stay atomic per table, budgeted across tables, and crash-safe on mid-stream failure?
**Path/Symbol:** `runFlush(options)`, `flushGroup(group, results, budget, run)`, `flushTable(...)`, IColdFlushRunResult {budgetExhausted, leftoverTables}, ITouchedBucket
**Signature:** groups processed serially; inside a group tables run in slices of `tableConcurrency` via mapWithConcurrency (results array pre-sized by index); row budget checked BETWEEN slices ("soft ... an oversized single table still completes atomically"); `budgetExhausted = leftoverTables > 0`.
**Decisive source:** :639-648 — "a mid-stream failure (a spill error surfaced by another table's eviction, an S3 hiccup, a feeder still unfinished) must not leave this table's feeders charged against the run-wide budget and evictable for the rest of the run. abort() frees each sorter's budget charge, temp files and registry slot; it is idempotent" → `await Promise.allSettled([...feeders.values()].map(f => f.abort())); throw error;`. Bucketing :539-543 — date-based regardless of mode: steady-state daily runs land <30d rows as DAY files while the first post-upgrade run lands the whole backlog directly as MONTH files — "a zero-ops instance gets the backfill layout for free".
**Flow/Invariant:** startSeq past existing max seq because "the feeder is still streaming the old parts while we upload, and S3 gives no guarantees for a GET racing an overwrite"; heal deletes ONLY consumed keys not rewritten (`healStaleParts`), stats delete only consumed keys then upsert fresh entries — concurrent runs' entries survive. Table-level failures become error'd ITableFlushResult rows (never abort the run).
**Probe (direct test):** `grep -c 'feeder.abort()' apps/nestjs-backend/src/features/record-history-cold/record-history-flusher.service.ts` → `1`; `grep -c 'backfill layout for free' apps/nestjs-backend/src/features/record-history-cold/record-history-flusher.service.ts` → `1` (comment text: "gets the backfill layout for free"); battery C20/C19 cover the sorter side.
**Retrieve:** `echo '{"project":"teable","pattern":"flushGroup","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — soft budgets + feeder-abort hygiene are the run-level invariants.
