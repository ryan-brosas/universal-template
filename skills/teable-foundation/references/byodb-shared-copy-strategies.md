<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-copy.service.ts` + `space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do shared-table rows move via psql COPY pipelines vs postgres_fdw, and how is row count recovered from command output?
**Path/Symbol:** `copySharedTable`/`parsePsqlCopyRowCount` (copy.service :294-327, :109-117), `copySharedTableViaPostgresFdw`/`parsePsqlInsertRowCount` (:321-352, :119-127), plan builders with `withExportedSnapshot` (copy-plan :85-95, :330-449)
**Signature:** psql_copy = TWO psql processes piped stdout→stdin: source runs `BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY; SET TRANSACTION SNAPSHOT '<id>'; COPY (SELECT <cols> FROM <qualified> WHERE <scope>) TO STDOUT; COMMIT;`, target runs `COPY <qualified> (<cols>) FROM STDIN`; both with `--no-psqlrc --quiet --set ON_ERROR_STOP=1`. postgres_fdw = ONE psql script: BEGIN → CREATE EXTENSION IF NOT EXISTS postgres_fdw → CREATE SCHEMA fdw_i → CREATE SERVER … FOREIGN DATA WRAPPER → CREATE USER MAPPING FOR CURRENT_USER → IMPORT FOREIGN SCHEMA src LIMIT TO (table) → INSERT INTO target SELECT FROM foreign WHERE scope → DROP SERVER CASCADE → DROP SCHEMA CASCADE → COMMIT.
**Data Shape:** copiedRows parsed by regex from COMBINED stdout+stderr — psql prints `COPY <n>` after stdin ingest (last match wins), fdw path prints `INSERT 0 <n>`; null when unparseable and totals propagate null (`sumCopiedRows` returns null if ANY table unknown — honest aggregate).
**Decisive source:** migration service :4765-4769 — snapshot ids are ONLY legal with psql_copy: "Snapshot-aware shared table copy is only supported by the psql_copy strategy" throws otherwise; pgcopydb likewise refuses snapshots and FK filtering. FDW plans get per-table schema/server names `${prefix}_${index}` so concurrent tables never collide.
**Flow/Invariant:** plans execute SEQUENTIALLY with per-table onTableCopied hooks updating job stats (progress fraction = copiedTables/total capped 0.99); a special hook pauses TARGET computed claims right after pause-scope rows land but before outbox rows ('pauses target computed claims after pause scopes are copied…') so restored outbox work can't run against half-migrated state.
**Probe (direct test):** specs 'executes shared table COPY pipelines sequentially' (copy.service.spec :454), 'parses psql COPY row counts from command output' (:34), 'wraps source shared-table COPY in the exported PostgreSQL snapshot transaction' (copy-plan.spec :346), 'copies shared rows through postgres_fdw when explicitly selected' (:3179); live: `grep -c "matchAll(/^INSERT" apps/nestjs-backend/src/features/space/space-data-db-copy.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"parsePsqlCopyRowCount","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt psql-pipe as default (snapshot-consistent) with fdw as no-shared-filesystem-fallback; row-count regexes are the contract for progress honesty.
