---
name: pipeshub-ai-foundation
description: "Use when porting agent-loop machinery from pipeshub-ai: fixed step primitive, loop strategies, parallel tool dispatch, dependency-aware sub-agent spawning, context compaction, hook middleware kernel, control-plane wiring, coding-sandbox lifecycle."
disable-model-invocation: true
---
# Pipeshub-AI Foundation

## Use this for
Use when building or refining an LLM agent turn loop in Python: one hook-instrumented step primitive driven by swappable loop shapes, wave-level duplicate suppression, a single hook-funneled tool executor with degenerate-name repair, dependency-aware spawn_agent batches with cross-turn result slots, object-keyed per-run state, atomic-group context compaction, streaming deltas over one authoritative response, lazy toolset disclosure, tag-based terminal tools, an Express-style per-event middleware kernel with monotonic decisions and fail-closed gates, config-driven control-plane provisioning with auto-added safety middleware, in-sandbox tool calls bridged through the same governed executor funnel, and interchangeable errors-as-data coding-sandbox backends under concurrency/lifetime limits. Source code and direct tests are ground truth; references carry decisive excerpts and graph retrieval.

## Load the matching source dump
- `references/agent-step.md` — the ONE fixed turn unit (guards → shaping → raced guardrail/LLM call → dispatch → footer) every loop must drive.
- `references/loop-strategies.md` — LoopStrategy inversion: six shapes composed from inject_user_message/pinned plans + last_tool_result, shared degraded-success tail.
- `references/single-shot-runner.md` — one-turn agent-as-function: minimal task_complete runtime, prompt-side schema hint, lenient-fence/strict-object parse behind ONE exception type the caller owns fallbacks for.
- `references/best-of-n-judge.md` — N parallel run_child candidates on one team_id; judge degrades to first-successful on every failure mode; local→original winner-index mapping through failed candidates.
- `references/tool-loop-dedup.md` — synchronous pre-wave check-then-add makes duplicate suppression race-free; TAG_DEDUP_EXACT opt-in.
- `references/tool-executor.md` — resolve-before-authorize funnel; repeated-name collapse + toolset-group expansion; usage-hint validation errors.
- `references/spawn-scheduler.md` — validate→launch-all-await-free→await-slots→finally-cancel; depends_on enforced across turns via SPAWN_RESULTS_SLOT.
- `references/scope-state-slots.md` — frozen StateSlot handles (inherit/persist policies) replace Agent fields and ContextVars across Run/Turn/Tool scopes.
- `references/context-auto-compact.md` — head/tail/summary compaction that never splits an assistant message from its tool results.
- `references/streaming-turn-loop.md` — per-token AG-UI deltas as presentation; exactly one StreamCompleteEvent is truth; final_answer arg re-route.
- `references/lazy-toolsets.md` — essentials+pinned upfront, fetch_tools growth under a permission ceiling, eager-by-default explicit grants.
- `references/tool-decorators.md` — @tool for functions AND methods; exception-proof colocated summaries; permissive default terminal outcome.
- `references/malformed-call-repair.md` — sentinel keys convert unparseable JSON args into corrective error results, never silent completions.
- `references/builder-dx.md` — fluent builder that stays a thin setter over the one ControlPlane construction pipeline.
- `references/terminal-tool-contract.md` — TAG_LIFECYCLE_TERMINAL + TerminalTool protocol stops the run without name hardcoding; typed output contract.
- `references/hook-kernel-pipelines.md` — HookRegistry: one Pipeline (fail-closed gates / fail-open reducers) or Wrapper per event; terminal decisions stop inside next().
- `references/hook-decision-monotonicity.md` — escalate-only decision API + severity maps make a later middleware structurally unable to weaken an earlier deny.
- `references/hook-wrapper-retry.md` — PRE_MODEL_CALL is Wrapper-composed nested closures because retry must re-invoke the chain N times.
- `references/turn-guards-installers.md` — marker-idempotent installers on a SHARED kernel; always-on guards vs opt-in behavior-changing gates.
- `references/supervisor-confidence-gate.md` — POST_TOOL_USE tag-keyed result-block for LOW-confidence plans; two-shape tolerant parser; block-the-result-not-the-run.
- `references/control-plane-composition.md` — numbered start() provisioning order; post-store hook wiring; auto-add safety middleware only when absent from hooks.
- `references/execute-code-rpc-bridge.md` — sandbox scripts call host tools through the SAME ToolExecutor funnel over JSON-lines stdio; blocked-tool taxonomy.
- `references/coding-sandbox-lifecycle.md` — errors-as-data runs, filesystem-only persistence, manager-owned concurrency/lifetime limits, mtime artifact diff.
- `references/transport-dialect-layer.md` — LLMTransport ABC as the ONE choke point; per-provider dialect rules (Anthropic cache breakpoints + thinking max_tokens growth, OpenAI reasoning_effort + index-keyed tool-call accumulation, Ollama repeat_penalty=1.0 + native format= structured output); TransportError(status_code, retryable) with 529.
- `references/opik-tracing-decorator.md` — OpikTracingTransport wraps the transport factory so every LLM call is traced with zero per-call-site changes; resolve_opik_gate single AND-function; _guarded_cm fail-open so tracing never breaks a real call.
- `references/run-child-guards.md` — AgentRuntime.run_child is the ONE child-launch path: spawn-depth raise, unconditional TAG_UI_ONLY strip, depth-gated TAG_SPAWN strip, run-context/scope/session inheritance, streaming=mirror_events flip.
- `references/role-factory-wiring.md` — AgentFactory turns Role pure-data into AgentSpec; idempotent wire_sub_agents registers named children as AgentTool on the shared registry for data-declared agent composition.
- `references/context-graph-projection.md` — build_context_graph turns timeline entries into a typed entity graph; run_to_agent pre-pass resolves spawns across out-of-order entries; upsert-merge node semantics.
- `references/timeline-trajectory-export.md` — one run = one JSONL trajectory row; run_id grouping (never trace_id), sequence_id re-sorting, parent_run_id preserved for multi-agent traces.
- `references/decision-trace-rubric-grader.md` — decision_trace folds citations onto their producing decision via (run_id, tool) pending map; RubricGrader grades a trajectory in ONE weighted structured call with per-entry degradation.
- `references/skill-learning-loop.md` — learn_from_execution pipeline: deterministic should_extract → structured extraction with ONE bounded reflection retry → dedup-before-grade quality gate → governor split approved/pending; never persists directly.
- `references/skill-format-layer.md` — agentskills.io-spec SKILL.md with lifecycle extensions namespaced under metadata["agent-loop"]; single shared validator with hard-vs-lint split; skip-not-fail scanning.
- `references/skill-storage-discovery.md` — multi-root store (one writable primary shadowing read-only extras), JSON index cache with deprecated-hidden search, sync catalog_snapshot for the sync prompt builder.
- `references/progressive-disclosure-preloading.md` — three disclosure tiers + PRE_AGENT relevance-band preloading whose mention tier collapses to nothing when the agent lacks load_skill.
- `references/hook-dispatch-bridge.md` — dispatch_* translates kernel decisions back to the legacy exception shape; only PRE_TURN distinguishes RunCancelled via metadata["cancelled"]; plus the async-by-design ContextWindow/ContextBudget seam.
- `references/phase-gate-driver.md` — PhaseDriver round-limited gate loops: only verdict turns consume budget, stopped≠passed outcome split, verify never returns early on a pass, revise→finish ladder.
- `references/checkpoint-snapshot-contract.md` — AgentCheckpoint field contract (identity quintet, budget, todos, persisted extensions, pending_tool_call_id) + the save_checkpoint funnel stamped post-turn and at HIL pauses.
- `references/hil-pause-resume-identity.md` — dual-id HIL bridge: hil_request_id routes the human answer, ORIGINAL call.id addresses the provider ToolMessage; pause-checkpoint-before-blocking.
- `references/run-resume-time-travel.md` — same-instance resume restoring budget/todos/extensions/identity at turn_index+1; rollback as session-tree branching (new run_id, branched_from metadata, history never mutated).
- `references/graph-skill-store.md` — GraphSkillStore on Arango+Neo4j with ONE code path: generic-interface-only access (never execute_query), construction-bound tenancy, owner permission edges, python-side non-equality filtering.
- `references/skill-revision-ledger.md` — version-keyed snapshot ids (millisecond timestamps collide under id-upsert); snapshot-current-then-bump-patch; rollback restores as a NEW revision.
- `references/full-overwrite-usage-carry-forward.md` — extract-before-overwrite keeps accumulated usage counters alive under full-document CRUD; parallel-array encoding for maps on primitive-only stores.
- `references/skill-usage-outcome-loop.md` — POST_AGENT SkillLearning: outcome feedback on ALL runs, extraction only on success, writer-sub-agent persistence handoff; zero-experience skills default healthy.
- `references/graph-usage-tracker.md` — durable per-skill counters co-located on agentSkills via read-modify-write; unknown-skill silent skip; bounded failure-notes ring; directional-not-exact by design.
- `references/builtin-seeder-fork-guard.md` — SEED_IDENTITY writer + updatedBy-based fork detection auto-upgrades unmodified builtin copies and never clobbers org edits; loud zero-packs startup guard.
- `references/scoped-skill-manager.md` — read-path-only decorator over the shared manager: over-fetch-then-filter search, deny-before-inner-call loads, __getattr__ passthrough for governance.
- `references/agent-graph-write-transactions.md` — multi-collection graph CRUD as one txn: validate-before-begin, commit-then-None-stamp rollback guard, attachment failures → partial_success warnings.
- `references/tombstone-delete-rollback-latch.md` — delete-path variant: sentinels declared before any await can raise; rollback attempted in EVERY except arm (double-arm latch); soft-delete-only semantics.
- `references/per-family-delete-recreate.md` — update-path attachment replacement: read-only gather → leaves-to-root delete committed FIRST → separate create txn (no torn deletes, no orphans).
- `references/referenced-skill-link-gating.md` — agent↔skill links are edge-only to externally-owned docs; missing-skill names skipped-with-log inside the same txn; agents never own skill nodes.
- `references/context-shaper-pipeline.md` — ControlPlane composes the PRE_MODEL shapers cheapest-first (L1 budget_reduction → L2 artifact_compaction → L3 clearing → L4 loop_compaction → L5 sliding_window → L6 deterministic → L7 auto_compact → L8 synthesis_guard); the platform factory adds L9 pairing repair and a TWO-phase auto_compact.
- `references/tool-pairing-two-layer-invariant.md` — safe_tail_boundary prevention + one final repair middleware: position-aware orphan drop then orphaned-call strip; global id presence is NOT sufficient.
- `references/loop-compaction-summary-absorption.md` — turn-boundary batch compaction whose marker grammar ([Loop compaction: / [artifacts: / omission count) is parsed back so prior summaries merge instead of cascade-truncating.
- `references/artifact-compaction-schema-priority.md` — old-turn artifacts compact at 0.5×budget; absolute-budget overflow ladders schema-bearing results first (recoverable) then plain, largest-first via O(1) running-total swaps.
- `references/turn-atomic-tool-result-clearing.md` — clears whole TURNS (assistant + its matching results), never individual messages; metadata-only refs keep re-call hints; protected names survive by name lookup; prefix-sniff idempotence.
- `references/deterministic-extractive-compact.md` — LLM-free per-class rules: human text verbatim, prior summaries verbatim, injections capped; call-id→name/args map re-attaches provenance to truncated results; identical input ⇒ identical output.
- `references/sliding-window-group-eviction.md` — oldest-first eviction on a working copy with an original-index map keeping pins stable; ToolMessages never evicted alone (group rides with parent assistant).
- `references/artifact-registration-write-side.md` — POST_TOOL_USE phase 1: over-threshold results persisted via ArtifactStore protocol (fail-open), ToolMessageMeta carried by executor; current turn keeps full content inline.
- `references/synthesis-guard-budget-floor.md` — terminal shaper ladder: strip thinking → clear old results (artifact-aware) → tail-truncate → raise ContextBudgetExceeded rather than send oversized context.
- `references/stall-detection-escalation.md` — POST_TURN error-heavy counter + PRE_MODEL warn-once/hard-directive pair sharing StateSlot state across turns; healthy turns reset both counter and latch.
- `references/require-critique-spawn-tree-gate.md` — installer-style PRE+POST_TOOL_USE gate: LOW-confidence create_plan denies execution tools until critique_plan passes; pending holder inherited BY REFERENCE so the spawn tree shares one bucket.
- `references/tool-preloading-relevance-bands.md` — PRE_AGENT deterministic twin of lazy toolsets: ≥0.75 unlocks the toolset into visible_tools, 0.4–0.75 injects a fetch_tools pointer, grant ceiling intersects before unlock, stale sections popped.
- `references/truncation-recovery-content-builder.md` — POST_MODEL builds synthetic error ToolResults for calls parsed from truncated arguments (escalating after 2 consecutive); short-circuit stays owned by the loop.
- `references/guard-middleware-family.md` — five independent guard layers kept separate: permission (name lists), enforce_mode (risk tags; plan mode read-only with harness-tool exemptions), tool/coding-sandbox safety (literal-text denylists that fire regardless of approval), e2b billing ceiling (requested-timeout budget), retry (Wrapper because it must re-invoke the chain).
- `references/budget-reduction-offload.md` — Layer-1 unconditional per-result char cap with own-marker-only idempotence + the OffloadStore write-only protocol (store-and-preview offload).
- `references/audit-log-per-event-factories.md` — legacy hook OBJECTS become per-event factory functions so ControlPlane composes observation a la carte.
- `references/structured-plan-slot.md` — create_plan dual path: validated step-graph into STRUCTURED_PLAN_SLOT vs verbatim DefaultPlanner text; confidence as enum-arg or trailing-line, never whole-plan JSON; all-errors-collected validation.
- `references/task-completion-outcome.md` — TaskCompletionOutcome parsed by the TOOL not the loop; empty-completion error_result bounce; malformed fields drop individually; tag+protocol terminality needs no name hardcoding.
- `references/final-answer-tool.md` — structured terminal answer behind DEFAULT-OFF flag (JSON payload breaks small-model streaming); formatting/citation rules live in the parameter description (instruction proximity), same Confidence enum as task_complete.
- `references/plan-result-critics-as-tools.md` — critique_plan/verify_result expose critics as tools; deterministic structural gate (registry/dag re-validation) precedes the LLM judge and fails fast with confidence=high failure.
- `references/request-review-hil-escalation.md` — submit → checkpoint-before-blocking (dual-id) → wait; proceed-approved when no HIL store; escalation is a tool because hooks can only enforce, never ask.
- `references/write-todos-stateless-tool.md` — full-replace todo list living on ctx.agent; stateless shared tool instance mutates per-run state through the handle context only.
- `references/find-cycle-keys-only-dfs.md` — one 25-line three-color detector for plan graphs AND spawn batches; keys-only traversal makes dangling edges "satisfied" at spawn time while plan validators pre-reject them.
- `references/child-result-content-shape.md` — the ONE child→parent dict for LLM and programmatic dispatch; omit-not-null fields, artifacts-as-refs-to-dependents, needs_input forced un-skimmable by prefix.
- `references/spawn-direct-dispatch-bypass.md` — single-AgentTool spawns skip the ReAct wrapper hop (domain spec + finalize_output); vetoed when share_parent_results needs the parent conversation.
- `references/detached-spawn-sole-launch-site.md` — detach=true children launch ONLY in handle() (step pre-launch excludes them); completion/failure always recorded to SPAWN_RESULTS_SLOT for later depends_on.
- `references/parent-results-collector.md` — last-REAL-user-message boundary (injected nudges never hide prior results); call-id→name re-association; sandbox tools excluded by default.
- `references/parent-results-budget-ladder.md` — digest preview + _meta'd JSON file staged to sandbox; count-don't-hide degradation ladder, chronological-first truncation, dependency results merged ahead.
- `references/agent-tool-static-composition.md` — AgentSpec as real Tool (middleware uniformity) with PER-PATH recursion guards (inherited spawn_depth inside runs, ContextVar outside); skill backfill across clean-scope children; result_note proximity principle.
- `references/handoff-inplace-role-mutation.md` — horizontal ownership transfer via in-place spec mutation; safe ONLY because step() rebuilds schemas/prompt every turn; visible_tools union never narrows.
- `references/route-task-failsafe-classifier.md` — one structured solo/multi_agent call whose every failure degrades to solo; threshold vocabulary mirrored between router and spawn_agent descriptions.
- `references/replan-advisory-split.md` — replan reads todos→LLM→returns text; write side stays exclusively write_todos'; reason rides inside a goal copy.
- `references/best-of-n-tool-surface.md` — thin adapter over run_best_of_n; read-only/idempotent candidate contract stated at commit time; run-granularity branching keeps the turn kernel untouched.
- `references/clarify-hil-pause-route.md` — record waiting_hil state BEFORE the blocking wait; clarify pauses-and-continues vs task_complete's needs_input completes-and-escalates.
- `references/docker-tar-choreography.md` — archive-only container I/O over a host-persistent working dir; reserved-subdir partitioning; traversal-guarded extraction; pre-run mtime baselines.
- `references/cwd-artifact-promotion.md` — /src mirror-back promotion with staged-byte baselines so cwd-written files become artifacts without re-reporting inputs.
- `references/sandbox-egress-networking.md` — backend×request AND-veto onto ONE dedicated egress bridge; installs always bridged, runs only when both flags agree; compose siblings stay unreachable.
- `references/e2b-command-normalization.md` — CommandExitException→tuple normalization; commands.run() never run_code() (Liskov); installed tsx binary, never npx.
- `references/sanitized-subprocess-env.md` — allowlist-only env with HOME/TMPDIR pointed INTO the sandbox and OUTPUT_DIR exported for cross-backend parity.
- `references/package-spec-validation.md` — fail-closed npm/PyPI charset regexes + PEP 503 canonical keys matched bidirectionally against allow/denylists.
- `references/reflection-engine.md` — failed CodeResult → categorized ErrorAnalysis with canned suggestions; input/-absence targeted override; import hints checked on full text.
- `references/kernel-confinement-ladder.md` — probe-once Seatbelt/bwrap wrap-or-degrade; unix-socket carve-out + mach-lookup allowance are load-bearing.
- `references/process-exit-cleanup.md` — atexit+SIGTERM/SIGINT sweep of registered sandbox dirs chaining pre-existing handlers (library, not owner).
- `references/rlimit-process-execution.md` — setrlimit preexec × confinement as orthogonal layers; killpg whole-group timeout; bootstrap before artifact snapshot.
- `references/local-workingdir-platform-traps.md` — realpath before confinement (macOS /private/var), short dir names (sockaddr_un 104B), output/ created pre-snapshot.
- `references/provider-backed-lazy-toolsets.md` — summary-only registration; per-name materialize lock with inside-lock re-check; materialize_many BEFORE visibility.
- `references/global-catalog-fallback.md` — zero-hit-only tri-state answer (match / exists-unattached / none); search can only say not_attached; SSE per hit.
- `references/special-route-dispatch.md` — handle() swapped in via override_execute INSIDE the executor funnel; registry-first + discovery-based builtin fallback.
- `references/tool-error-taxonomy.md` — alias-not-subclass hierarchy merge; name-vs-path uniqueness split; structured fields over string parsing.
- `references/capability-tag-vocabulary.md` — six TAG_* constants; dispatch asks has-tag never is-name; SPAWN vs SPAWN_BATCH distinction is load-bearing.
- `references/plan-ahead-grounding.md` — upfront plans grounded in EXACT callable names (delegate fallback) with network truth mirrored from the sandbox flag.
- `references/scoped-db-browser-sandboxes.md` — readonly/allowlist SQLite policy via keyword scan (honestly non-adversarial) + lazy single-page Chromium browser sandbox.
- `references/budget-enforcement-plane.md` — BudgetTracker as ONE PRE_TOOL_USE guard auto-added when absent; strict-> limits; model-aware pricing with Sonnet-class fallback; cache read/write tokens priced at input-price ratios (0.10/1.25).
- `references/middleware-path-tag-routing.md` — segment-glob path_match (`*`=one, `**`=zero+, FULL-path only), boundary-safe prefixed, by_tag/by_tags predicates over executor-attached effective tags.
- `references/sandbox-pretool-guard-pair.md` — coding_sandbox_safety denylist + e2b-only billing/timeout guard, both defense-in-depth OVER kernel confinement and both absence-check auto-added on `/toolsets/coding_sandbox/**`.
- `references/streaming-final-answer-extractor.md` — StreamingJsonStringExtractor state machine decoding `answer_markdown` from raw fragments (every escape splittable); per-index None markers; deltas are presentation, StreamCompleteEvent is truth.
- `references/tool-summarizer-dip-port.md` — ToolSummarizer Protocol injected onto runtime (library never imports product); total degradation to empty summaries; result summaries computed BEFORE truncation.
- `references/system-prompt-construction-layer.md` — AgentSpec holds / builder builds; fresh-pure-no-I/O per turn; named sections with spec<run override-by-name; skills overview is level-1 disclosure with do-not-preload instruction.
- `references/agentspec-frozen-contract.md` — Layer-0 pure-data agent definition; frozen ModelSpec.resolve as the one provider→Model site; lazy disclosure = permission ceiling; middleware installers travel WITH the spec.
- `references/toolset-builder-class-cache.md` — class-keyed import-time reflection cache + per-instance binding; connector registration MUST bypass register_toolset_object's name/path equality (`app__method` vs short path segment).
- `references/tool-search-index-split.md` — ToolIndex ABC ranks a LIVE registry snapshot (no index-side catalog, empty query ⇒ no matches); KeywordToolIndex token scoring with name-hit bonus.
- `references/approval-minimal-port-bridge.md` — request_approval(ctx)->bool port delegating to legacy ApprovalHook; legacy name = last path segment; denial stamps approval_decision_id into ctx.metadata.
- `references/contextvar-input-staging.md` — two sibling ContextVars (span-scoped parent-results handoff vs run-long skill resources) with merge-inner-wins nesting and the bare-set workaround for PRE middleware whose `with` block would reset before execute().
- `references/goal-feasibility-preflight.md` — FeasibilityChecker raises typed FeasibilityError between goal build and loop entry when `requires tool:` requirements miss the live registry; vacuous-pass degradation keeps it best-effort.
- `references/memory-consolidation-agent-curated.md` — memory_consolidate = mechanical get-before-delete-many + add-one swap; NO LLM call in the tool layer; provenance via consolidated_from metadata.
- `references/sqlite-fts5-memory-provider.md` — stdlib-only ranked memory: one sqlite3.Connection on to_thread behind one asyncio.Lock, dual-table same-commit writes, ×5 over-fetch before scope filter, bm25 sign inverted at the boundary.
- `references/governor-pressure-grow-shrink-split.md` — ResourceSnapshot carries BOTH baseline-credited `mem_pressure` (governs GROWTH) and raw occupancy `mem_pressure_raw` (drives MEM_SOFT/MEM_HARD brakes); crediting on shrink pushes trip points past what headroom can absorb — pinned by the all-in-one-container OOM regression trio.
- `references/governor-gate-demand-accounting.md` — loop-bound weighted AdmissionGate over a thread-safe LimitRegistry (never revokes, oversized-cost-admitted-alone, 1s safety-net poll, second-loop RuntimeError); integral permit-seconds demand accounting makes ms-scale holds visible to the 15s sampler.
- `references/governor-shielded-acquire-backpressure.md` — warn(10s)-then-wait-then-429 backpressure where shield only beats wait_for's OWN timeout; external cancellation must cancel the detached acquire AND release a permit it already won (`_cancel_and_release_if_granted`), else `in_use` overcounts forever.
- `references/governor-slowstart-ramp-policy.md` — warm-start at floors NEVER honours MAX_CONCURRENT_* as a starting value (the MAX_CONCURRENT_PARSING=1000 OOM incident); TCP-style slow-start doubling gated by resource-delta-vs-last-grow probing; any shrink re-arms slow start; index pool held at ceiling by design.
- `references/governor-resource-probe-chain.md` — dependency-free cgroup v2→v1→meminfo→psutil ladders returning paired-or-nothing fields; delta-based CPU utilisation (never cpu_percent/getloadavg) normalised by host CPUs for host-wide sources; BaselineMemoryTracker low-water-mark calibration capped at 50% of limit.
- `references/governor-service-wiring.md` — one ResourceGovernor per process; gates memoised per pool and bound to whichever loop asks (worker threads create theirs inside their own new_event_loop); sample commits hold `_state_lock` across read→compute→write so any-thread `report_memory_incident()` (BrokenProcessPool/MemoryError halving) can't be clobbered.
- `references/connector-resilience-policy.md` — ONE ResiliencePolicy per connector instance (HTTP transport AND vendor-SDK call sites share it); `pause()` arms a monotonic deadline with max() so a long Retry-After outlives later short backoffs, `acquire()` RE-CHECKS the gate after leaving the token queue or queued callers drain into a fresh throttle; backoff = capped exponential full-jitter with Retry-After winning but ADDED jitter spread, never subtracted time; sub-1/s budgets must spell `AsyncLimiter(1, 1/rate)`.
- `references/consumer-governor-bridge.md` — Kafka/Redis worker-loop consumers bridge to the main loop via run_coroutine_threadsafe+wrap_future+bounded-wait+cancel-on-anything; `acquire_parsing_slot` returns a `ParsingAdmission` carrying its OWN release closure (governor gate w/ tier+XL cost, or legacy semaphore fallback) so release never branches on primitive type; `GateWaiterToken` counts spawned-not-yet-admitted tasks for read-pause backpressure (`max(index,heavy ceilings)×4`); distributed Redis leases size to the RESOLVED CEILING, never the live adaptive value, and light parse gets its own `parsing:light` pool; renewal loop RAISES past its safety deadline instead of holding a reassigned slot.
- `references/messaging-error-backpressure-policy.md` — ordered chain-walking TERMINAL/TRANSIENT classifier (aiohttp transport scan → PARSE_BACKPRESSURE-code scan → terminal-content-chain scan BEFORE status extraction → six-vendor-shape status ladder incl. Google's string-status trap → root-type ladders → TRANSIENT default; classifier itself never raises) + the per-service max()-deadline BackpressureCoordinator whose signal extends-never-shortens and whose consumer polls `is_paused()` BEFORE claiming more work — a downstream 429 stops the READER, not just the failing call.
- `references/artifact-blob-graph-versioning.md` — cross-store version bump (blob store + graph DB): ordered writes, failure marking, dedupe contract instead of distributed transactions.
- `references/artifact-lineage-harness-capture.md` — lineage captured by a harness hook (not an exposed tool); version-pinned DERIVED_FROM edges compose across multi-runs.
- `references/artifact-two-phase-upload-grants.md` — upload grants safe by construction: single-use, server-derived keys, re-verification at commit.
- `references/circuit-breaker-halfopen-probe.md` — HALF_OPEN recovers via health_check() probe claiming a single slot (real requests aren't capped at cooldown); 30s stuck-probe escape.
- `references/dcr-pkce-client-bootstrap.md` — RFC 7591 dynamic registration persistence onto the client record + the PKCE/state helper trio every later auth/refresh needs.
- `references/dual-registry-decorator-discovery.md` — ToolsetRegistry (implemented tools) vs MCPRegistry (metadata templates): registration failure modes — silent import-failure vs loud duplicate type_ids.
- `references/mcp-oauth-discovery-chain.md` — OAuth discovery probe order + URL grammar so endpoints differing from admin-typed/catalog-default URLs still resolve (GitHub delegates hosts; Atlassian serves root-only metadata).
- `references/npm-command-parse-before-trust.md` — rejection ladder converting arbitrary user-pasted install commands into exactly one registry package spec; execution never happens in the parser.
- `references/oauth-refresh-lock-ladder.md` — token refresh serialized across REST route / background sweep / agent-loop on-demand path; legacy-DCR → shared-DCR → static client resolution order.
- `references/oauth-token-dialect-tolerance.md` — content-type ladder + error taxonomy for token exchange/refresh so form-encoded responders (GitHub-style) don't silently break background refresh.
- `references/skill-package-stateless-import.md` — preview→confirm import when the server refuses ALL staging state between calls; archive-safety rules.
- `references/code-span-tiling-engine.md` — ONE recursive tree-sitter walk whose symbols come out of the tiling pass, so byte-exact file coverage is structural; fresh Parser per call over cached Language; comment-forward/whitespace-backward attachment; min-line error reporting over ALL children.
- `references/language-config-table.md` — 18 grammars as frozen LanguageConfig dataclasses + loud import-time duplicate-extension ValueError; per-grammar quirk fields (decorator wrappers, trailing bodies, binding-as-function, declarator unwrap, impl_item name override); `.h` routes to the C++ grammar by design.
- `references/git-blob-file-role-gate.md` — path-only role cascade (GENERATED→TEST→MIGRATION→TYPE_DEF→BUILD→CONFIG→SCRIPT) with whole-segment dir matching and ignore≠classify split; called before the blob is fetched.
- `references/code-block-mapper.md` — two-pass span→block/group placement in separate index spaces; `kind:dotted.scope` identity with full parent chains; groups keep their whole body because only table/view groups get embedded; None-means-skipped contract.
- `references/docstring-signature-ladder.md` — per-language signature/docstring/subtoken metadata over each symbol's OWN tiled text; terminal-on-line Python colon search so annotated params can't hide the docstring; `\A`-anchored block docs so Groovy-style nested trailing comments never hand every member the next member's docs.
- `references/slash-command-plane.md` — flat one-file-one-command markdown commands with OPTIONAL frontmatter (`\A`-anchored regex so empty `---/---` blocks parse); `$ARGUMENTS` replace-all else args APPENDED as a trailing paragraph — never silently dropped; per-file degrade on dir load.
- `references/token-heuristic-context-windows.md` — 4-chars+4-overhead pre-call estimate strictly for SHAPING decisions (real usage lives in the budget tracker — never route estimates into cost enforcement); SlidingWindowContext evicts oldest non-SYSTEM and stalls when only system messages remain; ContextManager never evicts because external summarization owns trimming.
- `references/planner-critic-pipeline.md` — planners depend on `complete()`, critics on `complete_structured()` via single-method Protocols; deliberately asymmetric None-model degradation (Replanner→empty plan / PlanCritic→non-empty-passes / ResultCritic→trust success flag).
- `references/goal-plan-prompt-assembly.md` — Goal→three-section planner prompt; plan stored as RAW text with confidence parsed only from the trailing `Confidence:` line; empty requirement lists render as empty sections, never invented defaults.
- `references/structured-output-coercion.md` — walk-site coercion ladder for schema-HINT responses: list→[] / dict→None (distinguishes empty-object from not-an-object) / stringify-or-None; parse success alone doesn't validate shape.
- `references/model-transport-split.md` — Model = three runtime_checkable single-method Protocols composed into an ABC; TransportModel is the single adapter; per-call outcomes are explicit immutable envelopes (usage/stop_reason/model), never side-channel attributes read after await.
- `references/event-vocabulary-status.md` — additive event dialects (legacy ∥ AG-UI ∥ REASONING_*) fired alongside each other at identical call sites; explicit ToolCallStatus field replaces three consumers' payload-key-sniffing for blocked-vs-done; CompositeEmitter fans out to ALL sinks before raising.
- `references/runner-handle-protocols.md` — AgentRunner/AgentHandle Protocols live in `core/` so below-agent layers import them without cycles; `stream()` yields events only while `last_stream_result` carries the terminal result; RunContext.child inherits trace_id and concurrent-sibling team_id semantics.
- `references/image-embedding-result-ladder.md` — ImageEmbeddingResult ladder: providers NEVER raise and owe one index-keyed result per input; sync-or-async normalisation bridge lives in the ABC; VectorStore._build_image_points is the single failure-decider (error-skip + dimension-mismatch drop).
- `references/multimodal-provider-factory-registry.md` — dict-of-lambda builders keyed by EmbeddingProvider value; create() returns None (never raises) for unknown providers; narrow MultimodalProviderConfig dataclass keeps providers testable without a VectorStore.
- `references/embedding-response-index-mapping.md` — map_embedding_response binds response entries by their OWN `index` field (bool-excluded, arrival-order fallback, first-wins dupes); uncovered positions become errors, never disappear; error bodies surfaced ≤300 chars.
- `references/openai-compat-dual-schema-fallback.md` — OpenAI-compatible image embedding tries the standard batched `input` schema once per batch, then falls back to vLLM's single-image chat-`messages` extension per valid image; invalid images ride outside both arms.
- `references/model-capability-warning-ladder.md` — constructor-time model-name marker scan warns loudly but never gates: Jina allow-list vs Gemini deny-list polarity matches how each vendor fails; Gemini canonicalizes the SDK `models/` prefix.
- `references/titan-http200-failure-channel.md` — Titan reports per-image failures in body["message"] under HTTP 200, so message-presence gates embedding trust; outputEmbeddingLength resolved against {256,384,1024} with warn-and-fallback; return_exceptions gather normalises bare exceptions into results.
- `references/voyage-delegation-tail-completion.md` — Voyage provider delegates wire format to an already-multimodal LangChain Embeddings instance and owns only batching/accounting; short responses get tail-completed with errors so length parity holds.
- `references/cohere-search-document-pinning.md` — Cohere images ride `inputs` with input_type="search_document" (never the undocumented "image" fallback); inputs-batching is embed-v4-only so older generations warn at construction; oversized images log as expected skips.
- `references/ollama-future-proof-stub.md` — supports_multimodal()=False gates dispatch today while embed_images already speaks the PROPOSED upstream schema, so Ollama support lands without a code change here.
- `references/accessible-records-key-freshness.md` — three-class key taxonomy (kb / capp user-independent / cusr per-user hash fields); hash fields carry self-timestamped {"t","m"} envelopes because Redis expires whole keys only; empty maps ARE cached.
- `references/read-through-striped-singleflight.md` — fixed crc32-striped lock array (bounded by construction, stable across processes); double-checked read-through inside the lock; write skipped when the read tripped the breaker mid-request.
- `references/cache-outage-circuit-breaker.md` — any Redis op error trips a 30s monotonic backoff consulted by `enabled`, so one outage costs ONE timeout not three; create() never raises (failure ⇒ disabled cache); loader exceptions propagate unchanged.
- `references/best-effort-invalidation-hooks.md` — register-once module-global invalidator façade whose notify functions are no-ops unregistered and cannot raise; connector apps resolve org via the ORG_APP_RELATION edge (orgId property is KB-only); record-indexed invalidation deliberately KB-only.
- `references/connector-declarative-builder.md` — two-layer builder DSL: outer ConnectorBuilder (identity/auth/scopes) + inner ConnectorConfigBuilder serializable UI-schema dict; `build_decorator()` freezes config, re-points OAuthConfigs to the final name, and VALIDATES at import time so an incomplete OAuth connector crashes startup, never a request.
- `references/connector-instance-registry-sync.md` — `sync_with_database` DEACTIVATES active instances whose connector type vanished from code (never deletes; records intact), skips the KB→"Collections" registry-key alias, and best-effort backfills drifted permissionModel values.
- `references/connector-scope-access-lattice.md` — two predicates that intentionally disagree: team-scope MUTATIONS need creator-or-admin, but VIEWS additionally accept anyone holding an accessible-team-app edge id; personal scope is creator-only for both (admin excluded); unknown scope / exception ⇒ False.
- `references/connector-permission-model-declaration.md` — connectors declare RECORD_LEVEL (safe default) vs APP_LEVEL at build time; value denormalized onto every instance doc so query services route permission resolution WITHOUT importing connector code; startup backfills drift.
- `references/connector-catalog-beta-matrix.md` — catalog filter order scope→beta-flag(fail-open)→enterprise-team exclusion (the ONLY excluded cell: betas at team scope for enterprise accounts)→hidden→tokenized search→paginate→scope counts computed BEFORE search.
- `references/connector-instance-create-contract.md` — authType is write-once at creation (validated against supportedAuthTypes); duplicate-name uniqueness FAILS OPEN on graph errors but genuine duplicates raise ValueError that survives create+update; ORG_APP_RELATION edge follows the node upsert.
- `references/connector-record-upsert-kernel.md` — `_process_record`: version bumps only on real external_revision deltas (placeholders never bump; stub→real = version 0); COMPLETED stays on unchanged content, AUTO_INDEX_OFF is sticky across changes; nullable source fields carried forward because Neo4j `SET n +=` DELETES null keys; QUEUED promotion AFTER publish as a losing-is-fine CAS.
- `references/connector-deadlock-retry-transactions.md` — `@retry_on_deadlock(max_retries=3)` re-executes WHOLE handlers (0.1s·2ⁿ backoff) so each retry opens a fresh transaction; string-shaped detection works without neo4j installed; CAS status writes deliberately stay OUTSIDE transactions.
- `references/oauth-refresh-death-classification.md` — marker-list classifier (`invalid_grant`, …) over provider error STRINGS raises typed RefreshTokenInvalidError; bare 403 deliberately stays transient (WAF may block); missing refresh_token in a refresh response is back-filled with the old one (Google) while a new one wins (Atlassian rotation).
- `references/oauth-toolset-refresher-hardening.md` — second-gen config-path refresher adds per-path schedule-lock atomics, a 10 s REFRESH_COOLDOWN against duplicate schedules, 1 s immediate RE-CHECK instead of inline refresh, exponential retries on credential SAVES (a rotated-but-unsaved token = data loss), and identity-checked task cleanup under lock.
- `references/connector-init-singleflight.md` — module-global per-key asyncio.Lock created via `setdefault` (atomic miss→insert): lazy connector construction must single-flight or N racers multiply the configured rate limit N× with independent backoff states.
- `references/connector-task-manager-duality.md` — SyncTaskManager verbs encode duplicate policy: `start_sync` cancels-and-restarts (user-triggered sync), `start_if_idle` closes the caller's coroutine and declines (Kafka-redelivered reindex must not rewind progress); strong-ref task table + context-inheriting spawn wrapper.
- `references/connector-syncpoint-cursor-vault.md` — delta cursors stored under `{org}/{connector}/{type}/{key}` with declared sensitive fields AES-256-GCM encrypted using sha256(SECRET_KEY); documents carry self-describing `<field>_encrypted` flags; missing SECRET_KEY fails construction.
- `references/connector-notification-backoff.md` — error alerts dedupe per `{connector}:{title}:{message}` in a CLASS-level cache: 1 h initial backoff doubling to a 7-day cap, reset after long silence; INFO/SUCCESS never suppressed; publish is fire-and-forget off the sync path.
- `references/connector-filter-schema-system.md` — FilterField schema (UI) and pydantic Filter parser share ONE closed TYPE_OPERATORS vocabulary; runtime parsing coerces legacy shapes first (string→list, {id,label}→id, dict→(start,end) epoch-ms tuple) then validates operator∈type; SYNC filters gate fetching, INDEXING filters gate record indexing.
- `references/connector-factory-org-binding.md` — slug-keyed class registry (dispatch) vs display-name metadata registry (catalog) coexist by design; factory stamps live `data_entities_processor.org_id` POST-construction (multi-org fix); manual syncs invalidate the accessible-records cache in a finally to match the event-service path.
- `references/record-permission-edge-resolution.md` — USER/GROUP/ROLE principals resolved locally then DROPPED-with-warning when unknown (under-share beats fabricating an id); ORG is the only synthetic source; whole body is error-contained so permission failures never fail record ingestion.
- `references/principal-resolution-ladder.md` — the four-kind lookup table behind every permission edge (USER→email, GROUP/ROLE→(connector_id, external_id) pair, ORG→self.org_id by fiat); unresolved principals are dropped-with-warning, never guessed; PermissionType enum names differ from stored values (`READ`="READER") so all persisted comparisons key on `.value`.
- `references/user-group-membership-rebuild.md` — `on_new_user_groups`: group identity is the external `(connector_id, source_user_group_id)` pair; existing groups adopt the stored uuid then DELETE every inbound PERMISSION edge and rebuild membership wholesale from the full member roster; unresolvable members dropped with warning.
- `references/app-role-membership-plane.md` — `on_new_app_roles` mirrors the group choreography for ROLES nodes; `on_app_role_deleted` is strict (False when absent, `delete_nodes_and_edges` teardown) while user-group deletion treats already-gone as success — a deliberate caller-contract asymmetry.
- `references/group-to-user-permission-migration.md` — copy-or-upgrade transfer of a group's outbound PERMISSION edges onto one email-resolved user: strict `new_level > old_level` over `PERMISSION_HIERARCHY {READER:1..OWNER:4}`, delete-then-recreate on upgrade, malformed `_to` and per-edge errors contained, single final batch inside one transaction.
- `references/atomic-migrate-and-dissolve.md` — `migrate_group_to_user_by_external_id`: resolve-by-vendor-id → migrate → delete group ALL in one transaction; missing group = success-shaped no-op making re-delivery idempotent; Confluence drives it per-user during member sync.
- `references/incremental-membership-events.md` — webhook-scale single-edge add/remove between full rebuilds: False means nothing-done across every failure mode (unknown principal, absent group, duplicate add, no edge to remove); add is check-then-create so double-delivered events converge.
- `references/record-group-topology-edges.md` — `on_new_record_groups` four-edge ladder: BELONGS_TO→org always, →app only at observed roots (graph-read not payload-trust), parent BELONGS_TO + twin INHERIT_PERMISSIONS iff `inherit_permissions`, placeholder synthesis for unknown parents; rename touches only name+timestamp.
- `references/citation-tracking-live-tool-registration.md` — `citation_tracking` POST_TOOL_USE: register-if-absent idempotency under concurrent waves, unconditional caller grant + internal-search-gated root grant; a mid-run registration is invisible under an explicit tool_names grant unless appended there too.
- `references/fetch-record-live-rebuild-tool.md` — `_FetchFullRecordTool` rebuilds from the CURRENT virtual-records map every execute (retrieval replaces the map, snapshots go stale); the write-back getter must return THE dict in tool_state, never a defaulted copy.
- `references/attachment-rehydration-ladder.md` — turn-0-only PRE_TURN refill of citation maps from blob for follow-up requests, with post-snapshot visible_tools patching and honest could-not-load constraints.
- `references/image-injection-dedup-identity.md` — two image producers, one first UserMessage: per-image dedup keyed on normalized data URL identity (never "any image present"), pop-don't-peek batch delivery, capability-gated fallback registration.
- `references/turn-aware-tool-blocking.md` — consecutive-failure blocking counts TURNS not calls (parallel same-turn failures are one attempt fanned out); PRE_TOOL_USE deny because visible_tools is never consulted in single-registry configuration.
- `references/retry-with-status-wrapper.md` — product-specific SSE "retrying..." status rides the generic retry ladder as a PRE_MODEL_CALL wrap middleware: emit before each sleep, fail-open sink writes, None-sink tolerance.
- `references/terminal-answer-streaming.md` — TerminalAnswerStreamer turns AgentEvents into live answer_chunk SSE: per-token raw deltas, rate-limited citation re-normalization, TOOL_CALL_START preamble clearing with terminal-tool carve-out, withheld-flush at completion.
- `references/empty-response-completion-gate.md` — POST_MODEL nudge on empty responses via the recovery-message channel, bounded TREE-WIDE (context is shared parent+children), truncated responses left to their own handler.
- `references/error-string-classification.md` — hint-ladder classifier over flattened error strings (exception objects are gone); content_filter checked FIRST because its bodies embed retryable-looking codes while retrying is actively wrong.
- `references/history-seeded-preturn-hooks.md` — four orthogonal PRE_TURN restorers over one bounded transcript (results-reuse nudge, artifacts, attachments, visible tools); visible_tools seeding must run after RunScope exists or it silently no-ops.
- `references/result-accumulation-metadata-stash.md` — PRE stashes start-time+args into ctx.metadata, POST merges them into the legacy entry shape RespondPipeline already consumes; schema compatibility is the contract.
- `references/model-tier-scaffolding.md` — frozen per-request ModelProfile: reasoning clamps FRONTIER, local providers always SMALL, ≤32k SMALL/≤128k MID/else FRONTIER, unknown falls back 8k; worked traces go to MID only (SMALL mimics example format as literal text).
- `references/reasoning-persistence-gate.md` — env opt-OUT storage gate with uniform 4k truncation across both payload shapes; None omits the additive field entirely; live streaming is never gated.
- `references/registry-backed-artifact-store.md` — L1 in-memory + L2 registry adapter: write-through with manual L1 seeding, fail-open degradation to memory-only on registry failure, factory downgrade for standalone runs.
- `references/ask-user-question-eager-sse.md` — interactive payloads emitted from POST_TOOL_USE the moment they exist, gated on sink+UI-client, lenient JSON parse, shared emitted flag makes downstream fallbacks no-ops.
- `references/tool-naming-resolution-vocabulary.md` — registry-first path→LLM-name resolution with structural fallback; one owned INTERNAL_SEARCH vocabulary constant (four historical spellings) gates grant decisions across hooks.
- `references/capability-adaptive-hybrid-search-fanout.md` — capability-flag-gated HybridSearchRequest builder: sparse vectors only for client-side-sparse stores, text only for server-side-BM25 stores, per-request RRF declaration, cross-query point-id dedup first-wins.
- `references/embedding-config-hash-cache.md` — re-read AI_MODELS config every call, sha256[:16] hash over {provider,isDefault,model,endpoint}, outside-lock fast path + double-checked rebuild in to_thread, stamp-after-success, None-on-error; SparseEmbedder same shape capability-gated.
- `references/permission-first-search-ladder.md` — accessible virtualRecordId→recordId map BEFORE searching, must-filter {orgId, virtualRecordId∈accessible}, intersect returned ids with the permission map before fetching (cross-connector-leakage guard), explicit None-limit normalization, typed empty envelopes (404/503/200/500), tool-path falsy contract.
- `references/silent-batch-recovery-fetch.md` — order-preserving dict.fromkeys dedup → GRAPH_BATCH_CHUNK_SIZE IN-chunks → diff requested-vs-returned ids because swallow-errors helpers make failures look like "no rows" → concurrent per-id fallback with return_exceptions containment; files+mails+locations legs in ONE gather.
- `references/reranker-lazy-fusion-degradation.md` — constructor loads nothing; locked one-shot CrossEncoder load in to_thread (cuda fp16); TABLE scores content[0], IMAGE/empty excluded symmetrically from pair-building AND score-index walks; fixed 0.3·retriever+0.7·reranker fusion; every failure degrades to default-scored original order.
- `references/block-group-rehydration-knowledge-search.md` — isBlockGroup presence-gated branch, per-virtual-id memoized blob rehydration (None ⇒ skip), get_flattened_results splice where TABLE+seven valid group labels unpack (summary, children) tuples into individually citable results, one global score-desc sort, required_fields citation filter.
- `references/agent-search-state-accumulation-dedup.md` — key-based first-wins append into the shared ChatState `final_results` pool: dedup key `{virtual_record_id}_{block_index}`, entries missing either part are NEVER deduped (always appended); the state dict IS the cross-call accumulation bus for parallel/repeat tool calls so citations cannot inflate.
- `references/tool-error-in-content-envelope.md` — bare-str `@tool` ⇒ `ToolOutput.success` always True: every failure path funnels into a one-line `{"status":"error",...}` JSON envelope inside content; the colocated result_summary parses the CONTENT (`parse_json_maybe` → status=="error") instead of trusting `result.is_error`; module-global `_SOURCE_LABELS` is concurrency-safe by UUID uniqueness.
- `references/scope-narrow-fallback-fanout.md` — `KnowledgeScope.narrow_to` intersects model-supplied ids with the curated session filters; empty intersection returns SELF (hallucinated ids degrade to FULL agent scope, never an empty search); fan-out fires only for explicit ids resolving to >1 SAME-type source, per-leg `NO_KB_SELECTED` sentinel isolation + `return_exceptions=True` containment, first error status surfaces only on total failure.
- `references/retrieval-limit-budget-divisor.md` — no limit parameter exposed to the model: `adjusted_limit = 50` for ≤1 source else `100 // min(total_sources, 5)` — the divisor saturates at 5 so ≥5 sources floor at 20; the budget keys off CONFIGURED scope (state apps/kb for placeholder agents), never the requested ids.
- `references/record-escalation-candidate-plan.md` — coverage computed over ALL accumulated `final_results` (parallel calls report global counts), candidates walk records in pre-sort RELEVANCE order (deduped, ≤8, log-only exclusion reasons), the plan runs REGARDLESS of needs_whole_document (counts are the model's only fetch signal; the flag only chooses framing); coverage note at TOP, candidate table at BOTTOM.
- `references/multipart-image-return-stash-gate.md` — `is_multimodal_llm` config flag (never model-name sniffing) gates the multipart `[TextPart, *ImageParts]` return; the `pending_tool_images` fallback stash is written ONLY when `supports_multipart_tool_result=False` — writing both leaks memory across turns; ImageBudget exhaustion degrades to text, non-multimodal never receives an ImagePart.

## Capsule map
- **Turn engine** — `agent-step`: PRE_TURN guards → pre_model shaping over a copy → guardrail RACES the model call → POST_MODEL recovery veto → stale-round footers; deterministic concerns fire inside step regardless of loop shape. `loop-strategies`: ReAct/SingleShot/Reflexion/PlanExecute(pinned verbatim plan)/PlanCritiqueExecute(PhaseDriver)/Incremental all call the same primitives; ≥40-char max-turns degradation to success. `streaming-turn-loop`: reasoning envelopes lazily opened, deltas never touch history, missing StreamCompleteEvent ⇒ error.
- **Tool path** — `tool-executor`: single call_tool funnel (special routes ride override_execute under the same hooks), resolve-before-authorize, doubled-name collapse, parameter-list validation hints. `tool-loop-dedup`: whole-wave sync dedup pre-pass, tag-gated, steering skip message. `malformed-call-repair`: in-band sentinels short-circuit before resolve/execute with a re-issue lesson. `terminal-tool-contract`: tag+protocol terminality; first terminal outcome's confidence/record_ids/needs_input surface on AgentResult. `tool-decorators`: @tool dual mode (function wrap vs ToolMeta method annotation), exception-proof summaries, permissive default terminal outcome. Planning and coordination tools: `final-answer-tool`, `write-todos-stateless-tool`, `request-review-hil-escalation`, `clarify-hil-pause-route`, `replan-advisory-split`, `plan-result-critics-as-tools`, `task-completion-outcome`.
- **Scale & disclosure** — `lazy-toolsets`: 3 schemas instead of 300; ceilings only narrow. `context-auto-compact`: ratio-triggered middle-summarization between pinned head and group-atomic tail; preservation-list summarizer prompt; naive fallback. `parent-results-collector`, `parent-results-budget-ladder`, `child-result-content-shape`: parent/child result folding under context budgets.
- **Composition & state** — `spawn-scheduler`: pure batch validation (cycles/dupes/unresolved tools), await-free launch, prerequisite-output goal folding with JSON-aware truncation + artifact file staging, orphan-cancel finally. `scope-state-slots`: object-keyed slots with inherit-by-reference vs persist-to-checkpoint policies; ToolScope.messages snapshot timing is load-bearing for HIL resume. `builder-dx`: BuiltAgent bundles agent+control_plane for resource release; create_agent one-liner. `single-shot-runner`: the whole turn loop compressed to a one-call structured function for auxiliary jobs (intent/goal/judge). `best-of-n-judge`, `best-of-n-tool-surface`: judged parallel candidates over the standard child-launch primitive with total judge degradation. `structured-plan-slot`, `spawn-direct-dispatch-bypass`, `detached-spawn-sole-launch-site`, `route-task-failsafe-classifier`, `agent-tool-static-composition`, `handoff-inplace-role-mutation`, `find-cycle-keys-only-dfs`.
- **Decision pipeline (hooks kernel)** — `hook-kernel-pipelines`: one Pipeline/Wrapper per event, per-event terminal predicate + fail-closed/fail-open error policy. `hook-decision-monotonicity`: private `_decision` + escalate-only methods over severity maps — deny can never be un-decided. `hook-wrapper-retry`: PRE_MODEL_CALL alone is Wrapper-backed (re-invocable next). `turn-guards-installers`: marker-idempotent installers because Agents share one kernel; behavior-changing gates stay opt-in. `supervisor-confidence-gate`: deterministic LOW-plan block as POST_TOOL_USE result-block keyed on tag. `hook-dispatch-bridge`: dispatch_* translates kernel decisions back into Agent.run()'s legacy exception shape — only PRE_TURN distinguishes RunCancelled via metadata["cancelled"]; companion ContextWindow/ContextBudget seam (async-by-design interface, system-immortal eviction).
- **Service wiring & sandboxing** — `control-plane-composition`: numbered start() pipeline, deferred post-store hook wiring, auto-add safety middleware with absence checks. `execute-code-rpc-bridge`: in-sandbox tool() calls ride the same executor funnel; blocked tools need turn context or would recurse sandboxes. `coding-sandbox-lifecycle`: CodeResult errors-as-data + filesystem-only persistence across backends; SandboxManager sweeps/limits; $OUTPUT_DIR alias normalization; mtime artifact diff.
- **Provider transport & tracing** — `transport-dialect-layer`: LLMTransport ABC as the single choke point (explicit-value returns, defensive usage→0, TransportError(status_code,retryable) with 529); per-provider dialects (Anthropic cache-breakpoint discipline + thinking max_tokens growth, OpenAI reasoning_effort + index-keyed tool-call accumulation, Ollama repeat_penalty=1.0 + native format= structured output). `opik-tracing-decorator`: OpikTracingTransport wraps the transport factory (zero per-call-site changes); resolve_opik_gate single AND-function; _guarded_cm fail-open so tracing never breaks a real LLM call.
- **Sub-agent wiring** — `run-child-guards`: run_child is the ONE child-launch path — spawn-depth raise, unconditional TAG_UI_ONLY strip, depth-gated TAG_SPAWN strip, parent_run_ctx.child() inheritance, parent_scope one-shot stash, streaming=mirror_events flip. `role-factory-wiring`: AgentFactory turns Role pure-data into AgentSpec (override semantics); idempotent wire_sub_agents registers named children as AgentTool on the shared registry for data-declared agent composition.
- **Eval & timeline surfaces** — `context-graph-projection`: pure timeline→ContextGraph projection (spawned/called/blocked/touched/cited edges; run_to_agent pre-pass; upsert-merge nodes). `timeline-trajectory-export`: run-per-row JSONL for eval/RL datasets (run_id grouping, sequence_id re-sort, parent_run_id preserved). `decision-trace-rubric-grader`: decision reconstruction with citation folding + one-call weighted LLM grading.
- **Skill self-creation loop** — `skill-learning-loop`: extract→evaluate→govern pipeline with bounded reflection retry and never-persist-directly handoff. `skill-usage-outcome-loop`: activation/outcome feedback on all runs, extraction on success only, keep/refine/deprecate health ladder. `progressive-disclosure-preloading`: 3-tier disclosure + relevance-band PRE_AGENT preload with actability-gated pointers. `skill-format-layer`: agentskills.io-compatible SKILL.md parse/validate/render round-trip. `skill-storage-discovery`: multi-root store + JSON index + sync catalog read model.
- **Durable skill platform (graph-backed)** — `graph-usage-tracker`: per-skill usage counters on the skill's own document, RMW-not-$inc by design, unknown skills silently skipped. `builtin-seeder-fork-guard`: system-identity seeding with updatedBy fork detection — upgrades unmodified copies only. `scoped-skill-manager`: per-agent assignment view over reads/loads; writes delegate untouched.
- **Gated phases & durable runs** — `phase-gate-driver`: round budgets count only verdict turns; stopped (run over) is orthogonal to passed (gate green); verify phase's revise→finish ladder. `checkpoint-snapshot-contract` + `hil-pause-resume-identity` + `run-resume-time-travel`: what a checkpoint carries, the dual-id HIL bridge across restarts, same-instance resume with genuine identity continuity, session-tree rollback branching.
- **Graph-write durability (API routes)** — `agent-graph-write-transactions`: one txn across ~12 collections with validate-before-begin and the commit-then-None-stamp latch. `tombstone-delete-rollback-latch`: double-arm rollback when failures can originate outside the owning try. `per-family-delete-recreate`: two-phase attachment replacement (delete committed before re-create). `referenced-skill-link-gating`: edge-only links to externally-owned skill documents.
- **Graph-backed skill store** — `graph-skill-store`: one implementation for Arango+Neo4j via generic-interface-only access and construction-bound tenancy. `skill-revision-ledger`: version-keyed snapshots immune to millisecond upsert collisions; monotonic rollback. `full-overwrite-usage-carry-forward`: accumulated counters survive full-document overwrites via extract-before-write; parallel arrays as the map encoding primitive-only stores accept.
- **Context-engine middleware library** — `context-shaper-pipeline` composes the shapers cheapest-first with per-layer config flags. Compaction family: `loop-compaction-summary-absorption` (marker-grammar merge), `deterministic-extractive-compact` (LLM-free rules), `artifact-compaction-schema-priority` + `turn-atomic-tool-result-clearing` (payload replacement, turn-granular), `sliding-window-group-eviction` (atomic groups, original-index pins). Payload policy: `budget-reduction-offload`, `artifact-registration-write-side` (two-phase pipeline's write side). Terminal safety: `synthesis-guard-budget-floor` (typed raise) and `tool-pairing-two-layer-invariant` (prevention + final repair net). Cross-turn behavior: `stall-detection-escalation` (StateSlot pair) and `require-critique-spawn-tree-gate` (by-reference inherited pending gate). Disclosure & recovery: `tool-preloading-relevance-bands`, `truncation-recovery-content-builder`. Guards & observation: `guard-middleware-family`, `audit-log-per-event-factories`.
- **Coding-sandbox backends & support kit** — `docker-tar-choreography` (archive-only I/O, reserved subdirs, mtime baselines, traversal-guarded extraction) + `cwd-artifact-promotion` (/src mirror-back with staged-byte baselines — restores local-backend artifact parity) + `sandbox-egress-networking` (dedicated egress bridge behind a backend×request AND-veto). Local stack: `rlimit-process-execution` (setrlimit × confinement as orthogonal layers, killpg timeout, bootstrap-before-snapshot), `sanitized-subprocess-env` (allowlist env; HOME/TMPDIR into the sandbox; OUTPUT_DIR parity export), `local-workingdir-platform-traps` (realpath for Seatbelt; short names for AF_UNIX), `kernel-confinement-ladder` (probe-once wrap-or-degrade with the unix-socket and mach-lookup carve-outs), `process-exit-cleanup` (handler-chaining atexit/signal sweep). Shared kit: `package-spec-validation` (fail-closed regexes + PEP 503 bidirectional canonical keys), `reflection-engine` (category→suggestion ladder + input/-absence override). Remote twin: `e2b-command-normalization` (exception→data; no stateful run_code).
- **Tools plumbing seams** — `provider-backed-lazy-toolsets` (summary-first protocol; per-name materialize lock + re-check; materialize_many before visibility), `global-catalog-fallback` (zero-hit tri-state "exists-unattached" answer, never an execute grant), `special-route-dispatch` (handle() via override_execute inside the funnel; registry-first + discovery-based builtin fallback), `tool-error-taxonomy` (alias-merged hierarchy; name-vs-path uniqueness split), `capability-tag-vocabulary` (has-tag dispatch; SPAWN vs SPAWN_BATCH split), `plan-ahead-grounding` (plans reference exact/delegate tool names with sandbox network truth mirrored). Phase-3 taxonomy shells: `scoped-db-browser-sandboxes` (readonly/allowlist keyword-scan SQL policy; lazy single-page Chromium). Pass-10 additions: `budget-enforcement-plane` + `middleware-path-tag-routing` + `sandbox-pretool-guard-pair` (kernel-plane guards/routing), `streaming-final-answer-extractor` (turn-engine presentation), `tool-summarizer-dip-port` (runtime DIP), `system-prompt-construction-layer` + `agentspec-frozen-contract` (spec/prompt layers), `toolset-builder-class-cache` + `tool-search-index-split` + `approval-minimal-port-bridge` (tools plumbing). Pass-11 additions: `contextvar-input-staging` (sandbox file handoff), `goal-feasibility-preflight` + `memory-consolidation-agent-curated` + `sqlite-fts5-memory-provider` (agent pre-flight gate + memory tool/provider surface).
- **Resource-adaptive admission control (pass 12)** — `governor-pressure-grow-shrink-split`: baseline-credited pressure for growth vs raw occupancy for shrink brakes. `governor-gate-demand-accounting`: loop-bound weighted gates + thread-safe registry + folded permit-seconds demand. `governor-shielded-acquire-backpressure`: shielded warn/wait/429 with cancel-race permit release. `governor-slowstart-ramp-policy`: floor warm-start (caps raise ceilings, never skip ramps), slow-start doubling, delta-gated steps. `governor-resource-probe-chain`: cgroup ladders, delta-only utilisation, baseline calibration. `governor-service-wiring`: per-process governor across HTTP routes + worker-thread loops + incident fast path. Pass-13 extensions: `connector-resilience-policy` (per-connector rate-limit + backoff-gate + retry budget shared by HTTP and SDK call sites), `consumer-governor-bridge` (worker-loop→main-loop bridging, ParsingAdmission carry-your-release, waiter-count read-pause backpressure, ceiling-sized cluster leases), and `messaging-error-backpressure-policy` (ordered chain-walking failure taxonomy + per-service read-pause coordinator).
- **OAuth/MCP/artifact platform seams (pass 12 adoption of interrupted sibling drafts)** — `oauth-refresh-lock-ladder`, `oauth-token-dialect-tolerance`, `dcr-pkce-client-bootstrap`, `mcp-oauth-discovery-chain` (connector OAuth plane); `dual-registry-decorator-discovery`, `npm-command-parse-before-trust` (toolset/npm registries); `artifact-blob-graph-versioning`, `artifact-lineage-harness-capture`, `artifact-two-phase-upload-grants` (artifact store durability); `circuit-breaker-halfopen-probe` (base_client resilience); `skill-package-stateless-import` (stateless import choreography).
- **Code ingestion & parsing plane (pass 14)** — `code-span-tiling-engine`: one walker whose symbols come out of the tiling pass so byte-exact coverage is structural, not per-language hope. `language-config-table`: 18 grammars as frozen dataclasses with loud duplicate-extension detection. `git-blob-file-role-gate`: path-only index-vs-skip cascade with whole-segment matching and ignore≠classify split. `code-block-mapper`: spans→block tree with `kind:dotted.scope` identities and embed-free whole-body groups. `docstring-signature-ladder`: style-dispatched metadata extraction whose two regex scoping laws (terminal-on-line colon, `\A`-anchored docs) fail silently when ported wrong.
- **Library substrate seams (pass 15, zero-drift evidence sweep)** — `slash-command-plane`: markdown `/commands` expand into agent GOALS; `$ARGUMENTS` replace-all else append-not-drop. `token-heuristic-context-windows`: 4-chars+4-overhead shaping estimate kept strictly separate from the real-usage budget ledger; never-evicting ContextManager pairs with external summarization while SlidingWindowContext protects system messages. `planner-critic-pipeline` + `goal-plan-prompt-assembly`: planners ride `complete()` over RAW-text plans with trailing-Confidence parsing; critics ride `complete_structured()` with deliberately asymmetric None-model degradation; empty goal sections render empty. `structured-output-coercion`: walk-site list→[]/dict→None ladder over schema-HINT responses. `model-transport-split`: three runtime_checkable single-method Protocols compose into Model; TransportModel is the sole adapter; immutable response envelopes replace side-channel usage reads. `event-vocabulary-status`: additive legacy∥AG-UI∥REASONING dialects fired alongside each other; explicit ToolCallStatus replaces payload-key-sniffing; CompositeEmitter fans out to ALL sinks before raising. `runner-handle-protocols`: AgentRunner/AgentHandle Protocols live in `core/` to break import cycles; `stream()` yields events only while `last_stream_result` carries the terminal result; RunContext.child inherits trace_id/team semantics.
- **Multimodal embeddings plane (pass 16)** — `image-embedding-result-ladder`: never-raise, index-keyed one-result-per-input contract; ABC owns the sync/async normalisation bridge; `_build_image_points` is the single failure-decider (error-skip + dimension-mismatch drop). `multimodal-provider-factory-registry`: dict-of-lambda builders keyed by provider value, None-for-unknown, narrow config dataclass. `embedding-response-index-mapping`: bind by the response's OWN index field (bool-excluded, arrival fallback, first-wins), uncovered positions error-not-drop. `openai-compat-dual-schema-fallback`: standard batched `input` schema once per batch → vLLM chat-`messages` per-image fallback. `model-capability-warning-ladder`: warn-don't-gate constructor marker scans (Jina allow-list vs Gemini deny-list polarity). `titan-http200-failure-channel`: body["message"] is the failure channel under HTTP 200; outputEmbeddingLength ∈ {256,384,1024} resolved-with-warning. `voyage-delegation-tail-completion`: delegate wire format to LangChain, own batching/accounting, tail-complete short batches. `cohere-search-document-pinning`: input_type="search_document" discipline; inputs-batching is v4-only (warn older). `ollama-future-proof-stub`: capability flag gates today, embed_images already speaks the proposed upstream schema.
- **Distributed permission cache plane (pass 16)** — `accessible-records-key-freshness`: three-class key taxonomy with per-user hash fields carrying self-timestamped {"t","m"} envelopes under a write-refreshed key TTL; empty maps cached. `read-through-striped-singleflight`: fixed crc32-striped lock array + double-checked read-through + enabled-gated write. `cache-outage-circuit-breaker`: 30s monotonic backoff makes one outage cost one timeout; create() never raises; loader exceptions propagate unchanged. `best-effort-invalidation-hooks`: register-once/no-op-unregistered façade; ORG_APP_RELATION-edge org resolution (orgId property is KB-only); record-indexed invalidation deliberately KB-only so sync bursts don't thrash the cache.
- **Production hook middlewares & streaming surface (pass 20)** — `citation-tracking-live-tool-registration` + `fetch-record-live-rebuild-tool` (reactive tool registration/grant ladder and its live-rebuild data contract), `attachment-rehydration-ladder` + `image-injection-dedup-identity` (cross-request attachment/citation state, per-image dedup injection), `turn-aware-tool-blocking` (consecutive-failure deny keyed on turns), `retry-with-status-wrapper`, `terminal-answer-streaming`, `empty-response-completion-gate`, `error-string-classification`, `history-seeded-preturn-hooks`, `result-accumulation-metadata-stash`, `model-tier-scaffolding`, `reasoning-persistence-gate`, `registry-backed-artifact-store`, `ask-user-question-eager-sse`, `tool-naming-resolution-vocabulary`.
- **Connector-platform kernel (pass 18)** — `connector-declarative-builder` (import-time validated decorator DSL over serializable config; OAuth auto-registration keyed by final name), `connector-instance-registry-sync` (deactivate-only orphan reconciliation, never delete), `connector-scope-access-lattice` (view ⊇ access for team scope; creator-only personal), `connector-permission-model-declaration` (denormalized RECORD/APP_LEVEL so queries never import connector code), `connector-catalog-beta-matrix` (beta×account×scope filter order, fail-open flag, counts-before-search), `connector-instance-create-contract` (write-once authType, fail-open name uniqueness), `connector-record-upsert-kernel` (version-fill ladder, sticky AUTO_INDEX_OFF, null carry-forward vs `SET n +=` deletion, post-publish CAS to QUEUED), `connector-deadlock-retry-transactions` (whole-handler re-execution decorator), `oauth-refresh-death-classification` (marker classifier, 403-stays-transient, preserve-or-rotate refresh tokens), `oauth-toolset-refresher-hardening` (schedule-lock atomics, 10 s cooldown, save-retries), `connector-init-singleflight` (setdefault per-key lock; racers multiply rate limits), `connector-task-manager-duality` (cancel-restart vs decline-if-busy by job semantics), `connector-syncpoint-cursor-vault` (AES-GCM cursors with self-describing encrypted flags), `connector-notification-backoff` (class-level 1 h→7 d alert ladder, INFO never suppressed), `connector-filter-schema-system` (one closed operator vocabulary shared by UI schema and runtime parser), `connector-factory-org-binding` (post-construction org stamp = multi-org fix; finally-invalidate manual syncs), `record-permission-edge-resolution` (resolve-or-drop principals, error-contained). Pass-19 additions (group/role membership & migration plane — `data_source_entities_processor.py` :1615–2746 whole-region): `principal-resolution-ladder` (four-kind lookup table; drop-with-warning; enum name≠value trap), `user-group-membership-rebuild` (external-id identity + wholesale delete-edges-to rebuild), `app-role-membership-plane` (role upsert mirror + strict-vs-lenient delete asymmetry), `group-to-user-permission-migration` (copy-or-upgrade over PERMISSION_HIERARCHY, single final batch), `atomic-migrate-and-dissolve` (one-transaction migrate→delete with idempotent miss), `incremental-membership-events` (False-means-nothing-done single-edge deltas), `record-group-topology-edges` (org/app/parent/inherit four-edge ladder + placeholder synthesis).

- **Enterprise-search retrieval plane (pass 21)** — the query→permission-filtered-hybrid-search pipeline behind `QueryAppContainer` and six production callers (REST /search, agent tool, chat prefetch/bridge, KG ops): `capability-adaptive-hybrid-search-fanout` (one request builder, provider capabilities decide sparse/text legs), `embedding-config-hash-cache` (per-call config freshness + per-hash instance construction), `permission-first-search-ladder` (accessible-map-before-search ordering is the leak-proof invariant; None-limit incident normalization; typed empty envelopes), `silent-batch-recovery-fetch` (diff-based recovery when batch helpers swallow errors), `reranker-lazy-fusion-degradation` (locked lazy load, twin-walk block-type skip discipline, 0.3/0.7 fusion, total degradation), `block-group-rehydration-knowledge-search` (flag-presence gating, memoized blob rehydration, tuple-unpack child splice).
- **Agent knowledge-search tool surface (pass 22)** — the `Retrieval.search_internal_knowledge` agent tool that CONSUMES the pass-21 retrieval plane, with ChatState as the cross-call accumulation bus: `agent-search-state-accumulation-dedup` (key-based first-wins append; incomplete keys never deduped), `tool-error-in-content-envelope` (bare-str tool ⇒ errors live only in a JSON-in-content envelope; summaries parse content, never is_error), `scope-narrow-fallback-fanout` (intersect-or-widen scope; explicit-ids-only same-type fan-out with per-leg containment), `retrieval-limit-budget-divisor` (saturated-divisor internal budget, no model-facing limit), `record-escalation-candidate-plan` (global coverage → relevance-ordered fetch plan, unconditional, two-placement render), `multipart-image-return-stash-gate` (flag-gated multipart Parts return vs either/or fallback stash).
## Extending the foundation
Pass 20 (2026-08-24, drift re-entry `c28d1336`→`6850972`, giants-sweep lane): ff-pulled +2 commits — production-code delta ZERO (one test fixture `jira_issue_blocks.expected.json`, rest changelog/docs/CI) so capsules cite the drifted tree at `6850972`; executed the standing citation-vs-inventory census (453 uncited files) and posed the named question "how do production middlewares ride the mined hook kernel and how does the turn stream out" → mined whole-file the never-cited `agents/agent_loop/hooks/*` plane (attachment_resolver 640L, citations 377L, artifact_context 180L, memory 130L, retry_with_status 106L, tool_blocking 88L, result_accumulation 63L, completion_gate 61L, ask_user_question 64L, _tool_naming 55L) plus answer_streamer 232L, error_classification 85L, reasoning_persistence 64L, model_tier 119L, artifact_store 134L and the `_build_hooks` wiring region of factory.py :804-944 → TEN capsule-v2 in NEW map group 'Production hook middlewares & streaming surface' = 186→196. Gate 3: search_graph resolves citation_tracking :302-349 / ToolErrorTracker Methods / TerminalAnswerStreamer._clear_preamble :212-232 / classify_error :57-82 / ModelProfile.resolve :51-86 line-exact on the slugged twin; check_index_coverage stdin-JSON ×23 cited paths = no_recorded_issue+metadata_match generation_matches=true EXCEPT test_citations.py freshness=missing (read directly from source; labeled direct-source evidence in-capsule). Gate 5 REAL RUNNER via pass-17 recipe venv: adapter+hooks suites test_citations 5 + test_hooks 15 + TestToolErrorTracker 9 + test_retry_with_status 9 + test_answer_streamer 24 + test_completion_gate 7 + test_error_classification 5 + test_artifact_context 8 + test_citation_tracking 22 = 105 passed rc0 at pin, plus test_attachment_resolver 39 + test_ask_user_question/test_artifact_store green. Probe anchors live-grepped pre-write (~25 counts: register_tool_if_absent=3, require_internal_search_reference=7, turn_index=8, PIPESHUB_ANSWER_DELTA_INTERVAL_MS=3 via -cF, completion_gate_nudges=3, _HINTS=10, _MAX_REASONING_CHARS=4, _LOCAL_PROVIDERS=2, INTERNAL_SEARCH_TOOL_NAMES=2, visible_tools(memory)=10, goal.constraints.append trio=4). OMITTED-WITH-REASON pass 20: hooks/__init__.py (pure exports), sse_emitter/direct_transport/langchain_transport/mcp_* adapters (transport planes already owned by transport-dialect-layer + streaming-turn-loop capsules), evals/decomposition harness (eval tooling not a porting surface), prompt_traces/instance_creator/sub_agent_prompt (prompt-builder plane), protocol/transcript_collector (AG-UI export shape, adjacent to timeline-trajectory-export). Add one `references/<seam>.md` capsule for one graph-selected, source-confirmed porting question. Pass 19 (2026-08-24, HEAD UNCHANGED `c28d133`, origin/main re-fetched = 0 commits past pin; platforms-rover lane): executed standing conditional #1 — the group/role membership+migration plane of `connectors/core/base/data_processor/data_source_entities_processor.py` (:1615–2746 whole-region read top-to-bottom: on_new_record_groups, update_record_group_name, on_new_app_users, on_new_user_groups, on_new_app_roles, member add/remove, user-group + record-group deletion, migrate_group_permissions_to_user :2310–2474, migrate_group_to_user_by_external_id, on_app_role_deleted) → SEVEN capsule-v2 extending the Connector-platform kernel map entry (principal-resolution-ladder, user-group-membership-rebuild, app-role-membership-plane, group-to-user-permission-migration, atomic-migrate-and-dissolve, incremental-membership-events, record-group-topology-edges) = 179→186 capsule-v2. Gate 3: search_graph resolves `migrate_group_permissions_to_user` :2310–2474 and `on_new_user_groups` :1855–1936 line-exact; stdin-JSON check_index_coverage over all four cited paths (processor, permission.py model, its 244-test direct suite, confluence_cloud consumer) = no_recorded_issue + metadata_match, generation_matches=true. Gate 5: probe battery live-executed byte-exact from repo root pre-write (delete_edges_to=5 incl. two self-caught count corrections vs draft expectations; PERMISSION_HIERARCHY.get=2 via grep -cF; ARANGO_NODE_ID_PARTS=2; INHERIT_PERMISSIONS=4; get_user_by_email=11; delete_nodes_and_edges=4); real-runner pytest available per pass-17 recipe (/tmp/psh17venv) — direct-suite tests named in capsules verified present by def-test grep at exact lines (test_creates_new_user_group_with_members :774, test_migrates_and_deletes_group :2086, test_upgrades_existing_permission :1366 et al.). Adversarial wrong-project retrieval not needed this pass (single-graph lane, zero-drift pin unchanged since pass-18 verification). OMITTED-WITH-REASON pass 19: commented-out `on_user_removed` block (:2692–2746, upstream TODO says do not port until userAppRelation cleanup exists), thin getter passthroughs (:2029–2103), `_link_kb_record_to_app`/placeholder-parent helpers already covered by connector-record-upsert-kernel + record-group capsules. NEXT-PASS TARGETS (pass 20+, ONLY past `c28d133` drift or NEW named question): diff-first grep incl. these 7 refs FIRST; then (1) tool_builder toolset internals IF an agent-tool-surface question beyond the catalog emerges, (2) cli.py/serve ONLY on a runner question, (3) graph-provider traversal internals ONLY on permission-traversal question; ELSE squeezed-to-last-drop at 186/186 v2. Pass 16 (2026-08-24, HEAD UNCHANGED `c28d133`, zero upstream drift — executed BOTH remaining open standing conditionals): (1) `services/embeddings/multimodal/**` (12 files, 1,094L) read WHOLE-file → NINE capsule-v2 forming the Multimodal embeddings plane (result-ladder contract, factory registry, index-keyed response mapping with its two shipped regression tests, OpenAI-compat dual-schema fallback, warn-don't-gate capability ladders, Titan HTTP-200 body-message failure channel + return_exceptions normalisation, Voyage delegation + tail completion, Cohere search_document pinning, Ollama future-proof stub); consumer seam `VectorStore._build_image_points/_process_image_embeddings` (vectorstore.py :755–824) verified as the single failure-decider; direct suite `tests/unit/services/embeddings/multimodal/test_jina_provider.py` (243L) pins mapping+capability+ladder behavior; (2) `services/cache/accessible_records_cache.py` (431L) + `invalidation_hooks.py` (102L) read WHOLE-file → FOUR capsule-v2 forming the Distributed permission cache plane (key taxonomy + hash-field freshness envelopes, crc32-striped single-flight read-through, outage circuit-breaker with never-raising create, best-effort invalidation hooks with edge-based org resolution); direct suites `tests/unit/services/cache/test_accessible_records_cache.py` (492L) + `test_cache_env_contract.py` (66L); protected routing surface `tests/unit/services/graph_db/test_accessible_records_provider_routing.py` (396L). (149→162 capsule-v2; count erratum: an earlier draft of this section said 158 from a bad arithmetic — the commit-stat audit `git show --stat` is authoritative at 9+4=13 new refs, 149+13=162). Gate 5: 57/57 deterministic probes live-executed GREEN from repo root (two capsule-text probe defects caught and repaired pre-commit: bedrock `return_exceptions=True` count is 2 incl. comment, hooks guard reads `if invalidator is None:` on a local); runner caveat RETIRED — pytest runner NOW GREEN (pass 17, 2026-08-24): a uv venv (/tmp/psh17venv, CPython 3.12.14) satisfies the unit-test surface — ALL 20 capsule-cited direct-test files (577 tests) + the FULL code_parser suite pass at pin (1 skip: no .js fixture on host); direct suites for BOTH pass-16 planes (cache ×88, multimodal ×94 tests) all green; positive retrieval line-exact ×4 vs the slugged twin; adversarial retrieval of pipeshub symbols against nocodb graph = 0 hits. OMITTED-WITH-REASON pass 16: none inside either package — every file mined or folded (config.py folded into factory capsule, `_response.py` into the mapping capsule, `__init__.py` empty); `retrieval_service.get_accessible_virtual_record_ids` consumer (:762) and the graph-provider traversal bodies (:18875/:4815/:2519) remain platform-layer Arango/Neo4j internals behind the standing product-shell Boundary. Pass 14 (2026-08-24, HEAD UNCHANGED `c28d133` — zero upstream drift since pass 12's re-entry; dedicated lane drain-lane-pipeshub-ai): executed the standing open target — the `modules/parsers/code_parser/` package (~1.9kL: engine.py 583 + lang_config.py 503 + file_role.py 201 + code_file_parser.py 332 + models.py 63) read WHOLE-file top-to-bottom → FIVE capsule-v2: `code-span-tiling-engine` (symbols come out of the tiling pass so byte-exact coverage is structural; fresh-Parser-per-call over cached Language; comment-forward/whitespace-backward attachment; min-error-line over ALL children incl. unnamed missing tokens), `language-config-table` (18 grammars as frozen dataclasses; loud import-time duplicate-extension ValueError; `.h`→C++ grammar superset routing), `git-blob-file-role-gate` (path-only cascade GENERATED→TEST→…→SOURCE with whole-segment matching and ignore≠classify split; GitLab connector calls it BEFORE fetching, repos.py:794), `code-block-mapper` (two-pass placement in separate block/group index spaces; `kind:dotted.scope` identity over FULL parent chains; groups keep whole bodies because only table/view groups are embedded; None-means-skipped contract); plus `docstring-signature-ladder` (signature = first non-comment/decorator/attribute line capped 300; python docstring searched AFTER the `_PY_SIG_COLON_RE` terminal-on-line colon so annotations can't fake or hide it; `\A\s*/\*\*` ANCHORED block-doc so unanchored search would hand every member the NEXT member's docs under Groovy's nested trailing comments; line-comment runs stop at first internal blank; camelCase/snake_case subtokens for BM25 recall) = 136→141 capsule-v2. Graph evidence: parse_code callers_total=120 inbound (FileContentParser agent file-reads, events Processor, chatbot routes, BlocksParser, OCR parser); classify_file_role line-exact :154–193; all 8 cited paths no_recorded_issue on the slugged fresh twin. Direct probes: tests/unit/modules/parsers/code_parser/{test_exhaustiveness.py (byte-exact tiling ×13 shapes + 18 grammar samples + ≤120 real files each for .py/.ts/.tsx/.js, ABI-drift load test), test_file_role.py, test_code_file_parser.py}. OMITTED-WITH-REASON pass 14: none inside the package — all six files mined (models.py folded into code-block-mapper; __init__.py is a 22L re-export). Pass 13 (2026-08-23, HEAD unchanged `c28d133`, dedicated lane [DONE:196]): executed ALL THREE queued pass-12 targets whole-file — (1) `sources/client/resilience.py` (291L) → `connector-resilience-policy` incl. the explicit composition boundary vs circuit-breaker-halfopen-probe (breaker decides WHETHER, policy decides HOW FAST); (2) `messaging/error_classifier.py` (485L) + `backpressure.py` (105L) as ONE failure-policy capsule `messaging-error-backpressure-policy`; (3) the governor's Phase-1 consumer surface `messaging/consumer_concurrency.py` (482L) → `consumer-governor-bridge` = 133→136 capsule-v2. `modules/parsers/code_parser/engine.py` (583L chunking/file-role seam) REMAINS the open pass-14 target. Pass 10 (2026-08-23, HEAD unchanged `4a02110d`): re-ran the citation-vs-inventory grep FIRST (suffix-matched de-noise — raw grep false-positives on files cited via shorter path forms), executed ALL FOUR queued targets: (1) toolset/index/approval internals → 3 capsules; (2) `modules/pipeline/{executor,pipeline}.py` proved PHANTOM (pipeline module = planner/critic only, both already cited → omit); (3) sandbox base/local non-coding surface re-checked → nothing beyond execute-code-rpc-bridge + pass-9 local stack; (4) cli/serve stays conditional-only. Whole-file reads of the never-cited kernel planes yielded 7 more: budget tracker/pricing, routing matchers, sandbox guard pair, streaming final-answer extractor, tool summarizer DIP, system-prompt construction, AgentSpec contract; plus the class-keyed toolset cache in the plumbing batch — 102→112 capsule-v2. Pass 9 ([DONE:119]) ran the prior grep over the queued areas (AST de-noised) exposing the sandbox-backend + tools-plumbing planes — 18 capsules (84→102). Pass 8: all 16 planning/coordination files → 18 capsules (66→84). NEXT-PASS TARGETS (pass 11, only past `4a02110d` drift or a NEW porting question): re-run the grep FIRST at whatever HEAD then holds; residue is genuinely thin — (1) `agent/prompt.py` builder internals IF a prompt-section porting question emerges beyond construction-layer pins; (2) `cli.py`/`serve/app.py+playground.py` ONLY on a runner question; (3) upstream tests for middleware-path-tag-routing matcher internals / sandbox-guard deny branches would let those coverage caveats drop. Pass 11 ([DONE:162], 2026-08-23, HEAD unchanged `4a02110d`, zero upstream drift): re-ran the citation-vs-inventory grep FIRST (1,241-py inventory → 881 raw uncited → de-noised by stem+suffix matching against cited strings plus the standing product-shell Boundaries ruling); adopted-scope residue was genuinely thin — mined the LAST four unmined seams whole-file: `contextvar-input-staging` (dedicated 237L upstream test file exists — best-tested capsule in the leaf), `goal-feasibility-preflight`, `memory-consolidation-agent-curated`, `sqlite-fts5-memory-provider` (Hermes-pattern FTS5 BM25 ranked memory, stdlib-only) = 112→116 capsule-v2. Target-(3)-equivalent check: matcher internals and guard deny branches STILL have no direct upstream tests at pin — both caveats STAND. OMITTED-WITH-REASON pass 11: `sandbox/os_sandbox.py` + `tools/builtin/sandbox/os_sandbox.py` (22L ConfinedLocalSandbox subclass + run_shell tool = thin composition over kernel-confinement-ladder/rlimit surfaces), `knowledge_query.py` (provider-delegating tool, same ToolOutput/sources shape as memory surface), GoalBuilder/IntentParser bodies (auxiliary single-shot-runner jobs already cited), app/sandbox/* executors + agents/agent_loop/** product shell (standing Boundaries). NEXT-PASS TARGETS (pass 12): re-run the grep FIRST ONLY past `4a02110d` drift or a NEW porting question — ELSE declare squeezed-to-last-drop for cycle at 116/116 v2; standing conditionals: (1) `agent/prompt.py` internals IF a prompt-section question emerges beyond construction-layer pins; (2) cli/serve ONLY on a runner question; (3) drop the matcher-internals/sandbox-guard-deny caveats when upstream ships those tests; (4) InMemory/knowledge provider variants ONLY on a store-porting question beyond the sqlite exemplar. OMITTED-WITH-REASON pass 10: planner default + plan/result critics (already cited by structured-plan-slot / plan-result-critics-as-tools); skills provider plane (governor/index glue behind skill-storage-discovery + skill-learning-loop); in_memory/stdin store variants; roles/builtin/* prompt packs (data); eval/feedback.py + events/emitters/logging.py (trivial adapters behind mined surfaces); product-shell sources/connectors/Celery/frontend omissions STAND. Pass 12 ([DONE:188], 2026-08-23, drift re-entry to `c28d133`, +40 commits): citation-vs-inventory grep intersected with the diff → 156 never-cited changed py files clustered into NEW planes; resource_governor package (~2.5kL: models/policy/gate/admission/probe/controller/registry/tiers) read whole-file → 6 capsule-v2 (pressure grow/shrink split, gate+demand accounting, shielded-acquire backpressure, slow-start ramp policy, probe chain, service wiring); ADOPTED 11 finished-but-uncommitted interrupted-sibling drafts (oauth×4, registries/npm×2, artifacts×3, breaker, stateless import) after verifying their seams alive at HEAD = 116→133. OMITTED-WITH-REASON pass 12 (diff-plane triage; all candidates for pass 13): sources/client/resilience.py+http_resilient_transport.py (connector-side HTTP resilience — sibling of cited base_client breaker, needs its own whole-file read), services/messaging backpressure/error_classifier/consumer_concurrency governor glue (~1.2kL), modules/parsers/code_parser (~1.7kL tree-sitter engine), services/embeddings/multimodal provider lattice (9 files), utils/chat_helpers.py (+786L drift), sources/external/github github_.py+github_async.py (+1.4kL connector), accessible_records_cache+invalidation_hooks, agent_loop transports trio (azure_openai/gemini/openai_responses) + provider_conflicts, agents/chat_modes bridge/prefetch, api/middlewares/auth.py. NEXT-PASS TARGETS (pass 13+, ONLY past `c28d133` drift): 1. git log --oneline c28d133..HEAD -- backend/python/app FIRST — diff-driven seams only. 2. sources/client/resilience.py (291L) + http_resilient_transport.py (85L) whole-file as ONE comparative capsule vs circuit-breaker-halfopen-probe. 3. messaging error_classifier.py (485L) retry-vs-deadletter taxonomy + backpressure.py. 4. code_parser engine.py (583L) chunking/file-role classification. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf.

## Provenance
Pass 22 (2026-08-27, miner-pipeshub-ai dedicated lane, deep pass at unchanged pin `68509725e15c60da4db9f37bc183511a2c155017`): agent knowledge-search tool surface mined whole-file (`agents/actions/retrieval/retrieval.py` 739L + `knowledge_graph/ops/scope.py` narrow_to/to_filter_groups_for_source + `record_escalation/{coverage,policy,renderer}.py` decisive ranges + three direct suites whole-file) → SIX capsule-v2 in NEW map group 'Agent knowledge-search tool surface' = 208→214. The six capsules were drafted by an INTERRUPTED earlier run of this lane (its learning note research.md was written first per contract); this pass re-verified every cited source/test range against HEAD (all match), repaired the auto-generated loader/map wiring to hand-authored prose, and ran the gate battery — Codebase Memory MCP NOT connected this session, so evidence is direct source+test reads per AGENTS.md fallback (recorded in verification.md); real-runner pytest battery via rebuilt /tmp/psh22venv. Pass 21 (2026-08-26, miner-pipeshub-ai dedicated lane, deep pass at unchanged pin `68509725e15c60da4db9f37bc183511a2c155017`): enterprise-search retrieval plane mined whole-file (`modules/retrieval/retrieval_service.py` 922L, `modules/reranker/reranker.py` 131L + decisive ranges of three direct suites) → SIX capsule-v2 in NEW map group 'Enterprise-search retrieval plane' = 196→202; graph re-verified live on short-name project `pipeshub-ai` @ canonical root `/mnt/hdd/utopia/inspo/pipeshub-ai` (ready, full, head==base==pin, 127,449n/906,801e); Gate-5 REAL runner battery via ad-hoc `/tmp/psh21venv` = **124 passed / 0 failed rc=0** across test_retrieval_service + test_reranker + vector_db/test_retrieval_service; coverage ×7 cited paths no_recorded_issue (retrieval_service.py + its test flagged metadata_changed → validated by full direct reads; graph line offsets for several symbols are stale vs HEAD — source wins everywhere). Work record created: inspo/pipeshub-ai-work/{state,research,verification}.md. pipeshub-ai (Apache-2.0), `main@6850972` (pass-20 drift re-entry from `c28d133602543bd737b9791db84b76c5dee84ff7`: +2 commits e513443 tests-updates [one fixture] + 6850972 changelog job — ZERO production-code delta, capsules cite the drifted tree; graph re-verified live pass 20: 131,043n/934,313e ready head=base at c28d133 pre-pull). Pass 20 (2026-08-24, giants-sweep lane): production hook middlewares & streaming surface mined whole-file (hooks/* uncited plane + answer_streamer/error_classification/reasoning_persistence/model_tier/artifact_store + factory _build_hooks :804-944) → 10 capsule-v2 (186→196); REAL runner battery 105 passed rc0 via /tmp/psh17venv; coverage ×23 paths clean except one test-file freshness-missing read directly. `main@c28d133602543bd737b9791db84b76c5dee84ff7` (pass-12 drift re-entry from `4a02110d`; +40 commits, 200 py files, +23.6k/−3.9k lines; UNCHANGED through passes 13–18, origin/main verified 0 commits past pin at pass-16 AND pass-18 starts; graph re-verified live pass 18: 131,043n/934,313e ready, head=base, parse_partial confined to fixtures/helm/pytest.ini — zero app/sources|connectors files). Pass 18 (2026-08-24, deepening-C lane): NEW named porting question "how does a vendor integration become a registered, permission-filtered, token-refreshing agent tool surface" opened the never-cited `connectors/core/` kernel (~14.9k LOC) whole-file → 17 capsule-v2 forming the Connector-platform kernel plane; gate-5 REAL runner battery: the ENTIRE connectors-core test complex (`tests/unit/connectors/core/` 30 suites incl. registry 111, builder 64, filters 129, toolset-refresh 139, data_source_entities_processor 244, data_processor 128, graph_data_store 121) plus the four top-level token/oauth suites executed via pass-17's `/tmp/psh17venv` → rc=0, ~1,915 result dots, ZERO failures; all 30 new deterministic probe anchors grep-verified live BEFORE authoring; search_graph resolves schedule_token_refresh (:911-968), _process_record (:954-1087), retry_on_deadlock (:59-110), sync_with_database (:598-667) line-exact. Pass 16 (2026-08-24): BOTH remaining standing conditionals executed zero-drift — multimodal embeddings lattice whole-package → 9 capsule-v2, accessible_records_cache + invalidation_hooks whole-file → 4 capsule-v2 (149→162 — see the count erratum in Extending-the-foundation); 57/57 deterministic probes live-executed GREEN with two probe-text defects repaired pre-commit; positive retrieval line-exact ×4 on the slugged twin; adversarial nocodb retrieval 0 hits; pytest runner was blocked through pass 16; pass 17 built a uv venv and executed every cited suite green. Pass 14 (2026-08-24): code_parser package mined whole-file → 5 capsule-v2 (136→141); FOUR tiling/config/role/mapper drafts authored by an INTERRUPTED SIBLING session were gate-3 re-verified against source at pin (one anchor repaired :232→:233), live-probed via search_graph + trace_path (parse_code callers_total=120 inbound), then wired TOGETHER WITH the fifth (`docstring-signature-ladder`) mined this run; all cited source+test paths no_recorded_issue via stdin-JSON check_index_coverage on the slugged fresh twin. Pass 12 (2026-08-23): citation-vs-inventory grep FIRST over the diff → resource_governor package (~2.5kL, 10-file dedicated unit suite) mined whole into 6 capsule-v2; ADOPTED 11 finished-but-uncommitted pass-11-draft capsules (oauth/mcp/artifact/npm/registries/breaker/import — interrupted sibling session's work, seams verified alive at new HEAD) = 116→133 capsule-v2. Pass 13 (2026-08-23): three drift-new seams (connector resilience / consumer governor bridge / messaging error+backpressure) mined whole-file with direct-test probes → 133→136 capsule-v2. Passes 1–11 @ `4a02110d`: see git history of this leaf.

## Full view (memory graph)
Revalidate before porting: project `pipeshub-ai`, canonical root `/mnt/hdd/utopia/inspo/pipeshub-ai`, branch `main@6850972` (`68509725e15c60da4db9f37bc183511a2c155017`, verified live pass 21 via index_status verbose + git rev-parse), status ready, full mode, 127,449 nodes / 906,801 edges (gen 2026-08-15T22:39:17Z). TWIN RESOLVED pass 21: the old slugged project `mnt-hdd-utopia-inspo-platforms-pipeshub-ai` is gone from the registry; the short-name project `pipeshub-ai` IS the live index at this exact pin/HEAD — cite it. parse_partial = 36 files (YAML/Helm/openapi/test-type ranges only — no production Python gaps in cited planes); skipped = 0; excluded by design: .git/.claude/frontend/lib + pycache + 264 ignored assets. CAVEAT: several graph line offsets lag HEAD layout for recently-touched files (e.g. `_execute_parallel_searches` graph :752–818 vs HEAD :798–864) — always confirm decisive ranges by direct read; source wins. Run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, `get_code_snippet`; source and direct tests decide shipped claims. Pass-1 scope: `backend/python/app/agent_loop_lib/**` (~33k LOC embedded agent framework); pass-12 scope: `backend/python/app/services/resource_governor/**`; pass-14 scope: `backend/python/app/modules/parsers/code_parser/**`; pass-16 scope: `backend/python/app/services/embeddings/multimodal/**` + `backend/python/app/services/cache/{accessible_records_cache,invalidation_hooks}.py`; pass-18 scope: `backend/python/app/connectors/core/**` (registry/ builder/ factory/ base/{token_service,data_store,data_processor,connector,sync_point,event_service}/ interfaces/ sync/ — the vendor-integration kernel; per-vendor adapters in `connectors/sources/*` and `sources/*` remain platform shell). Pass-20 scope: `backend/python/app/agents/agent_loop/{hooks/*,answer_streamer.py,error_classification.py,reasoning_persistence.py,model_tier.py,artifact_store.py}` (the production middleware + streaming surface riding the pass-1 kernel).

## Boundaries
Adopt the step primitive + LoopStrategy inversion, wave-sync dedup, the single tool funnel with name repair, enforced dependency-aware spawning, StateSlot scoped state, atomic-group compaction, tag-based terminality, the per-event Pipeline/Wrapper kernel with monotonic decisions, marker-idempotent guard installers, same-funnel sandbox RPC bridging, errors-as-data sandbox backends behind a manager, the LLMTransport provider-dialect layer (Anthropic cache breakpoints, OpenAI reasoning_effort, Ollama native format=), the Opik transport-decorator tracing gate, the run_child depth/tag-guard child-launch path, the Role→AgentSpec factory with idempotent sub-agent wiring, the timeline→ContextGraph/trajectory/decision-trace eval surfaces, and the governed skill self-creation loop (extract→evaluate→govern→sub-agent-persist) with spec-compatible SKILL.md format, multi-root storage, 3-tier disclosure, and actability-gated preloading, plus the PhaseDriver gate-loop mechanics (verdict-turns-only budgeting, stopped/passed split, revise→finish ladder), the checkpoint/HIL-pause/resume durability trio (dual-id bridge across restarts, same-instance identity-true resume at turn_index+1, session-tree rollback branching), the dual-backend graph skill store (generic-interface-only access, construction-bound tenancy, version-keyed revision ledger with rollback-as-new-revision, usage carry-forward + parallel-array map encoding), and pass-9's sandbox/plumbing plane (archive-only container I/O with staged-byte artifact baselines, dedicated egress bridge behind a dual-flag veto, allowlist-only subprocess env with OUTPUT_DIR parity, fail-closed package-spec regexes with PEP 503 canonical keys, category→suggestion error reflection with targeted input-absence overrides, probe-once kernel confinement with unix-socket/mach-lookup carve-outs, handler-chaining exit cleanup, rlimit×confinement as orthogonal layers, summary-first provider toolsets with locked materialization, zero-hit-only global-catalog tri-state answers, special routes swapped in INSIDE the executor funnel via override_execute, name-vs-path error taxonomy with alias-merged hierarchy, capability-tag dispatch with the SPAWN/SPAWN_BATCH split, tool-grounded plan-ahead prompts mirroring sandbox network truth); adapt event dialects, thresholds (24K dep cap, 40-char degradation, 0.85 trigger, 2-turns-left warning, 0.75/0.4 preload bands, 0.7 pass threshold, 3/2 phase round budgets, n∈[2,5] candidates, ×4 search over-fetch, 20-note failure ring), config surfaces, and provider transports to host; omit the PipesHub platform layers around the library (connectors/sources, Celery containers, Arango/Neo4j graph providers, Slack bot integration, frontend). Pass-6 targets inside agent_loop_lib + adapter skills layer are ALL mined or omitted-with-reason. Pass-7 ([DONE:105]) reopened the leaf and ADOPTS the whole context-engine middleware library: cheapest-first shaper composition with a terminal typed raise, marker-grammar summary merging, turn-granular payload replacement, original-index pin stability under eviction, by-reference StateSlot inheritance for spawn-tree gates, deterministic relevance-band preloading, synthetic error results for truncated tool calls, and five independent guard layers (permission / risk-mode / literal-text safety / billing ceiling / re-invocable retry Wrapper); omit in_memory/stdin store variants of mined protocols and `roles/builtin/*` prompt packs. Pass 8 ([DONE:113]) ADOPTS the planning/coordination tool surface: validated plan graphs in a run-scoped slot with dual-shape confidence (enum-arg or trailing line, never whole-plan JSON), terminality as outcome-dataclass + tag/protocol (no name hardcoding, malformed fields drop individually), deterministic structural critique before any LLM judge, parent→child data handoff over a last-real-user boundary with count-don't-hide budget ladders, static agent-as-tool composition under per-path recursion guards (inherited spawn_depth inside runs / ContextVar outside), sole-launch-site detached spawns that always land results for later depends_on, horizontal handoff via in-place spec mutation riding the rebuild-every-turn invariant, and fail-safe-to-cheaper-route classification. Pass 9 ([DONE:119]) ADOPTS the sandbox-backend + tools-plumbing planes (archive-only container I/O, dual-flag egress veto, allowlist env, fail-closed package specs, error reflection, probe-once confinement with documented carve-outs, exit-cleanup chaining, rlimit layering, locked provider materialization, tri-state global fallback, in-funnel special routes, name/path taxonomy, capability tags, grounded plan-ahead). Pass 10 ([DONE:148]) ADOPTS the never-cited kernel planes: middleware-carried budget enforcement with model-aware cache-ratio pricing, segment-glob/tag matcher scoping, defense-in-depth sandbox guard pair with e2b-scoped billing budget, fragment-tolerant streaming answer decoding, DIP-injected best-effort tool summarization, fresh-pure per-turn prompt construction over a frozen pure-data AgentSpec, class-keyed toolset reflection caching with lenient connector registration, live-registry read-only tool ranking, and the minimal approval port bridging the legacy RiskLevel/HIL apparatus; plus pass-11's final adopted-scope seams (dual-lifetime ContextVar input staging — span-scoped handoff vs run-long skill resources with merge-inner-wins nesting and the bare-set PRE-middleware workaround under documented task-model preconditions; typed feasibility pre-flight between goal build and loop entry with vacuous-pass degradation; agent-curated memory consolidation where NO LLM call lives in the tool layer and provenance rides consolidated_from metadata; the stdlib-only SQLite+FTS5 BM25 ranked-memory provider — one serialized connection, same-commit dual-table writes, over-fetch-before-scope-filter, score sign inverted at the boundary). Pass 12 ([DONE:188]) ADOPTS the resource-adaptive admission-control package (baseline-credited growth vs raw-occupancy brakes, loop-bound weighted gates with integral permit-seconds demand accounting, shielded warn/wait/429 backpressure with cancel-race permit release, floor warm-start with slow-start resource-delta ramping where operator caps raise ceilings but never skip ramps, dependency-free cgroup probe ladders with host-vs-container utilisation denominators and low-water-mark baseline calibration, per-process governor wiring across HTTP routes and worker-thread loops with the any-thread OOM-incident halving fast path) plus the interrupted-sibling platform drafts (connector OAuth refresh-lock ladder + token dialect tolerance + RFC 7591 DCR/PKCE persistence + discovery URL grammar, dual toolset/MCP registries, npm install-command rejection ladder, artifact blob+graph version choreography / harness-captured DERIVED_FROM lineage / two-phase upload grants, the health-check-probe half-open circuit breaker, stateless preview→confirm import), plus pass-13's drift planes (per-instance connector resilience budgets with max()-armed backoff gates shared by HTTP+SDK call sites, worker-loop→main-loop consumer bridging with carry-your-release admission tokens and ceiling-sized cluster leases, ordered chain-walking message failure taxonomy with per-service read-pause backpressure coordination). Pass 14 ADOPTS the code-ingestion plane: byte-exact span tiling as the structural guarantee (symbols only from the tiling pass), grammar-difference absorption into frozen per-language config tables with loud duplicate-extension detection, path-only file-role gating before fetch (whole-segment matching, ignore≠classify, generated-beats-test cascade order), and two-pass span→block mapping with `kind:dotted.scope` identities over full parent chains and embed-free whole-body groups, plus style-dispatched signature/docstring metadata extraction whose terminal-colon and anchored-doc scoping laws keep retrieval metadata honest. Re-enter only if repo code advances materially past `main@c28d133` — then run the citation-vs-inventory grep over the diff FIRST; standing conditionals: multimodal embeddings lattice and accessible_records_cache (both EXECUTED pass 16); cli.py/serve ONLY on a runner question. Pass 15 executed the zero-drift EVIDENCE sweep (file-level census over all 1,305 `backend/python/app` modules: 180 files cited across 141 refs, remainder concentrated in recorded omitted platform layers) and ADOPTS the eight library-substrate seams above. Pass 16 ADOPTS the multimodal embeddings lattice + distributed permission cache plane above, closing BOTH standing conditionals. Pass 18 ([DONE:352]) ADOPTS the connector-platform kernel plane (declarative builder DSL with import-time OAuth validation, deactivate-only registry sync, view⊇access scope lattice, denormalized permission-model declaration, beta×account×scope catalog matrix, write-once authType creation, record-upsert version/status machine with null carry-forward, whole-handler deadlock retry, marker-classified refresh-token death + preserve-or-rotate, hardened toolset refresher, construction singleflight, task-manager cancel-restart vs decline-if-busy duality, AES-GCM cursor vault, alert backoff ladder, closed-vocabulary filter system, post-construction org binding, resolve-or-drop permission edges) — the `connectors/core/` kernel is now adopted-scope; per-vendor adapters (`connectors/sources/*`, `sources/*`) remain platform shell. Pass-19+ targets: ONLY past `c28d133` upstream drift or a NEW named porting question — candidates: `base/data_processor/data_source_entities_processor.py` group/role planes (on_new_user_groups/migrate_group_permissions_to_user ~:1855-2530) IF a group-membership porting question emerges; `registry/tool_builder.py` toolset internals IF an agent-tool-surface question beyond the connector catalog; cli.py/serve ONLY on a runner question — ELSE the foundation stands squeezed-to-last-drop at 179/179 v2 for the cycle. Pass-20's production middleware plane is adopt-first for any single-registry agent loop: register-if-absent reactive tool grants with internal-search gating, turn-aware consecutive-failure blocking, pop-based image delivery with identity dedup, tree-wide bounded completion nudging, event-driven answer streaming with rate-limited enrichment, and the four PRE_TURN history restorers; adapt the SSE/formatter dialects and hint vocabularies; omit PipesHub transcript serialization, blob-store plumbing, and provider-specific stripping lore beyond the capability-gate rule.
