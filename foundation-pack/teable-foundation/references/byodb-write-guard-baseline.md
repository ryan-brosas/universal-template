<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration-guard.service.ts` + `data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How does a running migration block conflicting writes app-wide, and how do BYODB targets get their schema kept current?
**Path/Symbol:** `SpaceDataDbMigrationGuardService` (:43-290), `DataDbMigrationService` (:64-313), `resolveDataDbInternalSchema` (data-db-internal-schema.ts :15-21)
**Signature:** guard ladders: assertSpaceSchemaWritable blocks on ALL active states; assertSpaceRecordWritable only on ['freezing_writes','switching'] AND switchOnCompletion jobs — record writes stay OPEN during the online copy phase; base/table variants resolve base→spaceId first and also check base_data_db_move_job. Missing-table tolerance: P2021/P2022/42P01/'does not exist'/'relation' errors → allow (pre-deploy clusters).
**Data Shape:** internal schema = `<BYODB_DATA_DB_INTERNAL_SCHEMA_PREFIX|'teable'>_<sha256(host:port/db)[:16]>`, validated against /^[a-z_]\w*$/i; data-DB migrations are directory-scanned SQL files applied idempotently into `__teable_data_schema_migrations(id PK, checksum sha256, applied_at)` under `pg_advisory_lock(hashtext('teable:data-db-migration:<schema>'))` with statement_timeout 300s / lock_timeout 30s.
**Decisive source:** guard :282-285 — `prismaClient` getter uses the ACTIVE TRANSACTION client (`txClient?.()`) for base/table→space resolution but the ROOT client for job lookups, so freeze checks inside a write transaction don't deadlock on their own tables ("uses the active transaction client for id resolution but root client for migration jobs" spec :195). Migration service keeps ONE in-flight migrate per connection via a Map<connectionId, Promise> (`runningConnections`) deduped across concurrent requests; checksum mismatch on an already-applied migration THROWS (drift detection). On bootstrap it backfills every non-disabled connection whose schemaVersion ≠ latest or status ∈ {migrating,error}.
**Flow/Invariant:** connection lifecycle states ready/migrating/error/disabled mirror binding states; ensureConnectionMigrated flips connection+bindings to migrating → apply → ready, and error paths set both lastError and binding state 'error'. Baseline schema version = lexicographically-last migration id; validation later requires the target to contain that exact id row.
**Probe (direct test):** specs 'allows writes when the migration job table has not been deployed yet' (guard.spec :43), 'allows record writes during the online copy phase while schema writes stay blocked' (:89), 'does not poison the active transaction when the migration job table is missing' (:240); live: `grep -c "pg_advisory_lock(hashtext(?))" apps/nestjs-backend/src/features/space/data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"ensureConnectionMigrated","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt two-tier writability + tx/root client split + advisory-lock checksummed baseline as one unit; adapt state names freely.
