<!-- capsule-v2 -->
**Source:** teable `record-history-flusher.service.ts` readBatch @ pin `06a4461e`
**Question:** Why keyset-read the buffer through knex with adaptive LIMITs instead of Prisma?
**Path/Symbol:** `nextReadBatchLimit(batchBytes, batchRows, cap)`, READ_BATCH_TARGET_BYTES=8MB, READ_BATCH_PROBE_ROWS=64, READ_BATCH_MIN_ROWS=1; flushTable batch loop
**Signature:** next batch limit = `min(cap, max(1, floor(8MB / avgRowBytes)))` — first batch probes weight at ≤64 rows, later batches re-target ~8MB per read; cap is a hard ceiling an operator may set BELOW the target.
**Decisive source:** :851-858 — "Read on the table's own pg connection via the NATIVE pg client (knex / node-postgres) ... Prisma's rust engine deterministically failed readBatch on one shared-DB table with 'Failed to convert rust String into napi string' even though the values are valid, sub-cap UTF-8 ... so the native driver sidesteps that engine quirk and the table drains." :88-94 probe kept small because "a table can average 500KB/row (real on the ai fleet), so a large first probe materializes hundreds of MB".
**Flow/Invariant:** Keyset pagination via `(record_id, created_time, id) > ($1,$2,$3)` row comparison matching the byte-order comparator; timestamps bound as UTC naive strings + `::timestamp` casts because created_time is TIMESTAMP WITHOUT TIME ZONE storing UTC and node-postgres binds Dates in process TZ (:865-869); createdTime read back via `to_char(...'Z')` then re-parsed so downstream ISO conversion stays UTC. Batch sizing uses PG-RETURNED truncated sizes. Loop drops each consumed source row (`batch[i] = undefined`) so multi-MB batches don't stay live.
**Probe (direct test):** spec "read batch limit re-targets the byte goal from observed row weight" pins all five cases (5000 / 8 / 1 / 2048 / 50) AND "reads record_history via the native pg (knex) client..." asserts `capturedSql.toContain('cold-truncated')`, NOT `$1`, bindings length 5, `'2026-06-01T00:00:00.000'`; live: `grep -c 'napi string' apps/nestjs-backend/src/features/record-history-cold/record-history-flusher.service.ts` → `1` (comment names the engine quirk); `grep -oE 'READ_BATCH_PROBE_ROWS = [0-9]+' ...` → `64`.
**Retrieve:** `echo '{"project":"teable","pattern":"nextReadBatchLimit","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — byte-adaptive reads + driver-quirk escape hatch.
