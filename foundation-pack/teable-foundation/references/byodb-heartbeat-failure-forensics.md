<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do you keep a long migration observable — heartbeats, live copy progress, and honest failure forensics — without blocking the work?
**Path/Symbol:** `withMigrationHeartbeat` (:3271-3307), `updateBaseSchemaCopyProgress`/`inspectBaseSchemaActiveCopyProgress` (:3001-3154), `sampleTargetSchemaCopiedBytes` (:3156-3179), `buildProcessFailureStats`/`formatProcessFailureResult` (:4994-5067), `buildReadOnlyTargetFailureMessage` (:5026-5039)
**Signature:** heartbeat wrapper: setInterval fires single-flight heartbeat (inFlight guard drops overlaps), final in-flight beat is AWAITED in finally so the last state lands before the next phase overwrites it; every stats write `.catch(() => undefined)` — observability NEVER fails the job.
**Data Shape:** active-copy sample = pg_stat_progress_copy joined to inventory estimates (`bytesProcessed/bytesTotal/tuplesProcessed/tuplesExcluded` per relation, phase dump|restore); copiedBytes only sampled from the RESTORE side because cumulative relation sizes on the target are the truth of what landed; failure stats discriminate pipeline vs process vs unknown with `{command, exit|signal, outputTail(800 chars)}`.
**Decisive source:** :5032-5038 — read-only-transaction failures are pattern-matched and rewritten into operator guidance: "Use a writable primary database connection. For Supabase, prefer the direct database endpoint or a writable session pooler instead of a read-only replica endpoint." Stale-recovery error builder (:1294-1342) reconstructs WHY the worker died from persisted copyStats (activeCopy.error → baseSchemas.error → heartbeat stage) so an interrupted dump explains itself after restart.
**Flow/Invariant:** post-copy row counts run INSIDE the heartbeat wrapper ('keeps base schema copy fresh while collecting post-copy row stats' spec :2742) — long tail phases keep beating too. All client destroys are `.catch(() => undefined)` in finally. Secrets: process args redacted at capture time (postgres://user:pass@, password='…', password=…, libpq percent-token errors) before any persistence.
**Probe (direct test):** specs 'records active pg_stat_progress_copy samples while dumping and restoring base schemas' (:2506), 'records a base schema heartbeat before pg_stat_progress_copy inspection resolves' (:2660), 'surfaces the last base schema copy error when recovering a stale copy job' (:2065), 'keeps target stderr when target stdin reports EPIPE' (runner.spec :771); live: `grep -c 'read-only transaction' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"withMigrationHeartbeat","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt fire-and-forget-stats + awaited-final-beat + failure-forensics-from-persisted-state trio; adapt progress SQL to your engine.
