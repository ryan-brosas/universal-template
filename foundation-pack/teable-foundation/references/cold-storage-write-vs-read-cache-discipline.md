<!-- capsule-v2 -->
**Source:** teable `record-history-cold-storage.service.ts` cache discipline @ pin `06a4461e`
**Question:** When may cold-part bytes be cached, and how is the etag LRU kept byte-honest?
**Path/Symbol:** `RecordHistoryColdStorageService`, `iterateRows` (write paths), `iterateRowsCached(key, {etag,size}, deadline?)`, `cachePart`, PART_CACHE_MAX_TOTAL_BYTES=64MB, PART_CACHE_MAX_ENTRY_BYTES=16MB, `ColdReadDeadlineError`, `downloadWithDeadline`
**Signature:** read-path LRU keyed `${key}@${etag}` from a LIVE listing; entries without etag or over 16MB bypass the cache; Map insertion-order = recency (get→delete→set refresh).
**Decisive source:** :28-32 — "Listings/downloads used by the WRITE paths are cache-free: parts are rewritten by the flusher/compactor running in another process, so a key-addressed byte cache can serve clobbered content. The READ path may use iterateRowsCached, which is keyed by key@etag from a live listing — a rewrite changes the etag and misses the cache by construction." :79-83 listMonths "always a live LIST ... any cross-request cache here would hide a freshly created month dir".
**Flow/Invariant:** Concurrent-miss accounting: re-caching an existing key first SUBTRACTS the old entry's bytes ("replacing without reclaiming the first entry's bytes would inflate the counter with phantom bytes and evict the rest of the cache early" :214-221; spec-pinned). Deadline applies to the buffering download ITSELF (`ColdReadDeadlineError` mid-stream destroy) — "a slow GET would otherwise run to completion before the caller's per-row deadline checks ever see a byte". Stats are advisory: ANY read/corruption error degrades to part scans.
**Probe (direct test):** spec "part byte cache accounting / re-caching the same key under concurrent misses does not leak phantom bytes" pins partCacheBytes===1024 size===1; live: `grep -oE 'PART_CACHE_MAX_TOTAL_BYTES = [0-9]+' apps/nestjs-backend/src/features/record-history-cold/record-history-cold-storage.service.ts` → `...= 64`; `grep -cF '@${version.etag}' ...` → `1`; `grep -c 'phantom bytes' ...` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"iterateRowsCached","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — write-cache-free / read-etag-keyed split is THE multi-process object-store rule.
