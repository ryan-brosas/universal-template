<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How does the migration repair a target that ALREADY has the space's schema (repair/re-migration), and how do legacy public sequences survive a dump/restore?
**Path/Symbol:** `resolveMigrationTargetInternalSchema` (:5595-5634), `buildInventory` relatedSharedTableIds (:5743-5801), `installTargetPublicUndoCaptureCompatibility` (:3181-3238), `prepareTargetLegacyPublicAutoNumberSequences`/`syncTargetLegacyPublicAutoNumberSequences` (:1719-1836)
**Signature:** repair = migrate INTO the internal schema an already-bound neighbor space uses: when request omits internalSchema, collect distinct schemas of BYODB members on the SAME physical DB; >1 distinct → CONFLICT ('must use a single BYODB internal schema before repair migration'); copy set = only spaces NOT already on that target (getRelatedSpaceIdsForCopy filters out members whose dataDbUrlFingerprint matches).
**Data Shape:** inventory records BOTH copy-scope and RELATED shared-table ids (`sharedTableIds` vs `relatedSharedTableIds`) so validation can distinguish "rows that must match" from "neighbor rows allowed to exist" — spec 'allows existing related target rows during repair validation' (:3830) and 'copies shared rows only for the default subset during repair migrations' (:3096).
**Decisive source:** :1719-1737 pre-create: for every legacy auto-number column whose backing sequence lives in `public` (pg_attrdef→pg_depend→pg_class relkind 'S' chain, attnum filter `attname='__auto_number'`), `CREATE SEQUENCE IF NOT EXISTS public.<name>` on the TARGET BEFORE restore — pg_dump's dump order can otherwise create tables whose defaults reference not-yet-created public sequences. :1740-1773 post-copy sync: read source `last_value/is_called`, then `setval(?::regclass, ?::bigint, ?::boolean)` with the SAME is_called so nextval parity is exact.
**Flow/Invariant:** undo-capture compatibility is installed BEFORE any base-schema restore: CREATE SCHEMA IF NOT EXISTS public + CREATE OR REPLACE FUNCTION public.__teable_capture_undo_row reading `current_setting('teable.undo_batch_id', true)` and no-op-ing when empty — restored trigger definitions reference it, and dry-run targets must pass validateUndoFunction's to_regprocedure probe. Sequence sync re-runs at final gate (`syncTargetLegacyPublicAutoNumberSequencesForJob`) because delta replay may have consumed ids.
**Probe (direct test):** specs 'starts a repair migration for default spaces linked to an existing target-bound space' (:1621), 'pre-creates legacy public auto-number sequences from dependency metadata' (:2338); live: `grep -c '::regclass, ?::bigint, ?::boolean' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"listLegacyPublicAutoNumberSequences","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt repair-mode scope narrowing + sequence pre-create/setval pairing + undo-fn compatibility shim as one unit.
