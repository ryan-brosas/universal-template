<!-- capsule-v2 -->
**Source:** teable `record-history-cold.processor.ts` bootstrap @ pin `06a4461e`
**Question:** How do schedulers behave under kill switch, redis-less fallback, and fleet rollback?
**Path/Symbol:** `RecordHistoryColdProcessor.onApplicationBootstrap`, FLUSH_JOB_ID, COMPACT_JOB_ID, COMPACT_CRON `'10 4 2 * *'`
**Signature:** kill-switched process PAUSES its worker (`this.worker.pause(true)`) and consumes nothing; enabled processes `upsertJobScheduler` daily `{every: 24h}` + monthly cron "04:10 UTC on the 2nd — every closed month has fully flushed"; redis-less fallback queue (no `upsertJobScheduler` function) → warn + return.
**Decisive source:** :53-58 — "deliberately NO scheduler removal here: no process can tell 'the feature was disabled everywhere' from 'other pods still run it', and removing from the shared redis would tear down their schedule. With a fleet-wide kill switch the schedulers' jobs are skipped at execution by process() and sit as at most a couple of delayed jobs per day until re-enable (or a manual scheduler cleanup)."
**Flow/Invariant:** Schedulers are ADD-ONLY from every booting process; rollback is neutralized at EXECUTION time, not registration time. Creating the flush scheduler fires its first run immediately (that starts migration on fresh install); deliberately NO boot-time kick job — "a fixed-id kick job needs a fleet-wide dedupe marker, and BullMQ's lazy retention pruning turns that marker into a footgun (see the 2026-07-08 stalls)" (:74-80); draining sooner = run the EE runner once (`flush --max-rows=0`), "a deliberate op, not boot magic".
**Probe (direct test):** spec "registers both schedulers at bootstrap and queues nothing else" asserts exactly two scheduler keys AND zero jobs; live: `grep -c 'deliberately NO scheduler removal here' apps/nestjs-backend/src/features/record-history-cold/record-history-cold.processor.ts` → `1`; `grep -c 'worker.pause(true)' ...` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"onApplicationBootstrap","limit":8}' | codebase-memory-mcp cli search_code` (scope: processor)
**Verdict:** adopt — add-only schedulers + execution-time gating is the multi-pod-safe pattern.
