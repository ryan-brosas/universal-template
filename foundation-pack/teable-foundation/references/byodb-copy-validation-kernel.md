<!-- capsule-v2 -->
**Source:** teable `apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` @ pin `06a4461e`
**Question:** How do you verify a copied database is byte-for-byte equivalent WITHOUT trusting COUNT(*) alone?
**Path/Symbol:** `validateBaseSchemaRows` (:6989-7063), `inspectRelationSignatures` (:7491-7591), `countAndHashRows` (:7607-7625), `contentHashRowExpression` (:7632-7650), `validateSharedTableRows` (:7255-7322)
**Signature:** per relation: kind → missing/kind-mismatch; columns/indexes/constraints/triggers compared as ORDERED signature lists (4 batched catalog queries per side, NOT per-table roundtrips — the Chinese comments :7005/:7490 note thousands of serial catalog queries were the validation bottleneck); rows compared by count + optional 64-bit multiset content hash at concurrency 8.
**Data Shape:** column signature `{ordinalPosition, columnName, formattedType, notNull, defaultExpression, identity, generated, collation}` — ordinalPosition STRIPPED before comparison (:7603-7605) because restore renumbers after dropped columns. Constraint signatures EXCLUDE contype 'n' (:7560) because PG18+ materializes NOT NULL as constraint rows and older majors don't — cross-version comparison would always mismatch. Trigger signatures filter migration's own `__teable_mig_delta_` triggers AND tgisinternal.
**Decisive source:** :7627-7631 hash design comment: "行内容指纹：sum(每行 64 位哈希) 与行序无关（多重集哈希；不用 xor，成对重复行会互相抵消）。时间类列的 ::text 渲染依赖会话 GUC（TimeZone/DateStyle），改用 extract(epoch ...) 消除两侧差异；数组类型套不了 epoch，退回 ::text。每列用 N/V<length>:<value> 编码… 用 || 操作符拼接而非 concat_ws：宽表列数会撞上函数 100 参数上限（FUNC_MAX_ARGS）" — expression = `'x'||substr(md5(col-expr),1,16)::bit(64)::bigint` summed.
**Flow/Invariant:** content hashing is enabled ONLY when column signatures already match (`!columnMismatch && sourceColumns?.length`, :7110-7113) or the target-side query would reference nonexistent columns; kill-switch env BYODB_SPACE_DATA_DB_VALIDATION_CONTENT_HASH accepts 0/false/off. Shared tables validate scope-count parity PLUS out-of-scope target rows must be ZERO (NOT(related-scope) count > 0 = failure). Validation window brackets itself with delta seq reads: if source changed mid-validation without a write gate, mismatches downgrade to a warning instead of failing.
**Probe (direct test):** specs 'fails validation when content hashes differ despite equal row counts' (:3580), 'renders time-based columns via extract(epoch) in the content hash query' (:3621), 'uses unambiguous length-prefixed column encoding for content hashes' (:3657), 'accepts restored table columns when dropped source columns leave ordinal gaps' (:3776), 'allows existing related target rows during repair validation' (:3830); live: `grep -c 'bit(64)::bigint' apps/nestjs-backend/src/features/space/space-data-db-migration.service.ts` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"contentHashRowExpression","limit":4}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt signature+multiset-hash verification wholesale; every normalization rule here exists because of a real cross-version/cross-session divergence.
