<!-- capsule-v2 -->
**Source:** teable `record-history-cold.processor.ts` chainCatchupFlush @ pin `06a4461e`
**Question:** How do you chain catch-up runs without BullMQ id-dedup ending or forking the chain?
**Path/Symbol:** `chainCatchupFlush(job)`, `CATCHUP_JOB_ID_PREFIX = 'record-history-cold-flush-catchup'`, `process()` execution gate
**Signature:** hop = `(job.data.catchupHop ?? 0) + 1`; enqueue `{catchupHop: hop}` with `jobId: prefix-${hop}` (colon-free), `delay: catchupDelayMs`, removeOnComplete/Fail true.
**Decisive source:** :18-22 — "BullMQ accepts ':' in scheduler ids and job NAMES (both proven in prod) but rejects it in CUSTOM job ids ('Custom Id cannot contain :'), so every id passed to queue.add() below must stay colon-free". :134-141 — "The jobId carries the hop number because BullMQ dedups an .add() whose id matches ANY existing job INCLUDING the one currently executing — a fixed id would end the chain at hop one. Unique ids alone would let a daily run spawn a second chain next to a live one (its hop numbering restarts), so before adding we check the queue for any other pending/active catch-up and skip if one exists" (getJobs(['delayed','waiting','active']) filtered by prefix ≠ self).
**Flow/Invariant:** Chaining fires only when `result.budgetExhausted` — "budgetExhausted implies >= maxRows of progress, so the chain can never hot-loop without work". Execution gate in process(): an ENABLED process executes WHATEVER cold job it receives ("per-name gating would let a pod 'complete' a peer's job without running it"); a kill-switched process skips everything so stale jobs cannot outlive a disable (:101-111). Monthly safety sweep: on UTC day 1 the flush ignores bookmarks (`new Date().getUTCDate() === 1`) so a stranded space waits ≤ a month.
**Probe (direct test):** specs: "chains a catch-up job with a colon-free id when the budget is exhausted", "increments the hop id along the chain", "does not start a second chain while one is pending" (FakeColdQueue mirrors both BullMQ rules verbatim); live: `grep -cF 'Custom Id cannot contain :' apps/nestjs-backend/src/features/record-history-cold/record-history-cold.processor.ts` → `1`; `grep -cF "CATCHUP_JOB_ID_PREFIX = 'record-history-cold-flush-catchup'" ...` → `1`; `grep -c '10 4 2 \* \*' ...` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"chainCatchupFlush","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — encodes three non-obvious BullMQ production rules in one seam.
