<!-- capsule-v2 -->
**Source:** teable `external-sort.ts` SortMemoryBudget + drainTo @ pin `06a4461e`
**Question:** Why is the sort memory cap GLOBAL across all bucket sorters, and how do eviction, in-flight writes, and drain freezing interact?
**Path/Symbol:** `SortMemoryBudget {register,unregister,charge,release,trackInflight,enforce}`, `approxColdRowBytes(row)`, `ExternalRowSorter.drainTo`, `settleSpills`, `evict()`, `cleanup()`
**Signature:** budget unit = serialized bytes (`64 + Σ field lengths`); "actual JS heap cost is ~2-3x this". `enforce()` loop: evict largest pendingBytes sorter → if none evictable wait on `Promise.race` of tracked in-flight spill writes → else return (overshoot bounded by one run; never spin).
**Decisive source:** :44-61 — record-major buffer reads keep EVERY bucket of a table live at once; "a per-sorter cap alone puts peak memory at O(#buckets x run size). On the 2026-07-08 cn drain a 21-month table (x4 table concurrency) multiplied that into 2-3GB of heap and a V8 OOM at the 2304MB default cap." Charge stays held until the spill WRITE LANDS ("the rows are still referenced by the in-flight gzip write"); enforce-waits-on-inflight IS the backpressure bounding total memory.
**Flow/Invariant:** `drainTo()` freezes the output set: set `draining=true`, unregister, `await settleSpills()` (allSettled then rethrow first error) BEFORE choosing in-memory vs merge path — "Skipping the settle would let a budget eviction racing this drain leave its rows in a file the merge never sees, and the caller would then delete buffer rows that were never written to a part" (:200-206). `evict()` no-ops once draining. Cross-table isolation: an eviction failure inside enforce() is swallowed (belongs to the evicted sorter's own table, which fails loudly later) — spec "a failed cross-table eviction does not throw into the table that triggered enforce". Flusher aborts feeders on mid-stream failure so dead sorters stop holding/leaking budget charges.
**Probe (direct test):** specs: "shared budget evicts the largest run" (LARGEST spills, not the adder), "drainTo waits for an un-awaited (budget-evicted) spill", "cleanup releases the budget charge"; live: `grep -c '2026-07-08 cn drain' apps/nestjs-backend/src/features/record-history-cold/external-sort.ts` → `1`; `grep -c 'await largest.evict()' ...` → `1`.
**Retrieve:** `echo '{"project":"teable","pattern":"SortMemoryBudget","limit":5}' | codebase-memory-mcp cli search_code`
**Verdict:** adopt — THE memory-bounding contract for concurrent external sorts.
