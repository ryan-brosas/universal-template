---
name: pi-hermes-memory-foundation
description: "Use when building agent persistent memory: SQLite FTS5 memory/session search, atomic Markdown memory with metadata comments, corruption-recovering SQLite manager, cross-process lock coordinator, procedural skill store, standing instructions, and content/secret scanning."
disable-model-invocation: true
---
# Pi-Hermes-Memory: Agent Persistent Memory Foundation

## Use this for
Build a coding-agent persistent-memory extension: token-aware Markdown memory (MEMORY.md/USER.md/failures.md) with metadata comments and atomic conflict-safe writes, an SQLite mirror with FTS5 full-text search over memories and session messages, corruption-recovering database management, a cross-process atomic lock coordinator, procedural skill storage, always-injected standing instructions, and content/secret scanning on every write. Source code and direct tests are ground truth; references carry decisive excerpts and graph retrieval. The repo's direct tests are excluded from the index by design (`fast-pattern`), so probes are named from the on-disk test files but the graph coverage caveat is stated in each capsule.

## Load the matching source dump
- `references/memory-store.md` — the §-delimited Markdown memory store with metadata comments, char limits, FIFO eviction, atomic conflict-safe writes, and frozen system-prompt snapshots.
- `references/sqlite-mirror.md` — idempotent Markdown→SQLite reconciliation with exact identity, orphan pruning, and scope-aware substring replace/remove.
- `references/fts5-search.md` — FTS5 memory/session search with natural-language query normalization, operator passthrough, and LIKE fallback.
- `references/sqlite-native-loader.md` — the better-sqlite3 loader with ABI-mismatch detection and one-shot npm rebuild recovery.
- `references/database-manager.md` — the corruption-recovering SQLite manager: WAL config, integrity checks, legacy-schema migration, and rebuild-or-recreate recovery.
- `references/atomic-lock-coordinator.md` — the cross-process SQLite-backed lock coordinator with token fencing, incarnation probing, stale takeover, and dead-lock GC.
- `references/content-scanner.md` — injection/exfiltration threat-pattern and secret/credential scanning that blocks every memory write.
- `references/session-indexer.md` — JSONL session parsing, incremental size/mtime backfill, and live-session indexing.
- `references/session-anchor-search.md` — the bounded markdown-request JSONL anchor search with scan caps, term scoring, and range merging.
- `references/skill-store.md` — procedural memory as Pi-native skills: create/patch/edit/move/delete with duplicate/similar/shadow guards and legacy migration.
- `references/standing-instructions.md` — the always-injected, hard-budgeted standing-instruction store with loud truncation.
- `references/correction-detector.md` — the two-pass correction detector that triggers an immediate memory save.
- `references/review-transport.md` — the direct in-process LLM completion transport with fresh-auth re-read, auth-rejection retry, and structured operation parsing.
- `references/config-paths.md` — layered config loading with validation and the agent-root/path normalization helpers.
- `references/deferred-task-singleton.md` — startup backfill scheduling that never blocks its trigger, double-runs, or dies at shutdown.
- `references/live-index-coalescing-window.md` — coalesced live-session indexing behind a host-persistence delay window with id-set reconciliation.
- `references/background-review-loop.md` — dual-threshold auto-save loop with evidence floor, single-flight flag, and direct→subprocess transport ladder.
- `references/consolidation-lock-ladder.md` — cross-process consolidation locking: poll-don't-fail, deferred-not-error outcomes, contended-reload short-circuit, heartbeat-decoupled staleness.
- `references/markdown-mutation-lock.md` — canonical-path-keyed SQLite mutex over Markdown memory files with poll-then-throw semantics.
- `references/child-subprocess-transport.md` — `pi -p` child LLM calls via temp-file prompts, file-sentinel cancellation, watchdog tree-kill, and override-failure retry.
- `references/auth-adapter-discovery.md` — static package.json scan that re-admits billing-critical provider auth adapters into sanitized child processes (#94).
- `references/project-identity-resolution.md` — git-worktree-shared project identity without spawning git, plus a legacy-name migration bridge (#120).
- `references/legacy-layout-migration.md` — crash-safe layout upgrades: exact-entry merge for Markdown folders; marker+snapshot+owned-rollback for SQLite generations.
- `references/directive-wall.md` — policy-only vs legacy-inject prompt modes where user-pinned rules ride every session and are structurally unwritable by the model (#121).
- `references/authoritative-file-scanner-hardening.md` — lstat/symlink-refuse/canonical-root validation chain for scanning user-editable state files.
- `references/dual-write-mirror.md` — Markdown-authoritative + SQLite-mirror write choreography: ordering, eviction cleanup, warning-not-error sync failures, observer dedupe.
- `references/structured-tool-schemas.md` — action-split tool schemas and structured list fields rendered server-side so models never hand-write Markdown formatting.
- `references/tool-result-summary-views.md` — shared {summary, expandedText, status} renderer with three-source failure merge and tail-preserving width compaction.
- `references/bounded-tool-output.md` — loud two-level truncation budgeting for tool output with suffix-reserved caps and clamped numeric params.
- `references/shutdown-choreography.md` — registration-order teardown where DB close placement reclaims the WAL and deferred tasks drain under a bounded race.
- `references/lazy-native-binding.md` — deferred better-sqlite3 load with ABI-recovery taxonomy, bun:sqlite shim parity (`VACUUM INTO` backup), and migration open-guard.
- `references/skills-unified-row-model.md` — one identity space where managed store rows and runtime-loaded external skills collide on normalized path keys; sha1 synthetic ids for externals.
- `references/skills-batch-action-result.md` — bulk move/delete where every failure becomes data: retain-on-blocked selection, focus-first-failure, post-batch index refresh.
- `references/skills-modal-input-arbitration.md` — busy latch + three focus areas route every keypress; close-during-async stops renders without leaking the in-flight action.
- `references/skills-command-degradation-ladder.md` — /memory-skills works headless (notify), lying-host (catch → re-collect → read-only list), and interactive (overlay modal).
- `references/memory-lookup-normalization.md` — pasted search-output lines normalized before every match; multi-match refusal with one scoped-failure-copy exception.
- `references/memory-mutation-plan.md` — LLM-authored edit lists validate whole against an unpublished draft, then commit once; plan-level size/shrink gates.
- `references/review-operation-pipeline.md` — "nothing to save" short-circuit, three-tier JSON salvage at parse, strict atomic-vs-best-effort fork at apply.
- `references/memory-search-tool-surface.md` — empty-query/empty-store/no-hit return as structured guidance naming the remedy tool, never thrown.
- `references/message-parts-projection.md` — pure [USER]/[ASSISTANT] transcript projection shared by review and flush windows that stay independent knobs.
- `references/project-store-indirection.md` — `value | null | () => value` refs resolved at event time so project binding can rebind without re-registration.
- `references/session-flush-duality.md` — CORRECTED pass 4: flush is awaited at BOTH compaction (30 s) and shutdown (10 s) — Pi awaits async session_shutdown handlers; the old fire-and-forget contract is gone.
- `references/review-model-auth-resolution.md` — CORRECTED pass 4: auth resolves through the public `getApiKeyAndHeaders` API with rotate-once-on-rejection retry; the `authStorage.reload()` internals reach was removed upstream (#139 contract rewritten).
- `references/fts-trigram-migration.md` — versioned drop-and-recreate of external-content FTS5 tables under BEGIN IMMEDIATE: in-lock completion re-check, 3-attempt busy cap, rollback-preserving errors (pass 4).
- `references/open-integrity-scan-deferral.md` — quick_check moves OFF the open path onto a setTimeout(0) scan with stale-handle guard, feeding the same recovery ladder as at-operation corruption (pass 4).
- `references/fts-trigram-cjk-search.md` — ≤2-char pure-CJK queries bypass the FTS/NL ladder into a scoped LIKE literal search; script+length gated so operators never degrade (pass 4).
- `references/session-index-bloat-guards.md` — tool_result text dropped at parse + 100 KiB head+tail per-message cap with self-describing notice inside the budget (#187) (pass 4).
- `references/watchdog-exit-taxonomy.md` — the detached process-tree killer's exit codes are data: 124 timeout / 143 cancellation / child code otherwise, idempotent SIGTERM→SIGKILL backstop (pass 4).
- `references/target-routing-guidance.md` — one availability-parameterized routing block shared by review/flush/correction prompts so both transports route facts identically (pass 4).
- `references/dual-transport-diagnostics.md` — a background LLM task stays silent on single-path failure but emits ONE actionable warning naming both root causes when direct AND subprocess both die (pass 4).
- `references/child-context-inheritance.md` — child `pi -p` calls inherit session cwd + active provider/model (override wins), while configured extension SOURCES pass through Pi's own `-e` resolver (pass 4).
- `references/stale-ctx-deferral.md` — "extension ctx is stale" mid-consolidation classifies as deferred-not-failed because the successor session repeats the work (#171) (pass 4).
- `references/overflow-grace-window.md` — first-overflow stamps a per-target clock; auto-consolidation defers 180 s with a manual-first error suffix; only successful mutations clear it (pass 4).
- `references/memory-full-error-entries.md` — capacity rejections carry decoded entries + usage so the model can fix-and-retry in one turn; metadata must never leak (#178) (pass 4).
- `references/whole-entry-replacement-guard.md` — replace refuses fragments dropping sibling lines from multi-fact entries, enforced on single AND atomic paths (pass 4).
- `references/recovery-snapshot-budget.md` — recovery-snapshot pruning reserves upcoming bytes pre-write and re-prunes post-publish; newest kept, symlink-refusing, grace-pinned (pass 4).
- `references/mutation-target-roundtrip.md` — search lines carry machine-checkable `[target=…]` labels with URL-encoded scopes; wrong-target mutations answer with `matching_targets`; lookup strips both label shapes (pass 4).
- `references/failure-injection-recency.md` — the prompt's failure block keeps the NEWEST N age-filtered failures newest-first (`slice(-N).reverse()`); zero disables (pass 4).
- `references/multiline-metadata-decode.md` — the entry-metadata strip regex needs the dotAll `s` flag or multiline facts leak their comments (#178 family) (pass 4).
- `references/skill-patch-content-normalization.md` — LLM section-patch payloads coerced/rejected (JSON arrays shaped per section kind, objects and header injection refused) so format drift can never wipe or splice sections (pass 5).
- `references/skill-shadow-write-refusal.md` — global writes the host would load-first are refused PRE-write with both paths named, making silent shadowing impossible to create (#125); project scope exempt (pass 5).
- `references/skill-similarity-gate.md` — ONE Jaccard score table partitions same-intent duplicates (name>0.7 ∧ desc>0.75 → patch) from name collisions (name>0.7 ∧ desc≤0.75 → rename); empty-token Jaccard pinned 0 (pass 5).
- `references/skill-scope-auto-resolution.md` — explicit scope wins; no project forces global; otherwise ≥2 strong signals or ≥1 strong+1 weak regex votes route project, default stays conservatively global (pass 5).
- `references/skill-cross-scope-move.md` — identity gates BEFORE mutation, atomic rename-first, EXDEV copy+unlink fallback whose failed unlink removes the destination copy (rollback outcome named in error) (pass 5).
- `references/skill-migration-sentinel.md` — always-on flat-markdown normalization + sentinel-gated legacy import where only THIS migration's own warning delta holds back the latch, so permanent foreign conflicts can't wedge retry (pass 5).
- `references/skill-modal-sort-grammar.md` — one comparator serves updated/created/name modes with null-last date ladders, ISO-string ordering, fixed G<P<E tail, over path-key-deduped managed+loaded rows (pass 5).
- `references/session-jsonl-strictness-split.md` — the position-citing anchor search fails the WHOLE search citing `path:line` while the ingestion parser skips malformed lines: strictness follows consumer trust, not politeness (pass 5).
- `references/resources-discover-contract.md` — host skill discovery returns `{skillPaths:[extension-global, project?]}` AND is the single mutation point for the SkillStore's project context (#126) (pass 6).
- `references/session-start-choreography.md` — the ordered startup ladder: one-shot best-effort migration latch, diff-gated project rebind through paired indirection callbacks, migrate-before-load ordering, backfill gated on init success (pass 6).
- `references/consolidator-injection-wiring.md` — stores get consolidation as a settable closure; project stores remap `memory→project` at install time so lock keys stay per-target; #135 warn-gate truth table (pass 6).
- `references/overflow-consolidation-outcome-algebra.md` — `"Memory at "` prefix as routing sentinel; five distinct outcome suffixes (grace / retry-ask / reason / reload-fail / freed-nothing); exactly one retry (pass 6).
- `references/interview-turn-injection.md` — command→agent-turn handoff: acknowledge existing profile, then ALWAYS `waitForIdle()` → `sendUserMessage(prompt)` (pass 6).

## Capsule map
- **Markdown memory** — `references/memory-store.md`: §-delimited entries, `<!-- created=…, last=…, project64=… -->` metadata comments, char limits, FIFO eviction, atomic temp+link/rename writes, external-write conflict retry, frozen system-prompt snapshot.
- **SQLite mirror** — `references/sqlite-mirror.md`: exact-identity upsert, scope reconcile with orphan pruning, LIKE-escaped substring replace/remove, exact-content remove.
- **Full-text search** — `references/fts5-search.md`: FTS5 MATCH subquery, natural-language normalization, operator passthrough, OR fallback, LIKE fallback for CJK, `isFts5QueryError` recovery.
- **Native loader** — `references/sqlite-native-loader.md`: lazy better-sqlite3 load, ABI/dlopen mismatch detection, one npm rebuild, actionable error.
- **Database manager** — `references/database-manager.md`: lazy Bun/better-sqlite3 ctor, WAL + busy_timeout + FK config, quick_check integrity, legacy migration, rebuild-or-recreate with row salvage and backup retention.
- **Locking** — `references/atomic-lock-coordinator.md`: SQLite `BEGIN IMMEDIATE` lock rows, token fencing, pid+incarnation liveness, stale takeover, heartbeat renew, dead-lock GC, shared coordinator.
- **Security** — `references/content-scanner.md`: invisible-unicode, threat-pattern, and secret/credential regex blocking; `scanSecrets` non-blocking variant.
- **Session indexing** — `references/session-indexer.md`: JSONL parse (skip thinking/tool_use, extract tool calls), incremental size/mtime backfill newest-first, live-session index, `needsBackfill`.
- **Anchor search** — `references/session-anchor-search.md`: markdown request parse (from/to/cwd/all/any/exclude), scan caps, term scoring, adjacent-range merge.
- **Procedural skills** — `references/skill-store.md`: slugify + frontmatter, duplicate/similar/name-collision/shadow guards, section patch with JSON-array coercion, legacy migration sentinel.
- **Standing instructions** — `references/standing-instructions.md`: always-injected fenced block, entry+char budget, loud truncation, user-only provenance.
- **Correction detection** — `references/correction-detector.md`: strong/weak/negative pattern filter with directive-word gating, rate-limited immediate save.
- **LLM transport** — `references/review-transport.md`: direct completion with fresh-auth re-read, auth-rejection retry-once, structured operation parse, atomic-shrink application.
- **Config & paths** — `references/config-paths.md`: defaults-override merge with per-field validation, overflow-strategy/transport/variant enums, agent-root + safe path normalization.
- **Deferred startup work** — `references/deferred-task-singleton.md`: singleton {inProgress,promise}, setTimeout(0) deferral, 50-file cap, timestamp-advance-only-on-complete, race-not-cancel shutdown wait.
- **Live index coalescing** — `references/live-index-coalescing-window.md`: 50 ms post-message_end window absorbing duplicate events, all-missing id-set sync under withCorruptionRecovery.
- **Session flush** — `references/session-flush-duality.md`: min-turns gate, snapshot+parts prompt, flush awaited at BOTH compaction (30 s) and shutdown (10 s) — Pi awaits async session_shutdown handlers; direct→subprocess ladder.
- **Background review** — `references/background-review-loop.md`: OR-ed turn/tool-call thresholds, ≥3-user-turn floor, ≥4-parts evidence gate, counters reset at trigger, onReviewSettled test seam.
- **Consolidation locking** — `references/consolidation-lock-ladder.md`: target+storage-hash lock keys, 45 s stale ≠ 180 s runtime via heartbeat, deferred outcome, contended-reload short-circuit.
- **Markdown mutation mutex** — `references/markdown-mutation-lock.md`: canonical-path keys over one shared coordinator DB two levels up; 5 s poll then throw.
- **Child subprocess transport** — `references/child-subprocess-transport.md`: @temp-file prompts (0600), cancel-file sentinel IPC, detached-group watchdog kill, override-failure text-gated single retry.
- **Auth-adapter discovery** — `references/auth-adapter-discovery.md`: naming-convention + exact-set package scan feeding `-e` admission lists for `--no-extensions` children (#94).
- **Project identity** — `references/project-identity-resolution.md`: pure-fs repo-root walk through .git files/commondir, repo-basename identity (#120), exists-check migration bridge.
- **Legacy layout migration** — `references/legacy-layout-migration.md`: non-destructive entry merge for project dirs; pending-marker + BEGIN IMMEDIATE pin + integrity-checked snapshot + dev/ino-owned rollback for sessions.db generations.
- **Directive wall** — `references/directive-wall.md`: policy-only vs legacy-inject modes, standing block last-in-every-mode (#121), slash-command-only writer as structural provenance enforcement.
- **File-scanner hardening** — `references/authoritative-file-scanner-hardening.md`: safe-name containment checks, lstat symlink-refusal, realpath parent-equality, ENOENT-vs-propagate split.
- **Dual-write mirror** — `references/dual-write-mirror.md`: Markdown-first ordering, eviction cleanup before add-sync, warning-not-error mirror failures, WeakSet observer dedupe, full-scope reconcile backstop.
- **Structured tool schemas** — `references/structured-tool-schemas.md`: action-split memory tools, skill_manage structured fields rendered to canonical sections, exactly-one-field patch inference, instructive errors.
- **Tool result views** — `references/tool-result-summary-views.md`: three-source status merge, control-char sanitization, tail-preserving failure truncation, outcome-vocabulary summaries.
- **Output budgeting** — `references/bounded-tool-output.md`: per-snippet + whole-output caps with size-and-remedy markers, suffix-reserved exact caps, clamped limit/snippetChars params.
- **Shutdown choreography** — `references/shutdown-choreography.md`: registration-order teardown contract, final metadata sync preventing restart re-parses, bounded Promise.all drain, close-placement WAL truncation.
- **Lazy native binding** — `references/lazy-native-binding.md`: first-use native load (#117), bun:sqlite shim parity incl. pragma shape + VACUUM INTO backup, migration open-guard fail-closed.
- **Skills manager rows** — `references/skills-unified-row-model.md`: managed+external merge on normalized path keys, sha1 synthetic external ids, recency/name sort ladder with category tiebreaks.
- **Skills bulk actions** — `references/skills-batch-action-result.md`: per-item catch → {successes, unchanged, blocked}, retain-on-failure selection, focus-first-blocked, post-batch `loadIndex()` refresh.
- **Skills modal keys** — `references/skills-modal-input-arbitration.md`: closed→busy→pending-confirm→focus-area guard order, close-during-async render gate, clone-commit filter drafts, printable-to-search redirect.
- **Skills UI degradation** — `references/skills-command-degradation-ladder.md`: callable-check headless gate, throwing-inventory tolerance, failed-mount catch re-collects and downgrades to the shared static list.
- **Memory lookup grammar** — `references/memory-lookup-normalization.md`: search-line prefix strip + doubled-label dedupe, 0/1/many match ladder, scoped failure copies as the only allowed ambiguity.
- **Memory mutation plan** — `references/memory-mutation-plan.md`: draft-scoped duplicate detection, whole-plan validation before single publish, charLimit + requireShrink gates.
- **Review model & auth** — `references/review-model-auth-resolution.md`: three-tier exact-match override with ambiguity refusal, thinking-off-with-override default, public-API credential resolution with rotate-once-on-rejection retry (#139 contract rewritten upstream).
- **Review ops pipeline** — `references/review-operation-pipeline.md`: decline short-circuit guarded by `{`-containment, raw→fenced→brace-slice JSON salvage, atomic (validate-all → applyMutationPlan) vs best-effort apply.
- **memory_search surface** — `references/memory-search-tool-surface.md`: validate → empty-store probe → guided no-hit, remedy-naming messages, structured envelope on every path.
- **Transcript projection** — `references/message-parts-projection.md`: pure role-prefixed lines, 0-means-unlimited window sentinel, review vs flush knobs decoupled by test.
- **Project refs** — `references/project-store-indirection.md`: tri-state value/null/closure refs resolved at event time; trimmed-empty names normalize to null scope.
- **FTS trigram migration** — `references/fts-trigram-migration.md`: versioned marker + physical re-check, BEGIN IMMEDIATE with busy-attempt cap, concurrent-migrator convergence.
- **Startup integrity deferral** — `references/open-integrity-scan-deferral.md`: post-open quick_check macrotask, stale-handle skip, recovery-ladder reuse, fresh scan per reopen.
- **Short-CJK fallback** — `references/fts-trigram-cjk-search.md`: ≤2-char Han/Kana/Hangul queries route to scoped escaped LIKE after FTS misses.
- **Index bloat guards** — `references/session-index-bloat-guards.md`: parse-time tool_result exclusion + 100 KiB head+tail cap (#187).
- **Watchdog exit codes** — `references/watchdog-exit-taxonomy.md`: 124/143/child-code taxonomy over idempotent group-kill with cancel-file polling.
- **Target routing block** — `references/target-routing-guidance.md`: shared 4-bullet routing guidance, conditional project bullet, injected at five prompt sites.
- **Dual-transport diagnostics** — `references/dual-transport-diagnostics.md`: silent single-path failure, one both-transports warning (300-char halves + remedy hint) on total failure.
- **Child context inheritance** — `references/child-context-inheritance.md`: cwd/model/signal inheritance with override precedence; configured `-e` sources forwarded to Pi's resolver.
- **Stale-ctx deferral** — `references/stale-ctx-deferral.md`: session-replacement during consolidation ⇒ deferred outcome via the contention channel; warning gate configurable.
- **Overflow grace window** — `references/overflow-grace-window.md`: first-overflow stamp, 180 s manual-first deferral suffix, markMutation-cleared on success.
- **Full-error entries envelope** — `references/memory-full-error-entries.md`: capacity errors carry target/usage/entry_count/decoded entries as a same-turn work order (#178).
- **Whole-entry replace guard** — `references/whole-entry-replacement-guard.md`: sibling-line preservation gate on replaceUnlocked AND applyMutationPlan.
- **Recovery snapshot budget** — `references/recovery-snapshot-budget.md`: two-phase prune with upcoming-bytes reservation, newest-always-kept, lstat symlink refusal.
- **Mutation-target round-trip** — `references/mutation-target-roundtrip.md`: display-target === mutation-target contract; wrong-target hints as data; paste-strip ladder both shapes.
- **Failure injection recency** — `references/failure-injection-recency.md`: newest-N newest-first slice, zero-means-off.
- **Multiline metadata decode** — `references/multiline-metadata-decode.md`: dotAll flag requirement for the end-anchored metadata strip pattern.
- **Patch normalization** — `references/skill-patch-content-normalization.md`: JSON-array coercion per section kind, object/header-injection refusal, marker-stripping idempotence.
- **Shadow write refusal** — `references/skill-shadow-write-refusal.md`: host-root load-precedence guard refusing global creates Pi would silently shadow (#125), same-root misconfig degrades to null.
- **Similarity gate** — `references/skill-similarity-gate.md`: shared Jaccard table → similar(patch) vs name-collision(rename) partition at 0.7/0.75, stop-worded tokens, best-match-first errors.
- **Scope auto-resolution** — `references/skill-scope-auto-resolution.md`: strong/weak regex vote (≥2 strong or 1+1) over name/description/body with conservative-global default.
- **Cross-scope move** — `references/skill-cross-scope-move.md`: pre-mutation identity gates, rename-first atomicity, EXDEV copy fallback with destination-removing rollback, empty-parent pruning bounded by scope root.
- **Migration sentinel** — `references/skill-migration-sentinel.md`: two-tier migration (always-on flat normalize + sentinel-gated legacy import), warnings-delta latch decoupling permanent foreign conflicts from retry.
- **Modal sort grammar** — `references/skill-modal-sort-grammar.md`: three-mode comparator with dateless-last ladders and G<P<E tail over path-key-deduped unified rows; sha1 external ids.
- **JSONL strictness split** — `references/session-jsonl-strictness-split.md`: anchor search aborts citing path:line vs ingestion parser skips; carry-forward session identity on both readers.
- **Resource discovery** — `references/resources-discover-contract.md`: `resources_discover` → `{skillPaths:[global, project?]}`, discovery-as-rebind side effect, unconditional extension-global contribution (#126).
- **Session-start ladder** — `references/session-start-choreography.md`: persistenceInitialized latch → diff-gated project rebind (paired configure callbacks) → skill migrate/roots/load order → latch-gated backfill.
- **Consolidator injection** — `references/consolidator-injection-wiring.md`: setter-injected closure, project `memory→project` toolTarget remap, deferred-info/failure-warn gates (#135).
- **Overflow outcome algebra** — `references/overflow-consolidation-outcome-algebra.md`: `"Memory at "` sentinel prefix, five outcome suffixes, single bounded retry (#144/#135 consumer side).
- **Turn injection** — `references/interview-turn-injection.md`: idle-wait then user-message send, always-send with additive preamble.

## Extending the foundation
Add one `references/<seam>.md` capsule for one graph-selected, source-confirmed porting question. Add one matching loader line and map entry; keep evidence in the capsule, not this leaf. Each new capsule must carry Path/Symbol, Signature, Data Shape, a labelled decisive source excerpt, Flow, Invariant, a direct-test Probe, and a `search_graph` Retrieve.

## Provenance
pi-hermes-memory (MIT, `main@71beae8a`); Codebase Memory project `pi-hermes-memory` (re-indexed full-tree: 1,960 nodes / 5,606 edges, branch main @ `71beae8a`, head==base, parse_partial = docs/mermaid diagrams only). Pass 1 pre-drain squeeze ran on the earlier 1,049-node fast index; pass 2 mined the never-cited handlers/tools/migration plane; pass 3 mined the symbol-level residue. Pass 4: drift re-entry 26f0acaa → 71beae8a (+27 upstream commits) — re-pinned + re-indexed in place, mined the whole drift plane into 13 capsules and CORRECTED two existing ones against source. **Pass 5 (this entry): zero-drift depth re-entry at UNCHANGED pin `main@71beae8a53be2cdc4901744cf85bd65a1b3030e6` (origin fetched, rev-list HEAD..origin/main = 0; graph live ready 1,960n/5,606e head==base==pin, generation_matches=true, no stale twin)** — whole-file re-reads of the two thinnest big files (`skill-store.ts` 950L had ONE capsule, `session-anchor-search.ts` 472L one) plus `skills-command.ts` :87–385 row-model region → +8 capsule-v2 (59→67; ERRATUM: commit fd2eef96 message miscounts this as "51->59" — parity is authoritative): patch-content normalization ladder, Pi-shadow write refusal (#125 contract), similarity-gate 2×2 partition, scope auto-resolution vote, cross-scope move rollback ladder, migration sentinel warnings-delta latch, modal sort grammar over unified rows, JSONL strictness divergence (anchor-search loud-fail vs parser skip, verified against `session-parser.ts:110-116`). All 7 new Retrieve queries live-executed rank-exact pre-write; coverage stdin-JSON ×5 cited paths no_recorded_issue + metadata_match + generation_matches=true; probes named from on-disk test files (`tests/` remains outside graph coverage; corrected at pass 6 — the pass-4 FULL re-index covers `tests/` too, so probes are now also graph-covered paths). **Pass 6 (this entry): zero-drift deepen/re-mine at UNCHANGED pin `main@71beae8a53be2cdc4901744cf85bd65a1b3030e6` (checkout HEAD verified equal; rev-list HEAD..origin/main = 0; graph ready 1,960n/5,606e, generation 2026-08-24T14:05:19Z, generation_matches=true)** — first work-recorded pass (prior passes predate the DSH work record; ledger row repaired from stale 0). Whole-file internalization of `src/index.ts` (350 L composition root) + whole-reads of the four "UI-only" omission candidates → +5 capsule-v2 (67→72): resources-discover-contract (#126 discovery-as-rebind), session-start-choreography (latch + diff-gated rebind + backfill gating), consolidator-injection-wiring (memory→project remap + #135 truth table), overflow-consolidation-outcome-algebra (#144/#135 consumer suffixes over `"Memory at "` sentinel), interview-turn-injection (waitForIdle→sendUserMessage); interview.ts REMOVED from the UI-only omission list, insights/learn-memory/switch-project/index-sessions verified display-only against source. Pre-write live test battery via repo runner (`npx tsx --test`): resources-discover 3, project-rebinding 1, interview 6, memory-store 91, auto-consolidate 35 = 136 passed / 0 failed; coverage stdin-check ×10 cited paths no_recorded_issue + metadata_match + generation_matches=true. Work record: inspo/pi-hermes-memory-work/{state,research,verification}.md.

## Full view (memory graph)
Revalidate `pi-hermes-memory` before porting: run `index_status`, `check_index_coverage`, `search_graph`, `trace_path`, and `get_code_snippet`. Record the graph root, branch, commit, mode, node/edge counts, freshness, and any coverage caveats; source and direct tests decide shipped claims. The current index is FULL-TREE (only `.claude`/`.git` dirs and image files excluded by design; 4 mermaid diagram files carry parse_partial ranges), so every cited source path IS graph-covered and direct-test probes are named from the on-disk `tests/` tree.

## Boundaries
Adopt the Markdown memory contract, the SQLite mirror + FTS5 search, the corruption-recovering DatabaseManager, the AtomicLockCoordinator, content/secret scanning, session indexing/anchor search, the SkillStore, StandingInstructions, correction detection, and the review transport — PLUS the pass-2 plane: deferred-task scheduling, session flush/review/consolidation orchestration, the child subprocess transport with auth-adapter admission, project identity resolution, crash-safe legacy migrations, the directive-wall injection architecture, file-scanner hardening, dual-write mirror choreography, structured tool schemas, shared result views, output budgeting, shutdown choreography, and lazy native binding — PLUS the pass-3 plane: the interactive skills-manager surface (unified rows, batch-action protocol, modal input arbitration, degradation ladder), memory lookup normalization + atomic mutation plans, review model/auth/operation pipeline depth, the memory_search tool surface, transcript projection windows, and project-store indirection — PLUS the pass-4 drift plane: versioned FTS tokenizer migrations, deferred startup integrity scans, short-CJK literal fallbacks, session-index bloat guards, watchdog exit-code taxonomy, shared target-routing prompt guidance, both-transports failure diagnostics, child context inheritance, stale-ctx deferral classification, overflow grace windows, full-error entries envelopes, whole-entry replacement guards, recovery-snapshot budgeting, mutation-target round-trip labeling, failure-injection recency, and multiline metadata decoding — PLUS the pass-5 depth plane: LLM patch-content normalization ladders, host-shadow write refusal as a structural (not detective) guard, single-score-table duplicate/collision partitions, regex-vote scope routing, cross-scope moves with rollback-outcome-naming errors, warnings-delta migration latches, dateless-safe three-mode list comparators over path-key-deduped unified rows, and consumer-trust JSONL strictness splits — PLUS the pass-6 composition-root plane: host resource discovery as the skill-store rebind point (#126), the ordered session-start ladder with its one-shot persistence latch and diff-gated project rebind, setter-injected consolidators with the project `memory→project` tool-target remap and #135 warn-gate truth table, the overflow→consolidation outcome algebra with its `"Memory at "` sentinel prefix and five distinct suffixes (#144/#135 consumer side), and idle-wait user-message turn injection. Adapt the config keys (incl. `overflowGraceMs`, `autoConsolidationWarnOnFailure`), default paths, threat/secret regex lists, correction pattern lists, TUI keymap letters, and provider/model resolution to the host. Omit only: Pi extension wiring proper (`index.ts` command registration shells beyond their load-bearing ordering invariants captured in shutdown-choreography/directive-wall/session-start-choreography), UI-only command bodies (`insights.ts`, `learn-memory.ts`, `switch-project.ts`, `index-sessions.ts` — display formatting over already-mined stores; all four source-verified display-only at pass 6, interview.ts carved out because its turn-injection mechanic is now mined), SkillsManagerModal render-string assembly (frame drawing over the mined input/state machines), constants.ts prompt packs beyond the routing block, the deprecated no-op `inheritedExtensionArgs` helper, and the extension-root migration ONLY as a Pi-specific path story (its mechanics ARE mined in legacy-layout-migration.md).
