<!-- capsule-v2 -->
**Source:** dub remap-discount-codes cron @ pin `29df217a29`
**Question:** How do partner discount codes migrate to the new group's discount without leaking provider-side coupons?
**Path/Symbol:** `apps/web/app/(ee)/api/cron/groups/remap-discount-codes/route.ts` (POST via `withCron`); `isDiscountEquivalent`; `deleteDiscountCodes`; `createDiscountCode`
**Signature:** input {programId, groupId, partnerIds, isGroupDeleted?}; per-partner discountCodes flattened program-wide.
**Data Shape:** oldDiscount read from `programEnrollments[0]?.discountCodes[0]?.discount` BEFORE any mutation (:46) — needed for the deletion tail even though enrollments may be re-shaped.
**Decisive source:** :73-84 partition by `isDiscountEquivalent(group.discount, code.discount)` — equivalent ⇒ re-point `discountId` via updateMany (:87-102); not equivalent ⇒ deleteDiscountCodes (provider-side coupon destroyed). :113-183 auto-provision tail: when `group.discount.autoProvisionEnabledAt`, default links lacking a code get createDiscountCode per link; provider errors `INTEGRATION_NOT_AVAILABLE | AUTH_EXPIRED | PERMISSIONS_REQUIRED | COUPON_NOT_FOUND` break the loop with a warn (skip remaining), ANYTHING ELSE rethrows :166-180. :186-202 isGroupDeleted tail: when zero discountCodes still reference the OLD discount, `discount.deleteMany({id: oldDiscount.id})` — completing the deferred GC that group DELETE deliberately skipped.
**Flow/Invariant:** This route is the ONLY place the dying group's discount can be deleted; ordering matters because movePartnersToGroup's fan-out needs the discount alive during remap.
**Probe (direct test):** from repo root: `grep -c 'isDiscountEquivalent' apps/web/app/(ee)/api/cron/groups/remap-discount-codes/route.ts` → `2`; `grep -c 'providerCode ===' apps/web/app/(ee)/api/cron/groups/remap-discount-codes/route.ts` → `4`; `grep -n 'remainingDiscountCodes === 0' apps/web/app/(ee)/api/cron/groups/remap-discount-codes/route.ts | cut -d: -f1` → `192`.
**Retrieve:** `echo '{"project":"mnt-hdd-utopia-inspo-platforms-dub","query":"isDiscountEquivalent","limit":5}' | codebase-memory-mcp cli search_graph` → rank#1 `lib/discounts/is-discount-equivalent.ts:isDiscountEquivalent` :8-36, plus the route Module/POST nodes at `[groupIdOrSlug]`… `remap-discount-codes/route.ts` 1-206 / 21-205.
**Verdict:** adopt — equivalence repartition + fail-open provider-error classes + final old-discount GC close the deletion handoff.
