---
name: fixture-good
description: clean fixture for the selftest
---

Run the gate before you commit. The command prints one line per check, and a non-zero exit means the change is not done.

Use the thread ID, not the comment ID.

Allowed:
Rejected:

- read the failing output first
- fix the smallest cause
- rerun the same command
