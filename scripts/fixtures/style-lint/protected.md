---
name: fixture-protected
description: protected spans must never trigger the linter
---

The config sample says `run actually fast` and mentions `really` in inline code.

```bash
echo "this actually works really well — utilize it and delve in"
```

> The mentor said: hold the boundary — not the feeling.

See https://example.com/really/actually-utilize for details.

